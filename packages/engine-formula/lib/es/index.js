var oc = Object.defineProperty;
var uc = (o, i, e) => i in o ? oc(o, i, { enumerable: !0, configurable: !0, writable: !0, value: e }) : o[i] = e;
var A = (o, i, e) => uc(o, typeof i != "symbol" ? i + "" : i, e);
import { ObjectMatrix as Ce, CommandType as Fe, RANGE_TYPE as et, AbsoluteRefType as Me, Tools as Ke, createIdentifier as ze, Disposable as Ve, IUniverInstanceService as Gn, LRUMap as cc, hashAlgorithm as lc, moveRangeByOffset as da, isValidRange as fc, isFormulaString as ct, isFormulaId as Ct, Rectangle as ii, cellToRange as hc, Inject as ye, UniverInstanceType as gr, BooleanNumber as io, LocaleService as mc, LocaleType as gt, numfmt as Et, BuildTextUtils as gc, isRealNum as q, getNumfmtParseValueFilter as dc, CellValueType as wt, isTextFormat as Cc, RichTextBuilder as _c, toDisposable as Ac, Injector as oo, generateRandomId as Ec, sortRules as bc, RTree as Ps, IConfigService as Ca, AsyncLock as yc, requestImmediateMacroTask as oi, ICommandService as cr, DataStreamTreeTokenType as Te, DisposableCollection as Rc, Optional as pc, Plugin as Nc, merge as Vc, touchDependencies as hs } from "@univerjs/core";
import { Subject as dr, BehaviorSubject as uo, distinctUntilChanged as co, combineLatest as Oc, map as wc, Observable as Sc, shareReplay as Mc } from "rxjs";
import { DataSyncPrimaryController as Dc } from "@univerjs/rpc";
var ar = /* @__PURE__ */ ((o) => (o.FALSE = "FALSE", o.TRUE = "TRUE", o))(ar || {}), Tt = /* @__PURE__ */ ((o) => (o[o.SUCCESS = 0] = "SUCCESS", o[o.ERROR = 1] = "ERROR", o))(Tt || {}), lt = /* @__PURE__ */ ((o) => (o[o.FRONT = 0] = "FRONT", o[o.BACK = 1] = "BACK", o))(lt || {});
function Lc(o, i, e, t, r) {
  for (let n = 0, s = o.length; n < s; n++) {
    const a = o[n];
    if (i !== a.unitId || e !== a.sheetId)
      continue;
    const { startRow: u, startColumn: c, endRow: l, endColumn: f } = a.range;
    if (t >= u && t <= l && r >= c && r <= f)
      return !0;
  }
  return !1;
}
var m = /* @__PURE__ */ ((o) => (o.DIV_BY_ZERO = "#DIV/0!", o.NAME = "#NAME?", o.VALUE = "#VALUE!", o.NUM = "#NUM!", o.NA = "#N/A", o.CYCLE = "#CYCLE!", o.REF = "#REF!", o.SPILL = "#SPILL!", o.CALC = "#CALC!", o.ERROR = "#ERROR!", o.CONNECT = "#GETTING_DATA", o.NULL = "#NULL!", o))(m || {});
const lr = new Set(Object.values(m)), ui = [...new Set(Object.values(m).map((o) => o.length))];
var xc = /* @__PURE__ */ ((o) => (o[o.Financial = 0] = "Financial", o[o.Date = 1] = "Date", o[o.Math = 2] = "Math", o[o.Statistical = 3] = "Statistical", o[o.Lookup = 4] = "Lookup", o[o.Database = 5] = "Database", o[o.Text = 6] = "Text", o[o.Logical = 7] = "Logical", o[o.Information = 8] = "Information", o[o.Engineering = 9] = "Engineering", o[o.Cube = 10] = "Cube", o[o.Compatibility = 11] = "Compatibility", o[o.Web = 12] = "Web", o[o.Array = 13] = "Array", o[o.Univer = 14] = "Univer", o[o.User = 15] = "User", o[o.DefinedName = 16] = "DefinedName", o))(xc || {}), X = /* @__PURE__ */ ((o) => (o.PLUS = "+", o.MINUS = "-", o.MULTIPLY = "*", o.DIVIDED = "/", o.CONCATENATE = "&", o.POWER = "^", o.EQUALS = "=", o.NOT_EQUAL = "<>", o.GREATER_THAN = ">", o.GREATER_THAN_OR_EQUAL = ">=", o.LESS_THAN = "<", o.LESS_THAN_OR_EQUAL = "<=", o))(X || {}), x = /* @__PURE__ */ ((o) => (o.EQUALS = "=", o.NOT_EQUAL = "<>", o.GREATER_THAN = ">", o.GREATER_THAN_OR_EQUAL = ">=", o.LESS_THAN = "<", o.LESS_THAN_OR_EQUAL = "<=", o))(x || {});
const cn = /* @__PURE__ */ new Map([
  ["<>", 4],
  ["<", 4],
  [">=", 4],
  ["=", 4],
  [">", 4],
  ["<=", 4],
  ["&", 3],
  ["+", 2],
  ["-", 2],
  ["/", 1],
  ["*", 1],
  ["^", 0]
  // ^
]), Ut = new Set(cn.keys()), Pc = /* @__PURE__ */ new Set([
  "=",
  "<>",
  ">",
  ">=",
  "<",
  "<="
  /* LESS_THAN_OR_EQUAL */
]);
var K = /* @__PURE__ */ ((o) => (o.OPEN_BRACKET = "(", o.CLOSE_BRACKET = ")", o.COMMA = ",", o.SINGLE_QUOTATION = "'", o.DOUBLE_QUOTATION = '"', o.OPEN_BRACES = "{", o.CLOSE_BRACES = "}", o.COLON = ":", o.OPEN_SQUARE_BRACKET = "[", o.CLOSE_SQUARE_BRACKET = "]", o))(K || {}), tt = /* @__PURE__ */ ((o) => (o.PERCENTAGE = "%", o.POUND = "#", o))(tt || {});
const ms = /* @__PURE__ */ new Set([
  "%",
  "#"
  /* POUND */
]);
var we = /* @__PURE__ */ ((o) => (o.AT = "@", o.MINUS = "-", o.PLUS = "+", o))(we || {});
const jc = " ", lo = [
  ...Object.values(x),
  ...Object.values(X),
  ...Object.values(K),
  ...Object.values(tt),
  ...Object.values(we)
];
function ln(o) {
  return lo.includes(o);
}
function Uc(o) {
  for (const i of lo)
    if (o.indexOf(i) > -1)
      return !0;
  return !1;
}
function oy(o) {
  return o[0] === "'" && o[o.length - 1] === "'" ? o.substring(1, o.length - 1) : o;
}
function uy(o) {
  return ln(o) && o !== K.CLOSE_BRACES && o !== K.CLOSE_BRACKET && o !== K.SINGLE_QUOTATION && o !== K.DOUBLE_QUOTATION || o === " ";
}
const vc = /* @__PURE__ */ new Set([
  X.PLUS,
  X.MINUS,
  X.MULTIPLY,
  X.DIVIDED,
  X.CONCATENATE,
  X.POWER,
  X.EQUALS,
  X.NOT_EQUAL,
  X.GREATER_THAN,
  X.GREATER_THAN_OR_EQUAL,
  X.LESS_THAN,
  X.LESS_THAN_OR_EQUAL,
  K.OPEN_BRACKET,
  K.COMMA,
  K.COLON,
  K.OPEN_BRACES,
  K.OPEN_SQUARE_BRACKET
]);
function Tc(o) {
  return vc.has(o);
}
const Bc = /* @__PURE__ */ new Set([
  X.PLUS,
  X.MINUS,
  X.MULTIPLY,
  X.DIVIDED,
  X.CONCATENATE,
  X.POWER,
  X.EQUALS,
  X.NOT_EQUAL,
  X.GREATER_THAN,
  X.GREATER_THAN_OR_EQUAL,
  X.LESS_THAN,
  X.LESS_THAN_OR_EQUAL,
  K.OPEN_BRACKET,
  K.COMMA,
  K.COLON,
  K.OPEN_BRACES,
  K.OPEN_SQUARE_BRACKET,
  tt.PERCENTAGE,
  tt.POUND
]);
function Ic(o) {
  return Bc.has(o);
}
const fo = '\\[([^\\[\\]\\/?:"<>|*\\\\]+)\\]', ci = new RegExp(fo), Fc = "((?![\\[\\]\\/?*\\\\]).)*!", kt = "$", _a = "\\s*?:\\s*?", En = "[A-Za-z]+", bn = "[1-9][0-9]*", wr = `'?(${fo})?(${Fc})?'?`, js = `\\${kt}?${En}\\${kt}?${bn}`, kc = `^(${we.AT})?${wr}${js}${_a}${js}$`, li = new RegExp(kc), $c = `^${wr}\\s*?${js}(${tt.POUND})?$`, fi = new RegExp($c), Yc = `^${wr}\\${kt}?${bn}${_a}\\${kt}?${bn}$`, hi = new RegExp(Yc), Hc = `^${wr}\\${kt}?${En}${_a}\\${kt}?${En}$`, mi = new RegExp(Hc), Qc = `^${wr}\\s*?\\${kt}?${bn}$`, gi = new RegExp(Qc), Gc = `^${wr}\\s*?\\${kt}?${En}$`, di = new RegExp(Gc), Wc = "{.*?}", Ci = new RegExp(Wc, "g");
function Yr(o) {
  return fi.lastIndex = 0, fi.test(o);
}
function qc(o) {
  return li.lastIndex = 0, li.test(o);
}
function ho(o) {
  return hi.lastIndex = 0, hi.test(o);
}
function mo(o) {
  return mi.lastIndex = 0, mi.test(o);
}
function go(o) {
  return gi.lastIndex = 0, gi.test(o);
}
function Co(o) {
  return di.lastIndex = 0, di.test(o);
}
function Kc(o) {
  return Ci.lastIndex = 0, Ci.test(o);
}
function _o(o) {
  return Yr(o) || qc(o) || ho(o) || mo(o);
}
function Xc(o) {
  const i = {};
  return Object.keys(o).forEach((e) => {
    const t = o[e];
    if (t == null)
      return !0;
    i[e] == null && (i[e] = {}), Object.keys(t).forEach((r) => {
      const n = t[r];
      i[e][r] = new Ce(n);
    });
  }), i;
}
function Zc(o) {
  const i = {};
  for (const e in o) {
    const t = o[e];
    if (t != null) {
      i[e] == null && (i[e] = {});
      for (const r in t) {
        const n = t[r], s = {};
        n.forValue((a, u, c) => {
          s[a] === void 0 && (s[a] = {}), s[a][u] = c;
        }), i[e][r] = s;
      }
    }
  }
  return i;
}
const zc = {
  id: "formula.mutation.register-function",
  type: Fe.MUTATION,
  handler: () => !0
}, Us = {
  id: "formula.mutation.set-array-formula-data",
  type: Fe.MUTATION,
  handler: (o, i) => !0
}, Jc = /[^0-9]/g, el = /[^A-Za-z]/g;
function fn(o) {
  let i = o[0] === "$";
  const e = o.substring(1);
  let t = e.indexOf("$") > -1;
  return Ke.isStringNumber(e) && i && !t && (i = !1, t = !0), i && t ? Me.ALL : i ? Me.COLUMN : t ? Me.ROW : Me.NONE;
}
function cy(o) {
  const i = o.split("!");
  i.length > 1 && (o = i[i.length - 1]);
  const e = o.split(":");
  return e.length > 1 ? {
    startAbsoluteRefType: fn(e[0]),
    endAbsoluteRefType: fn(e[1])
  } : { startAbsoluteRefType: fn(e[0]) };
}
function _i(o = Me.NONE) {
  let i = "", e = "";
  return o === Me.ROW ? i = "$" : o === Me.COLUMN ? e = "$" : o === Me.ALL && (i = "$", e = "$"), {
    rowAbsoluteString: i,
    columnAbsoluteString: e
  };
}
function $t(o) {
  const {
    startColumn: i,
    startRow: e,
    endColumn: t,
    endRow: r,
    startAbsoluteRefType: n,
    endAbsoluteRefType: s,
    rangeType: a = et.NORMAL
  } = o, u = _i(n), c = _i(s);
  if (a === et.ROW || a === et.ALL) {
    const h = `${u.rowAbsoluteString}${e + 1}`, d = `${c.rowAbsoluteString}${r + 1}`;
    return `${h}:${d}`;
  }
  if (a === et.COLUMN) {
    const h = `${u.columnAbsoluteString}${Ke.chatAtABC(i)}`, d = `${c.columnAbsoluteString}${Ke.chatAtABC(t)}`;
    return `${h}:${d}`;
  }
  const l = `${u.columnAbsoluteString}${Ke.chatAtABC(i)}${u.rowAbsoluteString}${e + 1}`, f = `${c.columnAbsoluteString}${Ke.chatAtABC(t)}${c.rowAbsoluteString}${r + 1}`;
  return l === f ? l : `${l}:${f}`;
}
function tl(o, i) {
  return `${Eo(o)}!${$t(i)}`;
}
function rl(o, i, e) {
  return Ts(o) || Ts(i) ? `'[${Bs(o)}]${Bs(i)}'!${$t(e)}` : `[${o}]${i}!${$t(e)}`;
}
function vs(o) {
  const { unitId: i, sheetName: e, range: t } = o;
  return i != null && i.length > 0 && e != null && e.length > 0 ? rl(i, e, t) : e != null && e.length > 0 ? tl(e, t) : $t(t);
}
function gs(o) {
  const i = Number.parseInt(o.replace(Jc, "")) - 1, e = Ke.ABCatNum(o.replace(el, "")), t = fn(o);
  return {
    row: i,
    column: e,
    absoluteRefType: t
  };
}
function Aa(o) {
  const i = ci.exec(o);
  let e = "";
  i != null && (e = i[0].trim(), e = Ai(e.slice(1, e.length - 1)), o = o.replace(ci, ""));
  const t = o.indexOf("!");
  let r = "", n = "";
  return t > -1 ? (r = o.substring(0, t), r[0] === "'" && r[r.length - 1] === "'" && (r = r.substring(1, r.length - 1)), r = Ai(r), n = o.substring(t + 1)) : n = o, {
    refBody: n,
    sheetName: r,
    unitId: e
  };
}
function Ur(o) {
  const { refBody: i, sheetName: e, unitId: t } = Aa(o), r = i.indexOf(":");
  if (r === -1) {
    const _ = gs(i), C = _.row, E = _.column, b = _.absoluteRefType;
    return {
      unitId: t,
      sheetName: e,
      range: {
        startRow: C,
        startColumn: E,
        endRow: C,
        endColumn: E,
        startAbsoluteRefType: b,
        endAbsoluteRefType: b
      }
    };
  }
  const n = i.substring(0, r), s = i.substring(r + 1), a = gs(n), u = gs(s), c = a.row > u.row ? u.row : a.row, l = a.column > u.column ? u.column : a.column, f = a.row > u.row ? a.row : u.row, h = a.column > u.column ? a.column : u.column;
  let d = et.NORMAL;
  return Number.isNaN(c) && Number.isNaN(f) ? d = et.COLUMN : Number.isNaN(l) && Number.isNaN(h) && (d = et.ROW), {
    unitId: t,
    sheetName: e,
    range: {
      startRow: c,
      startColumn: l,
      endRow: f,
      endColumn: h,
      startAbsoluteRefType: a.absoluteRefType,
      endAbsoluteRefType: u.absoluteRefType,
      rangeType: d
    }
  };
}
const nl = ["LOG10"];
function hn(o) {
  const i = Ao(o);
  if (!_o(i) || nl.includes(i.toUpperCase().trim()))
    return !1;
  const { range: e } = Ur(i);
  return !(e.endColumn >= 16384);
}
function Ao(o) {
  const i = [];
  let e = !1;
  for (let t = 0, r = o.length; t < r; t++) {
    const n = o[t];
    if (n === jc && !e)
      i.push(n);
    else {
      if (!e && (n === we.AT || n === we.MINUS || n === we.PLUS))
        continue;
      i.push(n), e = !0;
    }
  }
  return i.join("");
}
function ly(o, i) {
  const e = (o == null ? void 0 : o.split(",")) || [];
  return o === "" || e.length === 0 ? [] : sl(o) ? e.map((n) => {
    const s = Ur(n);
    return {
      unitId: s.unitId,
      sheetId: i(s.sheetName),
      range: s.range,
      sheetName: s.sheetName
    };
  }) : [];
}
function sl(o) {
  return ((o == null ? void 0 : o.split(",")) || []).every((e) => hn(e.trim()));
}
function Ts(o) {
  return o.length === 0 ? !1 : !!(Uc(o) || al(o) || il(o) || ol(o) || /[\s!$%^&*()+\-=\[\]{};':"\\|,.<>\/?（）]/.test(o));
}
function Eo(o) {
  return Ts(o) ? `'${Bs(o)}'` : o;
}
function Bs(o) {
  return o.replace(/'/g, "''");
}
function Ai(o) {
  return o.replace(/''/g, "'");
}
function al(o) {
  const i = o.match(/[1-9][0-9]{0,6}/);
  return /^[A-Z]+[1-9][0-9]{0,6}$/.test(o) && i !== null;
}
function il(o) {
  return /^(R(-?[0-9]+)?C(-?[0-9]+)?|C(-?[0-9]+)?|R(-?[0-9]+)?)$/.test(o);
}
function ol(o) {
  return !new RegExp("^\\p{Letter}", "u").test(o.charAt(0));
}
var ul = Object.getOwnPropertyDescriptor, cl = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? ul(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, ll = (o, i) => (e, t) => i(e, t, o);
let Is = class extends Ve {
  constructor(i) {
    super();
    // 18.2.6 definedNames (Defined Names)
    A(this, "_definedNameMap", {});
    A(this, "_nameCacheMap", {});
    // Cache for name-to-definition mapping
    A(this, "_update$", new dr());
    A(this, "update$", this._update$.asObservable());
    A(this, "_currentRange", {
      unitId: "",
      sheetId: "",
      range: {
        startRow: 0,
        endRow: 0,
        startColumn: 0,
        endColumn: 0
      }
    });
    A(this, "_currentRange$", new dr());
    A(this, "currentRange$", this._currentRange$.asObservable());
    A(this, "_focusRange$", new dr());
    A(this, "focusRange$", this._focusRange$.asObservable());
    this._univerInstanceService = i;
  }
  dispose() {
    super.dispose(), this._definedNameMap = {}, this._nameCacheMap = {}, this._update$.complete(), this._currentRange$.complete(), this._focusRange$.complete();
  }
  getWorksheetByRef(i, e) {
    var r;
    const { sheetName: t } = Aa(e);
    return (r = this._univerInstanceService.getUnit(i)) == null ? void 0 : r.getSheetBySheetName(t);
  }
  focusRange(i, e) {
    const t = this.getValueById(i, e);
    t !== void 0 && this._focusRange$.next({ ...t, unitId: i });
  }
  setCurrentRange(i) {
    this._currentRange = i, this._currentRange$.next(i);
  }
  getCurrentRange() {
    return this._currentRange;
  }
  getCurrentRangeForString() {
    return $t(this._currentRange.range);
  }
  registerDefinedNames(i, e) {
    this._definedNameMap[i] = e, this._updateCache(i), this._update();
  }
  registerDefinedName(i, e) {
    this._definedNameMap[i] === void 0 && (this._definedNameMap[i] = {}), this._definedNameMap[i][e.id] = e, this._updateCache(i), this._update();
  }
  removeDefinedName(i, e) {
    var t;
    (t = this._definedNameMap[i]) == null || delete t[e], this._updateCache(i), this._update();
  }
  removeUnitDefinedName(i) {
    delete this._definedNameMap[i], this._updateCache(i), this._update();
  }
  getDefinedNameMap(i) {
    return this._definedNameMap[i];
  }
  getValueByName(i, e) {
    const t = e.trim(), r = this._nameCacheMap[i];
    if (r && t in r)
      return r[t];
    const n = this._definedNameMap[i];
    if (n === void 0)
      return null;
    let s = null;
    for (const a of Object.values(n))
      if (a.name === t) {
        s = a;
        break;
      }
    return s && (this._nameCacheMap[i] = this._nameCacheMap[i] || {}, this._nameCacheMap[i][t] = s), s;
  }
  getValueById(i, e) {
    var t;
    return (t = this._definedNameMap[i]) == null ? void 0 : t[e];
  }
  hasDefinedName(i) {
    return this._definedNameMap[i] === void 0 ? !1 : (Array.from(Object.values(this._definedNameMap[i])).length || 0) !== 0;
  }
  _update() {
    this._update$.next(null);
  }
  _updateCache(i) {
    const e = this._definedNameMap[i];
    if (e === void 0) {
      delete this._nameCacheMap[i];
      return;
    }
    this._nameCacheMap[i] = {};
    for (const t of Object.values(e))
      this._nameCacheMap[i][t.name] = t;
  }
};
Is = cl([
  ll(0, Gn)
], Is);
const Sr = ze("univer.formula.defined-names.service"), fy = (o, i) => {
  const { unitId: e, id: t } = i;
  return {
    ...o.get(Sr).getValueById(e, t),
    unitId: e
  };
}, Ea = {
  id: "formula.mutation.set-defined-name",
  type: Fe.MUTATION,
  handler: (o, i) => {
    if (i == null)
      return !1;
    const e = o.get(Sr), { id: t, unitId: r, name: n, formulaOrRefString: s, comment: a, hidden: u, localSheetId: c } = i;
    return e.registerDefinedName(r, {
      id: t,
      name: n.trim(),
      formulaOrRefString: s.trim(),
      comment: a == null ? void 0 : a.trim(),
      hidden: u,
      localSheetId: c
    }), !0;
  }
}, bo = {
  id: "formula.mutation.remove-defined-name",
  type: Fe.MUTATION,
  handler: (o, i) => {
    if (i == null)
      return !1;
    const e = o.get(Sr), { unitId: t, id: r } = i;
    return e.removeDefinedName(t, r), !0;
  }
}, ba = {
  id: "formula.mutation.set-feature-calculation",
  type: Fe.MUTATION,
  handler: () => !0
}, ya = {
  id: "formula.mutation.remove-feature-calculation",
  type: Fe.MUTATION,
  handler: () => !0
}, yo = {
  id: "formula.mutation.set-formula-calculation-start",
  type: Fe.MUTATION,
  handler: () => !0
}, Ro = {
  id: "formula.mutation.set-formula-calculation-stop",
  type: Fe.MUTATION,
  handler: () => !0
}, yn = {
  id: "formula.mutation.set-formula-calculation-notification",
  type: Fe.MUTATION,
  handler: () => !0
}, po = {
  id: "formula.mutation.set-formula-calculation-result",
  type: Fe.MUTATION,
  handler: () => !0
}, No = {
  id: "formula.mutation.set-formula-data",
  type: Fe.MUTATION,
  handler: (o, i) => !0
}, Ra = {
  id: "formula.mutation.set-other-formula",
  type: Fe.MUTATION,
  handler: () => !0
}, pa = {
  id: "formula.mutation.remove-other-formula",
  type: Fe.MUTATION,
  handler: () => !0
}, fl = {
  id: "formula.mutation.set-super-table",
  type: Fe.MUTATION,
  handler: () => !0
}, hl = {
  id: "formula.mutation.remove-super-table",
  type: Fe.MUTATION,
  handler: () => !0
}, Vo = {
  id: "formula.mutation.set-super-table-option",
  type: Fe.MUTATION,
  handler: () => !0
};
class Dt {
  constructor(i) {
    A(this, "_cache");
    this._cache = new cc(i);
  }
  set(i, e) {
    const t = this._hash(i);
    this._cache.set(t, e);
  }
  get(i) {
    const e = this._hash(i);
    return this._cache.get(e);
  }
  clear() {
    this._cache.clear();
  }
  delete(i) {
    this._cache.delete(this._hash(i));
  }
  forEach(i, e) {
    this._cache.forEach(i, e);
  }
  _hash(i) {
    return i.length <= 64 ? i : lc(i).toString();
  }
}
const dt = "P_1", It = "R_1", Ar = "L_1", Hr = "LR_1", Oo = "LO_1", ml = "LET", vr = "LAMBDA", gl = "CUBE", dl = /* @__PURE__ */ new Set(["RAND", "RANDBETWEEN", "NOW", "TODAY"]), Fs = new Dt(1e5);
function Er(o) {
  const i = Fs.get(o);
  if (i)
    return i;
  const e = Ur(o);
  return Fs.set(o, e), Ur(o);
}
function Cl() {
  Fs.clear();
}
var Ye = /* @__PURE__ */ ((o) => (o[o.NORMAL = 0] = "NORMAL", o[o.NUMBER = 1] = "NUMBER", o[o.STRING = 2] = "STRING", o[o.FUNCTION = 3] = "FUNCTION", o[o.REFERENCE = 4] = "REFERENCE", o[o.ARRAY = 5] = "ARRAY", o[o.DEFINED_NAME = 6] = "DEFINED_NAME", o))(Ye || {});
function Ei(o) {
  let i = "";
  for (const e of o)
    typeof e == "string" ? i += e : i += e.token;
  return i;
}
class ae {
  constructor() {
    A(this, "_parent");
    A(this, "_token", It);
    A(this, "_children", []);
    A(this, "_lambdaId");
    A(this, "_functionDefinitionPrivacyVar");
    A(this, "_lambdaParameter", "");
    A(this, "_startIndex", -1);
    A(this, "_endIndex", -1);
    A(this, "_definedNames", []);
  }
  dispose() {
    var i;
    this._children.forEach((e) => {
      typeof e != "string" && e.dispose();
    }), (i = this._functionDefinitionPrivacyVar) == null || i.clear(), this._functionDefinitionPrivacyVar = null, this._children = [], this._parent = null, this._definedNames = [];
  }
  getDefinedNames() {
    return this._definedNames;
  }
  getStartIndex() {
    return this._startIndex;
  }
  getLambdaId() {
    return this._lambdaId;
  }
  setLambdaId(i) {
    this._lambdaId = i;
  }
  getFunctionDefinitionPrivacyVar() {
    return this._functionDefinitionPrivacyVar;
  }
  setLambdaPrivacyVar(i) {
    this._functionDefinitionPrivacyVar = i;
  }
  getLambdaParameter() {
    return this._lambdaParameter;
  }
  setLambdaParameter(i) {
    this._lambdaParameter = i;
  }
  getParent() {
    return this._parent;
  }
  setParent(i) {
    this._parent = i;
  }
  getChildren() {
    return this._children;
  }
  setChildren(i) {
    this._children = i;
  }
  addChildren(i) {
    this._children.push(i);
  }
  addChildrenFirst(i) {
    this._children.unshift(i);
  }
  getToken() {
    return this._token;
  }
  setToken(i) {
    this._token = i;
  }
  setIndex(i, e) {
    this._startIndex = i, this._endIndex = e;
  }
  setDefinedNames(i) {
    this._definedNames = i;
  }
  hasDefinedNames() {
    return this._definedNames.length > 0;
  }
  replaceChild(i, e) {
    const t = this._getIndexInParent(i);
    t != null && (this.getChildren().splice(t, 1, e), e.setParent(this));
  }
  changeToParent(i) {
    const e = this.getParent();
    e && e.removeChild(this), this.setParent(i), i.getChildren().push(this);
  }
  removeChild(i) {
    const e = this._getIndexInParent(i);
    e != null && this.getChildren().splice(e, 1);
  }
  serialize() {
    const i = this.getToken(), e = this.getChildren(), t = [], r = e.length;
    for (let n = 0; n < r; n++) {
      const s = e[n];
      s instanceof ae ? t.push(s.serialize()) : t.push(s);
    }
    return {
      token: i,
      st: this._startIndex,
      ed: this._endIndex,
      children: t
    };
  }
  _getIndexInParent(i) {
    const e = this.getChildren(), t = e.length;
    for (let r = 0; r < t; r++)
      if (e[r] === i)
        return r;
  }
}
const wo = 2e3, ds = new Dt(wo), Cs = new Dt(wo);
class Na extends Ve {
  constructor() {
    super(...arguments);
    A(this, "_currentLexerNode", new ae());
    A(this, "_upLevel", 0);
    A(this, "_segment", "");
    A(this, "_bracketState", []);
    // ()
    A(this, "_squareBracketState", 0);
    A(this, "_bracesState", 0);
    // {}
    A(this, "_singleQuotationState", 0);
    // ''
    A(this, "_doubleQuotationState", 0);
    // ""
    A(this, "_lambdaState", !1);
    // Lambda
    A(this, "_colonState", !1);
    // :
    A(this, "_formulaErrorCount", 0);
    A(this, "_tableBracketState", !1);
  }
  // Table3[[#All],[Column1]:[Column2]]
  dispose() {
    this._resetTemp(), this._currentLexerNode.dispose(), ds.clear(), Cs.clear();
  }
  getUpLevel() {
    return this._upLevel;
  }
  isColonClose() {
    return this._colonState === !1;
  }
  isColonOpen() {
    return this._colonState === !0;
  }
  isDoubleQuotationClose() {
    return this._doubleQuotationState === 0;
  }
  isLambdaOpen() {
    return this._lambdaState === !0;
  }
  isLambdaClose() {
    return this._lambdaState === !1;
  }
  isSingleQuotationClose() {
    return this._singleQuotationState === 0;
  }
  isBracesClose() {
    return this._bracesState === 0;
  }
  isBracketClose() {
    return this._bracketState.length === 0;
  }
  isSquareBracketClose() {
    return this._squareBracketState === 0;
  }
  getCurrentLexerNode() {
    return this._currentLexerNode;
  }
  getFunctionAndParameter(e, t) {
    const r = this._getCurrentParamIndex(e, t);
    if (r == null || r === m.VALUE)
      return;
    const n = r[0];
    if (typeof n == "string")
      return;
    let s = n.getParent(), a = n;
    for (; s; ) {
      const u = s.getToken();
      if (u !== dt && !ln(u) && s.getStartIndex() !== -1) {
        const c = s.getChildren().indexOf(a);
        return {
          functionName: u,
          paramIndex: c
        };
      }
      a = s, s = s.getParent();
    }
  }
  /**
   * Estimate the number of right brackets that need to be automatically added to the end of the formula.
   * @param formulaString
   */
  checkIfAddBracket(e) {
    let t = 0, r = e.length - 1, n = e[r];
    for (; (n === K.CLOSE_BRACKET || n === " ") && r >= 0; )
      n === K.CLOSE_BRACKET && t++, n = e[--r];
    const s = this._getCurrentParamIndex(e, e.length - 2);
    if (s == null || s === m.VALUE)
      return 0;
    const a = s[0];
    if (typeof a == "string")
      return 0;
    let u = a.getParent(), c = 0;
    for (s[1] === K.OPEN_BRACKET && c++; u; ) {
      const l = u.getToken();
      l !== dt && l !== K.COLON && u.getStartIndex() !== -1 && l.toUpperCase() !== vr && (t === 0 ? c += 1 : t--), u = u.getParent();
    }
    return c;
  }
  sequenceNodesBuilder(e) {
    const t = Cs.get(e);
    if (t)
      return [...t];
    const r = this._getSequenceArray(e);
    if (r.length === 0)
      return;
    const n = this.getSequenceNode(r);
    return Cs.set(e, [...n]), n;
  }
  convertRefersToAbsolute(e, t, r, n = "") {
    const s = this.sequenceNodesBuilder(e);
    if (s == null)
      return e;
    let a = "";
    e.substring(0, 1) === X.EQUALS && (a = X.EQUALS);
    for (let u = 0, c = s.length; u < c; u++) {
      const l = s[u];
      if (typeof l != "string" && l.nodeType === Ye.REFERENCE) {
        const { token: f, endIndex: h } = l, d = Er(f);
        if (d == null)
          continue;
        const { range: _, sheetName: C, unitId: E } = d, b = {
          ..._,
          startAbsoluteRefType: t,
          endAbsoluteRefType: r
        }, y = vs({
          range: b,
          unitId: E,
          sheetName: C || n
        }), p = y.length - f.length;
        s[u] = {
          ...l,
          token: y,
          endIndex: h + p
        };
        for (let N = u + 1; N < c; N++) {
          const w = s[N];
          typeof w != "string" && (w.startIndex += p, w.endIndex += p);
        }
      }
    }
    return `${a}${Ei(s)}`;
  }
  moveFormulaRefOffset(e, t, r, n = !1) {
    const s = this.sequenceNodesBuilder(e);
    if (s == null)
      return e;
    const a = [];
    for (let u = 0, c = s.length; u < c; u++) {
      const l = s[u];
      if (typeof l == "string" || l.nodeType !== Ye.REFERENCE) {
        a.push(l);
        continue;
      }
      const { token: f } = l, h = Er(f), { sheetName: d, unitId: _ } = h;
      let C = h.range;
      if (!n && C.startAbsoluteRefType === Me.ALL && C.endAbsoluteRefType === Me.ALL) {
        a.push(l);
        continue;
      } else
        C = da(C, t, r, n);
      let E = "";
      fc(C) ? E = vs({
        range: C,
        unitId: _,
        sheetName: d
      }) : E = m.REF, a.push({
        ...l,
        token: E
      });
    }
    return `=${Ei(a)}`;
  }
  /**
   * univer-pro/issues/1684
   * =sum({}{})
   */
  _formulaSpellCheck() {
    if (this._currentLexerNode.getChildren().length === 0)
      return !0;
    const e = this._currentLexerNode.getChildren()[0];
    return !!(e instanceof ae || Ut.has(e) || ms.has(e) || e === we.AT || e === K.COMMA || e === K.COLON || e === K.OPEN_BRACKET);
  }
  // eslint-disable-next-line complexity
  getSequenceNode(e) {
    const t = [];
    let r = !1;
    for (let n = 0, s = e.length; n < s; n++) {
      const a = e[n], u = e[n - 1], { segment: c, currentString: l } = a;
      if (l === K.DOUBLE_QUOTATION && (r = !0), (c !== "" || n === 0) && n !== s - 1) {
        t.push(l);
        continue;
      }
      let f = (u == null ? void 0 : u.segment) || "";
      const h = n - f.length;
      let d = n - 1;
      const _ = n - 1;
      if (n === s - 1 && this._isLastMergeString(l) && (f += l, d += 1), f === "" || cn.has(f)) {
        t.push(l);
        continue;
      }
      const C = f.trim(), E = Ao(C);
      r === !0 && C[C.length - 1] === K.DOUBLE_QUOTATION && C[0] !== K.OPEN_BRACES ? (r = !1, this._processPushSequenceNode(t, Ye.STRING, f, h, d, _)) : Yr(E) && hn(E) ? this._processPushSequenceNode(t, Ye.REFERENCE, f, h, d, _) : Ke.isStringNumber(C) ? this._processPushSequenceNode(t, Ye.NUMBER, f, h, d, _) : C.length > 0 && this._processPushSequenceNode(t, Ye.FUNCTION, f, h, d, _), (n !== s - 1 || !this._isLastMergeString(l)) && t.push(l);
    }
    return this._mergeSequenceNodeReference(t);
  }
  _processPushSequenceNode(e, t, r, n, s, a) {
    this._pushSequenceNode(
      e,
      {
        nodeType: t,
        token: r,
        startIndex: n,
        endIndex: s
      },
      a
    );
  }
  _getCurrentParamIndex(e, t) {
    return this._nodeMaker(e, void 0, t);
  }
  _isLastMergeString(e) {
    return e === K.DOUBLE_QUOTATION || Ke.isStringNumber(e) || !ln(e);
  }
  /**
   * Merge array and handle ref operations
   *
   */
  _mergeSequenceNodeReference(e) {
    const t = [], r = e.length;
    let n = 0;
    for (; n < r; ) {
      const s = e[n];
      if (typeof s == "string") {
        const a = e[n - 1];
        if (s.trim() === K.CLOSE_BRACES && a != null && typeof a != "string" && a.nodeType === Ye.FUNCTION && a.token.trim().substring(0, 1) === K.OPEN_BRACES) {
          a.nodeType = Ye.ARRAY, a.token += s, a.endIndex += s.length, n++;
          continue;
        }
        t.push(s);
      } else {
        const a = e[n + 1], u = e[n + 2];
        a === K.COLON && typeof s != "string" && u != null && typeof u != "string" && hn((s.token + a + u.token).trim()) && (s.nodeType = Ye.REFERENCE, s.token += a + u.token, s.endIndex = u.endIndex, n += 2), t.push(s);
      }
      n++;
    }
    return this._minusSplitSequenceNode(t);
  }
  /**
   * =-A1  Separate the negative sign from the ref string.
   */
  _minusSplitSequenceNode(e) {
    const t = [];
    for (const r of e) {
      if (typeof r != "string") {
        const s = r.token.match(/^(\s*([-@+]\s*)+)(.*)$/);
        if (s) {
          const a = s[1], u = s[3];
          if (hn(u.trim())) {
            const c = a.length, l = r.startIndex, f = r.startIndex + c - 1, h = {
              nodeType: Ye.NORMAL,
              // Use appropriate nodeType for operators
              token: a,
              startIndex: l,
              endIndex: f
            }, d = {
              nodeType: Ye.REFERENCE,
              token: u,
              startIndex: f + 1,
              endIndex: r.endIndex
            };
            t.push(h), t.push(d);
            continue;
          }
        }
      }
      t.push(r);
    }
    return t;
  }
  _pushSequenceNode(e, t, r) {
    const n = r - t.startIndex + 1;
    e.splice(e.length - n, n, t);
  }
  nodeMakerTest(e) {
    return this._nodeMaker(e);
  }
  treeBuilder(e, t = !0, r, n) {
    if (t === !0) {
      const h = ds.get(e), d = n == null ? void 0 : n(e);
      if (h && !d)
        return h;
    }
    this._resetCurrentLexerNode(), this._currentLexerNode.setToken(It);
    const s = [];
    let a = this._nodeMaker(e, s);
    if (a === m.VALUE || s.length === 0)
      return a;
    let u = !1, c = "", l = [];
    if (r) {
      const { hasDefinedName: h, sequenceString: d, definedNames: _ } = r(s);
      u = h, c = d, l = _;
    }
    if (u && (this._resetCurrentLexerNode(), this._currentLexerNode.setToken(It), a = this._nodeMaker(`=${c}`), a === m.VALUE))
      return a;
    const f = this._getTopNode(this._currentLexerNode);
    if (f && (this._currentLexerNode = f), t) {
      if (!this._suffixExpressionHandler(this._currentLexerNode))
        return m.VALUE;
      ds.set(e, this._currentLexerNode);
    }
    return u && this._currentLexerNode.setDefinedNames(l), this._currentLexerNode;
  }
  // eslint-disable-next-line complexity
  _suffixExpressionHandler(e) {
    var u;
    const t = e.getChildren();
    if (!t)
      return !1;
    const r = t.length, n = [], s = [];
    let a = !0;
    for (let c = 0; c < r; c++) {
      const l = t[c];
      if (l instanceof ae)
        a = this._suffixExpressionHandler(l), n.push(l);
      else {
        const f = l.trim();
        if (f === "")
          continue;
        if (Ut.has(f)) {
          if (f === X.PLUS && this._deletePlusForPreNode(t[c - 1]))
            continue;
          if (f !== X.PLUS && f !== X.MINUS && this._deletePlusForPreNode(t[c - 1]))
            return !1;
          for (; s.length > 0; ) {
            const h = (u = s[s.length - 1]) == null ? void 0 : u.trim();
            if (!h || h === K.OPEN_BRACKET)
              break;
            const d = cn.get(h), _ = cn.get(f);
            if (d === void 0 || _ === void 0)
              break;
            if (_ >= d)
              n.push(s.pop());
            else
              break;
          }
          s.push(l);
        } else if (f === K.OPEN_BRACKET)
          s.push(l);
        else if (f === K.CLOSE_BRACKET)
          this._processSuffixExpressionCloseBracket(n, s, t, c);
        else {
          if (this._checkCloseBracket(t[c - 1]))
            return !1;
          n.push(l);
        }
      }
    }
    return a ? this._processSuffixExpressionRemain(n, s, e) : !1;
  }
  _processSuffixExpressionRemain(e, t, r) {
    const n = e.length, s = e[n - 1];
    for (; t.length > 0; ) {
      const a = t.pop();
      if (!(s instanceof ae) && (a === K.OPEN_BRACKET || a === K.CLOSE_BRACKET))
        return !1;
      e.push(a);
    }
    return r.setChildren(e), !0;
  }
  _processSuffixExpressionCloseBracket(e, t, r, n) {
    var s;
    if (this._checkOpenBracket(r[n - 1]) || this._checkOperator(r[n - 1]))
      return !1;
    for (; t.length > 0; ) {
      const a = (s = t[t.length - 1]) == null ? void 0 : s.trim();
      if (!a)
        break;
      if (a === K.OPEN_BRACKET) {
        t.pop();
        break;
      }
      e.push(t.pop());
    }
  }
  _checkCloseBracket(e) {
    return e === K.CLOSE_BRACKET || e instanceof ae;
  }
  _checkOpenBracket(e) {
    return e === K.OPEN_BRACKET;
  }
  _checkOperator(e) {
    return e == null || e instanceof ae ? !1 : Ut.has(e);
  }
  _deletePlusForPreNode(e) {
    if (e == null)
      return !0;
    if (!(e instanceof ae)) {
      const t = e.trim();
      if (Ut.has(t) || t === K.OPEN_BRACKET)
        return !0;
    }
    return !1;
  }
  _resetCurrentLexerNode() {
    this._currentLexerNode = new ae();
  }
  _resetSegment() {
    this._segment = "";
  }
  _openBracket(e = 0) {
    this._bracketState.push(e);
  }
  _closeBracket() {
    this._bracketState.pop();
  }
  _openSquareBracket() {
    this._squareBracketState += 1;
  }
  _closeSquareBracket() {
    this._squareBracketState -= 1;
  }
  _getCurrentBracket() {
    const e = this._bracketState;
    return e[e.length - 1];
  }
  _changeCurrentBracket(e) {
    const t = this._bracketState;
    t[t.length - 1] = e;
  }
  _openBraces() {
    this._bracesState += 1;
  }
  _closeBraces() {
    this._bracesState -= 1;
  }
  _openSingleQuotation() {
    this._singleQuotationState += 1;
  }
  _closeSingleQuotation() {
    this._singleQuotationState -= 1;
  }
  _openDoubleQuotation() {
    this._doubleQuotationState += 1;
  }
  _closeDoubleQuotation() {
    this._doubleQuotationState -= 1;
  }
  _openLambda() {
    this._lambdaState = !0;
  }
  _closeLambda() {
    this._lambdaState = !1;
  }
  _openColon(e) {
    this._upLevel = e, this._colonState = !0;
  }
  _closeColon() {
    this._upLevel = 0, this._colonState = !1;
  }
  _isTableBracket() {
    return this._tableBracketState;
  }
  _openTableBracket() {
    this._tableBracketState = !0;
  }
  _closeTableBracket() {
    this._tableBracketState = !1;
  }
  _formalErrorOccurred() {
    this._formulaErrorCount += 1;
  }
  _hasFormalError() {
    return this._formulaErrorCount > 0;
  }
  _getLastChildCurrentLexerNode() {
    const e = this._currentLexerNode.getChildren();
    if (e && e.length > 0) {
      const t = e[e.length - 1];
      if (t instanceof ae)
        return t;
    }
    return !1;
  }
  _getLastChildCurrent() {
    const e = this._currentLexerNode.getChildren();
    return e && e.length > 0 ? e[e.length - 1] : !1;
  }
  _setParentCurrentLexerNode() {
    const e = this._currentLexerNode.getParent();
    return e ? (this._currentLexerNode = e, !0) : !1;
  }
  _setAncestorCurrentLexerNode() {
    var r, n, s, a, u, c;
    const e = (r = this._currentLexerNode) == null ? void 0 : r.getParent();
    let t = !1;
    if (e && e.getToken() === Ar) {
      if ((n = e == null ? void 0 : e.getParent()) != null && n.getParent()) {
        const l = (a = (s = this._currentLexerNode.getParent()) == null ? void 0 : s.getParent()) == null ? void 0 : a.getParent();
        l && (this._currentLexerNode = l), t = !0;
      }
    } else if (e != null && e.getParent()) {
      const l = (u = this._currentLexerNode.getParent()) == null ? void 0 : u.getParent();
      l && (this._currentLexerNode = l), t = !0;
    }
    for (let l = 0; l < this._upLevel; l++) {
      const f = (c = this._currentLexerNode) == null ? void 0 : c.getParent();
      f && (this._currentLexerNode = f), this._currentLexerNode ? t = !0 : t = !1;
    }
    return t;
  }
  _segmentCount() {
    return this._segment.trim().length;
  }
  _pushSegment(e) {
    this._segment += e;
  }
  _pushNodeToChildren(e, t = !1) {
    let r = e;
    if (r !== "") {
      const n = this._currentLexerNode.getChildren();
      if (!(r instanceof ae) && this.isColonOpen()) {
        const s = new ae();
        s.setToken(r), s.setParent(this._currentLexerNode), r = s;
      }
      t ? n.unshift(r) : n.push(r);
    }
    this.isColonOpen() && (this._setAncestorCurrentLexerNode(), this._closeColon());
  }
  _setCurrentLexerNode(e, t = !1) {
    this._pushNodeToChildren(e, t), e.setParent(this._currentLexerNode), this._currentLexerNode = e;
  }
  _newAndPushCurrentLexerNode(e, t, r = !1) {
    const n = new ae();
    n.setToken(e), n.setIndex(t - e.length, t - 1), this._setCurrentLexerNode(n, r);
  }
  _getTopNode(e) {
    let t = e;
    for (; t != null && t.getParent(); )
      t = t.getParent();
    return t;
  }
  _removeLastChild() {
    this._currentLexerNode.getChildren().splice(-1);
  }
  /**
   * fix univer-pro/issues/2447
   * =1/3+
   * =+
   * =sum(A1+)
   */
  _formulaErrorLastTokenCheck(e, t) {
    const r = this._findPreviousToken(e, t) || "", n = e.length - 1 === t;
    if (!n && this._isOperatorToken(r) || n && Tc(r))
      return !0;
    if (ms.has(r)) {
      const s = this._findSecondLastNonSpaceToken(e, t);
      if (s == null || Ic(s))
        return !0;
    }
    return !1;
  }
  _findPreviousToken(e, t) {
    let r = t;
    for (; r >= 0; ) {
      const n = e[r];
      if (n !== " ")
        return n;
      r--;
    }
  }
  _findSecondLastNonSpaceToken(e, t) {
    let r = t, n = 0;
    for (; r >= 0; ) {
      const s = e[r];
      if (s !== " " && (n++, n === 2))
        return s;
      r--;
    }
    return null;
  }
  _findNextToken(e, t) {
    let r = t;
    for (; r >= 0; ) {
      const n = e[r];
      if (n !== " ")
        return n;
      r++;
    }
  }
  _unexpectedEndingTokenExcludeOperator(e) {
    return (
      // OPERATOR_TOKEN_SET.has(prevString) ||
      e === K.OPEN_BRACKET || e === K.COMMA || e === X.EQUALS || e === ""
    );
  }
  _unexpectedEndingToken(e) {
    return !!(this._isOperatorToken(e) || this._unexpectedEndingTokenExcludeOperator(e));
  }
  _isOperatorToken(e) {
    return !!Ut.has(e);
  }
  _getSequenceArray(e) {
    const t = [];
    return this._nodeMaker(e, t), t;
  }
  _resetTemp() {
    this._currentLexerNode = new ae(), this._upLevel = 0, this._segment = "", this._bracketState = [], this._bracesState = 0, this._singleQuotationState = 0, this._doubleQuotationState = 0, this._lambdaState = !1, this._colonState = !1, this._formulaErrorCount = 0;
  }
  _checkErrorState() {
    return !!(this._bracketState.length > 0 || this._bracesState > 0 || this._singleQuotationState > 0 || this._doubleQuotationState > 0 || this._hasFormalError());
  }
  _checkSimilarErrorToken(e, t, r) {
    let n = t;
    if (e !== tt.POUND)
      return !0;
    let s = r[++n];
    for (; s === " "; )
      s = r[++n];
    return !!ln(s);
  }
  _checkIfErrorObject(e, t) {
    return !!this._findErrorObject(e, t);
  }
  _findErrorObject(e, t) {
    for (let r = 0; r < ui.length; r++) {
      const n = ui[r], s = t.slice(e, e + n).join("").toUpperCase();
      if (lr.has(s))
        return s;
    }
  }
  // NOTE@wzhudev: this method is over complex, need to refactor. Using recursive descend to parse the formula string
  // without a standalone lexer phase
  // eslint-disable-next-line max-lines-per-function, complexity
  _nodeMaker(e, t, r) {
    let n = e.replace(/\r\n$|\r|\n/g, " ");
    n.substring(0, 1) === X.EQUALS && (n = n.substring(1));
    const s = n.split(""), a = s.length;
    this._resetTemp(), this._formulaErrorLastTokenCheck(s, a - 1) && this._formalErrorOccurred();
    let u = 0;
    for (; u < a; ) {
      const c = s[u];
      if (r === u)
        return [this._currentLexerNode, c];
      if (c === tt.POUND && this.isSingleQuotationClose() && this.isDoubleQuotationClose() && this.isBracesClose() && this.isSquareBracketClose() && this._checkIfErrorObject(u, s)) {
        const l = this._findErrorObject(u, s);
        if (l == null)
          return m.VALUE;
        this._pushNodeToChildren(l);
        for (let f = 0; f < l.length; f++) {
          const h = s[u];
          this._pushSegment(h), this._addSequenceArray(t, h, u), u++;
        }
        this._resetSegment();
        continue;
      } else if (c === K.OPEN_BRACKET && this.isSingleQuotationClose() && this.isDoubleQuotationClose())
        if (this._segmentCount() > 0 || this.isLambdaOpen()) {
          this.isLambdaClose() && (this._newAndPushCurrentLexerNode(this._segment, u), this._resetSegment()), this._openBracket(
            1
            /* FUNCTION */
          ), this._closeLambda();
          const l = s[u + 1];
          if (l && l === K.CLOSE_BRACKET) {
            if (!this._setParentCurrentLexerNode() && u !== a - 1)
              return m.VALUE;
            this._addSequenceArray(t, c, u), u++, this._addSequenceArray(t, l, u), u++, this._closeBracket();
            continue;
          } else l && this._newAndPushCurrentLexerNode(dt, u);
        } else
          this._pushNodeToChildren(c), this._openBracket(
            0
            /* NORMAL */
          ), this._resetSegment();
      else if (c === K.CLOSE_BRACKET && this.isSingleQuotationClose() && this.isDoubleQuotationClose()) {
        this._formulaErrorLastTokenCheck(s, u - 1) && this._formalErrorOccurred(), this._pushNodeToChildren(this._segment), this._resetSegment();
        const l = this._getCurrentBracket();
        if (l === 0)
          this._pushNodeToChildren(c);
        else if (l === 1) {
          const f = s[u + 1];
          if (f && f === K.OPEN_BRACKET) {
            if (!this._setParentCurrentLexerNode() && u !== a - 1)
              return m.VALUE;
            this._newAndPushCurrentLexerNode(Ar, u, !0), this._openLambda();
          } else if (!this._setAncestorCurrentLexerNode() && u !== a - 1)
            return m.VALUE;
        } else
          return m.VALUE;
        this._closeBracket();
      } else if (c === K.OPEN_BRACES && this.isSingleQuotationClose() && this.isDoubleQuotationClose()) {
        if (this._pushSegment(c), this._openBraces(), !this._formulaSpellCheck())
          return m.VALUE;
      } else if (c === K.CLOSE_BRACES && this.isSingleQuotationClose() && this.isDoubleQuotationClose())
        this._pushSegment(c), this._pushNodeToChildren(this._segment), this._resetSegment(), this._closeBraces();
      else if (c === K.OPEN_SQUARE_BRACKET && this.isSingleQuotationClose() && this.isDoubleQuotationClose())
        this._segment.length > 0 && this._openTableBracket(), this._pushSegment(c), this._openSquareBracket();
      else if (c === K.CLOSE_SQUARE_BRACKET && this.isSingleQuotationClose() && this.isDoubleQuotationClose())
        this._closeSquareBracket(), this.isSquareBracketClose() ? (this._pushSegment(c), this._isTableBracket() && (this._pushNodeToChildren(this._segment), this._resetSegment()), this._closeTableBracket()) : this._pushSegment(c);
      else if (c === K.DOUBLE_QUOTATION && this.isSingleQuotationClose() && this.isSquareBracketClose()) {
        if (this.isDoubleQuotationClose())
          this._openDoubleQuotation();
        else {
          const l = s[u + 1];
          l && l === K.DOUBLE_QUOTATION ? u++ : this._closeDoubleQuotation();
        }
        this._pushSegment(c);
      } else if (c === K.SINGLE_QUOTATION && this.isDoubleQuotationClose()) {
        if (this.isSingleQuotationClose())
          this._openSingleQuotation(), this._segmentCount() === 0 && this._resetSegment();
        else {
          const l = s[u + 1];
          if (l && l === K.SINGLE_QUOTATION) {
            this._pushSegment(c), this._addSequenceArray(t, c, u), u++, this._pushSegment(l), this._addSequenceArray(t, l, u), u++;
            continue;
          } else
            this._closeSingleQuotation();
        }
        this._pushSegment(c);
      } else if (c === K.COMMA && this.isSingleQuotationClose() && this.isDoubleQuotationClose() && this.isBracesClose() && this.isSquareBracketClose()) {
        this._formulaErrorLastTokenCheck(s, u - 1) && this._formalErrorOccurred();
        const l = this._getCurrentBracket();
        if (l === 1 || l == null) {
          if (this._pushNodeToChildren(this._segment), this._resetSegment(), !this._setParentCurrentLexerNode() && u !== a - 1 && l != null)
            return m.VALUE;
          this._newAndPushCurrentLexerNode(dt, u);
        } else {
          const f = new ae();
          f.setToken(gl);
          const h = new ae();
          h.setToken(dt), h.changeToParent(f);
          const d = this._currentLexerNode.getParent();
          if (d && d.getToken() === K.COLON) {
            const _ = d.getParent();
            if (!_)
              return m.VALUE;
            d.changeToParent(h), _.setChildren([]), f.changeToParent(_);
          } else
            return m.VALUE;
          this._changeCurrentBracket(
            1
            /* FUNCTION */
          ), this._pushNodeToChildren(this._segment), this._resetSegment(), this._currentLexerNode = f, this._newAndPushCurrentLexerNode(dt, u);
        }
      } else if (c === K.COLON && this.isSingleQuotationClose() && this.isDoubleQuotationClose() && this.isBracesClose() && this.isSquareBracketClose()) {
        const l = new ae();
        l.setToken(c);
        const f = new ae();
        f.setToken(dt), f.setParent(l);
        const h = new ae();
        h.setToken(dt), h.setParent(l), l.getChildren().push(f, h);
        let d = l, _ = 0;
        if (this._segmentCount() > 0) {
          let C, E, b = 0;
          const y = this._segment.trim(), p = y[0], N = y[1];
          if (p === we.MINUS && (C = new ae(), C.setToken(we.MINUS), b++), (p === we.AT || N === we.AT) && (E = new ae(), E.setToken(we.AT), C && (C.addChildren(E), E.setParent(C)), b++), b > 0 && (this._segment = y.slice(b)), _ = b, E)
            if (E.addChildren(l), l.setParent(E), E.getParent()) {
              const S = E.getParent();
              S && (d = S);
            } else
              d = E;
          else C && (d = C, C.addChildren(l), l.setParent(C));
          const w = new ae();
          w.setToken(this._segment), w.setParent(f), f.getChildren().push(w), this._resetSegment();
        } else {
          const C = this._getLastChildCurrentLexerNode();
          C && C.changeToParent(f);
        }
        this._setCurrentLexerNode(d), this._currentLexerNode = h, this._openColon(_);
      } else if (ms.has(c) && this._checkSimilarErrorToken(c, u, s) && this.isSingleQuotationClose() && this.isDoubleQuotationClose() && this.isSquareBracketClose() && this.isBracesClose()) {
        this._pushNodeToChildren(this._segment);
        const l = new ae();
        l.setToken(c);
        const f = this._getLastChildCurrent();
        f instanceof ae ? f.changeToParent(l) : f !== !1 && (l.getChildren().push(f), this._removeLastChild()), this._pushNodeToChildren(l), l.setParent(this._currentLexerNode), this._resetSegment();
      } else if (Ut.has(c) && this.isSingleQuotationClose() && this.isDoubleQuotationClose() && this.isSquareBracketClose() && this.isBracesClose()) {
        let l = this._segment.trim();
        if (c === X.MINUS && l === "") {
          const f = this._findPreviousToken(s, u - 1) || "", h = this._findNextToken(s, u + 1) || "";
          if (this._unexpectedEndingTokenExcludeOperator(f) && this._isOperatorToken(h)) {
            this._pushNodeToChildren("0"), this._pushNodeToChildren(X.MINUS), this._addSequenceArray(t, c, u), this._resetSegment(), u++;
            continue;
          } else if (this._unexpectedEndingToken(f))
            if (h === X.PLUS) {
              this._pushSegment(X.MINUS), this._addSequenceArray(t, c, u), this._addSequenceArray(t, X.PLUS, u + 1), u += 2;
              continue;
            } else {
              this._pushSegment(X.MINUS), this._addSequenceArray(t, c, u), u++;
              continue;
            }
        } else if (this._segment.length > 0 && this._isScientificNotation(s, u, c)) {
          this._pushSegment(c), this._addSequenceArray(t, c, u), u++;
          continue;
        } else this._segment.length > 0 && l === "" ? l = this._segment : (this._pushNodeToChildren(this._segment), l = "");
        if (c === X.LESS_THAN || c === X.GREATER_THAN) {
          const f = s[u + 1];
          if (f && Ut.has(c + f)) {
            this._pushNodeToChildren(l + c + f), this._resetSegment(), this._addSequenceArray(t, c, u), u++, this._addSequenceArray(t, f, u), u++;
            continue;
          } else
            this._pushNodeToChildren(l + c);
        } else
          this._pushNodeToChildren(l + c);
        this._resetSegment();
      } else
        (this._segment !== "" || c !== " ") && this._pushSegment(c);
      this._addSequenceArray(t, c, u), u++;
    }
    if (this._pushNodeToChildren(this._segment), this._checkErrorState())
      return m.VALUE;
  }
  _isScientificNotation(e, t, r) {
    const n = e[t - 2];
    if (n && Number.isNaN(Number(n)) || !(r === X.MINUS || r === X.PLUS))
      return !1;
    const s = e[t + 1];
    if (s && Number.isNaN(Number(s)))
      return !1;
    const a = e[t - 1];
    return a && a.toUpperCase() === "E";
  }
  _addSequenceArray(e, t, r) {
    e == null || e.push({
      segment: this._segment,
      currentString: t,
      cur: r,
      currentLexerNode: this._currentLexerNode
    });
  }
}
function _l(o, i, e, t, r, n, s) {
  const a = (s == null ? void 0 : s.f) || "", u = (s == null ? void 0 : s.si) || "", c = ct(a), l = Ct(u), f = o.getValue(r, n), h = (f == null ? void 0 : f.f) || "", d = (f == null ? void 0 : f.si) || "";
  function _() {
    var C;
    if (ct(h) && Ct(d)) {
      const E = (C = e == null ? void 0 : e[d]) == null ? void 0 : C.f;
      E ? t.set(d, E) : t.set(d, h);
    }
  }
  if (c && l)
    d !== u && _(), o.setValue(r, n, {
      f: a,
      si: u
    }), e[u] = { f: a, r, c: n }, i.setValue(r, n, {
      f: a,
      si: u
    });
  else if (c && !l)
    h !== a && _(), o.setValue(r, n, {
      f: a
    }), i.setValue(r, n, {
      f: a
    });
  else if (!c && l) {
    d !== u && _();
    const C = e[u];
    if (C) {
      const E = n - C.c, b = r - C.r;
      o.setValue(r, n, { f: C.f, si: u, x: E, y: b }), i.setValue(r, n, { f: C.f, si: u, x: E, y: b });
    } else
      o.setValue(r, n, {
        f: "",
        si: u
      });
  } else !c && !l && o.getValue(r, n) && (_(), o.realDeleteValue(r, n), i.setValue(r, n, null));
}
function Al(o, i, e, t) {
  const r = o == null ? void 0 : o.getValue(e, t);
  if (r == null)
    return !0;
  const n = [];
  o.forValue((l, f, h) => {
    l === e && f === t || ii.intersects(h, r) && n.push(h);
  });
  const { startRow: s, startColumn: a, endRow: u, endColumn: c } = r;
  for (let l = s; l <= u; l++)
    for (let f = a; f <= c; f++) {
      let h = !1;
      const d = hc(l, f);
      n.some((_) => ii.contains(_, d) ? (h = !0, !0) : !1), h || i.realDeleteValue(l, f);
    }
}
var El = Object.getOwnPropertyDescriptor, bl = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? El(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, bi = (o, i) => (e, t) => i(e, t, o);
let br = class extends Ve {
  constructor(i, e) {
    super();
    A(this, "_arrayFormulaRange", {});
    A(this, "_arrayFormulaCellData", {});
    this._univerInstanceService = i, this._lexerTreeBuilder = e;
  }
  dispose() {
    super.dispose(), this._arrayFormulaRange = {}, this._arrayFormulaCellData = {};
  }
  clearPreviousArrayFormulaCellData(i) {
    Object.keys(i).forEach((e) => {
      const t = i[e];
      if (t == null)
        return !0;
      Object.keys(t).forEach((r) => {
        var c, l, f, h;
        const n = t[r], s = (l = (c = this._arrayFormulaRange) == null ? void 0 : c[e]) == null ? void 0 : l[r];
        if (s == null)
          return !0;
        const a = new Ce(s);
        let u = new Ce();
        ((f = this._arrayFormulaCellData[e]) == null ? void 0 : f[r]) != null && (u = new Ce(
          (h = this._arrayFormulaCellData[e]) == null ? void 0 : h[r]
        )), n.forValue((d, _) => {
          const C = a.getValue(d, _);
          if (C == null)
            return !0;
          const { startRow: E, startColumn: b, endRow: y, endColumn: p } = C;
          for (let N = E; N <= y; N++)
            for (let w = b; w <= p; w++)
              u.setValue(N, w, null);
          a.realDeleteValue(d, _);
        }), this._arrayFormulaCellData[e] && (this._arrayFormulaCellData[e][r] = u.getData());
      });
    });
  }
  mergeArrayFormulaCellData(i) {
    Object.keys(i).forEach((e) => {
      const t = i[e];
      if (t == null)
        return !0;
      this._arrayFormulaRange[e] == null && (this._arrayFormulaRange[e] = {}), this._arrayFormulaCellData[e] == null && (this._arrayFormulaCellData[e] = {}), Object.keys(t).forEach((r) => {
        var u, c;
        const n = t[r], s = new Ce((u = this._arrayFormulaRange[e]) == null ? void 0 : u[r]), a = new Ce((c = this._arrayFormulaCellData[e]) == null ? void 0 : c[r]);
        n.forValue((l, f) => {
          const h = s == null ? void 0 : s.getValue(l, f);
          if (h == null)
            return !0;
          const { startRow: d, startColumn: _, endRow: C, endColumn: E } = h;
          for (let b = d; b <= C; b++)
            for (let y = _; y <= E; y++)
              a.setValue(b, y, null);
        }), n.forValue((l, f, h) => {
          a.setValue(l, f, h);
        }), this._arrayFormulaCellData[e] && (this._arrayFormulaCellData[e][r] = a.getData());
      });
    });
  }
  getFormulaData() {
    const i = {}, e = this._univerInstanceService.getAllUnitsForType(gr.UNIVER_SHEET);
    return e.length === 0 || e.forEach((t) => {
      const r = t.getUnitId();
      i[r] = {}, t.getSheets().forEach((s) => {
        const a = s.getCellMatrix(), u = s.getSheetId();
        yi(i, r, u, a);
      });
    }), i;
  }
  getSheetFormulaData(i, e) {
    const t = {}, r = this._univerInstanceService.getUnit(i);
    if (r == null)
      return {};
    t[i] = {};
    const n = r.getSheetBySheetId(e);
    if (n == null)
      return {};
    const s = n.getCellMatrix();
    return yi(t, i, e, s), t[i][e];
  }
  getArrayFormulaRange() {
    return this._arrayFormulaRange;
  }
  setArrayFormulaRange(i) {
    this._arrayFormulaRange = i;
  }
  getArrayFormulaCellData() {
    return this._arrayFormulaCellData;
  }
  setArrayFormulaCellData(i) {
    this._arrayFormulaCellData = i;
  }
  mergeArrayFormulaRange(i) {
    Object.keys(i).forEach((e) => {
      const t = i[e];
      if (t == null)
        return !0;
      this._arrayFormulaRange[e] || (this._arrayFormulaRange[e] = {}), Object.keys(t).forEach((r) => {
        var a;
        const n = new Ce(t[r]), s = new Ce((a = this._arrayFormulaRange[e]) == null ? void 0 : a[r]);
        n.forValue((u, c, l) => {
          s.setValue(u, c, l);
        }), this._arrayFormulaRange[e] && (this._arrayFormulaRange[e][r] = s.getData());
      });
    });
  }
  deleteArrayFormulaRange(i, e, t, r) {
    var a;
    const n = (a = this._arrayFormulaRange[i]) == null ? void 0 : a[e];
    if (n == null)
      return;
    const s = new Ce(n);
    s.getValue(t, r) && (s.realDeleteValue(t, r), this._arrayFormulaRange[i] && (this._arrayFormulaRange[i][e] = s.getData()));
  }
  getCalculateData() {
    const i = this._univerInstanceService.getAllUnitsForType(gr.UNIVER_SHEET), e = {}, t = {}, r = {};
    for (const n of i) {
      const s = n.getUnitId(), a = n.getSheets(), u = {}, c = {};
      for (const l of a) {
        const f = l.getSheetId(), h = l.getConfig();
        u[f] = {
          cellData: new Ce(h.cellData),
          rowCount: h.rowCount,
          columnCount: h.columnCount,
          rowData: h.rowData,
          columnData: h.columnData,
          defaultRowHeight: h.defaultRowHeight,
          defaultColumnWidth: h.defaultColumnWidth
        }, c[l.getName()] = l.getSheetId();
      }
      e[s] = u, t[s] = n.getStyles(), r[s] = c;
    }
    return {
      allUnitData: e,
      unitStylesData: t,
      unitSheetNameMap: r
    };
  }
  /**
   * Get the hidden rows that are filtered or manually hidden.
   *
   * For formulas that are sensitive to hidden rows.
   */
  getHiddenRowsFiltered() {
    const i = this._univerInstanceService.getAllUnitsForType(gr.UNIVER_SHEET), e = {};
    for (const t of i) {
      const r = t.getUnitId(), n = t.getSheets();
      e[r] = {};
      for (const s of n) {
        const a = s.getSheetId();
        e[r][a] = {};
        const u = 0, c = s.getRowCount() - 1, l = {};
        for (let f = u; f <= c; f++)
          s.getRowVisible(f) || (l[f] = {
            hd: io.TRUE
          });
        e[r][a] = l;
      }
    }
    return e;
  }
  updateFormulaData(i, e, t) {
    const r = new Ce(t);
    let n = !1;
    r.forValue((h, d, _) => {
      if (_ && (ct(_.f) || Ct(_.si)))
        return n = !0, !1;
    });
    const s = this._getSheetFormulaIdMap(i, e), a = /* @__PURE__ */ new Map(), u = this.getFormulaData();
    u[i] == null && (u[i] = {});
    const c = u[i];
    c[e] == null && (c[e] = {});
    const l = new Ce(c[e] || {}), f = new Ce();
    return r.forValue((h, d, _) => {
      _l(l, f, s, a, h, d, _);
    }), !n && a.size === 0 || l.forValue((h, d, _) => {
      const C = (_ == null ? void 0 : _.f) || "", E = (_ == null ? void 0 : _.si) || "";
      if (Ct(E)) {
        const b = s == null ? void 0 : s[E], y = a.get(E);
        if (b && !ct(C)) {
          const p = b.f, N = d - b.c, w = h - b.r;
          l.setValue(h, d, { f: p, si: E, x: N, y: w }), r.getValue(h, d) !== void 0 && f.setValue(h, d, { f: p, si: E, x: N, y: w });
        } else if (typeof y == "string") {
          const p = (_ == null ? void 0 : _.x) || 0, N = (_ == null ? void 0 : _.y) || 0, w = this._lexerTreeBuilder.moveFormulaRefOffset(y, p, N);
          a.set(E, { r: h, c: d, f: w }), l.setValue(h, d, { f: w, si: E }), f.setValue(h, d, { f: w, si: E });
        } else if (typeof y == "object") {
          const p = d - y.c, N = h - y.r;
          l.setValue(h, d, { f: y.f, si: E, x: p, y: N }), f.setValue(h, d, { f: y.f, si: E, x: p, y: N });
        }
      }
    }), f.getMatrix();
  }
  updateArrayFormulaRange(i, e, t) {
    var a;
    const r = (a = this._arrayFormulaRange[i]) == null ? void 0 : a[e];
    if (!r) return;
    const n = new Ce(r);
    new Ce(t).forValue((u, c, l) => {
      n.realDeleteValue(u, c);
    });
  }
  updateArrayFormulaCellData(i, e, t) {
    var c, l;
    const r = (c = this._arrayFormulaRange[i]) == null ? void 0 : c[e];
    if (!r) return;
    const n = new Ce(r), s = (l = this._arrayFormulaCellData[i]) == null ? void 0 : l[e];
    if (!s) return;
    const a = new Ce(s);
    new Ce(t).forValue((f, h, d) => {
      Al(n, a, f, h);
    });
  }
  getFormulaStringByCell(i, e, t, r) {
    const n = this._univerInstanceService.getUnit(r);
    if (n == null)
      return null;
    const s = n.getSheetBySheetId(t);
    if (s == null)
      return null;
    const a = s.getCellMatrix(), u = a.getValue(i, e);
    if (u == null)
      return null;
    const { f: c, si: l } = u;
    if (ct(c))
      return c;
    if (Ct(l)) {
      let f = null;
      return a.forValue((h, d, _) => {
        if (_ == null)
          return !0;
        const { f: C, si: E } = _;
        if (ct(C) && l === E)
          return f = this._lexerTreeBuilder.moveFormulaRefOffset(
            C,
            e - d,
            i - h
          ), !1;
      }), f;
    }
    return null;
  }
  /**
   * Function to get all formula ranges
   * @returns
   */
  getFormulaDirtyRanges() {
    const i = this.getFormulaData(), e = [];
    for (const t in i) {
      const r = i[t];
      if (!r) continue;
      const n = this._univerInstanceService.getUnit(t);
      if (n)
        for (const s in r) {
          const a = r[s];
          if (!a) continue;
          const u = n.getSheetBySheetId(s);
          if (!u) continue;
          const c = {};
          for (const l of Object.keys(a)) {
            const f = Number(l);
            for (const h in a[f]) {
              const d = Number(h), _ = u.getCellRaw(f, d), C = ct(_ == null ? void 0 : _.f) || Ct(_ == null ? void 0 : _.si), E = (_ == null ? void 0 : _.v) === void 0;
              if (!(C && E)) continue;
              c[d] || (c[d] = []);
              const b = c[d].slice(-1)[0];
              b && b.endRow === f - 1 ? b.endRow = f : c[d].push({ startRow: f, endRow: f });
            }
          }
          for (const l in c) {
            const f = c[l];
            for (let h = 0; h < f.length; h++) {
              const d = f[h];
              e.push({
                unitId: t,
                sheetId: s,
                range: {
                  rangeType: et.NORMAL,
                  startRow: d.startRow,
                  endRow: d.endRow,
                  // Use endRow as the inclusive end row
                  startColumn: Number(l),
                  endColumn: Number(l)
                }
              });
            }
          }
        }
    }
    return e;
  }
  _getSheetFormulaIdMap(i, e) {
    const t = {}, r = this._univerInstanceService.getUnit(i);
    if (r == null)
      return t;
    const n = r.getSheetBySheetId(e);
    return n == null || n.getCellMatrix().forValue((a, u, c) => {
      if (c == null)
        return !0;
      const { f: l, si: f } = c;
      ct(l) && Ct(f) && (t[f] = { f: l, r: a, c: u });
    }), t;
  }
};
br = bl([
  bi(0, Gn),
  bi(1, ye(Na))
], br);
function yi(o, i, e, t) {
  o[i] || (o[i] = {}), o[i][e] || (o[i][e] = {});
  const r = /* @__PURE__ */ new Map(), n = new Ce(o[i][e]);
  t.forValue((a, u, c) => {
    const l = (c == null ? void 0 : c.f) || "", f = (c == null ? void 0 : c.si) || "", h = ct(l), d = Ct(f);
    h && d ? (n.setValue(a, u, {
      f: l,
      si: f
    }), r.set(f, { f: l, r: a, c: u })) : h && !d ? n.setValue(a, u, {
      f: l
    }) : !h && d && n.setValue(a, u, {
      f: "",
      si: f
    });
  }), n.forValue((a, u, c) => {
    const l = (c == null ? void 0 : c.f) || "", f = (c == null ? void 0 : c.si) || "";
    if (Ct(f) && !ct(l)) {
      const h = r.get(f);
      if (h) {
        const d = h.f, _ = u - h.c, C = a - h.r;
        n.setValue(a, u, { f: d, si: f, x: _, y: C });
      } else
        n.realDeleteValue(a, u);
    }
  });
  const s = n.getMatrix();
  return {
    [i]: {
      [e]: s
    }
  };
}
const yl = class ks {
  /**
   * Accept two comparable values and creates new instance of interval
   * Predicate Interval.comparable_less(low, high) supposed to return true on these values
   * @param low
   * @param high
   */
  constructor(i, e) {
    this.low = i, this.high = e;
  }
  /**
   * Clone interval
   * @returns {Interval}
   */
  clone() {
    return new ks(this.low, this.high);
  }
  /**
   * Propery max returns clone of this interval
   * @returns {Interval}
   */
  get max() {
    return this.clone();
  }
  /**
   * Predicate returns true is this interval less than other interval
   * @param other_interval
   * @returns {boolean}
   */
  less_than(i) {
    return this.low < i.low || this.low === i.low && this.high < i.high;
  }
  /**
   * Predicate returns true is this interval equals to other interval
   * @param other_interval
   * @returns {boolean}
   */
  equal_to(i) {
    return this.low === i.low && this.high === i.high;
  }
  /**
   * Predicate returns true if this interval intersects other interval
   * @param other_interval
   * @returns {boolean}
   */
  intersect(i) {
    return !this.not_intersect(i);
  }
  /**
   * Predicate returns true if this interval does not intersect other interval
   * @param other_interval
   * @returns {boolean}
   */
  not_intersect(i) {
    return this.high < i.low || i.high < this.low;
  }
  /**
   * Returns new interval merged with other interval
   * @param {Interval} other_interval - Other interval to merge with
   * @returns {Interval}
   */
  merge(i) {
    return new ks(
      this.low === void 0 ? i.low : this.low < i.low ? this.low : i.low,
      this.high === void 0 ? i.high : this.high > i.high ? this.high : i.high
    );
  }
  /**
   * Returns how key should return
   */
  output() {
    return [this.low, this.high];
  }
  /**
   * Function returns maximum between two comparable values
   * @param interval1
   * @param interval2
   * @returns {Interval}
   */
  static comparable_max(i, e) {
    return i.merge(e);
  }
  /**
   * Predicate returns true if first value less than second value
   * @param val1
   * @param val2
   * @returns {boolean}
   */
  static comparable_less_than(i, e) {
    return i < e;
  }
}, je = 0, Ae = 1;
class Kt {
  constructor(i = void 0, e = void 0, t = null, r = null, n = null, s = Ae) {
    if (this.left = t, this.right = r, this.parent = n, this.color = s, this.item = { key: i, value: e }, i && i instanceof Array && i.length === 2 && !Number.isNaN(i[0]) && !Number.isNaN(i[1])) {
      let [a, u] = i;
      a > u && ([a, u] = [u, a]), this.item.key = new yl(a, u);
    }
    this.max = this.item.key ? this.item.key.max : void 0;
  }
  isNil() {
    return this.item.key === void 0 && this.item.value === void 0 && this.left === null && this.right === null && this.color === Ae;
  }
  _value_less_than(i) {
    return this.item.value && i.item.value && this.item.value.less_than ? this.item.value.less_than(i.item.value) : this.item.value < i.item.value;
  }
  less_than(i) {
    return this.item.value === this.item.key && i.item.value === i.item.key ? this.item.key.less_than(i.item.key) : this.item.key.less_than(i.item.key) || this.item.key.equal_to(i.item.key) && this._value_less_than(i);
  }
  _value_equal(i) {
    return this.item.value && i.item.value && this.item.value.equal_to ? this.item.value.equal_to(i.item.value) : this.item.value === i.item.value;
  }
  equal_to(i) {
    return this.item.value === this.item.key && i.item.value === i.item.key ? this.item.key.equal_to(i.item.key) : this.item.key.equal_to(i.item.key) && this._value_equal(i);
  }
  intersect(i) {
    return this.item.key.intersect(i.item.key);
  }
  copy_data(i) {
    this.item.key = i.item.key, this.item.value = i.item.value;
  }
  update_max() {
    if (this.max = this.item.key ? this.item.key.max : void 0, this.right && this.right.max) {
      const i = this.item.key.constructor.comparable_max;
      this.max = i(this.max, this.right.max);
    }
    if (this.left && this.left.max) {
      const i = this.item.key.constructor.comparable_max;
      this.max = i(this.max, this.left.max);
    }
  }
  // Other_node does not intersect any node of left subtree, if this.left.max < other_node.item.key.low
  not_intersect_left_subtree(i) {
    const e = this.item.key.constructor.comparable_less_than;
    let t = this.left.max.high !== void 0 ? this.left.max.high : this.left.max;
    return e(t, i.item.key.low);
  }
  // Other_node does not intersect right subtree if other_node.item.key.high < this.right.key.low
  not_intersect_right_subtree(i) {
    const e = this.item.key.constructor.comparable_less_than;
    let t = this.right.max.low !== void 0 ? this.right.max.low : this.right.item.key.low;
    return e(i.item.key.high, t);
  }
}
class Va {
  /**
   * Construct new empty instance of IntervalTree
   */
  constructor() {
    this.root = null, this.nil_node = new Kt();
  }
  /**
   * Returns number of items stored in the interval tree
   * @returns {number}
   */
  get size() {
    let i = 0;
    return this.tree_walk(this.root, () => i++), i;
  }
  /**
   * Returns array of sorted keys in the ascending order
   * @returns {Array}
   */
  get keys() {
    let i = [];
    return this.tree_walk(this.root, (e) => i.push(
      e.item.key.output ? e.item.key.output() : e.item.key
    )), i;
  }
  /**
   * Return array of values in the ascending keys order
   * @returns {Array}
   */
  get values() {
    let i = [];
    return this.tree_walk(this.root, (e) => i.push(e.item.value)), i;
  }
  /**
   * Returns array of items (<key,value> pairs) in the ascended keys order
   * @returns {Array}
   */
  get items() {
    let i = [];
    return this.tree_walk(this.root, (e) => i.push({
      key: e.item.key.output ? e.item.key.output() : e.item.key,
      value: e.item.value
    })), i;
  }
  /**
   * Returns true if tree is empty
   * @returns {boolean}
   */
  isEmpty() {
    return this.root == null || this.root === this.nil_node;
  }
  /**
   * Clear tree
   */
  clear() {
    this.root = null;
  }
  /**
   * Insert new item into interval tree
   * @param {Interval} key - interval object or array of two numbers [low, high]
   * @param {any} value - value representing any object (optional)
   * @returns {Node} returns reference to inserted node as an object {key:interval, value: value}
   */
  insert(i, e = i) {
    if (i === void 0) return;
    let t = new Kt(i, e, this.nil_node, this.nil_node, null, je);
    return this.tree_insert(t), this.recalc_max(t), t;
  }
  /**
   * Returns true if item {key,value} exist in the tree
   * @param {Interval} key - interval correspondent to keys stored in the tree
   * @param {any} value - value object to be checked
   * @returns {boolean} true if item {key, value} exist in the tree, false otherwise
   */
  exist(i, e = i) {
    let t = new Kt(i, e);
    return !!this.tree_search(this.root, t);
  }
  /**
   * Remove entry {key, value} from the tree
   * @param {Interval} key - interval correspondent to keys stored in the tree
   * @param {any} value - value object
   * @returns {boolean} true if item {key, value} deleted, false if not found
   */
  remove(i, e = i) {
    let t = new Kt(i, e), r = this.tree_search(this.root, t);
    return r && this.tree_delete(r), r;
  }
  /**
   * Returns array of entry values which keys intersect with given interval <br/>
   * If no values stored in the tree, returns array of keys which intersect given interval
   * @param {Interval} interval - search interval, or tuple [low, high]
   * @param outputMapperFn(value,key) - optional function that maps (value, key) to custom output
   * @returns {Array}
   */
  search(i, e = (t, r) => t === r ? r.output() : t) {
    let t = new Kt(i), r = [];
    return this.tree_search_interval(this.root, t, r), r.map((n) => e(n.item.value, n.item.key));
  }
  /**
   * Returns true if intersection between given and any interval stored in the tree found
   * @param {Interval} interval - search interval or tuple [low, high]
   * @returns {boolean}
   */
  intersect_any(i) {
    let e = new Kt(i);
    return this.tree_find_any_interval(this.root, e);
  }
  /**
   * Tree visitor. For each node implement a callback function. <br/>
   * Method calls a callback function with two parameters (key, value)
   * @param visitor(key,value) - function to be called for each tree item
   */
  forEach(i) {
    this.tree_walk(this.root, (e) => i(e.item.key, e.item.value));
  }
  /**
   * Value Mapper. Walk through every node and map node value to another value
   * @param callback(value,key) - function to be called for each tree item
   */
  map(i) {
    const e = new Va();
    return this.tree_walk(this.root, (t) => e.insert(t.item.key, i(t.item.value, t.item.key))), e;
  }
  /**
   * @param {Interval} interval - optional if the iterator is intended to start from the beginning
   * @param outputMapperFn(value,key) - optional function that maps (value, key) to custom output
   * @returns {Iterator}
   */
  *iterate(i, e = (t, r) => t === r ? r.output() : t) {
    let t;
    for (i ? t = this.tree_search_nearest_forward(this.root, new Kt(i)) : this.root && (t = this.local_minimum(this.root)); t; )
      yield e(t.item.value, t.item.key), t = this.tree_successor(t);
  }
  recalc_max(i) {
    let e = i;
    for (; e.parent != null; )
      e.parent.update_max(), e = e.parent;
  }
  tree_insert(i) {
    let e = this.root, t = null;
    if (this.root == null || this.root === this.nil_node)
      this.root = i;
    else {
      for (; e !== this.nil_node; )
        t = e, i.less_than(e) ? e = e.left : e = e.right;
      i.parent = t, i.less_than(t) ? t.left = i : t.right = i;
    }
    this.insert_fixup(i);
  }
  // After insertion insert_node may have red-colored parent, and this is a single possible violation
  // Go upwords to the root and re-color until violation will be resolved
  insert_fixup(i) {
    let e, t;
    for (e = i; e !== this.root && e.parent.color === je; )
      e.parent === e.parent.parent.left ? (t = e.parent.parent.right, t.color === je ? (e.parent.color = Ae, t.color = Ae, e.parent.parent.color = je, e = e.parent.parent) : (e === e.parent.right && (e = e.parent, this.rotate_left(e)), e.parent.color = Ae, e.parent.parent.color = je, this.rotate_right(e.parent.parent))) : (t = e.parent.parent.left, t.color === je ? (e.parent.color = Ae, t.color = Ae, e.parent.parent.color = je, e = e.parent.parent) : (e === e.parent.left && (e = e.parent, this.rotate_right(e)), e.parent.color = Ae, e.parent.parent.color = je, this.rotate_left(e.parent.parent)));
    this.root.color = Ae;
  }
  tree_delete(i) {
    let e, t;
    i.left === this.nil_node || i.right === this.nil_node ? e = i : e = this.tree_successor(i), e.left !== this.nil_node ? t = e.left : t = e.right, t.parent = e.parent, e === this.root ? this.root = t : (e === e.parent.left ? e.parent.left = t : e.parent.right = t, e.parent.update_max()), this.recalc_max(t), e !== i && (i.copy_data(e), i.update_max(), this.recalc_max(i)), /*fix_node != this.nil_node && */
    e.color === Ae && this.delete_fixup(t);
  }
  delete_fixup(i) {
    let e = i, t;
    for (; e !== this.root && e.parent != null && e.color === Ae; )
      e === e.parent.left ? (t = e.parent.right, t.color === je && (t.color = Ae, e.parent.color = je, this.rotate_left(e.parent), t = e.parent.right), t.left.color === Ae && t.right.color === Ae ? (t.color = je, e = e.parent) : (t.right.color === Ae && (t.color = je, t.left.color = Ae, this.rotate_right(t), t = e.parent.right), t.color = e.parent.color, e.parent.color = Ae, t.right.color = Ae, this.rotate_left(e.parent), e = this.root)) : (t = e.parent.left, t.color === je && (t.color = Ae, e.parent.color = je, this.rotate_right(e.parent), t = e.parent.left), t.left.color === Ae && t.right.color === Ae ? (t.color = je, e = e.parent) : (t.left.color === Ae && (t.color = je, t.right.color = Ae, this.rotate_left(t), t = e.parent.left), t.color = e.parent.color, e.parent.color = Ae, t.left.color = Ae, this.rotate_right(e.parent), e = this.root));
    e.color = Ae;
  }
  tree_search(i, e) {
    if (!(i == null || i === this.nil_node))
      return e.equal_to(i) ? i : e.less_than(i) ? this.tree_search(i.left, e) : this.tree_search(i.right, e);
  }
  tree_search_nearest_forward(i, e) {
    let t, r = i;
    for (; r && r !== this.nil_node; )
      r.less_than(e) ? r.intersect(e) ? (t = r, r = r.left) : r = r.right : ((!t || r.less_than(t)) && (t = r), r = r.left);
    return t || null;
  }
  // Original search_interval method; container res support push() insertion
  // Search all intervals intersecting given one
  tree_search_interval(i, e, t) {
    i != null && i !== this.nil_node && (i.left !== this.nil_node && !i.not_intersect_left_subtree(e) && this.tree_search_interval(i.left, e, t), i.intersect(e) && t.push(i), i.right !== this.nil_node && !i.not_intersect_right_subtree(e) && this.tree_search_interval(i.right, e, t));
  }
  tree_find_any_interval(i, e) {
    let t = !1;
    return i != null && i !== this.nil_node && (i.left !== this.nil_node && !i.not_intersect_left_subtree(e) && (t = this.tree_find_any_interval(i.left, e)), t || (t = i.intersect(e)), !t && i.right !== this.nil_node && !i.not_intersect_right_subtree(e) && (t = this.tree_find_any_interval(i.right, e))), t;
  }
  local_minimum(i) {
    let e = i;
    for (; e.left != null && e.left !== this.nil_node; )
      e = e.left;
    return e;
  }
  // not in use
  local_maximum(i) {
    let e = i;
    for (; e.right != null && e.right !== this.nil_node; )
      e = e.right;
    return e;
  }
  tree_successor(i) {
    let e, t, r;
    if (i.right !== this.nil_node)
      e = this.local_minimum(i.right);
    else {
      for (t = i, r = i.parent; r != null && r.right === t; )
        t = r, r = r.parent;
      e = r;
    }
    return e;
  }
  //           |            right-rotate(T,y)       |
  //           y            ---------------.       x
  //          / \                                  / \
  //         x   c          left-rotate(T,x)      a   y
  //        / \             <---------------         / \
  //       a   b                                    b   c
  rotate_left(i) {
    let e = i.right;
    i.right = e.left, e.left !== this.nil_node && (e.left.parent = i), e.parent = i.parent, i === this.root ? this.root = e : i === i.parent.left ? i.parent.left = e : i.parent.right = e, e.left = i, i.parent = e, i != null && i !== this.nil_node && i.update_max(), e = i.parent, e != null && e !== this.nil_node && e.update_max();
  }
  rotate_right(i) {
    let e = i.left;
    i.left = e.right, e.right !== this.nil_node && (e.right.parent = i), e.parent = i.parent, i === this.root ? this.root = e : i === i.parent.left ? i.parent.left = e : i.parent.right = e, e.right = i, i.parent = e, i !== null && i !== this.nil_node && i.update_max(), e = i.parent, e != null && e !== this.nil_node && e.update_max();
  }
  tree_walk(i, e) {
    i != null && i !== this.nil_node && (this.tree_walk(i.left, e), e(i), this.tree_walk(i.right, e));
  }
  /* Return true if all red nodes have exactly two black child nodes */
  testRedBlackProperty() {
    let i = !0;
    return this.tree_walk(this.root, function(e) {
      e.color === je && (e.left.color === Ae && e.right.color === Ae || (i = !1));
    }), i;
  }
  /* Throw error if not every path from root to bottom has same black height */
  testBlackHeightProperty(i) {
    let e = 0, t = 0, r = 0;
    if (i.color === Ae && e++, i.left !== this.nil_node ? t = this.testBlackHeightProperty(i.left) : t = 1, i.right !== this.nil_node ? r = this.testBlackHeightProperty(i.right) : r = 1, t !== r)
      throw new Error("Red-black height property violated");
    return e += t, e;
  }
}
class Rl {
  constructor() {
    /**
     * {
     *    unitId:{
     *       sheetId:{
     *          'columnIndex': {
     *              10:[1,3,4,5],
     *              5:[2,6,11,22]
     *          }
     *       }
     *    }
     * }
     */
    A(this, "_cache", /* @__PURE__ */ new Map());
    A(this, "_continueBuildingCache", /* @__PURE__ */ new Map());
  }
  set(i, e, t, r, n, s = !1) {
    if (!this.shouldContinueBuildingCache(i, e, t, n) && !s)
      return;
    let a = this._cache.get(i);
    a == null && (a = /* @__PURE__ */ new Map(), this._cache.set(i, a));
    let u = a.get(e);
    u == null && (u = /* @__PURE__ */ new Map(), a.set(e, u));
    let c = u.get(t);
    if (c == null && (c = /* @__PURE__ */ new Map(), u.set(t, c)), s) {
      for (const [f, h] of c)
        if (h.has(n)) {
          h.delete(n);
          break;
        }
    }
    let l = c.get(r);
    l == null && (l = /* @__PURE__ */ new Set(), c.set(r, l)), l.add(n);
  }
  getCellValuePositions(i, e, t) {
    var r, n;
    return (n = (r = this._cache.get(i)) == null ? void 0 : r.get(e)) == null ? void 0 : n.get(t);
  }
  getCellPositions(i, e, t, r, n) {
    var a, u, c;
    const s = (c = (u = (a = this._cache.get(i)) == null ? void 0 : a.get(e)) == null ? void 0 : u.get(t)) == null ? void 0 : c.get(r);
    return s && [...s].filter((l) => n.some(([f, h]) => l >= f && l <= h));
  }
  setContinueBuildingCache(i, e, t, r, n) {
    if (t === -1 || r === -1 || n === -1)
      return;
    let s = this._continueBuildingCache.get(i);
    s == null && (s = /* @__PURE__ */ new Map(), this._continueBuildingCache.set(i, s));
    let a = s.get(e);
    a == null && (a = /* @__PURE__ */ new Map(), s.set(e, a));
    let u = a.get(t);
    if (u == null) {
      u = new Va(), u.insert([r, n]), a.set(t, u);
      return;
    }
    this._handleNewInterval(u, r, n);
  }
  shouldContinueBuildingCache(i, e, t, r) {
    var a, u;
    if (t === -1 || r === -1)
      return !1;
    const n = (u = (a = this._continueBuildingCache.get(i)) == null ? void 0 : a.get(e)) == null ? void 0 : u.get(t);
    return n ? n.search([r, r]).length === 0 : !0;
  }
  canUseCache(i, e, t, r, n) {
    var f, h;
    const s = (h = (f = this._continueBuildingCache.get(i)) == null ? void 0 : f.get(e)) == null ? void 0 : h.get(t);
    if (t === -1 || r === -1 || n === -1 || !s)
      return {
        rowsInCache: [],
        rowsNotInCache: []
      };
    const a = s.search([r, n]);
    if (a.length === 0)
      return {
        rowsInCache: [],
        rowsNotInCache: []
      };
    a.sort((d, _) => d[0] - _[0]);
    const u = [], c = [];
    let l = r;
    for (let d = 0; d < a.length; d++) {
      const [_, C] = a[d];
      if (l >= _) {
        if (n <= C) {
          u.push([l, n]);
          break;
        }
        u.push([l, C]), l = C + 1, d === a.length - 1 && l <= n && c.push([l, n]);
      } else {
        if (n > C) {
          u.push([_, C]), c.push([l, _ - 1]), l = C + 1, d === a.length - 1 && l <= n && c.push([l, n]);
          continue;
        }
        u.push([_, n]), c.push([l, _ - 1]);
      }
    }
    return {
      rowsInCache: u,
      rowsNotInCache: c
    };
  }
  clear() {
    this._cache.clear(), this._continueBuildingCache.clear();
  }
  _handleNewInterval(i, e, t) {
    let r = i.search([e, t]);
    if (r.length === 0) {
      const a = [e - 1 < 0 ? 0 : e - 1, t + 1];
      if (r = i.search(a), r.length === 0) {
        i.insert([e, t]);
        return;
      }
    }
    let n = e, s = t;
    for (const a of r)
      n = Math.min(n, a[0]), s = Math.max(s, a[1]), i.remove(a);
    i.insert([n, s]);
  }
}
const He = new Rl(), Oa = "engine-formula.config", So = 1, hy = "CYCLE_REFERENCE_COUNT", Ri = {};
class pl extends Ve {
  constructor() {
    super(...arguments);
    A(this, "_getRowFilteredCallback");
  }
  register(e) {
    this._getRowFilteredCallback = e;
  }
  getRowFiltered(e, t, r) {
    var n;
    return this._getRowFilteredCallback && (n = this._getRowFilteredCallback(e, t, r)) != null ? n : !1;
  }
}
const Mo = ze(
  "univer.formula.sheet-row-filtered.service"
);
var Nl = Object.getOwnPropertyDescriptor, Vl = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? Nl(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, en = (o, i) => (e, t) => i(e, t, o);
let $s = class extends Ve {
  constructor(i, e, t, r) {
    super();
    A(this, "_unitData", {});
    A(this, "_unitStylesData", {});
    A(this, "_arrayFormulaCellData", {});
    A(this, "_arrayFormulaRange", {});
    A(this, "_formulaData", {});
    A(this, "_sheetNameMap", {});
    A(this, "_forceCalculate", !1);
    A(this, "_clearDependencyTreeCache", {});
    A(this, "_dirtyRanges", []);
    A(this, "_dirtyNameMap", {});
    A(this, "_dirtyDefinedNameMap", {});
    A(this, "_dirtyUnitFeatureMap", {});
    A(this, "_dirtyUnitOtherFormulaMap", {});
    A(this, "_excludedCell");
    A(this, "_sheetIdToNameMap", {});
    A(this, "_executeUnitId", "");
    A(this, "_executeSubUnitId", "");
    this._univerInstanceService = i, this._localeService = e, this._formulaDataModel = t, this._sheetRowFilteredService = r;
  }
  dispose() {
    super.dispose(), this._unitData = {}, this._unitStylesData = {}, this._arrayFormulaCellData = {}, this._arrayFormulaRange = {}, this._formulaData = {}, this._sheetNameMap = {}, this._clearDependencyTreeCache = {}, this._dirtyRanges = [], this._dirtyNameMap = {}, this._dirtyDefinedNameMap = {}, this._dirtyUnitFeatureMap = {}, this._dirtyUnitOtherFormulaMap = {}, this._excludedCell = {}, this._sheetIdToNameMap = {};
  }
  getExecuteUnitId() {
    return this._executeUnitId;
  }
  getExecuteSubUnitId() {
    return this._executeSubUnitId;
  }
  setExecuteUnitId(i) {
    this._executeUnitId = i;
  }
  setExecuteSubUnitId(i) {
    this._executeSubUnitId = i;
  }
  getExcludedRange() {
    return this._excludedCell;
  }
  getUnitData() {
    return this._unitData;
  }
  getUnitStylesData() {
    return this._unitStylesData;
  }
  getFormulaData() {
    return this._formulaData;
  }
  getArrayFormulaCellData() {
    return this._arrayFormulaCellData;
  }
  getArrayFormulaRange() {
    return this._arrayFormulaRange;
  }
  getSheetNameMap() {
    return this._sheetNameMap;
  }
  isForceCalculate() {
    return this._forceCalculate;
  }
  getDirtyRanges() {
    return this._dirtyRanges;
  }
  getDirtyNameMap() {
    return this._dirtyNameMap;
  }
  getDirtyDefinedNameMap() {
    return this._dirtyDefinedNameMap;
  }
  getDirtyUnitFeatureMap() {
    return this._dirtyUnitFeatureMap;
  }
  getDirtyUnitOtherFormulaMap() {
    return this._dirtyUnitOtherFormulaMap;
  }
  getSheetName(i, e) {
    return this._sheetIdToNameMap[i] == null ? "" : this._sheetIdToNameMap[i][e] || "";
  }
  getClearDependencyTreeCache() {
    return this._clearDependencyTreeCache;
  }
  getLocale() {
    return this._localeService.getCurrentLocale();
  }
  getSheetsInfo() {
    const i = this._univerInstanceService.getCurrentUnitForType(gr.UNIVER_SHEET), { id: e, sheetOrder: t } = i.getSnapshot();
    return {
      sheetOrder: t,
      sheetNameMap: this._sheetIdToNameMap[e]
    };
  }
  getSheetRowColumnCount(i, e) {
    const t = this._univerInstanceService.getUnit(i), r = t == null ? void 0 : t.getSheetBySheetId(e), n = r == null ? void 0 : r.getSnapshot();
    if (!n)
      return { rowCount: 0, columnCount: 0 };
    const { rowCount: s, columnCount: a } = n;
    return { rowCount: s, columnCount: a };
  }
  getFilteredOutRows(i, e, t, r) {
    const n = [];
    for (let s = t; s <= r; s++)
      this._sheetRowFilteredService.getRowFiltered(i, e, s) && n.push(s);
    return n;
  }
  load(i) {
    if (i.allUnitData && i.unitSheetNameMap && i.unitStylesData)
      this._unitData = i.allUnitData, this._unitStylesData = i.unitStylesData, this._sheetNameMap = i.unitSheetNameMap;
    else {
      const { allUnitData: e, unitSheetNameMap: t, unitStylesData: r } = this._loadSheetData();
      this._unitData = e, this._unitStylesData = r, this._sheetNameMap = t;
    }
    i.rowData && this._applyUnitRowData(i.rowData), this._formulaData = i.formulaData, this._arrayFormulaCellData = Xc(i.arrayFormulaCellData), this._arrayFormulaRange = i.arrayFormulaRange, this._forceCalculate = i.forceCalculate, this._clearDependencyTreeCache = i.clearDependencyTreeCache || {}, this._dirtyRanges = i.dirtyRanges, this._dirtyNameMap = i.dirtyNameMap, this._dirtyDefinedNameMap = i.dirtyDefinedNameMap, this._dirtyUnitFeatureMap = i.dirtyUnitFeatureMap, this._dirtyUnitOtherFormulaMap = i.dirtyUnitOtherFormulaMap, this._excludedCell = i.excludedCell, this._mergeNameMap(this._sheetNameMap, this._dirtyNameMap);
  }
  getDirtyData() {
    return {
      forceCalculation: this._forceCalculate,
      dirtyRanges: this._dirtyRanges,
      dirtyNameMap: this._dirtyNameMap,
      dirtyDefinedNameMap: this._dirtyDefinedNameMap,
      dirtyUnitFeatureMap: this._dirtyUnitFeatureMap,
      dirtyUnitOtherFormulaMap: this._dirtyUnitOtherFormulaMap,
      clearDependencyTreeCache: this._clearDependencyTreeCache
    };
  }
  loadDirtyRangesAndExcludedCell(i, e) {
    this._dirtyRanges = i, this._excludedCell = e, this._dirtyNameMap = {};
  }
  registerUnitData(i) {
    this._unitData = i;
  }
  registerFormulaData(i) {
    this._formulaData = i;
  }
  registerSheetNameMap(i) {
    this._sheetNameMap = i;
  }
  // private _loadOtherFormulaData() {
  //     const unitAllDoc = this._univerInstanceService.getAllUniverDocsInstance();
  //     const unitAllSlide = this._univerInstanceService.getAllUniverSlidesInstance();
  //     const otherFormulaData: IOtherFormulaData = {};
  //     for (const documentDataModel of unitAllDoc) {
  //         const unitId = documentDataModel.getUnitId();
  //         if (otherFormulaData[unitId] == null) {
  //             otherFormulaData[unitId] = {};
  //         }
  //         if (otherFormulaData[unitId][DEFAULT_DOCUMENT_SUB_COMPONENT_ID] == null) {
  //             otherFormulaData[unitId][DEFAULT_DOCUMENT_SUB_COMPONENT_ID] = {};
  //         }
  //         const subComponent = otherFormulaData[unitId][DEFAULT_DOCUMENT_SUB_COMPONENT_ID];
  //         const customRanges = documentDataModel.getBody()?.customRanges;
  //         if (customRanges == null) {
  //             continue;
  //         }
  //         for (const customRange of customRanges) {
  //             subComponent[customRange.rangeId] = {
  //                 f: customRange.endIndex.toString(),
  //             };
  //         }
  //     }
  //     for (const slide of unitAllSlide) {
  //         const unitId = slide.getUnitId();
  //         if (otherFormulaData[unitId] == null) {
  //             otherFormulaData[unitId] = {};
  //         }
  //         if (otherFormulaData[unitId][DEFAULT_DOCUMENT_SUB_COMPONENT_ID] == null) {
  //             otherFormulaData[unitId][DEFAULT_DOCUMENT_SUB_COMPONENT_ID] = {};
  //         }
  //         const pages = slide.getPages();
  //         if (pages == null) {
  //             continue;
  //         }
  //         const pageIds = Object.keys(pages);
  //         for (const pageId of pageIds) {
  //             const page = pages[pageId];
  //             const subComponent = otherFormulaData[unitId][pageId];
  //             const pageElements = page.pageElements;
  //             if (pageElements == null) {
  //                 continue;
  //             }
  //             const pageElementIds = Object.keys(pageElements);
  //             for (const pageElementId of pageElementIds) {
  //                 const pageElement = pageElements[pageElementId];
  //                 subComponent[pageElementId] = {
  //                     f: pageElement.title,
  //                 };
  //             }
  //         }
  //     }
  //     return otherFormulaData;
  // }
  _mergeNameMap(i, e) {
    Object.keys(e).forEach((t) => {
      e[t] && Object.keys(e[t]).forEach((r) => {
        i[t] == null && (i[t] = {}), i[t][e[t][r]] = r;
      });
    }), this._sheetIdToNameMap = {}, Object.keys(i).forEach((t) => {
      Object.keys(i[t]).forEach((r) => {
        this._sheetIdToNameMap[t] == null && (this._sheetIdToNameMap[t] = {}), this._sheetIdToNameMap[t][i[t][r]] = r;
      });
    });
  }
  _loadSheetData() {
    const i = this._univerInstanceService.getCurrentUnitForType(gr.UNIVER_SHEET), e = i == null ? void 0 : i.getActiveSheet();
    return this._executeUnitId = i == null ? void 0 : i.getUnitId(), this._executeSubUnitId = e == null ? void 0 : e.getSheetId(), this._formulaDataModel.getCalculateData();
  }
  /**
   * There is no filter information in the worker, it must be passed in from the main thread after it is ready
   * @param rowData
   */
  _applyUnitRowData(i) {
    for (const e in i)
      if (i[e] != null)
        for (const t in i[e])
          i[e][t] != null && (this._unitData[e] == null && (this._unitData[e] = {}), this._unitData[e][t] == null && (this._unitData[e][t] = {
            cellData: new Ce({}),
            rowCount: 0,
            columnCount: 0,
            rowData: {},
            columnData: {}
          }), this._unitData[e][t].rowData = i[e][t]);
  }
};
$s = Vl([
  en(0, Gn),
  en(1, ye(mc)),
  en(2, ye(br)),
  en(3, ye(Mo))
], $s);
const Qt = ze(
  "univer.formula.current-data.service"
);
var Ol = Object.getOwnPropertyDescriptor, wl = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? Ol(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, _s = (o, i) => (e, t) => i(e, t, o);
let yr = class extends Ve {
  constructor(o, i, e) {
    super(), this._definedNamesService = o, this._lexerTreeBuilder = i, this._formulaCurrentConfigService = e;
  }
  treeBuilder(o, i = !0) {
    return this._lexerTreeBuilder.treeBuilder(o, i, this._injectDefinedName.bind(this), this._simpleCheckDefinedName.bind(this));
  }
  _simpleCheckDefinedName(o) {
    const i = this._formulaCurrentConfigService.getDirtyDefinedNameMap(), e = this._formulaCurrentConfigService.getExecuteUnitId();
    if (e != null && i[e] != null) {
      const t = Object.keys(i[e]);
      for (let r = 0, n = t.length; r < n; r++) {
        const s = t[r];
        if (o.indexOf(s) > -1)
          return !0;
      }
    }
    return !1;
  }
  _checkDefinedNameDirty(o) {
    const i = this._formulaCurrentConfigService.getDirtyDefinedNameMap(), e = this._formulaCurrentConfigService.getExecuteUnitId();
    if (e != null && i[e] != null) {
      const t = Object.keys(i[e]);
      for (let r = 0, n = t.length; r < n; r++)
        if (t[r] === o)
          return !0;
    }
    return !1;
  }
  _injectDefinedName(o) {
    const i = this._formulaCurrentConfigService.getExecuteUnitId();
    if (i == null)
      return {
        sequenceString: "",
        hasDefinedName: !1,
        definedNames: []
      };
    const e = this._lexerTreeBuilder.getSequenceNode(o);
    let t = "", r = !1;
    const n = [];
    for (let s = 0, a = e.length; s < a; s++) {
      const u = e[s];
      if (typeof u == "string") {
        t += u;
        continue;
      }
      const { nodeType: c, token: l } = u;
      if (c === Ye.REFERENCE || c === Ye.FUNCTION) {
        const f = this._definedNamesService.getValueByName(i, l);
        if (f) {
          let h = f.formulaOrRefString;
          h.substring(0, 1) === X.EQUALS && (h = h.substring(1)), t += h, n.push(f.name), r = !0;
        } else this._checkDefinedNameDirty(l) ? (t += m.NAME, r = !0, n.push(l)) : t += l;
      } else
        t += l;
    }
    return {
      sequenceString: t,
      hasDefinedName: r,
      definedNames: n
    };
  }
};
yr = wl([
  _s(0, Sr),
  _s(1, ye(Na)),
  _s(2, Qt)
], yr);
function mn(o) {
  if (o == null)
    return !0;
  const { v: i, f: e, si: t, p: r } = o;
  return !(!(i == null || typeof i == "string" && i.length === 0) || e != null && e.length > 0 || t != null && t.length > 0 || r != null);
}
function Do(o, i, e, t, r) {
  var s;
  const n = Object.keys(r);
  for (const a of n) {
    const u = r[a], c = (s = u == null ? void 0 : u[t]) == null ? void 0 : s[e];
    if (c == null)
      continue;
    const l = c.getValue(o, i);
    if (l != null)
      return l;
  }
}
const Sl = [
  "$",
  "£",
  "¥",
  "¤",
  "֏",
  "؋",
  "৳",
  "฿",
  "៛",
  "₡",
  "₦",
  "₩",
  "₪",
  "₫",
  "€",
  "₭",
  "₮",
  "₱",
  "₲",
  "₴",
  "₸",
  "₹",
  "₺",
  "₼",
  "₽",
  "₾",
  "₿"
], Ml = {
  currency: 2,
  date: 4,
  datetime: 5,
  error: 11,
  fraction: 7,
  general: 0,
  grouped: 11,
  number: 1,
  percent: 6,
  scientific: 8,
  text: 9,
  time: 5,
  unknown: 11
  /* Custom */
};
function my(o, i, e) {
  var u, c;
  if (o == null || i == null)
    return i;
  const t = (e == null ? void 0 : e.getStyleByCell(o)) || o.s, r = (e == null ? void 0 : e.getStyleByCell(i)) || i.s;
  if (t == null || r == null || typeof t != "object" || typeof r != "object")
    return i;
  const n = (u = t == null ? void 0 : t.n) == null ? void 0 : u.pattern, s = (c = r == null ? void 0 : r.n) == null ? void 0 : c.pattern;
  if (n == null || s == null)
    return i;
  const a = n || s;
  return r.n.pattern = a, i;
}
const Ys = new Dt(1e5);
function pi(o) {
  const i = Ys.get(o);
  if (i !== void 0)
    return i;
  const e = Ll(o);
  return Ys.set(o, e), e;
}
function Dl() {
  Ys.clear();
}
function Ll(o) {
  if (xl(o))
    return 3;
  const i = Et.getFormatInfo(o).type || "unknown";
  return Ml[i];
}
function xl(o) {
  return !!Sl.find((i) => o.includes(i)) && o.startsWith("_(");
}
function tn(o, i, e) {
  if (o === "")
    return i;
  if (i === "")
    return o;
  const t = pi(o), r = pi(i);
  return e === X.PLUS || e === X.MINUS ? t === 4 && r === 4 || t === 11 && r === 11 ? "" : i : e === X.MULTIPLY || e === X.DIVIDED ? t === 6 && r === 6 || t === 7 && r === 7 || t === 8 && r === 8 || t === 9 && r === 9 ? i : "" : o || i;
}
const Pl = /* @__PURE__ */ new Map([
  [gt.EN_US, "$"],
  [gt.RU_RU, "₽"],
  [gt.VI_VN, "₫"],
  [gt.ZH_CN, "¥"],
  [gt.ZH_TW, "NT$"],
  [gt.FR_FR, "€"],
  [gt.FA_IR, "﷼"],
  [gt.KO_KR, "₩"],
  [gt.ES_ES, "€"],
  [gt.CA_ES, "€"]
]);
function Ni(o) {
  return Pl.get(o) || "$";
}
function Je(o, i = 2) {
  let e = i;
  i > 127 && (e = 127);
  let t = "";
  return e > 0 && (t = `.${"0".repeat(e)}`), `"${Ni(o)}"#,##0${t}_);[Red]("${Ni(o)}"#,##0${t})`;
}
function jl(o, i, e = 2) {
  return Et.format(Je(o, e), i);
}
const wa = new Dt(1e5);
function Ul(o) {
  let i = o;
  i.startsWith('"') && i.endsWith('"') && (i = i.slice(1, -1));
  const e = wa.get(i);
  if (e)
    return {
      isNumberPattern: !0,
      value: e.value,
      pattern: e.pattern
    };
  const t = Et.parseNumber(i);
  if (t && t.z)
    return As(i, t.v, t.z);
  const r = Et.parseDate(i);
  if (r && r.z)
    return As(i, r.v, r.z);
  const n = Et.parseTime(i);
  return n && n.z ? As(i, n.v, n.z) : {
    isNumberPattern: !1
  };
}
function As(o, i, e) {
  return wa.set(o, {
    value: i,
    pattern: e
  }), {
    isNumberPattern: !0,
    value: i,
    pattern: e
  };
}
function vl() {
  wa.clear();
}
class Wn {
  constructor() {
    A(this, "pattern", "");
  }
  dispose() {
  }
  getPattern() {
    return this.pattern;
  }
  /**
   * Only used in NumberValueObject
   * @param pattern
   */
  setPattern(i) {
    this.pattern = i;
  }
  isError() {
    return !1;
  }
  isAsyncObject() {
    return !1;
  }
  isAsyncArrayObject() {
    return !1;
  }
  isReferenceObject() {
    return !1;
  }
  isArray() {
    return !1;
  }
  isValueObject() {
    return !1;
  }
  isEqualType(i) {
    return !1;
  }
}
class Gt extends Wn {
  constructor(e) {
    super();
    A(this, "_customData");
    this._rawValue = e;
  }
  isValueObject() {
    return !0;
  }
  toUnitRange() {
    return {
      range: {
        startColumn: -1,
        startRow: -1,
        endRow: -1,
        endColumn: -1
      },
      sheetId: "",
      unitId: ""
    };
  }
  getValue() {
    return 0;
  }
  getArrayValue() {
    return [];
  }
  setValue(e) {
  }
  setArrayValue(e) {
  }
  withCustomData(e) {
    return this._customData = e, this;
  }
  getCustomData() {
    return this._customData;
  }
  isCube() {
    return !1;
  }
  isString() {
    return !1;
  }
  isNumber() {
    return !1;
  }
  isBoolean() {
    return !1;
  }
  isLambda() {
    return !1;
  }
  isError() {
    return !1;
  }
  isNull() {
    return !1;
  }
  isHyperlink() {
    return !1;
  }
  sum() {
    return g.create(m.VALUE);
  }
  max() {
    return g.create(m.VALUE);
  }
  min() {
    return g.create(m.VALUE);
  }
  count() {
    return g.create(m.VALUE);
  }
  countA() {
    return g.create(m.VALUE);
  }
  countBlank() {
    return g.create(m.VALUE);
  }
  getNegative() {
    return g.create(m.VALUE);
  }
  getReciprocal() {
    return g.create(m.VALUE);
  }
  plus(e) {
    return g.create(m.VALUE);
  }
  minus(e) {
    return g.create(m.VALUE);
  }
  multiply(e) {
    return g.create(m.VALUE);
  }
  divided(e) {
    return g.create(m.VALUE);
  }
  mod(e) {
    return g.create(m.VALUE);
  }
  /**
   * return every value in the array after the callback function, excluding the error value
   * @param callbackFn
   * @returns
   */
  map(e) {
    return g.create(m.NAME);
  }
  /**
   * return every value in the array after the callback function
   * @param callbackFn
   * @returns
   */
  mapValue(e) {
    return g.create(m.NAME);
  }
  compare(e, t, r = !1) {
    return g.create(m.NAME);
  }
  isEqual(e) {
    return this.compare(e, x.EQUALS);
  }
  isNotEqual(e) {
    return this.compare(e, x.NOT_EQUAL);
  }
  isGreaterThanOrEqual(e) {
    return this.compare(e, x.GREATER_THAN_OR_EQUAL);
  }
  isLessThanOrEqual(e) {
    return this.compare(e, x.LESS_THAN_OR_EQUAL);
  }
  isLessThan(e) {
    return this.compare(e, x.LESS_THAN);
  }
  isGreaterThan(e) {
    return this.compare(e, x.GREATER_THAN);
  }
  concatenateFront(e) {
    return g.create(m.NAME);
  }
  concatenateBack(e) {
    return g.create(m.NAME);
  }
  plusBy(e) {
    return g.create(m.VALUE);
  }
  minusBy(e) {
    return g.create(m.VALUE);
  }
  multiplyBy(e) {
    return g.create(m.VALUE);
  }
  dividedBy(e) {
    return g.create(m.VALUE);
  }
  modInverse(e) {
    return g.create(m.VALUE);
  }
  compareBy(e, t) {
    return g.create(m.NAME);
  }
  concatenate(e, t = lt.FRONT) {
    let r = this.getValue().toString();
    if (typeof e == "string")
      t === lt.FRONT ? r = e + r : r += e;
    else if (typeof e == "number")
      t === lt.FRONT ? r = e.toString() + r : r += e.toString();
    else if (typeof e == "boolean") {
      const n = e ? "TRUE" : "FALSE";
      t === lt.FRONT ? r = n + r : r += n;
    }
    return r;
  }
  pow(e) {
    return g.create(m.VALUE);
  }
  powInverse(e) {
    return g.create(m.VALUE);
  }
  sqrt() {
    return g.create(m.VALUE);
  }
  cbrt() {
    return g.create(m.VALUE);
  }
  cos() {
    return g.create(m.VALUE);
  }
  cosh() {
    return g.create(m.VALUE);
  }
  acos() {
    return g.create(m.VALUE);
  }
  acosh() {
    return g.create(m.VALUE);
  }
  sin() {
    return g.create(m.VALUE);
  }
  sinh() {
    return g.create(m.VALUE);
  }
  asin() {
    return g.create(m.VALUE);
  }
  asinh() {
    return g.create(m.VALUE);
  }
  tan() {
    return g.create(m.VALUE);
  }
  tanh() {
    return g.create(m.VALUE);
  }
  atan() {
    return g.create(m.VALUE);
  }
  atan2(e) {
    return g.create(m.VALUE);
  }
  atan2Inverse(e) {
    return g.create(m.VALUE);
  }
  atanh() {
    return g.create(m.VALUE);
  }
  /**
   * Calculate the mean of the entire array.
   *
   * reference https://numpy.org/doc/stable/reference/generated/numpy.mean.html#numpy.mean
   *
   */
  mean() {
    return this;
  }
  /**
   * Calculate the median of the entire array.
   *
   * reference https://numpy.org/doc/stable/reference/generated/numpy.median.html
   *
   */
  median() {
    return this;
  }
  /**
   * Calculate the variance of the entire array.
   *
   * reference https://numpy.org/doc/stable/reference/generated/numpy.var.html
   */
  var() {
    return this;
  }
  /**
   * Calculate the standard deviation of the entire array.
   *
   * reference https://numpy.org/doc/stable/reference/generated/numpy.std.html
   */
  std() {
    return this;
  }
  log() {
    return g.create(m.VALUE);
  }
  log10() {
    return g.create(m.VALUE);
  }
  exp() {
    return g.create(m.VALUE);
  }
  abs() {
    return g.create(m.VALUE);
  }
  round(e) {
    return g.create(m.VALUE);
  }
  roundInverse(e) {
    return g.create(m.VALUE);
  }
  floor(e) {
    return g.create(m.VALUE);
  }
  floorInverse(e) {
    return g.create(m.VALUE);
  }
  ceil(e) {
    return g.create(m.VALUE);
  }
  ceilInverse(e) {
    return g.create(m.VALUE);
  }
  convertToNumberObjectValue() {
    return g.create(m.VALUE);
  }
  convertToBooleanObjectValue() {
    return g.create(m.VALUE);
  }
}
const Tl = 1e3, Hs = new Dt(Tl);
class g extends Gt {
  constructor(i, e = "") {
    super(i), this._errorType = i, this._errorContent = e;
  }
  static create(i, e = "") {
    const t = `${i}-${e}`, r = Hs.get(t);
    if (r)
      return r;
    const n = new g(i, e);
    return Hs.set(t, n), n;
  }
  getValue() {
    return this._errorType;
  }
  getErrorType() {
    return this._errorType;
  }
  getErrorContent() {
    return this._errorContent;
  }
  isEqualType(i) {
    return i.getErrorType() === this.getErrorType();
  }
  isError() {
    return !0;
  }
}
function Bl(o) {
  var i;
  if (o === null)
    return 0;
  if (o != null && o.p) {
    const e = o == null ? void 0 : o.p.body;
    if (e == null)
      return 0;
    const t = e.dataStream;
    return gc.transform.getPlainText(t);
  }
  return (i = o == null ? void 0 : o.v) != null ? i : 0;
}
function gy(o, i = !1) {
  return !i && !(ct(o == null ? void 0 : o.f) || Ct(o == null ? void 0 : o.si)) ? null : typeof (o == null ? void 0 : o.v) == "string" && lr.has(o.v) ? o.v : null;
}
var zt = /* @__PURE__ */ ((o) => (o[o.MIN = 0] = "MIN", o[o.MAX = 1] = "MAX", o))(zt || {}), Se = /* @__PURE__ */ ((o) => (o[o.NORMAL = 0] = "NORMAL", o[o.MIN = 1] = "MIN", o[o.MAX = 2] = "MAX", o))(Se || {});
function Rn() {
  return Intl && Intl.Collator ? new Intl.Collator(void 0, { numeric: !1 }).compare : (o, i) => o.localeCompare(i);
}
function Mr(o) {
  return o.indexOf("*") > -1 || o.indexOf("?") > -1;
}
function Es(o, i) {
  const e = Fl(i).replace(/~?[*?]/g, (r) => r.startsWith("~") ? `\\${r.substring(1)}` : r === "*" ? ".*" : r === "?" ? "." : r);
  return new RegExp(`^${e}$`).test(o);
}
function Vi(o) {
  return o.replace(/~?[*?]/g, (i) => i.startsWith("~") ? i.substring(1) : " ");
}
function Il(o, i, e) {
  let t = !1;
  switch (e) {
    case x.EQUALS:
      t = Es(o, i);
      break;
    case x.NOT_EQUAL:
      t = !Es(o, i);
      break;
    case x.GREATER_THAN:
    case x.GREATER_THAN_OR_EQUAL:
      t = Es(o, i) || o > Vi(i);
      break;
    case x.LESS_THAN:
    case x.LESS_THAN_OR_EQUAL:
      t = o < Vi(i);
      break;
  }
  return t;
}
function Fl(o) {
  return o.replace(/[.+^${}()|[\]\\]/g, "\\$&");
}
function Qs(o) {
  switch (o) {
    case 1:
      return 2;
    case 0:
      return 0;
    case -1:
      return 1;
    default:
      return 0;
  }
}
function Gs(o) {
  return o === -2 ? 1 : 0;
}
function Sa(o) {
  let i;
  switch (o) {
    case x.EQUALS:
      i = x.EQUALS;
      break;
    case x.GREATER_THAN:
      i = x.LESS_THAN;
      break;
    case x.GREATER_THAN_OR_EQUAL:
      i = x.LESS_THAN_OR_EQUAL;
      break;
    case x.LESS_THAN:
      i = x.GREATER_THAN;
      break;
    case x.LESS_THAN_OR_EQUAL:
      i = x.GREATER_THAN_OR_EQUAL;
      break;
    case x.NOT_EQUAL:
      i = x.NOT_EQUAL;
      break;
  }
  return i;
}
/*!
 *  decimal.js v10.6.0
 *  An arbitrary-precision Decimal type for JavaScript.
 *  https://github.com/MikeMcl/decimal.js
 *  Copyright (c) 2025 Michael Mclaughlin <M8ch88l@gmail.com>
 *  MIT Licence
 */
var hr = 9e15, Wt = 1e9, Ws = "0123456789abcdef", pn = "2.", Nn = "3.", qs = {
  // These values must be integers within the stated ranges (inclusive).
  // Most of these values can be changed at run-time using the `Decimal.config` method.
  // The maximum number of significant digits of the result of a calculation or base conversion.
  // E.g. `Decimal.config({ precision: 20 });`
  precision: 20,
  // 1 to MAX_DIGITS
  // The rounding mode used when rounding to `precision`.
  //
  // ROUND_UP         0 Away from zero.
  // ROUND_DOWN       1 Towards zero.
  // ROUND_CEIL       2 Towards +Infinity.
  // ROUND_FLOOR      3 Towards -Infinity.
  // ROUND_HALF_UP    4 Towards nearest neighbour. If equidistant, up.
  // ROUND_HALF_DOWN  5 Towards nearest neighbour. If equidistant, down.
  // ROUND_HALF_EVEN  6 Towards nearest neighbour. If equidistant, towards even neighbour.
  // ROUND_HALF_CEIL  7 Towards nearest neighbour. If equidistant, towards +Infinity.
  // ROUND_HALF_FLOOR 8 Towards nearest neighbour. If equidistant, towards -Infinity.
  //
  // E.g.
  // `Decimal.rounding = 4;`
  // `Decimal.rounding = Decimal.ROUND_HALF_UP;`
  rounding: 4,
  // 0 to 8
  // The modulo mode used when calculating the modulus: a mod n.
  // The quotient (q = a / n) is calculated according to the corresponding rounding mode.
  // The remainder (r) is calculated as: r = a - n * q.
  //
  // UP         0 The remainder is positive if the dividend is negative, else is negative.
  // DOWN       1 The remainder has the same sign as the dividend (JavaScript %).
  // FLOOR      3 The remainder has the same sign as the divisor (Python %).
  // HALF_EVEN  6 The IEEE 754 remainder function.
  // EUCLID     9 Euclidian division. q = sign(n) * floor(a / abs(n)). Always positive.
  //
  // Truncated division (1), floored division (3), the IEEE 754 remainder (6), and Euclidian
  // division (9) are commonly used for the modulus operation. The other rounding modes can also
  // be used, but they may not give useful results.
  modulo: 1,
  // 0 to 9
  // The exponent value at and beneath which `toString` returns exponential notation.
  // JavaScript numbers: -7
  toExpNeg: -7,
  // 0 to -EXP_LIMIT
  // The exponent value at and above which `toString` returns exponential notation.
  // JavaScript numbers: 21
  toExpPos: 21,
  // 0 to EXP_LIMIT
  // The minimum exponent value, beneath which underflow to zero occurs.
  // JavaScript numbers: -324  (5e-324)
  minE: -hr,
  // -1 to -EXP_LIMIT
  // The maximum exponent value, above which overflow to Infinity occurs.
  // JavaScript numbers: 308  (1.7976931348623157e+308)
  maxE: hr,
  // 1 to EXP_LIMIT
  // Whether to use cryptographically-secure random number generation, if available.
  crypto: !1
  // true/false
}, Lo, St, oe = !0, qn = "[DecimalError] ", Yt = qn + "Invalid argument: ", xo = qn + "Precision limit exceeded", Po = qn + "crypto unavailable", jo = "[object Decimal]", Ie = Math.floor, xe = Math.pow, kl = /^0b([01]+(\.[01]*)?|\.[01]+)(p[+-]?\d+)?$/i, $l = /^0x([0-9a-f]+(\.[0-9a-f]*)?|\.[0-9a-f]+)(p[+-]?\d+)?$/i, Yl = /^0o([0-7]+(\.[0-7]*)?|\.[0-7]+)(p[+-]?\d+)?$/i, Uo = /^(\d+(\.\d*)?|\.\d+)(e[+-]?\d+)?$/i, ft = 1e7, te = 7, Hl = 9007199254740991, Ql = pn.length - 1, Ks = Nn.length - 1, F = { toStringTag: jo };
F.absoluteValue = F.abs = function() {
  var o = new this.constructor(this);
  return o.s < 0 && (o.s = 1), J(o);
};
F.ceil = function() {
  return J(new this.constructor(this), this.e + 1, 2);
};
F.clampedTo = F.clamp = function(o, i) {
  var e, t = this, r = t.constructor;
  if (o = new r(o), i = new r(i), !o.s || !i.s) return new r(NaN);
  if (o.gt(i)) throw Error(Yt + i);
  return e = t.cmp(o), e < 0 ? o : t.cmp(i) > 0 ? i : new r(t);
};
F.comparedTo = F.cmp = function(o) {
  var i, e, t, r, n = this, s = n.d, a = (o = new n.constructor(o)).d, u = n.s, c = o.s;
  if (!s || !a)
    return !u || !c ? NaN : u !== c ? u : s === a ? 0 : !s ^ u < 0 ? 1 : -1;
  if (!s[0] || !a[0]) return s[0] ? u : a[0] ? -c : 0;
  if (u !== c) return u;
  if (n.e !== o.e) return n.e > o.e ^ u < 0 ? 1 : -1;
  for (t = s.length, r = a.length, i = 0, e = t < r ? t : r; i < e; ++i)
    if (s[i] !== a[i]) return s[i] > a[i] ^ u < 0 ? 1 : -1;
  return t === r ? 0 : t > r ^ u < 0 ? 1 : -1;
};
F.cosine = F.cos = function() {
  var o, i, e = this, t = e.constructor;
  return e.d ? e.d[0] ? (o = t.precision, i = t.rounding, t.precision = o + Math.max(e.e, e.sd()) + te, t.rounding = 1, e = Gl(t, Fo(t, e)), t.precision = o, t.rounding = i, J(St == 2 || St == 3 ? e.neg() : e, o, i, !0)) : new t(1) : new t(NaN);
};
F.cubeRoot = F.cbrt = function() {
  var o, i, e, t, r, n, s, a, u, c, l = this, f = l.constructor;
  if (!l.isFinite() || l.isZero()) return new f(l);
  for (oe = !1, n = l.s * xe(l.s * l, 1 / 3), !n || Math.abs(n) == 1 / 0 ? (e = Ue(l.d), o = l.e, (n = (o - e.length + 1) % 3) && (e += n == 1 || n == -2 ? "0" : "00"), n = xe(e, 1 / 3), o = Ie((o + 1) / 3) - (o % 3 == (o < 0 ? -1 : 2)), n == 1 / 0 ? e = "5e" + o : (e = n.toExponential(), e = e.slice(0, e.indexOf("e") + 1) + o), t = new f(e), t.s = l.s) : t = new f(n.toString()), s = (o = f.precision) + 3; ; )
    if (a = t, u = a.times(a).times(a), c = u.plus(l), t = Ne(c.plus(l).times(a), c.plus(u), s + 2, 1), Ue(a.d).slice(0, s) === (e = Ue(t.d)).slice(0, s))
      if (e = e.slice(s - 3, s + 1), e == "9999" || !r && e == "4999") {
        if (!r && (J(a, o + 1, 0), a.times(a).times(a).eq(l))) {
          t = a;
          break;
        }
        s += 4, r = 1;
      } else {
        (!+e || !+e.slice(1) && e.charAt(0) == "5") && (J(t, o + 1, 1), i = !t.times(t).times(t).eq(l));
        break;
      }
  return oe = !0, J(t, o, f.rounding, i);
};
F.decimalPlaces = F.dp = function() {
  var o, i = this.d, e = NaN;
  if (i) {
    if (o = i.length - 1, e = (o - Ie(this.e / te)) * te, o = i[o], o) for (; o % 10 == 0; o /= 10) e--;
    e < 0 && (e = 0);
  }
  return e;
};
F.dividedBy = F.div = function(o) {
  return Ne(this, new this.constructor(o));
};
F.dividedToIntegerBy = F.divToInt = function(o) {
  var i = this, e = i.constructor;
  return J(Ne(i, new e(o), 0, 1, 1), e.precision, e.rounding);
};
F.equals = F.eq = function(o) {
  return this.cmp(o) === 0;
};
F.floor = function() {
  return J(new this.constructor(this), this.e + 1, 3);
};
F.greaterThan = F.gt = function(o) {
  return this.cmp(o) > 0;
};
F.greaterThanOrEqualTo = F.gte = function(o) {
  var i = this.cmp(o);
  return i == 1 || i === 0;
};
F.hyperbolicCosine = F.cosh = function() {
  var o, i, e, t, r, n = this, s = n.constructor, a = new s(1);
  if (!n.isFinite()) return new s(n.s ? 1 / 0 : NaN);
  if (n.isZero()) return a;
  e = s.precision, t = s.rounding, s.precision = e + Math.max(n.e, n.sd()) + 4, s.rounding = 1, r = n.d.length, r < 32 ? (o = Math.ceil(r / 3), i = (1 / Xn(4, o)).toString()) : (o = 16, i = "2.3283064365386962890625e-10"), n = Rr(s, 1, n.times(i), new s(1), !0);
  for (var u, c = o, l = new s(8); c--; )
    u = n.times(n), n = a.minus(u.times(l.minus(u.times(l))));
  return J(n, s.precision = e, s.rounding = t, !0);
};
F.hyperbolicSine = F.sinh = function() {
  var o, i, e, t, r = this, n = r.constructor;
  if (!r.isFinite() || r.isZero()) return new n(r);
  if (i = n.precision, e = n.rounding, n.precision = i + Math.max(r.e, r.sd()) + 4, n.rounding = 1, t = r.d.length, t < 3)
    r = Rr(n, 2, r, r, !0);
  else {
    o = 1.4 * Math.sqrt(t), o = o > 16 ? 16 : o | 0, r = r.times(1 / Xn(5, o)), r = Rr(n, 2, r, r, !0);
    for (var s, a = new n(5), u = new n(16), c = new n(20); o--; )
      s = r.times(r), r = r.times(a.plus(s.times(u.times(s).plus(c))));
  }
  return n.precision = i, n.rounding = e, J(r, i, e, !0);
};
F.hyperbolicTangent = F.tanh = function() {
  var o, i, e = this, t = e.constructor;
  return e.isFinite() ? e.isZero() ? new t(e) : (o = t.precision, i = t.rounding, t.precision = o + 7, t.rounding = 1, Ne(e.sinh(), e.cosh(), t.precision = o, t.rounding = i)) : new t(e.s);
};
F.inverseCosine = F.acos = function() {
  var o = this, i = o.constructor, e = o.abs().cmp(1), t = i.precision, r = i.rounding;
  return e !== -1 ? e === 0 ? o.isNeg() ? At(i, t, r) : new i(0) : new i(NaN) : o.isZero() ? At(i, t + 4, r).times(0.5) : (i.precision = t + 6, i.rounding = 1, o = new i(1).minus(o).div(o.plus(1)).sqrt().atan(), i.precision = t, i.rounding = r, o.times(2));
};
F.inverseHyperbolicCosine = F.acosh = function() {
  var o, i, e = this, t = e.constructor;
  return e.lte(1) ? new t(e.eq(1) ? 0 : NaN) : e.isFinite() ? (o = t.precision, i = t.rounding, t.precision = o + Math.max(Math.abs(e.e), e.sd()) + 4, t.rounding = 1, oe = !1, e = e.times(e).minus(1).sqrt().plus(e), oe = !0, t.precision = o, t.rounding = i, e.ln()) : new t(e);
};
F.inverseHyperbolicSine = F.asinh = function() {
  var o, i, e = this, t = e.constructor;
  return !e.isFinite() || e.isZero() ? new t(e) : (o = t.precision, i = t.rounding, t.precision = o + 2 * Math.max(Math.abs(e.e), e.sd()) + 6, t.rounding = 1, oe = !1, e = e.times(e).plus(1).sqrt().plus(e), oe = !0, t.precision = o, t.rounding = i, e.ln());
};
F.inverseHyperbolicTangent = F.atanh = function() {
  var o, i, e, t, r = this, n = r.constructor;
  return r.isFinite() ? r.e >= 0 ? new n(r.abs().eq(1) ? r.s / 0 : r.isZero() ? r : NaN) : (o = n.precision, i = n.rounding, t = r.sd(), Math.max(t, o) < 2 * -r.e - 1 ? J(new n(r), o, i, !0) : (n.precision = e = t - r.e, r = Ne(r.plus(1), new n(1).minus(r), e + o, 1), n.precision = o + 4, n.rounding = 1, r = r.ln(), n.precision = o, n.rounding = i, r.times(0.5))) : new n(NaN);
};
F.inverseSine = F.asin = function() {
  var o, i, e, t, r = this, n = r.constructor;
  return r.isZero() ? new n(r) : (i = r.abs().cmp(1), e = n.precision, t = n.rounding, i !== -1 ? i === 0 ? (o = At(n, e + 4, t).times(0.5), o.s = r.s, o) : new n(NaN) : (n.precision = e + 6, n.rounding = 1, r = r.div(new n(1).minus(r.times(r)).sqrt().plus(1)).atan(), n.precision = e, n.rounding = t, r.times(2)));
};
F.inverseTangent = F.atan = function() {
  var o, i, e, t, r, n, s, a, u, c = this, l = c.constructor, f = l.precision, h = l.rounding;
  if (c.isFinite()) {
    if (c.isZero())
      return new l(c);
    if (c.abs().eq(1) && f + 4 <= Ks)
      return s = At(l, f + 4, h).times(0.25), s.s = c.s, s;
  } else {
    if (!c.s) return new l(NaN);
    if (f + 4 <= Ks)
      return s = At(l, f + 4, h).times(0.5), s.s = c.s, s;
  }
  for (l.precision = a = f + 10, l.rounding = 1, e = Math.min(28, a / te + 2 | 0), o = e; o; --o) c = c.div(c.times(c).plus(1).sqrt().plus(1));
  for (oe = !1, i = Math.ceil(a / te), t = 1, u = c.times(c), s = new l(c), r = c; o !== -1; )
    if (r = r.times(u), n = s.minus(r.div(t += 2)), r = r.times(u), s = n.plus(r.div(t += 2)), s.d[i] !== void 0) for (o = i; s.d[o] === n.d[o] && o--; ) ;
  return e && (s = s.times(2 << e - 1)), oe = !0, J(s, l.precision = f, l.rounding = h, !0);
};
F.isFinite = function() {
  return !!this.d;
};
F.isInteger = F.isInt = function() {
  return !!this.d && Ie(this.e / te) > this.d.length - 2;
};
F.isNaN = function() {
  return !this.s;
};
F.isNegative = F.isNeg = function() {
  return this.s < 0;
};
F.isPositive = F.isPos = function() {
  return this.s > 0;
};
F.isZero = function() {
  return !!this.d && this.d[0] === 0;
};
F.lessThan = F.lt = function(o) {
  return this.cmp(o) < 0;
};
F.lessThanOrEqualTo = F.lte = function(o) {
  return this.cmp(o) < 1;
};
F.logarithm = F.log = function(o) {
  var i, e, t, r, n, s, a, u, c = this, l = c.constructor, f = l.precision, h = l.rounding, d = 5;
  if (o == null)
    o = new l(10), i = !0;
  else {
    if (o = new l(o), e = o.d, o.s < 0 || !e || !e[0] || o.eq(1)) return new l(NaN);
    i = o.eq(10);
  }
  if (e = c.d, c.s < 0 || !e || !e[0] || c.eq(1))
    return new l(e && !e[0] ? -1 / 0 : c.s != 1 ? NaN : e ? 0 : 1 / 0);
  if (i)
    if (e.length > 1)
      n = !0;
    else {
      for (r = e[0]; r % 10 === 0; ) r /= 10;
      n = r !== 1;
    }
  if (oe = !1, a = f + d, s = Bt(c, a), t = i ? Vn(l, a + 10) : Bt(o, a), u = Ne(s, t, a, 1), Tr(u.d, r = f, h))
    do
      if (a += 10, s = Bt(c, a), t = i ? Vn(l, a + 10) : Bt(o, a), u = Ne(s, t, a, 1), !n) {
        +Ue(u.d).slice(r + 1, r + 15) + 1 == 1e14 && (u = J(u, f + 1, 0));
        break;
      }
    while (Tr(u.d, r += 10, h));
  return oe = !0, J(u, f, h);
};
F.minus = F.sub = function(o) {
  var i, e, t, r, n, s, a, u, c, l, f, h, d = this, _ = d.constructor;
  if (o = new _(o), !d.d || !o.d)
    return !d.s || !o.s ? o = new _(NaN) : d.d ? o.s = -o.s : o = new _(o.d || d.s !== o.s ? d : NaN), o;
  if (d.s != o.s)
    return o.s = -o.s, d.plus(o);
  if (c = d.d, h = o.d, a = _.precision, u = _.rounding, !c[0] || !h[0]) {
    if (h[0]) o.s = -o.s;
    else if (c[0]) o = new _(d);
    else return new _(u === 3 ? -0 : 0);
    return oe ? J(o, a, u) : o;
  }
  if (e = Ie(o.e / te), l = Ie(d.e / te), c = c.slice(), n = l - e, n) {
    for (f = n < 0, f ? (i = c, n = -n, s = h.length) : (i = h, e = l, s = c.length), t = Math.max(Math.ceil(a / te), s) + 2, n > t && (n = t, i.length = 1), i.reverse(), t = n; t--; ) i.push(0);
    i.reverse();
  } else {
    for (t = c.length, s = h.length, f = t < s, f && (s = t), t = 0; t < s; t++)
      if (c[t] != h[t]) {
        f = c[t] < h[t];
        break;
      }
    n = 0;
  }
  for (f && (i = c, c = h, h = i, o.s = -o.s), s = c.length, t = h.length - s; t > 0; --t) c[s++] = 0;
  for (t = h.length; t > n; ) {
    if (c[--t] < h[t]) {
      for (r = t; r && c[--r] === 0; ) c[r] = ft - 1;
      --c[r], c[t] += ft;
    }
    c[t] -= h[t];
  }
  for (; c[--s] === 0; ) c.pop();
  for (; c[0] === 0; c.shift()) --e;
  return c[0] ? (o.d = c, o.e = Kn(c, e), oe ? J(o, a, u) : o) : new _(u === 3 ? -0 : 0);
};
F.modulo = F.mod = function(o) {
  var i, e = this, t = e.constructor;
  return o = new t(o), !e.d || !o.s || o.d && !o.d[0] ? new t(NaN) : !o.d || e.d && !e.d[0] ? J(new t(e), t.precision, t.rounding) : (oe = !1, t.modulo == 9 ? (i = Ne(e, o.abs(), 0, 3, 1), i.s *= o.s) : i = Ne(e, o, 0, t.modulo, 1), i = i.times(o), oe = !0, e.minus(i));
};
F.naturalExponential = F.exp = function() {
  return Xs(this);
};
F.naturalLogarithm = F.ln = function() {
  return Bt(this);
};
F.negated = F.neg = function() {
  var o = new this.constructor(this);
  return o.s = -o.s, J(o);
};
F.plus = F.add = function(o) {
  var i, e, t, r, n, s, a, u, c, l, f = this, h = f.constructor;
  if (o = new h(o), !f.d || !o.d)
    return !f.s || !o.s ? o = new h(NaN) : f.d || (o = new h(o.d || f.s === o.s ? f : NaN)), o;
  if (f.s != o.s)
    return o.s = -o.s, f.minus(o);
  if (c = f.d, l = o.d, a = h.precision, u = h.rounding, !c[0] || !l[0])
    return l[0] || (o = new h(f)), oe ? J(o, a, u) : o;
  if (n = Ie(f.e / te), t = Ie(o.e / te), c = c.slice(), r = n - t, r) {
    for (r < 0 ? (e = c, r = -r, s = l.length) : (e = l, t = n, s = c.length), n = Math.ceil(a / te), s = n > s ? n + 1 : s + 1, r > s && (r = s, e.length = 1), e.reverse(); r--; ) e.push(0);
    e.reverse();
  }
  for (s = c.length, r = l.length, s - r < 0 && (r = s, e = l, l = c, c = e), i = 0; r; )
    i = (c[--r] = c[r] + l[r] + i) / ft | 0, c[r] %= ft;
  for (i && (c.unshift(i), ++t), s = c.length; c[--s] == 0; ) c.pop();
  return o.d = c, o.e = Kn(c, t), oe ? J(o, a, u) : o;
};
F.precision = F.sd = function(o) {
  var i, e = this;
  if (o !== void 0 && o !== !!o && o !== 1 && o !== 0) throw Error(Yt + o);
  return e.d ? (i = vo(e.d), o && e.e + 1 > i && (i = e.e + 1)) : i = NaN, i;
};
F.round = function() {
  var o = this, i = o.constructor;
  return J(new i(o), o.e + 1, i.rounding);
};
F.sine = F.sin = function() {
  var o, i, e = this, t = e.constructor;
  return e.isFinite() ? e.isZero() ? new t(e) : (o = t.precision, i = t.rounding, t.precision = o + Math.max(e.e, e.sd()) + te, t.rounding = 1, e = ql(t, Fo(t, e)), t.precision = o, t.rounding = i, J(St > 2 ? e.neg() : e, o, i, !0)) : new t(NaN);
};
F.squareRoot = F.sqrt = function() {
  var o, i, e, t, r, n, s = this, a = s.d, u = s.e, c = s.s, l = s.constructor;
  if (c !== 1 || !a || !a[0])
    return new l(!c || c < 0 && (!a || a[0]) ? NaN : a ? s : 1 / 0);
  for (oe = !1, c = Math.sqrt(+s), c == 0 || c == 1 / 0 ? (i = Ue(a), (i.length + u) % 2 == 0 && (i += "0"), c = Math.sqrt(i), u = Ie((u + 1) / 2) - (u < 0 || u % 2), c == 1 / 0 ? i = "5e" + u : (i = c.toExponential(), i = i.slice(0, i.indexOf("e") + 1) + u), t = new l(i)) : t = new l(c.toString()), e = (u = l.precision) + 3; ; )
    if (n = t, t = n.plus(Ne(s, n, e + 2, 1)).times(0.5), Ue(n.d).slice(0, e) === (i = Ue(t.d)).slice(0, e))
      if (i = i.slice(e - 3, e + 1), i == "9999" || !r && i == "4999") {
        if (!r && (J(n, u + 1, 0), n.times(n).eq(s))) {
          t = n;
          break;
        }
        e += 4, r = 1;
      } else {
        (!+i || !+i.slice(1) && i.charAt(0) == "5") && (J(t, u + 1, 1), o = !t.times(t).eq(s));
        break;
      }
  return oe = !0, J(t, u, l.rounding, o);
};
F.tangent = F.tan = function() {
  var o, i, e = this, t = e.constructor;
  return e.isFinite() ? e.isZero() ? new t(e) : (o = t.precision, i = t.rounding, t.precision = o + 10, t.rounding = 1, e = e.sin(), e.s = 1, e = Ne(e, new t(1).minus(e.times(e)).sqrt(), o + 10, 0), t.precision = o, t.rounding = i, J(St == 2 || St == 4 ? e.neg() : e, o, i, !0)) : new t(NaN);
};
F.times = F.mul = function(o) {
  var i, e, t, r, n, s, a, u, c, l = this, f = l.constructor, h = l.d, d = (o = new f(o)).d;
  if (o.s *= l.s, !h || !h[0] || !d || !d[0])
    return new f(!o.s || h && !h[0] && !d || d && !d[0] && !h ? NaN : !h || !d ? o.s / 0 : o.s * 0);
  for (e = Ie(l.e / te) + Ie(o.e / te), u = h.length, c = d.length, u < c && (n = h, h = d, d = n, s = u, u = c, c = s), n = [], s = u + c, t = s; t--; ) n.push(0);
  for (t = c; --t >= 0; ) {
    for (i = 0, r = u + t; r > t; )
      a = n[r] + d[t] * h[r - t - 1] + i, n[r--] = a % ft | 0, i = a / ft | 0;
    n[r] = (n[r] + i) % ft | 0;
  }
  for (; !n[--s]; ) n.pop();
  return i ? ++e : n.shift(), o.d = n, o.e = Kn(n, e), oe ? J(o, f.precision, f.rounding) : o;
};
F.toBinary = function(o, i) {
  return Ma(this, 2, o, i);
};
F.toDecimalPlaces = F.toDP = function(o, i) {
  var e = this, t = e.constructor;
  return e = new t(e), o === void 0 ? e : (Ge(o, 0, Wt), i === void 0 ? i = t.rounding : Ge(i, 0, 8), J(e, o + e.e + 1, i));
};
F.toExponential = function(o, i) {
  var e, t = this, r = t.constructor;
  return o === void 0 ? e = yt(t, !0) : (Ge(o, 0, Wt), i === void 0 ? i = r.rounding : Ge(i, 0, 8), t = J(new r(t), o + 1, i), e = yt(t, !0, o + 1)), t.isNeg() && !t.isZero() ? "-" + e : e;
};
F.toFixed = function(o, i) {
  var e, t, r = this, n = r.constructor;
  return o === void 0 ? e = yt(r) : (Ge(o, 0, Wt), i === void 0 ? i = n.rounding : Ge(i, 0, 8), t = J(new n(r), o + r.e + 1, i), e = yt(t, !1, o + t.e + 1)), r.isNeg() && !r.isZero() ? "-" + e : e;
};
F.toFraction = function(o) {
  var i, e, t, r, n, s, a, u, c, l, f, h, d = this, _ = d.d, C = d.constructor;
  if (!_) return new C(d);
  if (c = e = new C(1), t = u = new C(0), i = new C(t), n = i.e = vo(_) - d.e - 1, s = n % te, i.d[0] = xe(10, s < 0 ? te + s : s), o == null)
    o = n > 0 ? i : c;
  else {
    if (a = new C(o), !a.isInt() || a.lt(c)) throw Error(Yt + a);
    o = a.gt(i) ? n > 0 ? i : c : a;
  }
  for (oe = !1, a = new C(Ue(_)), l = C.precision, C.precision = n = _.length * te * 2; f = Ne(a, i, 0, 1, 1), r = e.plus(f.times(t)), r.cmp(o) != 1; )
    e = t, t = r, r = c, c = u.plus(f.times(r)), u = r, r = i, i = a.minus(f.times(r)), a = r;
  return r = Ne(o.minus(e), t, 0, 1, 1), u = u.plus(r.times(c)), e = e.plus(r.times(t)), u.s = c.s = d.s, h = Ne(c, t, n, 1).minus(d).abs().cmp(Ne(u, e, n, 1).minus(d).abs()) < 1 ? [c, t] : [u, e], C.precision = l, oe = !0, h;
};
F.toHexadecimal = F.toHex = function(o, i) {
  return Ma(this, 16, o, i);
};
F.toNearest = function(o, i) {
  var e = this, t = e.constructor;
  if (e = new t(e), o == null) {
    if (!e.d) return e;
    o = new t(1), i = t.rounding;
  } else {
    if (o = new t(o), i === void 0 ? i = t.rounding : Ge(i, 0, 8), !e.d) return o.s ? e : o;
    if (!o.d)
      return o.s && (o.s = e.s), o;
  }
  return o.d[0] ? (oe = !1, e = Ne(e, o, 0, i, 1).times(o), oe = !0, J(e)) : (o.s = e.s, e = o), e;
};
F.toNumber = function() {
  return +this;
};
F.toOctal = function(o, i) {
  return Ma(this, 8, o, i);
};
F.toPower = F.pow = function(o) {
  var i, e, t, r, n, s, a = this, u = a.constructor, c = +(o = new u(o));
  if (!a.d || !o.d || !a.d[0] || !o.d[0]) return new u(xe(+a, c));
  if (a = new u(a), a.eq(1)) return a;
  if (t = u.precision, n = u.rounding, o.eq(1)) return J(a, t, n);
  if (i = Ie(o.e / te), i >= o.d.length - 1 && (e = c < 0 ? -c : c) <= Hl)
    return r = To(u, a, e, t), o.s < 0 ? new u(1).div(r) : J(r, t, n);
  if (s = a.s, s < 0) {
    if (i < o.d.length - 1) return new u(NaN);
    if ((o.d[i] & 1) == 0 && (s = 1), a.e == 0 && a.d[0] == 1 && a.d.length == 1)
      return a.s = s, a;
  }
  return e = xe(+a, c), i = e == 0 || !isFinite(e) ? Ie(c * (Math.log("0." + Ue(a.d)) / Math.LN10 + a.e + 1)) : new u(e + "").e, i > u.maxE + 1 || i < u.minE - 1 ? new u(i > 0 ? s / 0 : 0) : (oe = !1, u.rounding = a.s = 1, e = Math.min(12, (i + "").length), r = Xs(o.times(Bt(a, t + e)), t), r.d && (r = J(r, t + 5, 1), Tr(r.d, t, n) && (i = t + 10, r = J(Xs(o.times(Bt(a, i + e)), i), i + 5, 1), +Ue(r.d).slice(t + 1, t + 15) + 1 == 1e14 && (r = J(r, t + 1, 0)))), r.s = s, oe = !0, u.rounding = n, J(r, t, n));
};
F.toPrecision = function(o, i) {
  var e, t = this, r = t.constructor;
  return o === void 0 ? e = yt(t, t.e <= r.toExpNeg || t.e >= r.toExpPos) : (Ge(o, 1, Wt), i === void 0 ? i = r.rounding : Ge(i, 0, 8), t = J(new r(t), o, i), e = yt(t, o <= t.e || t.e <= r.toExpNeg, o)), t.isNeg() && !t.isZero() ? "-" + e : e;
};
F.toSignificantDigits = F.toSD = function(o, i) {
  var e = this, t = e.constructor;
  return o === void 0 ? (o = t.precision, i = t.rounding) : (Ge(o, 1, Wt), i === void 0 ? i = t.rounding : Ge(i, 0, 8)), J(new t(e), o, i);
};
F.toString = function() {
  var o = this, i = o.constructor, e = yt(o, o.e <= i.toExpNeg || o.e >= i.toExpPos);
  return o.isNeg() && !o.isZero() ? "-" + e : e;
};
F.truncated = F.trunc = function() {
  return J(new this.constructor(this), this.e + 1, 1);
};
F.valueOf = F.toJSON = function() {
  var o = this, i = o.constructor, e = yt(o, o.e <= i.toExpNeg || o.e >= i.toExpPos);
  return o.isNeg() ? "-" + e : e;
};
function Ue(o) {
  var i, e, t, r = o.length - 1, n = "", s = o[0];
  if (r > 0) {
    for (n += s, i = 1; i < r; i++)
      t = o[i] + "", e = te - t.length, e && (n += vt(e)), n += t;
    s = o[i], t = s + "", e = te - t.length, e && (n += vt(e));
  } else if (s === 0)
    return "0";
  for (; s % 10 === 0; ) s /= 10;
  return n + s;
}
function Ge(o, i, e) {
  if (o !== ~~o || o < i || o > e)
    throw Error(Yt + o);
}
function Tr(o, i, e, t) {
  var r, n, s, a;
  for (n = o[0]; n >= 10; n /= 10) --i;
  return --i < 0 ? (i += te, r = 0) : (r = Math.ceil((i + 1) / te), i %= te), n = xe(10, te - i), a = o[r] % n | 0, t == null ? i < 3 ? (i == 0 ? a = a / 100 | 0 : i == 1 && (a = a / 10 | 0), s = e < 4 && a == 99999 || e > 3 && a == 49999 || a == 5e4 || a == 0) : s = (e < 4 && a + 1 == n || e > 3 && a + 1 == n / 2) && (o[r + 1] / n / 100 | 0) == xe(10, i - 2) - 1 || (a == n / 2 || a == 0) && (o[r + 1] / n / 100 | 0) == 0 : i < 4 ? (i == 0 ? a = a / 1e3 | 0 : i == 1 ? a = a / 100 | 0 : i == 2 && (a = a / 10 | 0), s = (t || e < 4) && a == 9999 || !t && e > 3 && a == 4999) : s = ((t || e < 4) && a + 1 == n || !t && e > 3 && a + 1 == n / 2) && (o[r + 1] / n / 1e3 | 0) == xe(10, i - 3) - 1, s;
}
function gn(o, i, e) {
  for (var t, r = [0], n, s = 0, a = o.length; s < a; ) {
    for (n = r.length; n--; ) r[n] *= i;
    for (r[0] += Ws.indexOf(o.charAt(s++)), t = 0; t < r.length; t++)
      r[t] > e - 1 && (r[t + 1] === void 0 && (r[t + 1] = 0), r[t + 1] += r[t] / e | 0, r[t] %= e);
  }
  return r.reverse();
}
function Gl(o, i) {
  var e, t, r;
  if (i.isZero()) return i;
  t = i.d.length, t < 32 ? (e = Math.ceil(t / 3), r = (1 / Xn(4, e)).toString()) : (e = 16, r = "2.3283064365386962890625e-10"), o.precision += e, i = Rr(o, 1, i.times(r), new o(1));
  for (var n = e; n--; ) {
    var s = i.times(i);
    i = s.times(s).minus(s).times(8).plus(1);
  }
  return o.precision -= e, i;
}
var Ne = /* @__PURE__ */ (function() {
  function o(t, r, n) {
    var s, a = 0, u = t.length;
    for (t = t.slice(); u--; )
      s = t[u] * r + a, t[u] = s % n | 0, a = s / n | 0;
    return a && t.unshift(a), t;
  }
  function i(t, r, n, s) {
    var a, u;
    if (n != s)
      u = n > s ? 1 : -1;
    else
      for (a = u = 0; a < n; a++)
        if (t[a] != r[a]) {
          u = t[a] > r[a] ? 1 : -1;
          break;
        }
    return u;
  }
  function e(t, r, n, s) {
    for (var a = 0; n--; )
      t[n] -= a, a = t[n] < r[n] ? 1 : 0, t[n] = a * s + t[n] - r[n];
    for (; !t[0] && t.length > 1; ) t.shift();
  }
  return function(t, r, n, s, a, u) {
    var c, l, f, h, d, _, C, E, b, y, p, N, w, S, M, D, L, v, j, k, G = t.constructor, ne = t.s == r.s ? 1 : -1, z = t.d, Q = r.d;
    if (!z || !z[0] || !Q || !Q[0])
      return new G(
        // Return NaN if either NaN, or both Infinity or 0.
        !t.s || !r.s || (z ? Q && z[0] == Q[0] : !Q) ? NaN : (
          // Return ±0 if x is 0 or y is ±Infinity, or return ±Infinity as y is 0.
          z && z[0] == 0 || !Q ? ne * 0 : ne / 0
        )
      );
    for (u ? (d = 1, l = t.e - r.e) : (u = ft, d = te, l = Ie(t.e / d) - Ie(r.e / d)), j = Q.length, L = z.length, b = new G(ne), y = b.d = [], f = 0; Q[f] == (z[f] || 0); f++) ;
    if (Q[f] > (z[f] || 0) && l--, n == null ? (S = n = G.precision, s = G.rounding) : a ? S = n + (t.e - r.e) + 1 : S = n, S < 0)
      y.push(1), _ = !0;
    else {
      if (S = S / d + 2 | 0, f = 0, j == 1) {
        for (h = 0, Q = Q[0], S++; (f < L || h) && S--; f++)
          M = h * u + (z[f] || 0), y[f] = M / Q | 0, h = M % Q | 0;
        _ = h || f < L;
      } else {
        for (h = u / (Q[0] + 1) | 0, h > 1 && (Q = o(Q, h, u), z = o(z, h, u), j = Q.length, L = z.length), D = j, p = z.slice(0, j), N = p.length; N < j; ) p[N++] = 0;
        k = Q.slice(), k.unshift(0), v = Q[0], Q[1] >= u / 2 && ++v;
        do
          h = 0, c = i(Q, p, j, N), c < 0 ? (w = p[0], j != N && (w = w * u + (p[1] || 0)), h = w / v | 0, h > 1 ? (h >= u && (h = u - 1), C = o(Q, h, u), E = C.length, N = p.length, c = i(C, p, E, N), c == 1 && (h--, e(C, j < E ? k : Q, E, u))) : (h == 0 && (c = h = 1), C = Q.slice()), E = C.length, E < N && C.unshift(0), e(p, C, N, u), c == -1 && (N = p.length, c = i(Q, p, j, N), c < 1 && (h++, e(p, j < N ? k : Q, N, u))), N = p.length) : c === 0 && (h++, p = [0]), y[f++] = h, c && p[0] ? p[N++] = z[D] || 0 : (p = [z[D]], N = 1);
        while ((D++ < L || p[0] !== void 0) && S--);
        _ = p[0] !== void 0;
      }
      y[0] || y.shift();
    }
    if (d == 1)
      b.e = l, Lo = _;
    else {
      for (f = 1, h = y[0]; h >= 10; h /= 10) f++;
      b.e = f + l * d - 1, J(b, a ? n + b.e + 1 : n, s, _);
    }
    return b;
  };
})();
function J(o, i, e, t) {
  var r, n, s, a, u, c, l, f, h, d = o.constructor;
  e: if (i != null) {
    if (f = o.d, !f) return o;
    for (r = 1, a = f[0]; a >= 10; a /= 10) r++;
    if (n = i - r, n < 0)
      n += te, s = i, l = f[h = 0], u = l / xe(10, r - s - 1) % 10 | 0;
    else if (h = Math.ceil((n + 1) / te), a = f.length, h >= a)
      if (t) {
        for (; a++ <= h; ) f.push(0);
        l = u = 0, r = 1, n %= te, s = n - te + 1;
      } else
        break e;
    else {
      for (l = a = f[h], r = 1; a >= 10; a /= 10) r++;
      n %= te, s = n - te + r, u = s < 0 ? 0 : l / xe(10, r - s - 1) % 10 | 0;
    }
    if (t = t || i < 0 || f[h + 1] !== void 0 || (s < 0 ? l : l % xe(10, r - s - 1)), c = e < 4 ? (u || t) && (e == 0 || e == (o.s < 0 ? 3 : 2)) : u > 5 || u == 5 && (e == 4 || t || e == 6 && // Check whether the digit to the left of the rounding digit is odd.
    (n > 0 ? s > 0 ? l / xe(10, r - s) : 0 : f[h - 1]) % 10 & 1 || e == (o.s < 0 ? 8 : 7)), i < 1 || !f[0])
      return f.length = 0, c ? (i -= o.e + 1, f[0] = xe(10, (te - i % te) % te), o.e = -i || 0) : f[0] = o.e = 0, o;
    if (n == 0 ? (f.length = h, a = 1, h--) : (f.length = h + 1, a = xe(10, te - n), f[h] = s > 0 ? (l / xe(10, r - s) % xe(10, s) | 0) * a : 0), c)
      for (; ; )
        if (h == 0) {
          for (n = 1, s = f[0]; s >= 10; s /= 10) n++;
          for (s = f[0] += a, a = 1; s >= 10; s /= 10) a++;
          n != a && (o.e++, f[0] == ft && (f[0] = 1));
          break;
        } else {
          if (f[h] += a, f[h] != ft) break;
          f[h--] = 0, a = 1;
        }
    for (n = f.length; f[--n] === 0; ) f.pop();
  }
  return oe && (o.e > d.maxE ? (o.d = null, o.e = NaN) : o.e < d.minE && (o.e = 0, o.d = [0])), o;
}
function yt(o, i, e) {
  if (!o.isFinite()) return Io(o);
  var t, r = o.e, n = Ue(o.d), s = n.length;
  return i ? (e && (t = e - s) > 0 ? n = n.charAt(0) + "." + n.slice(1) + vt(t) : s > 1 && (n = n.charAt(0) + "." + n.slice(1)), n = n + (o.e < 0 ? "e" : "e+") + o.e) : r < 0 ? (n = "0." + vt(-r - 1) + n, e && (t = e - s) > 0 && (n += vt(t))) : r >= s ? (n += vt(r + 1 - s), e && (t = e - r - 1) > 0 && (n = n + "." + vt(t))) : ((t = r + 1) < s && (n = n.slice(0, t) + "." + n.slice(t)), e && (t = e - s) > 0 && (r + 1 === s && (n += "."), n += vt(t))), n;
}
function Kn(o, i) {
  var e = o[0];
  for (i *= te; e >= 10; e /= 10) i++;
  return i;
}
function Vn(o, i, e) {
  if (i > Ql)
    throw oe = !0, e && (o.precision = e), Error(xo);
  return J(new o(pn), i, 1, !0);
}
function At(o, i, e) {
  if (i > Ks) throw Error(xo);
  return J(new o(Nn), i, e, !0);
}
function vo(o) {
  var i = o.length - 1, e = i * te + 1;
  if (i = o[i], i) {
    for (; i % 10 == 0; i /= 10) e--;
    for (i = o[0]; i >= 10; i /= 10) e++;
  }
  return e;
}
function vt(o) {
  for (var i = ""; o--; ) i += "0";
  return i;
}
function To(o, i, e, t) {
  var r, n = new o(1), s = Math.ceil(t / te + 4);
  for (oe = !1; ; ) {
    if (e % 2 && (n = n.times(i), wi(n.d, s) && (r = !0)), e = Ie(e / 2), e === 0) {
      e = n.d.length - 1, r && n.d[e] === 0 && ++n.d[e];
      break;
    }
    i = i.times(i), wi(i.d, s);
  }
  return oe = !0, n;
}
function Oi(o) {
  return o.d[o.d.length - 1] & 1;
}
function Bo(o, i, e) {
  for (var t, r, n = new o(i[0]), s = 0; ++s < i.length; ) {
    if (r = new o(i[s]), !r.s) {
      n = r;
      break;
    }
    t = n.cmp(r), (t === e || t === 0 && n.s === e) && (n = r);
  }
  return n;
}
function Xs(o, i) {
  var e, t, r, n, s, a, u, c = 0, l = 0, f = 0, h = o.constructor, d = h.rounding, _ = h.precision;
  if (!o.d || !o.d[0] || o.e > 17)
    return new h(o.d ? o.d[0] ? o.s < 0 ? 0 : 1 / 0 : 1 : o.s ? o.s < 0 ? 0 : o : NaN);
  for (i == null ? (oe = !1, u = _) : u = i, a = new h(0.03125); o.e > -2; )
    o = o.times(a), f += 5;
  for (t = Math.log(xe(2, f)) / Math.LN10 * 2 + 5 | 0, u += t, e = n = s = new h(1), h.precision = u; ; ) {
    if (n = J(n.times(o), u, 1), e = e.times(++l), a = s.plus(Ne(n, e, u, 1)), Ue(a.d).slice(0, u) === Ue(s.d).slice(0, u)) {
      for (r = f; r--; ) s = J(s.times(s), u, 1);
      if (i == null)
        if (c < 3 && Tr(s.d, u - t, d, c))
          h.precision = u += 10, e = n = a = new h(1), l = 0, c++;
        else
          return J(s, h.precision = _, d, oe = !0);
      else
        return h.precision = _, s;
    }
    s = a;
  }
}
function Bt(o, i) {
  var e, t, r, n, s, a, u, c, l, f, h, d = 1, _ = 10, C = o, E = C.d, b = C.constructor, y = b.rounding, p = b.precision;
  if (C.s < 0 || !E || !E[0] || !C.e && E[0] == 1 && E.length == 1)
    return new b(E && !E[0] ? -1 / 0 : C.s != 1 ? NaN : E ? 0 : C);
  if (i == null ? (oe = !1, l = p) : l = i, b.precision = l += _, e = Ue(E), t = e.charAt(0), Math.abs(n = C.e) < 15e14) {
    for (; t < 7 && t != 1 || t == 1 && e.charAt(1) > 3; )
      C = C.times(o), e = Ue(C.d), t = e.charAt(0), d++;
    n = C.e, t > 1 ? (C = new b("0." + e), n++) : C = new b(t + "." + e.slice(1));
  } else
    return c = Vn(b, l + 2, p).times(n + ""), C = Bt(new b(t + "." + e.slice(1)), l - _).plus(c), b.precision = p, i == null ? J(C, p, y, oe = !0) : C;
  for (f = C, u = s = C = Ne(C.minus(1), C.plus(1), l, 1), h = J(C.times(C), l, 1), r = 3; ; ) {
    if (s = J(s.times(h), l, 1), c = u.plus(Ne(s, new b(r), l, 1)), Ue(c.d).slice(0, l) === Ue(u.d).slice(0, l))
      if (u = u.times(2), n !== 0 && (u = u.plus(Vn(b, l + 2, p).times(n + ""))), u = Ne(u, new b(d), l, 1), i == null)
        if (Tr(u.d, l - _, y, a))
          b.precision = l += _, c = s = C = Ne(f.minus(1), f.plus(1), l, 1), h = J(C.times(C), l, 1), r = a = 1;
        else
          return J(u, b.precision = p, y, oe = !0);
      else
        return b.precision = p, u;
    u = c, r += 2;
  }
}
function Io(o) {
  return String(o.s * o.s / 0);
}
function dn(o, i) {
  var e, t, r;
  for ((e = i.indexOf(".")) > -1 && (i = i.replace(".", "")), (t = i.search(/e/i)) > 0 ? (e < 0 && (e = t), e += +i.slice(t + 1), i = i.substring(0, t)) : e < 0 && (e = i.length), t = 0; i.charCodeAt(t) === 48; t++) ;
  for (r = i.length; i.charCodeAt(r - 1) === 48; --r) ;
  if (i = i.slice(t, r), i) {
    if (r -= t, o.e = e = e - t - 1, o.d = [], t = (e + 1) % te, e < 0 && (t += te), t < r) {
      for (t && o.d.push(+i.slice(0, t)), r -= te; t < r; ) o.d.push(+i.slice(t, t += te));
      i = i.slice(t), t = te - i.length;
    } else
      t -= r;
    for (; t--; ) i += "0";
    o.d.push(+i), oe && (o.e > o.constructor.maxE ? (o.d = null, o.e = NaN) : o.e < o.constructor.minE && (o.e = 0, o.d = [0]));
  } else
    o.e = 0, o.d = [0];
  return o;
}
function Wl(o, i) {
  var e, t, r, n, s, a, u, c, l;
  if (i.indexOf("_") > -1) {
    if (i = i.replace(/(\d)_(?=\d)/g, "$1"), Uo.test(i)) return dn(o, i);
  } else if (i === "Infinity" || i === "NaN")
    return +i || (o.s = NaN), o.e = NaN, o.d = null, o;
  if ($l.test(i))
    e = 16, i = i.toLowerCase();
  else if (kl.test(i))
    e = 2;
  else if (Yl.test(i))
    e = 8;
  else
    throw Error(Yt + i);
  for (n = i.search(/p/i), n > 0 ? (u = +i.slice(n + 1), i = i.substring(2, n)) : i = i.slice(2), n = i.indexOf("."), s = n >= 0, t = o.constructor, s && (i = i.replace(".", ""), a = i.length, n = a - n, r = To(t, new t(e), n, n * 2)), c = gn(i, e, ft), l = c.length - 1, n = l; c[n] === 0; --n) c.pop();
  return n < 0 ? new t(o.s * 0) : (o.e = Kn(c, l), o.d = c, oe = !1, s && (o = Ne(o, r, a * 4)), u && (o = o.times(Math.abs(u) < 54 ? xe(2, u) : P.pow(2, u))), oe = !0, o);
}
function ql(o, i) {
  var e, t = i.d.length;
  if (t < 3)
    return i.isZero() ? i : Rr(o, 2, i, i);
  e = 1.4 * Math.sqrt(t), e = e > 16 ? 16 : e | 0, i = i.times(1 / Xn(5, e)), i = Rr(o, 2, i, i);
  for (var r, n = new o(5), s = new o(16), a = new o(20); e--; )
    r = i.times(i), i = i.times(n.plus(r.times(s.times(r).minus(a))));
  return i;
}
function Rr(o, i, e, t, r) {
  var n, s, a, u, c = o.precision, l = Math.ceil(c / te);
  for (oe = !1, u = e.times(e), a = new o(t); ; ) {
    if (s = Ne(a.times(u), new o(i++ * i++), c, 1), a = r ? t.plus(s) : t.minus(s), t = Ne(s.times(u), new o(i++ * i++), c, 1), s = a.plus(t), s.d[l] !== void 0) {
      for (n = l; s.d[n] === a.d[n] && n--; ) ;
      if (n == -1) break;
    }
    n = a, a = t, t = s, s = n;
  }
  return oe = !0, s.d.length = l + 1, s;
}
function Xn(o, i) {
  for (var e = o; --i; ) e *= o;
  return e;
}
function Fo(o, i) {
  var e, t = i.s < 0, r = At(o, o.precision, 1), n = r.times(0.5);
  if (i = i.abs(), i.lte(n))
    return St = t ? 4 : 1, i;
  if (e = i.divToInt(r), e.isZero())
    St = t ? 3 : 2;
  else {
    if (i = i.minus(e.times(r)), i.lte(n))
      return St = Oi(e) ? t ? 2 : 3 : t ? 4 : 1, i;
    St = Oi(e) ? t ? 1 : 4 : t ? 3 : 2;
  }
  return i.minus(r).abs();
}
function Ma(o, i, e, t) {
  var r, n, s, a, u, c, l, f, h, d = o.constructor, _ = e !== void 0;
  if (_ ? (Ge(e, 1, Wt), t === void 0 ? t = d.rounding : Ge(t, 0, 8)) : (e = d.precision, t = d.rounding), !o.isFinite())
    l = Io(o);
  else {
    for (l = yt(o), s = l.indexOf("."), _ ? (r = 2, i == 16 ? e = e * 4 - 3 : i == 8 && (e = e * 3 - 2)) : r = i, s >= 0 && (l = l.replace(".", ""), h = new d(1), h.e = l.length - s, h.d = gn(yt(h), 10, r), h.e = h.d.length), f = gn(l, 10, r), n = u = f.length; f[--u] == 0; ) f.pop();
    if (!f[0])
      l = _ ? "0p+0" : "0";
    else {
      if (s < 0 ? n-- : (o = new d(o), o.d = f, o.e = n, o = Ne(o, h, e, t, 0, r), f = o.d, n = o.e, c = Lo), s = f[e], a = r / 2, c = c || f[e + 1] !== void 0, c = t < 4 ? (s !== void 0 || c) && (t === 0 || t === (o.s < 0 ? 3 : 2)) : s > a || s === a && (t === 4 || c || t === 6 && f[e - 1] & 1 || t === (o.s < 0 ? 8 : 7)), f.length = e, c)
        for (; ++f[--e] > r - 1; )
          f[e] = 0, e || (++n, f.unshift(1));
      for (u = f.length; !f[u - 1]; --u) ;
      for (s = 0, l = ""; s < u; s++) l += Ws.charAt(f[s]);
      if (_) {
        if (u > 1)
          if (i == 16 || i == 8) {
            for (s = i == 16 ? 4 : 3, --u; u % s; u++) l += "0";
            for (f = gn(l, r, i), u = f.length; !f[u - 1]; --u) ;
            for (s = 1, l = "1."; s < u; s++) l += Ws.charAt(f[s]);
          } else
            l = l.charAt(0) + "." + l.slice(1);
        l = l + (n < 0 ? "p" : "p+") + n;
      } else if (n < 0) {
        for (; ++n; ) l = "0" + l;
        l = "0." + l;
      } else if (++n > u) for (n -= u; n--; ) l += "0";
      else n < u && (l = l.slice(0, n) + "." + l.slice(n));
    }
    l = (i == 16 ? "0x" : i == 2 ? "0b" : i == 8 ? "0o" : "") + l;
  }
  return o.s < 0 ? "-" + l : l;
}
function wi(o, i) {
  if (o.length > i)
    return o.length = i, !0;
}
function Kl(o) {
  return new this(o).abs();
}
function Xl(o) {
  return new this(o).acos();
}
function Zl(o) {
  return new this(o).acosh();
}
function zl(o, i) {
  return new this(o).plus(i);
}
function Jl(o) {
  return new this(o).asin();
}
function ef(o) {
  return new this(o).asinh();
}
function tf(o) {
  return new this(o).atan();
}
function rf(o) {
  return new this(o).atanh();
}
function nf(o, i) {
  o = new this(o), i = new this(i);
  var e, t = this.precision, r = this.rounding, n = t + 4;
  return !o.s || !i.s ? e = new this(NaN) : !o.d && !i.d ? (e = At(this, n, 1).times(i.s > 0 ? 0.25 : 0.75), e.s = o.s) : !i.d || o.isZero() ? (e = i.s < 0 ? At(this, t, r) : new this(0), e.s = o.s) : !o.d || i.isZero() ? (e = At(this, n, 1).times(0.5), e.s = o.s) : i.s < 0 ? (this.precision = n, this.rounding = 1, e = this.atan(Ne(o, i, n, 1)), i = At(this, n, 1), this.precision = t, this.rounding = r, e = o.s < 0 ? e.minus(i) : e.plus(i)) : e = this.atan(Ne(o, i, n, 1)), e;
}
function sf(o) {
  return new this(o).cbrt();
}
function af(o) {
  return J(o = new this(o), o.e + 1, 2);
}
function of(o, i, e) {
  return new this(o).clamp(i, e);
}
function uf(o) {
  if (!o || typeof o != "object") throw Error(qn + "Object expected");
  var i, e, t, r = o.defaults === !0, n = [
    "precision",
    1,
    Wt,
    "rounding",
    0,
    8,
    "toExpNeg",
    -hr,
    0,
    "toExpPos",
    0,
    hr,
    "maxE",
    0,
    hr,
    "minE",
    -hr,
    0,
    "modulo",
    0,
    9
  ];
  for (i = 0; i < n.length; i += 3)
    if (e = n[i], r && (this[e] = qs[e]), (t = o[e]) !== void 0)
      if (Ie(t) === t && t >= n[i + 1] && t <= n[i + 2]) this[e] = t;
      else throw Error(Yt + e + ": " + t);
  if (e = "crypto", r && (this[e] = qs[e]), (t = o[e]) !== void 0)
    if (t === !0 || t === !1 || t === 0 || t === 1)
      if (t)
        if (typeof crypto < "u" && crypto && (crypto.getRandomValues || crypto.randomBytes))
          this[e] = !0;
        else
          throw Error(Po);
      else
        this[e] = !1;
    else
      throw Error(Yt + e + ": " + t);
  return this;
}
function cf(o) {
  return new this(o).cos();
}
function lf(o) {
  return new this(o).cosh();
}
function ko(o) {
  var i, e, t;
  function r(n) {
    var s, a, u, c = this;
    if (!(c instanceof r)) return new r(n);
    if (c.constructor = r, Si(n)) {
      c.s = n.s, oe ? !n.d || n.e > r.maxE ? (c.e = NaN, c.d = null) : n.e < r.minE ? (c.e = 0, c.d = [0]) : (c.e = n.e, c.d = n.d.slice()) : (c.e = n.e, c.d = n.d ? n.d.slice() : n.d);
      return;
    }
    if (u = typeof n, u === "number") {
      if (n === 0) {
        c.s = 1 / n < 0 ? -1 : 1, c.e = 0, c.d = [0];
        return;
      }
      if (n < 0 ? (n = -n, c.s = -1) : c.s = 1, n === ~~n && n < 1e7) {
        for (s = 0, a = n; a >= 10; a /= 10) s++;
        oe ? s > r.maxE ? (c.e = NaN, c.d = null) : s < r.minE ? (c.e = 0, c.d = [0]) : (c.e = s, c.d = [n]) : (c.e = s, c.d = [n]);
        return;
      }
      if (n * 0 !== 0) {
        n || (c.s = NaN), c.e = NaN, c.d = null;
        return;
      }
      return dn(c, n.toString());
    }
    if (u === "string")
      return (a = n.charCodeAt(0)) === 45 ? (n = n.slice(1), c.s = -1) : (a === 43 && (n = n.slice(1)), c.s = 1), Uo.test(n) ? dn(c, n) : Wl(c, n);
    if (u === "bigint")
      return n < 0 ? (n = -n, c.s = -1) : c.s = 1, dn(c, n.toString());
    throw Error(Yt + n);
  }
  if (r.prototype = F, r.ROUND_UP = 0, r.ROUND_DOWN = 1, r.ROUND_CEIL = 2, r.ROUND_FLOOR = 3, r.ROUND_HALF_UP = 4, r.ROUND_HALF_DOWN = 5, r.ROUND_HALF_EVEN = 6, r.ROUND_HALF_CEIL = 7, r.ROUND_HALF_FLOOR = 8, r.EUCLID = 9, r.config = r.set = uf, r.clone = ko, r.isDecimal = Si, r.abs = Kl, r.acos = Xl, r.acosh = Zl, r.add = zl, r.asin = Jl, r.asinh = ef, r.atan = tf, r.atanh = rf, r.atan2 = nf, r.cbrt = sf, r.ceil = af, r.clamp = of, r.cos = cf, r.cosh = lf, r.div = ff, r.exp = hf, r.floor = mf, r.hypot = gf, r.ln = df, r.log = Cf, r.log10 = Af, r.log2 = _f, r.max = Ef, r.min = bf, r.mod = yf, r.mul = Rf, r.pow = pf, r.random = Nf, r.round = Vf, r.sign = Of, r.sin = wf, r.sinh = Sf, r.sqrt = Mf, r.sub = Df, r.sum = Lf, r.tan = xf, r.tanh = Pf, r.trunc = jf, o === void 0 && (o = {}), o && o.defaults !== !0)
    for (t = ["precision", "rounding", "toExpNeg", "toExpPos", "maxE", "minE", "modulo", "crypto"], i = 0; i < t.length; ) o.hasOwnProperty(e = t[i++]) || (o[e] = this[e]);
  return r.config(o), r;
}
function ff(o, i) {
  return new this(o).div(i);
}
function hf(o) {
  return new this(o).exp();
}
function mf(o) {
  return J(o = new this(o), o.e + 1, 3);
}
function gf() {
  var o, i, e = new this(0);
  for (oe = !1, o = 0; o < arguments.length; )
    if (i = new this(arguments[o++]), i.d)
      e.d && (e = e.plus(i.times(i)));
    else {
      if (i.s)
        return oe = !0, new this(1 / 0);
      e = i;
    }
  return oe = !0, e.sqrt();
}
function Si(o) {
  return o instanceof P || o && o.toStringTag === jo || !1;
}
function df(o) {
  return new this(o).ln();
}
function Cf(o, i) {
  return new this(o).log(i);
}
function _f(o) {
  return new this(o).log(2);
}
function Af(o) {
  return new this(o).log(10);
}
function Ef() {
  return Bo(this, arguments, -1);
}
function bf() {
  return Bo(this, arguments, 1);
}
function yf(o, i) {
  return new this(o).mod(i);
}
function Rf(o, i) {
  return new this(o).mul(i);
}
function pf(o, i) {
  return new this(o).pow(i);
}
function Nf(o) {
  var i, e, t, r, n = 0, s = new this(1), a = [];
  if (o === void 0 ? o = this.precision : Ge(o, 1, Wt), t = Math.ceil(o / te), this.crypto)
    if (crypto.getRandomValues)
      for (i = crypto.getRandomValues(new Uint32Array(t)); n < t; )
        r = i[n], r >= 429e7 ? i[n] = crypto.getRandomValues(new Uint32Array(1))[0] : a[n++] = r % 1e7;
    else if (crypto.randomBytes) {
      for (i = crypto.randomBytes(t *= 4); n < t; )
        r = i[n] + (i[n + 1] << 8) + (i[n + 2] << 16) + ((i[n + 3] & 127) << 24), r >= 214e7 ? crypto.randomBytes(4).copy(i, n) : (a.push(r % 1e7), n += 4);
      n = t / 4;
    } else
      throw Error(Po);
  else for (; n < t; ) a[n++] = Math.random() * 1e7 | 0;
  for (t = a[--n], o %= te, t && o && (r = xe(10, te - o), a[n] = (t / r | 0) * r); a[n] === 0; n--) a.pop();
  if (n < 0)
    e = 0, a = [0];
  else {
    for (e = -1; a[0] === 0; e -= te) a.shift();
    for (t = 1, r = a[0]; r >= 10; r /= 10) t++;
    t < te && (e -= te - t);
  }
  return s.e = e, s.d = a, s;
}
function Vf(o) {
  return J(o = new this(o), o.e + 1, this.rounding);
}
function Of(o) {
  return o = new this(o), o.d ? o.d[0] ? o.s : 0 * o.s : o.s || NaN;
}
function wf(o) {
  return new this(o).sin();
}
function Sf(o) {
  return new this(o).sinh();
}
function Mf(o) {
  return new this(o).sqrt();
}
function Df(o, i) {
  return new this(o).sub(i);
}
function Lf() {
  var o = 0, i = arguments, e = new this(i[o]);
  for (oe = !1; e.s && ++o < i.length; ) e = e.plus(i[o]);
  return oe = !0, J(e, this.precision, this.rounding);
}
function xf(o) {
  return new this(o).tan();
}
function Pf(o) {
  return new this(o).tanh();
}
function jf(o) {
  return J(o = new this(o), o.e + 1, 1);
}
F[Symbol.for("nodejs.util.inspect.custom")] = F.toString;
F[Symbol.toStringTag] = "Decimal";
var P = F.constructor = ko(qs);
pn = new P(pn);
Nn = new P(Nn);
function Uf(o, i) {
  const e = o + i;
  return Number.isSafeInteger(e) ? e : new P(o).add(i).toNumber();
}
function vf(o, i) {
  const e = o - i;
  return Number.isSafeInteger(e) ? e : new P(o).sub(i).toNumber();
}
function Dr(o, i) {
  const e = o * i;
  return Number.isSafeInteger(e) ? e : new P(o).mul(i).toNumber();
}
function Tf(o, i) {
  const e = o / i;
  return Number.isSafeInteger(e) ? e : new P(o).div(i).toNumber();
}
function Ft(o, i) {
  const e = 10 ** Math.trunc(i), t = Zn(o, e);
  return Math.round(Dr(o, e) + t) / e;
}
function Xe(o, i) {
  const e = 10 ** Math.trunc(i), t = Zn(o, e);
  return Math.floor(Dr(o, e) + t) / e;
}
function Qe(o, i) {
  const e = 10 ** Math.trunc(i), t = Zn(o, e);
  return Math.ceil(Dr(o, e) - t) / e;
}
function Zn(o, i) {
  return Number.EPSILON * Math.max(1, Math.abs(Dr(o, i)));
}
function Mi(o, i) {
  return o - i * Math.floor(o / i);
}
function Bf(o, i) {
  return o ** i;
}
function If(o) {
  return Math.sqrt(o);
}
function Di(o, i) {
  return o === i;
}
function Ff(o, i) {
  return o > i;
}
function kf(o, i) {
  return o >= i;
}
function $f(o, i) {
  return o < i;
}
function Yf(o, i) {
  return o <= i;
}
function Li(o, i = 15) {
  return Math.floor(Math.abs(o)).toString().length >= i ? o : Number.parseFloat(o.toPrecision(i));
}
function Hf(o, i, e = Number.EPSILON) {
  return Math.abs(o - i) < e;
}
function Da(o, i = 12, e = 1e-10) {
  const t = Li(o, i);
  return Hf(o, t, e) ? t : Li(o);
}
function Qf(o) {
  return o - Math.trunc(o);
}
const Qn = class Qn extends Gt {
  static create() {
    return this._instance = this._instance || new Qn(0), this._instance;
  }
  isNull() {
    return !0;
  }
  plus(i) {
    return R.create(0).plus(i);
  }
  minus(i) {
    return R.create(0).minus(i);
  }
  multiply(i) {
    return R.create(0).multiply(i);
  }
  divided(i) {
    return R.create(0).divided(i);
  }
  mod(i) {
    return R.create(0).mod(i);
  }
  compare(i, e) {
    return i.isString() ? U.create("").compare(i, e) : i.isBoolean() ? T.create(!1).compare(i, e) : R.create(0).compare(i, e);
  }
  concatenateFront(i) {
    return i.isArray() ? i.concatenateBack(U.create("")) : U.create(this.concatenate(i.getValue(), lt.FRONT));
  }
  concatenateBack(i) {
    return i.isArray() ? i.concatenateFront(U.create("")) : U.create(this.concatenate(i.getValue(), lt.BACK));
  }
  plusBy(i) {
    return R.create(0).plusBy(i);
  }
  minusBy(i) {
    return R.create(0).minusBy(i);
  }
  multiplyBy(i) {
    return R.create(0).multiplyBy(i);
  }
  dividedBy(i) {
    return R.create(0).dividedBy(i);
  }
  compareBy(i, e) {
    return typeof i == "string" ? U.create("").compareBy(i, e) : typeof i == "boolean" ? T.create(!1).compareBy(i, e) : R.create(0).compareBy(i, e);
  }
  pow(i) {
    return R.create(0).pow(i);
  }
  sqrt() {
    return R.create(0).sqrt();
  }
  cbrt() {
    return R.create(0).cbrt();
  }
  cos() {
    return R.create(0).cos();
  }
  cosh() {
    return R.create(0).cosh();
  }
  acos() {
    return R.create(0).acos();
  }
  acosh() {
    return R.create(0).acosh();
  }
  sin() {
    return R.create(0).sin();
  }
  sinh() {
    return R.create(0).sinh();
  }
  asin() {
    return R.create(0).asin();
  }
  asinh() {
    return R.create(0).asinh();
  }
  tan() {
    return R.create(0).tan();
  }
  tanh() {
    return R.create(0).tanh();
  }
  atan() {
    return R.create(0).atan();
  }
  atan2(i) {
    return R.create(0).atan2(i);
  }
  atanh() {
    return R.create(0).atanh();
  }
  log() {
    return g.create(m.NUM);
  }
  log10() {
    return g.create(m.NUM);
  }
  exp() {
    return R.create(0).exp();
  }
  abs() {
    return R.create(0).abs();
  }
  round(i) {
    return R.create(0).round(i);
  }
  floor(i) {
    return R.create(0).floor(i);
  }
  ceil(i) {
    return R.create(0).ceil(i);
  }
  convertToNumberObjectValue() {
    return R.create(0);
  }
  convertToBooleanObjectValue() {
    return T.create(!1);
  }
};
A(Qn, "_instance");
let ue = Qn;
const Jt = class Jt extends Gt {
  constructor(e) {
    super(e);
    A(this, "_value", !1);
    this._value = e;
  }
  static create(e) {
    return e ? (this._instanceTrue = this._instanceTrue || new Jt(!0), this._instanceTrue) : (this._instanceFalse = this._instanceFalse || new Jt(!1), this._instanceFalse);
  }
  getValue() {
    return this._value;
  }
  isBoolean() {
    return !0;
  }
  getNegative() {
    const e = this.getValue();
    let t = 0;
    return e && (t = 1), R.create(-t);
  }
  getReciprocal() {
    return this.getValue() ? R.create(1) : g.create(m.DIV_BY_ZERO);
  }
  plus(e) {
    return this._convertToNumber().plus(e);
  }
  minus(e) {
    return this._convertToNumber().minus(e);
  }
  multiply(e) {
    return this._convertToNumber().multiply(e);
  }
  divided(e) {
    return this._convertToNumber().divided(e);
  }
  mod(e) {
    return this._convertToNumber().mod(e);
  }
  compare(e, t) {
    return e.isArray() ? e.compare(this, Sa(t)) : e.isNull() ? this._convertToNumber().compare(e, t) : this.compareBy(e.getValue(), t);
  }
  compareBy(e, t) {
    let r = !1;
    if (typeof e == "string" || typeof e == "number")
      r = this._compareString(t);
    else if (typeof e == "boolean") {
      const n = R.create(e ? 1 : 0);
      return this._convertToNumber().compare(n, t);
    }
    return Jt.create(r);
  }
  _compareString(e) {
    switch (e) {
      case x.GREATER_THAN:
      case x.GREATER_THAN_OR_EQUAL:
        return !0;
      case x.EQUALS:
      case x.LESS_THAN:
      case x.LESS_THAN_OR_EQUAL:
      case x.NOT_EQUAL:
        return !1;
    }
  }
  concatenateFront(e) {
    return this._convertToNumber().concatenateFront(e);
  }
  concatenateBack(e) {
    return this._convertToNumber().concatenateBack(e);
  }
  _convertToNumber() {
    const e = this.getValue();
    let t = 0;
    return e && (t = 1), R.create(t);
  }
  pow(e) {
    return this._convertToNumber().pow(e);
  }
  sqrt() {
    return this._convertToNumber().sqrt();
  }
  cbrt() {
    return this._convertToNumber().cbrt();
  }
  cos() {
    return this._convertToNumber().cos();
  }
  cosh() {
    return this._convertToNumber().cosh();
  }
  acos() {
    return this._convertToNumber().acos();
  }
  acosh() {
    return this._convertToNumber().acosh();
  }
  sin() {
    return this._convertToNumber().sin();
  }
  sinh() {
    return this._convertToNumber().sinh();
  }
  asin() {
    return this._convertToNumber().asin();
  }
  asinh() {
    return this._convertToNumber().asinh();
  }
  tan() {
    return this._convertToNumber().tan();
  }
  tanh() {
    return this._convertToNumber().tanh();
  }
  atan() {
    return this._convertToNumber().atan();
  }
  atan2(e) {
    return this._convertToNumber().atan2(e);
  }
  atanh() {
    return this._convertToNumber().atanh();
  }
  log() {
    return this._convertToNumber().log();
  }
  log10() {
    return this._convertToNumber().log10();
  }
  exp() {
    return this._convertToNumber().exp();
  }
  abs() {
    return this._convertToNumber().abs();
  }
  round(e) {
    return this._convertToNumber().round(e);
  }
  floor(e) {
    return this._convertToNumber().floor(e);
  }
  ceil(e) {
    return this._convertToNumber().ceil(e);
  }
  convertToNumberObjectValue() {
    return Br(this.getValue());
  }
  convertToBooleanObjectValue() {
    return this;
  }
};
A(Jt, "_instanceTrue"), A(Jt, "_instanceFalse");
let T = Jt;
class R extends Gt {
  constructor(e) {
    super(e);
    A(this, "_value", 0);
    this._value = Number(e);
  }
  static create(e, t = "") {
    const r = new R(e);
    return t && r.setPattern(t), r;
  }
  getValue() {
    return this._value;
  }
  setValue(e) {
    this._value = e;
  }
  isNumber() {
    return !0;
  }
  getNegative() {
    return R.create(0).minus(this);
  }
  getReciprocal() {
    return R.create(1).divided(this);
  }
  plus(e) {
    if (e.isArray())
      return e.plus(this);
    let t = e;
    e.isNumber() || (t = e.convertToNumberObjectValue());
    let r = this.plusBy(t.getValue());
    if (r.isError())
      return r;
    const n = tn(this.getPattern(), t.getPattern(), X.PLUS);
    return r = R.create(Number(r.getValue()), n), r;
  }
  equalZero() {
    return this._value === 0;
  }
  minus(e) {
    if (e.isArray()) {
      const s = e.getNegative();
      return s.isError() ? s : s.plus(this);
    }
    let t = e;
    e.isNumber() || (t = e.convertToNumberObjectValue());
    let r = this.minusBy(t.getValue());
    if (r.isError())
      return r;
    const n = tn(this.getPattern(), t.getPattern(), X.MINUS);
    return r = R.create(Number(r.getValue()), n), r;
  }
  multiply(e) {
    if (e.isArray())
      return e.multiply(this);
    let t = e;
    e.isNumber() || (t = e.convertToNumberObjectValue());
    let r = this.multiplyBy(t.getValue());
    if (r.isError())
      return r;
    const n = tn(this.getPattern(), t.getPattern(), X.MULTIPLY);
    return r = R.create(Number(r.getValue()), n), r;
  }
  divided(e) {
    if (e.isArray()) {
      const s = e.getReciprocal();
      return s.isError() ? s : s.multiply(this);
    }
    let t = e;
    e.isNumber() || (t = e.convertToNumberObjectValue());
    let r = this.dividedBy(t.getValue());
    if (r.isError())
      return r;
    const n = tn(this.getPattern(), t.getPattern(), X.DIVIDED);
    return r = R.create(Number(r.getValue()), n), r;
  }
  mod(e) {
    if (e.isArray())
      return e.modInverse(this);
    const t = this.getValue(), r = e.getValue();
    if (e.isNull())
      return g.create(m.DIV_BY_ZERO);
    if (typeof r == "string")
      return g.create(m.VALUE);
    if (typeof r == "number") {
      if (r === 0)
        return g.create(m.DIV_BY_ZERO);
      if (!Number.isFinite(t) || !Number.isFinite(r) || Math.abs(r) * 11259e8 <= Math.abs(t))
        return g.create(m.NUM);
      const n = Mi(t, r);
      return Number.isFinite(n) ? R.create(n) : g.create(m.NUM);
    }
    if (typeof r == "boolean") {
      const n = r ? 1 : 0;
      return n === 0 ? g.create(m.DIV_BY_ZERO) : R.create(Mi(t, n));
    }
    return this;
  }
  concatenateFront(e) {
    return e.isArray() ? e.concatenateBack(this) : U.create(this.concatenate(e.getValue(), lt.FRONT));
  }
  concatenateBack(e) {
    return e.isArray() ? e.concatenateFront(this) : U.create(this.concatenate(e.getValue(), lt.BACK));
  }
  compare(e, t) {
    return e.isArray() ? e.compare(this, Sa(t)) : this.compareBy(e.getValue(), t);
  }
  plusBy(e) {
    if (typeof e == "string" && e.trim() === "")
      return g.create(m.VALUE);
    const t = +this.getValue(), r = +e;
    if (Number.isNaN(t) || Number.isNaN(r))
      return g.create(m.VALUE);
    if (!Number.isFinite(t) || !Number.isFinite(r))
      return g.create(m.NUM);
    const n = Uf(t, r);
    return Number.isFinite(n) ? R.create(n) : g.create(m.NUM);
  }
  minusBy(e) {
    if (typeof e == "string" && e.trim() === "")
      return g.create(m.VALUE);
    const t = +this.getValue(), r = +e;
    if (Number.isNaN(t) || Number.isNaN(r))
      return g.create(m.VALUE);
    if (!Number.isFinite(t) || !Number.isFinite(r))
      return g.create(m.NUM);
    const n = vf(t, r);
    return Number.isFinite(n) ? R.create(n) : g.create(m.NUM);
  }
  multiplyBy(e) {
    if (typeof e == "string" && e.trim() === "")
      return g.create(m.VALUE);
    const t = +this.getValue(), r = +e;
    if (Number.isNaN(t) || Number.isNaN(r))
      return g.create(m.VALUE);
    if (!Number.isFinite(t) || !Number.isFinite(r))
      return g.create(m.NUM);
    const n = Dr(t, r);
    return Number.isFinite(n) ? R.create(n) : g.create(m.NUM);
  }
  dividedBy(e) {
    if (typeof e == "string" && e.trim() === "")
      return g.create(m.VALUE);
    const t = +this.getValue(), r = +e;
    if (Number.isNaN(t) || Number.isNaN(r))
      return g.create(m.VALUE);
    if (!Number.isFinite(t) || !Number.isFinite(r))
      return g.create(m.NUM);
    if (r === 0)
      return g.create(m.DIV_BY_ZERO);
    const n = Tf(t, r);
    return Number.isFinite(n) ? R.create(n) : g.create(m.NUM);
  }
  compareBy(e, t) {
    const r = this.getValue();
    let n = !1;
    return typeof e == "string" ? n = this._compareString(t) : typeof e == "number" ? n = this._compareNumber(r, e, t) : typeof e == "boolean" && (n = this._compareBoolean(t)), T.create(n);
  }
  _compareString(e) {
    switch (e) {
      case x.EQUALS:
      case x.GREATER_THAN:
      case x.GREATER_THAN_OR_EQUAL:
        return !1;
      case x.LESS_THAN:
      case x.LESS_THAN_OR_EQUAL:
      case x.NOT_EQUAL:
        return !0;
    }
  }
  _compareNumber(e, t, r) {
    return !Number.isFinite(e) || !Number.isFinite(t) ? this._compareInfinity(e, t, r) : this._compareFiniteNumber(e, t, r);
  }
  _compareFiniteNumber(e, t, r) {
    switch (r) {
      case x.EQUALS:
        return Di(e, t);
      case x.GREATER_THAN:
        return Ff(e, t);
      case x.GREATER_THAN_OR_EQUAL:
        return kf(e, t);
      case x.LESS_THAN:
        return $f(e, t);
      case x.LESS_THAN_OR_EQUAL:
        return Yf(e, t);
      case x.NOT_EQUAL:
        return !Di(e, t);
    }
  }
  _compareBoolean(e) {
    switch (e) {
      case x.EQUALS:
      case x.GREATER_THAN:
      case x.GREATER_THAN_OR_EQUAL:
        return !1;
      case x.LESS_THAN:
      case x.LESS_THAN_OR_EQUAL:
      case x.NOT_EQUAL:
        return !0;
    }
  }
  pow(e) {
    if (e.isArray())
      return e.powInverse(this);
    if (this.isError())
      return this;
    const t = this.getValue();
    let r = e;
    if (e.isString() && (r = e.convertToNumberObjectValue()), r.isError())
      return r;
    const n = +r.getValue();
    if (Number.isNaN(n))
      return g.create(m.VALUE);
    if (!Number.isFinite(t) || !Number.isFinite(n))
      return g.create(m.NUM);
    if (t === 0)
      return n < 0 ? g.create(m.DIV_BY_ZERO) : n === 0 ? g.create(m.NUM) : R.create(0);
    const s = Bf(t, n);
    return Number.isFinite(s) ? R.create(s) : g.create(m.NUM);
  }
  sqrt() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = If(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  cbrt() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.cbrt(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  cos() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.cos(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  cosh() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.cosh(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  acos() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.acos(e);
    return Number.isNaN(t) ? g.create(m.NUM) : R.create(t);
  }
  acosh() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.acosh(e);
    return Number.isNaN(t) ? g.create(m.NUM) : R.create(t);
  }
  sin() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.sin(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  sinh() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.sinh(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  asin() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.asin(e);
    return Number.isNaN(t) ? g.create(m.NUM) : R.create(t);
  }
  asinh() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.asinh(e);
    return Number.isNaN(t) ? g.create(m.NUM) : R.create(t);
  }
  tan() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.tan(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  tanh() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.tanh(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  atan() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.atan(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  atan2(e) {
    if (e.isArray())
      return e.atan2Inverse(this);
    const t = this.getValue(), r = e.getValue();
    if (typeof r == "string")
      return g.create(m.VALUE);
    if (typeof r == "number") {
      if (!Number.isFinite(t) || !Number.isFinite(r))
        return g.create(m.NUM);
      if (t === 0 && r === 0)
        return g.create(m.DIV_BY_ZERO);
      const n = Math.atan2(t, r);
      return Number.isFinite(n) ? R.create(n) : g.create(m.NUM);
    }
    return typeof r == "boolean" ? R.create(Math.atan2(t, r ? 1 : 0)) : this;
  }
  atanh() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.atanh(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  log() {
    const e = this.getValue();
    if (typeof e == "number" && e <= 0 || !Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.log(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  log10() {
    const e = this.getValue();
    if (typeof e == "number" && e <= 0 || !Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.log10(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  exp() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.exp(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  abs() {
    const e = this.getValue();
    if (!Number.isFinite(e))
      return g.create(m.NUM);
    const t = Math.abs(e);
    return Number.isFinite(t) ? R.create(t) : g.create(m.NUM);
  }
  round(e) {
    if (e.isArray())
      return e.roundInverse(this);
    const t = this.getValue(), r = e.getValue();
    if (typeof r == "string")
      return g.create(m.VALUE);
    if (typeof r == "number") {
      if (!Number.isFinite(t) || !Number.isFinite(r))
        return g.create(m.NUM);
      const n = t < 0 ? -Ft(Math.abs(t), r) : Ft(t, r);
      return Number.isFinite(n) ? R.create(n) : g.create(m.NUM);
    }
    return typeof r == "boolean" ? R.create(Ft(t, r ? 1 : 0)) : this;
  }
  floor(e) {
    if (e.isArray())
      return e.floorInverse(this);
    const t = this.getValue(), r = e.getValue();
    if (typeof r == "string")
      return g.create(m.VALUE);
    if (typeof r == "number") {
      if (!Number.isFinite(t) || !Number.isFinite(r))
        return g.create(m.NUM);
      const n = t < 0 ? -Xe(Math.abs(t), r) : Xe(t, r);
      return Number.isFinite(n) ? R.create(n) : g.create(m.NUM);
    }
    return typeof r == "boolean" ? R.create(Xe(t, r ? 1 : 0)) : this;
  }
  ceil(e) {
    if (e.isArray())
      return e.ceilInverse(this);
    const t = this.getValue(), r = e.getValue();
    if (typeof r == "string")
      return g.create(m.VALUE);
    if (typeof r == "number") {
      if (!Number.isFinite(t) || !Number.isFinite(r))
        return g.create(m.NUM);
      const n = t < 0 ? -Qe(Math.abs(t), r) : Qe(t, r);
      return Number.isFinite(n) ? R.create(n) : g.create(m.NUM);
    }
    return typeof r == "boolean" ? R.create(Qe(t, r ? 1 : 0)) : this;
  }
  convertToNumberObjectValue() {
    return this;
  }
  convertToBooleanObjectValue() {
    return zn(!0);
  }
  _compareInfinity(e, t, r) {
    let n = !1;
    switch (r) {
      case x.EQUALS:
        n = e === t;
        break;
      case x.GREATER_THAN:
        n = e > t;
        break;
      case x.GREATER_THAN_OR_EQUAL:
        n = e >= t;
        break;
      case x.LESS_THAN:
        n = e < t;
        break;
      case x.LESS_THAN_OR_EQUAL:
        n = e <= t;
        break;
      case x.NOT_EQUAL:
        n = e !== t;
        break;
    }
    return n;
  }
}
const Gf = 1e5, Zs = new Dt(Gf);
class U extends Gt {
  constructor(e) {
    super(e);
    A(this, "_value");
    A(this, "_isHyperlink", !1);
    A(this, "_hyperlinkUrl", "");
    this._value = e;
  }
  static create(e, t) {
    var s;
    const r = Zs.get(e);
    if (r && t && this.checkCacheByOptions(r, t))
      return r;
    const n = new U(e);
    return t != null && t.isHyperlink && (n._isHyperlink = t.isHyperlink, n._hyperlinkUrl = (s = t.hyperlinkUrl) != null ? s : ""), Zs.set(e, n), n;
  }
  static checkCacheByOptions(e, t) {
    return !(e.isHyperlink() !== t.isHyperlink || e.getHyperlinkUrl() !== t.hyperlinkUrl);
  }
  getValue() {
    return this._value;
  }
  isString() {
    return !0;
  }
  isHyperlink() {
    return this._isHyperlink;
  }
  getHyperlinkUrl() {
    return this._hyperlinkUrl;
  }
  concatenateFront(e) {
    return e.isArray() ? e.concatenateBack(this) : U.create(this.concatenate(e.getValue(), lt.FRONT));
  }
  concatenateBack(e) {
    return e.isArray() ? e.concatenateFront(this) : U.create(this.concatenate(e.getValue(), lt.BACK));
  }
  plus(e) {
    return this.convertToNumberObjectValue().plus(e);
  }
  minus(e) {
    return this.convertToNumberObjectValue().minus(e);
  }
  multiply(e) {
    return this.convertToNumberObjectValue().multiply(e);
  }
  divided(e) {
    return this.convertToNumberObjectValue().divided(e);
  }
  compare(e, t, r) {
    return e.isArray() ? e.compare(this, Sa(t), r) : this.compareBy(e.getValue(), t, r);
  }
  compareBy(e, t, r = !1) {
    let n = this.getValue(), s = !1;
    if (typeof e == "string") {
      let a = e;
      if (r || (n = n.toLocaleLowerCase(), a = a.toLocaleLowerCase()), Mr(a))
        return this._checkWildcard(a, t);
      s = this._compareString(n, a, t);
    } else typeof e == "number" ? s = this._compareNumber(t) : typeof e == "boolean" && (s = this._compareBoolean(t));
    return T.create(s);
  }
  _compareString(e, t, r) {
    switch (r) {
      case x.EQUALS:
        return e === t;
      case x.GREATER_THAN:
        return e > t;
      case x.GREATER_THAN_OR_EQUAL:
        return e >= t;
      case x.LESS_THAN:
        return e < t;
      case x.LESS_THAN_OR_EQUAL:
        return e <= t;
      case x.NOT_EQUAL:
        return e !== t;
    }
  }
  _compareNumber(e) {
    switch (e) {
      case x.NOT_EQUAL:
      case x.GREATER_THAN:
      case x.GREATER_THAN_OR_EQUAL:
        return !0;
      case x.EQUALS:
      case x.LESS_THAN:
      case x.LESS_THAN_OR_EQUAL:
        return !1;
    }
  }
  _compareBoolean(e) {
    switch (e) {
      case x.EQUALS:
      case x.GREATER_THAN:
      case x.GREATER_THAN_OR_EQUAL:
        return !1;
      case x.LESS_THAN:
      case x.LESS_THAN_OR_EQUAL:
      case x.NOT_EQUAL:
        return !0;
    }
  }
  convertToNumberObjectValue() {
    const e = this.getValue(), t = dc(e);
    return t && t.z ? Br(t.v, t.z) : Br(e);
  }
  convertToBooleanObjectValue() {
    return T.create(!0);
  }
  _checkWildcard(e, t) {
    const r = this.getValue().toLocaleLowerCase(), n = Il(r, e, t);
    return T.create(n);
  }
}
function zn(o) {
  if (typeof o == "boolean")
    return T.create(o);
  let i = !1;
  if (typeof o == "string") {
    const e = o.toLocaleUpperCase();
    e === ar.TRUE ? i = !0 : e === ar.FALSE && (i = !1);
  } else
    o === 1 ? i = !0 : i = !1;
  return T.create(i);
}
function Wf(o) {
  let i = o.toString();
  return i.charAt(0) === '"' && i.charAt(i.length - 1) === '"' && (i = i.slice(1, -1), i = i.replace(/""/g, '"')), U.create(i);
}
function Br(o, i = "") {
  if (typeof o == "boolean") {
    let e = 0;
    return o && (e = 1), R.create(e, i);
  } else {
    if (typeof o == "number")
      return Number.isFinite(o) ? R.create(o, i) : g.create(m.NUM);
    if (q(o))
      return R.create(Number(o), i);
  }
  return g.create(m.VALUE);
}
function qf(o) {
  return "";
}
function $o(o = [], i = !1) {
  const e = [];
  for (let t = 0; t < o.length; t++) {
    const r = o[t];
    e[t] == null && (e[t] = []);
    for (let n = 0; n < r.length; n++) {
      const s = r[n];
      e[t][n] = Mt.create(s, i);
    }
  }
  return e;
}
function Kf(o = []) {
  const i = [];
  for (let e = 0; e < o.length; e++) {
    const t = o[e];
    i[e] == null && (i[e] = []);
    for (let r = 0; r < t.length; r++) {
      const n = t[r];
      n == null ? i[e][r] = null : n.isError() ? i[e][r] = n.getErrorType() : i[e][r] = n.getValue();
    }
  }
  return i;
}
class W extends Gt {
  constructor(e) {
    super(typeof e == "string" ? e : qf());
    A(this, "_values", []);
    A(this, "_numericData", null);
    A(this, "_rowCount", -1);
    A(this, "_columnCount", -1);
    A(this, "_unitId", "");
    A(this, "_sheetId", "");
    A(this, "_currentRow", -1);
    A(this, "_currentColumn", -1);
    A(this, "_sliceCache", /* @__PURE__ */ new Map());
    A(this, "_flattenCache");
    A(this, "_sortedNumericAsc");
    A(this, "_sortedNumericDesc");
    A(this, "_equalSearchFirstIndex");
    A(this, "_equalSearchLastIndex");
    /**
     * The default value of the array, null values in comparison results support setting to false
     */
    A(this, "_defaultValue", null);
    A(this, "_flattenPosition");
    this._values = this._formatValue(e);
  }
  /**
   * Create an array value object based on the string or IArrayValueObject data.
   * @param rawValue
   * @returns
   */
  static create(e) {
    return new W(e);
  }
  /**
   * Create an array value object based on the array data.
   * @param array
   * @returns
   */
  static createByArray(e) {
    const r = {
      calculateValueList: $o(e),
      rowCount: e.length,
      columnCount: e[0].length || 0,
      unitId: "",
      sheetId: "",
      row: -1,
      column: -1
    };
    return new W(r);
  }
  static createNumberArray(e, t, r, n = "", s = "", a = -1, u = -1) {
    const c = new W({ calculateValueList: [], rowCount: t, columnCount: r, unitId: n, sheetId: s, row: a, column: u });
    return c._values = [], c._numericData = e, c;
  }
  dispose() {
    this._values = [], this._numericData = null, this._defaultValue = null, this._flattenPosition = null, this._clearCache();
  }
  isNumericArray() {
    return this._numericData !== null;
  }
  getNumericData() {
    return this._numericData;
  }
  getSortedNumericValues(e) {
    if (e) {
      if (this._sortedNumericDesc) return this._sortedNumericDesc;
    } else if (this._sortedNumericAsc) return this._sortedNumericAsc;
    let t;
    if (this._numericData) {
      t = [];
      for (let n = 0; n < this._numericData.length; n++) {
        const s = this._numericData[n];
        Number.isFinite(s) && t.push(s);
      }
    } else {
      t = [];
      for (let n = 0; n < this._rowCount; n++) {
        const s = this._values[n];
        if (s)
          for (let a = 0; a < this._columnCount; a++) {
            const u = s[a];
            if (!u || u.isNull()) continue;
            if (u.isError()) return null;
            if (u.isBoolean() || u.isString()) continue;
            const c = u.getValue();
            Number.isFinite(c) && t.push(c);
          }
      }
    }
    if (t.length === 0) return null;
    const r = [...t].sort((n, s) => n - s);
    return this._sortedNumericAsc = r, this._sortedNumericDesc = [...r].reverse(), e ? this._sortedNumericDesc : this._sortedNumericAsc;
  }
  getEqualSearchIndex(e) {
    const t = e ? this._equalSearchFirstIndex : this._equalSearchLastIndex;
    if (t) return t;
    const r = /* @__PURE__ */ new Map(), n = this._rowCount, s = this._columnCount;
    if (e) {
      for (let a = n - 1; a >= 0; a--)
        for (let u = s - 1; u >= 0; u--) {
          const c = this._getRawValue(a, u);
          c !== null && r.set(c, { row: a, column: u });
        }
      this._equalSearchFirstIndex = r;
    } else {
      for (let a = 0; a < n; a++)
        for (let u = 0; u < s; u++) {
          const c = this._getRawValue(a, u);
          c !== null && r.set(c, { row: a, column: u });
        }
      this._equalSearchLastIndex = r;
    }
    return r;
  }
  _getRawValue(e, t) {
    var n;
    if (this._numericData !== null)
      return this._numericData[e * this._columnCount + t];
    const r = (n = this._values[e]) == null ? void 0 : n[t];
    return !r || r.isNull() || r.isError() ? null : r.getValue();
  }
  getNumberDirect(e, t) {
    var n;
    if (this._numericData !== null)
      return this._numericData[e * this._columnCount + t];
    const r = (n = this._values[e]) == null ? void 0 : n[t];
    return r ? r.getValue() : 0;
  }
  rawCompare(e, t, r, n) {
    var u;
    if (this._numericData !== null) {
      const c = this._numericData[e * this._columnCount + t];
      return typeof r == "number" ? this._rawCompareNumbers(c, r, n) : n === x.NOT_EQUAL;
    }
    const s = (u = this._values[e]) == null ? void 0 : u[t];
    if (!s || s.isNull() || s.isError()) return !1;
    const a = s.getValue();
    if (typeof a == typeof r) {
      if (typeof a == "string" && typeof r == "string")
        return this._rawCompareStrings(a.toLocaleLowerCase(), r.toLocaleLowerCase(), n);
      if (typeof a == "number" && typeof r == "number")
        return this._rawCompareNumbers(a, r, n);
    }
    return n === x.NOT_EQUAL;
  }
  _rawCompareNumbers(e, t, r) {
    switch (r) {
      case x.EQUALS:
        return e === t;
      case x.NOT_EQUAL:
        return e !== t;
      case x.GREATER_THAN:
        return e > t;
      case x.GREATER_THAN_OR_EQUAL:
        return e >= t;
      case x.LESS_THAN:
        return e < t;
      case x.LESS_THAN_OR_EQUAL:
        return e <= t;
    }
  }
  _rawCompareStrings(e, t, r) {
    switch (r) {
      case x.EQUALS:
        return e === t;
      case x.NOT_EQUAL:
        return e !== t;
      case x.GREATER_THAN:
        return e > t;
      case x.GREATER_THAN_OR_EQUAL:
        return e >= t;
      case x.LESS_THAN:
        return e < t;
      case x.LESS_THAN_OR_EQUAL:
        return e <= t;
    }
  }
  _materialize() {
    if (this._numericData === null)
      return;
    const e = this._numericData, t = this._rowCount, r = this._columnCount, n = new Array(t);
    for (let s = 0; s < t; s++) {
      const a = new Array(r), u = s * r;
      for (let c = 0; c < r; c++)
        a[c] = R.create(e[u + c]);
      n[s] = a;
    }
    this._values = n, this._numericData = null;
  }
  clone() {
    return this.map((e) => e);
  }
  getRowCount() {
    return this._rowCount;
  }
  setRowCount(e) {
    this._rowCount = e;
  }
  getColumnCount() {
    return this._columnCount;
  }
  setColumnCount(e) {
    this._columnCount = e;
  }
  setCurrent(e, t) {
    this._currentRow = e, this._currentColumn = t;
  }
  setUnitId(e) {
    this._unitId = e;
  }
  getUnitId() {
    return this._unitId;
  }
  setSheetId(e) {
    this._sheetId = e;
  }
  getSheetId() {
    return this._sheetId;
  }
  getCurrentRow() {
    return this._currentRow;
  }
  getCurrentColumn() {
    return this._currentColumn;
  }
  getArrayValue() {
    return this._materialize(), this._values;
  }
  setArrayValue(e) {
    this._clearCache(), this._numericData = null, this._values = e;
  }
  isArray() {
    return !0;
  }
  setDefaultValue(e) {
    this._defaultValue = e;
  }
  get(e, t) {
    var r;
    if (this._numericData !== null) {
      const n = e * this._columnCount + t;
      return n >= 0 && n < this._numericData.length ? R.create(this._numericData[n]) : this._defaultValue;
    }
    return ((r = this._values[e]) == null ? void 0 : r[t]) || this._defaultValue;
  }
  getRealValue(e, t) {
    if (this._numericData !== null) {
      const s = e * this._columnCount + t;
      return s >= 0 && s < this._numericData.length ? R.create(this._numericData[s]) : null;
    }
    const r = this._values[e];
    if (r == null)
      return null;
    const n = r[t];
    return n == null ? null : n;
  }
  getValueOrDefault(e, t) {
    return this.get(e, t) || this._defaultValue;
  }
  set(e, t, r) {
    if (e >= this._rowCount || t >= this._columnCount)
      throw new Error("Exceeding array bounds.");
    this._materialize(), this._clearCache(), this._values[e][t] = r;
  }
  getRangePosition() {
    const t = this.getRowCount(), r = 0, n = this.getColumnCount();
    return {
      startRow: 0,
      endRow: t - 1,
      startColumn: r,
      endColumn: n - 1
    };
  }
  iterator(e) {
    var u;
    const { startRow: t, endRow: r, startColumn: n, endColumn: s } = this.getRangePosition();
    if (this._numericData !== null) {
      const c = this._numericData, l = this._columnCount;
      for (let f = t; f <= r; f++) {
        const h = f * l;
        for (let d = n; d <= s; d++)
          if (e(R.create(c[h + d]), f, d) === !1)
            return;
      }
      return;
    }
    const a = this._values;
    for (let c = t; c <= r; c++)
      for (let l = n; l <= s; l++)
        if (e(((u = a[c]) == null ? void 0 : u[l]) || this._defaultValue, c, l) === !1)
          return;
  }
  iteratorReverse(e) {
    var u;
    const { startRow: t, endRow: r, startColumn: n, endColumn: s } = this.getRangePosition();
    if (this._numericData !== null) {
      const c = this._numericData, l = this._columnCount;
      for (let f = r; f >= t; f--) {
        const h = f * l;
        for (let d = s; d >= n; d--)
          if (e(R.create(c[h + d]), f, d) === !1)
            return;
      }
      return;
    }
    const a = this._values;
    for (let c = r; c >= t; c--)
      for (let l = s; l >= n; l--)
        if (e(((u = a[c]) == null ? void 0 : u[l]) || this._defaultValue, c, l) === !1)
          return;
  }
  getLastTruePosition() {
    let e;
    return this.iteratorReverse((t, r, n) => {
      if (t != null && t.isBoolean() && t.getValue() === !0)
        return e = {
          row: r,
          column: n
        }, !1;
    }), e;
  }
  getFirstTruePosition() {
    let e;
    return this.iterator((t, r, n) => {
      if (t != null && t.isBoolean() && t.getValue() === !0)
        return e = {
          row: r,
          column: n
        }, !1;
    }), e;
  }
  getFirstCell() {
    const { startRow: e, startColumn: t } = this.getRangePosition();
    return this.get(e, t) || this._defaultValue || ue.create();
  }
  getLastCell() {
    const { endRow: e, endColumn: t } = this.getRangePosition();
    return this.get(e, t) || this._defaultValue || ue.create();
  }
  /**
   * Referring to matrix calculations,
   * extract the matching values from a true/false matrix based on parameters and store them in a two-dimensional array.
   * implement x[x<10]
   * https://numpy.org/doc/stable/user/basics.indexing.html
   * @param takeArray
   */
  pick(e) {
    const t = this.pickRaw(e);
    return this._createNewArray(t, 1, t[0].length);
  }
  pickRaw(e) {
    const t = e.getRowCount(), r = e.getColumnCount();
    if (t !== this._rowCount || r !== this._columnCount)
      return [[ue.create()]];
    const n = [];
    n[0] = [];
    for (let s = 0; s < t; s++)
      for (let a = 0; a < r; a++) {
        const u = e.get(s, a);
        if (u == null || u.isError())
          continue;
        const c = u.getValue();
        if (c === !0 || c === 1) {
          const l = this.get(s, a);
          n[0].push(l);
        }
      }
    return n;
  }
  /**
   * Flatten a 2D array.
   * https://numpy.org/doc/stable/reference/generated/numpy.chararray.flatten.html#numpy.chararray.flatten
   */
  flatten() {
    if (this._flattenCache != null)
      return this._flattenCache;
    const e = [];
    e[0] = [];
    for (let r = 0; r < this._rowCount; r++)
      for (let n = 0; n < this._columnCount; n++) {
        const s = this.get(r, n);
        e[0].push(s);
      }
    const t = this._createNewArray(e, 1, e[0].length);
    return t.setDefaultValue(this._defaultValue), this._flattenCache = t, t;
  }
  /**
   * Flatten a 2D array.
   * In Excel, errors and blank cells are ignored, which results in a binary search that cannot strictly adhere to the number of cells.
   */
  flattenPosition() {
    if (this._flattenPosition != null)
      return this._flattenPosition;
    const e = [], t = [], r = [], n = [];
    let s = 0;
    for (let u = 0; u < this._rowCount; u++)
      for (let c = 0; c < this._columnCount; c++) {
        const l = this.get(u, c);
        if (l == null || l.isError() || l.isNull()) {
          s++;
          continue;
        }
        l.isString() ? (e.push(l), r.push(s++)) : (t.push(l), n.push(s++));
      }
    const a = {
      stringArray: e,
      numberArray: t,
      stringPosition: r,
      numberPosition: n
    };
    return this._flattenPosition = a, a;
  }
  /**
   * I'm looking to perform slicing operations on 2D arrays, similar to the functionality provided by NumPy.
   * https://numpy.org/doc/stable/user/basics.indexing.html
   * @rowParam start:stop:step
   * @columnParam start:stop:step
   * @param takeArray
   */
  slice(e, t) {
    let r = 0, n = this._rowCount, s = 1, a = 0, u = this._columnCount, c = 1;
    if (e != null && (r = e[0] || 0, n = e[1] || this._rowCount, s = e[2] || 1), t != null && (a = t[0] || 0, u = t[1] || this._columnCount, c = t[2] || 1), r >= this._rowCount || a >= this._columnCount)
      return;
    const l = `${r}_${n}_${s}_${a}_${u}_${c}`, f = this._sliceCache.get(l);
    if (f != null)
      return f;
    const h = [], d = this._values;
    let _ = 0, C = 0;
    for (let p = r; p < n; p += s) {
      C = 0, h[_] == null && (h[_] = []);
      for (let N = a; N < u; N += c) {
        if (!d[p])
          return;
        let w = d[p][N] || this._defaultValue;
        w == null && (w = ue.create()), h[_][C] = w, C++;
      }
      _++;
    }
    if (h.length === 0 || h[0].length === 0)
      return;
    const E = s > 1 ? -1 : r + this._currentRow, b = c > 1 ? -1 : a + this._currentColumn, y = this._createNewArray(h, h.length, h[0].length, E, b);
    return y.setDefaultValue(this._defaultValue), this._sliceCache.set(l, y), y;
  }
  sortByRow(e) {
    const t = this._transposeArray(this._values);
    t.sort(this._sort(e)), this._clearCache(), this._values = this._transposeArray(t);
  }
  sortByColumn(e) {
    this._clearCache(), this._values.sort(this._sort(e));
  }
  transpose() {
    const e = this._transposeArray(this._values), t = this._rowCount, r = this._columnCount, n = this._createNewArray(e, r, t);
    return n.setDefaultValue(this._defaultValue), n;
  }
  /**
   * Due to the inability to effectively utilize the cache,
   * the sequential matching approach is only used for special matches in XLOOKUP and XMATCH.
   * For example, when match_mode is set to 1 and -1 for an exact match. If not found, it returns the next smaller item.
   */
  orderSearch(e, t = Se.MIN, r = !1, n = !1) {
    if (t === Se.NORMAL && !n) {
      const f = e.getValue();
      return this.getEqualSearchIndex(!0).get(f) || void 0;
    }
    let s, a, u, c;
    const l = (f, h, d) => {
      if (f == null)
        return !0;
      let _;
      if (n === !0 ? _ = f.compare(e, x.EQUALS) : _ = f.isEqual(e), (_ == null ? void 0 : _.getValue()) === !0)
        return s = f, u = { row: h, column: d }, !1;
      t === Se.MAX ? f.isGreaterThan(e).getValue() === !0 && (a == null || f.minus(e).abs().isLessThanOrEqual(a.minus(e).abs()).getValue() === !0) && (a = f, c = { row: h, column: d }) : t === Se.MIN && f.isLessThan(e).getValue() === !0 && (a == null || f.minus(e).abs().isLessThanOrEqual(a.minus(e).abs()).getValue() === !0) && (a = f, c = { row: h, column: d });
    };
    if (r ? this.iteratorReverse((f, h, d) => l(f, h, d)) : this.iterator((f, h, d) => l(f, h, d)), s != null)
      return u;
    if (a != null)
      return c;
  }
  binarySearch(e, t = zt.MIN, r = Se.MIN) {
    if (e.isError())
      return;
    const { stringArray: n, stringPosition: s, numberArray: a, numberPosition: u } = this.flattenPosition();
    return e.isString() ? this._binarySearch(e, n, s, t, r) : this._binarySearch(e, a, u, t, r);
  }
  /**
   * searchType defaults to ascending order
   *
   * matchType defaults to the maximum value less than the search value, which is used for the default matching mode of VLOOKUP/LOOKUP/HLOOKUP.
   * @param valueObject
   * @param searchArray
   * @param positionArray
   * @param searchType
   * @param matchType
   * @returns
   */
  _binarySearch(e, t, r, n = zt.MIN, s = Se.MIN) {
    const a = Rn(), u = Number(e.getValue()), c = !Number.isNaN(u);
    let l = 0, f = t.length - 1, h = -1, d = -1, _ = -1;
    for (; l <= f; ) {
      const C = Math.floor((l + f) / 2), E = t[C];
      let b;
      if (E.isNull())
        b = n === zt.MIN ? 1 : -1;
      else {
        const y = E.getValue();
        if (c) {
          const p = Number(y);
          b = Number.isNaN(p) ? 1 : Math.sign(p - u);
        } else
          b = a(y.toString().toLocaleLowerCase(), e.getValue().toString().toLocaleLowerCase());
      }
      if (n === zt.MAX && (b = -b), b === 0) {
        h = C;
        break;
      }
      b < 0 ? (d = C, l = C + 1) : (_ = C, f = C - 1);
    }
    if (s === Se.NORMAL)
      return h !== -1 ? r[h] : void 0;
    if (s === Se.MIN)
      return h !== -1 ? r[h] : n === zt.MIN ? r[d] : r[_];
    if (s === Se.MAX)
      return h !== -1 ? r[h] : n === zt.MIN ? r[_] : r[d];
  }
  sum() {
    if (this._numericData !== null) {
      const t = this._numericData;
      let r = 0;
      for (let n = 0, s = t.length; n < s; n++)
        r += t[n];
      return R.create(r);
    }
    let e = R.create(0);
    return this.iterator((t) => {
      if (t == null || t.isString() || t.isBoolean() || t.isNull())
        return !0;
      if (t.isError())
        return e = t, !1;
      e = e.plus(
        t
      );
    }), e;
  }
  max() {
    let e = R.create(Number.NEGATIVE_INFINITY);
    return this.iterator((t) => {
      if (t == null)
        return !0;
      if (t.isError())
        return e = t, !1;
      if (t.isString() || t.isNull() || t.isBoolean())
        return !0;
      e.isLessThan(t).getValue() && (e = t);
    }), e;
  }
  min() {
    let e = R.create(Number.POSITIVE_INFINITY);
    return this.iterator((t) => {
      if (t == null)
        return !0;
      if (t.isError())
        return e = t, !1;
      if (t.isString() || t.isNull() || t.isBoolean())
        return !0;
      e.isGreaterThan(t).getValue() && (e = t);
    }), e;
  }
  count() {
    let e = R.create(0);
    return this.iterator((t) => {
      if (t == null || t.isError() || t.isString() || t.isNull() || t.isBoolean())
        return !0;
      e = e.plusBy(1);
    }), e;
  }
  countA() {
    let e = R.create(0);
    return this.iterator((t) => {
      if (t == null || t.isNull())
        return !0;
      e = e.plusBy(1);
    }), e;
  }
  countBlank() {
    let e = R.create(0);
    return this.iterator((t) => {
      (t == null || t.isNull() || t.getValue() === "") && (e = e.plusBy(1));
    }), e;
  }
  getNegative() {
    return W.create("{0}").minus(this);
  }
  getReciprocal() {
    return W.create("{1}").divided(this);
  }
  plus(e) {
    return this._batchOperator(
      e,
      1
      /* PLUS */
    );
  }
  minus(e) {
    return this._batchOperator(
      e,
      0
      /* MINUS */
    );
  }
  multiply(e) {
    return this._batchOperator(
      e,
      2
      /* MULTIPLY */
    );
  }
  divided(e) {
    return this._batchOperator(
      e,
      3
      /* DIVIDED */
    );
  }
  mod(e) {
    return this._batchOperator(
      e,
      4
      /* MOD */
    );
  }
  modInverse(e) {
    return this.map((t) => t.isError() ? t : e.mod(t));
  }
  compare(e, t, r) {
    return this._batchOperator(e, 5, t, r);
  }
  concatenateFront(e) {
    return this._batchOperator(
      e,
      6
      /* CONCATENATE_FRONT */
    );
  }
  concatenateBack(e) {
    return this._batchOperator(
      e,
      7
      /* CONCATENATE_BACK */
    );
  }
  map(e) {
    const t = (r, n, s) => r == null ? ue.create() : r.isError() ? r : e(r, n, s);
    return this.mapValue(t);
  }
  // READNOW: MEMORY HOTSPOT #1 — mapValue allocates a full BaseValueObject[][]
  // for every formula operation (comparison, arithmetic, etc.). With SUMPRODUCT
  // over 10K rows, each sub-expression creates a 10K-element array. Nested
  // operations (e.g. (A=B)*(C*D)) compound: each * and = creates its own copy.
  // For 10K formulas each scanning 10K rows, this produces ~100M+ array objects.
  // Root cause of OOM in ArrayValueObject allocation (99% of heap in profiling).
  mapValue(e) {
    var s;
    const t = this._rowCount, r = this._columnCount, n = new Array(t);
    if (this._numericData !== null) {
      const a = this._numericData;
      for (let u = 0; u < t; u++) {
        const c = new Array(r), l = u * r;
        for (let f = 0; f < r; f++)
          c[f] = e(R.create(a[l + f]), u, f);
        n[u] = c;
      }
      return this._createNewArray(n, t, r);
    }
    for (let a = 0; a < t; a++) {
      const u = new Array(r);
      for (let c = 0; c < r; c++) {
        const l = (s = this._values) == null ? void 0 : s[a];
        if (l == null)
          u[c] = g.create(m.VALUE);
        else {
          const f = l[c] || this._defaultValue;
          f ? u[c] = e(f, a, c) : u[c] = ue.create();
        }
      }
      n[a] = u;
    }
    return this._createNewArray(n, t, r);
  }
  pow(e) {
    return this._batchOperator(
      e,
      8
      /* POW */
    );
  }
  /**
   *
   * @param valueObject In the case of an inverse, it is certainly not an array.
   * @returns
   */
  powInverse(e) {
    return this.map((t) => t.isError() ? t : e.pow(t));
  }
  sqrt() {
    return this.map((e) => e.isError() ? e : e.sqrt());
  }
  cbrt() {
    return this.map((e) => e.isError() ? e : e.cbrt());
  }
  cos() {
    return this.map((e) => e.isError() ? e : e.cos());
  }
  cosh() {
    return this.map((e) => e.isError() ? e : e.cosh());
  }
  acos() {
    return this.map((e) => e.isError() ? e : e.acos());
  }
  acosh() {
    return this.map((e) => e.isError() ? e : e.acosh());
  }
  sin() {
    return this.map((e) => e.isError() ? e : e.sin());
  }
  sinh() {
    return this.map((e) => e.isError() ? e : e.sinh());
  }
  asin() {
    return this.map((e) => e.isError() ? e : e.asin());
  }
  asinh() {
    return this.map((e) => e.isError() ? e : e.asinh());
  }
  tan() {
    return this.map((e) => e.isError() ? e : e.tan());
  }
  tanh() {
    return this.map((e) => e.isError() ? e : e.tanh());
  }
  atan() {
    return this.map((e) => e.isError() ? e : e.atan());
  }
  atanh() {
    return this.map((e) => e.isError() ? e : e.atanh());
  }
  atan2(e) {
    return this._batchOperator(
      e,
      12
      /* ATAN2 */
    );
  }
  atan2Inverse(e) {
    return this.map((t) => t.isError() ? t : e.atan2(t));
  }
  mean(e = 0) {
    const t = this.sum(), r = this.count();
    return t.divided(e === 0 ? r : r.minusBy(1));
  }
  median() {
    const e = this.flattenPosition().numberArray, t = this._createNewArray([e], 1, e.length), r = t.getColumnCount();
    if (r <= 1)
      return t.get(0, 0) || ue.create();
    if (t.sortByRow(0), r % 2 === 0) {
      const n = t.get(0, r / 2) || ue.create(), s = t.get(0, r / 2 - 1) || ue.create();
      return n.plus(s).divided(R.create(2));
    }
    return t.get(0, (r - 1) / 2) || ue.create();
  }
  /**
   * ┌──────────────┬────────────────────────────────┬───────────────────┐
   * │ Function     │ Ignore logical values and text │ Type              │
   * ├──────────────┼────────────────────────────────┼───────────────────┤
   * │ VAR.S (VAR)  │ TRUE                           │ sample            │
   * │ VAR.P (VARP) │ TRUE                           │ entire population │
   * │ VARA         │ FALSE                          │ sample            │
   * │ VARPA        │ FALSE                          │ entire population │
   * └──────────────┴────────────────────────────────┴───────────────────┘
   *
   * for VARPA and VARA, strings and FALSE are counted as 0, TRUE is counted as 1
   * for VAR.S/VAR, or VAR.P/VARP, strings,TRUE and FALSE are ignored
   * Since sum ignores strings and booleans, they are ignored here too, and VAR.S and VAR.P are used more
   *
   * VAR.S assumes that its arguments are a sample of the population, like numpy.var(data, ddof=1)
   * VAR.P assumes that its arguments are the entire population, like numpy.var(data, ddof=0)
   * numpy.var uses ddof=0 (Delta Degrees of Freedom) by default, so we use ddof=0 here
   *
   */
  var(e = 0) {
    const t = this.mean(), r = [[]];
    this.iterator((l) => {
      if (l == null || l.isError() || l.isString() || l.isBoolean() || l.isNull())
        return;
      const f = l.minus(t).pow(R.create(2));
      f.isError() || r[0].push(f);
    });
    const { _unitId: n, _sheetId: s, _currentRow: a, _currentColumn: u } = this;
    return W.create({
      calculateValueList: r,
      rowCount: 1,
      columnCount: r[0].length,
      unitId: n,
      sheetId: s,
      row: a,
      column: u
    }).mean(e);
  }
  /**
   * STDEV.P (STDEVP): ddof=0, ignore strings and booleans
   * STDEV.S (STDEV): ddof=1, ignore strings and booleans
   *
   * STDEVPA: ddof=0,
   * STDEVA: ddof=1,
   * @returns
   */
  std(e = 0) {
    const t = this.var(e);
    return t.isError() ? t : t.sqrt();
  }
  log() {
    return this.map((e) => e.isError() ? e : e.log());
  }
  log10() {
    return this.map((e) => e.isError() ? e : e.log10());
  }
  exp() {
    return this.map((e) => e.isError() ? e : e.exp());
  }
  abs() {
    return this.map((e) => e.isError() ? e : e.abs());
  }
  round(e) {
    return this._batchOperator(
      e,
      9
      /* ROUND */
    );
  }
  roundInverse(e) {
    return this.map((t) => t.isError() ? t : e.round(t));
  }
  floor(e) {
    return this._batchOperator(
      e,
      10
      /* FLOOR */
    );
  }
  floorInverse(e) {
    return this.map((t) => t.isError() ? t : e.floor(t));
  }
  ceil(e) {
    return this._batchOperator(
      e,
      11
      /* CEIL */
    );
  }
  ceilInverse(e) {
    return this.map((t) => t.isError() ? t : e.ceil(t));
  }
  toValue() {
    return Kf(this._values);
  }
  _clearCache() {
    this._flattenCache = null, this._sortedNumericAsc = null, this._sortedNumericDesc = null, this._equalSearchFirstIndex = null, this._equalSearchLastIndex = null, this._sliceCache.clear();
  }
  _sort(e) {
    const t = Rn();
    return (r, n) => {
      const s = r[e], a = n[e];
      return s == null ? 1 : a == null ? -1 : s.isError() && s.isError() ? 0 : s.isError() ? 1 : a.isError() ? -1 : t(
        s.getValue(),
        a.getValue()
      );
    };
  }
  _transposeArray(e) {
    const t = e.length, r = e[0].length, n = [];
    for (let s = 0; s < r; s++) {
      n[s] = [];
      for (let a = 0; a < t; a++)
        n[s][a] = e[a][s];
    }
    return n;
  }
  _extractNumericData() {
    if (this._numericData !== null) return this._numericData;
    const e = this._rowCount, t = this._columnCount, r = e * t, n = new Float64Array(r);
    for (let s = 0; s < e; s++) {
      const a = this._values[s];
      if (a == null) return null;
      const u = s * t;
      for (let c = 0; c < t; c++) {
        const l = a[c] || this._defaultValue;
        if (l == null || l.isNull() || l.isError() || l.isString()) return null;
        n[u + c] = l.isBoolean() ? l.getValue() ? 1 : 0 : l.getValue();
      }
    }
    return n;
  }
  _typedCompareEquals(e) {
    const t = this._rowCount, r = this._columnCount, n = t * r, s = new Float64Array(n), a = e.getValue();
    if (this._numericData !== null) {
      if (typeof a != "number") return null;
      const u = this._numericData;
      for (let c = 0; c < n; c++)
        s[c] = u[c] === a ? 1 : 0;
    } else
      for (let u = 0; u < t; u++) {
        const c = this._values[u];
        if (c == null) continue;
        const l = u * r;
        for (let f = 0; f < r; f++) {
          const h = c[f];
          if (h == null || h.isNull())
            s[l + f] = a === "" || a === null ? 1 : 0;
          else {
            if (h.isError())
              return null;
            s[l + f] = h.getValue() === a ? 1 : 0;
          }
        }
      }
    return W.createNumberArray(s, t, r, this._unitId, this._sheetId, this._currentRow, this._currentColumn);
  }
  _typedArithmeticFromValues(e, t) {
    const r = this._rowCount, n = this._columnCount, s = r * n, a = new Float64Array(s);
    for (let u = 0; u < r; u++) {
      const c = this._values[u];
      if (c == null) return null;
      const l = u * n;
      for (let f = 0; f < n; f++) {
        const h = c[f];
        if (h == null || h.isNull() || h.isError() || h.isString()) return null;
        const d = h.isBoolean() ? h.getValue() ? 1 : 0 : h.getValue();
        switch (t) {
          case 2:
            a[l + f] = d * e;
            break;
          case 1:
            a[l + f] = d + e;
            break;
          case 0:
            a[l + f] = d - e;
            break;
          case 3:
            if (e === 0) return null;
            a[l + f] = d / e;
            break;
          default:
            return null;
        }
      }
    }
    return W.createNumberArray(a, r, n, this._unitId, this._sheetId, this._currentRow, this._currentColumn);
  }
  _typedBinaryScalar(e, t, r) {
    const n = this._numericData;
    if (n === null)
      return null;
    const s = n.length, a = new Float64Array(s);
    switch (t) {
      case 2:
        for (let u = 0; u < s; u++) a[u] = n[u] * e;
        break;
      case 1:
        for (let u = 0; u < s; u++) a[u] = n[u] + e;
        break;
      case 0:
        for (let u = 0; u < s; u++) a[u] = n[u] - e;
        break;
      case 3:
        if (e === 0) return null;
        for (let u = 0; u < s; u++) a[u] = n[u] / e;
        break;
      case 5: {
        if (!r) return null;
        let u;
        switch (r) {
          case x.EQUALS:
            u = (c, l) => c === l ? 1 : 0;
            break;
          case x.NOT_EQUAL:
            u = (c, l) => c !== l ? 1 : 0;
            break;
          case x.GREATER_THAN:
            u = (c, l) => c > l ? 1 : 0;
            break;
          case x.GREATER_THAN_OR_EQUAL:
            u = (c, l) => c >= l ? 1 : 0;
            break;
          case x.LESS_THAN:
            u = (c, l) => c < l ? 1 : 0;
            break;
          case x.LESS_THAN_OR_EQUAL:
            u = (c, l) => c <= l ? 1 : 0;
            break;
          default:
            return null;
        }
        for (let c = 0; c < s; c++) a[c] = u(n[c], e);
        break;
      }
      default:
        return null;
    }
    return W.createNumberArray(a, this._rowCount, this._columnCount, this._unitId, this._sheetId, this._currentRow, this._currentColumn);
  }
  _typedBinaryArray(e, t, r, n, s) {
    const a = this._numericData;
    if (a === null || this._rowCount !== t || this._columnCount !== r)
      return null;
    const u = a.length, c = new Float64Array(u);
    switch (n) {
      case 2:
        for (let l = 0; l < u; l++) c[l] = a[l] * e[l];
        break;
      case 1:
        for (let l = 0; l < u; l++) c[l] = a[l] + e[l];
        break;
      case 0:
        for (let l = 0; l < u; l++) c[l] = a[l] - e[l];
        break;
      case 3:
        for (let l = 0; l < u; l++) c[l] = e[l] === 0 ? Number.NaN : a[l] / e[l];
        break;
      case 5: {
        if (!s) return null;
        let l;
        switch (s) {
          case x.EQUALS:
            l = (f, h) => f === h ? 1 : 0;
            break;
          case x.NOT_EQUAL:
            l = (f, h) => f !== h ? 1 : 0;
            break;
          case x.GREATER_THAN:
            l = (f, h) => f > h ? 1 : 0;
            break;
          case x.GREATER_THAN_OR_EQUAL:
            l = (f, h) => f >= h ? 1 : 0;
            break;
          case x.LESS_THAN:
            l = (f, h) => f < h ? 1 : 0;
            break;
          case x.LESS_THAN_OR_EQUAL:
            l = (f, h) => f <= h ? 1 : 0;
            break;
          default:
            return null;
        }
        for (let f = 0; f < u; f++) c[f] = l(a[f], e[f]);
        break;
      }
      default:
        return null;
    }
    return W.createNumberArray(c, this._rowCount, this._columnCount, this._unitId, this._sheetId, this._currentRow, this._currentColumn);
  }
  _batchOperator(e, t, r, n) {
    let s = this._rowCount, a = this._columnCount;
    if (e.isArray()) {
      const u = e.getRowCount(), c = e.getColumnCount();
      if (s = Math.max(u, s), a = Math.max(c, a), this._numericData !== null && e._numericData !== null) {
        const l = this._typedBinaryArray(
          e._numericData,
          u,
          c,
          t,
          r
        );
        if (l) return l;
      }
      if (this._numericData !== null && e._numericData === null && this._rowCount === u && this._columnCount === c) {
        const l = e._extractNumericData();
        if (l) {
          const f = this._typedBinaryArray(l, u, c, t, r);
          if (f) return f;
        }
      }
      if (this._numericData === null && e._numericData !== null && this._rowCount === u && this._columnCount === c) {
        const l = this._extractNumericData();
        if (l) {
          const h = W.createNumberArray(l, this._rowCount, this._columnCount, this._unitId, this._sheetId, this._currentRow, this._currentColumn)._typedBinaryArray(e._numericData, u, c, t, r);
          if (h) return h;
        }
      }
      if (u === 1 && c === 1) {
        const l = e.getFirstCell();
        if (this._numericData !== null && l.isNumber()) {
          const _ = this._typedBinaryScalar(l.getValue(), t, r);
          if (_) return _;
        }
        if (t === 5 && r === x.EQUALS && !n) {
          const _ = this._typedCompareEquals(l);
          if (_) return _;
        }
        const f = new Array(a);
        for (let _ = 0; _ < a; _++)
          f[_] = l;
        const h = Array.from({ length: s }, () => new Array(a));
        for (let _ = 0; _ < a; _++)
          this._batchOperatorValue(
            f[_],
            _,
            h,
            t,
            r,
            n
          );
        const d = this._createNewArray(h, s, a);
        return d.setDefaultValue(T.create(!1)), d;
      } else if (u === 1 && this._columnCount > 1) {
        const l = e.getArrayValue(), f = new Array(a);
        for (let _ = 0; _ < a; _++)
          f[_] = l[0][_];
        const h = Array.from({ length: s }, () => new Array(a));
        for (let _ = 0; _ < a; _++)
          this._batchOperatorValue(
            f[_],
            _,
            h,
            t,
            r,
            n
          );
        const d = this._createNewArray(h, s, a);
        return d.setDefaultValue(T.create(!1)), d;
      } else
        return this._batchOperatorArray(e, t, r, n);
    } else {
      if (this._numericData !== null && e.isNumber()) {
        const f = this._typedBinaryScalar(e.getValue(), t, r);
        if (f) return f;
      }
      if (t === 5 && r === x.EQUALS && !n) {
        const f = this._typedCompareEquals(e);
        if (f) return f;
      }
      if (this._numericData === null && t !== 5 && t !== 6 && t !== 7 && e.isNumber()) {
        const f = this._typedArithmeticFromValues(e.getValue(), t);
        if (f) return f;
      }
      const u = new Array(a);
      for (let f = 0; f < a; f++)
        u[f] = e;
      const c = Array.from({ length: s }, () => new Array(a));
      for (let f = 0; f < a; f++)
        this._batchOperatorValue(
          u[f],
          f,
          c,
          t,
          r,
          n
        );
      const l = this._createNewArray(c, s, a);
      return l.setDefaultValue(T.create(!1)), l;
    }
  }
  // eslint-disable-next-line max-lines-per-function
  _batchOperatorValue(e, t, r, n, s, a) {
    const u = this._rowCount, c = this.getUnitId(), l = this.getSheetId(), f = this.getCurrentRow(), h = this.getCurrentColumn();
    if (n === 5) {
      const { rowsInCache: d, rowsNotInCache: _ } = He.canUseCache(
        c,
        l,
        t + h,
        f,
        f + u - 1
      );
      if (d.length > 0) {
        if (s === x.EQUALS) {
          const C = He.getCellPositions(
            c,
            l,
            t + h,
            e.getValue(),
            d
          );
          C != null && C.forEach((E) => {
            if (E < f || E > f + u - 1)
              return;
            const b = E - f;
            r[b] == null && (r[b] = []), r[b][t] = T.create(!0);
          });
        } else {
          const C = He.getCellValuePositions(
            c,
            l,
            t + h
          );
          C != null && C.forEach((E, b) => {
            let y = ue.create();
            lr.has(b) ? y = g.create(b) : typeof b == "string" ? y = U.create(b) : typeof b == "number" ? y = R.create(b) : typeof b == "boolean" && (y = T.create(b));
            let p;
            y.isError() ? p = y : e.isError() ? p = e : p = y.compare(e, s, a), (p.isError() || p.getValue() === !0) && E.forEach((N) => {
              N >= f && N <= f + u - 1 && (r[N - f] == null && (r[N - f] = []), r[N - f][t] = p);
            });
          });
        }
        if (_.length > 0)
          for (const C of _) {
            const [E, b] = C;
            for (let y = E; y <= b; y++)
              this.__batchOperatorRowValue(
                e,
                t,
                r,
                n,
                y - f,
                c,
                l,
                f,
                h,
                s,
                a
              );
            He.setContinueBuildingCache(
              c,
              l,
              t + h,
              E,
              b
            );
          }
        return;
      }
    }
    for (let d = 0; d < u; d++)
      this.__batchOperatorRowValue(
        e,
        t,
        r,
        n,
        d,
        c,
        l,
        f,
        h,
        s,
        a
      );
    He.setContinueBuildingCache(
      c,
      l,
      t + h,
      f,
      f + u - 1
    );
  }
  // eslint-disable-next-line
  __batchOperatorRowValue(e, t, r, n, s, a, u, c, l, f, h) {
    const d = this.getValueOrDefault(s, t);
    if (r[s] == null && (r[s] = []), d && e)
      if (d.isError())
        r[s][t] = d;
      else if (e.isError())
        r[s][t] = e;
      else
        switch (n) {
          case 1:
            r[s][t] = d.plus(e);
            break;
          case 0:
            r[s][t] = d.minus(e);
            break;
          case 2:
            r[s][t] = d.multiply(e);
            break;
          case 3:
            r[s][t] = d.divided(e);
            break;
          case 4:
            r[s][t] = d.mod(e);
            break;
          case 5:
            f ? r[s][t] = d.compare(e, f, h) : r[s][t] = g.create(m.VALUE);
            break;
          case 6:
            r[s][t] = d.concatenateFront(e);
            break;
          case 7:
            r[s][t] = d.concatenateBack(e);
            break;
          case 8:
            r[s][t] = d.pow(e);
            break;
          case 9:
            r[s][t] = d.round(e);
            break;
          case 10:
            r[s][t] = d.floor(e);
            break;
          case 12:
            r[s][t] = d.atan2(e);
            break;
          case 11:
            r[s][t] = d.ceil(e);
            break;
        }
    else
      r[s][t] = g.create(m.NA);
    d != null && (d.isError() ? He.set(
      a,
      u,
      t + l,
      d.getErrorType(),
      s + c
    ) : He.set(
      a,
      u,
      t + l,
      d.getValue(),
      s + c
    ));
  }
  // eslint-disable-next-line max-lines-per-function, complexity
  _batchOperatorArray(e, t, r, n) {
    let s = e.getRowCount(), a = e.getColumnCount();
    s < this._rowCount && (s = this._rowCount), a < this._columnCount && (a = this._columnCount);
    const u = new Array(s), c = this._checkArrayCalculateType(this), l = this._checkArrayCalculateType(e);
    for (let f = 0; f < s; f++) {
      const h = new Array(a);
      for (let d = 0; d < a; d++) {
        let _;
        c === 3 ? _ = this.getValueOrDefault(0, 0) : c === 1 ? _ = this.getValueOrDefault(0, d) : c === 2 ? _ = this.getValueOrDefault(f, 0) : _ = this.getValueOrDefault(f, d);
        let C;
        if (l === 3 ? C = e.getValueOrDefault(0, 0) : l === 1 ? C = e.getValueOrDefault(0, d) : l === 2 ? C = e.getValueOrDefault(f, 0) : C = e.getValueOrDefault(f, d), _ && C)
          if (_.isError())
            h[d] = _;
          else if (C.isError())
            h[d] = C;
          else
            switch (t) {
              case 1:
                h[d] = _.plus(C);
                break;
              case 0:
                h[d] = _.minus(C);
                break;
              case 2:
                h[d] = _.multiply(C);
                break;
              case 3:
                h[d] = _.divided(C);
                break;
              case 4:
                h[d] = _.mod(C);
                break;
              case 5:
                r ? h[d] = _.compare(C, r, n) : h[d] = g.create(m.VALUE);
                break;
              case 6:
                h[d] = _.concatenateFront(C);
                break;
              case 7:
                h[d] = _.concatenateBack(C);
                break;
              case 8:
                h[d] = _.pow(C);
                break;
              case 9:
                h[d] = _.round(C);
                break;
              case 12:
                h[d] = _.atan2(C);
                break;
              case 10:
                h[d] = _.floor(C);
                break;
              case 11:
                h[d] = _.ceil(C);
                break;
            }
        else
          h[d] = g.create(m.NA);
      }
      u[f] = h;
    }
    return this._createNewArray(u, s, a);
  }
  _checkArrayCalculateType(e) {
    return e.getRowCount() === 1 && e.getColumnCount() === 1 ? 3 : e.getRowCount() === 1 ? 1 : e.getColumnCount() === 1 ? 2 : 0;
  }
  _formatValue(e) {
    if (typeof e != "string")
      return e = e, this._rowCount = e.rowCount, this._columnCount = e.columnCount, this._unitId = e.unitId, this._sheetId = e.sheetId, this._currentRow = e.row, this._currentColumn = e.column, e.calculateValueList;
    e = e.slice(1, -1);
    const t = e.split(";"), r = t.length, n = [];
    let s = 0;
    for (let a = 0; a < r; a++) {
      const c = t[a].split(","), l = c.length;
      s < l && (s = l);
      const f = [];
      for (let h = 0; h < l; h++) {
        const d = c[h].trim();
        f.push(Mt.create(d));
      }
      n.push(f);
    }
    return this._rowCount = r, this._columnCount = s, n;
  }
  // READNOW: Every mapValue and _batchOperator call ends here, wrapping the
  // intermediate BaseValueObject[][] in a new ArrayValueObject. The 2D array
  // is held in calculateValueList and stays in memory until GC. No pooling
  // or reuse — each operation allocates fresh.
  _createNewArray(e, t, r, n = -1, s = -1) {
    (this._currentColumn === -1 || this._currentRow === -1) && (n = -1, s = -1);
    const a = {
      calculateValueList: e,
      rowCount: t,
      columnCount: r,
      unitId: this.getUnitId(),
      sheetId: this.getSheetId(),
      row: n,
      column: s
    };
    return W.create(a);
  }
}
class Mt {
  static create(i, e = !1) {
    if (i == null)
      return ue.create();
    if (typeof i == "boolean")
      return T.create(i);
    if (typeof i == "string") {
      const t = i.toLocaleUpperCase().trim();
      if (lr.has(t))
        return g.create(t);
      if (t === ar.TRUE || t === ar.FALSE)
        return zn(i);
      if (q(i))
        return R.create(Number(i));
      if (!e) {
        const { isNumberPattern: n, value: s, pattern: a } = Ul(i);
        if (n)
          return R.create(s, a);
      }
      const r = i.replace(/\n/g, "").replace(/\r/g, "");
      return !Xf(r) && Kc(r) ? W.create(r) : Wf(i);
    }
    return typeof i == "number" ? Br(i) : g.create(m.VALUE);
  }
}
function Xf(o) {
  const i = o.trim();
  return i.startsWith('"') && i.endsWith('"');
}
const Zf = 1e4, jr = new Dt(Zf);
class Jn extends Wn {
  constructor(e) {
    super();
    A(this, "_forcedSheetId", "");
    A(this, "_forcedSheetName", "");
    A(this, "_defaultSheetId", "");
    A(this, "_rangeData", {
      startColumn: -1,
      startRow: -1,
      endRow: -1,
      endColumn: -1
    });
    A(this, "_unitData", {});
    A(this, "_unitStylesData", {});
    A(this, "_filteredOutRows", []);
    A(this, "_defaultUnitId", "");
    A(this, "_forcedUnitId", "");
    A(this, "_runtimeData", {});
    A(this, "_arrayFormulaCellData", {});
    A(this, "_runtimeArrayFormulaCellData", {});
    A(this, "_runtimeFeatureCellData", {});
    A(this, "_refOffsetX", 0);
    A(this, "_refOffsetY", 0);
    this._token = e;
  }
  dispose() {
    this._unitData = {}, this._unitStylesData = {}, this._runtimeData = {};
  }
  getToken() {
    return this._token;
  }
  setToken(e) {
    this._token = e;
  }
  isExceedRange() {
    const { startRow: e, endRow: t, startColumn: r, endColumn: n } = this.getRangePosition();
    return e < 0 || r < 0 || t >= this.getActiveSheetRowCount() || n >= this.getActiveSheetColumnCount();
  }
  setRefOffset(e = 0, t = 0) {
    this._refOffsetX = e, this._refOffsetY = t;
  }
  getRefOffset() {
    return {
      x: this._refOffsetX,
      y: this._refOffsetY
    };
  }
  getRangePosition() {
    let { startRow: e, startColumn: t, endRow: r, endColumn: n } = da(this._rangeData, this._refOffsetX, this._refOffsetY);
    return Number.isNaN(e) && (e = 0), Number.isNaN(t) && (t = 0), Number.isNaN(r) && (r = this.getActiveSheetRowCount() - 1), Number.isNaN(n) && (n = this.getActiveSheetColumnCount() - 1), {
      ...this._rangeData,
      startRow: e,
      endRow: r,
      startColumn: t,
      endColumn: n
    };
  }
  isReferenceObject() {
    return !0;
  }
  iterator(e) {
    const { startRow: t, endRow: r, startColumn: n, endColumn: s } = this.getRangePosition();
    if (this._checkIfWorksheetMiss())
      return e(g.create(m.VALUE), t, n);
    const a = this._forcedUnitId || this._defaultUnitId, u = this._forcedSheetId || this._defaultSheetId;
    for (let c = t; c <= r; c++)
      for (let l = n; l <= s; l++) {
        if (c < 0 || l < 0)
          return e(g.create(m.REF), c, l);
        const f = this.getCellData(c, l);
        let h = !1;
        if (mn(f)) {
          h = e(null, c, l);
          continue;
        }
        let d = this.getCellValueObject(f);
        if (c === t && l === n) {
          const _ = this.getCellPattern(a, u, c, l);
          if (_ && d.isNumber()) {
            const C = Number(d.getValue());
            d = R.create(C, _);
          }
        }
        if (h = e(d, c, l), h === !1)
          return;
      }
  }
  getFirstCell() {
    if (this._checkIfWorksheetMiss())
      return g.create(m.VALUE);
    const { startRow: e, startColumn: t } = this.getRangePosition(), r = this.getCellData(e, t);
    if (!r)
      return R.create(0);
    let n = this.getCellValueObject(r);
    const s = this._forcedUnitId || this._defaultUnitId, a = this._forcedSheetId || this._defaultSheetId, u = this.getCellPattern(s, a, e, t);
    if (u && n.isNumber()) {
      const c = Number(n.getValue());
      n = R.create(c, u);
    }
    return n;
  }
  getRangeData() {
    return this._rangeData;
  }
  setRangeData(e) {
    this._rangeData = e;
  }
  getUnitId() {
    return this._forcedUnitId && this._forcedUnitId.length > 0 ? this._forcedUnitId : this._defaultUnitId;
  }
  getSheetId() {
    return this._forcedSheetId && this._forcedSheetId.length > 0 ? this._forcedSheetId : this._defaultSheetId;
  }
  setForcedUnitIdDirect(e) {
    e.length > 0 && (this._forcedUnitId = e);
  }
  getForcedUnitId() {
    return this._forcedUnitId;
  }
  setForcedSheetId(e) {
    var t;
    this._forcedSheetId = (t = e[this.getUnitId()]) == null ? void 0 : t[this._forcedSheetName];
  }
  setForcedSheetIdDirect(e) {
    this._forcedSheetId = e;
  }
  getForcedSheetId() {
    return this._forcedSheetId;
  }
  setForcedSheetName(e) {
    e.length > 0 && (this._forcedSheetName = e);
  }
  getForcedSheetName() {
    return this._forcedSheetName;
  }
  setDefaultSheetId(e) {
    this._defaultSheetId = e;
  }
  getDefaultSheetId() {
    return this._defaultSheetId;
  }
  setDefaultUnitId(e) {
    this._defaultUnitId = e;
  }
  getDefaultUnitId() {
    return this._defaultUnitId;
  }
  getUnitData() {
    return this._unitData;
  }
  setUnitData(e) {
    this._unitData = e;
  }
  getUnitStylesData() {
    return this._unitStylesData;
  }
  setUnitStylesData(e) {
    this._unitStylesData = e;
  }
  getFilteredOutRows() {
    return this._filteredOutRows;
  }
  setFilteredOutRows(e) {
    this._filteredOutRows = e;
  }
  getRuntimeData() {
    return this._runtimeData;
  }
  setRuntimeData(e) {
    this._runtimeData = e;
  }
  getArrayFormulaCellData() {
    return this._arrayFormulaCellData;
  }
  setArrayFormulaCellData(e) {
    this._arrayFormulaCellData = e;
  }
  getRuntimeArrayFormulaCellData() {
    return this._runtimeArrayFormulaCellData;
  }
  setRuntimeArrayFormulaCellData(e) {
    this._runtimeArrayFormulaCellData = e;
  }
  getRuntimeFeatureCellData() {
    return this._runtimeFeatureCellData;
  }
  setRuntimeFeatureCellData(e) {
    this._runtimeFeatureCellData = e;
  }
  getActiveSheetRowCount() {
    var e;
    return ((e = this.getCurrentActiveSheetData()) == null ? void 0 : e.rowCount) || 0;
  }
  getActiveSheetColumnCount() {
    var e;
    return ((e = this.getCurrentActiveSheetData()) == null ? void 0 : e.columnCount) || 0;
  }
  getRowCount() {
    return this._rangeData.endRow - this._rangeData.startRow + 1;
  }
  getColumnCount() {
    return this._rangeData.endColumn - this._rangeData.startColumn + 1;
  }
  getRowData() {
    var e;
    return ((e = this.getCurrentActiveSheetData()) == null ? void 0 : e.rowData) || {};
  }
  getColumnData() {
    var e;
    return ((e = this.getCurrentActiveSheetData()) == null ? void 0 : e.columnData) || {};
  }
  isCell() {
    return !1;
  }
  isColumn() {
    return !1;
  }
  isRow() {
    return !1;
  }
  isRange() {
    return !1;
  }
  isTable() {
    return !1;
  }
  unionBy(e) {
    return g.create(m.REF);
  }
  unionRange(e, t) {
    return {
      startRow: -1,
      startColumn: -1,
      endRow: -1,
      endColumn: -1
    };
  }
  getCellValueObject(e) {
    const t = Bl(e);
    if (lr.has(t))
      return g.create(t);
    if (e.t === wt.NUMBER) {
      const r = this._getPatternByCell(e);
      return Cc(r) ? U.create(t.toString()) : Br(t, r);
    }
    return e.t === wt.STRING || e.t === wt.FORCE_STRING ? U.create(t.toString()) : e.t === wt.BOOLEAN ? zn(t) : Mt.create(t);
  }
  _getPatternByCell(e) {
    var n;
    const t = this._unitStylesData[this.getUnitId()];
    if (!t) return "";
    const r = t.getStyleByCell(e);
    return ((n = r == null ? void 0 : r.n) == null ? void 0 : n.pattern) || "";
  }
  getCellByRow(e) {
    return this.getCellByPosition(e);
  }
  getCellByColumn(e) {
    return this.getCellByPosition(void 0, e);
  }
  getCurrentActiveSheetData() {
    var e;
    return (e = this._unitData[this.getUnitId()]) == null ? void 0 : e[this.getSheetId()];
  }
  getCurrentStylesData() {
    return this._unitStylesData[this.getUnitId()];
  }
  getCurrentRuntimeSheetData() {
    var e, t;
    return (t = (e = this._runtimeData) == null ? void 0 : e[this.getUnitId()]) == null ? void 0 : t[this.getSheetId()];
  }
  getCurrentActiveArrayFormulaCellData() {
    var e, t;
    return (t = (e = this._arrayFormulaCellData) == null ? void 0 : e[this.getUnitId()]) == null ? void 0 : t[this.getSheetId()];
  }
  getCurrentRuntimeActiveArrayFormulaCellData() {
    var e, t;
    return (t = (e = this._runtimeArrayFormulaCellData) == null ? void 0 : e[this.getUnitId()]) == null ? void 0 : t[this.getSheetId()];
  }
  getCellData(e, t) {
    const r = this.getCurrentActiveSheetData(), n = this.getCurrentRuntimeSheetData(), s = this.getCurrentActiveArrayFormulaCellData(), a = this.getCurrentRuntimeActiveArrayFormulaCellData();
    return (n == null ? void 0 : n.getValue(e, t)) || (a == null ? void 0 : a.getValue(e, t)) || this.getRuntimeFeatureCellValue(e, t) || (s == null ? void 0 : s.getValue(e, t)) || (r == null ? void 0 : r.cellData.getValue(e, t));
  }
  getRuntimeFeatureCellValue(e, t) {
    return Do(e, t, this.getSheetId(), this.getUnitId(), this._runtimeFeatureCellData);
  }
  getCellByPosition(e, t) {
    let r = e, n = t;
    r || (r = this._rangeData.startRow), n || (n = this._rangeData.startColumn);
    const s = this.getCellData(r, n);
    return s ? this.getCellValueObject(s) : g.create(m.VALUE);
  }
  /**
   * Get the pattern of the cell
   * @param unitId
   * @param sheetId
   * @param row
   * @param column
   * @returns
   */
  getCellPattern(e, t, r, n) {
    var c, l, f, h;
    const s = this._unitStylesData[e];
    if (!s)
      return "";
    const a = (f = (l = (c = this._unitData[e]) == null ? void 0 : c[t]) == null ? void 0 : l.cellData) == null ? void 0 : f.getValue(r, n);
    if (!a)
      return "";
    const u = s.getStyleByCell(a);
    return ((h = u == null ? void 0 : u.n) == null ? void 0 : h.pattern) || "";
  }
  toArrayValueObject(e = !0) {
    var C;
    const { startRow: t, endRow: r, startColumn: n, endColumn: s } = this.getRangePosition(), a = `${this.getUnitId()}_${this.getSheetId()}_${t}_${r}_${n}_${s}`, u = jr.get(a);
    if (u && e)
      return u;
    const c = r - t + 1, l = s - n + 1;
    if (c < 0 || l < 0)
      return this._getBlankArrayValueObject();
    let f = c > 0 && l > 0;
    const h = f ? new Float64Array(c * l) : null, d = f ? [] : new Array(c);
    this.iterator((E, b, y) => {
      const p = b - t, N = y - n;
      if (E == null && (E = ue.create()), f && (E.isNumber() ? h[p * l + N] = E.getValue() : f = !1), !f) {
        if (d.length === 0) {
          for (let w = 0; w < c; w++)
            d[w] = new Array(l);
          for (let w = 0; w <= p; w++) {
            const S = w * l, M = w < p ? l : N;
            for (let D = 0; D < M; D++)
              d[w][D] = R.create(h[S + D]);
          }
        }
        d[p] || (d[p] = new Array(l)), d[p][N] = E;
      }
    });
    let _;
    if (f && h)
      _ = W.createNumberArray(
        h,
        c,
        l,
        this.getUnitId(),
        this.getSheetId(),
        t,
        n
      );
    else {
      const E = {
        calculateValueList: d,
        rowCount: d.length,
        columnCount: ((C = d[0]) == null ? void 0 : C.length) || 0,
        unitId: this.getUnitId(),
        sheetId: this.getSheetId(),
        row: t,
        column: n
      };
      _ = W.create(E);
    }
    return e && jr.set(a, _), _;
  }
  toUnitRange() {
    return {
      range: this.getRangePosition(),
      sheetId: this.getSheetId(),
      unitId: this.getUnitId()
    };
  }
  _checkIfWorksheetMiss() {
    return (this._forcedSheetId == null || this._forcedSheetId.length === 0) && this._forcedSheetName.length > 0;
  }
  _getBlankArrayValueObject() {
    const e = {
      calculateValueList: [],
      rowCount: 0,
      columnCount: 0,
      unitId: this.getUnitId(),
      sheetId: this.getSheetId(),
      row: 0,
      column: 0
    };
    return W.create(e);
  }
}
class zf extends Wn {
  constructor(i) {
    super(), this._promise = i;
  }
  isAsyncObject() {
    return !0;
  }
  async getValue() {
    return this._promise;
  }
}
class Jf extends Wn {
  constructor(i) {
    super(), this._promiseList = i;
  }
  isAsyncArrayObject() {
    return !0;
  }
  async getValue() {
    var t;
    const i = [];
    for (let r = 0; r < this._promiseList.length; r++) {
      const n = this._promiseList[r];
      i[r] == null && (i[r] = []);
      for (let s = 0; s < n.length; s++) {
        const a = n[s];
        a.isAsyncObject() ? i[r][s] = await a.getValue() : i[r][s] = a;
      }
    }
    const e = {
      calculateValueList: i,
      rowCount: i.length,
      columnCount: ((t = i[0]) == null ? void 0 : t.length) || 0,
      unitId: "",
      sheetId: "",
      row: 0,
      column: 0
    };
    return W.create(e);
  }
}
class On extends Jn {
  constructor(i, e, t) {
    super(""), this.setRangeData(i), e && this.setForcedSheetIdDirect(e), t && this.setForcedUnitIdDirect(t);
  }
  isRange() {
    return !0;
  }
}
class La extends Jn {
  constructor(i) {
    super(i);
    const e = Er(i);
    this.setForcedUnitIdDirect(e.unitId), this.setForcedSheetName(e.sheetName), this.setRangeData(e.range);
  }
  isCell() {
    return !0;
  }
  unionBy(i) {
    if (!i.isCell())
      return g.create(m.REF);
    const e = i, t = this.unionRange(this.getRangeData(), e.getRangeData());
    return this._createRange(t);
  }
  unionRange(i, e) {
    const t = i.startRow, r = i.startColumn, n = e.startRow, s = e.startColumn, a = {
      startRow: -1,
      startColumn: -1,
      endRow: -1,
      endColumn: -1
    };
    return t > n ? (a.startRow = n, a.endRow = t) : (a.startRow = t, a.endRow = n), r > s ? (a.startColumn = s, a.endColumn = r) : (a.startColumn = r, a.endColumn = s), i.startAbsoluteRefType && (a.startAbsoluteRefType = i.startAbsoluteRefType), e.startAbsoluteRefType && (a.endAbsoluteRefType = e.startAbsoluteRefType), a;
  }
  _createRange(i) {
    const e = new On(
      i,
      this.getForcedSheetId(),
      this.getForcedUnitId()
    );
    e.setUnitData(this.getUnitData()), e.setDefaultSheetId(this.getDefaultSheetId()), e.setDefaultUnitId(this.getDefaultUnitId()), e.setRuntimeData(this.getRuntimeData()), e.setUnitStylesData(this.getUnitStylesData()), e.setArrayFormulaCellData(this.getArrayFormulaCellData()), e.setRuntimeArrayFormulaCellData(this.getRuntimeArrayFormulaCellData()), e.setRuntimeFeatureCellData(this.getRuntimeFeatureCellData());
    const { x: t, y: r } = this.getRefOffset();
    e.setRefOffset(t, r);
    const n = this.getForcedSheetId();
    e.setForcedSheetName(this.getForcedSheetName()), n != null && e.setForcedSheetIdDirect(n);
    const s = this.getForcedUnitId();
    return s && e.setForcedUnitIdDirect(s), e;
  }
}
class xa extends Jn {
  constructor(i) {
    super(i);
    const e = Er(i);
    this.setForcedUnitIdDirect(e.unitId), this.setForcedSheetName(e.sheetName);
    const t = {
      ...e.range,
      startColumn: e.range.startColumn,
      startRow: Number.NaN,
      endColumn: e.range.endColumn,
      endRow: Number.NaN,
      rangeType: et.COLUMN
    };
    this.setRangeData(t);
  }
  isColumn() {
    return !0;
  }
  unionBy(i) {
    if (!i.isColumn())
      return g.create(m.REF);
    const e = i;
    if (e.getForcedSheetName() !== void 0 && e.getForcedSheetName() !== "")
      return g.create(m.REF);
    const t = this.getRangeData(), r = e.getRangeData(), n = r.startColumn;
    if (n >= t.startColumn && n <= t.endColumn)
      return this;
    const s = t.startColumn;
    return n > s ? t.endColumn = n : (t.startColumn = n, t.endColumn = s), r.startAbsoluteRefType && (t.endAbsoluteRefType = r.startAbsoluteRefType), t.rangeType = et.COLUMN, this.setToken(`${this.getToken()}${K.COLON}${e.getToken()}`), this;
  }
}
class Pa extends Jn {
  constructor(i) {
    super(i);
    const e = Er(i);
    this.setForcedUnitIdDirect(e.unitId), this.setForcedSheetName(e.sheetName);
    const t = {
      ...e.range,
      startColumn: Number.NaN,
      startRow: e.range.startRow,
      endColumn: Number.NaN,
      endRow: e.range.endRow,
      rangeType: et.ROW
    };
    this.setRangeData(t);
  }
  isRow() {
    return !0;
  }
  unionBy(i) {
    if (!i.isRow())
      return g.create(m.REF);
    const e = i;
    if (e.getForcedSheetName() !== void 0 && e.getForcedSheetName() !== "")
      return g.create(m.REF);
    const t = this.getRangeData(), r = e.getRangeData(), n = r.startRow;
    if (n >= t.startRow && n <= t.endRow)
      return this;
    const s = t.startRow;
    return n > s ? t.endRow = n : (t.startRow = n, t.endRow = s), r.startAbsoluteRefType && (t.endAbsoluteRefType = r.startAbsoluteRefType), t.rangeType = et.ROW, this.setToken(`${this.getToken()}${K.COLON}${e.getToken()}`), this;
  }
}
function O(o, i, e, t) {
  const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = [];
  for (let a = 0; a < o; a++) {
    const u = [];
    for (let c = 0; c < i; c++) {
      if (r === 1 && n === 1) {
        const f = e.isArray() ? e.get(0, 0) : e;
        u.push(f);
        continue;
      }
      if (r === 1 && c < n) {
        const f = e.get(0, c);
        u.push(f);
        continue;
      }
      if (n === 1 && a < r) {
        const f = e.get(a, 0);
        u.push(f);
        continue;
      }
      if (a >= r || c >= n) {
        u.push(t != null ? t : ue.create());
        continue;
      }
      const l = e.get(a, c);
      u.push(l);
    }
    s.push(u);
  }
  return Qr(s, o, i);
}
function Qr(o, i, e, t = "", r = "") {
  const n = {
    calculateValueList: o,
    rowCount: i,
    columnCount: e,
    unitId: t,
    sheetId: r,
    row: -1,
    column: -1
  };
  return W.create(n);
}
function pt(o) {
  const i = [
    x.EQUALS,
    x.NOT_EQUAL,
    x.GREATER_THAN_OR_EQUAL,
    x.GREATER_THAN,
    x.LESS_THAN_OR_EQUAL,
    x.LESS_THAN
  ];
  for (const e of i)
    if (o.startsWith(e)) {
      const t = o.substring(e.length);
      return [e, Mt.create(t)];
    }
  return [x.EQUALS, Mt.create(o)];
}
function es(o, i, e, t) {
  if (!e)
    if (i.isString()) {
      const r = `${i.getValue()}`, [n, s] = pt(r);
      e = n, i = s;
    } else
      e = x.EQUALS;
  return o.compare(i, e, t);
}
function eh(o, i) {
  const e = Math.max(o.isArray() ? o.getRowCount() : 1, i.isArray() ? i.getRowCount() : 1), t = Math.max(o.isArray() ? o.getColumnCount() : 1, i.isArray() ? i.getColumnCount() : 1), r = O(e, t, o), n = O(e, t, i);
  return r.mapValue((s, a, u) => {
    const c = n.get(a, u);
    return s != null && s.isError() ? s : c != null && c.isError() ? c : s != null && s.isBoolean() && (c != null && c.isBoolean()) ? zn(s.getValue() && c.getValue()) : T.create(!1);
  });
}
function th(o) {
  const i = o.getValue();
  let e = 0;
  return i && (e = 1), R.create(e);
}
function xi(o) {
  return o.isArray() && o.getRowCount() === 1 && o.getColumnCount() === 1 ? !0 : o.isReferenceObject() ? !!(o.isCell() || o.getRowCount() === 1 && o.getColumnCount() === 1) : (o = o, !!(o.isString() || o.isNumber() || o.isBoolean() || o.isError() || o.isNull()));
}
function rh(o) {
  const i = o == null ? void 0 : o.getPattern();
  let e = {}, t = {};
  if (i && (e = {
    s: {
      n: {
        pattern: i
      }
    }
  }), o != null && o.getCustomData() && (t = {
    custom: o.getCustomData()
  }), o == null)
    return {
      v: null,
      ...e
    };
  if (o.isError())
    return {
      v: o.getErrorType(),
      t: wt.STRING,
      ...e,
      ...t
    };
  if (o.isValueObject()) {
    const r = o, n = r.getValue();
    return r.isNumber() ? {
      v: n,
      t: wt.NUMBER,
      ...e,
      ...t
    } : r.isBoolean() ? {
      v: n ? 1 : 0,
      t: wt.BOOLEAN,
      ...e,
      ...t
    } : r.isString() ? {
      v: n,
      t: wt.STRING,
      ...e,
      ...t
    } : r.isNull() ? {
      v: null,
      ...e,
      ...t
    } : {
      v: n,
      t: wt.STRING,
      ...e,
      ...t
    };
  }
}
function nh(o) {
  let i = 0, e = 0;
  return o.forEach((t, r) => {
    if (r % 2 === 1)
      if (t.isArray()) {
        const n = t;
        i = Math.max(i, n.getRowCount()), e = Math.max(e, n.getColumnCount());
      } else
        i = Math.max(i, 1), e = Math.max(e, 1);
  }), { maxRowLength: i, maxColumnLength: e };
}
function Gr(o, i) {
  if (o.length === 0 || o.length % 2 !== 0)
    return {
      isError: !0,
      errorObject: g.create(m.VALUE),
      rangeIsDifferentSize: !1,
      criteriaMaxRowLength: 0,
      criteriaMaxColumnLength: 0,
      targetRange: null,
      variants: []
    };
  let e = null, t = -1, r = -1;
  if (i) {
    if (!i.isReferenceObject())
      return {
        isError: !0,
        errorObject: g.create(m.VALUE),
        rangeIsDifferentSize: !1,
        criteriaMaxRowLength: 0,
        criteriaMaxColumnLength: 0,
        targetRange: null,
        variants: []
      };
    e = i.toArrayValueObject(), t = e.getRowCount(), r = e.getColumnCount();
  }
  let n = 0, s = 0, a = !1;
  const u = [];
  for (let c = 0; c < o.length; c++)
    if (c % 2 === 1) {
      const l = o[c - 1], f = o[c];
      if (!l.isReferenceObject())
        return {
          isError: !0,
          errorObject: g.create(m.VALUE),
          rangeIsDifferentSize: !1,
          criteriaMaxRowLength: 0,
          criteriaMaxColumnLength: 0,
          targetRange: null,
          variants: []
        };
      const h = l.toArrayValueObject(), d = h.getRowCount(), _ = h.getColumnCount();
      c === 1 && t === -1 && r === -1 ? (t = d, r = _) : !a && (t !== d || r !== _) && (a = !0);
      let C = f;
      f.isReferenceObject() && (C = f.toArrayValueObject()), n = Math.max(n, C.isArray() ? C.getRowCount() : 1), s = Math.max(s, C.isArray() ? C.getColumnCount() : 1), u.push(h), u.push(C);
    }
  return {
    isError: !1,
    errorObject: null,
    rangeIsDifferentSize: a,
    criteriaMaxRowLength: n,
    criteriaMaxColumnLength: s,
    targetRange: e,
    variants: u
  };
}
function zs(o) {
  return o.isArray() ? o : W.createByArray([[o.getValue()]]);
}
function Rt(o, i, e, t = !1) {
  const r = [];
  for (let n = 0; n < o.length; n++) {
    if (n % 2 === 1) continue;
    const s = o[n], a = o[n + 1];
    O(i, e, a, g.create(m.NA)).iterator((c, l, f) => {
      if (!c)
        return;
      let h = es(s, c);
      if (t && (h = ts(h, s, c)), r[l] === void 0 && (r[l] = []), r[l][f] === void 0) {
        r[l][f] = h;
        return;
      }
      r[l][f] = eh(r[l][f], h);
    });
  }
  return r;
}
function ts(o, i, e) {
  const [t, r] = pt(`${e.getValue()}`);
  return o.mapValue((n, s, a) => {
    const u = i.get(s, a);
    if (u && Lr(u, r))
      return n;
    if (u != null && u.isError() && r.isError() && u.getValue() === r.getValue())
      return T.create(!0);
    if (t === x.EQUALS || t === x.NOT_EQUAL) {
      if (u != null && u.isNumber() && r.isString()) {
        const c = r.convertToNumberObjectValue();
        if (c.isNumber())
          return u.compare(c, t);
      }
      if (r.isNumber() && (u != null && u.isString())) {
        const c = u.convertToNumberObjectValue();
        if (c.isNumber())
          return c.compare(r, t);
      }
      if (t === x.EQUALS)
        return T.create(!1);
      if (t === x.NOT_EQUAL)
        return T.create(!0);
    }
    return T.create(!1);
  });
}
function Lr(o, i) {
  if (o.isNumber() && i.isNumber() || o.isBoolean() && i.isBoolean())
    return !0;
  const e = o.isString() && o.getValue() === "", t = i.isString() && i.getValue() === "";
  return !!((e || o.isNull()) && (t || i.isNull()) || o.isString() && !e && i.isString() && !t);
}
var Cn = /* @__PURE__ */ ((o) => (o[o.CELL = 0] = "CELL", o[o.COLUMN = 1] = "COLUMN", o[o.ROW = 2] = "ROW", o))(Cn || {});
function sh(o, i) {
  let e;
  switch (i) {
    case 0:
      e = new La(o);
      break;
    case 1:
      e = new xa(o);
      break;
    case 2:
      e = new Pa(o);
      break;
    default:
      throw new Error("Unknown reference object type");
  }
  return e;
}
function ah(o, i) {
  let e;
  return o.isCell() && i.isCell() || o.isRow() && i.isRow() || o.isColumn() && i.isColumn() ? e = o.unionBy(i) : e = g.create(m.NAME), e;
}
var ih = Object.getOwnPropertyDescriptor, oh = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? ih(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, uh = (o, i) => (e, t) => i(e, t, o);
let Js = class extends Ve {
  constructor(o) {
    super(), this._univerInstanceService = o;
  }
  generateCellValue(o, i) {
    if (i.trim() === "")
      return {
        v: ""
      };
    let e = o;
    if (o.startsWith("#") && _o(o.slice(1))) {
      const { unitId: t, sheetName: r, range: n } = Ur(o.slice(1)), s = this._univerInstanceService.getCurrentUnitOfType(gr.UNIVER_SHEET);
      if (t === "" || t === s.getUnitId())
        if (r === "")
          e = `#gid=${s.getActiveSheet().getSheetId()}&range=${$t(n)}`;
        else {
          const a = s.getSheetBySheetName(r);
          a && (e = `#gid=${a.getSheetId()}&range=${$t(n)}`);
        }
    } else Ke.isLegalUrl(o) && (e = Ke.normalizeUrl(o));
    return {
      p: _c.create().insertLink(i, e).getData()
    };
  }
};
Js = oh([
  uh(0, Gn)
], Js);
const Yo = ze(
  "univer.formula.hyperlink-engine-formula.service"
);
var ch = Object.getOwnPropertyDescriptor, lh = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? ch(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, Pi = (o, i) => (e, t) => i(e, t, o), $e = /* @__PURE__ */ ((o) => (o[o.IDLE = 0] = "IDLE", o[o.START = 1] = "START", o[o.START_DEPENDENCY = 2] = "START_DEPENDENCY", o[o.START_CALCULATION = 3] = "START_CALCULATION", o[o.CURRENTLY_CALCULATING = 4] = "CURRENTLY_CALCULATING", o[o.START_DEPENDENCY_ARRAY_FORMULA = 5] = "START_DEPENDENCY_ARRAY_FORMULA", o[o.START_CALCULATION_ARRAY_FORMULA = 6] = "START_CALCULATION_ARRAY_FORMULA", o[o.CURRENTLY_CALCULATING_ARRAY_FORMULA = 7] = "CURRENTLY_CALCULATING_ARRAY_FORMULA", o[o.CALCULATION_COMPLETED = 8] = "CALCULATION_COMPLETED", o))($e || {}), mr = /* @__PURE__ */ ((o) => (o[o.INITIAL = 0] = "INITIAL", o[o.STOP_EXECUTION = 1] = "STOP_EXECUTION", o[o.NOT_EXECUTED = 2] = "NOT_EXECUTED", o[o.SUCCESS = 3] = "SUCCESS", o))(mr || {});
let ea = class extends Ve {
  constructor(i, e) {
    super();
    A(this, "_formulaExecuteStage", 0);
    A(this, "_stopState", !1);
    A(this, "_currentRow", -1);
    A(this, "_currentColumn", -1);
    A(this, "_currentRowCount", Number.NEGATIVE_INFINITY);
    A(this, "_currentColumnCount", Number.NEGATIVE_INFINITY);
    A(this, "_currentSubUnitId", "");
    A(this, "_currentUnitId", "");
    A(this, "_runtimeData", {});
    A(this, "_runtimeOtherData", {});
    // Data returned by other businesses through formula calculation, excluding the sheet.
    A(this, "_unitArrayFormulaRange", {});
    A(this, "_runtimeArrayFormulaCellData", {});
    A(this, "_runtimeClearArrayFormulaCellData", {});
    A(this, "_runtimeFeatureRange", {});
    A(this, "_runtimeFeatureCellData", {});
    A(this, "_functionsExecutedState", 0);
    // lambdaId: { key: BaseAstNode }
    A(this, "_functionDefinitionPrivacyVar", /* @__PURE__ */ new Map());
    A(this, "_totalFormulasToCalculate", 0);
    A(this, "_completedFormulasCount", 0);
    A(this, "_totalArrayFormulasToCalculate", 0);
    A(this, "_completedArrayFormulasCount", 0);
    A(this, "_formulaCycleIndex", 0);
    A(this, "_isCycleDependency", !1);
    this._currentConfigService = i, this._hyperlinkEngineFormulaService = e;
  }
  get currentRow() {
    return this._currentRow;
  }
  get currentColumn() {
    return this._currentColumn;
  }
  get currentRowCount() {
    return this._currentRowCount;
  }
  get currentColumnCount() {
    return this._currentColumnCount;
  }
  get currentSubUnitId() {
    return this._currentSubUnitId;
  }
  get currentUnitId() {
    return this._currentUnitId;
  }
  dispose() {
    super.dispose(), this.reset(), this._runtimeFeatureCellData = {}, this._runtimeFeatureRange = {}, this.clearReferenceAndNumberformatCache();
  }
  enableCycleDependency() {
    this._isCycleDependency = !0;
  }
  disableCycleDependency() {
    this._isCycleDependency = !1;
  }
  isCycleDependency() {
    return this._isCycleDependency;
  }
  setFormulaCycleIndex(i) {
    this._formulaCycleIndex = i;
  }
  getFormulaCycleIndex() {
    return this._formulaCycleIndex;
  }
  setTotalArrayFormulasToCalculate(i) {
    this._totalArrayFormulasToCalculate = i;
  }
  getTotalArrayFormulasToCalculate() {
    return this._totalArrayFormulasToCalculate;
  }
  setCompletedArrayFormulasCount(i) {
    this._completedArrayFormulasCount = i;
  }
  getCompletedArrayFormulasCount() {
    return this._completedArrayFormulasCount;
  }
  setTotalFormulasToCalculate(i) {
    this._totalFormulasToCalculate = i;
  }
  getTotalFormulasToCalculate() {
    return this._totalFormulasToCalculate;
  }
  setCompletedFormulasCount(i) {
    this._completedFormulasCount = i;
  }
  getCompletedFormulasCount() {
    return this._completedFormulasCount;
  }
  markedAsSuccessfullyExecuted() {
    this._functionsExecutedState = 3;
  }
  markedAsNoFunctionsExecuted() {
    this._functionsExecutedState = 2;
  }
  markedAsStopFunctionsExecuted() {
    this._functionsExecutedState = 1;
  }
  markedAsInitialFunctionsExecuted() {
    this._functionsExecutedState = 0;
  }
  stopExecution() {
    this._stopState = !0, this.setFormulaExecuteStage(
      0
      /* IDLE */
    );
  }
  isStopExecution() {
    return this._stopState;
  }
  setFormulaExecuteStage(i) {
    this._formulaExecuteStage = i;
  }
  getFormulaExecuteStage() {
    return this._formulaExecuteStage;
  }
  reset() {
    this._formulaExecuteStage = 0, this._runtimeData = {}, this._runtimeOtherData = {}, this._unitArrayFormulaRange = {}, this._runtimeArrayFormulaCellData = {}, this._runtimeClearArrayFormulaCellData = {}, this._functionDefinitionPrivacyVar.clear(), this.markedAsInitialFunctionsExecuted(), this._stopState = !1, this._isCycleDependency = !1, this._totalFormulasToCalculate = 0, this._completedFormulasCount = 0, this.clearReferenceAndNumberformatCache();
  }
  clearReferenceAndNumberformatCache() {
    Dl(), vl(), Cl();
  }
  setCurrent(i, e, t, r, n, s) {
    this._currentRow = i, this._currentColumn = e, this._currentRowCount = t, this._currentColumnCount = r, this._currentSubUnitId = n, this._currentUnitId = s;
  }
  clearFunctionDefinitionPrivacyVar() {
    this._functionDefinitionPrivacyVar.clear();
  }
  registerFunctionDefinitionPrivacyVar(i, e) {
    this._functionDefinitionPrivacyVar.set(i, e);
  }
  getFunctionDefinitionPrivacyVar(i) {
    return this._functionDefinitionPrivacyVar.get(i);
  }
  setRuntimeOtherData(i, e, t, r) {
    const n = this._currentSubUnitId, s = this._currentUnitId;
    this._runtimeOtherData[s] === void 0 && (this._runtimeOtherData[s] = {});
    const a = this._runtimeOtherData[s];
    (a[n] === void 0 || a[n] === null) && (a[n] = {});
    const u = a[n];
    let c = [];
    if (r.isReferenceObject() || r.isValueObject() && r.isArray()) {
      const l = r, { startRow: f, startColumn: h } = l.getRangePosition();
      l.iterator((d, _, C) => {
        const E = this._getValueObjectOfRuntimeData(d), b = _ - f, y = C - h;
        c[b] == null && (c[b] = []), c[b][y] = E;
      });
    } else
      c = [[this._getValueObjectOfRuntimeData(r)]];
    (u[i] === void 0 || u[i] === null) && (u[i] = {}), (u[i][t] === void 0 || u[i][t] === null) && (u[i][t] = {}), u[i][t][e] = c;
  }
  // eslint-disable-next-line max-lines-per-function
  setRuntimeData(i) {
    const e = this._currentRow, t = this._currentColumn, r = this._currentRowCount, n = this.currentColumnCount, s = this._currentSubUnitId, a = this._currentUnitId;
    this._runtimeData[a] == null && (this._runtimeData[a] = {});
    const u = this._runtimeData[a];
    u[s] == null && (u[s] = new Ce()), this._unitArrayFormulaRange[a] == null && (this._unitArrayFormulaRange[a] = {});
    const c = this._unitArrayFormulaRange[a];
    (c[s] === null || c[s] === void 0) && (c[s] = {});
    const l = new Ce(c[s]);
    this._runtimeArrayFormulaCellData[a] === void 0 && (this._runtimeArrayFormulaCellData[a] = {});
    const f = this._runtimeArrayFormulaCellData[a];
    f[s] == null && (f[s] = new Ce()), this._runtimeClearArrayFormulaCellData[a] === void 0 && (this._runtimeClearArrayFormulaCellData[a] = {});
    const h = this._runtimeClearArrayFormulaCellData[a];
    h[s] == null && (h[s] = new Ce());
    const d = u[s], _ = f[s], C = h[s];
    if (i.isReferenceObject() || i.isValueObject() && i.isArray()) {
      const E = i, { startRow: b, startColumn: y, endRow: p, endColumn: N } = E.getRangePosition();
      if (b === p && y === N) {
        const S = E.getFirstCell(), M = this._getValueObjectOfRuntimeData(S);
        d.setValue(e, t, M), C.setValue(e, t, M), He.set(
          a,
          s,
          t,
          S.getValue(),
          e,
          !0
        );
        return;
      }
      const w = {
        startRow: e,
        startColumn: t,
        endRow: p - b + e,
        endColumn: N - y + t
      };
      if (l.setValue(e, t, w), this._checkIfArrayFormulaRangeHasData(a, s, e, t, w) || this._checkIfArrayFormulaExceeded(r, n, w)) {
        const S = this._getValueObjectOfRuntimeData(g.create(m.SPILL));
        d.setValue(e, t, S), C.setValue(e, t, S), He.set(
          a,
          s,
          t,
          m.SPILL,
          e,
          !0
        );
        const M = this._currentConfigService.getUnitData();
        E.iterator((D, L, v) => {
          var ne, z;
          const j = L - b + e, k = v - y + t, G = (z = (ne = M[a]) == null ? void 0 : ne[s]) == null ? void 0 : z.cellData.getValue(j, k);
          if (L === b && v === y)
            _.setValue(e, t, S);
          else if (G != null)
            G.v == null && (G.v = ""), _.setValue(j, k, G);
          else {
            if (this._isInOtherArrayFormulaRange(a, s, e, t, j, k))
              return !0;
            _.setValue(j, k, { v: "" });
          }
        });
      } else {
        const S = g.create(m.SPILL);
        E.iterator((M, D, L) => {
          He.set(
            a,
            s,
            t - y + L,
            M ? M.getValue() : 0,
            e - b + D,
            !0
          );
          const v = this._getValueObjectOfRuntimeData(M);
          if (D === b && L === y) {
            if (M != null && M.isError() && M.isEqualType(S))
              return C.setValue(e, t, {}), d.setValue(e, t, { ...this._getValueObjectOfRuntimeData(S) }), !1;
            d.setValue(e, t, { ...v });
          }
          const j = D - b + e, k = L - y + t;
          _.setValue(j, k, v);
        });
      }
    } else {
      const E = this._getValueObjectOfRuntimeData(i);
      d.setValue(e, t, E), He.set(
        a,
        s,
        t,
        i.getValue(),
        e,
        !0
      ), C.setValue(e, t, E);
    }
  }
  _getValueObjectOfRuntimeData(i) {
    return i != null && i.isString() && i.isHyperlink() ? this._hyperlinkEngineFormulaService.generateCellValue(
      i.getHyperlinkUrl(),
      i.getValue()
    ) : rh(i);
  }
  getUnitData() {
    return this._runtimeData;
  }
  getUnitArrayFormula() {
    return this._unitArrayFormulaRange;
  }
  getRuntimeOtherData() {
    return this._runtimeOtherData;
  }
  getRuntimeArrayFormulaCellData() {
    return this._runtimeArrayFormulaCellData;
  }
  getRuntimeClearArrayFormulaCellData() {
    return this._runtimeClearArrayFormulaCellData;
  }
  getRuntimeFeatureRange() {
    return this._runtimeFeatureRange;
  }
  setRuntimeFeatureRange(i, e) {
    this._runtimeFeatureRange[i] = e;
  }
  getRuntimeFeatureCellData() {
    return this._runtimeFeatureCellData;
  }
  setRuntimeFeatureCellData(i, e) {
    this._runtimeFeatureCellData[i] = e;
  }
  getAllRuntimeData() {
    return {
      unitData: this.getUnitData(),
      arrayFormulaRange: this.getUnitArrayFormula(),
      unitOtherData: this.getRuntimeOtherData(),
      functionsExecutedState: this._functionsExecutedState,
      arrayFormulaCellData: this.getRuntimeArrayFormulaCellData(),
      clearArrayFormulaCellData: this.getRuntimeClearArrayFormulaCellData(),
      runtimeFeatureRange: this.getRuntimeFeatureRange(),
      runtimeFeatureCellData: this.getRuntimeFeatureCellData()
    };
  }
  getRuntimeState() {
    return {
      totalFormulasToCalculate: this.getTotalFormulasToCalculate(),
      completedFormulasCount: this.getCompletedFormulasCount(),
      totalArrayFormulasToCalculate: this.getTotalArrayFormulasToCalculate(),
      completedArrayFormulasCount: this.getCompletedArrayFormulasCount(),
      stage: this.getFormulaExecuteStage(),
      formulaCycleIndex: this.getFormulaCycleIndex()
    };
  }
  // eslint-disable-next-line complexity
  _checkIfArrayFormulaRangeHasData(i, e, t, r, n) {
    var h, d, _, C, E, b, y, p, N, w, S;
    const { startRow: s, startColumn: a, endRow: u, endColumn: c } = n, l = this._currentConfigService.getUnitData(), f = this._currentConfigService.getArrayFormulaCellData();
    (_ = (d = (h = this._unitArrayFormulaRange[i]) == null ? void 0 : h[e]) == null ? void 0 : d[t]) == null || _[r];
    for (let M = s; M <= u; M++)
      for (let D = a; D <= c; D++) {
        if (M === t && r === D)
          continue;
        const L = (b = (E = (C = this._runtimeData) == null ? void 0 : C[i]) == null ? void 0 : E[e]) == null ? void 0 : b.getValue(M, D);
        (p = (y = f == null ? void 0 : f[i]) == null ? void 0 : y[e]) == null || p.getValue(
          M,
          D
        );
        const v = (S = (w = (N = l == null ? void 0 : l[i]) == null ? void 0 : N[e]) == null ? void 0 : w.cellData) == null ? void 0 : S.getValue(M, D), j = this._getRuntimeFeatureCellValue(M, D, e, i);
        if (!mn(L) || this._isInOtherArrayFormulaRange(i, e, t, r, M, D) || !mn(v) || !mn(j))
          return !0;
      }
    return !1;
  }
  _getRuntimeFeatureCellValue(i, e, t, r) {
    return Do(i, e, t, r, this._runtimeFeatureCellData);
  }
  _arrayCellHasData(i) {
    return i == null ? !1 : i.v !== void 0;
  }
  /**
   * If the current array formula in the extended area intersects with the existing array formula, a #SPILL! error will be reported. Note that if other array formulas are already #SPILL!, they will not conflict with the current array formula
   * @param formulaUnitId
   * @param formulaSheetId
   * @param formulaRow
   * @param formulaColumn
   * @param r
   * @param c
   * @returns
   */
  _isInOtherArrayFormulaRange(i, e, t, r, n, s) {
    var l;
    const a = (l = this._currentConfigService.getArrayFormulaRange()[i]) == null ? void 0 : l[e];
    if (a == null)
      return !1;
    let u = !1;
    return new Ce(a).forValue((f, h, d) => {
      var E, b;
      if (f === t && h === r)
        return;
      const _ = this._isInArrayFormulaRange(d, n, s), C = (b = (E = this._runtimeData[i]) == null ? void 0 : E[e]) == null ? void 0 : b.getValue(f, h);
      _ && (C == null ? void 0 : C.v) !== m.SPILL && (u = !0);
    }), u;
  }
  _isInArrayFormulaRange(i, e, t) {
    if (i == null)
      return !1;
    const { startRow: r, startColumn: n, endRow: s, endColumn: a } = i;
    return e >= r && e <= s && t >= n && t <= a;
  }
  _checkIfArrayFormulaExceeded(i, e, t) {
    return t.endRow >= i || t.endColumn >= e;
  }
  _isInDirtyRange(i, e, t, r) {
    const n = this._currentConfigService.getDirtyRanges();
    return n.length === 0 ? !0 : Lc(n, i, e, t, r);
  }
};
ea = lh([
  Pi(0, Qt),
  Pi(1, Yo)
], ea);
const Lt = ze("univer.formula.runtime.service");
var ie = /* @__PURE__ */ ((o) => (o[o.REFERENCE = 1] = "REFERENCE", o[o.VALUE = 2] = "VALUE", o[o.OPERATOR = 3] = "OPERATOR", o[o.FUNCTION = 4] = "FUNCTION", o[o.LAMBDA = 5] = "LAMBDA", o[o.LAMBDA_PARAMETER = 6] = "LAMBDA_PARAMETER", o[o.ERROR = 7] = "ERROR", o[o.BASE = 8] = "BASE", o[o.ROOT = 9] = "ROOT", o[o.UNION = 10] = "UNION", o[o.PREFIX = 11] = "PREFIX", o[o.SUFFIX = 12] = "SUFFIX", o[o.NULL = 13] = "NULL", o))(ie || {});
const Nt = /* @__PURE__ */ new Map([
  [1, 7],
  [2, 9],
  [3, 8],
  [4, 6],
  [5, 1],
  [6, 2],
  [9, 10],
  [10, 3],
  [11, 4],
  [12, 5]
]);
class We {
  constructor(i) {
    A(this, "_children", []);
    A(this, "_definedNames");
    A(this, "_parent");
    A(this, "_valueObject");
    A(this, "_calculateState", !1);
    A(this, "_async", !1);
    A(this, "_address", !1);
    A(this, "_isForcedCalculateFunction", !1);
    this._token = i;
  }
  dispose() {
    var i;
    this._children.forEach((e) => {
      e.dispose();
    }), (i = this._valueObject) == null || i.dispose(), this._valueObject = null, this._children = [], this._definedNames = null, this._parent = null;
  }
  get nodeType() {
    return ie.BASE;
  }
  isAsync() {
    return this._async;
  }
  isAddress() {
    return this._address;
  }
  isForcedCalculateFunction() {
    return this._isForcedCalculateFunction;
  }
  setAsync() {
    this._async = !0;
  }
  setAddress() {
    this._address = !0;
  }
  getParent() {
    return this._parent;
  }
  setParent(i) {
    this._parent = i, i.addChildren(this);
  }
  setForcedCalculateFunction() {
    this._isForcedCalculateFunction = !0;
  }
  getChildren() {
    return this._children;
  }
  addChildren(...i) {
    this._children.push(...i);
  }
  getToken() {
    return this._token;
  }
  setValue(i) {
    this._valueObject = i;
  }
  getValue() {
    return this._valueObject;
  }
  /**
   * Release intermediate value objects from this node and all descendants
   * without destroying the AST structure. The tree remains valid for
   * re-execution on the next dirty cycle.
   */
  clearIntermediate() {
    this._valueObject = null;
    const i = this._children;
    for (let e = 0, t = i.length; e < t; e++)
      i[e].clearIntermediate();
  }
  isCalculated() {
    return this._calculateState;
  }
  setCalculated() {
    this._calculateState = !0;
  }
  execute() {
  }
  setNotEmpty(i = !0) {
  }
  async executeAsync() {
    return Promise.resolve(Tt.SUCCESS);
  }
  serialize() {
    const i = this.getToken(), e = this.getChildren(), t = [], r = e.length;
    for (let s = 0; s < r; s++) {
      const a = e[s];
      t.push(a.serialize());
    }
    const n = {
      token: i,
      nodeType: this.nodeType
    };
    return r > 0 && (n.children = t), n;
  }
  hasDefinedName(i) {
    var e;
    return ((e = this._definedNames) == null ? void 0 : e.includes(i)) || !1;
  }
  setDefinedNames(i) {
    this._definedNames = i;
  }
  getDefinedNames() {
    return this._definedNames;
  }
}
class Pe extends We {
  constructor(e) {
    super(e);
    A(this, "_errorValueObject");
    this._errorValueObject = g.create(e);
  }
  get nodeType() {
    return ie.ERROR;
  }
  static create(e) {
    return new Pe(e);
  }
  getValue() {
    return this._errorValueObject;
  }
}
const Vt = 100;
class Ot {
  get zIndex() {
    return 0;
  }
  dispose() {
  }
  create(i, e, t) {
    let r;
    return i instanceof ae ? r = i.getToken() : r = i, new We(r);
  }
}
class ta extends We {
  get nodeType() {
    return ie.ROOT;
  }
  execute() {
    const i = this.getChildren();
    if (i.length > 1) {
      this.setValue(g.create(m.VALUE));
      return;
    }
    const e = i[0];
    e == null ? this.setValue(g.create(m.VALUE)) : this.setValue(e.getValue());
  }
}
class Ho extends Ot {
  get zIndex() {
    return Nt.get(ie.ROOT) || Vt;
  }
  checkAndCreateNodeType(i) {
    if (!(i instanceof ae))
      return;
    if (i.getToken() === It)
      return new ta(It);
  }
}
const fh = 5e3, Ir = new Dt(fh);
function ji(o, i, e, t, r) {
  let n = Ir.get(`${o}${i}`);
  if (n && !Qo(n, r))
    return n;
  const s = e.treeBuilder(i);
  if (lr.has(s))
    return Pe.create(s);
  if (n = t.parse(s), n == null)
    throw new Error("astNode is null");
  return Ir.set(`${o}${i}`, n), n;
}
function Qo(o, i) {
  const e = i.getDirtyDefinedNameMap(), t = i.getExecuteUnitId();
  if (t != null && e[t] != null) {
    const r = Object.keys(e[t]);
    for (let n = 0, s = r.length; n < s; n++) {
      const a = r[n];
      if (o.hasDefinedName(a))
        return !0;
    }
  }
  return !1;
}
function hh(o, i, e) {
  return !!(i != null && Qo(i, e));
}
const qt = ze("univer.formula-function.service");
class mh extends Ve {
  constructor() {
    super(...arguments);
    A(this, "_functionExecutors", /* @__PURE__ */ new Map());
    A(this, "_functionDescriptions", /* @__PURE__ */ new Map());
  }
  dispose() {
    super.dispose(), this._functionExecutors.clear(), this._functionDescriptions.clear();
  }
  registerExecutors(...e) {
    for (let t = 0; t < e.length; t++) {
      const r = e[t];
      this._functionExecutors.set(r.name, r);
    }
  }
  getExecutors() {
    return this._functionExecutors;
  }
  getExecutor(e) {
    return this._functionExecutors.get(e);
  }
  hasExecutor(e) {
    return this._functionExecutors.has(e);
  }
  unregisterExecutors(...e) {
    for (let t = 0; t < e.length; t++) {
      const r = e[t];
      this._functionExecutors.delete(r);
    }
  }
  registerDescriptions(...e) {
    for (let t = 0; t < e.length; t++) {
      const r = e[t];
      this._functionDescriptions.set(r.functionName, r);
    }
    return Ac(() => {
      for (let t = 0; t < e.length; t++) {
        const r = e[t];
        this._functionDescriptions.delete(r.functionName);
      }
    });
  }
  getDescriptions() {
    return this._functionDescriptions;
  }
  getDescription(e) {
    return this._functionDescriptions.get(e);
  }
  hasDescription(e) {
    return this._functionDescriptions.has(e);
  }
  unregisterDescriptions(...e) {
    for (let t = 0; t < e.length; t++) {
      const r = e[t];
      this._functionDescriptions.delete(r);
    }
  }
  deleteFormulaAstCacheKey(...e) {
    Ir.forEach((t, r) => {
      e.forEach((n) => {
        r.includes(n) && Ir.delete(r);
      });
    });
  }
}
var Be = /* @__PURE__ */ ((o) => (o.COMPARE = "COMPARE", o.DIVIDED = "DIVIDED", o.MINUS = "MINUS", o.MULTIPLY = "MULTIPLY", o.PLUS = "PLUS", o.UNION = "UNION", o.CUBE = "CUBE", o))(Be || {}), gh = Object.getOwnPropertyDescriptor, dh = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? gh(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, Ui = (o, i) => (e, t) => i(e, t, o);
class wn extends We {
  constructor(i, e, t) {
    super(e), this._runtimeService = i, this._operatorString = e, this._functionExecutor = t;
  }
  get nodeType() {
    return ie.PREFIX;
  }
  execute() {
    let e = this.getChildren()[0].getValue(), t;
    if (e == null)
      throw new Error("object is null");
    this._operatorString === we.MINUS ? (e.isReferenceObject() && (e = e.toArrayValueObject()), t = this._functionExecutor.calculate(
      R.create(0),
      e
    )) : this._operatorString === we.AT ? t = this._handlerAT(e) : t = g.create(m.VALUE), this.setValue(t);
  }
  _handlerAT(i) {
    if (!i.isReferenceObject())
      return g.create(m.VALUE);
    const e = i;
    if (e.isCell())
      return g.create(m.VALUE);
    const t = this._runtimeService, r = t.currentRow || 0, n = t.currentColumn || 0, s = e.getRangePosition(), { startRow: a, startColumn: u, endRow: c, endColumn: l } = s;
    return l !== u && c !== a || a === c && u === l ? g.create(m.VALUE) : c === a && n >= u && n <= l ? e.getCellByColumn(n) : u === l && r >= a && r <= c ? e.getCellByRow(r) : e.isTable() ? e.getCellByPosition(r) : g.create(m.VALUE);
  }
}
let Sn = class extends Ot {
  constructor(o, i) {
    super(), this._functionService = o, this._runtimeService = i;
  }
  get zIndex() {
    return Nt.get(ie.PREFIX) || Vt;
  }
  checkAndCreateNodeType(o) {
    if (!(o instanceof ae))
      return;
    const i = o.getToken(), e = i.trim();
    if (e.charAt(0) === '"' && e.charAt(e.length - 1) === '"')
      return;
    let t = "";
    if (e === we.MINUS)
      t = Be.MINUS;
    else return e === we.AT ? new wn(this._runtimeService, e) : void 0;
    const r = this._functionService.getExecutor(t);
    return r ? new wn(this._runtimeService, e, r) : (console.error(`No function ${i}`), Pe.create(m.NAME));
  }
};
Sn = dh([
  Ui(0, qt),
  Ui(1, Lt)
], Sn);
new RegExp(we.MINUS, "g");
new RegExp(we.AT, "g");
function Go(o, i, e) {
  let t, r, n = o;
  const s = n.slice(0, 2);
  let a = 0;
  if (s[0] === we.MINUS) {
    const u = i.getExecutor(Be.MINUS);
    t = new wn(e, we.MINUS, u), a++;
  }
  return s[0] === we.AT && (r = new wn(e, we.AT), t && r.setParent(t), a++), a > 0 && (n = n.slice(a)), { tokenTrim: n, minusPrefixNode: t, atPrefixNode: r };
}
var Ch = Object.getOwnPropertyDescriptor, _h = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? Ch(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, fr = (o, i) => (e, t) => i(e, t, o);
class Ah extends We {
  constructor(i, e, t, r, n, s) {
    super(i), this._functionExecutor = e, this._currentConfigService = t, this._runtimeService = r, this._definedNamesService = n, this._formulaDataModel = s, this._functionExecutor.isAsync() && this.setAsync(), this._functionExecutor.isAddress() && this.setAddress(), this._functionExecutor.needsLocale && this._setLocale(), this._functionExecutor.needsSheetsInfo && this._setSheetsInfo(), this._functionExecutor.needsFormulaDataModel && this._functionExecutor.setFormulaDataModel(this._formulaDataModel);
  }
  get nodeType() {
    return ie.FUNCTION;
  }
  async executeAsync() {
    const i = [], e = this.getChildren(), t = e.length;
    this._compatibility();
    for (let s = 0; s < t; s++) {
      const u = e[s].getValue();
      u != null && (u.isReferenceObject() && !this._functionExecutor.needsReferenceObject ? i.push(u.toArrayValueObject()) : i.push(u));
    }
    const r = await this._calculateAsync(i);
    let n;
    return r.isAsyncObject() || r.isAsyncArrayObject() ? n = await r.getValue() : n = r, this._setRefData(n), this.setValue(n), Promise.resolve(Tt.SUCCESS);
  }
  execute() {
    const i = [], e = this.getChildren(), t = e.length;
    this._compatibility();
    for (let n = 0; n < t; n++) {
      const a = e[n].getValue();
      a != null && (a.isReferenceObject() && this._functionExecutor.needsFilteredOutRows && this._setFilteredOutRows(a), a.isReferenceObject() && !this._functionExecutor.needsReferenceObject ? i.push(a.toArrayValueObject()) : i.push(a));
    }
    const r = this._calculate(i);
    this._setRefData(r), this.setValue(r);
  }
  isFunctionExecutorArgumentsIgnoreNumberPattern() {
    return this._functionExecutor.isArgumentsIgnoreNumberPattern();
  }
  /**
   * Compatibility handling for special functions.
   */
  _compatibility() {
    this._lookupCompatibility();
  }
  /**
   * The LOOKUP function follows the following rules when dealing with vectors of different sizes:
   *    If the lookup_vector is larger than the result_vector,
   *    the LOOKUP function will ignore the extra portion of the lookup_vector and only use the portion of the result_vector that is the same size as the lookup_vector for lookup and returning results.
   *    If the lookup_vector is smaller than the result_vector,
   *    the LOOKUP function will continue using the last value of the result_vector for lookup and returning results after the last value of the lookup_vector.
   */
  _lookupCompatibility() {
    const i = this.getChildren(), e = i.length;
    if (!this._functionExecutor.needsExpandParams || e !== 3)
      return;
    const t = i[1].getValue(), r = i[2].getValue();
    if (!(t != null && t.isReferenceObject()) && !(r != null && r.isReferenceObject()))
      return;
    let n, s;
    if (t != null && t.isReferenceObject()) {
      const _ = t.getRangeData(), { startRow: C, startColumn: E, endRow: b, endColumn: y } = _;
      n = b - C + 1, s = y - E + 1;
    } else
      n = t != null && t.isArray() ? t.getRowCount() : 1, s = t != null && t.isArray() ? t.getColumnCount() : 1;
    const a = r.getRangeData(), { startRow: u, startColumn: c, endRow: l, endColumn: f } = a, h = l - u + 1, d = f - c + 1;
    n !== h && (a.endRow += n - h), s !== d && (a.endColumn += s - d);
  }
  /**
   * Transform the result of a custom function to a NodeValueType.
   */
  _handleCustomResult(i) {
    var t;
    if (typeof i != "object" || i == null)
      return Mt.create(i);
    const e = $o(i);
    return W.create({
      calculateValueList: e,
      rowCount: e.length,
      columnCount: ((t = e[0]) == null ? void 0 : t.length) || 0,
      unitId: "",
      sheetId: "",
      row: -1,
      column: -1
    });
  }
  _handleAddressFunction() {
    this._functionExecutor.isAddress() && this._setDefinedNamesForFunction();
  }
  _mapVariantsToValues(i) {
    return i.map((e) => e.isArray() ? e.toValue() : e.isLambda() ? e : e.getValue());
  }
  _calculate(i) {
    const { minParams: e, maxParams: t } = this._functionExecutor;
    if (e !== -1 && t !== -1 && (i.length < e || i.length > t))
      return g.create(m.NA);
    let r;
    if (this._setRefInfo(), this._functionExecutor.isCustom()) {
      const n = this._functionExecutor.calculateCustom(
        ...this._mapVariantsToValues(i)
      );
      r = this._handleCustomResult(n);
    } else
      this._handleAddressFunction(), r = this._functionExecutor.calculate(...i);
    return r;
  }
  async _calculateAsync(i) {
    const { minParams: e, maxParams: t } = this._functionExecutor;
    if (e !== -1 && t !== -1 && (i.length < e || i.length > t))
      return g.create(m.NA);
    let r;
    if (this._setRefInfo(), this._functionExecutor.isCustom()) {
      const n = await this._functionExecutor.calculateCustom(
        ...this._mapVariantsToValues(i)
      );
      r = this._handleCustomResult(n);
    } else
      this._handleAddressFunction(), r = this._functionExecutor.calculate(...i);
    return r;
  }
  _setDefinedNamesForFunction() {
    const i = this._currentConfigService.getExecuteUnitId();
    if (i == null)
      return;
    const e = this._definedNamesService.getDefinedNameMap(i);
    e != null && this._functionExecutor.setDefinedNames(e);
  }
  _setRefInfo() {
    const { currentUnitId: i, currentSubUnitId: e, currentRow: t, currentColumn: r } = this._runtimeService;
    if (this._functionExecutor.setRefInfo(i, e, t, r), this._functionExecutor.needsSheetRowColumnCount) {
      const { rowCount: n, columnCount: s } = this._currentConfigService.getSheetRowColumnCount(i, e);
      this._functionExecutor.setSheetRowColumnCount(n, s);
    }
  }
  _setRefData(i) {
    if (!i.isReferenceObject())
      return;
    const e = i;
    e.setForcedSheetId(this._currentConfigService.getSheetNameMap()), e.setUnitData(this._currentConfigService.getUnitData()), e.setArrayFormulaCellData(this._currentConfigService.getArrayFormulaCellData()), e.setRuntimeData(this._runtimeService.getUnitData()), e.setRuntimeArrayFormulaCellData(this._runtimeService.getRuntimeArrayFormulaCellData()), e.setRuntimeFeatureCellData(this._runtimeService.getRuntimeFeatureCellData());
  }
  _setLocale() {
    this._functionExecutor.setLocale(this._currentConfigService.getLocale());
  }
  _setSheetsInfo() {
    this._functionExecutor.setSheetsInfo(this._currentConfigService.getSheetsInfo());
  }
  _setFilteredOutRows(i) {
    const { startRow: e, endRow: t } = i.getRangePosition(), r = this._currentConfigService.getFilteredOutRows(
      i.getUnitId(),
      i.getSheetId(),
      e,
      t
    );
    i.setFilteredOutRows(r);
  }
}
class Eh extends We {
  constructor(i = "Error") {
    super(i);
  }
  get nodeType() {
    return ie.FUNCTION;
  }
  async executeAsync() {
    return this.setValue(g.create(m.NAME)), Promise.resolve(Tt.SUCCESS);
  }
  execute() {
    this.setValue(g.create(m.NAME));
  }
}
let Mn = class extends Ot {
  constructor(o, i, e, t, r, n) {
    super(), this._functionService = o, this._currentConfigService = i, this._runtimeService = e, this._definedNamesService = t, this._injector = r, this._formulaDataModel = n;
  }
  get zIndex() {
    return Nt.get(ie.FUNCTION) || Vt;
  }
  create(o) {
    const i = this._functionService.getExecutor(o);
    return i ? new Ah(
      o,
      i,
      this._currentConfigService,
      this._runtimeService,
      this._definedNamesService,
      this._formulaDataModel
    ) : (console.error(`No function ${o}`), Pe.create(m.NAME));
  }
  checkAndCreateNodeType(o) {
    if (typeof o == "string")
      return;
    const i = o.getToken(), { tokenTrim: e, minusPrefixNode: t, atPrefixNode: r } = Go(i.trim(), this._functionService, this._runtimeService);
    if (!Number.isNaN(Number(e)) && !this._isParentUnionNode(o))
      return Pe.create(m.VALUE);
    const n = e.toUpperCase();
    if (this._functionService.hasExecutor(n)) {
      const s = this.create(n);
      return r ? s.setParent(r) : t && s.setParent(t), s;
    }
  }
  _isParentUnionNode(o) {
    var i, e;
    return ((e = (i = o.getParent()) == null ? void 0 : i.getParent()) == null ? void 0 : e.getToken()) === K.COLON;
  }
};
Mn = _h([
  fr(0, qt),
  fr(1, Qt),
  fr(2, Lt),
  fr(3, Sr),
  fr(4, ye(oo)),
  fr(5, ye(br))
], Mn);
var bh = Object.getOwnPropertyDescriptor, yh = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? bh(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, Rh = (o, i) => (e, t) => i(e, t, o);
let pr = class extends Ve {
  constructor(o) {
    super(), this._runtimeService = o;
  }
  async executeAsync(o) {
    if (!o || !o.node)
      return Promise.resolve(g.create(m.VALUE));
    const i = o.node, e = o.refOffsetX, t = o.refOffsetY;
    await this._executeAsync(i, e, t);
    const r = i.getValue();
    return r == null ? Promise.resolve(g.create(m.VALUE)) : Promise.resolve(r);
  }
  execute(o) {
    if (!o || !o.node)
      return g.create(m.VALUE);
    const i = o.node, e = o.refOffsetX, t = o.refOffsetY;
    this._execute(i, e, t);
    const r = i.getValue();
    return r == null ? g.create(m.VALUE) : r;
  }
  executePreCalculateNode(o) {
    return o.execute(), o.getValue();
  }
  checkAsyncNode(o) {
    if (o == null)
      return !1;
    const i = [];
    this._checkAsyncNode(o, i);
    for (let e = 0, t = i.length; e < t; e++)
      if (i[e] === !0)
        return !0;
    return !1;
  }
  _checkAsyncNode(o, i) {
    const e = o.getChildren(), t = e.length;
    for (let r = 0; r < t; r++) {
      const n = e[r];
      i.push(n.isAsync()), this._checkAsyncNode(n, i);
    }
  }
  async _executeAsync(o, i = 0, e = 0) {
    if (this._runtimeService.isStopExecution())
      return Promise.resolve(Tt.ERROR);
    const t = o.getChildren(), r = t.length;
    for (let n = 0; n < r; n++) {
      const s = t[n];
      if (s.getToken().toUpperCase() === vr && s.isEmptyParamFunction()) {
        s.execute();
        continue;
      }
      await this._executeAsync(s, i, e);
    }
    return o.nodeType === ie.REFERENCE && o.setRefOffset(i, e), o.nodeType === ie.FUNCTION && o.isAsync() ? await o.executeAsync() : o.execute(), Promise.resolve(Tt.SUCCESS);
  }
  _execute(o, i = 0, e = 0) {
    if (this._runtimeService.isStopExecution())
      return Tt.ERROR;
    const t = o.getChildren(), r = t.length;
    for (let n = 0; n < r; n++) {
      const s = t[n];
      if (s.getToken().toUpperCase() === vr && s.isEmptyParamFunction()) {
        s.execute();
        continue;
      }
      this._execute(s, i, e);
    }
    return o.nodeType === ie.REFERENCE && o.setRefOffset(i, e), o.execute(), Tt.SUCCESS;
  }
};
pr = yh([
  Rh(0, Lt)
], pr);
function Wo(o) {
  return o instanceof ae ? o.getToken() === Ar : !1;
}
function ph(o) {
  return o instanceof ae ? o.getToken() === Oo : !1;
}
function ja(o, i, e) {
  const t = o.getChildren(), r = t.length, n = t[0];
  for (let s = 0; s < r; s++) {
    const a = t[s];
    if (!(Wo(n) && s !== 0))
      if (a instanceof ae)
        ja(a, i, e);
      else {
        const u = a.trim();
        if (e.has(u)) {
          const c = new ae();
          c.setToken(Hr), c.setLambdaId(i), c.setLambdaPrivacyVar(e), c.setLambdaParameter(u), t[s] = c;
        }
      }
  }
}
function Nh(o) {
  let i = o;
  for (; i != null && i.getParent(); )
    i = i.getParent();
  return i;
}
function vi(o, i = 0, e = 0) {
  return {
    node: o,
    refOffsetX: i,
    refOffsetY: e
  };
}
function qo(o) {
  if (!o)
    return;
  if (o.getToken() !== Hr)
    return o;
  const i = o, e = i.getCurrentLambdaPrivacyVar(), t = i.getLambdaParameter();
  if (!e)
    return;
  const r = e.get(t);
  return r == null && o.getValue() ? o : qo(r);
}
class Ua extends Gt {
  constructor(e, t, r) {
    super(0);
    A(this, "_lambdaPrivacyValueMap", /* @__PURE__ */ new Map());
    this._lambdaNode = e, this._interpreter = t, this._lambdaPrivacyVarKeys = r, this._lambdaPrivacyValueMap.clear();
  }
  static create(e, t, r) {
    return new Ua(e, t, r);
  }
  dispose() {
    this._lambdaPrivacyValueMap.clear(), this._lambdaPrivacyValueMap = /* @__PURE__ */ new Map(), this._lambdaNode = null, this._interpreter = null, this._lambdaPrivacyVarKeys = [];
  }
  isLambda() {
    return !0;
  }
  execute(...e) {
    const t = this._lambdaPrivacyVarKeys.length;
    if (e.length !== t || !this._interpreter || !this._lambdaNode)
      return g.create(m.VALUE);
    this._setLambdaPrivacyValueMap(e), this._setLambdaNodeValue(this._lambdaNode), this._lambdaNode.setNotEmpty(!1);
    let r;
    if (this._interpreter.checkAsyncNode(this._lambdaNode))
      r = new zf(this._interpreter.executeAsync(vi(this._lambdaNode)));
    else {
      const n = this._interpreter.execute(vi(this._lambdaNode));
      n.isReferenceObject() ? r = n.toArrayValueObject() : r = n;
    }
    return this._lambdaNode.setNotEmpty(!0), r;
  }
  /**
   * Execute custom lambda function, handle basic types
   * @param variants
   */
  executeCustom(...e) {
    const t = e.map((r) => Mt.create(r));
    return this.execute(...t);
  }
  _setLambdaNodeValue(e) {
    if (!e)
      return;
    const t = e.getChildren(), r = t.length;
    for (let n = 0; n < r; n++) {
      const s = t[n];
      if (s.getToken() === Hr) {
        const u = s.getLambdaParameter(), c = this._lambdaPrivacyValueMap.get(u);
        if (c)
          s.setValue(c);
        else {
          const l = s.getCurrentLambdaPrivacyVar(), f = qo(l.get(u));
          f != null && s.setValue(f.getValue());
        }
        continue;
      }
      this._setLambdaNodeValue(s);
    }
  }
  _setLambdaPrivacyValueMap(e) {
    for (let t = 0; t < e.length; t++) {
      const r = e[t], n = this._lambdaPrivacyVarKeys[t];
      this._lambdaPrivacyValueMap.set(n, r);
    }
  }
  getLambdaPrivacyVarKeys() {
    return this._lambdaPrivacyVarKeys;
  }
}
var Vh = Object.getOwnPropertyDescriptor, Oh = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? Vh(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, Ti = (o, i) => (e, t) => i(e, t, o);
class wh extends We {
  constructor(e, t, r, n) {
    super(e);
    A(this, "_isNotEmpty", !0);
    this._lambdaId = t, this._interpreter = r, this._lambdaPrivacyVarKeys = n;
  }
  get nodeType() {
    return ie.LAMBDA;
  }
  setNotEmpty(e = !1) {
    this._isNotEmpty = e;
  }
  isEmptyParamFunction() {
    return this.getChildren().length < 2 && this._isNotEmpty;
  }
  isFunctionParameter() {
    return this._lambdaId === null;
  }
  getLambdaId() {
    return this._lambdaId;
  }
  execute() {
    if (this.isEmptyParamFunction())
      this.setValue(Ua.create(this, this._interpreter, this._lambdaPrivacyVarKeys));
    else {
      const e = this.getChildren(), t = e.length;
      this.setValue(e[t - 1].getValue());
    }
  }
  // override async executeAsync() {
  //     if (this.isEmptyParamFunction()) {
  //         await this.setValue(LambdaValueObjectObject.create(this, this._interpreter, this._lambdaPrivacyVarKeys));
  //     } else {
  //         const children = this.getChildren();
  //         const childrenCount = children.length;
  //         await this.setValue(children[childrenCount - 1].getValue());
  //     }
  //     return Promise.resolve(AstNodePromiseType.SUCCESS);
  // }
}
let Dn = class extends Ot {
  constructor(o, i) {
    super(), this._runtimeService = o, this._interpreter = i;
  }
  get zIndex() {
    return Nt.get(ie.LAMBDA) || Vt;
  }
  create(o) {
    const i = o.getChildren(), e = i[0];
    let t = i.slice(1, -1);
    const r = i[i.length - 1];
    if (!(e instanceof ae && r instanceof ae))
      return Pe.create(m.NAME);
    if (e.getToken() === Ar) {
      const a = e.getChildren();
      if (t.length !== a.length)
        return Pe.create(m.VALUE);
    } else
      t = i.slice(0, -1);
    const n = Ec(8), s = /* @__PURE__ */ new Map();
    for (let a = 0; a < t.length; a++) {
      const u = t[a];
      if (u instanceof ae) {
        const c = u.getChildren()[0];
        u.setToken(Oo), s.set(c.trim(), void 0);
      } else
        return Pe.create(m.VALUE);
    }
    return this._runtimeService.registerFunctionDefinitionPrivacyVar(n, s), this._updateLambdaStatement(r, n, s), new wh(o.getToken(), n, this._interpreter, [...s.keys()]);
  }
  checkAndCreateNodeType(o) {
    if (!(!(o instanceof ae) || o.getToken().trim().toUpperCase() !== vr))
      return this.create(o);
  }
  _updateLambdaStatement(o, i, e) {
    ja(o, i, e);
  }
};
Dn = Oh([
  Ti(0, Lt),
  Ti(1, ye(pr))
], Dn);
function Ko(o) {
  if (!o)
    return;
  if (o.getToken() !== Hr)
    return o;
  const i = o, e = i.getCurrentLambdaPrivacyVar(), t = i.getLambdaParameter();
  if (e)
    return Ko(e.get(t));
}
class Sh extends We {
  constructor(i, e, t) {
    super(i), this._lambdaParameter = e, this._currentLambdaPrivacyVar = t;
  }
  getLambdaParameter() {
    return this._lambdaParameter;
  }
  getCurrentLambdaPrivacyVar() {
    return this._currentLambdaPrivacyVar;
  }
  get nodeType() {
    return ie.LAMBDA_PARAMETER;
  }
  execute() {
    const i = Ko(this._currentLambdaPrivacyVar.get(this._lambdaParameter));
    if (i)
      this.setValue(i.getValue());
    else {
      const e = this.getValue();
      (e == null || e.isError()) && this.setValue(g.create(m.NAME));
    }
  }
}
class Xo extends Ot {
  get zIndex() {
    return Nt.get(ie.LAMBDA_PARAMETER) || Vt;
  }
  create(i) {
    const e = i.getFunctionDefinitionPrivacyVar(), t = i.getLambdaParameter();
    return e ? new Sh(i.getToken(), t, e) : new Pe(m.NAME);
  }
  checkAndCreateNodeType(i) {
    if (!(!(i instanceof ae) || i.getToken().trim() !== Hr))
      return this.create(i);
  }
}
class Mh extends We {
  constructor(i) {
    super(i), this._operatorString = i;
  }
  get nodeType() {
    return ie.NULL;
  }
  execute() {
    this.setValue(ue.create());
  }
}
var Y = /* @__PURE__ */ ((o) => (o.ABS = "ABS", o.ACOS = "ACOS", o.ACOSH = "ACOSH", o.ACOT = "ACOT", o.ACOTH = "ACOTH", o.AGGREGATE = "AGGREGATE", o.ARABIC = "ARABIC", o.ASIN = "ASIN", o.ASINH = "ASINH", o.ATAN = "ATAN", o.ATAN2 = "ATAN2", o.ATANH = "ATANH", o.BASE = "BASE", o.CEILING = "CEILING", o.CEILING_MATH = "CEILING.MATH", o.CEILING_PRECISE = "CEILING.PRECISE", o.COMBIN = "COMBIN", o.COMBINA = "COMBINA", o.COS = "COS", o.COSH = "COSH", o.COT = "COT", o.COTH = "COTH", o.CSC = "CSC", o.CSCH = "CSCH", o.DECIMAL = "DECIMAL", o.DEGREES = "DEGREES", o.EVEN = "EVEN", o.EXP = "EXP", o.FACT = "FACT", o.FACTDOUBLE = "FACTDOUBLE", o.FLOOR = "FLOOR", o.FLOOR_MATH = "FLOOR.MATH", o.FLOOR_PRECISE = "FLOOR.PRECISE", o.GCD = "GCD", o.INT = "INT", o.ISO_CEILING = "ISO.CEILING", o.LCM = "LCM", o.LET = "LET", o.LN = "LN", o.LOG = "LOG", o.LOG10 = "LOG10", o.MDETERM = "MDETERM", o.MINVERSE = "MINVERSE", o.MMULT = "MMULT", o.MOD = "MOD", o.MROUND = "MROUND", o.MULTINOMIAL = "MULTINOMIAL", o.MUNIT = "MUNIT", o.ODD = "ODD", o.PI = "PI", o.POWER = "POWER", o.PRODUCT = "PRODUCT", o.QUOTIENT = "QUOTIENT", o.RADIANS = "RADIANS", o.RAND = "RAND", o.RANDARRAY = "RANDARRAY", o.RANDBETWEEN = "RANDBETWEEN", o.ROMAN = "ROMAN", o.ROUND = "ROUND", o.ROUNDBANK = "ROUNDBANK", o.ROUNDDOWN = "ROUNDDOWN", o.ROUNDUP = "ROUNDUP", o.SEC = "SEC", o.SECH = "SECH", o.SERIESSUM = "SERIESSUM", o.SEQUENCE = "SEQUENCE", o.SIGN = "SIGN", o.SIN = "SIN", o.SINH = "SINH", o.SQRT = "SQRT", o.SQRTPI = "SQRTPI", o.SUBTOTAL = "SUBTOTAL", o.SUM = "SUM", o.SUMIF = "SUMIF", o.SUMIFS = "SUMIFS", o.SUMPRODUCT = "SUMPRODUCT", o.SUMSQ = "SUMSQ", o.SUMX2MY2 = "SUMX2MY2", o.SUMX2PY2 = "SUMX2PY2", o.SUMXMY2 = "SUMXMY2", o.TAN = "TAN", o.TANH = "TANH", o.TRUNC = "TRUNC", o))(Y || {}), se = /* @__PURE__ */ ((o) => (o.ASC = "ASC", o.ARRAYTOTEXT = "ARRAYTOTEXT", o.BAHTTEXT = "BAHTTEXT", o.CHAR = "CHAR", o.CLEAN = "CLEAN", o.CODE = "CODE", o.CONCAT = "CONCAT", o.CONCATENATE = "CONCATENATE", o.DBCS = "DBCS", o.DOLLAR = "DOLLAR", o.EXACT = "EXACT", o.FIND = "FIND", o.FINDB = "FINDB", o.FIXED = "FIXED", o.LEFT = "LEFT", o.LEFTB = "LEFTB", o.LEN = "LEN", o.LENB = "LENB", o.LOWER = "LOWER", o.MID = "MID", o.MIDB = "MIDB", o.NUMBERSTRING = "NUMBERSTRING", o.NUMBERVALUE = "NUMBERVALUE", o.PHONETIC = "PHONETIC", o.PROPER = "PROPER", o.REGEXEXTRACT = "REGEXEXTRACT", o.REGEXMATCH = "REGEXMATCH", o.REGEXREPLACE = "REGEXREPLACE", o.REPLACE = "REPLACE", o.REPLACEB = "REPLACEB", o.REPT = "REPT", o.RIGHT = "RIGHT", o.RIGHTB = "RIGHTB", o.SEARCH = "SEARCH", o.SEARCHB = "SEARCHB", o.SUBSTITUTE = "SUBSTITUTE", o.T = "T", o.TEXT = "TEXT", o.TEXTAFTER = "TEXTAFTER", o.TEXTBEFORE = "TEXTBEFORE", o.TEXTJOIN = "TEXTJOIN", o.TEXTSPLIT = "TEXTSPLIT", o.TRIM = "TRIM", o.UNICHAR = "UNICHAR", o.UNICODE = "UNICODE", o.UPPER = "UPPER", o.VALUE = "VALUE", o.VALUETOTEXT = "VALUETOTEXT", o.CALL = "CALL", o.EUROCONVERT = "EUROCONVERT", o.REGISTER_ID = "REGISTER.ID", o))(se || {}), Dh = Object.getOwnPropertyDescriptor, Lh = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? Dh(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, xh = (o, i) => (e, t) => i(e, t, o);
class Ph extends We {
  constructor(i, e) {
    super(i), this._functionExecutor = e;
  }
  get nodeType() {
    return ie.OPERATOR;
  }
  execute() {
    const i = this.getChildren();
    this._functionExecutor.name === Be.COMPARE && this._functionExecutor.setCompareType(this.getToken());
    const e = i[0], t = i[1];
    let r = e == null ? void 0 : e.getValue(), n = t == null ? void 0 : t.getValue();
    const s = this.getToken();
    if ((r == null || n == null) && s !== X.MINUS && s !== X.PLUS) {
      this.setValue(g.create(m.VALUE));
      return;
    }
    r == null && (r = ue.create()), n == null && (n = ue.create()), r.isReferenceObject() && (r = r.toArrayValueObject()), n.isReferenceObject() && (n = n.toArrayValueObject()), this.setValue(
      this._functionExecutor.calculate(
        r,
        n
      )
    );
  }
}
let Ln = class extends Ot {
  constructor(o) {
    super(), this._functionService = o;
  }
  get zIndex() {
    return Nt.get(ie.OPERATOR) || Vt;
  }
  create(o) {
    let i = "";
    const e = o;
    e === X.PLUS ? i = Be.PLUS : e === X.MINUS ? i = Be.MINUS : e === X.MULTIPLY ? i = Be.MULTIPLY : e === X.DIVIDED ? i = Be.DIVIDED : e === X.CONCATENATE ? i = se.CONCATENATE : e === X.POWER ? i = Y.POWER : Pc.has(e) && (i = Be.COMPARE);
    const t = this._functionService.getExecutor(i);
    return t ? new Ph(e, t) : (console.error(`No function ${o}`), Pe.create(m.NAME));
  }
  checkAndCreateNodeType(o) {
    if (o instanceof ae)
      return;
    const i = o.trim();
    if (!(i.charAt(0) === '"' && i.charAt(i.length - 1) === '"') && Ut.has(i))
      return this.create(i);
  }
};
Ln = Lh([
  xh(0, qt)
], Ln);
var jh = Object.getOwnPropertyDescriptor, Uh = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? jh(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, bs = (o, i) => (e, t) => i(e, t, o);
class ys extends We {
  constructor(e, t, r, n, s = !1) {
    super(r);
    A(this, "_refOffsetX", 0);
    A(this, "_refOffsetY", 0);
    this._currentConfigService = e, this._runtimeService = t, this._referenceObjectType = n, this._isPrepareMerge = s;
  }
  get nodeType() {
    return ie.REFERENCE;
  }
  execute() {
    const e = this._currentConfigService, t = this._runtimeService, r = sh(this.getToken(), this._referenceObjectType);
    r.setDefaultUnitId(t.currentUnitId), r.setDefaultSheetId(t.currentSubUnitId), r.setForcedSheetId(e.getSheetNameMap()), r.setUnitData(e.getUnitData()), r.setArrayFormulaCellData(e.getArrayFormulaCellData()), r.setRuntimeData(t.getUnitData()), r.setUnitStylesData(e.getUnitStylesData()), r.setRuntimeArrayFormulaCellData(t.getRuntimeArrayFormulaCellData()), r.setRuntimeFeatureCellData(t.getRuntimeFeatureCellData());
    const { x: n, y: s } = this.getRefOffset();
    r.setRefOffset(n, s), !this._isPrepareMerge && r.isExceedRange() ? this.setValue(g.create(m.NAME)) : this.setValue(r);
  }
  setRefOffset(e = 0, t = 0) {
    this._refOffsetX = e, this._refOffsetY = t;
  }
  getRefOffset() {
    return {
      x: this._refOffsetX,
      y: this._refOffsetY
    };
  }
}
let xn = class extends Ot {
  constructor(o, i, e) {
    super(), this._currentConfigService = o, this._formulaRuntimeService = i, this._functionService = e;
  }
  get zIndex() {
    return Nt.get(ie.REFERENCE) || Vt;
  }
  checkAndCreateNodeType(o) {
    var l, f;
    let i = !1, e, t = !1;
    o instanceof ae ? (i = !0, e = o.getToken().trim(), ((f = (l = o.getParent()) == null ? void 0 : l.getParent()) == null ? void 0 : f.getToken().trim()) === K.COLON && (t = !0)) : e = o.trim();
    const r = this._currentConfigService, n = this._formulaRuntimeService, { tokenTrim: s, minusPrefixNode: a, atPrefixNode: u } = Go(e, this._functionService, n);
    if (!i && s.charAt(0) === '"' && s.charAt(s.length - 1) === '"')
      return;
    let c;
    if (Yr(s) ? c = new ys(r, n, s, Cn.CELL, t) : i && this._checkParentIsUnionOperator(o) && (go(s) ? c = new ys(r, n, s, Cn.ROW, t) : Co(s) && (c = new ys(r, n, s, Cn.COLUMN, t))), c)
      return u ? c.setParent(u) : a && c.setParent(a), c;
  }
  _checkParentIsUnionOperator(o) {
    var i, e;
    return ((e = (i = o.getParent()) == null ? void 0 : i.getParent()) == null ? void 0 : e.getToken().trim()) === K.COLON;
  }
};
xn = Uh([
  bs(0, Qt),
  bs(1, Lt),
  bs(2, qt)
], xn);
var vh = Object.getOwnPropertyDescriptor, Th = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? vh(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, Rs = (o, i) => (e, t) => i(e, t, o);
class Bi extends We {
  constructor(i, e, t, r) {
    super(t), this._currentConfigService = i, this._lexer = e, this._operatorString = t, this._functionExecutor = r;
  }
  get nodeType() {
    return ie.SUFFIX;
  }
  execute() {
    var r;
    let e = (r = this.getChildren()[0]) == null ? void 0 : r.getValue(), t;
    if (e == null) {
      this.setValue(g.create(m.ERROR));
      return;
    }
    if (this._operatorString === tt.PERCENTAGE) {
      if (e.isReferenceObject() && (e = e.toArrayValueObject()), t = this._functionExecutor.calculate(
        e,
        R.create(100)
      ), t.isNumber()) {
        const n = Number(t.getValue());
        t = R.create(n, "0.00%");
      }
    } else this._operatorString === tt.POUND ? t = this._handlerPound(e) : t = g.create(m.VALUE);
    this.setValue(t);
  }
  _handlerPound(i) {
    var u, c, l, f;
    if (!i.isReferenceObject() || !i.isCell())
      return g.create(m.VALUE);
    const e = i, t = e.getRangePosition(), r = e.getUnitId(), n = e.getSheetId(), s = this._currentConfigService.getFormulaData(), a = (f = (l = (c = (u = s == null ? void 0 : s[r]) == null ? void 0 : u[n]) == null ? void 0 : c[t.startRow]) == null ? void 0 : l[t.startColumn]) == null ? void 0 : f.f;
    return a && this._lexer.treeBuilder(a), g.create(m.VALUE);
  }
}
let Pn = class extends Ot {
  constructor(o, i, e) {
    super(), this._functionService = o, this._lexer = i, this._currentConfigService = e;
  }
  get zIndex() {
    return Nt.get(ie.SUFFIX) || Vt;
  }
  checkAndCreateNodeType(o) {
    if (!(o instanceof ae))
      return;
    const i = o.getToken().trim();
    if (i.charAt(0) === '"' && i.charAt(i.length - 1) === '"')
      return;
    let e = "";
    if (i === tt.PERCENTAGE)
      e = Be.DIVIDED;
    else return i === tt.POUND ? new Bi(this._currentConfigService, this._lexer, i) : void 0;
    const t = this._functionService.getExecutor(e);
    return t ? new Bi(this._currentConfigService, this._lexer, i, t) : (console.error(`No function ${o}`), Pe.create(m.NAME));
  }
};
Pn = Th([
  Rs(0, qt),
  Rs(1, ye(yr)),
  Rs(2, Qt)
], Pn);
var Bh = Object.getOwnPropertyDescriptor, Ih = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? Bh(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, Fh = (o, i) => (e, t) => i(e, t, o);
class kh extends We {
  constructor(i) {
    super(i);
  }
  get nodeType() {
    return ie.UNION;
  }
  execute() {
    const i = this.getChildren(), e = i[0], t = i[1], r = e.getValue(), n = t.getValue();
    if (r == null || n == null)
      throw new Error("leftNode and rightNode");
    let s;
    this.getToken() === K.COLON ? s = this._unionFunction(r, n) : s = g.create(m.NAME), this.setValue(s);
  }
  _unionFunction(i, e) {
    return i.isError() || e.isError() || !i.isReferenceObject() || !e.isReferenceObject() ? g.create(m.REF) : (i = i, e = e, ah(i, e));
  }
}
let jn = class extends Ot {
  constructor(o) {
    super(), this._functionService = o;
  }
  get zIndex() {
    return Nt.get(ie.UNION) || Vt;
  }
  create(o) {
    return new kh(o);
  }
  checkAndCreateNodeType(o) {
    if (!(o instanceof ae))
      return;
    const e = o.getToken().trim();
    if (!(e.charAt(0) === '"' && e.charAt(e.length - 1) === '"') && e === K.COLON)
      return this.create(e);
  }
};
jn = Ih([
  Fh(0, qt)
], jn);
class $h extends We {
  constructor(i) {
    super(i);
  }
  get nodeType() {
    return ie.VALUE;
  }
  execute() {
    var e, t, r;
    const i = ((e = this.getParent()) == null ? void 0 : e.nodeType) === ie.FUNCTION && ((r = (t = this.getParent()) == null ? void 0 : t.isFunctionExecutorArgumentsIgnoreNumberPattern) == null ? void 0 : r.call(t));
    this.setValue(Mt.create(this.getToken(), i));
  }
}
class Zo extends Ot {
  get zIndex() {
    return Nt.get(ie.VALUE) || Vt;
  }
  _checkValueNode(i) {
    if (Number.isNaN(Number(i))) {
      const e = i.trim(), t = e.charAt(0), r = e.charAt(e.length - 1);
      if (lr.has(e))
        return this.create(e);
      if (t === '"' && r === '"')
        return this.create(e);
      if (t === "{" && r === "}")
        return this.create(e);
      const n = e.toUpperCase();
      if (n === ar.TRUE || n === ar.FALSE)
        return this.create(n);
    } else
      return this.create(i);
  }
  create(i) {
    return new $h(i);
  }
  checkAndCreateNodeType(i) {
    if (!(i instanceof ae))
      return this._checkValueNode(i);
  }
}
var Yh = Object.getOwnPropertyDescriptor, Hh = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? Yh(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, ut = (o, i) => (e, t) => i(e, t, o);
let Fr = class extends Ve {
  constructor(i, e, t, r, n, s, a, u, c, l, f) {
    super();
    A(this, "_astNodeFactoryList", []);
    this._runtimeService = i, this._astRootNodeFactory = e, this._functionNodeFactory = t, this._lambdaNodeFactory = r, this._lambdaParameterNodeFactory = n, this._operatorNodeFactory = s, this._prefixNodeFactory = a, this._referenceNodeFactory = u, this._suffixNodeFactory = c, this._unionNodeFactory = l, this._valueNodeFactory = f, this._initializeAstNode();
  }
  dispose() {
    this._astNodeFactoryList.forEach((i) => {
      i.dispose();
    }), this._astNodeFactoryList = [];
  }
  parse(i) {
    const e = new ta(It), t = this._parse(i, e);
    return i.hasDefinedNames() && (t == null || t.setDefinedNames(i.getDefinedNames())), t;
  }
  _lambdaParameterHandler(i, e) {
    if (e.getLambdaId == null)
      return Pe.create(m.VALUE);
    const t = e.getLambdaId(), r = new ta(It), n = this._runtimeService.getFunctionDefinitionPrivacyVar(t);
    if (!n)
      return !1;
    const s = [...n.keys()], a = i.getChildren(), u = a.length;
    for (let c = 0; c < u; c++) {
      const l = a[c];
      if (l instanceof ae) {
        ja(l, t, n), this._parse(l, r);
        const h = r.getChildren()[c];
        h != null && n.set(s[c], h);
      } else
        return !1;
    }
    return r.setParent(e), e;
  }
  _changeLetToLambda(i) {
    const e = i.getChildren(), t = e.length;
    if (t % 2 !== 1 || t === 0)
      return;
    const r = new ae();
    r.setToken(vr);
    const n = new ae();
    n.setToken(Ar);
    const s = [...e];
    for (let u = 0; u < t; u++) {
      const c = s[u];
      if (!(c instanceof ae))
        return;
      u % 2 === 0 ? c.changeToParent(r) : c.changeToParent(n);
    }
    r.addChildrenFirst(n), n.setParent(r);
    const a = i.getParent();
    return a == null || a.replaceChild(i, r), r;
  }
  // eslint-disable-next-line max-lines-per-function, complexity
  _parse(i, e) {
    var l;
    const t = i.getChildren(), r = t.length, n = [];
    let s = null;
    const a = i.getToken().trim().toUpperCase();
    if (a === ml) {
      const f = this._changeLetToLambda(i);
      return f != null ? this._parse(f, e) : Pe.create(m.ERROR);
    }
    if (a === dt) {
      if (s = e, r === 0)
        return new Mh(It).setParent(e), s;
    } else {
      if (a === Ar) {
        let f = this._lambdaParameterHandler(i, e);
        return f === !1 && (f = Pe.create(m.ERROR)), f;
      }
      if (s = this._checkAstNode(i), s == null)
        return Pe.create(m.NAME);
    }
    const u = t[0];
    for (let f = 0; f < r; f++) {
      const h = t[f];
      if (Wo(u)) {
        if (f !== 0 && f !== r - 1)
          continue;
      } else if (ph(h) && f !== r - 1)
        continue;
      let d = null;
      if (h instanceof ae) {
        if (h.getToken() === dt && h.getChildren().length === 0) {
          const _ = (l = h.getParent()) == null ? void 0 : l.getChildren();
          if (_ && _.length === 1)
            return Pe.create(m.NAME);
        }
        if (d = this._parse(h, s), d === s)
          continue;
      } else
        d = this._checkAstNode(h);
      if (d == null)
        return Pe.create(m.NAME);
      if (d = Nh(d), d == null || (d == null ? void 0 : d.nodeType) === ie.ERROR)
        return d;
      switch (d.nodeType) {
        // case NodeType.ERROR:
        //     return astNode;
        case ie.FUNCTION: {
          const _ = d.getToken().trim().toUpperCase();
          dl.has(_) && d.setForcedCalculateFunction(), n.push(d);
          break;
        }
        case ie.LAMBDA:
          n.push(d);
          break;
        case ie.LAMBDA_PARAMETER:
          n.push(d);
          break;
        case ie.OPERATOR: {
          const _ = n.pop(), C = n.pop();
          C && C.setParent(d), _ && _.setParent(d), n.push(d);
          break;
        }
        case ie.REFERENCE:
          n.push(d);
          break;
        case ie.ROOT:
          n.push(d);
          break;
        case ie.UNION:
          n.push(d);
          break;
        case ie.VALUE:
          n.push(d);
          break;
        case ie.PREFIX:
          n.push(d);
          break;
        case ie.SUFFIX:
          n.push(d);
          break;
      }
    }
    const c = n.length;
    for (let f = 0; f < c; f++)
      n[f].setParent(s);
    return s;
  }
  // private _setPrefixRefOffset(astNode: BaseAstNode) {
  //     const children = astNode.getChildren();
  //     const childrenCount = children.length;
  //     for (let i = 0; i < childrenCount; i++) {
  //         const item = children[i];
  //         if (item.nodeType === NodeType.REFERENCE) {
  //             item.setRefOffset(this._refOffsetX, this._refOffsetY);
  //         }
  //     }
  // }
  _checkAstNode(i) {
    let e = null;
    const t = this._astNodeFactoryList.length;
    for (let r = 0; r < t && (e = this._astNodeFactoryList[r].checkAndCreateNodeType(i), e == null); r++)
      ;
    return e == null ? new Eh() : e;
  }
  _initializeAstNode() {
    this._astNodeFactoryList = [
      this._astRootNodeFactory,
      this._functionNodeFactory,
      this._lambdaNodeFactory,
      this._lambdaParameterNodeFactory,
      this._operatorNodeFactory,
      this._prefixNodeFactory,
      this._referenceNodeFactory,
      this._suffixNodeFactory,
      this._unionNodeFactory,
      this._valueNodeFactory
    ].sort(bc);
  }
};
Fr = Hh([
  ut(0, Lt),
  ut(1, ye(Ho)),
  ut(2, ye(Mn)),
  ut(3, ye(Dn)),
  ut(4, ye(Xo)),
  ut(5, ye(Ln)),
  ut(6, ye(Sn)),
  ut(7, ye(xn)),
  ut(8, ye(Pn)),
  ut(9, ye(jn)),
  ut(10, ye(Zo))
], Fr);
class Qh extends Ve {
  constructor() {
    super(...arguments);
    A(this, "_otherFormulaData", /* @__PURE__ */ new Map());
    //  [unitId: string]: Nullable<{ [sheetId: string]: { [formulaId: string]: Set<number> } }>;
    A(this, "_featureFormulaData", /* @__PURE__ */ new Map());
    // [unitId: string]: Nullable<{ [sheetId: string]: { [featureId: string]: Nullable<number> } }>;
    A(this, "_formulaData", /* @__PURE__ */ new Map());
    // [unitId: string]: Nullable<{ [sheetId: string]: ObjectMatrix<number> }>;
    A(this, "_definedNameMap", /* @__PURE__ */ new Map());
    // unitId -> definedName -> treeId
    A(this, "_otherFormulaDataMainData", /* @__PURE__ */ new Set());
    A(this, "_dependencyRTreeCache", new Ps());
    A(this, "_dependencyTreeIdLast", 0);
  }
  buildDependencyTree(e, t) {
    throw new Error("Method not implemented.");
  }
  getTreeById(e) {
    throw new Error("Method not implemented.");
  }
  getAllTree() {
    throw new Error("Method not implemented.");
  }
  reset() {
    throw new Error("Method not implemented.");
  }
  addOtherFormulaDependency(e, t, r, n) {
    throw new Error("Method not implemented.");
  }
  removeOtherFormulaDependency(e, t, r) {
    throw new Error("Method not implemented.");
  }
  clearOtherFormulaDependency(e, t) {
    throw new Error("Method not implemented.");
  }
  addFeatureFormulaDependency(e, t, r, n) {
    throw new Error("Method not implemented.");
  }
  removeFeatureFormulaDependency(e, t, r) {
    throw new Error("Method not implemented.");
  }
  clearFeatureFormulaDependency(e, t) {
    throw new Error("Method not implemented.");
  }
  addFormulaDependency(e, t, r, n, s) {
    throw new Error("Method not implemented.");
  }
  removeFormulaDependency(e, t, r, n) {
    throw new Error("Method not implemented.");
  }
  clearFormulaDependency(e, t) {
    throw new Error("Method not implemented.");
  }
  removeFormulaDependencyByDefinedName(e, t) {
    throw new Error("Method not implemented.");
  }
  searchDependency(e, t) {
    return this._dependencyRTreeCache.bulkSearch(e, t);
  }
  _restDependencyTreeId() {
    this._dependencyTreeIdLast = 0;
  }
  getOtherFormulaDependency(e, t, r) {
    var n, s;
    return (s = (n = this._otherFormulaData.get(e)) == null ? void 0 : n.get(t)) == null ? void 0 : s.get(r);
  }
  addOtherFormulaDependencyMainData(e) {
    this._otherFormulaDataMainData.add(e);
  }
  hasOtherFormulaDataMainData(e) {
    return this._otherFormulaDataMainData.has(e);
  }
  _removeDependencyRTreeCacheById(e, t) {
    this._dependencyRTreeCache.removeById(e, t);
  }
  getFeatureFormulaDependency(e, t, r) {
    var n, s;
    return (s = (n = this._featureFormulaData.get(e)) == null ? void 0 : n.get(t)) == null ? void 0 : s.get(r);
  }
  getFormulaDependency(e, t, r, n) {
    var s, a;
    return (a = (s = this._formulaData.get(e)) == null ? void 0 : s.get(t)) == null ? void 0 : a.getValue(r, n);
  }
  addDependencyRTreeCache(e) {
    const t = [];
    for (let r = 0; r < e.rangeList.length; r++) {
      const n = e.rangeList[r], { unitId: s, sheetId: a, range: u } = n;
      t.push({
        unitId: s,
        sheetId: a,
        range: u,
        id: e.treeId
      });
    }
    this._dependencyRTreeCache.bulkInsert(t), this._addAllTreeMap(e);
  }
  getLastTreeId() {
    const e = this._dependencyTreeIdLast;
    return this._dependencyTreeIdLast++, e;
  }
  _addAllTreeMap(e) {
    throw new Error("Method not implemented.");
  }
  _addDefinedName(e, t, r) {
    this._definedNameMap.has(e) || this._definedNameMap.set(e, /* @__PURE__ */ new Map());
    const n = this._definedNameMap.get(e);
    n.has(t) || n.set(t, /* @__PURE__ */ new Set()), n.get(t).add(r);
  }
  addFormulaDependencyByDefinedName(e, t) {
    const r = e.treeId, n = (t == null ? void 0 : t.getDefinedNames()) || [];
    for (const s of n)
      this._addDefinedName(e.unitId, s, r);
  }
}
class Gh extends Qh {
  constructor() {
    super(...arguments);
    A(this, "_allTreeMap", /* @__PURE__ */ new Map());
  }
  dispose() {
    super.dispose(), this.reset();
  }
  buildDependencyTree(e, t = []) {
    const r = this.getAllTree();
    return e.length === 0 ? (this._buildReverseDependency(r, t), r) : (this._buildDependencyTree(r, e), this._buildReverseDependency(r, e), r);
  }
  /**
   * Build the dependency relationship between the trees.
   * @param allTrees  all FormulaDependencyTree
   * @param shouldBeBuildTrees  FormulaDependencyTree[] | FormulaDependencyTreeCache
   */
  _buildDependencyTree(e, t) {
    const r = /* @__PURE__ */ new Map();
    for (let n = 0; n < t.length; n++) {
      const s = t[n];
      r.set(s.treeId, s);
    }
    for (let n = 0; n < e.length; n++) {
      const s = e[n], a = s.toRTreeItem(), u = this._dependencyRTreeCache.bulkSearch(a);
      for (const c of u) {
        const l = r.get(c);
        l && s !== l && !l.hasChildren(s.treeId) && l.pushChildren(s);
      }
    }
    r.clear();
  }
  /**
   * Build the reverse dependency relationship between the trees.
   * @param allTrees
   * @param dependencyTrees
   */
  _buildReverseDependency(e, t) {
    const r = /* @__PURE__ */ new Map();
    for (let n = 0; n < e.length; n++) {
      const s = e[n];
      r.set(s.treeId, s);
    }
    for (let n = 0; n < t.length; n++) {
      const s = t[n], a = s.toRTreeItem(), u = this._dependencyRTreeCache.bulkSearch(a);
      for (const c of u) {
        const l = r.get(c);
        l && s !== l && !l.hasChildren(s.treeId) && l.pushChildren(s);
      }
    }
    r.clear();
  }
  /**
   * Get all FormulaDependencyTree from _otherFormulaData, _featureFormulaData, _formulaData
   * return FormulaDependencyTree[]
   */
  getAllTree() {
    const e = [];
    return this._allTreeMap.forEach((t) => {
      t.resetState(), e.push(t);
    }), e;
  }
  getTreeById(e) {
    return this._allTreeMap.get(e);
  }
  reset() {
    this._otherFormulaData.clear(), this._featureFormulaData.clear(), this._formulaData.clear(), this._definedNameMap.clear(), this._otherFormulaDataMainData.clear(), this._dependencyRTreeCache.clear(), this._allTreeMap.clear(), this._restDependencyTreeId();
  }
  addOtherFormulaDependency(e, t, r, n) {
    this._otherFormulaData.has(e) || this._otherFormulaData.set(e, /* @__PURE__ */ new Map());
    const s = this._otherFormulaData.get(e);
    s.has(t) || s.set(t, /* @__PURE__ */ new Map());
    const a = s.get(t);
    a.has(r) || a.set(r, new Ce()), a.get(r).setValue(n.refOffsetX, n.refOffsetY, n.treeId), this._addAllTreeMap(n);
  }
  removeOtherFormulaDependency(e, t, r) {
    const n = this._otherFormulaData.get(e);
    if (n && n.has(t)) {
      const s = n.get(t);
      r.forEach((a) => {
        const u = s.get(a);
        u != null && (u.forValue((c, l, f) => {
          this._removeDependencyRTreeCache(f), this.clearDependencyForTree(this._allTreeMap.get(f)), this._removeAllTreeMap(f);
        }), s.delete(a), this._otherFormulaDataMainData.delete(a));
      }), s.size === 0 && n.delete(t), n.size === 0 && this._otherFormulaData.delete(e);
    }
  }
  clearOtherFormulaDependency(e, t) {
    const r = this._otherFormulaData.get(e);
    if (t && r && r.has(t)) {
      const n = r.get(t);
      this._removeDependencyRTreeCacheById(e, t);
      for (const s of n.keys()) {
        const a = n.get(s);
        a != null && (a.forValue((u, c, l) => {
          const f = this._allTreeMap.get(l);
          f && (this.clearDependencyForTree(f), this._removeAllTreeMap(l));
        }), this._otherFormulaDataMainData.delete(s));
      }
      n.clear();
    } else if (r) {
      for (const n of r.keys()) {
        const s = r.get(n);
        this._removeDependencyRTreeCacheById(e, n);
        for (const a of s.keys()) {
          const u = s.get(a);
          u != null && (u.forValue((c, l, f) => {
            const h = this._allTreeMap.get(f);
            h && (this.clearDependencyForTree(h), this._removeAllTreeMap(f));
          }), this._otherFormulaDataMainData.delete(a));
        }
      }
      this._otherFormulaData.delete(e);
    }
  }
  addFeatureFormulaDependency(e, t, r, n) {
    this._featureFormulaData.has(e) || this._featureFormulaData.set(e, /* @__PURE__ */ new Map());
    const s = this._featureFormulaData.get(e);
    s.has(t) || s.set(t, /* @__PURE__ */ new Map()), s.get(t).set(r, n.treeId), this._addAllTreeMap(n);
  }
  removeFeatureFormulaDependency(e, t, r) {
    const n = this._featureFormulaData.get(e);
    if (n && n.has(t)) {
      const s = n.get(t);
      r.forEach((a) => {
        const u = s.get(a);
        u != null && (this._removeDependencyRTreeCache(u), s.delete(a), this.clearDependencyForTree(this._allTreeMap.get(u)), this._removeAllTreeMap(u));
      });
    }
  }
  clearFeatureFormulaDependency(e, t) {
    const r = this._featureFormulaData.get(e);
    if (t && r && r.has(t)) {
      const n = r.get(t);
      this._removeDependencyRTreeCacheById(e, t), n.forEach((s) => {
        s != null && (this.clearDependencyForTree(this._allTreeMap.get(s)), this._removeAllTreeMap(s));
      }), n.clear();
    } else r && (r.forEach((n, s) => {
      this._removeDependencyRTreeCacheById(e, s), n.forEach((a) => {
        a != null && (this.clearDependencyForTree(this._allTreeMap.get(a)), this._removeAllTreeMap(a));
      });
    }), this._featureFormulaData.delete(e));
  }
  addFormulaDependency(e, t, r, n, s) {
    this._formulaData.has(e) || this._formulaData.set(e, /* @__PURE__ */ new Map());
    const a = this._formulaData.get(e);
    a.has(t) || a.set(t, new Ce()), a.get(t).setValue(r, n, s.treeId), this._addAllTreeMap(s);
  }
  removeFormulaDependency(e, t, r, n) {
    const s = this._formulaData.get(e);
    if (s && s.has(t)) {
      const a = s.get(t), u = a.getValue(r, n);
      if (u == null)
        return;
      this._removeDependencyRTreeCache(u), a.realDeleteValue(r, n), this.clearDependencyForTree(this._allTreeMap.get(u)), this._removeAllTreeMap(u);
    }
  }
  clearFormulaDependency(e, t) {
    const r = this._formulaData.get(e);
    if (t && r && r.has(t)) {
      const n = r.get(t);
      this._removeDependencyRTreeCacheById(e, t), n.forValue((s, a, u) => {
        if (u == null)
          return !0;
        this.clearDependencyForTree(this._allTreeMap.get(u)), this._removeAllTreeMap(u);
      }), n.reset();
    } else r && (r.forEach((n, s) => {
      this._removeDependencyRTreeCacheById(e, s), n.forValue((a, u, c) => {
        if (c == null)
          return !0;
        this.clearDependencyForTree(this._allTreeMap.get(c)), this._removeAllTreeMap(c);
      });
    }), this._formulaData.delete(e));
  }
  /**
   * Clear the dependency relationship of the tree.
   * establish the relationship between the parent and the child.
   * @param shouldBeClearTree
   */
  clearDependencyForTree(e) {
    if (e == null)
      return;
    const t = e.parents, r = e.children, n = this._allTreeMap;
    for (const s of t) {
      const a = n.get(s);
      a == null || a.children.delete(e.treeId);
    }
    for (const s of r) {
      const a = n.get(s);
      a == null || a.parents.delete(e.treeId);
    }
    e.dispose();
  }
  _removeDependencyRTreeCache(e) {
    if (e == null)
      return;
    const t = this._allTreeMap.get(e);
    if (t) {
      const r = [];
      for (let n = 0; n < t.rangeList.length; n++) {
        const s = t.rangeList[n], { unitId: a, sheetId: u, range: c } = s;
        r.push({
          unitId: a,
          sheetId: u,
          range: c,
          id: e
        });
      }
      this._dependencyRTreeCache.bulkRemove(r);
    }
  }
  removeFormulaDependencyByDefinedName(e, t) {
    const r = this._definedNameMap.get(e);
    if (r) {
      const n = r.get(t);
      if (n) {
        for (const s of n)
          this._removeDependencyRTreeCache(s), this.clearDependencyForTree(this._allTreeMap.get(s)), this._removeAllTreeMap(s);
        n.clear();
      }
    }
  }
  _removeAllTreeMap(e) {
    e != null && this._allTreeMap.delete(e);
  }
  _addAllTreeMap(e) {
    this._allTreeMap.set(e.treeId, e);
  }
}
const rs = ze(
  "univer.formula.dependency-manager.service"
);
class Wh extends Ve {
  constructor() {
    super(...arguments);
    A(this, "_referenceExecutorMap", /* @__PURE__ */ new Map());
    // unitId -> subUnitId -> featureId -> IFeatureCalculationManagerParam
    A(this, "_onChanged$", new dr());
    A(this, "onChanged$", this._onChanged$.asObservable());
  }
  dispose() {
    super.dispose(), this._referenceExecutorMap.clear(), this._onChanged$.complete();
  }
  remove(e, t, r) {
    r.forEach((n) => {
      var s, a;
      (a = (s = this._referenceExecutorMap.get(e)) == null ? void 0 : s.get(t)) == null || a.delete(n);
    }), this._onChanged$.next({
      unitId: e,
      subUnitId: t,
      featureIds: r
    });
  }
  get(e, t, r) {
    var n, s;
    return (s = (n = this._referenceExecutorMap.get(e)) == null ? void 0 : n.get(t)) == null ? void 0 : s.get(r);
  }
  has(e, t, r) {
    var n, s;
    return !!((s = (n = this._referenceExecutorMap.get(e)) == null ? void 0 : n.get(t)) != null && s.has(r));
  }
  register(e, t, r, n) {
    let s = this._referenceExecutorMap.get(e);
    s || (s = /* @__PURE__ */ new Map(), this._referenceExecutorMap.set(e, s));
    let a = s.get(t);
    a || (a = /* @__PURE__ */ new Map(), s.set(t, a)), this._onChanged$.next({
      unitId: e,
      subUnitId: t,
      featureIds: [r]
    }), a.set(r, n);
  }
  getReferenceExecutorMap() {
    return this._referenceExecutorMap;
  }
}
const kr = ze(
  "univer.formula.feature-calculation-manager.service"
);
class qh extends Ve {
  constructor() {
    super(...arguments);
    A(this, "_otherFormulaData", {});
  }
  dispose() {
    super.dispose(), this._otherFormulaData = {};
  }
  remove(e) {
    var s, a, u;
    const { unitId: t, subUnitId: r, formulaId: n } = e;
    (u = (a = (s = this._otherFormulaData) == null ? void 0 : s[t]) == null ? void 0 : a[r]) == null || delete u[n];
  }
  get(e) {
    var s, a;
    const { unitId: t, subUnitId: r, formulaId: n } = e;
    return (a = (s = this._otherFormulaData[t]) == null ? void 0 : s[r]) == null ? void 0 : a[n];
  }
  has(e) {
    var s, a;
    const { unitId: t, subUnitId: r, formulaId: n } = e;
    return ((a = (s = this._otherFormulaData[t]) == null ? void 0 : s[r]) == null ? void 0 : a[n]) != null;
  }
  register(e) {
    const { unitId: t, subUnitId: r, formulaId: n, item: s } = e;
    this._otherFormulaData[t] || (this._otherFormulaData[t] = {}), this._otherFormulaData[t][r] || (this._otherFormulaData[t][r] = {}), this._otherFormulaData[t][r][n] = s;
  }
  batchRegister(e) {
    Object.keys(e).forEach((t) => {
      const r = e[t];
      if (r == null)
        return !0;
      Object.keys(r).forEach((n) => {
        const s = r[n];
        if (s == null)
          return !0;
        Object.keys(s).forEach((a) => {
          const u = s[a];
          if (u == null)
            return !0;
          this.register({
            unitId: t,
            subUnitId: n,
            formulaId: a,
            item: u
          });
        });
      });
    });
  }
  batchRemove(e) {
    Object.keys(e).forEach((t) => {
      const r = e[t];
      if (r == null)
        return !0;
      Object.keys(r).forEach((n) => {
        const s = r[n];
        if (s == null)
          return !0;
        Object.keys(s).forEach((a) => {
          this.remove({
            unitId: t,
            subUnitId: n,
            formulaId: a
          });
        });
      });
    });
  }
  getOtherFormulaData() {
    return this._otherFormulaData;
  }
}
const va = ze(
  "univer.formula.other-formula-manager.service"
);
var ra = /* @__PURE__ */ ((o) => (o[o.NORMAL_FORMULA = 0] = "NORMAL_FORMULA", o[o.OTHER_FORMULA = 1] = "OTHER_FORMULA", o[o.FEATURE_FORMULA = 2] = "FEATURE_FORMULA", o))(ra || {});
class zo {
  constructor() {
    A(this, "_state", 0);
    A(this, "treeId");
    // READNOW: MEMORY HOTSPOT #4 — Every formula tree node has two Sets for
    // dependency tracking. With 10K+ formula cells, that's 20K+ Set instances.
    // The OOM stack trace shows Runtime_SetGrow as the final allocation that
    // tips over the heap — Sets are resized via OrderedHashTable::Rehash which
    // allocates FixedArrays. The Sets themselves aren't huge, but they compound
    // with the array allocations from mapValue/_batchOperator.
    // Also see _clearFeatureCalculationNode in formula-dependency.ts which
    // rebuilds these Sets on every recalc pass.
    A(this, "children", /* @__PURE__ */ new Set());
    A(this, "parents", /* @__PURE__ */ new Set());
  }
  resetState() {
    this._state = 0;
  }
  setAdded() {
    this._state = 1;
  }
  isAdded() {
    return this._state === 1;
  }
  setSkip() {
    this._state = 2;
  }
  isSkip() {
    return this._state === 2;
  }
  pushChildren(i) {
    this.children.add(i.treeId), i._pushParent(this);
  }
  hasChildren(i) {
    return this.children.has(i);
  }
  _pushParent(i) {
    this.parents.add(i.treeId);
  }
}
class ps extends zo {
  constructor() {
    super(...arguments);
    A(this, "refTree");
    A(this, "refOffsetX", -1);
    A(this, "refOffsetY", -1);
    A(this, "isCache", !1);
    A(this, "isDirty", !1);
    A(this, "addressFunctionNodes", []);
    A(this, "getDirtyData");
    A(this, "featureId");
  }
  get isVirtual() {
    return !0;
  }
  get row() {
    return this.refTree == null ? -1 : this.refTree.row + this.refOffsetY;
  }
  get column() {
    return this.refTree == null ? -1 : this.refTree.column + this.refOffsetX;
  }
  get rowCount() {
    return this.refTree == null ? 0 : this.refTree.rowCount;
  }
  get columnCount() {
    return this.refTree == null ? 0 : this.refTree.columnCount;
  }
  get unitId() {
    return this.refTree == null ? "" : this.refTree.unitId;
  }
  get subUnitId() {
    return this.refTree == null ? "" : this.refTree.subUnitId;
  }
  get formula() {
    var e, t;
    return (t = (e = this.refTree) == null ? void 0 : e.formula) != null ? t : "";
  }
  get nodeData() {
    return {
      node: this.node,
      refOffsetX: this.refOffsetX,
      refOffsetY: this.refOffsetY
    };
  }
  get node() {
    var e;
    return (e = this.refTree) == null ? void 0 : e.node;
  }
  dispose() {
    this.refTree = null;
  }
  get rangeList() {
    const e = [];
    if (this.refTree == null)
      return [];
    for (let t = 0; t < this.refTree.rangeList.length; t++) {
      const r = this.refTree.rangeList[t];
      e.push({
        unitId: r.unitId,
        sheetId: r.sheetId,
        range: da(r.range, this.refOffsetX, this.refOffsetY)
      });
    }
    return e;
  }
  toRTreeItem() {
    const e = this.row, t = this.column;
    return [{
      unitId: this.unitId,
      sheetId: this.subUnitId,
      range: {
        startRow: e,
        startColumn: t,
        endRow: e,
        endColumn: t
      }
    }];
  }
  inRangeData(e) {
    const t = e.startRow, r = e.startColumn, n = e.endRow, s = e.endColumn, a = this.row, u = this.column;
    return !(a < t || a > n || u < r || u > s);
  }
  dependencySheetName(e) {
    return this.refTree == null ? !1 : this.refTree.dependencySheetName(e);
  }
  isExcludeRange(e) {
    var r;
    const t = this.rangeList;
    if (t.length === 0)
      return !1;
    for (let n = 0, s = t.length; n < s; n++) {
      const a = t[n], { unitId: u, sheetId: c, range: l } = a, f = (r = e == null ? void 0 : e[u]) == null ? void 0 : r[c];
      let { startRow: h, endRow: d, startColumn: _, endColumn: C } = l;
      Number.isNaN(h) && (h = 0), Number.isNaN(_) && (_ = 0), Number.isNaN(d) && (d = Number.POSITIVE_INFINITY), Number.isNaN(C) && (C = Number.POSITIVE_INFINITY);
      let E = !1;
      if (f == null || f.forValue((b, y) => {
        if (b >= h && b <= d && y >= _ && y <= C)
          return E = !0, !1;
      }), E)
        return !0;
    }
    return !1;
  }
  get formulaId() {
    return this.refTree == null ? "" : this.refTree.formulaId;
  }
}
class Ns extends zo {
  constructor(e) {
    super();
    A(this, "isCache", !1);
    A(this, "featureId");
    A(this, "featureDirtyRanges", []);
    A(this, "refOffsetX", 0);
    A(this, "refOffsetY", 0);
    A(this, "type", 0);
    A(this, "formulaId");
    A(this, "subUnitId", "");
    A(this, "unitId", "");
    A(this, "rangeList", []);
    A(this, "formula", "");
    A(this, "row", -1);
    A(this, "column", -1);
    A(this, "rowCount", Number.NEGATIVE_INFINITY);
    A(this, "columnCount", Number.NEGATIVE_INFINITY);
    A(this, "isDirty", !1);
    A(this, "node");
    A(this, "addressFunctionNodes", []);
    A(this, "getDirtyData");
    this.treeId = e;
  }
  get isVirtual() {
    return !1;
  }
  get nodeData() {
    return {
      node: this.node,
      refOffsetX: 0,
      refOffsetY: 0
    };
  }
  toJson() {
    return {
      formula: this.formula,
      refOffsetX: this.refOffsetX,
      refOffsetY: this.refOffsetY
    };
  }
  dispose() {
    this.featureDirtyRanges = [], this.rangeList = [], this.addressFunctionNodes = [], this.getDirtyData = null;
  }
  inRangeData(e) {
    const t = e.startRow, r = e.startColumn, n = e.endRow, s = e.endColumn, a = this.row, u = this.column;
    return !(a < t || a > n || u < r || u > s);
  }
  dependencySheetName(e) {
    var r;
    const t = this.rangeList;
    if (t.length === 0 || e == null)
      return !1;
    for (let n = 0, s = t.length; n < s; n++) {
      const a = t[n], { unitId: u, sheetId: c } = a;
      if (((r = e[u]) == null ? void 0 : r[c]) != null)
        return !0;
    }
    return !1;
  }
  isExcludeRange(e) {
    var r;
    const t = this.rangeList;
    if (t.length === 0)
      return !1;
    for (let n = 0, s = t.length; n < s; n++) {
      const a = t[n], { unitId: u, sheetId: c, range: l } = a, f = (r = e == null ? void 0 : e[u]) == null ? void 0 : r[c];
      let { startRow: h, endRow: d, startColumn: _, endColumn: C } = l;
      Number.isNaN(h) && (h = 0), Number.isNaN(_) && (_ = 0), Number.isNaN(d) && (d = Number.POSITIVE_INFINITY), Number.isNaN(C) && (C = Number.POSITIVE_INFINITY);
      let E = !1;
      if (f == null || f.forValue((b, y) => {
        if (b >= h && b <= d && y >= _ && y <= C)
          return E = !0, !1;
      }), E)
        return !0;
    }
    return !1;
  }
  // pushChildren(tree: FormulaDependencyTree) {
  //     this.children.add(tree.treeId);
  //     tree._pushParent(this);
  // }
  /**
   * Add the range corresponding to the current ast node.
   * @param range
   */
  pushRangeList(e) {
    this.rangeList.push(...e);
  }
  shouldBePushRangeList() {
    return this.rangeList.length === 0 && this.type !== 2;
  }
  // hasChildren(treeId: number) {
  //     return this.children.has(treeId);
  // }
  toRTreeItem() {
    if (this.featureId != null)
      return this.featureDirtyRanges;
    const e = this.row, t = this.column;
    return [{
      unitId: this.unitId,
      sheetId: this.subUnitId,
      range: {
        startRow: e,
        startColumn: t,
        endRow: e,
        endColumn: t
      }
    }];
  }
  /**
   * Determine whether it is dependent on other trees.
   * @param dependenceTree
   */
  // dependency(dependenceTree: FormulaDependencyTree) {
  //     if (this.rangeList.length === 0) {
  //         return false;
  //     }
  //     for (let r = 0, len = this.rangeList.length; r < len; r++) {
  //         const unitRange = this.rangeList[r];
  //         const unitId = unitRange.unitId;
  //         const sheetId = unitRange.sheetId;
  //         const range = unitRange.range;
  //         if (
  //             dependenceTree.unitId === unitId &&
  //             dependenceTree.subUnitId === sheetId &&
  //             dependenceTree.inRangeData(range)
  //         ) {
  //             return true;
  //         }
  //     }
  //     return false;
  // }
  // private _pushParent(tree: FormulaDependencyTree) {
  //     this.parents.add(tree.treeId);
  // }
}
var Kh = Object.getOwnPropertyDescriptor, Xh = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? Kh(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, jt = (o, i) => (e, t) => i(e, t, o);
const Zh = /([A-Z]+)(\d+)/g;
function zh(o, i) {
  return o.replace(Zh, (e, t, r) => {
    const n = Number.parseInt(r, 10) - i;
    return `${t}{${n}}`;
  });
}
function Xt(o) {
  return o.getLastTreeId() || 0;
}
const na = ze("engine-formula.dependency-generator");
let sa = class extends Ve {
  constructor(i, e, t, r, n, s, a, u) {
    super();
    A(this, "_updateRangeFlattenCache", /* @__PURE__ */ new Map());
    A(this, "_hasBuiltDependencies", !1);
    A(this, "_dependencyRTreeCacheForAddressFunction", new Ps());
    A(this, "_executedAddressFunctionNodeIds", /* @__PURE__ */ new Set());
    this._currentConfigService = i, this._runtimeService = e, this._otherFormulaManagerService = t, this._featureCalculationManagerService = r, this._interpreter = n, this._astTreeBuilder = s, this._lexer = a, this._dependencyManagerService = u;
  }
  dispose() {
    this._updateRangeFlattenCache.clear(), this._dependencyRTreeCacheForAddressFunction.clear(), this._hasBuiltDependencies = !1, Ir.clear();
  }
  async generate() {
    var h, d;
    this._updateRangeFlatten();
    const i = this._currentConfigService.isForceCalculate(), e = this._currentConfigService.getClearDependencyTreeCache(), t = e != null && Object.keys(e).length > 0;
    if (!i && !t && this._hasBuiltDependencies) {
      const _ = this._currentConfigService.getDirtyRanges(), C = this._currentConfigService.getFormulaData();
      let E = !1;
      for (const b of _) {
        const { unitId: y, sheetId: p, range: N } = b, w = (h = C == null ? void 0 : C[y]) == null ? void 0 : h[p];
        if (w)
          for (let S = N.startRow; S <= N.endRow && !E; S++)
            for (let M = N.startColumn; M <= N.endColumn && !E; M++) {
              const D = (d = w[S]) == null ? void 0 : d[M];
              D && D.f && this._dependencyManagerService.getFormulaDependency(y, p, S, M) == null && (E = !0);
            }
      }
      if (!E) {
        const b = this._dependencyManagerService.searchDependency(_), y = this._dependencyRTreeCacheForAddressFunction.bulkSearch(_);
        for (const S of y)
          b.add(S);
        if (b.size === 0)
          return Promise.resolve([]);
        const p = [];
        for (const S of b) {
          const M = this._dependencyManagerService.getTreeById(S);
          M && (M.isDirty = !0, M.resetState(), p.push(M));
        }
        const N = this._calculateRunList(p);
        return this._checkIsCycleDependency(N) && this._runtimeService.enableCycleDependency(), Promise.resolve(N);
      }
    }
    const r = this._currentConfigService.getFormulaData(), n = this._otherFormulaManagerService.getOtherFormulaData();
    t && Object.keys(e).forEach((_) => {
      _ != null && Object.keys(e[_]).forEach((C) => {
        C != null && (this._dependencyManagerService.clearOtherFormulaDependency(_, C), this._dependencyManagerService.clearFeatureFormulaDependency(_, C), this._dependencyManagerService.clearFormulaDependency(_, C));
      });
    });
    const s = this._currentConfigService.getUnitData(), a = await this._generateTreeList(r, n, s), u = this._getUpdateTreeListAndMakeDependency(a);
    let c = this._calculateRunList(u);
    return this._dependencyFeatureCalculation(c) && (c.forEach((_) => {
      _.resetState();
    }), c = this._calculateRunList(c)), this._checkIsCycleDependency(c) && this._runtimeService.enableCycleDependency(), this._dependencyRTreeCacheForAddressFunction.clear(), this._hasBuiltDependencies = !0, Promise.resolve(c);
  }
  _dependencyFeatureCalculation(i) {
    const e = this._featureCalculationManagerService.getReferenceExecutorMap();
    if (e.size === 0)
      return;
    this._clearFeatureCalculationNode(i);
    let t = !1;
    return e.forEach((r, n) => {
      r.forEach((s, a) => {
        s.forEach((u, c) => {
          const { unitId: l, subUnitId: f, getDirtyData: h } = u, d = h(this._currentConfigService.getDirtyData(), this._runtimeService.getAllRuntimeData()), _ = this._convertDirtyRangesToUnitRange(d.dirtyRanges), C = this._intersectFeatureCalculation(_, i, { unitId: l, subUnitId: f, featureId: c });
          if (C.length > 0) {
            let E = this._getExistTreeList({ unitId: l, subUnitId: f, featureId: c }, i);
            E == null && (E = this._getFeatureFormulaTree(c, Xt(this._dependencyManagerService), u), i.push(E)), E.parents = /* @__PURE__ */ new Set(), C.forEach((b) => {
              b.hasChildren(E.treeId) || b.pushChildren(E);
            }), t = !0;
          }
        });
      });
    }), t;
  }
  // READNOW: MEMORY HOTSPOT #5 — Rebuilds children and parents Sets for every
  // tree node on each recalc pass. For 10K formulas, this allocates 20K new
  // Sets per pass, iterates the old Sets, and discards them. This is the Set
  // churn that shows up in the OOM stack trace as Runtime_SetGrow.
  _clearFeatureCalculationNode(i) {
    const e = this._featureCalculationManagerService.getReferenceExecutorMap();
    i.forEach((t) => {
      var r, n, s, a;
      for (const u of t.children) {
        const c = this._dependencyManagerService.getTreeById(u);
        if (!c) {
          t.children.delete(u);
          continue;
        }
        c.featureId && ((n = (r = e.get(t.unitId)) == null ? void 0 : r.get(t.subUnitId)) != null && n.has(c.featureId)) && t.children.delete(u);
      }
      for (const u of t.parents) {
        const c = this._dependencyManagerService.getTreeById(u);
        if (!c) {
          t.parents.delete(u);
          continue;
        }
        c.featureId && ((a = (s = e.get(t.unitId)) == null ? void 0 : s.get(t.subUnitId)) != null && a.has(c.featureId)) && t.parents.delete(u);
      }
    });
  }
  /**
   * TODO @DR-Univer: The next step will be to try changing the incoming dirtyRanges to an array, thus avoiding conversion.
   * @param dirtyRanges
   * @returns
   */
  _convertDirtyRangesToUnitRange(i) {
    const e = [];
    for (const t in i) {
      const r = i[t];
      for (const n in r) {
        const s = r[n];
        for (const a of s)
          e.push({
            unitId: t,
            sheetId: n,
            range: a
          });
      }
    }
    return e;
  }
  _intersectFeatureCalculation(i, e, t) {
    const r = [], n = this._dependencyManagerService.searchDependency(i);
    for (let s = 0, a = e.length; s < a; s++) {
      const u = e[s];
      if (u.unitId === t.unitId && u.subUnitId === t.subUnitId && u.featureId === t.featureId)
        continue;
      n.has(u.treeId) && r.push(u);
    }
    return r;
  }
  _getExistTreeList(i, e) {
    const { unitId: t, subUnitId: r, featureId: n } = i;
    for (let s = 0, a = e.length; s < a; s++) {
      const u = e[s];
      if (u.unitId === t && u.subUnitId === r && u.featureId === n)
        return u;
    }
  }
  _isCyclicUtil(i, e, t) {
    const r = this._dependencyManagerService.getTreeById(i);
    if (r == null)
      return !1;
    if (!e.has(r.treeId)) {
      e.add(r.treeId), t.add(r.treeId);
      for (const n of r.children)
        if (!e.has(n) && this._isCyclicUtil(n, e, t) || t.has(n))
          return !0;
    }
    return t.delete(r.treeId), !1;
  }
  _checkIsCycleDependency(i) {
    const e = /* @__PURE__ */ new Set(), t = /* @__PURE__ */ new Set();
    for (let r = 0, n = i.length; r < n; r++) {
      const s = i[r];
      if (this._isCyclicUtil(s.treeId, e, t) === !0)
        return !0;
    }
    return !1;
  }
  /**
   * Generate nodes for the dependency tree, where each node contains all the reference data ranges included in each formula.
   * @param formulaData
   */
  async _generateTreeList(i, e, t) {
    const r = Object.keys(i), n = Object.keys(e), s = [];
    this._currentConfigService.isForceCalculate() && this._dependencyManagerService.reset(), this._registerFormulas(r, i, t, s), this._registerOtherFormulas(e, n, s), this._registerFeatureFormulas(s);
    for (let u = 0, c = s.length; u < c; u++) {
      const l = s[u];
      if (!l.formula)
        continue;
      const f = this._getTreeNode(l);
      l.isDirty = this._includeTree(l, f);
      const h = this._getAddressFunctionNodeList(f);
      if (h.length > 0 && (l.addressFunctionNodes = h), l.isVirtual)
        continue;
      this._runtimeService.setCurrent(
        l.row,
        l.column,
        l.rowCount,
        l.columnCount,
        l.subUnitId,
        l.unitId
      );
      const d = await this._getRangeListByNode({
        node: f,
        refOffsetX: l.refOffsetX,
        refOffsetY: l.refOffsetY
      });
      l.pushRangeList(d);
    }
    for (let u = 0, c = s.length; u < c; u++) {
      const l = s[u];
      l.isCache || this._dependencyManagerService.addDependencyRTreeCache(l);
    }
    return await this._calculateListByFunctionRefNode(s), s;
  }
  // private _moveRangeList(tree: FormulaDependencyTree, refOffsetX: number, refOffsetY: number) {
  //     const rangeList = tree.rangeList;
  //     const newRangeList = [];
  //     for (let i = 0, len = rangeList.length; i < len; i++) {
  //         const unitRange = rangeList[i];
  //         const newRange = {
  //             unitId: tree.unitId,
  //             sheetId: tree.subUnitId,
  //             range: moveRangeByOffset(unitRange.range, refOffsetX, refOffsetY),
  //         };
  //         newRangeList.push(newRange);
  //     }
  //     return newRangeList;
  // }
  _registerFeatureFormulas(i) {
    this._featureCalculationManagerService.getReferenceExecutorMap().forEach((t, r) => {
      t.forEach((n, s) => {
        n.forEach((a, u) => {
          const c = this._dependencyManagerService.getFeatureFormulaDependency(a.unitId, a.subUnitId, u);
          i.push(this._getFeatureFormulaTree(u, c, a));
        });
      });
    });
  }
  _getFeatureFormulaTree(i, e, t) {
    const { unitId: r, subUnitId: n, dependencyRanges: s, getDirtyData: a } = t, u = e || Xt(this._dependencyManagerService), c = new Ns(u);
    c.unitId = r, c.subUnitId = n, c.rangeList = s, c.getDirtyData = a;
    const l = a(this._currentConfigService.getDirtyData(), this._runtimeService.getAllRuntimeData()), f = this._convertDirtyRangesToUnitRange(l.dirtyRanges);
    return c.featureDirtyRanges = f, c.featureId = i, c.type = ra.FEATURE_FORMULA, this._dependencyManagerService.addFeatureFormulaDependency(r, n, i, c), this._dependencyManagerService.getFeatureFormulaDependency(t.unitId, t.subUnitId, i) && (c.isCache = !0), c;
  }
  _registerOtherFormulas(i, e, t) {
    for (const r of e) {
      const n = i[r];
      if (n == null)
        continue;
      const s = Object.keys(n);
      for (const a of s) {
        const u = n[a];
        if (u == null)
          continue;
        const c = Object.keys(u);
        for (const l of c) {
          const f = this._dependencyManagerService.hasOtherFormulaDataMainData(l), h = u[l], { f: d, ranges: _ } = h;
          let C = !1;
          f && (C = !0);
          const E = ji(r, d, this._lexer, this._astTreeBuilder, this._currentConfigService), { firstRow: b, firstColumn: y } = this._getFirstCellOfRange(_), p = this._dependencyManagerService.getOtherFormulaDependency(r, a, l), N = (p == null ? void 0 : p.getValue(0, 0)) || Xt(this._dependencyManagerService), w = new Ns(N);
          for (let S = 0; S < _.length; S++) {
            const M = _[S], { startRow: D, startColumn: L, endRow: v, endColumn: j } = M;
            for (let k = D; k <= v; k++)
              for (let G = L; G <= j; G++) {
                const ne = G - y, z = k - b;
                if (ne === 0 && z === 0) {
                  w.node = E, w.formula = d, w.unitId = r, w.subUnitId = a, w.formulaId = l, w.type = ra.OTHER_FORMULA, w.isCache = C, t.push(w), this._dependencyManagerService.addOtherFormulaDependency(r, a, l, w), this._dependencyManagerService.addFormulaDependencyByDefinedName(w);
                  continue;
                }
                const Q = new ps();
                Q.treeId = (p == null ? void 0 : p.getValue(ne, z)) || Xt(this._dependencyManagerService), Q.refTree = w, Q.refOffsetX = ne, Q.refOffsetY = z, Q.isCache = C, this._dependencyManagerService.addOtherFormulaDependency(r, a, l, Q), this._dependencyManagerService.addFormulaDependencyByDefinedName(Q), t.push(Q);
              }
          }
          this._dependencyManagerService.addOtherFormulaDependencyMainData(l);
        }
      }
    }
  }
  _getFirstCellOfRange(i) {
    const e = i[0];
    return {
      firstRow: e.startRow,
      firstColumn: e.startColumn
    };
  }
  _registerFormulas(i, e, t, r) {
    for (const n of i) {
      const s = e[n];
      if (s == null)
        continue;
      const a = Object.keys(s);
      for (const u of a) {
        const c = new Ce(s[u] || {}), l = /* @__PURE__ */ new Map();
        c.forValue((h, d, _) => {
          if (_ == null)
            return !0;
          const { x: C = 0, y: E = 0, si: b } = _;
          if (!(C === 0 && E === 0 && b != null))
            return !0;
          const y = this._dependencyManagerService.getFormulaDependency(n, u, h, d);
          if (y != null) {
            const N = this._dependencyManagerService.getTreeById(y);
            if (N && N.formula === _.f)
              return N.isCache = !0, l.set(b, N), r.push(N), !0;
          }
          const p = this._createFDtree(n, u, h, d, t, _);
          y != null ? p.treeId = y : (this._dependencyManagerService.addFormulaDependency(n, u, h, d, p), this._dependencyManagerService.addFormulaDependencyByDefinedName(p)), l.set(b, p), r.push(p);
        });
        const f = /* @__PURE__ */ new Map();
        c.forValue((h, d, _) => {
          if (_ == null)
            return !0;
          const { x: C = 0, y: E = 0, si: b } = _;
          if (C === 0 && E === 0 && b != null)
            return !0;
          const y = this._dependencyManagerService.getFormulaDependency(n, u, h, d);
          if (y != null) {
            const N = this._dependencyManagerService.getTreeById(y);
            if (N) {
              const w = N.isVirtual && (b ? l.has(b) : !0), S = !N.isVirtual && N.formula === _.f;
              if (w || S)
                return N.isCache = !0, r.push(N), !0;
            }
          }
          let p;
          if (b && l.has(b)) {
            const N = l.get(b);
            p = this._createVirtualFDtree(N, _);
          } else {
            const N = _.f, w = h + 1, S = zh(N, w), M = f.get(d);
            M && M.template === S ? p = this._createFillDownVirtualFDtree(M.tree, h - M.row) : (p = this._createFDtree(n, u, h, d, t, _), f.set(d, {
              tree: p,
              row: h,
              template: S
            }));
          }
          y != null ? p.treeId = y : (this._dependencyManagerService.addFormulaDependency(n, u, h, d, p), this._dependencyManagerService.addFormulaDependencyByDefinedName(p)), r.push(p);
        }), f.clear(), l.clear();
      }
    }
  }
  _createFDtree(i, e, t, r, n, s) {
    const { f: a, x: u = 0, y: c = 0 } = s, l = new Ns(Xt(this._dependencyManagerService)), f = n[i][e], h = ji(i, a, this._lexer, this._astTreeBuilder, this._currentConfigService);
    return l.node = h, l.formula = a, l.unitId = i, l.subUnitId = e, l.row = t, l.column = r, l.rowCount = f.rowCount, l.columnCount = f.columnCount, l;
  }
  _createVirtualFDtree(i, e) {
    const { x: t = 0, y: r = 0 } = e, n = new ps();
    return n.treeId = Xt(this._dependencyManagerService), n.refTree = i, n.refOffsetX = t, n.refOffsetY = r, n;
  }
  _createFillDownVirtualFDtree(i, e) {
    const t = new ps();
    return t.treeId = Xt(this._dependencyManagerService), t.refTree = i, t.refOffsetX = 0, t.refOffsetY = e, t;
  }
  /**
   * Break down the dirty areas into ranges for subsequent matching.
   */
  _updateRangeFlatten() {
    const i = this._currentConfigService.isForceCalculate(), e = this._currentConfigService.getDirtyRanges();
    if (!i) {
      this._updateRangeFlattenCache.clear();
      for (let t = 0; t < e.length; t++) {
        const r = e[t], n = r.range, s = r.sheetId, a = r.unitId;
        this._addFlattenCache(a, s, n);
      }
    }
  }
  _addFlattenCache(i, e, t) {
    let r = this._updateRangeFlattenCache.get(i);
    r == null && (r = /* @__PURE__ */ new Map(), this._updateRangeFlattenCache.set(i, r));
    let n = r.get(e);
    n == null && (n = [], r.set(e, n)), n.push(t);
  }
  _isPreCalculateNode(i) {
    return i.nodeType === ie.UNION || i.nodeType === ie.PREFIX && i.getToken() === we.AT || i.nodeType === ie.SUFFIX && i.getToken() === tt.POUND;
  }
  _nodeTraversalRef(i, e) {
    const t = i.getChildren(), r = t.length;
    for (let n = 0; n < r; n++) {
      const s = t[n];
      if (this._isPreCalculateNode(s)) {
        if (e.push(s), s.nodeType === ie.UNION)
          for (const a of s.getChildren())
            a.nodeType === ie.FUNCTION && a.isAddress() && this._nodeTraversalRef(a, e);
        continue;
      } else s.nodeType === ie.REFERENCE && e.push(s);
      this._nodeTraversalRef(s, e);
    }
  }
  _nodeTraversalReferenceFunction(i, e) {
    const t = i.getChildren(), r = t.length;
    for (let n = 0; n < r; n++) {
      const s = t[n];
      if (s.nodeType === ie.FUNCTION && s.isAddress()) {
        e.push(s);
        continue;
      }
      this._nodeTraversalReferenceFunction(s, e);
    }
  }
  async _executeNode(i, e = 0, t = 0) {
    let r;
    const n = {
      node: i,
      refOffsetX: e,
      refOffsetY: t
    };
    return this._interpreter.checkAsyncNode(i) ? r = await this._interpreter.executeAsync(n) : r = this._interpreter.execute(n), r;
  }
  /**
   * Calculate the range required for collection in advance,
   * including references and location functions (such as OFFSET, INDIRECT, INDEX, etc.).
   * @param node
   */
  async _getRangeListByNode(i) {
    const e = [], t = i.refOffsetX, r = i.refOffsetY, n = i.node;
    if (n == null)
      return [];
    this._nodeTraversalRef(n, e);
    const s = [];
    for (let a = 0, u = e.length; a < u; a++) {
      const c = e[a], f = (await this._executeNode(c, t, r)).toUnitRange();
      s.push(f), c.setValue(null);
    }
    return s;
  }
  _getAddressFunctionNodeList(i) {
    const e = [];
    return i == null ? [] : (this._nodeTraversalReferenceFunction(i, e), e);
  }
  _getTreeNode(i) {
    return i.node;
  }
  async _buildDirtyRangesByAddressFunction(i, e) {
    const t = e.addressFunctionNodes;
    if (t.length === 0)
      return;
    const r = e.refOffsetX, n = e.refOffsetY, s = await this._getRangeListByFunctionRefNode(t, r, n);
    e.addressFunctionNodes = [], this._addDependencyTreeByAddressFunction(e, s);
    const a = i.bulkSearch(s), u = this._buildTreeNodeById(a);
    u.length !== 0 && await this._calculateAddressFunctionRuntimeData(i, u);
  }
  async _calculateListByFunctionRefNode(i) {
    const e = new Ps();
    for (let t = 0, r = i.length; t < r; t++) {
      const n = i[t];
      e.insert({
        unitId: n.unitId,
        sheetId: n.subUnitId,
        range: {
          startRow: n.row,
          startColumn: n.column,
          endRow: n.row,
          endColumn: n.column
        },
        id: n.treeId
      });
    }
    this._executedAddressFunctionNodeIds.clear();
    for (let t = 0, r = i.length; t < r; t++) {
      const n = i[t];
      await this._calculateAddressFunction(e, n);
    }
  }
  async _calculateAddressFunction(i, e) {
    const t = e.addressFunctionNodes;
    if (t.length === 0)
      return;
    const r = e.refOffsetX, n = e.refOffsetY;
    this._runtimeService.setCurrent(
      e.row,
      e.column,
      e.rowCount,
      e.columnCount,
      e.subUnitId,
      e.unitId
    );
    const s = [];
    for (let c = 0, l = t.length; c < l; c++) {
      const f = await this._getRangeListByNode({
        node: t[c],
        refOffsetX: r,
        refOffsetY: n
      });
      s.push(...f);
    }
    const a = /* @__PURE__ */ new Set();
    this._searchDependencyByAddressFunction(i, s, a);
    const u = this._buildTreeNodeById(a);
    if (u.length === 0) {
      await this._buildDirtyRangesByAddressFunction(i, e);
      return;
    }
    await this._calculateAddressFunctionRuntimeData(i, u), await this._buildDirtyRangesByAddressFunction(i, e);
  }
  async _calculateAddressFunctionRuntimeData(i, e) {
    for (; e.length > 0; ) {
      const t = e.pop(), n = {
        node: this._getTreeNode(t),
        refOffsetX: t.refOffsetX,
        refOffsetY: t.refOffsetY
      };
      await this._calculateAddressFunction(i, t), this._runtimeService.setCurrent(
        t.row,
        t.column,
        t.rowCount,
        t.columnCount,
        t.subUnitId,
        t.unitId
      );
      let s;
      this._interpreter.checkAsyncNode(n.node) ? s = await this._interpreter.executeAsync(n) : s = this._interpreter.execute(n), t.formulaId != null ? this._runtimeService.setRuntimeOtherData(t.formulaId, t.refOffsetX, t.refOffsetY, s) : this._runtimeService.setRuntimeData(s);
    }
  }
  _buildTreeNodeById(i) {
    const e = [];
    for (const t of i) {
      const r = this._getTreeById(t);
      !r || this._executedAddressFunctionNodeIds.has(t) || (this._executedAddressFunctionNodeIds.add(t), e.push(r));
    }
    return e;
  }
  _searchDependencyByAddressFunction(i, e, t) {
    const r = i.bulkSearch(e), n = this._dependencyRTreeCacheForAddressFunction.bulkSearch(e);
    for (const a of n)
      t.has(a) || t.add(a);
    const s = [];
    for (const a of r) {
      const u = this._getTreeById(a);
      u && !t.has(a) && (s.push(...u.rangeList), t.add(a));
    }
    return s.length > 0 && this._searchDependencyByAddressFunction(i, s, t), t;
  }
  _getTreeById(i) {
    return this._dependencyManagerService.getTreeById(i);
  }
  _addDependencyTreeByAddressFunction(i, e) {
    const t = [];
    for (let r = 0; r < e.length; r++) {
      const n = e[r], { unitId: s, sheetId: a, range: u } = n;
      t.push({
        unitId: s,
        sheetId: a,
        range: u,
        id: i.treeId
      });
    }
    this._dependencyRTreeCacheForAddressFunction.bulkInsert(t);
  }
  /**
   * Calculate the range required for collection in advance,
   * including references and location functions (such as OFFSET, INDIRECT, INDEX, etc.).
   * @param node
   */
  async _getRangeListByFunctionRefNode(i, e, t) {
    const r = [];
    for (let n = 0, s = i.length; n < s; n++) {
      const a = i[n], c = (await this._executeNode(a, e, t)).toUnitRange();
      r.push(c), a.setValue(null);
    }
    return r;
  }
  /**
   * Build a formula dependency tree based on the dependency relationships.
   * @param treeList
   */
  _getUpdateTreeListAndMakeDependency(i) {
    const e = [], t = /* @__PURE__ */ new Set(), r = this._currentConfigService.isForceCalculate(), n = this._currentConfigService.getDirtyRanges(), s = this._dependencyManagerService.searchDependency(n), a = this._dependencyRTreeCacheForAddressFunction.bulkSearch(n);
    for (const c of a)
      s.add(c);
    const u = this._dependencyManagerService.buildDependencyTree(i);
    for (const c of u) {
      const l = c.treeId;
      (r || c.isDirty || c.dependencySheetName(this._currentConfigService.getDirtyNameMap()) || //O(n) n=tree.rangeList.length
      s.has(l) && !c.isExcludeRange(this._currentConfigService.getExcludedRange())) && !t.has(l) && (e.push(c), t.add(l));
    }
    return e;
  }
  _includeTreeFeature(i) {
    var n, s;
    const e = i.unitId, t = i.subUnitId, r = i.featureId;
    if (r != null) {
      const a = this._currentConfigService.getDirtyUnitFeatureMap();
      if (((s = (n = a == null ? void 0 : a[e]) == null ? void 0 : n[t]) == null ? void 0 : s[r]) != null)
        return !0;
    }
    return !1;
  }
  _includeOtherFormula(i) {
    var n, s;
    const e = i.unitId, t = i.subUnitId, r = i.formulaId;
    if (r != null) {
      const a = this._currentConfigService.getDirtyUnitOtherFormulaMap();
      if (((s = (n = a == null ? void 0 : a[e]) == null ? void 0 : n[t]) == null ? void 0 : s[r]) != null)
        return !0;
    }
    return !1;
  }
  _detectForcedRecalculationNode(i, e) {
    return e == null ? !1 : this._detectForcedRecalculationNodeRecursion(e);
  }
  _detectForcedRecalculationNodeRecursion(i) {
    if (i.isForcedCalculateFunction())
      return !0;
    const e = i.getChildren();
    for (let t = 0, r = e.length; t < r; t++) {
      const n = e[t];
      if (this._detectForcedRecalculationNodeRecursion(n))
        return !0;
    }
    return !1;
  }
  /**
   * Determine whether all ranges of the current node exist within the dirty area.
   * If they are within the dirty area, return true, indicating that this node needs to be calculated.
   * @param tree
   */
  _includeTree(i, e) {
    var c, l, f;
    const t = i.unitId, r = i.subUnitId;
    if (this._detectForcedRecalculationNode(i, e) === !0 || this._includeTreeFeature(i) === !0 || this._includeOtherFormula(i) === !0 || hh(i, e, this._currentConfigService) === !0)
      return !0;
    const n = (l = (c = this._currentConfigService.getExcludedRange()) == null ? void 0 : c[t]) == null ? void 0 : l[r];
    if ((n == null ? void 0 : n.getValue(i.row, i.column)) != null)
      return !1;
    if (((f = this._currentConfigService.getDirtyNameMap()[t]) == null ? void 0 : f[r]) != null)
      return !0;
    if (!this._updateRangeFlattenCache.has(t))
      return !1;
    const a = this._updateRangeFlattenCache.get(t);
    if (!a.has(r))
      return !1;
    const u = a.get(r);
    for (const h of u)
      if (i.inRangeData(h))
        return !0;
    return !1;
  }
  /**
   * Generate the final formula calculation order array by traversing the dependency tree established via depth-first search.
   * @param treeList
   */
  _calculateRunList(i) {
    i.length;
    const e = i, t = [], r = [];
    for (; e.length > 0; ) {
      const n = e.pop();
      if (n === void 0 || n.isSkip())
        continue;
      if (n.isAdded()) {
        t.push(n), n.setSkip();
        continue;
      }
      r.length = 0;
      for (const a of n.parents) {
        const u = this._dependencyManagerService.getTreeById(a);
        if (!u)
          throw new Error("ParentDependencyTree object is null");
        u.isAdded() || n.isSkip() || r.push(u);
      }
      const s = this._dependencyRTreeCacheForAddressFunction.bulkSearch(n.toRTreeItem());
      for (const a of s) {
        const u = this._dependencyManagerService.getTreeById(a);
        if (!u)
          throw new Error("ParentDependencyTree object is null");
        u.isAdded() || n.isSkip() || r.push(u);
      }
      r.length === 0 ? (t.push(n), n.setSkip()) : (n.setAdded(), e.push(n, ...r));
    }
    return t;
  }
};
sa = Xh([
  jt(0, Qt),
  jt(1, Lt),
  jt(2, va),
  jt(3, kr),
  jt(4, ye(pr)),
  jt(5, ye(Fr)),
  jt(6, ye(yr)),
  jt(7, rs)
], sa);
class V {
  constructor(i) {
    A(this, "_unitId");
    A(this, "_subUnitId");
    A(this, "_row", -1);
    A(this, "_column", -1);
    A(this, "_definedNames");
    A(this, "_locale");
    A(this, "_sheetOrder");
    A(this, "_sheetNameMap");
    A(this, "_formulaDataModel");
    A(this, "_rowCount", -1);
    A(this, "_columnCount", -1);
    /**
     * Whether the function needs to expand the parameters
     */
    A(this, "needsExpandParams", !1);
    /**
     * Whether the function needs to pass in reference object
     */
    A(this, "needsReferenceObject", !1);
    /**
     * Whether the function needs handle locale
     */
    A(this, "needsLocale", !1);
    /**
     * Whether the function needs sheets info
     */
    A(this, "needsSheetsInfo", !1);
    /**
     * Whether the function needs function methods in FormulaDataModel
     */
    A(this, "needsFormulaDataModel", !1);
    /**
     * Whether the function needs the number of rows and columns in the sheet
     */
    A(this, "needsSheetRowColumnCount", !1);
    /**
     * Whether the function needs to filter out rows
     */
    A(this, "needsFilteredOutRows", !1);
    /**
     * Minimum number of parameters
     */
    A(this, "minParams", -1);
    /**
     * Maximum number of parameters
     */
    A(this, "maxParams", -1);
    this._name = i;
  }
  get name() {
    return this._name;
  }
  get unitId() {
    return this._unitId;
  }
  get subUnitId() {
    return this._subUnitId;
  }
  get row() {
    return this._row;
  }
  get column() {
    return this._column;
  }
  dispose() {
  }
  /**
   * In Excel, to inject a defined name into a function that has positioning capabilities,
   * such as using the INDIRECT function to reference a named range,
   * you can write it as follows:
   * =INDIRECT("DefinedName1")
   */
  getDefinedName(i) {
    var t;
    const e = this._definedNames;
    return e == null ? null : (t = Array.from(Object.values(e)).filter((r) => r.name === i)) == null ? void 0 : t[0];
  }
  setDefinedNames(i) {
    this._definedNames = i;
  }
  getLocale() {
    return this._locale;
  }
  setLocale(i) {
    this._locale = i;
  }
  getSheetsInfo() {
    return {
      sheetOrder: this._sheetOrder,
      sheetNameMap: this._sheetNameMap
    };
  }
  setSheetsInfo({
    sheetOrder: i,
    sheetNameMap: e
  }) {
    this._sheetOrder = i, this._sheetNameMap = e;
  }
  setFormulaDataModel(i) {
    this._formulaDataModel = i;
  }
  setSheetRowColumnCount(i, e) {
    this._rowCount = i, this._columnCount = e;
  }
  isAsync() {
    return !1;
  }
  isAddress() {
    return !1;
  }
  isCustom() {
    return !1;
  }
  isArgumentsIgnoreNumberPattern() {
    return !1;
  }
  setRefInfo(i, e, t, r) {
    this._unitId = i, this._subUnitId = e, this._row = t, this._column = r;
  }
  calculateCustom(...i) {
    return null;
  }
  calculate(...i) {
    return g.create(m.VALUE);
  }
  checkArrayType(i) {
    return i.isReferenceObject() || i.isValueObject() && i.isArray();
  }
  /**
   * Starting with 1
   * For instance, The column number (starting with 1 for the left-most column of table_array) that contains the return value.
   * https://support.microsoft.com/en-us/office/vlookup-function-0bbc8083-26fe-4963-8ab8-93a18ad188a1
   * @param indexNum
   */
  getIndexNumValue(i, e = 1) {
    let t = i;
    if (t.isArray() && (t = t.getFirstCell()), t.isBoolean())
      return t.getValue() === !1 ? g.create(m.VALUE) : e;
    if (t.isString()) {
      const r = Number(t.getValue());
      return Number.isNaN(r) ? g.create(m.REF) : r;
    } else if (t.isNumber())
      return t.getValue();
    return g.create(m.VALUE);
  }
  /**
   * A logical value that specifies 1/TRUE , 0/FALSE, default 1
   * For instance range_lookup, A logical value that specifies whether you want VLOOKUP to find an approximate or an exact match
   * Approximate match - 1/TRUE
   * Exact match - 0/FALSE
   * https://support.microsoft.com/en-us/office/vlookup-function-0bbc8083-26fe-4963-8ab8-93a18ad188a1
   * For instance A1, A logical value that specifies what type of reference is contained in the cell ref_text.
   * If a1 is TRUE or omitted, ref_text is interpreted as an A1-style reference.
   * If a1 is FALSE, ref_text is interpreted as an R1C1-style reference.
   * https://support.microsoft.com/zh-cn/office/indirect-%E5%87%BD%E6%95%B0-474b3a3a-8a26-4f44-b491-92b6306fa261
   * @param logicValueObject
   */
  getZeroOrOneByOneDefault(i) {
    if (i == null)
      return 1;
    let e = 1;
    if (i.isArray() && (i = i.getFirstCell()), i.isBoolean())
      i.getValue() === !1 && (e = 0);
    else {
      if (i.isString())
        return;
      i.isNumber() && i.getValue() === 0 && (e = 0);
    }
    return e;
  }
  /**
   * A logical value that specifies 1/TRUE , 0/FALSE, -1, default 1.
   * The difference from getZeroOrOneByOneDefault is that we need to get -1
   * @param logicValueObject
   */
  getMatchTypeValue(i) {
    if (i == null)
      return 1;
    let e = 1;
    if (i.isArray() && (i = i.getFirstCell()), i.isBoolean())
      i.getValue() === !1 && (e = 0);
    else {
      if (i.isString())
        return;
      if (i.isNumber()) {
        const t = i.getValue();
        t <= 0 && (e = t);
      }
    }
    return e;
  }
  binarySearch(i, e, t, r, n) {
    const s = e.binarySearch(i, r, n);
    if (s == null)
      return g.create(m.NA);
    let a;
    return t.getRowCount() === 1 ? a = t.get(0, s) || ue.create() : a = t.get(s, 0) || ue.create(), a.isNull() ? R.create(0) : a;
  }
  _getOneFirstByRaw(i) {
    return i.length === 0 ? g.create(m.NA) : i[0][0] || g.create(m.NA);
  }
  _getOneLastByRaw(i) {
    return i.length === 0 ? g.create(m.NA) : i[i.length - 1][i[0].length - 1] || g.create(m.NA);
  }
  equalSearch(i, e, t, r = !0) {
    const n = i.getValue(), a = e.getEqualSearchIndex(r).get(n);
    return a && t.get(a.row, a.column) || g.create(m.NA);
  }
  fuzzySearch(i, e, t, r = !0) {
    const n = t.pickRaw(e.compare(i, x.EQUALS));
    return r ? this._getOneFirstByRaw(n) : this._getOneLastByRaw(n);
  }
  orderSearch(i, e, t, r = Se.MIN, n = !1) {
    const s = e.orderSearch(i, r, n);
    if (s == null)
      return g.create(m.NA);
    const a = t.get(s.row, s.column) || ue.create();
    return a.isNull() ? g.create(m.NA) : a;
  }
  binarySearchExpand(i, e, t, r = 0, n, s) {
    const a = e.binarySearch(i, n, s);
    return a == null ? g.create(m.NA) : r === 0 ? t.slice([a, a + 1]) : t.slice(void 0, [a, a + 1]);
  }
  equalSearchExpand(i, e, t, r = !0, n = 0) {
    const s = e.isEqual(i);
    let a;
    return r ? a = s.getFirstTruePosition() : a = s.getLastTruePosition(), a == null ? g.create(m.NA) : n === 0 ? t.slice([a.row, a.row + 1]) : t.slice(void 0, [a.column, a.column + 1]);
  }
  fuzzySearchExpand(i, e, t, r = !0, n = 0) {
    const s = e.compare(i, x.EQUALS);
    let a;
    return r ? a = s.getFirstTruePosition() : a = s.getLastTruePosition(), a == null ? g.create(m.NA) : n === 0 ? t.slice([a.row, a.row + 1]) : t.slice(void 0, [a.column, a.column + 1]);
  }
  orderSearchExpand(i, e, t, r = Se.MIN, n = !1, s = 0) {
    const a = e.orderSearch(i, r, n);
    return a == null ? g.create(m.NA) : s === 0 ? t.slice([a.row, a.row + 1]) : t.slice(void 0, [a.column, a.column + 1]);
  }
  flattenArray(i, e = !0) {
    const t = [];
    t[0] = [];
    for (let r = 0; r < i.length; r++) {
      let n = i[r];
      if ((n.isString() || n.isBoolean() || n.isNull()) && (n = n.convertToNumberObjectValue()), n.isError())
        return n;
      if (n.isArray()) {
        let s;
        if (n.iterator((a) => {
          if (a == null || a.isNull() || e && (a.isString() || a.isBoolean()))
            return !0;
          if (a = this._includingLogicalValuesAndText(a), a.isError())
            return s = a, !1;
          t[0].push(a);
        }), s != null && s.isError())
          return s;
      } else
        t[0].push(n);
    }
    return Qr(t, 1, t[0].length);
  }
  _includingLogicalValuesAndText(i) {
    if (i.isBoolean() && (i = th(i)), i.isString()) {
      const e = Number(i.getValue());
      i = R.create(Number.isNaN(e) ? 0 : e);
    }
    return i;
  }
  createReferenceObject(i, e) {
    const t = i.getForcedUnitId(), r = i.getForcedSheetId() || "", n = i.getForcedSheetName(), a = vs({
      unitId: t,
      sheetName: n,
      range: e
    });
    let u;
    return Yr(a) ? u = new La(a) : go(a) ? u = new Pa(a) : Co(a) ? u = new xa(a) : u = new On(e, r, t), this._setReferenceDefault(i, u);
  }
  _setReferenceDefault(i, e) {
    return this.unitId == null || this.subUnitId == null ? g.create(m.REF) : (e.setDefaultUnitId(this.unitId), e.setDefaultSheetId(this.subUnitId), e.setUnitData(i.getUnitData()), e.setRuntimeData(i.getRuntimeData()), e.setArrayFormulaCellData(i.getArrayFormulaCellData()), e.setRuntimeArrayFormulaCellData(i.getRuntimeArrayFormulaCellData()), e);
  }
}
const aa = /* @__PURE__ */ new Map();
function Vs() {
  aa.clear();
}
function Ii(o) {
  return typeof o == "string" ? `s${o}` : typeof o == "number" ? `n${o}` : typeof o == "boolean" ? o ? "b1" : "b0" : "x";
}
class Jh extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, t, r) {
    if (!e.isReferenceObject() || r && !r.isReferenceObject())
      return g.create(m.VALUE);
    let n = t;
    if (t.isReferenceObject() && (n = t.toArrayValueObject()), n.isArray()) {
      const s = n.mapValue((a) => this._handleSingleObject(e, a, r));
      return s.getRowCount() === 1 && s.getColumnCount() === 1 ? s.get(0, 0) : s;
    }
    return this._handleSingleObject(e, n, r);
  }
  _handleSingleObject(e, t, r) {
    if (!t.isError()) {
      let l = x.EQUALS, f = t;
      if (t.isString()) {
        const [h, d] = pt(`${t.getValue()}`);
        l = h, f = d;
      }
      if (l === x.EQUALS) {
        const h = this._hashSumif(e, f, r);
        if (h !== null) return h;
      }
    }
    const n = e.toArrayValueObject();
    let s = es(n, t);
    s = ts(s, n, t);
    const a = n.getRowCount(), u = n.getColumnCount();
    let c = n;
    if (r) {
      c = r.toArrayValueObject();
      const l = c.getRowCount(), f = c.getColumnCount();
      if (a !== l || u !== f) {
        const h = r.getRangeData();
        h.endRow = h.startRow + a - 1, h.endColumn = h.startColumn + u - 1, r.setRangeData(h), c = r.toArrayValueObject();
      }
    }
    return c.pick(s).sum();
  }
  _hashSumif(e, t, r) {
    const n = e.toArrayValueObject();
    let s = n;
    r && (s = r.toArrayValueObject());
    const a = n.getCurrentRow(), u = n.getCurrentColumn(), c = s.getCurrentRow(), l = s.getCurrentColumn(), f = `${n.getUnitId()}_${n.getSheetId()}_${a}_${u}_${n.getRowCount()}_${n.getColumnCount()}_${c}_${l}`;
    let h = aa.get(f);
    if (!h) {
      h = /* @__PURE__ */ new Map();
      const _ = n.getRowCount(), C = n.getColumnCount();
      for (let E = 0; E < _; E++)
        for (let b = 0; b < C; b++) {
          const y = n.get(E, b);
          if (!y || y.isError()) continue;
          const p = s.get(E, b);
          if (!p || p.isError() || !p.isNumber()) continue;
          const N = Ii(y.getValue()), w = p.getValue();
          h.set(N, (h.get(N) || 0) + w);
        }
      aa.set(f, h);
    }
    const d = Ii(t.getValue());
    return R.create(h.get(d) || 0);
  }
}
const ia = /* @__PURE__ */ new Map();
function Os() {
  ia.clear();
}
function Fi(o) {
  return typeof o == "string" ? `s${o}` : typeof o == "number" ? `n${o}` : typeof o == "boolean" ? o ? "b1" : "b0" : "x";
}
class em extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, t) {
    if (!e.isReferenceObject())
      return g.create(m.VALUE);
    let r = t;
    if (t.isReferenceObject() && (r = t.toArrayValueObject()), r.isArray()) {
      const n = r.mapValue((s) => this._handleSingleObject(e, s));
      return n.getRowCount() === 1 && n.getColumnCount() === 1 ? n.get(0, 0) : n;
    }
    return this._handleSingleObject(e, r);
  }
  _handleSingleObject(e, t) {
    if (!t.isError()) {
      let a = x.EQUALS, u = t;
      if (t.isString()) {
        const [c, l] = pt(`${t.getValue()}`);
        a = c, u = l;
      }
      if (a === x.EQUALS) {
        const c = this._hashCountif(e, u);
        if (c !== null) return c;
      }
    }
    const r = e.toArrayValueObject();
    let n = es(r, t);
    n = ts(n, r, t);
    const s = r.pick(n);
    return this._countA(s);
  }
  _hashCountif(e, t) {
    const r = e.toArrayValueObject(), n = `${r.getUnitId()}_${r.getSheetId()}_${r.getCurrentRow()}_${r.getCurrentColumn()}_${r.getRowCount()}_${r.getColumnCount()}`;
    let s = ia.get(n);
    if (!s) {
      s = /* @__PURE__ */ new Map();
      const u = r.getRowCount(), c = r.getColumnCount();
      for (let l = 0; l < u; l++)
        for (let f = 0; f < c; f++) {
          const h = r.get(l, f);
          if (!h || h.isError()) continue;
          const d = Fi(h.getValue());
          s.set(d, (s.get(d) || 0) + 1);
        }
      ia.set(n, s);
    }
    const a = Fi(t.getValue());
    return R.create(s.get(a) || 0);
  }
  _countA(e) {
    let t = R.create(0);
    return e.iterator((r) => {
      if (r == null)
        return !0;
      t = t.plusBy(1);
    }), t;
  }
}
const oa = /* @__PURE__ */ new Map();
function ws() {
  oa.clear();
}
function ki(o) {
  return typeof o == "string" ? `s${o}` : typeof o == "number" ? `n${o}` : typeof o == "boolean" ? o ? "b1" : "b0" : "x";
}
class tm extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 255);
    A(this, "needsReferenceObject", !0);
  }
  calculate(...e) {
    const {
      isError: t,
      errorObject: r,
      rangeIsDifferentSize: n,
      criteriaMaxRowLength: s,
      criteriaMaxColumnLength: a,
      variants: u
    } = Gr(e);
    if (t)
      return r;
    if (n)
      return s === 1 && a === 1 ? g.create(m.VALUE) : O(s, a, g.create(m.VALUE));
    if (s === 1 && a === 1)
      return this._scalarCountifs(u);
    const c = Rt(u, s, a, !0);
    return this._aggregateResults(c);
  }
  /**
   * Fast path: all criteria are scalar (1×1). Iterate ranges once per cell,
   * checking all criteria per row — zero intermediate array allocations.
   */
  _scalarCountifs(e) {
    const t = e.length / 2, r = new Array(t), n = new Array(t), s = new Array(t);
    for (let h = 0; h < t; h++) {
      const d = e[h * 2], _ = e[h * 2 + 1];
      if (!d.isArray())
        return this._fallbackCountifs(e);
      r[h] = d;
      const C = _.isArray() && _.get(0, 0) || _;
      if (C.isError())
        return C;
      if (C.isString()) {
        const [E, b] = pt(`${C.getValue()}`);
        n[h] = E, s[h] = b;
      } else
        n[h] = x.EQUALS, s[h] = C;
    }
    if (n.every((h) => h === x.EQUALS)) {
      const h = this._hashCountifs(r, s);
      if (h !== null) return h;
    }
    const a = r[0].getRowCount(), u = r[0].getColumnCount();
    let c = 0;
    const l = new Array(t);
    let f = !0;
    for (let h = 0; h < t; h++) {
      const d = s[h].getValue();
      l[h] = typeof d == "string" ? d.toLocaleLowerCase() : d, typeof d == "string" && Mr(d) && (f = !1);
    }
    if (f)
      for (let h = 0; h < a; h++)
        for (let d = 0; d < u; d++) {
          let _ = !0;
          for (let C = 0; C < t; C++)
            if (!r[C].rawCompare(h, d, l[C], n[C])) {
              _ = !1;
              break;
            }
          _ && c++;
        }
    else
      for (let h = 0; h < a; h++)
        for (let d = 0; d < u; d++) {
          let _ = !0;
          for (let C = 0; C < t; C++) {
            const E = r[C].get(h, d);
            if (!E || E.isError()) {
              _ = !1;
              break;
            }
            if (!Lr(E, s[C])) {
              const y = n[C];
              if (y === x.EQUALS || y === x.NOT_EQUAL) {
                if (E.isNumber() && s[C].isString()) {
                  const p = s[C].convertToNumberObjectValue();
                  if (p.isNumber()) {
                    const N = E.compare(p, y);
                    (N.isError() || N.getValue() !== !0) && (_ = !1);
                    continue;
                  }
                }
                if (s[C].isNumber() && E.isString()) {
                  const p = E.convertToNumberObjectValue();
                  if (p.isNumber()) {
                    const N = p.compare(s[C], y);
                    (N.isError() || N.getValue() !== !0) && (_ = !1);
                    continue;
                  }
                }
                _ = y === x.NOT_EQUAL;
              } else
                _ = !1;
              if (!_) break;
              continue;
            }
            const b = E.compare(s[C], n[C]);
            if (b.isError() || b.getValue() !== !0) {
              _ = !1;
              break;
            }
          }
          _ && c++;
        }
    return R.create(c);
  }
  _hashCountifs(e, t) {
    const r = e.length;
    let n = "";
    for (let u = 0; u < r; u++) {
      const c = e[u];
      n += `${c.getUnitId()}_${c.getSheetId()}_${c.getCurrentRow()}_${c.getCurrentColumn()}_${c.getRowCount()}_${c.getColumnCount()};`;
    }
    let s = oa.get(n);
    if (!s) {
      s = /* @__PURE__ */ new Map();
      const u = e[0].getRowCount(), c = e[0].getColumnCount();
      for (let l = 0; l < u; l++)
        for (let f = 0; f < c; f++) {
          let h = "", d = !0;
          for (let _ = 0; _ < r; _++) {
            const C = e[_].get(l, f);
            if (!C || C.isError()) {
              d = !1;
              break;
            }
            _ > 0 && (h += "\0"), h += ki(C.getValue());
          }
          d && s.set(h, (s.get(h) || 0) + 1);
        }
      oa.set(n, s);
    }
    let a = "";
    for (let u = 0; u < r; u++)
      u > 0 && (a += "\0"), a += ki(t[u].getValue());
    return R.create(s.get(a) || 0);
  }
  _fallbackCountifs(e) {
    const t = Rt(e, 1, 1, !0);
    return this._aggregateResults(t);
  }
  _aggregateResults(e) {
    const t = e.map((r) => r.map((n) => rm(n)));
    return t.length === 1 && t[0].length === 1 ? t[0][0] : W.create({
      calculateValueList: t,
      rowCount: t.length,
      columnCount: t[0].length,
      unitId: this.unitId || "",
      sheetId: this.subUnitId || "",
      row: this.row,
      column: this.column
    });
  }
}
function rm(o) {
  let i = 0;
  return o.iterator((e) => {
    e != null && e.isBoolean() && e.getValue() === !0 && i++;
  }), R.create(i);
}
var nm = Object.getOwnPropertyDescriptor, sm = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? nm(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, Zt = (o, i) => (e, t) => i(e, t, o);
const am = 500;
function im(o) {
  const i = Number(o == null ? void 0 : o.batchExecutionCount);
  return !Number.isFinite(i) || i <= 0 ? Number.POSITIVE_INFINITY : Math.floor(i);
}
const ua = ze("engine-formula.calculate-formula.service");
let ca = class extends Ve {
  constructor(i, e, t, r, n, s, a) {
    super();
    A(this, "_executionInProgressListener$", new dr());
    A(this, "executionInProgressListener$", this._executionInProgressListener$.asObservable());
    A(this, "_executionCompleteListener$", new dr());
    A(this, "executionCompleteListener$", this._executionCompleteListener$.asObservable());
    A(this, "_executeLock", new yc());
    A(this, "_executionTrace", []);
    this._configService = i, this._lexer = e, this._currentConfigService = t, this._runtimeService = r, this._formulaDependencyGenerator = n, this._interpreter = s, this._astTreeBuilder = a;
  }
  dispose() {
    super.dispose(), this._executionInProgressListener$.complete(), this._executionCompleteListener$.complete(), jr.clear(), He.clear(), ws(), Os(), Vs(), Hs.clear(), Zs.clear();
  }
  getExecutionTrace() {
    return this._executionTrace;
  }
  /**
   * Stop the execution of the formula.
   */
  stopFormulaExecution() {
    this._runtimeService.stopExecution();
  }
  /**
   * When the feature is loading,
   * the pre-calculated content needs to be input to the formula engine in advance,
   * so that the formula can read the correct values.
   * @param featureId
   * @param featureData
   */
  setRuntimeFeatureCellData(i, e) {
    this._runtimeService.setRuntimeFeatureCellData(i, e);
  }
  setRuntimeFeatureRange(i, e) {
    this._runtimeService.setRuntimeFeatureRange(i, e);
  }
  async execute(i) {
    jr.clear(), He.clear(), ws(), Os(), Vs(), this._runtimeService.setFormulaExecuteStage($e.START), this._executionInProgressListener$.next(this._runtimeService.getRuntimeState()), this._currentConfigService.load(i), this._runtimeService.reset();
    const e = i.maxIteration || So;
    this._executeLock.acquire("FORMULA_EXECUTION_LOCK", async () => {
      for (let t = 0; t < e && (this._runtimeService.setFormulaCycleIndex(t), await this._executeStep(), jr.clear(), ws(), Os(), Vs(), !!this._runtimeService.isCycleDependency()); t++)
        ;
      this._runtimeService.setFormulaExecuteStage($e.CALCULATION_COMPLETED), this._executionInProgressListener$.next(this._runtimeService.getRuntimeState()), this._executionCompleteListener$.next(this._runtimeService.getAllRuntimeData()), He.clear(), this._runtimeService.reset();
    });
  }
  async _executeStep() {
    const i = await this._apply();
    if (i == null)
      return;
    const { arrayFormulaRange: e, runtimeFeatureRange: t } = i, { dirtyRanges: r, excludedCell: n } = this._getArrayFormulaDirtyRangeAndExcludedRange(
      e,
      t
    );
    return r == null || r.length === 0 || (this._currentConfigService.loadDirtyRangesAndExcludedCell(r, n), await this._apply(!0)), !0;
  }
  _getArrayFormulaDirtyRangeAndExcludedRange(i, e) {
    const t = [], r = {};
    return Object.keys(i).forEach((n) => {
      const s = i[n];
      if (s == null)
        return !0;
      Object.keys(s).forEach((a) => {
        const u = new Ce(s[a]);
        if (u == null)
          return !0;
        const c = new Ce();
        u.forValue((l, f, h) => {
          c.setValue(l, f, !0), t.push({ unitId: n, sheetId: a, range: h });
        }), r[n] == null && (r[n] = {}), r[n][a] = c;
      });
    }), Object.keys(e).forEach((n) => {
      const s = e[n];
      Object.keys(s).forEach((a) => {
        const u = s[a];
        if (u == null)
          return !0;
        Object.keys(u).forEach((c) => {
          const l = u[c];
          if (l == null)
            return !0;
          for (const f of l)
            t.push({ unitId: a, sheetId: c, range: f });
        });
      });
    }), { dirtyRanges: t, excludedCell: r };
  }
  // eslint-disable-next-line max-lines-per-function
  async _apply(i = !1) {
    i ? this._runtimeService.setFormulaExecuteStage($e.START_DEPENDENCY_ARRAY_FORMULA) : this._runtimeService.setFormulaExecuteStage($e.START_DEPENDENCY), this._executionInProgressListener$.next(this._runtimeService.getRuntimeState());
    const e = (await this._formulaDependencyGenerator.generate()).reverse(), t = this._interpreter;
    i ? (this._runtimeService.setFormulaExecuteStage($e.START_CALCULATION_ARRAY_FORMULA), this._runtimeService.setTotalArrayFormulasToCalculate(e.length)) : (this._runtimeService.setFormulaExecuteStage($e.START_CALCULATION), this._runtimeService.setTotalFormulasToCalculate(e.length)), this._executionInProgressListener$.next(this._runtimeService.getRuntimeState());
    let r = [];
    const n = this._configService.getConfig(Oa), s = (n == null ? void 0 : n.intervalCount) || am, a = im(n), u = e.length;
    let c = 0;
    for (; c < u; ) {
      const l = Math.min(u, c + a);
      for (let f = c; f < l; f++) {
        const h = e[f], d = h.nodeData, _ = h.getDirtyData;
        if (f !== 0 && f % s === 0 && (await new Promise((E) => {
          const b = oi(E);
          r.push(b);
        }), i ? (this._runtimeService.setFormulaExecuteStage(
          $e.CURRENTLY_CALCULATING_ARRAY_FORMULA
        ), this._runtimeService.setCompletedArrayFormulasCount(f)) : (this._runtimeService.setFormulaExecuteStage($e.CURRENTLY_CALCULATING), this._runtimeService.setCompletedFormulasCount(f)), this._executionInProgressListener$.next(this._runtimeService.getRuntimeState()), this._runtimeService.isStopExecution())) {
          this._runtimeService.setFormulaExecuteStage($e.IDLE), this._runtimeService.markedAsStopFunctionsExecuted(), this._executionCompleteListener$.next(this._runtimeService.getAllRuntimeData());
          return;
        }
        if (this._runtimeService.setCurrent(
          h.row,
          h.column,
          h.rowCount,
          h.columnCount,
          h.subUnitId,
          h.unitId
        ), d == null && _ == null)
          continue;
        let C;
        if (_ != null && h.featureId != null) {
          const { runtimeCellData: E, dirtyRanges: b } = _(this._currentConfigService.getDirtyData(), this._runtimeService.getAllRuntimeData());
          this._runtimeService.setRuntimeFeatureCellData(h.featureId, E), this._runtimeService.setRuntimeFeatureRange(h.featureId, b);
        } else d != null && (t.checkAsyncNode(d.node) ? C = await t.executeAsync(d) : C = t.execute(d), h.formulaId != null ? this._runtimeService.setRuntimeOtherData(h.formulaId, h.refOffsetX, h.refOffsetY, C) : this._runtimeService.setRuntimeData(C), d.node.clearIntermediate());
      }
      if (c = l, i ? (this._runtimeService.setFormulaExecuteStage($e.CURRENTLY_CALCULATING_ARRAY_FORMULA), this._runtimeService.setCompletedArrayFormulasCount(c)) : (this._runtimeService.setFormulaExecuteStage($e.CURRENTLY_CALCULATING), this._runtimeService.setCompletedFormulasCount(c)), this._executionInProgressListener$.next(this._runtimeService.getRuntimeState()), c < u && await new Promise((f) => {
        const h = oi(f);
        r.push(h);
      }), this._runtimeService.isStopExecution()) {
        this._runtimeService.setFormulaExecuteStage($e.IDLE), this._runtimeService.markedAsStopFunctionsExecuted(), this._executionCompleteListener$.next(this._runtimeService.getAllRuntimeData());
        return;
      }
    }
    return r.forEach((l) => l()), r = [], u > 0 ? this._runtimeService.markedAsSuccessfullyExecuted() : i || this._runtimeService.markedAsNoFunctionsExecuted(), this._runtimeService.getAllRuntimeData();
  }
  calculate(i, e = !0) {
    const t = this._lexer.treeBuilder(i, e);
    if (Object.values(m).includes(t))
      return Pe.create(t);
    const r = this._astTreeBuilder.parse(t);
    r == null || r.serialize();
  }
};
ca = sm([
  Zt(0, Ca),
  Zt(1, ye(yr)),
  Zt(2, Qt),
  Zt(3, Lt),
  Zt(4, na),
  Zt(5, ye(pr)),
  Zt(6, ye(Fr))
], ca);
var om = Object.getOwnPropertyDescriptor, um = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? om(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, Ss = (o, i) => (e, t) => i(e, t, o);
let Un = class extends Ve {
  constructor(o, i, e) {
    super(), this._commandService = o, this._calculateFormulaService = i, this._formulaDataModel = e, this._initialize();
  }
  _initialize() {
    this._commandExecutedListener(), this._initialExecuteFormulaListener();
  }
  _commandExecutedListener() {
    this.disposeWithMe(
      this._commandService.onCommandExecuted((o) => {
        if (o.id === Ro.id)
          this._calculateFormulaService.stopFormulaExecution();
        else if (o.id === yo.id)
          this._calculate(o.params);
        else if (o.id === Us.id) {
          const i = o.params;
          if (i == null)
            return;
          const { arrayFormulaRange: e, arrayFormulaCellData: t } = i;
          this._formulaDataModel.setArrayFormulaRange(e), this._formulaDataModel.setArrayFormulaCellData(t);
        }
      })
    );
  }
  async _calculate(o) {
    const { forceCalculation: i = !1, dirtyRanges: e = [], dirtyNameMap: t = {}, dirtyDefinedNameMap: r = {}, dirtyUnitFeatureMap: n = {}, dirtyUnitOtherFormulaMap: s = {}, clearDependencyTreeCache: a = {}, maxIteration: u = So, rowData: c } = o, l = this._formulaDataModel.getFormulaData(), f = this._formulaDataModel.getArrayFormulaCellData(), h = this._formulaDataModel.getArrayFormulaRange();
    this._calculateFormulaService.execute({
      formulaData: l,
      arrayFormulaCellData: f,
      arrayFormulaRange: h,
      forceCalculate: i,
      dirtyRanges: e,
      dirtyNameMap: t,
      dirtyDefinedNameMap: r,
      dirtyUnitFeatureMap: n,
      dirtyUnitOtherFormulaMap: s,
      clearDependencyTreeCache: a,
      maxIteration: u,
      rowData: c
    });
  }
  // Notification
  _initialExecuteFormulaListener() {
    this._calculateFormulaService.executionCompleteListener$.subscribe(async (o) => {
      const i = o.functionsExecutedState;
      switch (i) {
        case mr.NOT_EXECUTED:
          break;
        case mr.STOP_EXECUTION:
          break;
        case mr.SUCCESS:
          await this._applyResult(o);
          break;
        case mr.INITIAL:
          break;
      }
      this._commandService.executeCommand(
        yn.id,
        {
          functionsExecutedState: i
        },
        {
          onlyLocal: !0
        }
      );
    }), this._calculateFormulaService.executionInProgressListener$.subscribe((o) => {
      this._commandService.executeCommand(
        yn.id,
        {
          stageInfo: o
        },
        {
          onlyLocal: !0
        }
      );
    });
  }
  async _applyResult(o) {
    const { unitData: i, unitOtherData: e, arrayFormulaRange: t, arrayFormulaCellData: r, clearArrayFormulaCellData: n } = o;
    if (!i) {
      console.error("No sheetData from Formula Engine!");
      return;
    }
    t && (this._formulaDataModel.clearPreviousArrayFormulaCellData(n), this._formulaDataModel.mergeArrayFormulaCellData(r), this._formulaDataModel.mergeArrayFormulaRange(t), this._commandService.executeCommand(
      Us.id,
      {
        arrayFormulaRange: this._formulaDataModel.getArrayFormulaRange(),
        arrayFormulaCellData: this._formulaDataModel.getArrayFormulaCellData()
      },
      {
        onlyLocal: !0
      }
    )), this._commandService.executeCommand(
      po.id,
      {
        unitData: Zc(i),
        unitOtherData: e
      },
      {
        onlyLocal: !0
      }
    );
  }
};
Un = um([
  Ss(0, cr),
  Ss(1, ua),
  Ss(2, ye(br))
], Un);
const $i = /[\[\]]/g;
function Yi(o, i) {
  if ($i.test(o)) {
    const e = Number(o.replace($i, ""));
    return i + e;
  }
  return Number(o) - 1;
}
function Ms(o, i = 0, e = 0) {
  o = o.toLocaleUpperCase();
  const t = o.split(/[RC]/), r = t[1], n = t[2], s = Yi(r, i), a = Yi(n, e);
  return {
    row: s,
    column: a,
    absoluteRefType: Me.NONE
  };
}
function cm(o, i = 0, e = 0) {
  const { refBody: t, sheetName: r, unitId: n } = Aa(o), s = t.indexOf(":");
  if (s === -1) {
    const C = Ms(t, i, e), E = C.row, b = C.column, y = C.absoluteRefType;
    return {
      unitId: n,
      sheetName: r,
      range: {
        startRow: E,
        startColumn: b,
        endRow: E,
        endColumn: b,
        startAbsoluteRefType: y,
        endAbsoluteRefType: y
      }
    };
  }
  const a = t.substring(0, s), u = t.substring(s + 1), c = Ms(a, i, e), l = Ms(u, i, e), f = c.row, h = c.column, d = l.row, _ = l.column;
  return {
    unitId: n,
    sheetName: r,
    range: {
      startRow: f,
      startColumn: h,
      endRow: d,
      endColumn: _,
      startAbsoluteRefType: c.absoluteRefType,
      endAbsoluteRefType: l.absoluteRefType
    }
  };
}
function lm(o) {
  const i = rn(o.startRow, o.startAbsoluteRefType, !0), e = rn(o.startColumn, o.startAbsoluteRefType, !1), t = rn(o.endRow, o.endAbsoluteRefType, !0), r = rn(o.endColumn, o.endAbsoluteRefType, !1);
  return i === t && e === r ? `R${i}C${e}` : `R${i}C${e}:R${t}C${r}`;
}
function rn(o, i = Me.ALL, e) {
  switch (o += 1, i) {
    case Me.ALL:
      return `${o}`;
    case Me.ROW:
      return e ? `${o}` : `[${o}]`;
    case Me.COLUMN:
      return e ? `[${o}]` : `${o}`;
    case Me.NONE:
      return `[${o}]`;
  }
}
function tr(o) {
  let i = o;
  if (o.isArray()) {
    const e = o.getRowCount(), t = o.getColumnCount();
    if (e > 1 || t > 1)
      return g.create(m.VALUE);
    i = o.get(0, 0);
  }
  return i.isError(), i;
}
function Wr(...o) {
  for (let i = 0; i < o.length; i++) {
    const e = tr(o[i]);
    if (e.isError())
      return {
        isError: !0,
        errorObject: e
      };
    o[i] = e;
  }
  return {
    isError: !1,
    variants: o
  };
}
function H(...o) {
  for (let i = 0; i < o.length; i++) {
    const e = tr(o[i]);
    if (e.isError())
      return {
        isError: !0,
        errorObject: e
      };
    if (e.isBoolean())
      return {
        isError: !0,
        errorObject: g.create(m.VALUE)
      };
    o[i] = e;
  }
  return {
    isError: !1,
    variants: o
  };
}
function ke(...o) {
  for (let i = 0; i < o.length; i++) {
    let e = o[i];
    if (e.isError())
      return {
        isError: !0,
        errorObject: e
      };
    if (e.isNull())
      return {
        isError: !0,
        errorObject: g.create(m.NA)
      };
    if (e = tr(o[i]), e.isError())
      return {
        isError: !0,
        errorObject: e
      };
    if (e.isBoolean())
      return {
        isError: !0,
        errorObject: g.create(m.VALUE)
      };
    o[i] = e;
  }
  return {
    isError: !1,
    variants: o
  };
}
function I(...o) {
  for (let i = 0; i < o.length; i++) {
    let e = o[i];
    if (e.isString() && (e = e.convertToNumberObjectValue()), e.isError())
      return {
        isError: !0,
        errorObject: e
      };
    o[i] = e;
  }
  return {
    isError: !1,
    variants: o
  };
}
class fm extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    if (e.isError())
      return e;
    const { isError: n, errorObject: s, variants: a } = Wr(t, r);
    if (n)
      return s;
    const { isError: u, errorObject: c, variants: l } = I(...a);
    if (u)
      return c;
    const [f, h] = l, d = Math.floor(+f.getValue()), _ = Math.floor(+h.getValue());
    if (d < 0 || _ < 0)
      return g.create(m.NUM);
    if (d === 0 || _ === 0)
      return g.create(m.REF);
    const C = e.isArray() ? e.getRowCount() : 1, E = e.isArray() ? e.getColumnCount() : 1;
    if (C === 1 && E === 1)
      return e.isArray() ? e.get(0, 0) : e;
    const b = d > C ? C : d, y = _ > E ? E : _;
    return e.slice([0, b], [0, y]);
  }
}
class hm extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = [];
    for (let r = 0; r < e.length; r++) {
      const n = e[r], s = n.isArray() ? n.getRowCount() : 1, a = n.isArray() ? n.getColumnCount() : 1;
      for (let u = 0; u < s; u++)
        for (let c = 0; c < a; c++) {
          const l = n.isArray() ? n.get(u, c) : n;
          t.push([l]);
        }
    }
    return W.create({
      calculateValueList: t,
      rowCount: t.length,
      columnCount: 1,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
}
var la = /* @__PURE__ */ ((o) => (o.ARRAY_CONSTRAIN = "ARRAY_CONSTRAIN", o.FLATTEN = "FLATTEN", o))(la || {});
const mm = [
  [fm, la.ARRAY_CONSTRAIN],
  [hm, la.FLATTEN]
];
var Nr;
((o) => {
  const i = 0.636619772;
  function e(n, s) {
    let a = 0;
    for (let u = 0; u < n.length; ++u)
      a = s * a + n[u];
    return a;
  }
  function t(n, s, a, u, c) {
    if (s === 0)
      return a;
    if (s === 1)
      return u;
    const l = 2 / n;
    let f = a, h = u, d = u;
    for (let _ = 1; _ < s; ++_)
      d = h * _ * l + c * f, f = h, h = d;
    return d;
  }
  function r(n, s, a, u) {
    return function(l, f) {
      if (a) {
        if (l === 0)
          return a === 1 ? -1 / 0 : 1 / 0;
        if (l < 0)
          return Number.NaN;
      }
      if (f === 0)
        return n(l);
      if (f === 1)
        return s(l);
      if (f < 0)
        return Number.NaN;
      const h = f | 0, d = n(l), _ = s(l);
      return t(l, h, d, _, u);
    };
  }
  o.besselj = /* @__PURE__ */ (() => {
    const n = [-184.9052456, 77392.33017, -1121442418e-2, 6516196407e-1, -13362590354, 57568490574], s = [1, 267.8532712, 59272.64853, 9494680718e-3, 1029532985, 57568490411], a = [2093887211e-16, -2073370639e-15, 2734510407e-14, -0.001098628627, 1], u = [-934935152e-16, 7621095161e-16, -6911147651e-15, 1430488765e-13, -0.01562499995];
    function c(C) {
      let E = 0, b = 0, y = 0, p = C * C;
      if (C < 8)
        b = e(n, p), y = e(s, p), E = b / y;
      else {
        const N = C - 0.785398164;
        p = 64 / p, b = e(a, p), y = e(u, p), E = Math.sqrt(i / C) * (Math.cos(N) * b - Math.sin(N) * y * 8 / C);
      }
      return E;
    }
    const l = [-30.16036606, 15704.4826, -2972611439e-3, 2423968531e-1, -7895059235, 72362614232], f = [1, 376.9991397, 99447.43394, 1858330474e-2, 2300535178, 144725228442], h = [-240337019e-15, 2457520174e-15, -3516396496e-14, 183105e-8, 1], d = [105787412e-15, -88228987e-14, 8449199096e-15, -2002690873e-13, 0.04687499995];
    function _(C) {
      let E = 0, b = 0, y = 0, p = C * C;
      const N = Math.abs(C) - 2.356194491;
      return Math.abs(C) < 8 ? (b = C * e(l, p), y = e(f, p), E = b / y) : (p = 64 / p, b = e(h, p), y = e(d, p), E = Math.sqrt(i / Math.abs(C)) * (Math.cos(N) * b - Math.sin(N) * y * 8 / Math.abs(C)), C < 0 && (E = -E)), E;
    }
    return function C(E, b) {
      const y = Math.round(b);
      if (!Number.isFinite(E))
        return Number.isNaN(E) ? E : 0;
      if (y < 0)
        return (y % 2 ? -1 : 1) * C(E, -y);
      if (E < 0)
        return (y % 2 ? -1 : 1) * C(-E, y);
      if (y === 0)
        return c(E);
      if (y === 1)
        return _(E);
      if (E === 0)
        return 0;
      let p = 0;
      if (E > y)
        p = t(E, y, c(E), _(E), -1);
      else {
        const N = 2 * Math.floor((y + Math.floor(Math.sqrt(40 * y))) / 2);
        let w = !1, S = 0, M = 0, D = 1, L = 0;
        const v = 2 / E;
        for (let j = N; j > 0; j--)
          if (L = j * v * D - S, S = D, D = L, Math.abs(D) > 1e10 && (D *= 1e-10, S *= 1e-10, p *= 1e-10, M *= 1e-10), w && (M += D), w = !w, j === y && (p = S), N - j > 100 && p === 0)
            return Number.NaN;
        M = 2 * M - D, p /= M;
      }
      return p;
    };
  })(), o.bessely = (() => {
    const n = [228.4622733, -86327.92757, 1087988129e-2, -5123598036e-1, 7062834065, -2957821389], s = [1, 226.1030244, 47447.2647, 7189466438e-3, 7452499648e-1, 40076544269], a = [2093887211e-16, -2073370639e-15, 2734510407e-14, -0.001098628627, 1], u = [-934945152e-16, 7621095161e-16, -6911147651e-15, 1430488765e-13, -0.01562499995];
    function c(C) {
      let E = 0, b = 0, y = 0, p = C * C;
      const N = C - 0.785398164;
      return C < 8 ? (b = e(n, p), y = e(s, p), E = b / y + i * (0, o.besselj)(C, 0) * Math.log(C)) : (p = 64 / p, b = e(a, p), y = e(u, p), E = Math.sqrt(i / C) * (Math.sin(N) * b + Math.cos(N) * y * 8 / C)), E;
    }
    const l = [8511.937935, -4237922726e-3, 7349264551e-1, -51534381390, 127527439e4, -4900604943e3], f = [1, 354.9632885, 102042.605, 2245904002e-2, 3733650367, 424441966400, 249958057e5], h = [-240337019e-15, 2457520174e-15, -3516396496e-14, 183105e-8, 1], d = [105787412e-15, -88228987e-14, 8449199096e-15, -2002690873e-13, 0.04687499995];
    function _(C) {
      let E = 0, b = 0, y = 0, p = C * C;
      const N = C - 2.356194491;
      return C < 8 ? (b = C * e(l, p), y = e(f, p), E = b / y + i * ((0, o.besselj)(C, 1) * Math.log(C) - 1 / C)) : (p = 64 / p, b = e(h, p), y = e(d, p), E = Math.sqrt(i / C) * (Math.sin(N) * b + Math.cos(N) * y * 8 / C)), E;
    }
    return r(c, _, 1, -1);
  })(), o.besseli = /* @__PURE__ */ (() => {
    const n = [45813e-7, 0.0360768, 0.2659732, 1.2067492, 3.0899424, 3.5156229, 1], s = [392377e-8, -0.01647633, 0.02635537, -0.02057706, 916281e-8, -157565e-8, 225319e-8, 0.01328592, 0.39894228];
    function a(f) {
      return f <= 3.75 ? e(n, f * f / (3.75 * 3.75)) : Math.exp(Math.abs(f)) / Math.sqrt(Math.abs(f)) * e(s, 3.75 / Math.abs(f));
    }
    const u = [32411e-8, 301532e-8, 0.02658733, 0.15084934, 0.51498869, 0.87890594, 0.5], c = [-420059e-8, 0.01787654, -0.02895312, 0.02282967, -0.01031555, 163801e-8, -362018e-8, -0.03988024, 0.39894228];
    function l(f) {
      return f < 3.75 ? f * e(u, f * f / (3.75 * 3.75)) : (f < 0 ? -1 : 1) * Math.exp(Math.abs(f)) / Math.sqrt(Math.abs(f)) * e(c, 3.75 / Math.abs(f));
    }
    return function f(h, d) {
      const _ = Math.round(d);
      if (_ === 0)
        return a(h);
      if (_ === 1)
        return l(h);
      if (_ < 0)
        return Number.NaN;
      if (Math.abs(h) === 0)
        return 0;
      if (h === 1 / 0)
        return 1 / 0;
      let C = 0, E;
      const b = 2 / Math.abs(h);
      let y = 0, p = 1, N = 0;
      const w = 2 * Math.round((_ + Math.round(Math.sqrt(40 * _))) / 2);
      for (E = w; E > 0; E--)
        if (N = E * b * p + y, y = p, p = N, Math.abs(p) > 1e10 && (p *= 1e-10, y *= 1e-10, C *= 1e-10), E === _ && (C = y), w - E > 100 && C === 0)
          return Number.NaN;
      return C *= f(h, 0) / p, h < 0 && _ % 2 ? -C : C;
    };
  })(), o.besselk = (() => {
    const n = [74e-7, 1075e-7, 262698e-8, 0.0348859, 0.23069756, 0.4227842, -0.57721566], s = [53208e-8, -25154e-7, 587872e-8, -0.01062446, 0.02189568, -0.07832358, 1.25331414];
    function a(f) {
      return f <= 2 ? -Math.log(f / 2) * (0, o.besseli)(f, 0) + e(n, f * f / 4) : Math.exp(-f) / Math.sqrt(f) * e(s, 2 / f);
    }
    const u = [-4686e-8, -110404e-8, -0.01919402, -0.18156897, -0.67278579, 0.15443144, 1], c = [-68245e-8, 325614e-8, -780353e-8, 0.01504268, -0.0365562, 0.23498619, 1.25331414];
    function l(f) {
      return f <= 2 ? Math.log(f / 2) * (0, o.besseli)(f, 1) + 1 / f * e(u, f * f / 4) : Math.exp(-f) / Math.sqrt(f) * e(c, 2 / f);
    }
    return r(a, l, 2, 1);
  })();
})(Nr || (Nr = {}));
function Ta(o) {
  return /^[01]{1,10}$/.test(o);
}
function Ba(o) {
  return /^[0-7]{1,10}$/.test(o);
}
function Ia(o) {
  return /^[0-9A-Fa-f]{1,10}$/.test(o);
}
function rr(o) {
  if (o === 0)
    return 0;
  const i = [
    -1.3026537197817094,
    // eslint-disable-next-line no-loss-of-precision
    0.6419697923564902,
    0.019476473204185836,
    -0.00956151478680863,
    -946595344482036e-18,
    366839497852761e-18,
    42523324806907e-18,
    -20278578112534e-18,
    -1624290004647e-18,
    130365583558e-17,
    15626441722e-18,
    -85238095915e-18,
    6529054439e-18,
    5059343495e-18,
    -991364156e-18,
    -227365122e-18,
    96467911e-18,
    2394038e-18,
    -6886027e-18,
    894487e-18,
    313092e-18,
    -112708e-18,
    381e-18,
    7106e-18,
    -1523e-18,
    -94e-18,
    121e-18,
    -28e-18
  ];
  let e = o, t = !1;
  e < 0 && (e = -e, t = !0);
  const r = 2 / (2 + e), n = 4 * r - 2;
  let s = 0, a = 0, u;
  for (let l = i.length - 1; l > 0; l--)
    u = s, s = n * s - a + i[l], a = u;
  const c = r * Math.exp(-e * e + 0.5 * (i[0] + n * s) - a);
  return t ? c - 1 : 1 - c;
}
function Fa(o) {
  return 1 - rr(o);
}
function gm(o) {
  if (o >= 2)
    return -100;
  if (o <= 0)
    return 100;
  const i = o < 1 ? o : 2 - o, e = Math.sqrt(-2 * Math.log(i / 2));
  let t = -0.70711 * ((2.30753 + e * 0.27061) / (1 + e * (0.99229 + e * 0.04481)) - e);
  for (let r = 0; r < 2; r++) {
    const n = Fa(t) - i;
    t += n / (1.1283791670955126 * Math.exp(-t * t) - t * n);
  }
  return o < 1 ? t : -t;
}
function Cr(o, i = 1) {
  let e = Math.floor(o);
  if (o < 0)
    return Number.NaN;
  let t = 1;
  for (; e > 1 && Number.isFinite(t); )
    t *= e, e -= i;
  return t;
}
function nr(o, i) {
  const e = Math.min(o - i, i);
  let t = 1;
  for (let r = 1; r <= e && Number.isFinite(t); r++)
    t *= o - r + 1, t /= r;
  return t;
}
function fa(o, i) {
  let e = Math.floor(o), t = Math.floor(i);
  for (; t !== 0; ) {
    const r = t;
    t = e % t, e = r;
  }
  return e;
}
function Hi(o, i) {
  const e = fa(o, i);
  return e === 0 ? 0 : Math.abs(o * i) / e;
}
function ka(o) {
  const i = o.length;
  if (i === 1)
    return o[0][0];
  if (i === 2)
    return o[0][0] * o[1][1] - o[0][1] * o[1][0];
  const { rowSwap: e, smallPivotDetected: t, luMatrix: r, permutation: n } = Jo(o);
  if (t)
    return 0;
  let s = e ? 1 : -1;
  for (let a = 0; a < n.length; a++)
    s *= r[a][a];
  return s === 0 ? 0 : s;
}
function dm(o) {
  const i = ka(o);
  return i === 0 ? null : o.length === 1 ? [[1 / i]] : _m(o).map((r) => r.map((n) => n / i));
}
function Cm(o, i, e) {
  return o.filter((t, r) => r !== i).map((t) => t.filter((r, n) => n !== e));
}
function _m(o) {
  const i = o.length, e = Array.from({ length: i }, () => new Array(i).fill(0));
  for (let t = 0; t < i; t++)
    for (let r = 0; r < i; r++) {
      const s = ((t + r) % 2 === 0 ? 1 : -1) * ka(Cm(o, t, r));
      e[r][t] = s === 0 ? 0 : s;
    }
  return e;
}
function _r(o, i) {
  return o.map((e) => i[0].map(
    (t, r) => e.reduce((n, s, a) => n + s * i[a][r], 0)
  ));
}
function _t(o) {
  return o[0].map((i, e) => o.map((t) => t[e]));
}
function Am(o) {
  const { smallPivotDetected: i, luMatrix: e, permutation: t } = Jo(o);
  return i ? null : Em(e, t);
}
function Jo(o) {
  const i = _t(o), e = i.length, t = i[0].length;
  let r = !0, n = !1;
  const s = ha(e, t, 0), a = new Array(t).fill(0).map((u, c) => c);
  for (let u = 0; u < t; u++) {
    for (let h = 0; h < u; h++) {
      let d = i[h][u];
      for (let _ = 0; _ < h; _++)
        d -= s[h][_] * s[_][u];
      s[h][u] = d;
    }
    let c = -1 / 0, l = u;
    for (let h = u; h < e; h++) {
      let d = i[h][u];
      for (let C = 0; C < u; C++)
        d -= s[h][C] * s[C][u];
      s[h][u] = d;
      const _ = Math.abs(d);
      _ > c && (c = _, l = h);
    }
    if (Math.abs(s[l][u]) < 1e-11) {
      n = !0;
      break;
    }
    l !== u && ([s[u], s[l]] = [s[l], s[u]], [i[u], i[l]] = [i[l], i[u]], [a[u], a[l]] = [a[l], a[u]], r = !r);
    const f = s[u][u];
    for (let h = u + 1; h < e; h++)
      s[h][u] /= f;
  }
  return {
    rowSwap: r,
    smallPivotDetected: n,
    luMatrix: s,
    permutation: a
  };
}
function Em(o, i) {
  const e = i.length, t = ha(e, e, 0);
  for (let n = 0; n < e; n++)
    t[n][n] = 1;
  const r = ha(e, e, 0);
  for (let n = 0; n < e; n++) {
    const s = i[n];
    for (let a = 0; a < e; a++)
      r[n][a] = t[s][a];
  }
  for (let n = 0; n < e; n++) {
    const s = r[n];
    for (let a = n + 1; a < e; a++) {
      const u = o[a][n];
      for (let c = 0; c < e; c++)
        r[a][c] -= s[c] * u;
    }
  }
  for (let n = e - 1; n >= 0; n--) {
    const s = r[n], a = o[n][n];
    for (let u = 0; u < e; u++)
      s[u] /= a;
    for (let u = 0; u < n; u++) {
      const c = o[u][n];
      for (let l = 0; l < e; l++)
        r[u][l] -= s[l] * c;
    }
  }
  return r;
}
function ha(o, i, e) {
  const t = [];
  for (let r = 0; r < o; r++) {
    t[r] = [];
    for (let n = 0; n < i; n++)
      t[r].push(e);
  }
  return t;
}
function bm(o) {
  const i = ym(o);
  if (!i)
    return null;
  const { matrixU: e, matrixS: t, matrixV: r } = i, n = _t(e), s = Array.from({ length: t.length }, () => new Array(o[0].length).fill(0)), a = Math.max(o.length, o[0].length) * Number.EPSILON * t[0];
  for (let u = 0; u < t.length; u++)
    Math.abs(t[u]) > a && (s[u][u] = 1 / t[u]);
  return _r(r, _r(s, n));
}
function ym(o) {
  const i = _t(o), e = i.length, t = i[0].length;
  if (e < t)
    return null;
  const r = new Array(t).fill(0), n = new Array(t).fill(0), s = Array.from({ length: t }, () => new Array(t).fill(0));
  let a = Number.EPSILON, u = 0, c = 0, l = 0;
  for (let C = 0; C < t; C++) {
    if (r[C] = u, c = Ds(i, C, e, C, C), c <= 1e-64 / a)
      u = 0;
    else {
      u = Math.sqrt(c), i[C][C] >= 0 && (u = -u);
      const b = i[C][C] * u - c;
      i[C][C] -= u;
      for (let y = C + 1; y < t; y++) {
        c = Ds(i, C, e, C, y);
        for (let p = C; p < e; p++)
          i[p][y] += c / b * i[p][C];
      }
    }
    if (n[C] = u, c = Qi(i, C + 1, t, C, C), c <= 1e-64 / a)
      u = 0;
    else {
      u = Math.sqrt(c), i[C][C + 1] >= 0 && (u = -u);
      const b = i[C][C + 1] * u - c;
      i[C][C + 1] -= u;
      for (let y = C + 1; y < t; y++)
        r[y] = i[C][y] / b;
      for (let y = C + 1; y < e; y++) {
        c = Qi(i, C + 1, t, y, C);
        for (let p = C + 1; p < t; p++)
          i[y][p] += c * r[p];
      }
    }
    const E = Math.abs(n[C]) + Math.abs(r[C]);
    E > l && (l = E);
  }
  let f = 0;
  for (let C = t - 1; C >= 0; C--) {
    if (u !== 0) {
      for (let E = f; E < t; E++)
        s[E][C] = i[C][E] / (u * i[C][C + 1]);
      for (let E = f; E < t; E++) {
        c = 0;
        for (let b = f; b < t; b++)
          c += i[C][b] * s[b][E];
        for (let b = f; b < t; b++)
          s[b][E] += c * s[b][C];
      }
    }
    for (let E = f; E < t; E++)
      s[C][E] = 0, s[E][C] = 0;
    s[C][C] = 1, u = r[C], f = C;
  }
  for (let C = t - 1; C >= 0; C--) {
    u = n[C];
    for (let E = C + 1; E < t; E++)
      i[C][E] = 0;
    if (u !== 0) {
      for (let E = C + 1; E < t; E++) {
        c = Ds(i, C + 1, e, C, E);
        for (let b = C; b < e; b++)
          i[b][E] += c / (i[C][C] * u) * i[b][C];
      }
      for (let E = C; E < e; E++)
        i[E][C] /= u;
    } else
      for (let E = C; E < e; E++)
        i[E][C] = 0;
    i[C][C] += 1;
  }
  a *= l;
  let h = 0, d = 0, _ = 0;
  for (let C = t - 1; C >= 0; C--)
    for (let E = 0; E < 50; E++) {
      let b = !1, y = C;
      for (; y >= 0; y--) {
        if (Math.abs(r[y]) <= a) {
          b = !0;
          break;
        }
        if (Math.abs(n[y - 1]) <= a)
          break;
      }
      if (!b) {
        let S = 0, M = 1;
        for (let D = y; D < C + 1 && (h = M * r[D], d = n[D], r[D] *= S, !(Math.abs(h) <= a)); D++) {
          _ = nn(h, d), n[D] = _, S = d / _, M = -h / _;
          for (let L = 0; L < e; L++) {
            const v = i[L][y - 1], j = i[L][D];
            i[L][y - 1] = v * S + j * M, i[L][D] = -v * M + j * S;
          }
        }
      }
      if (y === C) {
        if (n[C] < 0) {
          n[C] = -n[C];
          for (let S = 0; S < t; S++)
            s[S][C] = -s[S][C];
        }
        break;
      }
      if (E >= 49)
        return null;
      let p = n[y];
      h = ((n[C - 1] - n[C]) * (n[C - 1] + n[C]) + (r[C - 1] - r[C]) * (r[C - 1] + r[C])) / (2 * r[C] * n[C - 1]), _ = nn(h, 1), h < 0 ? h = ((p - n[C]) * (p + n[C]) + r[C] * (n[C - 1] / (h - _) - r[C])) / p : h = ((p - n[C]) * (p + n[C]) + r[C] * (n[C - 1] / (h + _) - r[C])) / p;
      let N = 1, w = 1;
      for (let S = y + 1; S < C + 1; S++) {
        let M = r[S], D = n[S];
        d = w * M, M *= N, _ = nn(h, d), r[S - 1] = _, N = h / _, w = d / _, h = p * N + M * w, d = D * w, M = -p * w + M * N, D *= N;
        for (let L = 0; L < t; L++) {
          const v = s[L][S - 1], j = s[L][S];
          s[L][S - 1] = v * N + j * w, s[L][S] = -v * w + j * N;
        }
        _ = nn(h, d), n[S - 1] = _, N = h / _, w = d / _, h = N * M + w * D, p = -w * M + N * D;
        for (let L = 0; L < e; L++) {
          const v = i[L][S - 1], j = i[L][S];
          i[L][S - 1] = v * N + j * w, i[L][S] = -v * w + j * N;
        }
      }
      r[y] = 0, r[C] = h, n[C] = p;
    }
  for (let C = 0; C < n.length; C++)
    n[C] < a && (n[C] = 0);
  for (let C = 0; C < t; C++)
    for (let E = C - 1; E >= 0; E--)
      if (n[E] < n[C]) {
        const b = n[E];
        n[E] = n[C], n[C] = b;
        for (let y = 0; y < i.length; y++) {
          const p = i[y][C];
          i[y][C] = i[y][E], i[y][E] = p;
        }
        for (let y = 0; y < s.length; y++) {
          const p = s[y][C];
          s[y][C] = s[y][E], s[y][E] = p;
        }
        C = E;
      }
  return {
    matrixU: i,
    matrixS: n,
    matrixV: s
  };
}
function nn(o, i) {
  let e = 0;
  return Math.abs(o) > Math.abs(i) ? (e = i / o, Math.abs(o) * Math.sqrt(1 + e * e)) : i !== 0 ? (e = o / i, Math.abs(i) * Math.sqrt(1 + e * e)) : 0;
}
function Ds(o, i, e, t, r) {
  let n = 0;
  for (let s = i; s < e; s++)
    n += o[s][t] * o[s][r];
  return n;
}
function Qi(o, i, e, t, r) {
  let n = 0;
  for (let s = i; s < e; s++)
    n += o[t][s] * o[r][s];
  return n;
}
const sn = /* @__PURE__ */ new Map([
  ["I", 1],
  ["V", 5],
  ["X", 10],
  ["L", 50],
  ["C", 100],
  ["D", 500],
  ["M", 1e3]
]), Rm = /* @__PURE__ */ new Map([
  [1, "I"],
  [4, "IV"],
  [5, "V"],
  [9, "IX"],
  [10, "X"],
  [40, "XL"],
  [45, "VL"],
  [49, "IL"],
  [50, "L"],
  [90, "XC"],
  [95, "VC"],
  [99, "IC"],
  [100, "C"],
  [400, "CD"],
  [450, "LD"],
  [490, "XD"],
  [495, "VD"],
  [499, "ID"],
  [500, "D"],
  [900, "CM"],
  [950, "LM"],
  [990, "XM"],
  [995, "VM"],
  [999, "IM"],
  [1e3, "M"]
]), pm = [
  [1, 4, 5, 9, 10, 40, 50, 90, 100, 400, 500, 900, 1e3, 4e3],
  [1, 4, 5, 9, 10, 40, 45, 50, 90, 95, 100, 400, 450, 500, 900, 950, 1e3, 4e3],
  [1, 4, 5, 9, 10, 40, 45, 49, 50, 90, 95, 99, 100, 400, 450, 490, 500, 900, 950, 990, 1e3, 4e3],
  [1, 4, 5, 9, 10, 40, 45, 49, 50, 90, 95, 99, 100, 400, 450, 490, 495, 500, 900, 950, 990, 995, 1e3, 4e3],
  [1, 4, 5, 9, 10, 40, 45, 49, 50, 90, 95, 99, 100, 400, 450, 490, 495, 499, 500, 900, 950, 990, 995, 999, 1e3, 4e3]
];
function eu(o, i, e) {
  return o <= 0 ? 0 : o >= 1 ? 1 : ns(o, i, e);
}
function Nm(o, i, e) {
  return o <= 0 || o >= 1 ? 0 : i === 1 && e === 1 ? 1 : i < 512 && e < 512 ? o ** (i - 1) * (1 - o) ** (e - 1) / Ya(i, e) : Math.exp((i - 1) * Math.log(o) + (e - 1) * Math.log(1 - o) - Ha(i, e));
}
function $a(o, i, e) {
  if (o <= 0)
    return 0;
  if (o >= 1)
    return 1;
  const t = 1e-8;
  let r;
  if (i >= 1 && e >= 1) {
    const c = o < 0.5 ? o : 1 - o, l = Math.sqrt(-2 * Math.log(c));
    r = (2.30753 + l * 0.27061) / (1 + l * (0.99229 + l * 0.04481)) - l, o < 0.5 && (r = -r);
    const f = (r * r - 3) / 6, h = 2 / (1 / (2 * i - 1) + 1 / (2 * e - 1)), d = r * Math.sqrt(f + h) / h - (1 / (2 * e - 1) - 1 / (2 * i - 1)) * (f + 5 / 6 - 2 / (3 * h));
    r = i / (i + e * Math.exp(2 * d));
  } else {
    const c = Math.exp(i * Math.log(i / (i + e))) / i, l = Math.exp(e * Math.log(e / (i + e))) / e, f = c + l;
    o < c / f ? r = (i * f * o) ** (1 / i) : r = 1 - (e * f * (1 - o)) ** (1 / e);
  }
  const n = -Ha(i, e);
  let s, a, u;
  for (let c = 0; c < 10; c++) {
    if (r === 0 || r === 1)
      return r;
    if (s = ns(r, i, e) - o, a = Math.exp((i - 1) * Math.log(r) + (e - 1) * Math.log(1 - r) + n), u = s / a, r -= a = u / (1 - 0.5 * Math.min(1, u * ((i - 1) / r - (e - 1) / (1 - r)))), r <= 0 && (r = 0.5 * (r + a)), r >= 1 && (r = 0.5 * (r + a + 1)), Math.abs(a) < t * r && c > 0)
      break;
  }
  return r;
}
function ns(o, i, e) {
  const t = o === 0 || o === 1 ? 0 : Math.exp(ht(i + e) - ht(i) - ht(e) + i * Math.log(o) + e * Math.log(1 - o));
  return o < (i + 1) / (i + e + 2) ? t * Gi(o, i, e) / i : 1 - t * Gi(1 - o, e, i) / e;
}
function Gi(o, i, e) {
  let n = 1 - (i + e) * o / (i + 1);
  Math.abs(n) < 1e-8 && (n = 1e-8), n = 1 / n;
  let s = 1, a = n;
  for (let u = 1; u <= 100; u++) {
    let c = u * (e - u) * o / ((i - 1 + u * 2) * (i + u * 2));
    if (n = 1 + c * n, Math.abs(n) < 1e-8 && (n = 1e-8), s = 1 + c / s, Math.abs(s) < 1e-8 && (s = 1e-8), n = 1 / n, a *= n * s, c = -(i + u) * (i + e + u) * o / ((i + u * 2) * (i + 1 + u * 2)), n = 1 + c * n, Math.abs(n) < 1e-8 && (n = 1e-8), s = 1 + c / s, Math.abs(s) < 1e-8 && (s = 1e-8), n = 1 / n, a *= n * s, Math.abs(n * s - 1) < 1e-8)
      break;
  }
  return a;
}
function Ya(o, i) {
  return o + i > 170 ? Math.exp(Ha(o, i)) : _n(o) * _n(i) / _n(o + i);
}
function Ha(o, i) {
  return ht(o) + ht(i) - ht(o + i);
}
function tu(o, i, e) {
  if (o < 0)
    return 0;
  if (o >= i)
    return 1;
  if (e < 0 || e > 1 || i <= 0)
    return Number.NaN;
  let t = 0;
  for (let r = 0; r <= o; r++)
    t += Qa(r, i, e);
  return t;
}
function Qa(o, i, e) {
  return e === 0 || e === 1 ? i * e === o ? 1 : 0 : nr(i, o) * e ** o * (1 - e) ** (i - o);
}
function Ga(o, i) {
  return o <= 0 ? 0 : qa(i / 2, o / 2);
}
function Vm(o, i) {
  return o < 0 ? 0 : o === 0 && i === 2 ? 0.5 : Math.exp((i / 2 - 1) * Math.log(o) - o / 2 - i / 2 * Math.log(2) - ht(i / 2));
}
function ru(o, i) {
  return o <= 0 ? 0 : o >= 1 ? 1 / 0 : 2 * au(o, i / 2);
}
function Wa(o, i, e) {
  return o < 0 ? 0 : ns(i * o / (i * o + e), i / 2, e / 2);
}
function Om(o, i, e) {
  if (o < 0)
    return 0;
  if (o === 0 && i < 2)
    return 1 / 0;
  if (o === 0 && i === 2)
    return 1;
  let t = 1 / Ya(i / 2, e / 2);
  return t *= (i / e) ** (i / 2), t *= o ** (i / 2 - 1), t *= (1 + i / e * o) ** (-(i + e) / 2), t;
}
function nu(o, i, e) {
  return o <= 0 ? 0 : o >= 1 ? 1 / 0 : e / (i * (1 / $a(o, i / 2, e / 2) - 1));
}
function wm(o, i) {
  return o < 0 ? 0 : 1 - Math.exp(-i * o);
}
function Sm(o, i) {
  return o < 0 ? 0 : i * Math.exp(-i * o);
}
function su(o, i, e) {
  const t = i.length;
  let r = 0, n = 0;
  for (let h = 0; h < t; h++)
    r += i[h], n += e[h];
  const s = r / t, a = n / t;
  let u = 0, c = 0;
  for (let h = 0; h < t; h++)
    u += (i[h] - s) * (e[h] - a), c += (e[h] - a) ** 2;
  if (c === 0)
    return 1 / 0;
  const l = u / c;
  return s - l * a + l * o;
}
function _n(o) {
  const i = [
    -1.716185138865495,
    24.76565080557592,
    -379.80425647094563,
    629.3311553128184,
    866.9662027904133,
    -31451.272968848367,
    -36144.413418691176,
    66456.14382024054
  ], e = [
    -30.8402300119739,
    315.35062697960416,
    -1015.1563674902192,
    -3107.771671572311,
    // eslint-disable-next-line no-loss-of-precision
    22538.11842098015,
    // eslint-disable-next-line no-loss-of-precision
    4755.846277527881,
    -134659.9598649693,
    -115132.2596755535
  ];
  let t = 0, r = o;
  if (o > 171.6243769536076)
    return 1 / 0;
  if (r <= 0) {
    const f = r % 1 + 36e-17;
    if (f)
      t = (r & 1 ? -1 : 1) * Math.PI / Math.sin(Math.PI * f), r = 1 - r;
    else
      return 1 / 0;
  }
  const n = r;
  let s = 0, a;
  r < 1 ? a = r++ : a = (r -= s = (r | 0) - 1) - 1;
  let u = 0, c = 0;
  for (let f = 0; f < 8; ++f)
    u = (u + i[f]) * a, c = c * a + e[f];
  let l = u / c + 1;
  if (n < r)
    l /= n;
  else if (n > r)
    for (let f = 0; f < s; ++f)
      l *= r, r++;
  return t && (l = t / l), l;
}
function Mm(o, i, e) {
  return o <= 0 ? 0 : qa(i, o / e);
}
function Dm(o, i, e) {
  return o < 0 ? 0 : o === 0 && i === 1 ? 1 / e : Math.exp((i - 1) * Math.log(o) - o / e - ht(i) - i * Math.log(e));
}
function Lm(o, i, e) {
  return o <= 0 ? 0 : o >= 1 ? 1 / 0 : e * au(o, i);
}
function ht(o) {
  const i = [
    76.18009172947146,
    -86.50532032941678,
    24.01409824083091,
    // eslint-disable-line
    -1.231739572450155,
    0.001208650973866179,
    -5395239384953e-18
  ];
  let e = o, t = o + 5.5;
  t -= (o + 0.5) * Math.log(t);
  let r = 1.000000000190015;
  for (let n = 0; n < 6; n++)
    r += i[n] / ++e;
  return -t + Math.log(2.5066282746310007 * r / o);
}
function qa(o, i) {
  if (i < 0 || o <= 0)
    return Number.NaN;
  const e = 1e-30, t = -~(Math.log(o >= 1 ? o : 1 / o) * 8.5 + o * 0.4 + 17), r = ht(o), n = Math.exp(-i + o * Math.log(i) - r);
  let s = o, a = 1 / o, u = a;
  if (i < o + 1) {
    if (n === 0)
      return 0;
    for (let d = 1; d <= t && (a += u *= i / ++s, !(Math.abs(u) < Math.abs(a) * e)); d++)
      ;
    return a * n;
  }
  if (n === 0)
    return 1;
  let c = i + 1 - o, l = 1 / e, f = 1 / c, h = f;
  for (let d = 1; d <= t; d++) {
    const _ = -d * (d - o);
    if (c += 2, f = _ * f + c, Math.abs(f) < e && (f = e), l = c + _ / l, Math.abs(l) < e && (l = e), f = 1 / f, h *= f * l, Math.abs(f * l - 1) < e)
      break;
  }
  return 1 - h * n;
}
function au(o, i) {
  if (o <= 0)
    return 0;
  if (o >= 1)
    return Math.max(100, i + 100 * Math.sqrt(i));
  let e;
  if (i > 1) {
    const a = o < 0.5 ? o : 1 - o, u = Math.sqrt(-2 * Math.log(a));
    e = (2.30753 + u * 0.27061) / (1 + u * (0.99229 + u * 0.04481)) - u, o < 0.5 && (e = -e), e = Math.max(1e-3, i * (1 - 1 / (9 * i) - e / (3 * Math.sqrt(i))) ** 3);
  } else {
    const a = 1 - i * (0.253 + i * 0.12);
    o < a ? e = (o / a) ** (1 / i) : e = 1 - Math.log(1 - (o - a) / (1 - a));
  }
  const t = 1e-8, r = ht(i);
  let n, s;
  for (let a = 0; a < 12; a++) {
    if (e <= 0)
      return 0;
    if (n = qa(i, e) - o, i > 1 ? s = Math.exp((i - 1) * (Math.log(i - 1) - 1) - r) * Math.exp(-(e - (i - 1)) + (i - 1) * (Math.log(e) - Math.log(i - 1))) : s = Math.exp(-e + (i - 1) * Math.log(e) - r), s !== 0 && (s = n / s / (1 - 0.5 * Math.min(1, n / s * ((i - 1) / e - 1)))), e -= s, e <= 0 && (e = 0.5 * (e + s)), Math.abs(s) < t * e)
      break;
  }
  return e;
}
function xm(o, i, e, t) {
  let r = 0;
  for (let n = 0; n <= o; n++)
    r += Ka(n, i, e, t);
  return r;
}
function Ka(o, i, e, t) {
  return i - o > t - e ? 0 : nr(e, o) * nr(t - e, i - o) / nr(t, i);
}
function iu(o, i, e) {
  return o < 0 ? 0 : 0.5 + 0.5 * rr((Math.log(o) - i) / Math.sqrt(2 * e * e));
}
function Pm(o, i, e) {
  return o <= 0 ? 0 : Math.exp(-Math.log(o) - 0.5 * Math.log(2 * Math.PI) - Math.log(e) - (Math.log(o) - i) ** 2 / (2 * e * e));
}
function jm(o, i, e) {
  return Math.exp(ss(o, i, e));
}
function Um(o, i, e) {
  if (o < 0)
    return 0;
  let t = 0;
  for (let r = 0; r <= o; r++)
    t += Xa(r, i, e);
  return t;
}
function Xa(o, i, e) {
  return o < 0 ? 0 : nr(o + i - 1, i - 1) * e ** i * (1 - e) ** o;
}
function qr(o, i, e) {
  return 0.5 * (1 + rr((o - i) / Math.sqrt(2 * e * e)));
}
function ou(o, i, e) {
  return Math.exp(-0.5 * Math.log(2 * Math.PI) - Math.log(e) - (o - i) ** 2 / (2 * e * e));
}
function ss(o, i, e) {
  return -1.4142135623730951 * e * gm(2 * o) + i;
}
function vm(o, i) {
  let e = 0;
  for (let t = 0; t <= o; t++)
    e += uu(t, i);
  return e;
}
function uu(o, i) {
  return Math.exp(-i) * i ** o / Cr(o);
}
function Kr(o, i) {
  const e = 0.5 * ns(i / (o ** 2 + i), i / 2, 0.5);
  return o < 0 ? e : 1 - e;
}
function Tm(o, i) {
  const e = (1 + o ** 2 / i) ** (-(i + 1) / 2);
  return 1 / (Math.sqrt(i) * Ya(0.5, i / 2)) * e;
}
function as(o, i) {
  let e = $a(2 * Math.min(o, 1 - o), 0.5 * i, 0.5);
  return e = Math.sqrt(i * (1 - e) / e), o > 0.5 ? e : -e;
}
function rt(o, i, e, t, r) {
  const n = [], s = [];
  let a = !0;
  for (let u = 0; u < e; u++) {
    const c = Math.floor(u / t), l = u % t, f = Math.floor(u / r), h = u % r, d = o.isArray() ? o.get(c, l) : o, _ = i.isArray() ? i.get(f, h) : i;
    if (d.isError())
      return {
        isError: !0,
        errorObject: d,
        array1Values: n,
        array2Values: s,
        noCalculate: a
      };
    if (_.isError())
      return {
        isError: !0,
        errorObject: _,
        array1Values: n,
        array2Values: s,
        noCalculate: a
      };
    if (d.isNull() || _.isNull() || d.isBoolean() || _.isBoolean())
      continue;
    const C = d.getValue(), E = _.getValue();
    !q(C) || !q(E) || (n.push(+C), s.push(+E), a = !1);
  }
  return {
    isError: !1,
    errorObject: null,
    array1Values: n,
    array2Values: s,
    noCalculate: a
  };
}
function is(o, i, e) {
  const t = o.isArray() ? o.getRowCount() : 1, r = o.isArray() ? o.getColumnCount() : 1;
  let n = t, s = r;
  if (i && !i.isNull() && (n = i.isArray() ? i.getRowCount() : 1, s = i.isArray() ? i.getColumnCount() : 1, t === 1 && s !== r || r === 1 && n !== t || t !== 1 && r !== 1 && (n !== t || s !== r)))
    return {
      isError: !0,
      errorObject: g.create(m.REF)
    };
  if (e && !e.isNull()) {
    const a = e.isArray() ? e.getRowCount() : 1, u = e.isArray() ? e.getColumnCount() : 1;
    if (t === 1 && n > 1 && a !== n || r === 1 && s > 1 && u !== s)
      return {
        isError: !0,
        errorObject: g.create(m.REF)
      };
  }
  return {
    isError: !1,
    errorObject: null
  };
}
function bt(o) {
  const i = o.isArray() ? o.getRowCount() : 1, e = o.isArray() ? o.getColumnCount() : 1, t = [];
  for (let r = 0; r < i; r++) {
    t[r] = [];
    for (let n = 0; n < e; n++) {
      const s = o.isArray() ? o.get(r, n) : o;
      if (s.isError() || s.isNull() || s.isBoolean() || s.isString())
        return g.create(m.VALUE);
      t[r].push(+s.getValue());
    }
  }
  return t;
}
function os(o, i) {
  const e = [];
  let t = 1;
  for (let r = 0; r < o; r++) {
    e[r] = [];
    for (let n = 0; n < i; n++)
      e[r].push(t++);
  }
  return e;
}
function us(o, i, e, t) {
  let r = i;
  t && (r = i.map((a) => Math.log(a)));
  let n, s;
  return e ? { slope: n, intercept: s } = Bm(o, r) : { slope: n, intercept: s } = Im(o, r), t && (n = Math.exp(n), s = Math.exp(s)), Number.isNaN(n) && !e && (n = 0), { slope: n, intercept: s, Y: r };
}
function Bm(o, i) {
  const e = i.length;
  let t = 0, r = 0, n = 0, s = 0;
  for (let l = 0; l < e; l++)
    t += o[l], r += i[l], n += o[l] * o[l], s += o[l] * i[l];
  const u = (e * s - t * r) / (e * n - t * t), c = 1 / e * r - u * (1 / e) * t;
  return {
    slope: u,
    intercept: c
  };
}
function Im(o, i) {
  const e = [[...o]], t = [...i];
  let r = e.length, n = e[0].length, s = Math.min(r, n);
  const a = new Array(s).fill(0);
  for (let c = 0; c < s; c++) {
    const l = e[c];
    let f = 0;
    for (let d = 0; d < n; d++)
      f += l[d] ** 2;
    const h = l[c] < 0 ? Math.sqrt(f) : -Math.sqrt(f);
    if (a[c] = h, h !== 0) {
      l[c] -= h;
      for (let d = c + 1; d < r; d++) {
        let _ = 0;
        for (let C = c; C < n; C++)
          _ -= e[d][C] * l[C];
        _ /= h * l[c];
        for (let C = c; C < n; C++)
          e[d][C] -= _ * l[C];
      }
    }
  }
  r = e.length, n = e[0].length, s = Math.min(r, n);
  const u = new Array(r).fill(0);
  for (let c = 0; c < s; c++) {
    const l = e[c];
    let f = 0;
    for (let h = 0; h < n; h++)
      f += t[h] * l[h];
    f /= a[c] * l[c];
    for (let h = 0; h < n; h++)
      t[h] += f * l[h];
  }
  for (let c = a.length - 1; c >= 0; c--) {
    t[c] /= a[c];
    const l = t[c], f = e[c];
    u[c] = l;
    for (let h = 0; h < c; h++)
      t[h] -= l * f[h];
  }
  return {
    slope: u[0],
    intercept: 0
  };
}
function cs(o, i, e, t, r) {
  const n = o.length === 1 && o[0].length > 1;
  let s = o;
  r && (s = o.map((C) => C.map((E) => Math.log(E))));
  let a = i, u = e;
  n && (s = _t(s), a = _t(a), u = _t(u)), t && (a = a.map((C) => [...C, 1]));
  const c = _t(a), l = _r(c, a), f = _r(c, s);
  let h = Am(l);
  if (!h && (h = bm(l), !h))
    return g.create(m.NA);
  let d = _r(h, f);
  t || d.push([0]), d = _t(d);
  const _ = d[0].pop();
  if (d[0].reverse(), d[0].push(_), r)
    for (let C = 0; C < d[0].length; C++)
      d[0][C] = Math.exp(d[0][C]);
  return {
    coefficients: d,
    Y: s,
    X: a,
    newX: u,
    XTXInverse: h
  };
}
class cu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 5);
  }
  calculate(e, t, r, n, s) {
    let a = n != null ? n : R.create(0), u = s != null ? s : R.create(1);
    a.isNull() && (a = R.create(0)), u.isNull() && (u = R.create(1));
    const c = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      a.isArray() ? a.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1
    ), l = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      a.isArray() ? a.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1
    ), f = O(c, l, e, g.create(m.NA)), h = O(c, l, t, g.create(m.NA)), d = O(c, l, r, g.create(m.NA)), _ = O(c, l, a, g.create(m.NA)), C = O(c, l, u, g.create(m.NA)), E = f.mapValue((b, y, p) => {
      const N = h.get(y, p), w = d.get(y, p), S = _.get(y, p), M = C.get(y, p);
      return this._handleSingleObject(b, N, w, S, M);
    });
    return c === 1 && l === 1 ? E.get(0, 0) : E;
  }
  _handleSingleObject(e, t, r, n, s) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (r.isError())
      return r;
    if (n.isError())
      return n;
    if (s.isError())
      return s;
    const { isError: a, errorObject: u, variants: c } = I(e, t, r, n, s);
    if (a)
      return u;
    const [l, f, h, d, _] = c, C = +l.getValue(), E = +f.getValue(), b = +h.getValue(), y = +d.getValue(), p = +_.getValue();
    if (E <= 0 || b <= 0 || C <= 0 || C >= 1 || y >= p)
      return g.create(m.NUM);
    const N = $a(C, E, b) * (p - y) + y;
    return R.create(N);
  }
}
class lu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = O(s, a, n, g.create(m.NA)), h = u.mapValue((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C), y = f.get(_, C);
      return d.isError() ? d : E.isError() ? E : b.isError() ? b : y.isError() ? y : this._handleSingleObject(d, E, b, y);
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
  _handleSingleObject(e, t, r, n) {
    const { isError: s, errorObject: a, variants: u } = I(e, t, r, n);
    if (s)
      return a;
    const [c, l, f, h] = u, d = Math.floor(+c.getValue()), _ = Math.floor(+l.getValue()), C = +f.getValue(), E = +h.getValue();
    if (d < 0 || d > _ || C < 0 || C > 1)
      return g.create(m.NUM);
    let b;
    return E ? b = tu(d, _, C) : b = Qa(d, _, C), R.create(b);
  }
}
class fu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = Math.floor(+u.getValue()), h = +c.getValue(), d = +l.getValue();
    if (f < 0 || h <= 0 || h >= 1 || d <= 0 || d >= 1)
      return g.create(m.NUM);
    let _ = 0;
    for (; _ <= f && !(tu(_, f, h) >= d); )
      _++;
    return R.create(_);
  }
}
class hu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      return c.isError() ? c : h.isError() ? h : this._handleSingleObject(c, h);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
  _handleSingleObject(e, t) {
    const { isError: r, errorObject: n, variants: s } = I(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = Math.floor(+u.getValue());
    if (c < 0 || l < 1 || l > 10 ** 10)
      return g.create(m.NUM);
    const f = 1 - Ga(c, l);
    return R.create(f);
  }
}
class mu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      return c.isError() ? c : h.isError() ? h : this._handleSingleObject(c, h);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
  _handleSingleObject(e, t) {
    const { isError: r, errorObject: n, variants: s } = I(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = Math.floor(+u.getValue());
    if (c < 0 || c > 1 || l < 1 || l > 10 ** 10)
      return g.create(m.NUM);
    const f = ru(1 - c, l);
    return Number.isNaN(f) || !Number.isFinite(f) ? g.create(m.NUM) : R.create(f);
  }
}
class gu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    let u = e;
    if (e.isArray() && r === 1 && n === 1 && (u = e.get(0, 0)), u.isError())
      return u;
    let c = t;
    if (t.isArray() && s === 1 && a === 1 && (c = t.get(0, 0)), c.isError())
      return c;
    if (r * n === 1 || s * a === 1)
      return u.isNull() || c.isNull() ? g.create(m.VALUE) : g.create(m.NA);
    if (r * n !== s * a)
      return g.create(m.NA);
    const {
      isError: l,
      errorObject: f,
      array1Values: h,
      array2Values: d,
      noCalculate: _
    } = rt(
      e,
      t,
      r * n,
      n,
      a
    );
    return l ? f : _ ? g.create(m.DIV_BY_ZERO) : this._getResult(h, d, r, n);
  }
  _getResult(e, t, r, n) {
    let s = 0;
    for (let c = 0; c < e.length; c++) {
      if (t[c] === 0)
        return g.create(m.DIV_BY_ZERO);
      s += (e[c] - t[c]) ** 2 / t[c];
    }
    let a = (r - 1) * (n - 1);
    r === 1 ? a = n - 1 : n === 1 && (a = r - 1);
    const u = 1 - Ga(s, a);
    return R.create(u);
  }
}
class du extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      if (f.isError())
        return f;
      if (_.isError())
        return _;
      if (C.isError())
        return C;
      const { isError: E, errorObject: b, variants: y } = I(f, _, C);
      if (E)
        return b;
      const [p, N, w] = y, S = +p.getValue(), M = +N.getValue(), D = Math.floor(+w.getValue());
      if (S <= 0 || S >= 1 || M <= 0 || D < 1)
        return g.create(m.NUM);
      const L = Math.abs(ss(S / 2, 0, 1) * M / Math.sqrt(D));
      return R.create(L);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
}
class Cu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    let u = e;
    if (e.isArray() && r === 1 && n === 1 && (u = e.get(0, 0)), u.isError())
      return u;
    let c = t;
    if (t.isArray() && s === 1 && a === 1 && (c = t.get(0, 0)), c.isError())
      return c;
    if ((r * n === 1 || s * a === 1) && (u.isNull() || c.isNull()))
      return g.create(m.VALUE);
    if (r * n !== s * a)
      return g.create(m.NA);
    const {
      isError: l,
      errorObject: f,
      array1Values: h,
      array2Values: d,
      noCalculate: _
    } = rt(
      e,
      t,
      r * n,
      n,
      a
    );
    return l ? f : _ ? g.create(m.DIV_BY_ZERO) : this._getResult(h, d);
  }
  _getResult(e, t) {
    if (e.length === 0)
      return g.create(m.DIV_BY_ZERO);
    const r = e.length;
    let n = 0, s = 0;
    for (let l = 0; l < r; l++)
      n += e[l], s += t[l];
    const a = n / r, u = s / r;
    let c = 0;
    for (let l = 0; l < r; l++) {
      const f = e[l] - a, h = t[l] - u;
      c += f * h;
    }
    return R.create(c / r);
  }
}
class _u extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = +c.getValue(), d = +l.getValue();
    if (f < 0 || h <= 0)
      return g.create(m.NUM);
    let _;
    return d ? _ = wm(f, h) : _ = Sm(f, h), R.create(_);
  }
}
class Au extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = Math.floor(+c.getValue()), d = Math.floor(+l.getValue());
    if (f < 0 || h < 1 || h > 10 ** 10 || d < 1 || d > 10 ** 10)
      return g.create(m.NUM);
    const _ = 1 - Wa(f, h, d);
    return R.create(_);
  }
}
class Eu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = Math.floor(+c.getValue()), d = Math.floor(+l.getValue());
    if (f < 0 || f > 1 || h < 1 || h > 10 ** 10 || d < 1 || d > 10 ** 10)
      return g.create(m.NUM);
    const _ = nu(1 - f, h, d);
    return Number.isNaN(_) || !Number.isFinite(_) ? g.create(m.NUM) : R.create(_);
  }
}
class bu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const {
      isError: r,
      errorObject: n,
      variance: s,
      ns1: a
    } = this._getValues(e);
    if (r)
      return n;
    const {
      isError: u,
      errorObject: c,
      variance: l,
      ns1: f
    } = this._getValues(t);
    if (u)
      return c;
    let h = 2 * (1 - Wa(s / l, a, f));
    return h > 1 && (h = 2 - h), R.create(h);
  }
  // eslint-disable-next-line
  _getValues(e) {
    let t = 0, r = 0;
    const n = e.isArray() ? e.getRowCount() : 1, s = e.isArray() ? e.getColumnCount() : 1;
    if (n === 1 && s === 1) {
      const f = e.isArray() ? e.get(0, 0) : e;
      return f.isError() ? {
        isError: !0,
        errorObject: f,
        variance: t,
        ns1: r
      } : f.isNull() ? {
        isError: !0,
        errorObject: g.create(m.VALUE),
        variance: t,
        ns1: r
      } : {
        isError: !0,
        errorObject: g.create(m.DIV_BY_ZERO),
        variance: t,
        ns1: r
      };
    }
    const a = [];
    let u = 0;
    for (let f = 0; f < n; f++)
      for (let h = 0; h < s; h++) {
        const d = e.isArray() ? e.get(f, h) : e;
        if (d.isError())
          return {
            isError: !0,
            errorObject: d,
            variance: t,
            ns1: r
          };
        if (d.isNull() || d.isBoolean())
          continue;
        const _ = d.getValue();
        q(_) && (a.push(+_), u += +_);
      }
    if (a.length <= 1)
      return {
        isError: !0,
        errorObject: g.create(m.DIV_BY_ZERO),
        variance: t,
        ns1: r
      };
    const c = u / a.length;
    let l = 0;
    for (let f = 0; f < a.length; f++)
      l += (a[f] - c) ** 2;
    return r = a.length - 1, t = l / r, t === 0 ? {
      isError: !0,
      errorObject: g.create(m.DIV_BY_ZERO),
      variance: t,
      ns1: r
    } : {
      isError: !1,
      errorObject: null,
      variance: t,
      ns1: r
    };
  }
}
class yu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = O(s, a, n, g.create(m.NA)), h = u.mapValue((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C), y = f.get(_, C);
      return d.isError() ? d : E.isError() ? E : b.isError() ? b : y.isError() ? y : this._handleSingleObject(d, E, b, y);
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
  _handleSingleObject(e, t, r, n) {
    const { isError: s, errorObject: a, variants: u } = I(e, t, r, n);
    if (s)
      return a;
    const [c, l, f, h] = u, d = +c.getValue(), _ = +l.getValue(), C = +f.getValue(), E = +h.getValue();
    if (d < 0 || _ <= 0 || C <= 0)
      return g.create(m.NUM);
    let b;
    return E ? b = Mm(d, _, C) : b = Dm(d, _, C), R.create(b);
  }
}
class Ru extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = +c.getValue(), d = +l.getValue();
    if (f < 0 || f > 1 || h <= 0 || d <= 0)
      return g.create(m.NUM);
    const _ = Lm(f, h, d);
    return Number.isNaN(_) || !Number.isFinite(_) ? g.create(m.NUM) : R.create(_);
  }
}
class pu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = +c.getValue(), d = +l.getValue();
    if (f <= 0 || f >= 1 || d <= 0)
      return g.create(m.NUM);
    const _ = jm(f, h, d);
    return Number.isNaN(_) || !Number.isFinite(_) ? g.create(m.NUM) : R.create(_);
  }
}
class Nu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = {};
    let r = 0, n = 1;
    for (let s = 0; s < e.length; s++) {
      const a = e[s];
      if (a.isError())
        return a;
      if (a.isString()) {
        const l = a.convertToNumberObjectValue();
        if (l.isError())
          return l;
      }
      const u = a.isArray() ? a.getRowCount() : 1, c = a.isArray() ? a.getColumnCount() : 1;
      for (let l = 0; l < u; l++)
        for (let f = 0; f < c; f++) {
          const h = a.isArray() ? a.get(l, f) : a;
          if (h.isError())
            return h;
          if (h.isNull() || h.isBoolean() || h.isString())
            continue;
          const d = h.getValue();
          q(d) && (t[+d] ? (t[+d].count++, t[+d].count > n && (n = t[+d].count)) : t[+d] = { count: 1, order: r++ });
        }
    }
    return r === 0 || n === 1 ? new g(m.NA) : this._getResult(t, n);
  }
  _getResult(e, t) {
    const r = Object.entries(e).filter(([n, { count: s }]) => s === t).sort((n, s) => n[1].order - s[1].order).map(([n]) => +n);
    return R.create(r[0]);
  }
}
class Vu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = O(s, a, n, g.create(m.NA)), h = u.mapValue((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C), y = f.get(_, C);
      return d.isError() ? d : E.isError() ? E : b.isError() ? b : y.isError() ? y : this._handleSingleObject(d, E, b, y);
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
  _handleSingleObject(e, t, r, n) {
    const { isError: s, errorObject: a, variants: u } = I(e, t, r, n);
    if (s)
      return a;
    const [c, l, f, h] = u, d = +c.getValue(), _ = +l.getValue(), C = +f.getValue(), E = +h.getValue();
    if (C <= 0)
      return g.create(m.NUM);
    let b;
    return E ? b = qr(d, _, C) : b = ou(d, _, C), R.create(b);
  }
}
class Ou extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = +c.getValue(), d = +l.getValue();
    if (f <= 0 || f >= 1 || d <= 0)
      return g.create(m.NUM);
    const _ = ss(f, h, d);
    return R.create(_);
  }
}
class wu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return e.getRowCount() === 1 && e.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const { isError: t, errorObject: r, variants: n } = I(e);
    if (t)
      return r;
    const [s] = n, a = +s.getValue();
    if (a <= 0 || a >= 1)
      return g.create(m.NUM);
    const u = ss(a, 0, 1);
    return R.create(u);
  }
}
class Su extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = this._getValues(e);
    if (t.isArray()) {
      const n = t.mapValue((s) => this._handleSingleObject(r, s));
      return n.getRowCount() === 1 && n.getColumnCount() === 1 ? n.get(0, 0) : n;
    }
    return this._handleSingleObject(r, t);
  }
  _handleSingleObject(e, t) {
    if (e instanceof g)
      return e;
    const { isError: r, errorObject: n, variants: s } = I(t);
    if (r)
      return n;
    const [a] = s, u = +a.getValue(), c = e.length;
    if (u < 0 || u > 1)
      return g.create(m.NUM);
    const l = u * (c - 1), f = Math.floor(l), h = l - f;
    if (h === 0)
      return R.create(e[f]);
    const d = e[f] + h * (e[f + 1] - e[f]);
    return R.create(d);
  }
  _getValues(e) {
    const t = e.isArray() ? e.getRowCount() : 1, r = e.isArray() ? e.getColumnCount() : 1, n = [];
    for (let s = 0; s < t; s++)
      for (let a = 0; a < r; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        if (u.isNull() || u.isBoolean() || u.isString())
          continue;
        const c = u.getValue();
        q(c) && n.push(+c);
      }
    return n.length === 0 ? g.create(m.NUM) : n.sort((s, a) => s - a);
  }
}
class Mu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = this._getValues(e);
    let s = r != null ? r : R.create(3);
    s.isNull() && (s = R.create(3));
    const a = Math.max(
      t.isArray() ? t.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1
    ), u = Math.max(
      t.isArray() ? t.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1
    ), c = O(a, u, t, g.create(m.NA)), l = O(a, u, s, g.create(m.NA)), f = c.mapValue((h, d, _) => {
      const C = l.get(d, _);
      return h.isError() ? h : C.isError() ? C : this._handleSingleObject(n, h, C);
    });
    return a === 1 && u === 1 ? f.get(0, 0) : f;
  }
  _handleSingleObject(e, t, r) {
    if (e instanceof g)
      return e;
    const { isError: n, errorObject: s, variants: a } = I(t, r);
    if (n)
      return s;
    const [u, c] = a, l = +u.getValue(), f = Math.floor(+c.getValue()), h = e.length;
    if (h === 0 || l < e[0] || l > e[h - 1])
      return g.create(m.NA);
    if (h === 1)
      return l === e[0] ? R.create(1) : g.create(m.NA);
    let d = 0, _ = !1, C = 0;
    for (; !_ && C < h; )
      l === e[C] ? (d = C / (h - 1), _ = !0) : l > e[C] && C + 1 < h && l < e[C + 1] && (d = (C + (l - e[C]) / (e[C + 1] - e[C])) / (h - 1), _ = !0), C++;
    return _ ? f < 1 ? g.create(m.NUM) : (d = Xe(d, f), R.create(d)) : g.create(m.NA);
  }
  _getValues(e) {
    if (e.isArray()) {
      const s = e.getSortedNumericValues(!1);
      if (s) return s;
    }
    const t = e.isArray() ? e.getRowCount() : 1, r = e.isArray() ? e.getColumnCount() : 1, n = [];
    for (let s = 0; s < t; s++)
      for (let a = 0; a < r; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        if (u.isNull() || u.isBoolean())
          continue;
        const c = u.getValue();
        q(c) && n.push(+c);
      }
    return n.sort((s, a) => s - a);
  }
}
class Du extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = Math.floor(+u.getValue()), h = +c.getValue(), d = +l.getValue();
    if (f < 0 || h < 0)
      return g.create(m.NUM);
    let _;
    return d ? _ = vm(f, h) : _ = uu(f, h), R.create(_);
  }
}
class Lu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = this._getValues(e);
    if (t.isArray()) {
      const n = t.mapValue((s) => this._handleSingleObject(r, s));
      return n.getRowCount() === 1 && n.getColumnCount() === 1 ? n.get(0, 0) : n;
    }
    return this._handleSingleObject(r, t);
  }
  _handleSingleObject(e, t) {
    if (e instanceof g)
      return e;
    const { isError: r, errorObject: n, variants: s } = I(t);
    if (r)
      return n;
    const [a] = s, u = Math.floor(+a.getValue());
    if (u < 0 || u > 4)
      return g.create(m.NUM);
    const c = u / 4, l = e.length, f = c * (l - 1), h = Math.floor(f), d = f - h;
    if (d === 0)
      return R.create(e[h]);
    const _ = e[h] + d * (e[h + 1] - e[h]);
    return R.create(_);
  }
  _getValues(e) {
    const t = e.isArray() ? e.getRowCount() : 1, r = e.isArray() ? e.getColumnCount() : 1, n = [];
    for (let s = 0; s < t; s++)
      for (let a = 0; a < r; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        if (u.isNull() || u.isBoolean())
          continue;
        const c = u.getValue();
        q(c) && n.push(+c);
      }
    return n.length === 0 ? g.create(m.NUM) : n.sort((s, a) => s - a);
  }
}
class xu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = this.flattenArray(e);
    return t.isError() ? t : t.std();
  }
}
class Pu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = this.flattenArray(e);
    return t.isError() ? t : t.std(1);
  }
}
class ju extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      return c.isError() ? c : h.isError() ? h : this._handleSingleObject(c, h);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
  _handleSingleObject(e, t) {
    const { isError: r, errorObject: n, variants: s } = I(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = Math.floor(+u.getValue());
    if (c <= 0 || c > 1 || l < 1 || l > 10 ** 10)
      return g.create(m.NUM);
    const f = Math.abs(as(c / 2, l));
    return Number.isNaN(f) || !Number.isFinite(f) ? g.create(m.NUM) : R.create(f);
  }
}
class Uu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  // eslint-disable-next-line
  calculate(e, t, r, n) {
    const { isError: s, errorObject: a, array1Values: u, array2Values: c } = this._handleArray1AndArray2(e, t), l = this._getArrayValues(e), f = this._getArrayValues(t), h = Math.max(
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), d = Math.max(
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), _ = O(h, d, r, g.create(m.NA)), C = O(h, d, n, g.create(m.NA)), E = _.mapValue((b, y, p) => {
      const N = C.get(y, p);
      if (e.isError())
        return e;
      if (t.isError())
        return t;
      if (b.isError())
        return b;
      if (N.isError())
        return N;
      const { isError: w, errorObject: S, variants: M } = I(b, N);
      if (w)
        return S;
      const [D, L] = M, v = Math.floor(+D.getValue()), j = Math.floor(+L.getValue());
      return ![1, 2].includes(v) || ![1, 2, 3].includes(j) ? g.create(m.NUM) : j === 1 && s ? a : j !== 1 && l instanceof g ? l : j !== 1 && f instanceof g ? f : this._handleSingleObject(
        j === 1 ? u : l,
        j === 1 ? c : f,
        v,
        j
      );
    });
    return h === 1 && d === 1 ? E.get(0, 0) : E;
  }
  _handleSingleObject(e, t, r, n) {
    if (e.length < 2 || t.length < 2)
      return g.create(m.DIV_BY_ZERO);
    const { isError: s, errorObject: a, x: u, degFreedom: c } = this._getTDistParamByArrayValues(e, t, n);
    if (s)
      return a;
    let l = Kr(-u, c);
    return r === 2 && (l *= 2), Number.isNaN(l) || !Number.isFinite(l) ? g.create(m.NUM) : R.create(l);
  }
  _getArrayValues(e) {
    const t = e.isArray() ? e.getRowCount() : 1, r = e.isArray() ? e.getColumnCount() : 1, n = [];
    for (let s = 0; s < t; s++)
      for (let a = 0; a < r; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        if (u.isNull()) {
          if (t * r === 1)
            return g.create(m.VALUE);
          continue;
        }
        u.isBoolean() || u.isString() || n.push(+u.getValue());
      }
    return n;
  }
  // eslint-disable-next-line
  _handleArray1AndArray2(e, t) {
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    let u = e;
    if (e.isArray() && r === 1 && n === 1 && (u = e.get(0, 0)), u.isError())
      return {
        isError: !0,
        errorObject: u,
        array1Values: [],
        array2Values: []
      };
    let c = t;
    if (t.isArray() && s === 1 && a === 1 && (c = t.get(0, 0)), c.isError())
      return {
        isError: !0,
        errorObject: c,
        array1Values: [],
        array2Values: []
      };
    if ((r * n === 1 || s * a === 1) && (u.isNull() || c.isNull()))
      return {
        isError: !0,
        errorObject: g.create(m.VALUE),
        array1Values: [],
        array2Values: []
      };
    if (r * n !== s * a)
      return {
        isError: !0,
        errorObject: g.create(m.NA),
        array1Values: [],
        array2Values: []
      };
    const {
      isError: l,
      errorObject: f,
      array1Values: h,
      array2Values: d,
      noCalculate: _
    } = rt(
      e,
      t,
      r * n,
      n,
      a
    );
    return l ? {
      isError: !0,
      errorObject: f,
      array1Values: [],
      array2Values: []
    } : _ || h.length < 2 ? {
      isError: !0,
      errorObject: g.create(m.DIV_BY_ZERO),
      array1Values: [],
      array2Values: []
    } : {
      isError: !1,
      errorObject: null,
      array1Values: h,
      array2Values: d
    };
  }
  _getTDistParamByArrayValues(e, t, r) {
    return r === 1 ? this._getTDistParamByType1(e, t) : r === 2 ? this._getTDistParamByType2(e, t) : this._getTDistParamByType3(e, t);
  }
  _getTDistParamByType1(e, t) {
    const r = e.length;
    let n = 0, s = 0, a = 0;
    for (let h = 0; h < r; h++)
      n += e[h], s += t[h], a += (e[h] - t[h]) ** 2;
    const u = n - s, c = r * a - u ** 2, l = r - 1;
    return c === 0 ? {
      isError: !0,
      errorObject: g.create(m.DIV_BY_ZERO),
      x: 0,
      degFreedom: l
    } : {
      isError: !1,
      errorObject: null,
      x: Math.abs(u) * Math.sqrt(l / c),
      degFreedom: l
    };
  }
  _getTDistParamByType2(e, t) {
    const r = e.length, n = t.length;
    let s = 0, a = 0;
    for (let E = 0; E < r; E++)
      s += e[E], a += e[E] ** 2;
    let u = 0, c = 0;
    for (let E = 0; E < n; E++)
      u += t[E], c += t[E] ** 2;
    const l = a - s ** 2 / r, f = c - u ** 2 / n, h = Math.sqrt(l + f);
    if (h === 0)
      return {
        isError: !0,
        errorObject: g.create(m.DIV_BY_ZERO),
        x: 0,
        degFreedom: 0
      };
    const d = r - 1 + n - 1, _ = Math.sqrt(r * n * d / (r + n));
    return {
      isError: !1,
      errorObject: null,
      x: Math.abs(s / r - u / n) / h * _,
      degFreedom: d
    };
  }
  _getTDistParamByType3(e, t) {
    const r = e.length, n = t.length;
    let s = 0, a = 0;
    for (let C = 0; C < r; C++)
      s += e[C], a += e[C] ** 2;
    let u = 0, c = 0;
    for (let C = 0; C < n; C++)
      u += t[C], c += t[C] ** 2;
    const l = (a - s ** 2 / r) / (r * (r - 1)), f = (c - u ** 2 / n) / (n * (n - 1));
    if (l + f === 0)
      return {
        isError: !0,
        errorObject: g.create(m.DIV_BY_ZERO),
        x: 0,
        degFreedom: 0
      };
    const h = l / (l + f), d = Math.abs(s / r - u / n) / Math.sqrt(l + f), _ = 1 / (h ** 2 / (r - 1) + (1 - h) ** 2 / (n - 1));
    return {
      isError: !1,
      errorObject: null,
      x: d,
      degFreedom: _
    };
  }
}
class vu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = this.flattenArray(e);
    return t.isError() ? t : t.var();
  }
}
class Tu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = this.flattenArray(e);
    return t.isError() ? t : t.var(1);
  }
}
class Bu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = O(s, a, n, g.create(m.NA)), h = u.mapValue((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C), y = f.get(_, C);
      return d.isError() ? d : E.isError() ? E : b.isError() ? b : y.isError() ? y : this._handleSingleObject(d, E, b, y);
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
  _handleSingleObject(e, t, r, n) {
    const { isError: s, errorObject: a, variants: u } = I(e, t, r, n);
    if (s)
      return a;
    const [c, l, f, h] = u, d = +c.getValue(), _ = +l.getValue(), C = +f.getValue(), E = +h.getValue();
    if (d < 0 || _ <= 0 || C <= 0)
      return g.create(m.NUM);
    const b = Math.exp(-((d / C) ** _));
    let y;
    return E ? y = 1 - b : y = _ / C ** _ * d ** (_ - 1) * b, Number.isNaN(y) || !Number.isFinite(y) ? g.create(m.NUM) : R.create(y);
  }
}
class Iu extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = this._getArrayValues(e), s = Math.max(
      t.isArray() ? t.getRowCount() : 1,
      r != null && r.isArray() ? r.getRowCount() : 1
    ), a = Math.max(
      t.isArray() ? t.getColumnCount() : 1,
      r != null && r.isArray() ? r.getColumnCount() : 1
    ), u = O(s, a, t, g.create(m.NA)), c = r ? O(s, a, r, g.create(m.NA)) : void 0, l = u.mapValue((f, h, d) => {
      if (n instanceof g)
        return n;
      if (f.isError())
        return f;
      const _ = r ? c.get(h, d) : void 0;
      return _ != null && _.isError() ? _ : n.length === 0 ? g.create(m.NA) : n.length === 1 ? g.create(m.DIV_BY_ZERO) : this._handleSingleObject(n, f, _);
    });
    return s === 1 && a === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(t);
    if (n)
      return s;
    const [u] = a, c = +u.getValue(), l = e.length;
    let f = 0, h = 0;
    for (let E = 0; E < l; E++)
      f += e[E], h += e[E] ** 2;
    let d = 0;
    if (r !== void 0) {
      const { isError: E, errorObject: b, variants: y } = I(r);
      if (E)
        return b;
      const [p] = y;
      d = +p.getValue();
    } else {
      const E = f / l;
      d = Math.sqrt((h - 2 * E * f + l * E ** 2) / (l - 1));
    }
    if (d <= 0)
      return g.create(m.NUM);
    const _ = (f / l - c) / (d / Math.sqrt(l)), C = 1 - qr(_, 0, 1);
    return Number.isNaN(C) || !Number.isFinite(C) ? g.create(m.NUM) : R.create(C);
  }
  _getArrayValues(e) {
    const t = [], r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1;
    for (let s = 0; s < r; s++)
      for (let a = 0; a < n; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        if (u.isNull() || u.isBoolean() || u.isString())
          continue;
        const c = u.getValue();
        q(c) && t.push(+c);
      }
    return t;
  }
}
class Fm extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 5);
  }
  calculate(e, t, r, n, s) {
    let a = n != null ? n : R.create(0), u = s != null ? s : R.create(1);
    a.isNull() && (a = R.create(0)), u.isNull() && (u = R.create(1));
    const c = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      a.isArray() ? a.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1
    ), l = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      a.isArray() ? a.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1
    ), f = O(c, l, e, g.create(m.NA)), h = O(c, l, t, g.create(m.NA)), d = O(c, l, r, g.create(m.NA)), _ = O(c, l, a, g.create(m.NA)), C = O(c, l, u, g.create(m.NA)), E = f.mapValue((b, y, p) => {
      const N = h.get(y, p), w = d.get(y, p), S = _.get(y, p), M = C.get(y, p);
      return this._handleSingleObject(b, N, w, S, M);
    });
    return c === 1 && l === 1 ? E.get(0, 0) : E;
  }
  _handleSingleObject(e, t, r, n, s) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (r.isError())
      return r;
    if (n.isError())
      return n;
    if (s.isError())
      return s;
    const { isError: a, errorObject: u, variants: c } = I(e, t, r, n, s);
    if (a)
      return u;
    const [l, f, h, d, _] = c, C = +l.getValue(), E = +f.getValue(), b = +h.getValue(), y = +d.getValue(), p = +_.getValue();
    if (E <= 0 || b <= 0 || C < y || C > p || y === p)
      return g.create(m.NUM);
    const N = eu((C - y) / (p - y), E, b);
    return R.create(N);
  }
}
var le = /* @__PURE__ */ ((o) => (o.BETADIST = "BETADIST", o.BETAINV = "BETAINV", o.BINOMDIST = "BINOMDIST", o.CHIDIST = "CHIDIST", o.CHIINV = "CHIINV", o.CHITEST = "CHITEST", o.CONFIDENCE = "CONFIDENCE", o.COVAR = "COVAR", o.CRITBINOM = "CRITBINOM", o.EXPONDIST = "EXPONDIST", o.FDIST = "FDIST", o.FINV = "FINV", o.FTEST = "FTEST", o.GAMMADIST = "GAMMADIST", o.GAMMAINV = "GAMMAINV", o.HYPGEOMDIST = "HYPGEOMDIST", o.LOGINV = "LOGINV", o.LOGNORMDIST = "LOGNORMDIST", o.MODE = "MODE", o.NEGBINOMDIST = "NEGBINOMDIST", o.NORMDIST = "NORMDIST", o.NORMINV = "NORMINV", o.NORMSDIST = "NORMSDIST", o.NORMSINV = "NORMSINV", o.PERCENTILE = "PERCENTILE", o.PERCENTRANK = "PERCENTRANK", o.POISSON = "POISSON", o.QUARTILE = "QUARTILE", o.RANK = "RANK", o.STDEV = "STDEV", o.STDEVP = "STDEVP", o.TDIST = "TDIST", o.TINV = "TINV", o.TTEST = "TTEST", o.VAR = "VAR", o.VARP = "VARP", o.WEIBULL = "WEIBULL", o.ZTEST = "ZTEST", o))(le || {});
class km extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = O(s, a, n, g.create(m.NA)), h = u.mapValue((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C), y = f.get(_, C);
      return d.isError() ? d : E.isError() ? E : b.isError() ? b : y.isError() ? y : this._handleSingleObject(d, E, b, y);
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
  _handleSingleObject(e, t, r, n) {
    const { isError: s, errorObject: a, variants: u } = I(e, t, r, n);
    if (s)
      return a;
    const [c, l, f, h] = u, d = Math.floor(+c.getValue()), _ = Math.floor(+l.getValue()), C = Math.floor(+f.getValue()), E = Math.floor(+h.getValue());
    if (d < 0 || d > _ || d > C || d < _ - E + C || _ <= 0 || _ > E || C <= 0 || C > E || E <= 0)
      return g.create(m.NUM);
    let b = Ka(d, _, C, E);
    return Number.isNaN(b) && (b = 0), R.create(b);
  }
}
class $m extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = +c.getValue(), d = +l.getValue();
    if (f <= 0 || d <= 0)
      return g.create(m.NUM);
    const _ = iu(f, h, d);
    return R.create(_);
  }
}
class Ym extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = Math.floor(+u.getValue()), h = Math.floor(+c.getValue()), d = +l.getValue();
    if (f < 0 || h < 1 || d <= 0 || d >= 1)
      return g.create(m.NUM);
    const _ = Xa(f, h, d);
    return R.create(_);
  }
}
class Hm extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return e.getRowCount() === 1 && e.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const { isError: t, errorObject: r, variants: n } = I(e);
    if (t)
      return r;
    const [s] = n, a = +s.getValue(), u = qr(a, 0, 1);
    return R.create(u);
  }
}
class Qm extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, t, r) {
    let n = e;
    n.isReferenceObject() && (n = n.toArrayValueObject());
    const { refHasError: s, refErrorObject: a, refNumbers: u } = this._checkRefReferenceObject(t);
    let c = r != null ? r : R.create(0);
    c.isReferenceObject() && (c = c.toArrayValueObject());
    const l = Math.max(
      n.isArray() ? n.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1
    ), f = Math.max(
      n.isArray() ? n.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1
    ), h = O(l, f, n, g.create(m.NA)), d = O(l, f, c, g.create(m.NA)), _ = h.map((C, E, b) => {
      const y = d.get(E, b);
      if (!e.isReferenceObject() && e.isNull())
        return g.create(m.NA);
      if (s)
        return a;
      if (y.isError())
        return y;
      const p = +C.getValue(), N = +y.getValue();
      if (Number.isNaN(p) || Number.isNaN(N))
        return g.create(m.VALUE);
      const S = u.sort((M, D) => N ? M - D : D - M).indexOf(p);
      return S === -1 ? g.create(m.NA) : R.create(S + 1);
    });
    return l === 1 && f === 1 ? _.get(0, 0) : _;
  }
  _checkRefReferenceObject(e) {
    let t = !1, r = g.create(m.NA);
    const n = [];
    return e.isReferenceObject() ? (e.toArrayValueObject().iterator((a) => {
      const u = a;
      if (u.isError())
        return t = !0, r = u, !1;
      if (u.isNull() || u.isBoolean())
        return !0;
      const c = +u.getValue();
      if (Number.isNaN(c))
        return !0;
      n.push(c);
    }), {
      refHasError: t,
      refErrorObject: r,
      refNumbers: n
    }) : {
      refHasError: !0,
      refErrorObject: r,
      refNumbers: n
    };
  }
}
class Gm extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = Math.floor(+c.getValue()), d = Math.floor(+l.getValue());
    if (f < 0 || h < 1 || h > 10 ** 10 || d < 1 || d > 2)
      return g.create(m.NUM);
    let _ = Kr(-f, h);
    return d === 2 && (_ *= 2), Number.isNaN(_) || !Number.isFinite(_) ? g.create(m.NUM) : R.create(_);
  }
}
const Wm = [
  [Fm, le.BETADIST],
  [cu, le.BETAINV],
  [lu, le.BINOMDIST],
  [hu, le.CHIDIST],
  [mu, le.CHIINV],
  [gu, le.CHITEST],
  [du, le.CONFIDENCE],
  [Cu, le.COVAR],
  [fu, le.CRITBINOM],
  [_u, le.EXPONDIST],
  [Au, le.FDIST],
  [Eu, le.FINV],
  [bu, le.FTEST],
  [yu, le.GAMMADIST],
  [Ru, le.GAMMAINV],
  [km, le.HYPGEOMDIST],
  [$m, le.LOGNORMDIST],
  [pu, le.LOGINV],
  [Nu, le.MODE],
  [Ym, le.NEGBINOMDIST],
  [Vu, le.NORMDIST],
  [Ou, le.NORMINV],
  [Hm, le.NORMSDIST],
  [wu, le.NORMSINV],
  [Su, le.PERCENTILE],
  [Mu, le.PERCENTRANK],
  [Du, le.POISSON],
  [Lu, le.QUARTILE],
  [Qm, le.RANK],
  [Pu, le.STDEV],
  [xu, le.STDEVP],
  [Gm, le.TDIST],
  [ju, le.TINV],
  [Uu, le.TTEST],
  [Tu, le.VAR],
  [vu, le.VARP],
  [Bu, le.WEIBULL],
  [Iu, le.ZTEST]
], qm = [];
var Km = /* @__PURE__ */ ((o) => (o.CUBEKPIMEMBER = "CUBEKPIMEMBER", o.CUBEMEMBER = "CUBEMEMBER", o.CUBEMEMBERPROPERTY = "CUBEMEMBERPROPERTY", o.CUBERANKEDMEMBER = "CUBERANKEDMEMBER", o.CUBESET = "CUBESET", o.CUBESETCOUNT = "CUBESETCOUNT", o.CUBEVALUE = "CUBEVALUE", o))(Km || {});
class Xm extends V {
  isCustom() {
    return !0;
  }
}
class dy extends Xm {
  isAsync() {
    return !0;
  }
}
function nt(o) {
  const i = [];
  if (o.isError())
    return {
      isError: !0,
      errorObject: o,
      databaseValues: i
    };
  const e = o.isArray() ? o.getRowCount() : 1, t = o.isArray() ? o.getColumnCount() : 1;
  if (e < 2)
    return {
      isError: !0,
      errorObject: g.create(m.VALUE),
      databaseValues: i
    };
  for (let r = 0; r < e; r++) {
    const n = [];
    for (let s = 0; s < t; s++) {
      const a = o.get(r, s);
      if (a.isNull()) {
        n.push(null);
        continue;
      }
      let u = `${a.getValue()}`;
      if (a.isBoolean() && (u = u.toLocaleUpperCase()), a.isNumber() || q(u)) {
        n.push(+u);
        continue;
      }
      n.push(u);
    }
    i.push(n);
  }
  return {
    isError: !1,
    errorObject: null,
    databaseValues: i
  };
}
function st(o, i) {
  let e = -1;
  if (o.isError())
    return {
      isError: !0,
      errorObject: o,
      fieldIndex: e
    };
  const t = o.isArray() ? o.getRowCount() : 1, r = o.isArray() ? o.getColumnCount() : 1;
  if (t > 1 || r > 1)
    return {
      isError: !0,
      errorObject: g.create(m.VALUE),
      fieldIndex: e
    };
  const n = o.isArray() ? o.get(0, 0) : o;
  let s = `${n.getValue()}`;
  if (n.isNull() ? s = 0 : n.isBoolean() ? s = n.getValue() ? 1 : 0 : (n.isNumber() || q(s)) && (s = Math.floor(+s)), typeof s == "number") {
    if (s < 1 || s > i[0].length)
      return {
        isError: !0,
        errorObject: g.create(m.VALUE),
        fieldIndex: e
      };
    e = s - 1;
  } else if (e = i[0].findIndex((a) => a === null ? !1 : `${a}`.toLocaleLowerCase() === s.toLocaleLowerCase()), e === -1)
    return {
      isError: !0,
      errorObject: g.create(m.VALUE),
      fieldIndex: e
    };
  return {
    isError: !1,
    errorObject: null,
    fieldIndex: e
  };
}
function at(o) {
  const i = [];
  if (o.isError())
    return {
      isError: !0,
      errorObject: o,
      criteriaValues: i
    };
  const e = o.isArray() ? o.getRowCount() : 1, t = o.isArray() ? o.getColumnCount() : 1;
  if (e < 2)
    return {
      isError: !0,
      errorObject: g.create(m.VALUE),
      criteriaValues: i
    };
  for (let r = 0; r < e; r++) {
    const n = [];
    for (let s = 0; s < t; s++) {
      const a = o.get(r, s);
      if (a.isNull()) {
        n.push(null);
        continue;
      }
      const u = `${a.getValue()}`;
      if (a.isBoolean()) {
        n.push(a.getValue() ? 1 : 0);
        continue;
      }
      if (a.isNumber() || q(u)) {
        n.push(+u);
        continue;
      }
      n.push(u);
    }
    i.push(n);
  }
  return {
    isError: !1,
    errorObject: null,
    criteriaValues: i
  };
}
function it(o, i, e) {
  const t = o.length, r = o[0].length, n = {};
  let s = !1;
  for (let a = 1; a < t; a++) {
    let u = !0;
    for (let c = 0; c < r; c++) {
      const l = o[a][c];
      if (l === null)
        continue;
      let f = n[c];
      if (f === void 0) {
        const h = o[0][c];
        f = i[0].findIndex((d) => d === null || h === null ? !1 : `${d}`.toLocaleLowerCase() === `${h}`.toLocaleLowerCase()), n[c] = f;
      }
      if (f === -1 && (typeof l == "string" || l === 0)) {
        u = !1;
        break;
      } else if (f > -1) {
        const h = i[e][f];
        if (h === null) {
          u = !1;
          break;
        }
        const [d, _] = pt(`${l}`);
        if (!Mt.create(`${h}`).compare(_, d).getValue()) {
          u = !1;
          break;
        }
      }
    }
    if (u) {
      s = !0;
      break;
    }
  }
  return s;
}
class Zm extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, databaseValues: a } = nt(e);
    if (n)
      return s;
    const { isError: u, errorObject: c, fieldIndex: l } = st(t, a);
    if (u)
      return c;
    const { isError: f, errorObject: h, criteriaValues: d } = at(r);
    if (f)
      return h;
    let _ = 0, C = 0;
    for (let E = 1; E < a.length; E++) {
      const b = a[E][l];
      typeof b == "number" && it(d, a, E) && (_ += b, C++);
    }
    return C === 0 ? g.create(m.DIV_BY_ZERO) : R.create(_ / C);
  }
}
class zm extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, databaseValues: a } = nt(e);
    if (n)
      return s;
    const { isError: u, errorObject: c, fieldIndex: l } = st(t, a);
    if (u)
      return c;
    const { isError: f, errorObject: h, criteriaValues: d } = at(r);
    if (f)
      return h;
    let _ = 0;
    for (let C = 1; C < a.length; C++)
      typeof a[C][l] == "number" && it(d, a, C) && _++;
    return R.create(_);
  }
}
class Jm extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, databaseValues: a } = nt(e);
    if (n)
      return s;
    const { isError: u, errorObject: c, fieldIndex: l } = st(t, a);
    if (u)
      return c;
    const { isError: f, errorObject: h, criteriaValues: d } = at(r);
    if (f)
      return h;
    let _ = 0;
    for (let C = 1; C < a.length; C++) {
      const E = a[C][l];
      E != null && it(d, a, C) && _++;
    }
    return R.create(_);
  }
}
class eg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, databaseValues: a } = nt(e);
    if (n)
      return s;
    const { isError: u, errorObject: c, fieldIndex: l } = st(t, a);
    if (u)
      return c;
    const { isError: f, errorObject: h, criteriaValues: d } = at(r);
    if (f)
      return h;
    let _ = 0, C = 0;
    for (let E = 1; E < a.length; E++) {
      const b = a[E][l];
      b != null && it(d, a, E) && (_++, C = E);
    }
    return _ === 0 ? g.create(m.VALUE) : _ > 1 ? g.create(m.NUM) : e.get(C, l);
  }
}
class tg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, databaseValues: a } = nt(e);
    if (n)
      return s;
    const { isError: u, errorObject: c, fieldIndex: l } = st(t, a);
    if (u)
      return c;
    const { isError: f, errorObject: h, criteriaValues: d } = at(r);
    if (f)
      return h;
    let _ = -1 / 0, C = 0;
    for (let E = 1; E < a.length; E++) {
      const b = a[E][l];
      typeof b == "number" && it(d, a, E) && (_ = Math.max(_, b), C++);
    }
    return C === 0 ? R.create(0) : R.create(_);
  }
}
class rg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, databaseValues: a } = nt(e);
    if (n)
      return s;
    const { isError: u, errorObject: c, fieldIndex: l } = st(t, a);
    if (u)
      return c;
    const { isError: f, errorObject: h, criteriaValues: d } = at(r);
    if (f)
      return h;
    let _ = 1 / 0, C = 0;
    for (let E = 1; E < a.length; E++) {
      const b = a[E][l];
      typeof b == "number" && it(d, a, E) && (_ = Math.min(_, b), C++);
    }
    return C === 0 ? R.create(0) : R.create(_);
  }
}
class ng extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, databaseValues: a } = nt(e);
    if (n)
      return s;
    const { isError: u, errorObject: c, fieldIndex: l } = st(t, a);
    if (u)
      return c;
    const { isError: f, errorObject: h, criteriaValues: d } = at(r);
    if (f)
      return h;
    let _ = 1, C = 0;
    for (let E = 1; E < a.length; E++) {
      const b = a[E][l];
      typeof b == "number" && it(d, a, E) && (_ *= b, C++);
    }
    return C === 0 ? R.create(0) : R.create(_);
  }
}
class sg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, databaseValues: a } = nt(e);
    if (n)
      return s;
    const { isError: u, errorObject: c, fieldIndex: l } = st(t, a);
    if (u)
      return c;
    const { isError: f, errorObject: h, criteriaValues: d } = at(r);
    if (f)
      return h;
    const _ = [];
    let C = 0, E = 0;
    for (let N = 1; N < a.length; N++) {
      const w = a[N][l];
      typeof w == "number" && it(d, a, N) && (_.push(w), C += w, E++);
    }
    if (E <= 1)
      return g.create(m.DIV_BY_ZERO);
    const b = C / E;
    let y = 0;
    for (let N = 0; N < E; N++)
      y += (_[N] - b) ** 2;
    const p = Math.sqrt(y / (E - 1));
    return R.create(p);
  }
}
class ag extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, databaseValues: a } = nt(e);
    if (n)
      return s;
    const { isError: u, errorObject: c, fieldIndex: l } = st(t, a);
    if (u)
      return c;
    const { isError: f, errorObject: h, criteriaValues: d } = at(r);
    if (f)
      return h;
    const _ = [];
    let C = 0, E = 0;
    for (let N = 1; N < a.length; N++) {
      const w = a[N][l];
      typeof w == "number" && it(d, a, N) && (_.push(w), C += w, E++);
    }
    if (E === 0)
      return g.create(m.DIV_BY_ZERO);
    const b = C / E;
    let y = 0;
    for (let N = 0; N < E; N++)
      y += (_[N] - b) ** 2;
    const p = Math.sqrt(y / E);
    return R.create(p);
  }
}
class ig extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, databaseValues: a } = nt(e);
    if (n)
      return s;
    const { isError: u, errorObject: c, fieldIndex: l } = st(t, a);
    if (u)
      return c;
    const { isError: f, errorObject: h, criteriaValues: d } = at(r);
    if (f)
      return h;
    let _ = 0;
    for (let C = 1; C < a.length; C++) {
      const E = a[C][l];
      typeof E == "number" && it(d, a, C) && (_ += E);
    }
    return R.create(_);
  }
}
class og extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, databaseValues: a } = nt(e);
    if (n)
      return s;
    const { isError: u, errorObject: c, fieldIndex: l } = st(t, a);
    if (u)
      return c;
    const { isError: f, errorObject: h, criteriaValues: d } = at(r);
    if (f)
      return h;
    const _ = [];
    let C = 0, E = 0;
    for (let N = 1; N < a.length; N++) {
      const w = a[N][l];
      typeof w == "number" && it(d, a, N) && (_.push(w), C += w, E++);
    }
    if (E <= 1)
      return g.create(m.DIV_BY_ZERO);
    const b = C / E;
    let y = 0;
    for (let N = 0; N < E; N++)
      y += (_[N] - b) ** 2;
    const p = y / (E - 1);
    return R.create(p);
  }
}
class ug extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, databaseValues: a } = nt(e);
    if (n)
      return s;
    const { isError: u, errorObject: c, fieldIndex: l } = st(t, a);
    if (u)
      return c;
    const { isError: f, errorObject: h, criteriaValues: d } = at(r);
    if (f)
      return h;
    const _ = [];
    let C = 0, E = 0;
    for (let N = 1; N < a.length; N++) {
      const w = a[N][l];
      typeof w == "number" && it(d, a, N) && (_.push(w), C += w, E++);
    }
    if (E === 0)
      return g.create(m.DIV_BY_ZERO);
    const b = C / E;
    let y = 0;
    for (let N = 0; N < E; N++)
      y += (_[N] - b) ** 2;
    const p = y / E;
    return R.create(p);
  }
}
var qe = /* @__PURE__ */ ((o) => (o.DAVERAGE = "DAVERAGE", o.DCOUNT = "DCOUNT", o.DCOUNTA = "DCOUNTA", o.DGET = "DGET", o.DMAX = "DMAX", o.DMIN = "DMIN", o.DPRODUCT = "DPRODUCT", o.DSTDEV = "DSTDEV", o.DSTDEVP = "DSTDEVP", o.DSUM = "DSUM", o.DVAR = "DVAR", o.DVARP = "DVARP", o))(qe || {});
const cg = [
  [Zm, qe.DAVERAGE],
  [zm, qe.DCOUNT],
  [Jm, qe.DCOUNTA],
  [eg, qe.DGET],
  [tg, qe.DMAX],
  [rg, qe.DMIN],
  [ng, qe.DPRODUCT],
  [sg, qe.DSTDEV],
  [ag, qe.DSTDEVP],
  [ig, qe.DSUM],
  [og, qe.DVAR],
  [ug, qe.DVARP]
], Za = "yyyy/mm/dd;@", lg = "yyyy/mm/dd hh:mm", fg = "h:mm A/P";
function Re(o) {
  const i = new Date(Date.UTC(1900, 0, 1)), e = new Date(Date.UTC(1900, 1, 28)), t = Date.UTC(o.getFullYear(), o.getMonth(), o.getDate());
  let r = (t - i.getTime()) / (1e3 * 3600 * 24);
  return t > e.getTime() && (r += 1), Math.floor(r) + 1;
}
function Xr(o) {
  const i = new Date(Date.UTC(1900, 0, 1, 0, 0, 0)), e = new Date(Date.UTC(1900, 1, 28, 0, 0, 0));
  let r = (o.getTime() - i.getTime()) / (1e3 * 3600 * 24);
  return o > e && (r += 1), r + 1;
}
function re(o) {
  const i = new Date(Date.UTC(1900, 0, 1)), e = new Date(Date.UTC(1900, 1, 28));
  let t = Math.floor(o) - 1;
  return t > (e.getTime() - i.getTime()) / (1e3 * 3600 * 24) && (t -= 1), new Date(i.getTime() + t * (1e3 * 3600 * 24));
}
function za(o) {
  const i = new Date(Date.UTC(1900, 0, 1, 0, 0, 0)), e = new Date(Date.UTC(1900, 1, 28, 0, 0, 0));
  let t = o - 1;
  return t > (e.getTime() - i.getTime()) / (1e3 * 3600 * 24) && (t -= 1), t < 0 && (t = o), new Date(i.getTime() + t * (1e3 * 3600 * 24));
}
function Ja(o) {
  if (!/^\d{4}[-/](0?[1-9]|1[012])[-/](0?[1-9]|[12][0-9]|3[01])$/.test(o))
    return !1;
  const e = o.replace(/-/g, "/").replace(/T.+/, ""), t = /* @__PURE__ */ new Date(`${e}`);
  if (Number.isNaN(t.getTime()))
    return !1;
  const r = t.getFullYear(), n = (t.getMonth() + 1).toString().padStart(2, "0"), s = t.getDate().toString().padStart(2, "0"), a = `${r}-${n}-${s}`;
  return o.replace(/\//g, "-").split("-").map((c) => c.padStart(2, "0")).join("-") === a;
}
function vn(o) {
  return Et.parseDate(o);
}
function hg(o) {
  return Et.parseValue(o);
}
function Tn(o) {
  return Et.parseTime(o);
}
function ei(o) {
  return Et.getFormatInfo(o).isDate;
}
const Fu = {
  1: [6, 0],
  // Saturday, Sunday
  2: [0, 1],
  // Sunday, Monday
  3: [1, 2],
  // Monday, Tuesday
  4: [2, 3],
  // Tuesday, Wednesday
  5: [3, 4],
  // Wednesday, Thursday
  6: [4, 5],
  // Thursday, Friday
  7: [5, 6],
  // Friday, Saturday
  11: [0],
  // Sunday only
  12: [1],
  // Monday only
  13: [2],
  // Tuesday only
  14: [3],
  // Wednesday only
  15: [4],
  // Thursday only
  16: [5],
  // Friday only
  17: [6]
  // Saturday only
};
function $r(o) {
  return !!(typeof o == "string" && /^[0|1]{7}/.test(o) || Fu[Number(o)]);
}
function ku(o) {
  if (!$r(o))
    return [];
  if (typeof o == "string" && /^[0|1]{7}/.test(o)) {
    const i = [];
    for (let e = 1; e <= o.length; e++)
      `${o[e - 1]}` == "1" && (e === o.length ? i.push(0) : i.push(e));
    return i;
  }
  return Fu[Number(o)] || [];
}
function Bn(o, i, e = 1, t) {
  const r = ku(e), n = Math.floor(o), s = Math.floor(i), a = s > n ? n : s;
  let u = 0;
  const c = Math.abs(Math.floor(i) - Math.floor(o)) + 1;
  for (let l = 0; l < c; l++) {
    const f = a + l;
    if (t && t.length > 0 && t.some((d) => Math.floor(d) === f))
      continue;
    const h = ir(f);
    r.includes(h) || u++;
  }
  return s >= n ? u : -u;
}
function In(o, i, e = 1, t) {
  const r = ku(e), n = Math.floor(o);
  let s = n, a = Math.abs(i);
  for (let u = 1; u <= a; u++) {
    const c = i < 0 ? n - u : n + u;
    if (c < 0)
      return g.create(m.NUM);
    if (t && t.length > 0 && t.some((f) => Math.floor(f) === c)) {
      a++;
      continue;
    }
    const l = ir(c);
    if (r.includes(l)) {
      a++;
      continue;
    }
    s = c;
  }
  return s;
}
function $(o) {
  if (o.isError())
    return o;
  const i = o.getValue();
  if (o.isString()) {
    let e;
    if (vn(`${i}`))
      e = vn(`${i}`).v;
    else if (Tn(`${i}`))
      e = Tn(`${i}`).v;
    else if (q(i))
      e = +i;
    else
      return g.create(m.VALUE);
    return e instanceof Date && (e = Xr(e)), +e < 0 || +e > 2958465 ? g.create(m.NUM) : +e;
  } else {
    const e = +o.getValue();
    return e < 0 || e > 2958465 ? g.create(m.NUM) : e;
  }
}
function ir(o) {
  const i = Math.floor(o) === 60;
  let e = re(o);
  const t = new Date(Date.UTC(e.getFullYear(), e.getMonth(), e.getDate())).getTime(), r = new Date(Date.UTC(1900, 1, 28)).getTime();
  return !i && t <= r && (e = new Date(t - 24 * 3600 * 1e3)), new Date(Date.UTC(e.getFullYear(), e.getMonth(), e.getDate())).getUTCDay();
}
function _e(o, i, e) {
  switch (e) {
    case 0:
      return mg(o, i);
    case 1:
      return gg(o, i);
    case 2:
      return {
        days: Math.abs(i - o),
        yearDays: 360
      };
    case 3:
      return {
        days: Math.abs(i - o),
        yearDays: 365
      };
    case 4:
      return dg(o, i);
    default:
      return {
        days: Math.abs(i - o),
        yearDays: 365
      };
  }
}
function mg(o, i) {
  const e = re(o), t = o > 0 ? e.getUTCFullYear() : 1900, r = o > 0 ? e.getUTCMonth() + 1 : 1;
  let n = o > 0 ? e.getUTCDate() : 0, s = re(i), a = i > 0 ? s.getUTCFullYear() : 1900, u = i > 0 ? s.getUTCMonth() + 1 : 1, c = i > 0 ? s.getUTCDate() : 0;
  r === 2 ? re(o + 1).getUTCMonth() + 1 === 3 && (n = 30) : n === 31 && (n = 30), c === 31 && (n < 30 ? (s = re(i + 1), a = s.getUTCFullYear(), u = s.getUTCMonth() + 1, c = s.getUTCDate()) : c = 30);
  const l = (a - t) * 360, f = i >= o ? 30 - n : -n, h = i >= o ? c : c - 30, d = (i >= o ? u - r - 1 : u - r + 1) * 30;
  return {
    days: Math.abs(l + f + h + d),
    yearDays: 360
  };
}
function gg(o, i) {
  const e = re(o), t = o > 0 ? e.getUTCFullYear() : 1900, r = re(i), n = i > 0 ? r.getUTCFullYear() : 1900, s = Math.abs(i - o), a = Math.abs(n - t) + 1;
  let u, c;
  if (n < t) {
    const l = new Date(Date.UTC(n, 0, 1)), f = new Date(Date.UTC(t, 11, 31));
    u = Re(l), c = Re(f), n === 1900 && (u += 1);
  } else {
    const l = new Date(Date.UTC(t, 0, 1)), f = new Date(Date.UTC(n, 11, 31));
    u = Re(l), c = Re(f), t === 1900 && (u += 1);
  }
  return {
    days: s,
    yearDays: (c - u + 1) / a
  };
}
function dg(o, i) {
  const e = re(o), t = o > 0 ? e.getUTCFullYear() : 1900, r = o > 0 ? e.getUTCMonth() + 1 : 1;
  let n = o > 0 ? e.getUTCDate() : 0;
  const s = re(i), a = i > 0 ? s.getUTCFullYear() : 1900, u = i > 0 ? s.getUTCMonth() + 1 : 1;
  let c = i > 0 ? s.getUTCDate() : 0;
  n === 31 && (n = 30), c === 31 && (c = 30);
  const l = (a - t) * 360, f = i >= o ? 30 - n : -n, h = i >= o ? c : c - 30, d = (i >= o ? u - r - 1 : u - r + 1) * 30;
  return {
    days: Math.abs(l + f + h + d),
    yearDays: 360
  };
}
function Cg(o) {
  return o % 4 === 0 && o % 100 !== 0 || o % 400 === 0;
}
function _g(o) {
  return o % 4 === 0 && o % 100 !== 0 || o % 400 === 0 || o === 1900;
}
const Ag = [31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31], Eg = [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
function Zr(o, i) {
  return Cg(o) ? Ag[i] : Eg[i];
}
function ls(o) {
  return _g(o) ? 366 : 365;
}
function $u(o, i) {
  switch (i) {
    case 0:
    case 2:
    case 4:
      return 360;
    case 1:
      return ls(re(o).getUTCFullYear());
    case 3:
      return 365;
    default:
      return -1;
  }
}
function Vr(o, i, e) {
  return Zr(o, i) === e;
}
function Or(o, i) {
  let e = o.getUTCFullYear(), t = o.getUTCMonth();
  const r = o.getUTCDate();
  return Vr(e, t, r) ? (o.setUTCDate(1), o.setUTCMonth(o.getUTCMonth() + i), e = o.getUTCFullYear(), t = o.getUTCMonth(), o.setUTCDate(Zr(e, t))) : o.setUTCMonth(o.getUTCMonth() + i), o;
}
class bg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (r.isError())
      return r;
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e), u = O(n, s, t), c = O(n, s, r);
    return a.map((l, f, h) => {
      const d = u.get(f, h) || ue.create(), _ = c.get(f, h) || ue.create();
      if (l.isError())
        return l;
      if (d.isError())
        return d;
      if (_.isError())
        return _;
      if (l.isString() || d.isString() || _.isString())
        return g.create(m.VALUE);
      let C = +l.getValue();
      const E = Math.floor(+d.getValue()), b = +_.getValue();
      if (C < 0 || C > 9999)
        return g.create(m.NUM);
      C >= 0 && C < 1899 && (C += 1900);
      const y = new Date(C, E - 1, b), p = Re(y);
      return p < 0 ? g.create(m.NUM) : R.create(p, Za);
    });
  }
}
class yg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    let n = e, s = t, a = r;
    if (n.isArray() && (n = n.get(0, 0)), s.isArray() && (s = s.get(0, 0)), a.isArray() && (a = a.get(0, 0)), n.isError())
      return n;
    if (s.isError())
      return s;
    if (a.isError())
      return a;
    const u = $(n);
    if (typeof u != "number")
      return u;
    const c = $(s);
    return typeof c != "number" ? c : c < u || !a.isString() ? g.create(m.NUM) : this._getResultByUnit(u, c, a);
  }
  _getResultByUnit(e, t, r) {
    const n = re(e), s = n.getUTCFullYear(), a = n.getUTCMonth() + 1, u = n.getUTCDate(), c = re(t), l = c.getUTCFullYear(), f = c.getUTCMonth() + 1, h = c.getUTCDate(), d = `${r.getValue()}`.toLocaleUpperCase();
    let _ = 0, C;
    switch (d) {
      case "Y":
        _ = l - s, (f < a || f === a && h < u) && (_ -= 1);
        break;
      case "M":
        _ = (l - s) * 12 + f - a, h < u && (_ -= 1);
        break;
      case "D":
        _ = Math.floor(t) - Math.floor(e);
        break;
      case "MD":
        _ = h - u, h < u && (C = new Date(Date.UTC(l, f - 1, 0)), _ += Zr(C.getUTCFullYear(), C.getUTCMonth()));
        break;
      case "YM":
        _ = f - a, (f < a || f === a && h < u) && (_ += 12), h < u && (_ -= 1);
        break;
      case "YD":
        C = new Date(Date.UTC(s, f - 1, h)), (f < a || f === a && h < u) && (C = new Date(Date.UTC(s + 1, f - 1, h))), _ = Math.floor(Re(C)) - Math.floor(e);
        break;
      default:
        return g.create(m.NUM);
    }
    return R.create(_);
  }
}
class Rg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  isArgumentsIgnoreNumberPattern() {
    return !0;
  }
  calculate(e) {
    return e.isArray() ? e.mapValue((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    if (e.isString()) {
      const t = `${e.getValue()}`;
      let r = vn(t);
      if (r === null && (r = Tn(t)), r) {
        let { v: n, z: s } = r;
        if (s && ei(s))
          return n instanceof Date && (n = Xr(n)), R.create(Math.trunc(+n));
      }
    }
    return g.create(m.VALUE);
  }
}
class pg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isError() ? e : e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t;
    const r = e.getValue();
    if (e.isString()) {
      if (!Ja(`${r}`))
        return g.create(m.VALUE);
      t = /* @__PURE__ */ new Date(`${r}`);
    } else {
      const a = +e.getValue();
      if (a < 0)
        return g.create(m.NUM);
      if (a === 0)
        return R.create(0);
      t = re(a);
    }
    const n = t.getDate();
    return R.create(n);
  }
}
class Ng extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e), a = O(r, n, t), u = s.map((c, l, f) => {
      const h = a.get(l, f);
      if (c.isError())
        return c;
      if (h.isError())
        return h;
      const d = $(c);
      if (typeof d != "number")
        return d;
      const _ = $(h);
      if (typeof _ != "number")
        return _;
      const C = Math.floor(d) - Math.floor(_);
      return R.create(C);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
}
class Vg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = r != null ? r : T.create(!1);
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (n.isError())
      return n;
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, n, g.create(m.NA)), f = u.map((h, d, _) => {
      const C = c.get(d, _);
      let E = l.get(d, _);
      if (h.isError())
        return h;
      const b = $(h);
      if (typeof b != "number")
        return b;
      if (C.isError())
        return C;
      const y = $(C);
      if (typeof y != "number")
        return y;
      if (E.isString() && (E = E.convertToNumberObjectValue()), E.isError())
        return E;
      const p = +E.getValue(), { days: N } = _e(b, y, p ? 4 : 0), w = y >= b ? N : -N;
      return R.create(w);
    });
    return s === 1 && a === 1 ? f.get(0, 0) : f;
  }
}
class Og extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e), a = O(r, n, t);
    return s.map((u, c, l) => {
      const f = a.get(c, l) || ue.create();
      if (u.isError())
        return u;
      if (f.isError())
        return f;
      if (u.isString() || u.isBoolean() || f.isString() || f.isBoolean())
        return g.create(m.VALUE);
      const h = +u.getValue();
      if (h < 0)
        return g.create(m.NUM);
      const d = Math.floor(+f.getValue()), _ = re(h), C = _.getUTCFullYear(), E = _.getUTCMonth() + d, b = _.getUTCDate(), y = new Date(Date.UTC(C, E, b)), p = Re(y);
      return R.create(p, Za);
    });
  }
}
class wg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = e, n = t;
    if (r.isArray()) {
      const d = r.getRowCount(), _ = r.getColumnCount();
      if (d > 1 || _ > 1)
        return g.create(m.VALUE);
      r = r.get(0, 0);
    }
    if (n.isArray()) {
      const d = n.getRowCount(), _ = n.getColumnCount();
      if (d > 1 || _ > 1)
        return g.create(m.VALUE);
      n = n.get(0, 0);
    }
    if (r.isError())
      return r;
    if (n.isError())
      return n;
    const s = $(r);
    if (typeof s != "number")
      return s;
    if (n.isBoolean())
      return g.create(m.VALUE);
    const a = re(s), u = s > 0 ? a.getUTCFullYear() : 1900, c = s > 0 ? a.getUTCMonth() : 0, l = Math.floor(+n.getValue());
    if (Number.isNaN(l))
      return g.create(m.VALUE);
    const f = new Date(Date.UTC(u, c + l + 1, 0)), h = Re(f);
    return R.create(h);
  }
}
class Sg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(1), { isError: n, errorObject: s, timestampIsReferenceObject: a, timestampObject: u, unitObject: c } = this._checkVariants(e, r);
    if (n)
      return s;
    if (u.isNull() || u.isBoolean() || u.isString() || !a && u.isNumber() && u.getPattern() !== "")
      return g.create(m.VALUE);
    let l = +u.getValue();
    const { isError: f, errorObject: h, variants: d } = I(c);
    if (f)
      return h;
    const [_] = d, C = Math.floor(+_.getValue());
    if (l < 0 || C < 1 || C > 3)
      return g.create(m.NUM);
    C === 1 && (l = l * 1e3), C === 3 && (l = l / 1e3);
    const E = new Date(Date.UTC(1970, 0, 1, 0, 0, 0, 0) + l);
    if (Number.isNaN(E.getTime())) {
      const b = 25569 + l / 864e5;
      return R.create(b);
    } else {
      const b = Xr(E);
      return R.create(b, "yyyy-MM-dd AM/PM hh:mm:ss");
    }
  }
  _checkVariants(e, t) {
    const r = e.isReferenceObject(), n = t.isReferenceObject();
    let s = e;
    if (r && (s = e.toArrayValueObject()), s.isArray()) {
      const u = s.getRowCount(), c = s.getColumnCount();
      if (u > 1 || c > 1)
        return {
          isError: !0,
          errorObject: g.create(m.VALUE)
        };
      s = s.get(0, 0);
    }
    if (s.isError())
      return {
        isError: !0,
        errorObject: s
      };
    let a = t;
    if (n && (a = t.toArrayValueObject()), a.isArray()) {
      const u = a.getRowCount(), c = a.getColumnCount();
      if (u > 1 || c > 1)
        return {
          isError: !0,
          errorObject: g.create(m.VALUE)
        };
      a = a.get(0, 0);
    }
    return a.isError() ? {
      isError: !0,
      errorObject: a
    } : {
      isError: !1,
      errorObject: null,
      timestampIsReferenceObject: r,
      timestampObject: s,
      unitIsReferenceObject: n,
      unitObject: a
    };
  }
}
var be = /* @__PURE__ */ ((o) => (o.DATE = "DATE", o.DATEDIF = "DATEDIF", o.DATEVALUE = "DATEVALUE", o.DAY = "DAY", o.DAYS = "DAYS", o.DAYS360 = "DAYS360", o.EDATE = "EDATE", o.EOMONTH = "EOMONTH", o.EPOCHTODATE = "EPOCHTODATE", o.HOUR = "HOUR", o.ISOWEEKNUM = "ISOWEEKNUM", o.MINUTE = "MINUTE", o.MONTH = "MONTH", o.NETWORKDAYS = "NETWORKDAYS", o.NETWORKDAYS_INTL = "NETWORKDAYS.INTL", o.NOW = "NOW", o.SECOND = "SECOND", o.TIME = "TIME", o.TIMEVALUE = "TIMEVALUE", o.TO_DATE = "TO_DATE", o.TODAY = "TODAY", o.WEEKDAY = "WEEKDAY", o.WEEKNUM = "WEEKNUM", o.WORKDAY = "WORKDAY", o.WORKDAY_INTL = "WORKDAY.INTL", o.YEAR = "YEAR", o.YEARFRAC = "YEARFRAC", o))(be || {});
class Mg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isError() ? e : e.isArray() ? e.map((t) => t.isError() ? t : this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const t = $(e);
    if (typeof t != "number")
      return t;
    if (t === 0)
      return R.create(0);
    const n = za(t).getUTCHours();
    return R.create(n);
  }
}
class Dg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isError() ? e : e.isArray() ? e.map((t) => t.isError() ? t : this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const t = $(e);
    if (typeof t != "number")
      return t;
    const r = re(t), n = t > 0 ? r.getUTCFullYear() : 1900;
    let s = new Date(Date.UTC(n, 0, 1)), a = Re(s), u = ir(a), c;
    u < 1 ? c = a + 1 : u <= 4 ? c = a - (u - 1) : c = a + (11 - u), t < c && (s = new Date(Date.UTC(n - 1, 0, 1)), a = Re(s), u = ir(a), u < 1 ? c = a + 1 : u <= 4 ? c = a - (u - 1) : c = a + (11 - u));
    const l = Math.ceil((t - c + 1) / 7);
    return R.create(l);
  }
}
class Lg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isError() ? e : e.isArray() ? e.map((t) => t.isError() ? t : this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const t = $(e);
    if (typeof t != "number")
      return t;
    if (t === 0)
      return R.create(0);
    const n = za(t).getUTCMinutes();
    return R.create(n);
  }
}
class xg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    let t;
    const r = e.getValue();
    if (e.isString()) {
      if (!Ja(`${r}`))
        return g.create(m.VALUE);
      t = /* @__PURE__ */ new Date(`${r}`);
    } else {
      const a = +e.getValue();
      if (a < 0)
        return g.create(m.NUM);
      if (a === 0)
        return R.create(1);
      t = re(a);
    }
    const n = t.getUTCMonth() + 1;
    return R.create(n);
  }
}
class Pg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    let n = e, s = t;
    if (n.isArray()) {
      const l = n.getRowCount(), f = n.getColumnCount();
      if (l > 1 || f > 1)
        return g.create(m.VALUE);
      n = n.get(0, 0);
    }
    if (n.isError())
      return n;
    if (s.isArray()) {
      const l = s.getRowCount(), f = s.getColumnCount();
      if (l > 1 || f > 1)
        return g.create(m.VALUE);
      s = s.get(0, 0);
    }
    if (s.isError())
      return s;
    if (r != null && r.isError())
      return r;
    if (n.isBoolean() || s.isBoolean())
      return g.create(m.VALUE);
    const a = $(n);
    if (typeof a != "number")
      return a;
    const u = $(s);
    if (typeof u != "number")
      return u;
    if (r)
      return this._getResultByHolidays(a, u, r);
    const c = Bn(a, u);
    return R.create(c);
  }
  _getResultByHolidays(e, t, r) {
    const n = [];
    if (r != null && r.isArray()) {
      const a = r.getRowCount(), u = r.getColumnCount();
      for (let c = 0; c < a; c++)
        for (let l = 0; l < u; l++) {
          const f = r.get(c, l);
          if (f.isBoolean())
            return g.create(m.VALUE);
          const h = $(f);
          if (typeof h != "number")
            return h;
          n.push(h);
        }
    } else {
      if (r.isBoolean())
        return g.create(m.VALUE);
      const a = $(r);
      if (typeof a != "number")
        return a;
      n.push(a);
    }
    const s = Bn(e, t, 1, n);
    return R.create(s);
  }
}
class jg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    return e.isError() ? e : t.isError() ? t : r != null && r.isError() ? r : n != null && n.isError() ? n : r != null && r.isArray() ? r.map((s) => this._handleSingleObject(e, t, s, n)) : this._handleSingleObject(e, t, r, n);
  }
  _handleSingleObject(e, t, r, n) {
    let s = e, a = t;
    if (s.isArray()) {
      const h = s.getRowCount(), d = s.getColumnCount();
      if (h > 1 || d > 1)
        return g.create(m.VALUE);
      s = s.get(0, 0);
    }
    if (s.isError())
      return s;
    if (a.isArray()) {
      const h = a.getRowCount(), d = a.getColumnCount();
      if (h > 1 || d > 1)
        return g.create(m.VALUE);
      a = a.get(0, 0);
    }
    if (a.isError())
      return a;
    let u = 1;
    if (r) {
      if (u = r.getValue(), r.isBoolean() && (u = +u), r.isString() && !$r(u))
        return g.create(m.VALUE);
      if (!$r(u))
        return g.create(m.NUM);
    }
    if (s.isBoolean() || a.isBoolean())
      return g.create(m.VALUE);
    const c = $(s);
    if (typeof c != "number")
      return c;
    const l = $(a);
    if (typeof l != "number")
      return l;
    if (n)
      return this._getResultByHolidays(c, l, u, n);
    const f = Bn(c, l, u);
    return R.create(f);
  }
  _getResultByHolidays(e, t, r, n) {
    const s = [];
    if (n != null && n.isArray()) {
      const u = n.getRowCount(), c = n.getColumnCount();
      for (let l = 0; l < u; l++)
        for (let f = 0; f < c; f++) {
          const h = n.get(l, f);
          if (h.isBoolean())
            return g.create(m.VALUE);
          const d = $(h);
          if (typeof d != "number")
            return d;
          s.push(d);
        }
    } else {
      if (n.isBoolean())
        return g.create(m.VALUE);
      const u = $(n);
      if (typeof u != "number")
        return u;
      s.push(u);
    }
    const a = Bn(e, t, r, s);
    return R.create(a);
  }
}
class Ug extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 0);
    A(this, "maxParams", 0);
  }
  calculate() {
    const e = /* @__PURE__ */ new Date(), t = new Date(Date.UTC(e.getFullYear(), e.getMonth(), e.getDate(), e.getHours(), e.getMinutes(), e.getSeconds())), r = Xr(t);
    return R.create(r, lg);
  }
}
class vg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isError() ? e : e.isArray() ? e.map((t) => t.isError() ? t : this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const t = $(e);
    if (typeof t != "number")
      return t;
    if (t === 0)
      return R.create(0);
    const n = za(t).getUTCSeconds();
    return R.create(n);
  }
}
class Tg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (r.isError())
      return r;
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e), u = O(n, s, t), c = O(n, s, r);
    return a.map((l, f, h) => this._calculateTime(l, u, c, f, h));
  }
  _calculateTime(e, t, r, n, s) {
    let a = e, u = t.get(n, s) || ue.create(), c = r.get(n, s) || ue.create();
    if ((a.isString() || a.isBoolean()) && (a = a.convertToNumberObjectValue()), (u.isString() || u.isBoolean()) && (u = u.convertToNumberObjectValue()), (c.isString() || c.isBoolean()) && (c = c.convertToNumberObjectValue()), a.isError())
      return a;
    if (u.isError())
      return u;
    if (c.isError())
      return c;
    let l = Math.floor(+a.getValue()), f = Math.floor(+u.getValue()), h = Math.floor(+c.getValue());
    if (l < 0 || f < 0 || h < 0 || l > 32767 || f > 32767 || h > 32767)
      return g.create(m.NUM);
    f += Math.floor(h / 60), h %= 60, l += Math.floor(f / 60), f %= 60, l %= 24;
    const _ = (l * 3600 + f * 60 + h) / 86400;
    return R.create(_, fg);
  }
}
class Bg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  isArgumentsIgnoreNumberPattern() {
    return !0;
  }
  calculate(e) {
    return e.isArray() ? e.mapValue((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    if (e.isString()) {
      const t = `${e.getValue()}`, r = hg(t);
      if (r) {
        let { v: n, z: s } = r;
        if (s && ei(s))
          return n instanceof Date && (n = Xr(n)), R.create(Qf(+n));
      }
    }
    return g.create(m.VALUE);
  }
}
class Ig extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e) {
    const t = e.isReferenceObject();
    let r = e;
    if (t && (r = e.toArrayValueObject()), r.isArray()) {
      const s = r.getRowCount(), a = r.getColumnCount();
      if (s > 1 || a > 1)
        return g.create(m.VALUE);
      r = r.get(0, 0);
    }
    if (r.isError() || r.isNull() || r.isBoolean() || r.isString() || !t && r.isNumber() && r.getPattern() !== "")
      return r;
    const n = +r.getValue();
    return R.create(n, "yyyy-MM-dd hh:mm:ss AM/PM");
  }
}
class Fg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 0);
    A(this, "maxParams", 0);
  }
  calculate() {
    const e = Re(/* @__PURE__ */ new Date());
    return R.create(e, Za);
  }
}
class kg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
    A(this, "_returnTypeMap", {
      1: [1, 2, 3, 4, 5, 6, 7],
      // Sunday = 1 ~ Saturday = 7
      2: [7, 1, 2, 3, 4, 5, 6],
      // Monday = 1 ~ Sunday = 7
      3: [6, 0, 1, 2, 3, 4, 5],
      // Monday = 0 ~ Sunday = 6
      11: [7, 1, 2, 3, 4, 5, 6],
      // Monday = 1 ~ Sunday = 7
      12: [6, 7, 1, 2, 3, 4, 5],
      // Tuesday = 1 ~ Monday = 7
      13: [5, 6, 7, 1, 2, 3, 4],
      // Wednesday = 1 ~ Tuesday = 7
      14: [4, 5, 6, 7, 1, 2, 3],
      // Thursday = 1 ~ Wednesday = 7
      15: [3, 4, 5, 6, 7, 1, 2],
      // Friday = 1 ~ Thursday = 7
      16: [2, 3, 4, 5, 6, 7, 1],
      // Saturday = 1 ~ Friday = 7
      17: [1, 2, 3, 4, 5, 6, 7]
      // Sunday = 1 ~ Saturday = 7
    });
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(1);
    if (e.isError())
      return e;
    if (r.isError())
      return r;
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, r, g.create(m.NA)), c = a.map((l, f, h) => {
      const d = u.get(f, h);
      return this._handleSingleObject(l, d);
    });
    return n === 1 && s === 1 ? c.get(0, 0) : c;
  }
  _handleSingleObject(e, t) {
    let r = t;
    if (e.isError())
      return e;
    if (r.isError())
      return r;
    const n = $(e);
    if (typeof n != "number")
      return n;
    if (r.isString() && (r = r.convertToNumberObjectValue(), r.isError()))
      return r;
    const s = Math.floor(+r.getValue());
    if (!this._returnTypeMap[s])
      return g.create(m.NUM);
    const a = ir(n), u = this._returnTypeMap[s][a];
    return R.create(u);
  }
}
class $g extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
    A(this, "_returnTypeMap", {
      1: 0,
      2: 1,
      11: 1,
      12: 2,
      13: 3,
      14: 4,
      15: 5,
      16: 6,
      17: 0,
      21: 4
    });
  }
  calculate(e, t) {
    let r = e, n = t != null ? t : R.create(1);
    if (r.isArray()) {
      const u = r.getRowCount(), c = r.getColumnCount();
      if (u > 1 || c > 1)
        return g.create(m.VALUE);
      r = r.get(0, 0);
    }
    if (r.isError())
      return r;
    if (n.isArray()) {
      const u = n.getRowCount(), c = n.getColumnCount();
      if (u > 1 || c > 1)
        return g.create(m.VALUE);
      n = n.get(0, 0);
    }
    if (n.isError())
      return n;
    if (r.isBoolean())
      return g.create(m.VALUE);
    const s = $(r);
    if (typeof s != "number")
      return s;
    if (n.isBoolean())
      return g.create(m.VALUE);
    const a = Math.floor(+n.getValue());
    return Number.isNaN(a) ? g.create(m.VALUE) : a in this._returnTypeMap ? this._getResult(s, a) : g.create(m.NUM);
  }
  _getResult(e, t) {
    const r = re(e), n = e > 0 ? r.getUTCFullYear() : 1900;
    let s = new Date(Date.UTC(n, 0, 1)), a = Re(s), u = ir(a), c;
    if (t === 21)
      u < 1 ? c = a + 1 : u <= 4 ? c = a - (u - 1) : c = a + (11 - u), e < c && (s = new Date(Date.UTC(n - 1, 0, 1)), a = Re(s), u = ir(a), u < 1 ? c = a + 1 : u <= 4 ? c = a - (u - 1) : c = a + (11 - u));
    else {
      const f = this._returnTypeMap[t];
      u < f ? c = a - (u + 7 - f) : c = a - (u - f);
    }
    const l = Math.ceil((e - c + 1) / 7);
    return R.create(l);
  }
}
class Yg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    let n = e, s = t;
    if (n.isArray()) {
      const l = n.getRowCount(), f = n.getColumnCount();
      if (l > 1 || f > 1)
        return g.create(m.VALUE);
      n = n.get(0, 0);
    }
    if (n.isError())
      return n;
    if (s.isArray()) {
      const l = s.getRowCount(), f = s.getColumnCount();
      if (l > 1 || f > 1)
        return g.create(m.VALUE);
      s = s.get(0, 0);
    }
    if (s.isError())
      return s;
    if (r != null && r.isError())
      return r;
    if (n.isBoolean() || s.isBoolean())
      return g.create(m.VALUE);
    const a = $(n);
    if (typeof a != "number")
      return a;
    const u = +s.getValue();
    if (Number.isNaN(u))
      return g.create(m.VALUE);
    if (r)
      return this._getResultByHolidays(a, u, r);
    const c = In(a, u);
    return typeof c != "number" ? c : R.create(c);
  }
  _getResultByHolidays(e, t, r) {
    const n = [];
    if (r != null && r.isArray()) {
      const a = r.getRowCount(), u = r.getColumnCount();
      for (let c = 0; c < a; c++)
        for (let l = 0; l < u; l++) {
          const f = r.get(c, l);
          if (f.isBoolean())
            return g.create(m.VALUE);
          const h = $(f);
          if (typeof h != "number")
            return h;
          n.push(h);
        }
    } else {
      if (r.isBoolean())
        return g.create(m.VALUE);
      const a = $(r);
      if (typeof a != "number")
        return a;
      n.push(a);
    }
    const s = In(e, t, 1, n);
    return typeof s != "number" ? s : R.create(s);
  }
}
class Hg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    return e.isError() ? e : t.isError() ? t : r != null && r.isError() ? r : n != null && n.isError() ? n : r != null && r.isArray() ? r.map((s) => this._handleSingleObject(e, t, s, n)) : this._handleSingleObject(e, t, r, n);
  }
  _handleSingleObject(e, t, r, n) {
    const s = r != null ? r : R.create(1), a = this._checkArrayError(e);
    if (a.isError())
      return a;
    const u = this._checkArrayError(t);
    if (u.isError())
      return u;
    if (a.isBoolean() || u.isBoolean())
      return g.create(m.VALUE);
    const c = $(e);
    if (typeof c != "number")
      return c;
    const l = +t.getValue();
    if (Number.isNaN(l))
      return g.create(m.VALUE);
    let f = s.getValue();
    if (s.isBoolean() && (f = +f), s.isString() && (!$r(f) || f === "1111111"))
      return g.create(m.VALUE);
    if (!$r(f))
      return g.create(m.NUM);
    if (n)
      return this._getResultByHolidays(c, l, f, n);
    const h = In(c, l, f);
    return typeof h != "number" ? h : R.create(h);
  }
  _checkArrayError(e) {
    let t = e;
    if (t.isArray()) {
      const r = t.getRowCount(), n = t.getColumnCount();
      if (r > 1 || n > 1)
        return g.create(m.VALUE);
      t = t.get(0, 0);
    }
    return t.isError(), t;
  }
  _getResultByHolidays(e, t, r, n) {
    const s = [];
    if (n != null && n.isArray()) {
      const u = n.getRowCount(), c = n.getColumnCount();
      for (let l = 0; l < u; l++)
        for (let f = 0; f < c; f++) {
          const h = n.get(l, f);
          if (h.isBoolean())
            return g.create(m.VALUE);
          const d = $(h);
          if (typeof d != "number")
            return d;
          s.push(d);
        }
    } else {
      if (n.isBoolean())
        return g.create(m.VALUE);
      const u = $(n);
      if (typeof u != "number")
        return u;
      s.push(u);
    }
    const a = In(e, t, r, s);
    return typeof a != "number" ? a : R.create(a);
  }
}
class Qg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    let t;
    const r = e.getValue();
    if (e.isString()) {
      if (!Ja(`${r}`))
        return g.create(m.VALUE);
      t = /* @__PURE__ */ new Date(`${r}`);
    } else {
      const a = +r;
      if (a < 0)
        return g.create(m.NUM);
      if (a === 0)
        return R.create(1900);
      t = re(a);
    }
    const n = t.getUTCFullYear();
    return R.create(n);
  }
}
class Gg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    let n = r != null ? r : R.create(0);
    const s = tr(e);
    if (s.isError())
      return s;
    const a = tr(t);
    if (a.isError())
      return a;
    if (n = tr(n), n.isError())
      return n;
    if (s.isBoolean() || a.isBoolean() || n.isBoolean())
      return g.create(m.VALUE);
    const u = $(s);
    if (typeof u != "number")
      return u;
    const c = $(a);
    if (typeof c != "number")
      return c;
    const l = Math.floor(+n.getValue());
    if (Number.isNaN(l))
      return g.create(m.VALUE);
    if (l < 0 || l > 4)
      return g.create(m.NUM);
    const { days: f, yearDays: h } = _e(u, c, l), d = f / h;
    return R.create(d);
  }
}
const Wg = [
  [bg, be.DATE],
  [yg, be.DATEDIF],
  [Rg, be.DATEVALUE],
  [pg, be.DAY],
  [Ng, be.DAYS],
  [Vg, be.DAYS360],
  [Og, be.EDATE],
  [wg, be.EOMONTH],
  [Sg, be.EPOCHTODATE],
  [Mg, be.HOUR],
  [Dg, be.ISOWEEKNUM],
  [Lg, be.MINUTE],
  [xg, be.MONTH],
  [Pg, be.NETWORKDAYS],
  [jg, be.NETWORKDAYS_INTL],
  [Ug, be.NOW],
  [vg, be.SECOND],
  [Tg, be.TIME],
  [Bg, be.TIMEVALUE],
  [Ig, be.TO_DATE],
  [Fg, be.TODAY],
  [kg, be.WEEKDAY],
  [$g, be.WEEKNUM],
  [Yg, be.WORKDAY],
  [Hg, be.WORKDAY_INTL],
  [Qg, be.YEAR],
  [Gg, be.YEARFRAC]
];
class qg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull() || t.isNull())
      return g.create(m.NA);
    const { isError: r, errorObject: n, variants: s } = H(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = Math.floor(+u.getValue());
    if (Number.isNaN(c) || Number.isNaN(l))
      return g.create(m.VALUE);
    if (l < 0)
      return g.create(m.NUM);
    const f = Nr.besseli(c, l);
    return Number.isNaN(f) || !Number.isFinite(f) ? g.create(m.NUM) : R.create(f);
  }
}
class Kg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull() || t.isNull())
      return g.create(m.NA);
    const { isError: r, errorObject: n, variants: s } = H(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = Math.floor(+u.getValue());
    if (Number.isNaN(c) || Number.isNaN(l))
      return g.create(m.VALUE);
    if (l < 0)
      return g.create(m.NUM);
    const f = Nr.besselj(c, l);
    return Number.isNaN(f) || !Number.isFinite(f) ? g.create(m.NUM) : R.create(f);
  }
}
class Xg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull() || t.isNull())
      return g.create(m.NA);
    const { isError: r, errorObject: n, variants: s } = H(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = Math.floor(+u.getValue());
    if (Number.isNaN(c) || Number.isNaN(l))
      return g.create(m.VALUE);
    if (l < 0)
      return g.create(m.NUM);
    const f = Nr.besselk(c, l);
    return Number.isNaN(f) || !Number.isFinite(f) ? g.create(m.NUM) : R.create(f);
  }
}
class Zg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull() || t.isNull())
      return g.create(m.NA);
    const { isError: r, errorObject: n, variants: s } = H(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = Math.floor(+u.getValue());
    if (Number.isNaN(c) || Number.isNaN(l))
      return g.create(m.VALUE);
    if (l < 0)
      return g.create(m.NUM);
    const f = Nr.bessely(c, l);
    return Number.isNaN(f) || !Number.isFinite(f) ? g.create(m.NUM) : R.create(f);
  }
}
class zg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`;
    if (!Ta(a))
      return g.create(m.NUM);
    let u;
    return a.length === 10 && a.substring(0, 1) === "1" ? u = Number.parseInt(a.substring(1), 2) - 512 : u = Number.parseInt(a, 2), R.create(u);
  }
}
class Jg extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull())
      return g.create(m.NA);
    let r = 0;
    if (t) {
      const { isError: f, errorObject: h, variants: d } = H(t);
      if (f)
        return h;
      const [_] = d;
      if (r = Math.floor(+_.getValue()), Number.isNaN(r))
        return g.create(m.VALUE);
      if (r < 0 || r > 10)
        return g.create(m.NUM);
    }
    const { isError: n, errorObject: s, variants: a } = H(e);
    if (n)
      return s;
    const [u] = a, c = `${u.getValue()}`;
    if (!Ta(c))
      return g.create(m.NUM);
    let l;
    if (c.length === 10 && c.substring(0, 1) === "1")
      l = (1099511627264 + Number.parseInt(c.substring(1), 2)).toString(16);
    else if (l = Number.parseInt(c, 2).toString(16), t) {
      if (r < l.length)
        return g.create(m.NUM);
      l = "0".repeat(r - l.length) + l;
    }
    return U.create(l.toLocaleUpperCase());
  }
}
class e1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull())
      return g.create(m.NA);
    let r = 0;
    if (t) {
      const { isError: f, errorObject: h, variants: d } = H(t);
      if (f)
        return h;
      const [_] = d;
      if (r = Math.floor(+_.getValue()), Number.isNaN(r))
        return g.create(m.VALUE);
      if (r < 0 || r > 10)
        return g.create(m.NUM);
    }
    const { isError: n, errorObject: s, variants: a } = H(e);
    if (n)
      return s;
    const [u] = a, c = `${u.getValue()}`;
    if (!Ta(c))
      return g.create(m.NUM);
    let l;
    if (c.length === 10 && c.substring(0, 1) === "1")
      l = (1073741312 + Number.parseInt(c.substring(1), 2)).toString(8);
    else if (l = Number.parseInt(c, 2).toString(8), t) {
      if (r < l.length)
        return g.create(m.NUM);
      l = "0".repeat(r - l.length) + l;
    }
    return U.create(l);
  }
}
class t1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.map((c, l, f) => {
      let h = c;
      if (h.isString() && (h = h.convertToNumberObjectValue()), h.isError())
        return h;
      let d = a.get(l, f);
      if (d.isString() && (d = d.convertToNumberObjectValue()), d.isError())
        return d;
      const _ = +h.getValue(), C = +d.getValue();
      if (_ < 0 || C < 0 || Math.floor(_) !== _ || Math.floor(C) !== C || _ > 281474976710655 || C > 281474976710655)
        return g.create(m.NUM);
      const E = _ & C;
      return R.create(E);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
}
class r1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.map((c, l, f) => {
      let h = c;
      if (h.isString() && (h = h.convertToNumberObjectValue()), h.isError())
        return h;
      let d = a.get(l, f);
      if (d.isString() && (d = d.convertToNumberObjectValue()), d.isError())
        return d;
      const _ = +h.getValue();
      let C = +d.getValue();
      if (_ < 0 || Math.floor(_) !== _ || _ > 281474976710655 || Math.abs(C) > 53)
        return g.create(m.NUM);
      C = Math.trunc(C);
      const E = Number(C >= 0 ? BigInt(_) << BigInt(C) : BigInt(_) >> BigInt(-C));
      return E > 281474976710655 ? g.create(m.NUM) : R.create(E);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
}
class n1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.map((c, l, f) => {
      let h = c;
      if (h.isString() && (h = h.convertToNumberObjectValue()), h.isError())
        return h;
      let d = a.get(l, f);
      if (d.isString() && (d = d.convertToNumberObjectValue()), d.isError())
        return d;
      const _ = +h.getValue(), C = +d.getValue();
      if (_ < 0 || C < 0 || Math.floor(_) !== _ || Math.floor(C) !== C || _ > 281474976710655 || C > 281474976710655)
        return g.create(m.NUM);
      const E = Number(BigInt(_) | BigInt(C));
      return R.create(E);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
}
class s1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.map((c, l, f) => {
      let h = c;
      if (h.isString() && (h = h.convertToNumberObjectValue()), h.isError())
        return h;
      let d = a.get(l, f);
      if (d.isString() && (d = d.convertToNumberObjectValue()), d.isError())
        return d;
      const _ = +h.getValue();
      let C = +d.getValue();
      if (_ < 0 || Math.floor(_) !== _ || _ > 281474976710655 || Math.abs(C) > 53)
        return g.create(m.NUM);
      C = Math.trunc(C);
      const E = Number(C >= 0 ? BigInt(_) >> BigInt(C) : BigInt(_) << BigInt(-C));
      return E > 281474976710655 ? g.create(m.NUM) : R.create(E);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
}
class a1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.map((c, l, f) => {
      let h = c;
      if (h.isString() && (h = h.convertToNumberObjectValue()), h.isError())
        return h;
      let d = a.get(l, f);
      if (d.isString() && (d = d.convertToNumberObjectValue()), d.isError())
        return d;
      const _ = +h.getValue(), C = +d.getValue();
      if (_ < 0 || C < 0 || Math.floor(_) !== _ || Math.floor(C) !== C || _ > 281474976710655 || C > 281474976710655)
        return g.create(m.NUM);
      const E = _ ^ C;
      return R.create(E);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
}
P.prototype.cos = function() {
  const o = this.toNumber();
  return new P(Math.cos(o));
};
let he = class ce {
  constructor(i) {
    A(this, "_inumber", "");
    A(this, "_realNum", 0);
    A(this, "_iNum", 0);
    A(this, "_suffix", "");
    A(this, "_isError", !1);
    if (`${i}`.trim() === "") {
      this._isError = !0;
      return;
    }
    this._inumber = i, this._getImReal(), this._getImAginary(), this._getImSuffix();
  }
  static getComplex(i, e, t) {
    const r = new P(i).toSignificantDigits(15).toNumber(), n = new P(e).toSignificantDigits(15).toNumber(), s = t === "" ? "i" : t;
    let a;
    if (r === 0 && n === 0)
      a = 0;
    else if (r === 0)
      a = n === 1 ? s : `${n}${s}`;
    else if (n === 0)
      a = r;
    else {
      const u = n > 0 ? "+" : "", c = n === 1 ? s : `${n}${s}`;
      a = `${r}${u}${c}`;
    }
    return a;
  }
  static createByComplexStr(i, e, t) {
    const r = ce.getComplex(i, e, t);
    return new ce(r);
  }
  _getImReal() {
    if (this._inumber === 0 || this._inumber === "0") {
      this._realNum = 0;
      return;
    }
    const i = `${this._inumber}`;
    if (["i", "+i", "1i", "+1i", "-i", "-1i", "j", "+j", "1j", "+1j", "-j", "-1j"].indexOf(i) >= 0) {
      this._realNum = 0;
      return;
    }
    let e = i.indexOf("+"), t = i.indexOf("-");
    e === 0 && (e = i.indexOf("+", 1)), t === 0 && (t = i.indexOf("-", 1));
    const r = i.substring(i.length - 1, i.length), n = r === "i" || r === "j";
    if (e >= 0 || t >= 0) {
      if (!n) {
        this._isError = !0;
        return;
      }
      e >= 0 ? Number.isNaN(+i.substring(0, e)) || Number.isNaN(+i.substring(e + 1, i.length - 1)) ? this._isError = !0 : this._realNum = +i.substring(0, e) : Number.isNaN(+i.substring(0, t)) || Number.isNaN(+i.substring(t + 1, i.length - 1)) ? this._isError = !0 : this._realNum = +i.substring(0, t);
    } else
      n ? Number.isNaN(+i.substring(0, i.length - 1)) ? this._isError = !0 : this._realNum = 0 : Number.isNaN(+i) ? this._isError = !0 : this._realNum = +i;
  }
  _getImAginary() {
    if (this._isError)
      return;
    if (this._inumber === 0 || this._inumber === "0") {
      this._iNum = 0;
      return;
    }
    let i = `${this._inumber}`;
    if (["i", "j"].indexOf(i) >= 0) {
      this._iNum = 1;
      return;
    }
    i = i.replace("+i", "+1i").replace("-i", "-1i").replace("+j", "+1j").replace("-j", "-1j");
    let e = i.indexOf("+"), t = i.indexOf("-");
    e === 0 && (e = i.indexOf("+", 1)), t === 0 && (t = i.indexOf("-", 1));
    const r = i.substring(i.length - 1, i.length), n = r === "i" || r === "j";
    if (e >= 0 || t >= 0) {
      if (!n) {
        this._isError = !0;
        return;
      }
      e >= 0 ? Number.isNaN(+i.substring(0, e)) || Number.isNaN(+i.substring(e + 1, i.length - 1)) ? this._isError = !0 : this._iNum = +i.substring(e + 1, i.length - 1) : Number.isNaN(+i.substring(0, t)) || Number.isNaN(+i.substring(t + 1, i.length - 1)) ? this._isError = !0 : this._iNum = -+i.substring(t + 1, i.length - 1);
    } else
      n ? Number.isNaN(+i.substring(0, i.length - 1)) ? this._isError = !0 : this._iNum = +i.substring(0, i.length - 1) : Number.isNaN(+i) ? this._isError = !0 : this._iNum = 0;
  }
  _getImSuffix() {
    const i = `${this._inumber}`, e = i.substring(i.length - 1);
    this._suffix = e === "i" || e === "j" ? e : "";
  }
  getRealNum() {
    return this._realNum;
  }
  getINum() {
    return this._iNum;
  }
  getSuffix() {
    return this._suffix;
  }
  isError() {
    return this._isError;
  }
  toString() {
    return ce.getComplex(this._realNum, this._iNum, this._suffix);
  }
  isDifferentSuffixes(i) {
    const e = i.getSuffix();
    return this._suffix === "" || e === "" ? !1 : this._suffix !== e;
  }
  Abs() {
    return P.sqrt(P.pow(this._realNum, 2).add(P.pow(this._iNum, 2))).toSignificantDigits(16).toNumber();
  }
  Argument() {
    const i = P.sqrt(P.pow(this._realNum, 2).add(P.pow(this._iNum, 2)));
    let e = P.acos(new P(this._realNum).div(i)).toSignificantDigits(16).toNumber();
    return this._iNum < 0 && (e = -e), e;
  }
  Conjugate() {
    return ce.getComplex(this._realNum, -this._iNum, this._suffix);
  }
  Cos() {
    if (this._iNum) {
      const i = P.cos(this._realNum).mul(P.cosh(this._iNum)).toNumber(), e = P.sin(this._realNum).mul(P.sinh(this._iNum)).negated().toNumber();
      return ce.getComplex(i, e, this._suffix);
    } else {
      const i = P.cos(this._realNum).toNumber();
      return ce.getComplex(i, this._iNum, this._suffix);
    }
  }
  Cosh() {
    if (!Number.isFinite(Math.sinh(this._realNum)) || !Number.isFinite(Math.cosh(this._realNum)))
      return this._isError = !0, "";
    if (this._iNum) {
      const i = P.cosh(this._realNum).mul(P.cos(this._iNum)).toNumber(), e = P.sinh(this._realNum).mul(P.sin(this._iNum)).toNumber();
      return ce.getComplex(i, e, this._suffix);
    } else {
      const i = P.cosh(this._realNum).toNumber();
      return ce.getComplex(i, this._iNum, this._suffix);
    }
  }
  Cot() {
    if (this._iNum) {
      const i = P.cosh(this._iNum * 2).sub(P.cos(this._realNum * 2)), e = P.sin(this._realNum * 2).div(i).toNumber(), t = P.sinh(this._iNum * 2).div(i).negated().toNumber();
      return ce.getComplex(e, t, this._suffix);
    } else {
      const i = new P(1).div(P.tan(this._realNum)).toNumber();
      return ce.getComplex(i, this._iNum, this._suffix);
    }
  }
  Coth() {
    if (!Number.isFinite(Math.sinh(this._realNum)) || !Number.isFinite(Math.cosh(this._realNum)))
      return this._isError = !0, "";
    if (this._iNum) {
      const i = P.cosh(this._realNum * 2).sub(P.cos(this._iNum * 2)), e = P.sinh(this._realNum * 2).div(i).toNumber(), t = P.sin(this._iNum * 2).div(i).negated().toNumber();
      return ce.getComplex(e, t, this._suffix);
    } else {
      const i = new P(1).div(P.tanh(this._realNum)).toNumber();
      return ce.getComplex(i, this._iNum, this._suffix);
    }
  }
  Csc() {
    if (this._iNum) {
      const i = P.cosh(this._iNum * 2).sub(P.cos(this._realNum * 2)), e = P.sin(this._realNum).mul(P.cosh(this._iNum)).mul(2).div(i).toNumber(), t = P.cos(this._realNum).mul(P.sinh(this._iNum)).mul(-2).div(i).toNumber();
      return ce.getComplex(e, t, this._suffix);
    } else {
      const i = new P(1).div(P.sin(this._realNum)).toNumber();
      return ce.getComplex(i, this._iNum, this._suffix);
    }
  }
  Csch() {
    if (!Number.isFinite(Math.sinh(this._realNum)) || !Number.isFinite(Math.cosh(this._realNum)))
      return ce.getComplex(0, 0, this._suffix);
    if (this._iNum) {
      const i = P.cosh(this._realNum * 2).sub(P.cos(this._iNum * 2)), e = P.sinh(this._realNum).mul(P.cos(this._iNum)).mul(2).div(i).toNumber(), t = P.cosh(this._realNum).mul(P.sin(this._iNum)).mul(-2).div(i).toNumber();
      return ce.getComplex(e, t, this._suffix);
    } else {
      const i = new P(1).div(P.sinh(this._realNum)).toNumber();
      return ce.getComplex(i, this._iNum, this._suffix);
    }
  }
  Div(i) {
    const e = new P(this._realNum), t = new P(this._iNum), r = new P(i.getRealNum()), n = new P(i.getINum()), s = r.mul(r).add(n.mul(n)), a = e.mul(r).add(t.mul(n)).div(s).toNumber(), u = t.mul(r).sub(e.mul(n)).div(s).toNumber(), c = this._suffix === "" ? i.getSuffix() : this._suffix;
    return ce.getComplex(a, u, c);
  }
  Exp() {
    if (!Number.isFinite(Math.exp(this._realNum)))
      return this._isError = !0, "";
    const i = P.exp(this._realNum).mul(P.cos(this._iNum)).toNumber(), e = P.exp(this._realNum).mul(P.sin(this._iNum)).toNumber();
    return ce.getComplex(i, e, this._suffix);
  }
  Ln() {
    const i = P.sqrt(P.pow(this._realNum, 2).add(P.pow(this._iNum, 2))), e = P.ln(i).toNumber(), t = P.acos(new P(this._realNum).div(i)).toNumber();
    return ce.getComplex(e, t, this._suffix);
  }
  Log(i) {
    const e = P.sqrt(P.pow(this._realNum, 2).add(P.pow(this._iNum, 2))), t = P.ln(e);
    let r = P.acos(new P(this._realNum).div(e));
    this._iNum < 0 && (r = r.negated());
    const n = P.ln(i), s = new P(0), a = n.mul(n).add(s.mul(s));
    if (a.eq(0))
      return this._isError = !0, "";
    const u = t.mul(n).add(r.mul(s)).div(a).toNumber(), c = r.mul(n).sub(t.mul(s)).div(a).toNumber();
    return ce.getComplex(u, c, this._suffix);
  }
  Power(i) {
    if (this._realNum === 0 && this._iNum === 0)
      return i > 0 ? ce.getComplex(this._realNum, this._iNum, this._suffix) : (this._isError = !0, "");
    let e = P.sqrt(P.pow(this._realNum, 2).add(P.pow(this._iNum, 2))), t = P.acos(new P(this._realNum).div(e));
    this._iNum < 0 && (t = t.negated()), e = P.pow(e, i), t = t.mul(i);
    const r = P.cos(t).mul(e).toNumber(), n = P.sin(t).mul(e).toNumber();
    return !Number.isFinite(r) || !Number.isFinite(n) ? (this._isError = !0, "") : ce.getComplex(r, n, this._suffix);
  }
  Product(i) {
    const e = new P(this._realNum), t = new P(this._iNum), r = new P(i.getRealNum()), n = new P(i.getINum()), s = e.mul(r).sub(t.mul(n)).toNumber(), a = e.mul(n).add(t.mul(r)).toNumber(), u = this._suffix === "" ? i.getSuffix() : this._suffix;
    return ce.getComplex(s, a, u);
  }
  Sec() {
    if (this._iNum) {
      const i = P.cosh(this._iNum * 2).add(P.cos(this._realNum * 2)), e = P.cos(this._realNum).mul(P.cosh(this._iNum)).mul(2).div(i).toNumber(), t = P.sin(this._realNum).mul(P.sinh(this._iNum)).mul(2).div(i).toNumber();
      return ce.getComplex(e, t, this._suffix);
    } else {
      const i = new P(1).div(P.cos(this._realNum)).toNumber();
      return ce.getComplex(i, this._iNum, this._suffix);
    }
  }
  Sech() {
    if (!Number.isFinite(Math.sinh(this._realNum * 2)) || !Number.isFinite(Math.cosh(this._realNum * 2)))
      return ce.getComplex(0, 0, this._suffix);
    if (this._iNum) {
      const i = P.cosh(this._realNum * 2).add(P.cos(this._iNum * 2)), e = P.cosh(this._realNum).mul(P.cos(this._iNum)).mul(2).div(i).toNumber(), t = P.sinh(this._realNum).mul(P.sin(this._iNum)).mul(-2).div(i).toNumber();
      return ce.getComplex(e, t, this._suffix);
    } else {
      const i = new P(1).div(P.cosh(this._realNum)).toNumber();
      return ce.getComplex(i, this._iNum, this._suffix);
    }
  }
  Sin() {
    if (this._iNum) {
      const i = P.sin(this._realNum).mul(P.cosh(this._iNum)).toNumber(), e = P.cos(this._realNum).mul(P.sinh(this._iNum)).toNumber();
      return ce.getComplex(i, e, this._suffix);
    } else {
      const i = P.sin(this._realNum).toNumber();
      return ce.getComplex(i, this._iNum, this._suffix);
    }
  }
  Sinh() {
    if (!Number.isFinite(Math.sinh(this._realNum)) || !Number.isFinite(Math.cosh(this._realNum)))
      return this._isError = !0, "";
    if (this._iNum) {
      const i = P.sinh(this._realNum).mul(P.cos(this._iNum)).toNumber(), e = P.cosh(this._realNum).mul(P.sin(this._iNum)).toNumber();
      return ce.getComplex(i, e, this._suffix);
    } else {
      const i = P.sinh(this._realNum).toNumber();
      return ce.getComplex(i, this._iNum, this._suffix);
    }
  }
  Sqrt() {
    const i = P.sqrt(P.pow(this._realNum, 2).add(P.pow(this._iNum, 2))), e = P.sqrt(i);
    let t = P.acos(new P(this._realNum).div(i));
    this._iNum < 0 && (t = t.negated());
    const r = e.mul(P.cos(t.div(2).toNumber())).toNumber(), n = e.mul(P.sin(t.div(2))).toNumber();
    return ce.getComplex(r, n, this._suffix);
  }
  Sub(i) {
    const e = new P(this._realNum), t = new P(this._iNum), r = new P(i.getRealNum()), n = new P(i.getINum()), s = e.sub(r).toNumber(), a = t.sub(n).toNumber(), u = this._suffix === "" ? i.getSuffix() : this._suffix;
    return ce.getComplex(s, a, u);
  }
  Sum(i) {
    const e = new P(this._realNum), t = new P(this._iNum), r = new P(i.getRealNum()), n = new P(i.getINum()), s = e.add(r).toNumber(), a = t.add(n).toNumber(), u = this._suffix === "" ? i.getSuffix() : this._suffix;
    return ce.getComplex(s, a, u);
  }
  Tan() {
    if (this._iNum) {
      const i = P.cos(this._realNum * 2).add(P.cosh(this._iNum * 2)), e = P.sin(this._realNum * 2).div(i).toNumber(), t = P.sinh(this._iNum * 2).div(i).toNumber();
      return ce.getComplex(e, t, this._suffix);
    } else {
      const i = P.tan(this._realNum).toNumber();
      return ce.getComplex(i, this._iNum, this._suffix);
    }
  }
  Tanh() {
    if (!Number.isFinite(Math.sinh(this._realNum)) || !Number.isFinite(Math.cosh(this._realNum)))
      return this._isError = !0, "";
    if (this._iNum) {
      const i = P.cosh(this._realNum * 2).add(P.cos(this._iNum * 2)), e = P.sinh(this._realNum * 2).div(i).toNumber(), t = P.sin(this._iNum * 2).div(i).toNumber();
      return ce.getComplex(e, t, this._suffix);
    } else {
      const i = P.tanh(this._realNum).toNumber();
      return ce.getComplex(i, this._iNum, this._suffix);
    }
  }
};
class i1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = r != null ? r : U.create("i"), { isError: s, errorObject: a, variants: u } = H(e, t, n);
    if (s)
      return a;
    const [c, l, f] = u, h = +c.getValue(), d = +l.getValue(), _ = `${f.getValue()}`;
    if (Number.isNaN(h) || Number.isNaN(d) || _ !== "i" && _ !== "j")
      return g.create(m.VALUE);
    const C = he.getComplex(h, d, _);
    return typeof C == "number" ? R.create(C) : U.create(C);
  }
}
class o1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
    // List of units supported by CONVERT and units defined by the International System of Units
    // [Name, Symbol, Alternate symbols, Quantity, ISU, CONVERT, Conversion ratio]
    A(this, "_units", [
      ["a.u. of action", "?", null, "action", !1, !1, 105457168181818e-48],
      ["a.u. of charge", "e", null, "electric_charge", !1, !1, 160217653141414e-33],
      ["a.u. of energy", "Eh", null, "energy", !1, !1, 435974417757576e-32],
      ["a.u. of length", "a?", null, "length", !1, !1, 529177210818182e-25],
      ["a.u. of mass", "m?", null, "mass", !1, !1, 910938261616162e-45],
      ["a.u. of time", "?/Eh", null, "time", !1, !1, 241888432650516e-31],
      ["admiralty knot", "admkn", null, "speed", !1, !0, 0.514773333],
      ["ampere", "A", null, "electric_current", !0, !1, 1],
      ["ampere per meter", "A/m", null, "magnetic_field_intensity", !0, !1, 1],
      ["ångström", "Å", ["ang"], "length", !1, !0, 1e-10],
      ["are", "ar", null, "area", !1, !0, 100],
      ["astronomical unit", "ua", null, "length", !1, !1, 149597870691667e-25],
      ["bar", "bar", null, "pressure", !1, !1, 1e5],
      ["barn", "b", null, "area", !1, !1, 1e-28],
      ["becquerel", "Bq", null, "radioactivity", !0, !1, 1],
      ["bit", "bit", ["b"], "information", !1, !0, 1],
      ["btu", "BTU", ["btu"], "energy", !1, !0, 1055.05585262],
      ["byte", "byte", null, "information", !1, !0, 8],
      ["candela", "cd", null, "luminous_intensity", !0, !1, 1],
      ["candela per square metre", "cd/m?", null, "luminance", !0, !1, 1],
      ["centigrade", "C", ["cel"], "temperature", !0, !1, 1],
      ["cubic ångström", "ang3", ["ang^3"], "volume", !1, !0, 1e-30],
      ["cubic foot", "ft3", ["ft^3"], "volume", !1, !0, 0.028316846592],
      ["cubic inch", "in3", ["in^3"], "volume", !1, !0, 16387064e-12],
      ["cubic light-year", "ly3", ["ly^3"], "volume", !1, !0, 846786664623715e-61],
      ["cubic metre", "m3", ["m^3"], "volume", !0, !0, 1],
      ["cubic mile", "mi3", ["mi^3"], "volume", !1, !0, 416818182544058e-5],
      ["cubic nautical mile", "Nmi3", ["Nmi^3"], "volume", !1, !0, 6352182208],
      ["cubic Pica", "Pica3", ["Picapt3", "Pica^3", "Picapt^3"], "volume", !1, !0, 758660370370369e-22],
      ["cubic yard", "yd3", ["yd^3"], "volume", !1, !0, 0.764554857984],
      ["cup", "cup", null, "volume", !1, !0, 2365882365e-13],
      ["dalton", "Da", ["u"], "mass", !1, !1, 166053886282828e-41],
      ["day", "d", ["day"], "time", !1, !0, 86400],
      ["degree", "°", null, "angle", !1, !1, 0.0174532925199433],
      ["dyne", "dyn", ["dy"], "force", !1, !0, 1e-5],
      ["electronvolt", "eV", ["ev"], "energy", !1, !0, 1.60217656514141],
      ["ell", "ell", null, "length", !1, !0, 1.143],
      ["erg", "erg", ["e"], "energy", !1, !0, 1e-7],
      ["fahrenheit", "F", ["fah"], "temperature", !0, !1, 1],
      ["fluid ounce", "oz", null, "volume", !1, !0, 295735295625e-16],
      ["foot", "ft", null, "length", !1, !0, 0.3048],
      ["foot-pound", "flb", null, "energy", !1, !0, 1.3558179483314],
      ["gal", "Gal", null, "acceleration", !1, !1, 0.01],
      ["gallon", "gal", null, "volume", !1, !0, 0.003785411784],
      ["gauss", "G", ["ga"], "magnetic_flux_density", !1, !0, 1],
      ["grain", "grain", null, "mass", !1, !0, 647989e-10],
      ["gram", "g", null, "mass", !1, !0, 1e-3],
      ["gray", "Gy", null, "absorbed_dose", !0, !1, 1],
      ["gross registered ton", "GRT", ["regton"], "volume", !1, !0, 2.8316846592],
      ["hectare", "ha", null, "area", !1, !0, 1e4],
      ["henry", "H", null, "inductance", !0, !1, 1],
      ["hertz", "Hz", null, "frequency", !0, !1, 1],
      ["horsepower", "HP", ["h"], "power", !1, !0, 745.69987158227],
      ["horsepower-hour", "HPh", ["hh", "hph"], "energy", !1, !0, 2684519538e-3],
      ["hour", "h", ["hr"], "time", !1, !0, 3600],
      ["imperial gallon (U.K.)", "uk_gal", null, "volume", !1, !0, 454609e-8],
      ["imperial hundredweight", "lcwt", ["uk_cwt", "hweight"], "mass", !1, !0, 50.802345],
      ["imperial quart (U.K)", "uk_qt", null, "volume", !1, !0, 0.0011365225],
      ["imperial ton", "brton", ["uk_ton", "LTON"], "mass", !1, !0, 1016.046909],
      ["inch", "in", null, "length", !1, !0, 0.0254],
      ["international acre", "uk_acre", null, "area", !1, !0, 4046.8564224],
      ["IT calorie", "cal", null, "energy", !1, !0, 4.1868],
      ["joule", "J", null, "energy", !0, !0, 1],
      ["katal", "kat", null, "catalytic_activity", !0, !1, 1],
      ["kelvin", "K", ["kel"], "temperature", !0, !0, 1],
      ["kilogram", "kg", null, "mass", !0, !0, 1],
      ["knot", "kn", null, "speed", !1, !0, 0.514444444444444],
      ["light-year", "ly", null, "length", !1, !0, 9460730472580800],
      ["litre", "L", ["l", "lt"], "volume", !1, !0, 1e-3],
      ["lumen", "lm", null, "luminous_flux", !0, !1, 1],
      ["lux", "lx", null, "illuminance", !0, !1, 1],
      ["maxwell", "Mx", null, "magnetic_flux", !1, !1, 1e-18],
      ["measurement ton", "MTON", null, "volume", !1, !0, 1.13267386368],
      ["meter per hour", "m/h", ["m/hr"], "speed", !1, !0, 27777777777778e-17],
      ["meter per second", "m/s", ["m/sec"], "speed", !0, !0, 1],
      ["meter per second squared", "m?s??", null, "acceleration", !0, !1, 1],
      ["parsec", "pc", ["parsec"], "length", !1, !0, 30856775814671900],
      ["meter squared per second", "m?/s", null, "kinematic_viscosity", !0, !1, 1],
      ["metre", "m", null, "length", !0, !0, 1],
      ["miles per hour", "mph", null, "speed", !1, !0, 0.44704],
      ["millimetre of mercury", "mmHg", null, "pressure", !1, !1, 133.322],
      ["minute", "?", null, "angle", !1, !1, 290888208665722e-18],
      ["minute", "min", ["mn"], "time", !1, !0, 60],
      ["modern teaspoon", "tspm", null, "volume", !1, !0, 5e-6],
      ["mole", "mol", null, "amount_of_substance", !0, !1, 1],
      ["morgen", "Morgen", null, "area", !1, !0, 2500],
      ["n.u. of action", "?", null, "action", !1, !1, 105457168181818e-48],
      ["n.u. of mass", "m?", null, "mass", !1, !1, 910938261616162e-45],
      ["n.u. of speed", "c?", null, "speed", !1, !1, 299792458],
      ["n.u. of time", "?/(me?c??)", null, "time", !1, !1, 128808866778687e-35],
      ["nautical mile", "M", ["Nmi"], "length", !1, !0, 1852],
      ["newton", "N", null, "force", !0, !0, 1],
      ["œrsted", "Oe ", null, "magnetic_field_intensity", !1, !1, 79.5774715459477],
      ["ohm", "Ω", null, "electric_resistance", !0, !1, 1],
      ["ounce mass", "ozm", null, "mass", !1, !0, 0.028349523125],
      ["pascal", "Pa", null, "pressure", !0, !1, 1],
      ["pascal second", "Pa?s", null, "dynamic_viscosity", !0, !1, 1],
      ["pferdestärke", "PS", null, "power", !1, !0, 735.49875],
      ["phot", "ph", null, "illuminance", !1, !1, 1e-4],
      ["pica (1/6 inch)", "pica", null, "length", !1, !0, 35277777777778e-17],
      ["pica (1/72 inch)", "Pica", ["Picapt"], "length", !1, !0, 0.00423333333333333],
      ["poise", "P", null, "dynamic_viscosity", !1, !1, 0.1],
      ["pond", "pond", null, "force", !1, !0, 980665e-8],
      ["pound force", "lbf", null, "force", !1, !0, 4.4482216152605],
      ["pound mass", "lbm", null, "mass", !1, !0, 0.45359237],
      ["quart", "qt", null, "volume", !1, !0, 946352946e-12],
      ["radian", "rad", null, "angle", !0, !1, 1],
      ["rankine", "Rank", null, "temperature", !1, !0, 1],
      ["reaumur", "Reau", null, "temperature", !1, !0, 1],
      ["second", "?", null, "angle", !1, !1, 484813681109536e-20],
      ["second", "s", ["sec"], "time", !0, !0, 1],
      ["short hundredweight", "cwt", ["shweight"], "mass", !1, !0, 45.359237],
      ["siemens", "S", null, "electrical_conductance", !0, !1, 1],
      ["sievert", "Sv", null, "equivalent_dose", !0, !1, 1],
      ["slug", "sg", null, "mass", !1, !0, 14.59390294],
      ["square ångström", "ang2", ["ang^2"], "area", !1, !0, 1e-20],
      ["square foot", "ft2", ["ft^2"], "area", !1, !0, 0.09290304],
      ["square inch", "in2", ["in^2"], "area", !1, !0, 64516e-8],
      ["square light-year", "ly2", ["ly^2"], "area", !1, !0, 895054210748189e17],
      ["square meter", "m?", null, "area", !0, !0, 1],
      ["square mile", "mi2", ["mi^2"], "area", !1, !0, 2589988110336e-6],
      ["square nautical mile", "Nmi2", ["Nmi^2"], "area", !1, !0, 3429904],
      ["square Pica", "Pica2", ["Picapt2", "Pica^2", "Picapt^2"], "area", !1, !0, 1792111111111e-17],
      ["square yard", "yd2", ["yd^2"], "area", !1, !0, 0.83612736],
      ["statute mile", "mi", null, "length", !1, !0, 1609.344],
      ["steradian", "sr", null, "solid_angle", !0, !1, 1],
      ["stilb", "sb", null, "luminance", !1, !1, 1e-4],
      ["stokes", "St", null, "kinematic_viscosity", !1, !1, 1e-4],
      ["stone", "stone", null, "mass", !1, !0, 6.35029318],
      ["tablespoon", "tbs", null, "volume", !1, !0, 147868e-10],
      ["teaspoon", "tsp", null, "volume", !1, !0, 492892e-11],
      ["tesla", "T", null, "magnetic_flux_density", !0, !0, 1],
      ["thermodynamic calorie", "c", null, "energy", !1, !0, 4.184],
      ["ton", "ton", null, "mass", !1, !0, 907.18474],
      ["tonne", "t", null, "mass", !1, !1, 1e3],
      ["U.K. pint", "uk_pt", null, "volume", !1, !0, 56826125e-11],
      ["U.S. bushel", "bushel", null, "volume", !1, !0, 0.03523907],
      ["U.S. oil barrel", "barrel", null, "volume", !1, !0, 0.158987295],
      ["U.S. pint", "pt", ["us_pt"], "volume", !1, !0, 473176473e-12],
      ["U.S. survey mile", "survey_mi", null, "length", !1, !0, 1609.347219],
      ["U.S. survey/statute acre", "us_acre", null, "area", !1, !0, 4046.87261],
      ["volt", "V", null, "voltage", !0, !1, 1],
      ["watt", "W", null, "power", !0, !0, 1],
      ["watt-hour", "Wh", ["wh"], "energy", !1, !0, 3600],
      ["weber", "Wb", null, "magnetic_flux", !0, !1, 1],
      ["yard", "yd", null, "length", !1, !0, 0.9144],
      ["year", "yr", null, "time", !1, !0, 31557600]
    ]);
    // Binary prefixes
    // [Name, Prefix power of 2 value, Previx value, Abbreviation, Derived from]
    A(this, "_binaryPrefixes", {
      Yi: ["yobi", 80, 12089258196146292e8, "Yi", "yotta"],
      Zi: ["zebi", 70, 11805916207174113e5, "Zi", "zetta"],
      Ei: ["exbi", 60, 1152921504606847e3, "Ei", "exa"],
      Pi: ["pebi", 50, 1125899906842624, "Pi", "peta"],
      Ti: ["tebi", 40, 1099511627776, "Ti", "tera"],
      Gi: ["gibi", 30, 1073741824, "Gi", "giga"],
      Mi: ["mebi", 20, 1048576, "Mi", "mega"],
      ki: ["kibi", 10, 1024, "ki", "kilo"]
    });
    // Unit prefixes
    // [Name, Multiplier, Abbreviation]
    A(this, "_unitPrefixes", {
      Y: ["yotta", 1e24, "Y"],
      Z: ["zetta", 1e21, "Z"],
      E: ["exa", 1e18, "E"],
      P: ["peta", 1e15, "P"],
      T: ["tera", 1e12, "T"],
      G: ["giga", 1e9, "G"],
      M: ["mega", 1e6, "M"],
      k: ["kilo", 1e3, "k"],
      h: ["hecto", 100, "h"],
      e: ["dekao", 10, "e"],
      d: ["deci", 0.1, "d"],
      c: ["centi", 0.01, "c"],
      m: ["milli", 1e-3, "m"],
      u: ["micro", 1e-6, "u"],
      n: ["nano", 1e-9, "n"],
      p: ["pico", 1e-12, "p"],
      f: ["femto", 1e-15, "f"],
      a: ["atto", 1e-18, "a"],
      z: ["zepto", 1e-21, "z"],
      y: ["yocto", 1e-24, "y"]
    });
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = H(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = `${c.getValue()}`, d = `${l.getValue()}`;
    if (Number.isNaN(f))
      return g.create(m.VALUE);
    let _, C = 1, E, b = 1;
    const { _from: y, _to: p } = this._lookupFromAndToUnits(h, d);
    if (_ = y, E = p, _ === null) {
      const { _from: w, _fromMultiplier: S } = this._lookupFromPrefix(h);
      _ = w, C = S;
    }
    if (E === null) {
      const { _to: w, _toMultiplier: S } = this._lookupToPrefix(d);
      E = w, b = S;
    }
    if (_ === null || E === null || _[3] !== E[3])
      return g.create(m.NA);
    let N;
    return _[3] === "temperature" ? (N = this._getTemperatureConversion(f, _[1], E[1]), N = +N.toFixed(2)) : N = f * _[6] * C / (E[6] * b), R.create(N);
  }
  _lookupFromAndToUnits(e, t) {
    let r = null, n = null, s;
    for (let a = 0; a < this._units.length; a++)
      s = this._units[a][2] === null ? [] : this._units[a][2], (this._units[a][1] === e || s.indexOf(e) >= 0) && (r = this._units[a]), (this._units[a][1] === t || s.indexOf(t) >= 0) && (n = this._units[a]);
    return {
      _from: r,
      _to: n
    };
  }
  _lookupFromPrefix(e) {
    let t = null, r = 1, n = e, s;
    const a = this._binaryPrefixes[e.substring(0, 2)];
    let u = this._unitPrefixes[e.substring(0, 1)];
    e.substring(0, 2) === "da" && (u = ["dekao", 10, "da"]), a ? (r = a[2], n = e.substring(2)) : u && (r = u[1], n = e.substring(u[2].length));
    for (let c = 0; c < this._units.length; c++)
      s = this._units[c][2] === null ? [] : this._units[c][2], (this._units[c][1] === n || s.indexOf(n) >= 0) && (t = this._units[c]);
    return {
      _from: t,
      _fromMultiplier: r
    };
  }
  _lookupToPrefix(e) {
    let t = null, r = 1, n = e, s;
    const a = this._binaryPrefixes[e.substring(0, 2)];
    let u = this._unitPrefixes[e.substring(0, 1)];
    e.substring(0, 2) === "da" && (u = ["dekao", 10, "da"]), a ? (r = a[2], n = e.substring(2)) : u && (r = u[1], n = e.substring(u[2].length));
    for (let c = 0; c < this._units.length; c++)
      s = this._units[c][2] === null ? [] : this._units[c][2], (this._units[c][1] === n || s.indexOf(n) >= 0) && (t = this._units[c]);
    return {
      _to: t,
      _toMultiplier: r
    };
  }
  _getTemperatureConversion(e, t, r) {
    switch (t) {
      case "C":
        return this._centigradeConversion(e, r);
      case "F":
        return this._fahrenheitConversion(e, r);
      case "K":
        return this._kelvinConversion(e, r);
      case "Rank":
        return this._rankineConversion(e, r);
      case "Reau":
        return this._reaumurConversion(e, r);
      default:
        return e;
    }
  }
  _centigradeConversion(e, t) {
    switch (t) {
      case "F":
        return e * 9 / 5 + 32;
      case "K":
        return e + 273.15;
      case "Rank":
        return (e + 273.15) * 9 / 5;
      case "Reau":
        return e * 4 / 5;
      default:
        return e;
    }
  }
  _fahrenheitConversion(e, t) {
    switch (t) {
      case "C":
        return (e - 32) * 5 / 9;
      case "K":
        return (e - 32) * 5 / 9 + 273.15;
      case "Rank":
        return e + 459.67;
      case "Reau":
        return (e - 32) * 4 / 9;
      default:
        return e;
    }
  }
  _kelvinConversion(e, t) {
    switch (t) {
      case "C":
        return e - 273.15;
      case "F":
        return (e - 273.15) * 9 / 5 + 32;
      case "Rank":
        return e * 9 / 5;
      case "Reau":
        return (e - 273.15) * 4 / 5;
      default:
        return e;
    }
  }
  _rankineConversion(e, t) {
    switch (t) {
      case "C":
        return (e - 491.67) * 5 / 9;
      case "F":
        return e - 459.67;
      case "K":
        return e * 5 / 9;
      case "Reau":
        return (e - 491.67) * 4 / 9;
      default:
        return e;
    }
  }
  _reaumurConversion(e, t) {
    switch (t) {
      case "C":
        return e * 5 / 4;
      case "F":
        return e * 9 / 4 + 32;
      case "K":
        return e * 5 / 4 + 273.15;
      case "Rank":
        return e * 9 / 4 + 491.67;
      default:
        return e;
    }
  }
}
class u1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull())
      return g.create(m.NA);
    let r = 0;
    if (t) {
      const { isError: f, errorObject: h, variants: d } = H(t);
      if (f)
        return h;
      const [_] = d;
      if (r = Math.floor(+_.getValue()), Number.isNaN(r))
        return g.create(m.VALUE);
      if (r < 0 || r > 10)
        return g.create(m.NUM);
    }
    const { isError: n, errorObject: s, variants: a } = H(e);
    if (n)
      return s;
    const [u] = a, c = Math.trunc(+u.getValue());
    if (Number.isNaN(c))
      return g.create(m.VALUE);
    if (!/^-?[0-9]{1,3}$/.test(`${c}`) || c < -512 || c > 511)
      return g.create(m.NUM);
    let l;
    if (c < 0) {
      const f = (512 + c).toString(2);
      l = `1${"0".repeat(9 - f.length)}${f}`;
    } else if (l = Number.parseInt(`${c}`, 10).toString(2), t) {
      if (r < l.length)
        return g.create(m.NUM);
      l = "0".repeat(r - l.length) + l;
    }
    return U.create(l);
  }
}
class c1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull())
      return g.create(m.NA);
    let r = 0;
    if (t) {
      const { isError: f, errorObject: h, variants: d } = H(t);
      if (f)
        return h;
      const [_] = d;
      if (r = Math.floor(+_.getValue()), Number.isNaN(r))
        return g.create(m.VALUE);
      if (r < 0 || r > 10)
        return g.create(m.NUM);
    }
    const { isError: n, errorObject: s, variants: a } = H(e);
    if (n)
      return s;
    const [u] = a, c = Math.trunc(+u.getValue());
    if (Number.isNaN(c))
      return g.create(m.VALUE);
    if (!/^-?[0-9]{1,12}$/.test(`${c}`) || c < -549755813888 || c > 549755813887)
      return g.create(m.NUM);
    let l;
    if (c < 0)
      l = (1099511627776 + c).toString(16);
    else if (l = Number.parseInt(`${c}`, 10).toString(16), t) {
      if (r < l.length)
        return g.create(m.NUM);
      l = "0".repeat(r - l.length) + l;
    }
    return U.create(l.toLocaleUpperCase());
  }
}
class l1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull())
      return g.create(m.NA);
    let r = 0;
    if (t) {
      const { isError: f, errorObject: h, variants: d } = H(t);
      if (f)
        return h;
      const [_] = d;
      if (r = Math.floor(+_.getValue()), Number.isNaN(r))
        return g.create(m.VALUE);
      if (r < 0 || r > 10)
        return g.create(m.NUM);
    }
    const { isError: n, errorObject: s, variants: a } = H(e);
    if (n)
      return s;
    const [u] = a, c = Math.trunc(+u.getValue());
    if (Number.isNaN(c))
      return g.create(m.VALUE);
    if (!/^-?[0-9]{1,9}$/.test(`${c}`) || c < -536870912 || c > 536870911)
      return g.create(m.NUM);
    let l;
    if (c < 0)
      l = (1073741824 + c).toString(8);
    else if (l = Number.parseInt(`${c}`, 10).toString(8), t) {
      if (r < l.length)
        return g.create(m.NUM);
      l = "0".repeat(r - l.length) + l;
    }
    return U.create(l);
  }
}
class f1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(0), { isError: n, errorObject: s, variants: a } = H(e, r);
    if (n)
      return s;
    const [u, c] = a, l = +u.getValue(), f = +c.getValue();
    if (Number.isNaN(l) || Number.isNaN(f))
      return g.create(m.VALUE);
    const h = l === f ? 1 : 0;
    return R.create(h);
  }
}
class h1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r;
    if (t) {
      const { isError: n, errorObject: s, variants: a } = H(e, t);
      if (n)
        return s;
      const [u, c] = a, l = +u.getValue(), f = +c.getValue();
      if (Number.isNaN(l) || Number.isNaN(f))
        return g.create(m.VALUE);
      r = rr(f) - rr(l);
    } else {
      const { isError: n, errorObject: s, variants: a } = H(e);
      if (n)
        return s;
      const [u] = a, c = +u.getValue();
      if (Number.isNaN(c))
        return g.create(m.VALUE);
      r = rr(c);
    }
    return R.create(r);
  }
}
class m1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = +s.getValue();
    if (Number.isNaN(a))
      return g.create(m.VALUE);
    const u = rr(a);
    return R.create(u);
  }
}
class g1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = +s.getValue();
    if (Number.isNaN(a))
      return g.create(m.VALUE);
    const u = Fa(a);
    return R.create(u);
  }
}
class d1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = +s.getValue();
    if (Number.isNaN(a))
      return g.create(m.VALUE);
    const u = Fa(a);
    return R.create(u);
  }
}
var Z = /* @__PURE__ */ ((o) => (o.BESSELI = "BESSELI", o.BESSELJ = "BESSELJ", o.BESSELK = "BESSELK", o.BESSELY = "BESSELY", o.BIN2DEC = "BIN2DEC", o.BIN2HEX = "BIN2HEX", o.BIN2OCT = "BIN2OCT", o.BITAND = "BITAND", o.BITLSHIFT = "BITLSHIFT", o.BITOR = "BITOR", o.BITRSHIFT = "BITRSHIFT", o.BITXOR = "BITXOR", o.COMPLEX = "COMPLEX", o.CONVERT = "CONVERT", o.DEC2BIN = "DEC2BIN", o.DEC2HEX = "DEC2HEX", o.DEC2OCT = "DEC2OCT", o.DELTA = "DELTA", o.ERF = "ERF", o.ERF_PRECISE = "ERF.PRECISE", o.ERFC = "ERFC", o.ERFC_PRECISE = "ERFC.PRECISE", o.GESTEP = "GESTEP", o.HEX2BIN = "HEX2BIN", o.HEX2DEC = "HEX2DEC", o.HEX2OCT = "HEX2OCT", o.IMABS = "IMABS", o.IMAGINARY = "IMAGINARY", o.IMARGUMENT = "IMARGUMENT", o.IMCONJUGATE = "IMCONJUGATE", o.IMCOS = "IMCOS", o.IMCOSH = "IMCOSH", o.IMCOT = "IMCOT", o.IMCOTH = "IMCOTH", o.IMCSC = "IMCSC", o.IMCSCH = "IMCSCH", o.IMDIV = "IMDIV", o.IMEXP = "IMEXP", o.IMLN = "IMLN", o.IMLOG = "IMLOG", o.IMLOG10 = "IMLOG10", o.IMLOG2 = "IMLOG2", o.IMPOWER = "IMPOWER", o.IMPRODUCT = "IMPRODUCT", o.IMREAL = "IMREAL", o.IMSEC = "IMSEC", o.IMSECH = "IMSECH", o.IMSIN = "IMSIN", o.IMSINH = "IMSINH", o.IMSQRT = "IMSQRT", o.IMSUB = "IMSUB", o.IMSUM = "IMSUM", o.IMTAN = "IMTAN", o.IMTANH = "IMTANH", o.OCT2BIN = "OCT2BIN", o.OCT2DEC = "OCT2DEC", o.OCT2HEX = "OCT2HEX", o))(Z || {});
class C1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull())
      return g.create(m.NA);
    const r = t != null ? t : R.create(0), { isError: n, errorObject: s, variants: a } = H(e, r);
    if (n)
      return s;
    const [u, c] = a, l = +u.getValue(), f = +c.getValue();
    if (Number.isNaN(l) || Number.isNaN(f))
      return g.create(m.VALUE);
    const h = l >= f ? 1 : 0;
    return R.create(h);
  }
}
class _1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull())
      return g.create(m.NA);
    let r = 0;
    if (t) {
      const { isError: d, errorObject: _, variants: C } = H(t);
      if (d)
        return _;
      const [E] = C;
      if (r = Math.floor(+E.getValue()), Number.isNaN(r))
        return g.create(m.VALUE);
      if (r < 0 || r > 10)
        return g.create(m.NUM);
    }
    const { isError: n, errorObject: s, variants: a } = H(e);
    if (n)
      return s;
    const [u] = a, c = `${u.getValue()}`;
    if (!Ia(c))
      return g.create(m.NUM);
    const l = c.length === 10 && c.substring(0, 1).toLocaleUpperCase() === "F", f = l ? Number.parseInt(c, 16) - 1099511627776 : Number.parseInt(c, 16);
    if (f < -512 || f > 511)
      return g.create(m.NUM);
    let h;
    if (l) {
      const d = (512 + f).toString(2);
      h = `1${"0".repeat(9 - d.length)}${d}`;
    } else if (h = f.toString(2), t) {
      if (r < h.length)
        return g.create(m.NUM);
      h = "0".repeat(r - h.length) + h;
    }
    return U.create(h);
  }
}
class A1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`;
    if (!Ia(a))
      return g.create(m.NUM);
    let u = Number.parseInt(a, 16);
    return u >= 549755813888 && (u -= 1099511627776), R.create(u);
  }
}
class E1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull())
      return g.create(m.NA);
    let r = 0;
    if (t) {
      const { isError: h, errorObject: d, variants: _ } = H(t);
      if (h)
        return d;
      const [C] = _;
      if (r = Math.floor(+C.getValue()), Number.isNaN(r))
        return g.create(m.VALUE);
      if (r < 0 || r > 10)
        return g.create(m.NUM);
    }
    const { isError: n, errorObject: s, variants: a } = H(e);
    if (n)
      return s;
    const [u] = a, c = `${u.getValue()}`;
    if (!Ia(c))
      return g.create(m.NUM);
    const l = Number.parseInt(c, 16);
    if (l > 536870911 && l < 1098974756864)
      return g.create(m.NUM);
    let f;
    if (l >= 1098974756864)
      f = (l - 1098437885952).toString(8);
    else if (f = l.toString(8), t) {
      if (r < f.length)
        return g.create(m.NUM);
      f = "0".repeat(r - f.length) + f;
    }
    return U.create(f);
  }
}
class b1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.Abs();
    return R.create(c);
  }
}
class y1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.getINum();
    return R.create(c);
  }
}
class R1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    if (u.getRealNum() === 0 && u.getINum() === 0)
      return g.create(m.DIV_BY_ZERO);
    const c = u.Argument();
    return R.create(c);
  }
}
class p1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.Conjugate();
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class N1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.Cos();
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class V1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.Cosh();
    return u.isError() ? g.create(m.NUM) : typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class O1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError() || u.getRealNum() === 0 && u.getINum() === 0)
      return g.create(m.NUM);
    const c = u.Cot();
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class w1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError() || u.getRealNum() === 0 && u.getINum() === 0)
      return g.create(m.NUM);
    const c = u.Coth();
    return u.isError() ? g.create(m.NUM) : typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class S1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError() || u.getRealNum() === 0 && u.getINum() === 0)
      return g.create(m.NUM);
    const c = u.Csc();
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class M1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError() || u.getRealNum() === 0 && u.getINum() === 0)
      return g.create(m.NUM);
    const c = u.Csch();
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class D1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull() || t.isNull())
      return g.create(m.NA);
    const { isError: r, errorObject: n, variants: s } = H(e, t);
    if (r)
      return n;
    const [a, u] = s, c = `${a.getValue()}`, l = `${u.getValue()}`, f = new he(c), h = new he(l);
    if (f.isError() || h.isError() || f.isDifferentSuffixes(h) || h.getRealNum() === 0 && h.getINum() === 0)
      return g.create(m.NUM);
    const d = f.Div(h);
    return typeof d == "number" || q(d) ? R.create(+d) : U.create(d);
  }
}
class L1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.Exp();
    return u.isError() ? g.create(m.NUM) : typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class x1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError() || u.getRealNum() === 0 && u.getINum() === 0)
      return g.create(m.NUM);
    const c = u.Ln();
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class P1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const { isError: r, errorObject: n, variants: s } = H(e);
    if (r)
      return n;
    const [a] = s;
    let u = t != null ? t : R.create(10);
    if (u.isArray()) {
      const b = u.getRowCount(), y = u.getColumnCount();
      if (b > 1 || y > 1)
        return g.create(m.VALUE);
      u = u.get(0, 0);
    }
    const { isError: c, errorObject: l, variants: f } = I(u);
    if (c)
      return l;
    const [h] = f, d = `${a.getValue()}`, _ = +h.getValue(), C = new he(d);
    if (C.isError() || C.getRealNum() === 0 && C.getINum() === 0 || _ <= 0)
      return g.create(m.NUM);
    const E = C.Log(_);
    return C.isError() ? g.create(m.NUM) : typeof E == "number" || q(E) ? R.create(+E) : U.create(E);
  }
}
class j1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError() || u.getRealNum() === 0 && u.getINum() === 0)
      return g.create(m.NUM);
    const c = u.Log(2);
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class U1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError() || u.getRealNum() === 0 && u.getINum() === 0)
      return g.create(m.NUM);
    const c = u.Log(10);
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class v1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull() || t.isNull())
      return g.create(m.NA);
    const { isError: r, errorObject: n, variants: s } = H(e, t);
    if (r)
      return n;
    const [a, u] = s, c = `${a.getValue()}`, l = new he(c);
    if (l.isError())
      return g.create(m.NUM);
    const f = +u.getValue();
    if (Number.isNaN(f))
      return g.create(m.VALUE);
    const h = l.Power(f);
    return l.isError() ? g.create(m.NUM) : typeof h == "number" || q(h) ? R.create(+h) : U.create(h);
  }
}
class T1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = "";
    for (let r = 0; r < e.length; r++) {
      if (t instanceof g)
        return t;
      const n = e[r];
      if (n.isArray()) {
        let s = !1, a;
        if (n.iterator((u) => {
          if (t = this._handleSingleObject(u, t), t instanceof g)
            return s = !0, a = t, !1;
        }), s)
          return a;
      } else
        t = this._handleSingleObject(n, t);
    }
    return t instanceof g ? t : typeof t == "number" || q(t) ? R.create(+t) : U.create(t);
  }
  _handleSingleObject(e, t) {
    let r = t;
    if (e.isError())
      return e;
    if (e.isBoolean())
      return g.create(m.VALUE);
    const n = `${e.getValue()}`;
    if (typeof t != "number" && !t) {
      const s = new he(n);
      if (s.isError())
        return g.create(m.NUM);
      r = s.toString();
    } else {
      const s = new he(t), a = new he(n);
      if (s.isError() || a.isError())
        return g.create(m.NUM);
      if (s.isDifferentSuffixes(a))
        return g.create(m.VALUE);
      r = s.Product(a);
    }
    return r;
  }
}
class B1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.getRealNum();
    return R.create(c);
  }
}
class I1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.Sec();
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class F1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.Sech();
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class k1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.Sin();
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class $1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.Sinh();
    return u.isError() ? g.create(m.NUM) : typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class Y1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    if (u.getRealNum() === 0 && u.getINum() === 0)
      return R.create(0);
    const c = u.Sqrt();
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class H1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull() || t.isNull())
      return g.create(m.NA);
    const { isError: r, errorObject: n, variants: s } = H(e, t);
    if (r)
      return n;
    const [a, u] = s, c = `${a.getValue()}`, l = `${u.getValue()}`, f = new he(c), h = new he(l);
    if (f.isError() || h.isError() || f.isDifferentSuffixes(h))
      return g.create(m.NUM);
    const d = f.Sub(h);
    return typeof d == "number" || q(d) ? R.create(+d) : U.create(d);
  }
}
class Q1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = "";
    for (let r = 0; r < e.length; r++) {
      if (t instanceof g)
        return t;
      const n = e[r];
      if (n.isArray()) {
        let s = !1, a;
        if (n.iterator((u) => {
          if (t = this._handleSingleObject(u, t), t instanceof g)
            return s = !0, a = t, !1;
        }), s)
          return a;
      } else
        t = this._handleSingleObject(n, t);
    }
    return t instanceof g ? t : typeof t == "number" || q(t) ? R.create(+t) : U.create(t);
  }
  _handleSingleObject(e, t) {
    let r = t;
    if (e.isError())
      return e;
    if (e.isBoolean())
      return g.create(m.VALUE);
    const n = `${e.getValue()}`;
    if (typeof t != "number" && !t) {
      const s = new he(n);
      if (s.isError())
        return g.create(m.NUM);
      r = s.toString();
    } else {
      const s = new he(t), a = new he(n);
      if (s.isError() || a.isError())
        return g.create(m.NUM);
      if (s.isDifferentSuffixes(a))
        return g.create(m.VALUE);
      r = s.Sum(a);
    }
    return r;
  }
}
class G1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.Tan();
    return typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class W1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`, u = new he(a);
    if (u.isError())
      return g.create(m.NUM);
    const c = u.Tanh();
    return u.isError() ? g.create(m.NUM) : typeof c == "number" || q(c) ? R.create(+c) : U.create(c);
  }
}
class q1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull())
      return g.create(m.NA);
    let r = 0;
    if (t) {
      const { isError: d, errorObject: _, variants: C } = H(t);
      if (d)
        return _;
      const [E] = C;
      if (r = Math.floor(+E.getValue()), Number.isNaN(r))
        return g.create(m.VALUE);
      if (r < 0 || r > 10)
        return g.create(m.NUM);
    }
    const { isError: n, errorObject: s, variants: a } = H(e);
    if (n)
      return s;
    const [u] = a, c = `${u.getValue()}`;
    if (!Ba(c))
      return g.create(m.NUM);
    const l = c.length === 10 && c.substring(0, 1) === "7", f = l ? Number.parseInt(c, 8) - 1073741824 : Number.parseInt(c, 8);
    if (f < -512 || f > 511)
      return g.create(m.NUM);
    let h;
    if (l) {
      const d = (512 + f).toString(2);
      h = `1${"0".repeat(9 - d.length)}${d}`;
    } else if (h = f.toString(2), t) {
      if (r < h.length)
        return g.create(m.NUM);
      h = "0".repeat(r - h.length) + h;
    }
    return U.create(h);
  }
}
class K1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const { isError: t, errorObject: r, variants: n } = H(e);
    if (t)
      return r;
    const [s] = n, a = `${s.getValue()}`;
    if (!Ba(a))
      return g.create(m.NUM);
    let u = Number.parseInt(a, 8);
    return u >= 536870912 && (u -= 1073741824), R.create(u);
  }
}
class X1 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isNull())
      return g.create(m.NA);
    let r = 0;
    if (t) {
      const { isError: h, errorObject: d, variants: _ } = H(t);
      if (h)
        return d;
      const [C] = _;
      if (r = Math.floor(+C.getValue()), Number.isNaN(r))
        return g.create(m.VALUE);
      if (r < 0 || r > 10)
        return g.create(m.NUM);
    }
    const { isError: n, errorObject: s, variants: a } = H(e);
    if (n)
      return s;
    const [u] = a, c = `${u.getValue()}`;
    if (!Ba(c))
      return g.create(m.NUM);
    const l = Number.parseInt(c, 8);
    let f;
    if (l >= 536870912)
      f = `ff${(l + 3221225472).toString(16)}`;
    else if (f = l.toString(16), t) {
      if (r < f.length)
        return g.create(m.NUM);
      f = "0".repeat(r - f.length) + f;
    }
    return U.create(f.toLocaleUpperCase());
  }
}
const Z1 = [
  [qg, Z.BESSELI],
  [Kg, Z.BESSELJ],
  [Xg, Z.BESSELK],
  [Zg, Z.BESSELY],
  [zg, Z.BIN2DEC],
  [Jg, Z.BIN2HEX],
  [e1, Z.BIN2OCT],
  [t1, Z.BITAND],
  [r1, Z.BITLSHIFT],
  [n1, Z.BITOR],
  [s1, Z.BITRSHIFT],
  [a1, Z.BITXOR],
  [i1, Z.COMPLEX],
  [o1, Z.CONVERT],
  [u1, Z.DEC2BIN],
  [c1, Z.DEC2HEX],
  [l1, Z.DEC2OCT],
  [f1, Z.DELTA],
  [h1, Z.ERF],
  [m1, Z.ERF_PRECISE],
  [g1, Z.ERFC],
  [d1, Z.ERFC_PRECISE],
  [C1, Z.GESTEP],
  [_1, Z.HEX2BIN],
  [A1, Z.HEX2DEC],
  [E1, Z.HEX2OCT],
  [b1, Z.IMABS],
  [y1, Z.IMAGINARY],
  [R1, Z.IMARGUMENT],
  [p1, Z.IMCONJUGATE],
  [N1, Z.IMCOS],
  [V1, Z.IMCOSH],
  [O1, Z.IMCOT],
  [w1, Z.IMCOTH],
  [S1, Z.IMCSC],
  [M1, Z.IMCSCH],
  [D1, Z.IMDIV],
  [L1, Z.IMEXP],
  [x1, Z.IMLN],
  [P1, Z.IMLOG],
  [U1, Z.IMLOG10],
  [j1, Z.IMLOG2],
  [v1, Z.IMPOWER],
  [T1, Z.IMPRODUCT],
  [B1, Z.IMREAL],
  [I1, Z.IMSEC],
  [F1, Z.IMSECH],
  [k1, Z.IMSIN],
  [$1, Z.IMSINH],
  [Y1, Z.IMSQRT],
  [H1, Z.IMSUB],
  [Q1, Z.IMSUM],
  [G1, Z.IMTAN],
  [W1, Z.IMTANH],
  [q1, Z.OCT2BIN],
  [K1, Z.OCT2DEC],
  [X1, Z.OCT2HEX]
];
var ee = /* @__PURE__ */ ((o) => (o.ACCRINT = "ACCRINT", o.ACCRINTM = "ACCRINTM", o.AMORDEGRC = "AMORDEGRC", o.AMORLINC = "AMORLINC", o.COUPDAYBS = "COUPDAYBS", o.COUPDAYS = "COUPDAYS", o.COUPDAYSNC = "COUPDAYSNC", o.COUPNCD = "COUPNCD", o.COUPNUM = "COUPNUM", o.COUPPCD = "COUPPCD", o.CUMIPMT = "CUMIPMT", o.CUMPRINC = "CUMPRINC", o.DB = "DB", o.DDB = "DDB", o.DISC = "DISC", o.DOLLARDE = "DOLLARDE", o.DOLLARFR = "DOLLARFR", o.DURATION = "DURATION", o.EFFECT = "EFFECT", o.FV = "FV", o.FVSCHEDULE = "FVSCHEDULE", o.INTRATE = "INTRATE", o.IPMT = "IPMT", o.IRR = "IRR", o.ISPMT = "ISPMT", o.MDURATION = "MDURATION", o.MIRR = "MIRR", o.NOMINAL = "NOMINAL", o.NPER = "NPER", o.NPV = "NPV", o.ODDFPRICE = "ODDFPRICE", o.ODDFYIELD = "ODDFYIELD", o.ODDLPRICE = "ODDLPRICE", o.ODDLYIELD = "ODDLYIELD", o.PDURATION = "PDURATION", o.PMT = "PMT", o.PPMT = "PPMT", o.PRICE = "PRICE", o.PRICEDISC = "PRICEDISC", o.PRICEMAT = "PRICEMAT", o.PV = "PV", o.RATE = "RATE", o.RECEIVED = "RECEIVED", o.RRI = "RRI", o.SLN = "SLN", o.SYD = "SYD", o.TBILLEQ = "TBILLEQ", o.TBILLPRICE = "TBILLPRICE", o.TBILLYIELD = "TBILLYIELD", o.VDB = "VDB", o.XIRR = "XIRR", o.XNPV = "XNPV", o.YIELD = "YIELD", o.YIELDDISC = "YIELDDISC", o.YIELDMAT = "YIELDMAT", o))(ee || {});
function fs(o, i, e, t) {
  const r = Ht(o, i, e), { days: n } = _e(r, o, t);
  return n;
}
function Ze(o, i, e, t) {
  let r;
  if (t === 1) {
    const n = Ht(o, i, e);
    let s = re(n);
    s = Or(s, 12 / e);
    const a = Re(s);
    n < 0 && e === 1 ? r = 365 : r = a - n;
  } else t === 3 ? r = 365 / e : r = 360 / e;
  return r;
}
function Yu(o, i, e) {
  const t = re(o);
  let r = re(i);
  for (r.setUTCFullYear(t.getUTCFullYear()), r < t && r.setUTCFullYear(r.getUTCFullYear() + 1); r > t; )
    r = Or(r, -12 / e);
  return r = Or(r, 12 / e), Re(r);
}
function or(o, i, e) {
  let t = 0;
  const r = re(o);
  let n = re(i);
  for (; n > r; )
    n = Or(n, -12 / e), t++;
  return t;
}
function Ht(o, i, e) {
  const t = re(o);
  let r = re(i);
  for (r.setUTCFullYear(t.getUTCFullYear()), r < t && r.setUTCFullYear(r.getUTCFullYear() + 1); r > t; )
    r = Or(r, -12 / e);
  return Re(r);
}
function Hu(o, i, e, t, r, n) {
  const s = fs(o, i, r, n), a = Ze(o, i, r, n), u = or(o, i, r), c = (a - s) / a - 1, l = t / r + 1, f = e * 100 / r;
  let h = 0, d = 0;
  for (let E = 1; E <= u; E++) {
    const b = E + c, y = f / l ** b;
    h += b * y, d += y;
  }
  const _ = u + c, C = 100 / l ** _;
  return h += _ * C, d += C, h / d / r;
}
function zr(o, i, e, t, r) {
  let n;
  if (o === 0)
    n = (e + t) / i;
  else {
    const s = (1 + o) ** i;
    n = r === 1 ? (t * o / (s - 1) + e * o / (1 - 1 / s)) / (1 + o) : t * o / (s - 1) + e * o / (1 - 1 / s);
  }
  return -n;
}
function ur(o, i, e, t, r) {
  let n;
  if (o === 0)
    n = t + e * i;
  else {
    if (o === -1 && i === 0)
      return Number.NaN;
    const s = (1 + o) ** i;
    n = r === 1 ? t * s + e * (1 + o) * (s - 1) / o : t * s + e * (s - 1) / o;
  }
  return -n;
}
function Qu(o, i, e, t, r, n) {
  const s = zr(o, e, t, r, n);
  return (i === 1 ? n === 1 ? 0 : -t : n === 1 ? ur(o, i - 2, s, t, 1) - s : ur(o, i - 1, s, t, 0)) * o;
}
function Gu(o, i) {
  let e = 0;
  for (let t = 1; t <= i.length; t++)
    e += i[t - 1] / (1 + o) ** t;
  return e;
}
function Wu(o, i, e, t, r, n, s, a, u) {
  const c = er(e, t, u), l = Ze(o, t, a, u);
  return c < l ? z1(
    o,
    i,
    e,
    t,
    r,
    n,
    s,
    a,
    u,
    c,
    l
  ) : J1(
    o,
    i,
    e,
    t,
    r,
    n,
    s,
    a,
    u,
    l
  );
}
function z1(o, i, e, t, r, n, s, a, u, c, l) {
  let f = 0;
  const h = or(o, i, a), d = er(o, t, u);
  f += s / (1 + n / a) ** (h - 1 + d / l), f += 100 * r / a * c / l / (1 + n / a) ** (d / l);
  for (let C = 2; C <= h; C++)
    f += 100 * r / a / (1 + n / a) ** (C - 1 + d / l);
  const _ = er(e, o, u);
  return f -= 100 * r / a * _ / l, f;
}
function J1(o, i, e, t, r, n, s, a, u, c) {
  let l = 0;
  const f = or(t, i, a), h = e0(t, o, 12 / a);
  let d;
  if (u === 2 || u === 3) {
    const y = Yu(o, t, a);
    d = er(o, y, u);
  } else {
    const y = Ht(o, t, a), { days: p } = _e(y, o, u);
    d = c - p;
  }
  l += s / (1 + n / a) ** (f + h + d / c);
  const _ = or(e, t, a);
  let C = t, E = 0, b = 0;
  for (let y = _; y >= 1; y--) {
    const p = sr(C, -12 / a, !1), N = u === 1 ? er(p, C, u) : c, w = y > 1 ? N : er(e, C, u);
    E += w / N;
    const S = e > p ? e : p, M = o < C ? o : C, D = er(S, M, u);
    b += D / N, C = p;
  }
  l += 100 * r / a * E / (1 + n / a) ** (h + d / c);
  for (let y = 1; y <= f; y++)
    l += 100 * r / a / (1 + n / a) ** (y + h + d / c);
  return l -= 100 * r / a * b, l;
}
function er(o, i, e) {
  const { days: t } = _e(o, i, e);
  return o < i ? t : 0;
}
function qu(o, i, e) {
  const t = re(o), r = t.getUTCFullYear(), n = t.getUTCMonth(), s = t.getUTCDate(), a = Vr(r, n, s), u = re(i), c = u.getUTCFullYear(), l = u.getUTCMonth(), f = u.getUTCDate(), h = Vr(c, l, f);
  return !(s !== f && !(a && h) || Math.abs((c - r) * 12 + (l - n)) % (12 / e) !== 0);
}
function xr(o, i, e) {
  return Ht(o, i, e) >= 0;
}
function sr(o, i, e) {
  let t = re(o);
  if (t = Or(t, i), e) {
    const r = t.getUTCFullYear(), n = t.getUTCMonth(), s = Zr(r, n);
    t.setUTCDate(s);
  }
  return Re(t);
}
function e0(o, i, e, t) {
  const r = re(o), n = re(i), s = r.getUTCFullYear(), a = r.getUTCMonth(), u = r.getUTCDate(), c = n.getUTCFullYear(), l = n.getUTCMonth(), f = n.getUTCDate(), h = Vr(s, a, u), d = !h && a !== 1 && u > 28 && u < Zr(s, a) ? Vr(c, l, f) : h, _ = sr(i, 0, d);
  let C = 1 + +(i < _), E = sr(_, e, d);
  for (; !(e > 0 ? E >= i : E <= i); )
    E = sr(E, e, d), C++;
  return C;
}
function ti(o, i) {
  let n = 1, s = 0, a = o, u;
  for (; n > 1e-7 && s < 500; ) {
    const c = (i(a + 1e-7) - i(a - 1e-7)) / 2e-7;
    u = a - i(a) / c, s++, n = Math.abs(u - a), a = u;
  }
  return Number.isNaN(a) || Math.abs(a) === 1 / 0 || s === 500 ? t0(o, i) : a;
}
function t0(o, i) {
  const r = Number.MAX_VALUE, n = -1, s = 1.6;
  let a = o - 0.01 <= n ? n + 1e-7 : o - 0.01, u = o + 0.01 >= r ? r - 1e-7 : o + 0.01, c, l, f = 0;
  if (o <= n || o >= r)
    return g.create(m.NUM);
  for (let E = 0; E < 60; E++) {
    c = a <= n ? n + 1e-7 : a, l = u >= r ? r - 1e-7 : u;
    const b = i(c), y = i(l);
    if (b * y <= 0)
      break;
    if (b * y > 0)
      a = c + s * (c - l), u = l + s * (l - c);
    else
      return g.create(m.NUM);
    if (E === 59)
      return g.create(m.NUM);
  }
  c = c, l = l;
  let h = i(c);
  const d = i(l);
  let _, C;
  if (Math.abs(h) < 1e-7 || Math.abs(d) < 1e-7)
    return g.create(m.NUM);
  do
    C = c + (l - c) / 2, _ = i(C), h * _ < 0 ? l = C : c = C, h = i(c), f++;
  while (Math.abs(_) > 1e-7 && f < 60);
  return C;
}
function An(o, i, e, t, r, n, s) {
  const a = or(o, i, n), u = Ze(o, i, n, s), c = fs(o, i, n, s);
  if (a === 1) {
    const h = u - c, d = 100 * e / n + r, _ = t / n * h / u + 1, C = 100 * e / n * c / u;
    return d / _ - C;
  }
  const l = u - c;
  let f = r / (1 + t / n) ** (a - 1 + l / u);
  for (let h = 1; h <= a; h++)
    f += 100 * e / n / (1 + t / n) ** (h - 1 + l / u);
  return f -= 100 * e / n * c / u, f;
}
function ma(o, i, e, t, r) {
  let n = 0, s = r / e;
  s >= 1 ? (s = 1, n = t === 1 ? o : 0) : n = o * (1 - s) ** (t - 1);
  const a = o * (1 - s) ** t;
  let u = 0;
  return a < i ? u = n - i : u = n - a, u < 0 && (u = 0), u;
}
class r0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 6);
    A(this, "maxParams", 8);
  }
  calculate(e, t, r, n, s, a, u, c) {
    const l = u != null ? u : R.create(0), f = c != null ? c : T.create(!0), { isError: h, errorObject: d, variants: _ } = H(e, t, r, n, s, a, l);
    if (h)
      return d;
    const [C, E, b, y, p, N, w] = _, S = $(C);
    if (typeof S != "number")
      return S;
    const M = $(E);
    if (typeof M != "number")
      return M;
    const D = $(b);
    if (typeof D != "number")
      return D;
    const L = +y.getValue(), v = +p.getValue(), j = Math.floor(+N.getValue()), k = Math.floor(+w.getValue()), G = +f.getValue();
    return Number.isNaN(L) || Number.isNaN(v) || Number.isNaN(j) || Number.isNaN(k) || Number.isNaN(G) ? g.create(m.VALUE) : L <= 0 || v <= 0 || ![1, 2, 4].includes(j) || k < 0 || k > 4 || Math.floor(S) >= Math.floor(D) ? g.create(m.NUM) : this._getResult(S, M, D, L, v, j, k, G);
  }
  _getResult(e, t, r, n, s, a, u, c) {
    let l = Ht(e, t, a);
    if (l <= 0)
      return R.create(0);
    l = Ht(r, t, a);
    const f = 12 / a, h = re(t), d = h.getUTCFullYear(), _ = h.getUTCMonth(), C = h.getUTCDate(), E = Vr(d, _, C);
    let b = sr(t, -f, E);
    if (r > t && c)
      for (b = t; b < r; )
        b = sr(b, f, E);
    let y = e > b ? e : b, { days: p } = _e(y, r, u);
    if (l >= e) {
      const { days: L } = _e(y, r, u ? 4 : 0);
      p = L;
    }
    r < y && (p = -p);
    let N = Ze(b, t, a, u), w = p / N, S = b, M = e;
    for (; S > e; ) {
      M = S, S = sr(S, -f, E), y = e > S ? e : S;
      const { days: L } = _e(y, M, u);
      if (u === 0)
        M >= y || e <= S ? p = L : p = -L, N = Ze(S, M, a, u);
      else if (p = M < y ? -L : L, u === 3)
        N = 365 / a;
      else {
        const { days: v } = _e(S, M, u);
        N = M < S ? -v : v;
      }
      w += e <= S ? c ? 1 : 0 : p / N;
    }
    const D = s * n / a * w;
    return R.create(D);
  }
}
class n0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 5);
  }
  calculate(e, t, r, n, s) {
    const a = s != null ? s : R.create(0), { isError: u, errorObject: c, variants: l } = H(e, t, r, n, a);
    if (u)
      return c;
    const [f, h, d, _, C] = l, E = $(f);
    if (typeof E != "number")
      return E;
    const b = $(h);
    if (typeof b != "number")
      return b;
    const y = +d.getValue(), p = +_.getValue(), N = Math.floor(+C.getValue());
    if (Number.isNaN(y) || Number.isNaN(p) || Number.isNaN(N))
      return g.create(m.VALUE);
    if (y <= 0 || p <= 0 || N < 0 || N > 4 || Math.floor(E) > Math.floor(b))
      return g.create(m.NUM);
    if (Math.floor(E) === Math.floor(b))
      return R.create(0);
    const { days: w, yearDays: S } = _e(E, b, N), M = p * y * w / S;
    return R.create(M);
  }
}
class s0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 6);
    A(this, "maxParams", 7);
  }
  calculate(e, t, r, n, s, a, u) {
    const c = u != null ? u : R.create(0), { isError: l, errorObject: f, variants: h } = H(e, t, r, n, s, a, c);
    if (l)
      return f;
    const [d, _, C, E, b, y, p] = h, N = $(_);
    if (typeof N != "number")
      return N;
    const w = $(C);
    if (typeof w != "number")
      return w;
    const S = +d.getValue(), M = +E.getValue();
    let D = +b.getValue();
    const L = +y.getValue(), v = Math.floor(+p.getValue());
    return Number.isNaN(S) || Number.isNaN(M) || Number.isNaN(D) || Number.isNaN(L) || Number.isNaN(v) ? g.create(m.VALUE) : S <= 0 || M < 0 || S < M || Math.floor(N) > Math.floor(w) || D < 0 || L <= 0 || ![0, 1, 3, 4].includes(v) ? g.create(m.NUM) : (D > 1 ? D = Math.floor(D) : D = Math.ceil(D), this._getResult(S, N, w, M, D, L, v));
  }
  _getResult(e, t, r, n, s, a, u) {
    const c = e - n, l = e * a, { days: f, yearDays: h } = _e(t, r, u), d = f / h, _ = Math.ceil(c / l - d);
    if (_ < 0)
      return R.create(0);
    let C = l;
    return s === 0 ? C = l * d : s === _ ? C = c - l * (d + s - 1) : s > _ && (C = 0), R.create(C);
  }
}
class a0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = n != null ? n : R.create(0), { isError: a, errorObject: u, variants: c } = H(e, t, r, s);
    if (a)
      return u;
    const [l, f, h, d] = c, _ = $(l);
    if (typeof _ != "number")
      return _;
    const C = $(f);
    if (typeof C != "number")
      return C;
    const E = Math.floor(+h.getValue()), b = Math.floor(+d.getValue());
    if (Number.isNaN(E) || Number.isNaN(b))
      return g.create(m.VALUE);
    if (![1, 2, 4].includes(E) || b < 0 || b > 4 || Math.floor(_) >= Math.floor(C))
      return g.create(m.NUM);
    const y = fs(_, C, E, b);
    return R.create(y);
  }
}
class i0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = n != null ? n : R.create(0), { isError: a, errorObject: u, variants: c } = H(e, t, r, s);
    if (a)
      return u;
    const [l, f, h, d] = c, _ = $(l);
    if (typeof _ != "number")
      return _;
    const C = $(f);
    if (typeof C != "number")
      return C;
    const E = Math.floor(+h.getValue()), b = Math.floor(+d.getValue());
    if (Number.isNaN(E) || Number.isNaN(b))
      return g.create(m.VALUE);
    if (![1, 2, 4].includes(E) || b < 0 || b > 4 || Math.floor(_) >= Math.floor(C))
      return g.create(m.NUM);
    const y = Ze(_, C, E, b);
    return R.create(y);
  }
}
class o0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = n != null ? n : R.create(0), { isError: a, errorObject: u, variants: c } = H(e, t, r, s);
    if (a)
      return u;
    const [l, f, h, d] = c, _ = $(l);
    if (typeof _ != "number")
      return _;
    const C = $(f);
    if (typeof C != "number")
      return C;
    const E = Math.floor(+h.getValue()), b = Math.floor(+d.getValue());
    if (Number.isNaN(E) || Number.isNaN(b))
      return g.create(m.VALUE);
    if (![1, 2, 4].includes(E) || b < 0 || b > 4 || Math.floor(_) >= Math.floor(C))
      return g.create(m.NUM);
    const y = re(_), p = re(C);
    for (p.setUTCFullYear(y.getUTCFullYear()), p < y && p.setUTCFullYear(p.getUTCFullYear() + 1); p > y; )
      p.setUTCMonth(p.getUTCMonth() - 12 / E);
    p.setUTCMonth(p.getUTCMonth() + 12 / E);
    const N = Re(p), { days: w } = _e(_, N, b);
    return R.create(w);
  }
}
class u0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = n != null ? n : R.create(0), { isError: a, errorObject: u, variants: c } = H(e, t, r, s);
    if (a)
      return u;
    const [l, f, h, d] = c, _ = $(l);
    if (typeof _ != "number")
      return _;
    const C = $(f);
    if (typeof C != "number")
      return C;
    const E = Math.floor(+h.getValue()), b = Math.floor(+d.getValue());
    if (Number.isNaN(E) || Number.isNaN(b))
      return g.create(m.VALUE);
    if (![1, 2, 4].includes(E) || b < 0 || b > 4 || Math.floor(_) >= Math.floor(C))
      return g.create(m.NUM);
    const y = Yu(_, C, E);
    return R.create(y);
  }
}
class c0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = n != null ? n : R.create(0), { isError: a, errorObject: u, variants: c } = H(e, t, r, s);
    if (a)
      return u;
    const [l, f, h, d] = c, _ = $(l);
    if (typeof _ != "number")
      return _;
    const C = $(f);
    if (typeof C != "number")
      return C;
    const E = Math.floor(+h.getValue()), b = Math.floor(+d.getValue());
    if (Number.isNaN(E) || Number.isNaN(b))
      return g.create(m.VALUE);
    if (![1, 2, 4].includes(E) || b < 0 || b > 4 || Math.floor(_) >= Math.floor(C) || Ht(_, C, E) < 0)
      return g.create(m.NUM);
    const p = or(_, C, E);
    return R.create(p);
  }
}
class l0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = n != null ? n : R.create(0), { isError: a, errorObject: u, variants: c } = H(e, t, r, s);
    if (a)
      return u;
    const [l, f, h, d] = c, _ = $(l);
    if (typeof _ != "number")
      return _;
    const C = $(f);
    if (typeof C != "number")
      return C;
    const E = Math.floor(+h.getValue()), b = Math.floor(+d.getValue());
    if (Number.isNaN(E) || Number.isNaN(b))
      return g.create(m.VALUE);
    if (![1, 2, 4].includes(E) || b < 0 || b > 4 || Math.floor(_) >= Math.floor(C))
      return g.create(m.NUM);
    let y = Ht(_, C, E);
    return y < 0 && (y = 0), R.create(y);
  }
}
class f0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 6);
    A(this, "maxParams", 6);
  }
  calculate(e, t, r, n, s, a) {
    const { isError: u, errorObject: c, variants: l } = H(e, t, r, n, s, a);
    if (u)
      return c;
    const [f, h, d, _, C, E] = l, b = +f.getValue(), y = +h.getValue(), p = +d.getValue(), N = +_.getValue(), w = +C.getValue(), S = +E.getValue();
    return Number.isNaN(b) || Number.isNaN(y) || Number.isNaN(p) || Number.isNaN(N) || Number.isNaN(w) || Number.isNaN(S) ? g.create(m.VALUE) : b <= 0 || y <= 0 || p <= 0 || N < 1 || w < 1 || N > w || N > y || w > y || ![0, 1].includes(S) ? g.create(m.NUM) : Math.trunc(N) !== N && Math.trunc(w) !== w && Math.trunc(N) === Math.trunc(w) ? R.create(0) : this._getResult(b, y, p, N, w, S);
  }
  _getResult(e, t, r, n, s, a) {
    const u = zr(e, t, r, 0, a);
    let c = 0, l = Math.ceil(n);
    l === 1 && (a === 0 && (c = -r), l++);
    let f = !1;
    for (let h = l; h <= s; h++) {
      const d = a === 1 ? ur(e, h - 2, u, r, 1) : ur(e, h - 1, u, r, 0);
      if (d === 0) {
        f = !0;
        break;
      }
      c += a === 1 ? d - u : d;
    }
    return c *= e, (c < u * (s - n + 1) || f) && (c = u * (s - n + 1)), R.create(c);
  }
}
class h0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 6);
    A(this, "maxParams", 6);
  }
  calculate(e, t, r, n, s, a) {
    const { isError: u, errorObject: c, variants: l } = H(e, t, r, n, s, a);
    if (u)
      return c;
    const [f, h, d, _, C, E] = l, b = +f.getValue(), y = +h.getValue(), p = +d.getValue(), N = +_.getValue(), w = +C.getValue(), S = +E.getValue();
    return Number.isNaN(b) || Number.isNaN(y) || Number.isNaN(p) || Number.isNaN(N) || Number.isNaN(w) || Number.isNaN(S) ? g.create(m.VALUE) : b <= 0 || y <= 0 || p <= 0 || N < 1 || w < 1 || N > w || ![0, 1].includes(S) ? g.create(m.NUM) : Math.trunc(N) !== N && Math.trunc(w) !== w && Math.trunc(N) === Math.trunc(w) ? R.create(0) : this._getResult(b, y, p, N, w, S);
  }
  _getResult(e, t, r, n, s, a) {
    const u = zr(e, t, r, 0, a);
    let c = 0, l = Math.ceil(n);
    l === 1 && (c = a === 0 ? u + r * e : u, l++);
    for (let f = l; f <= s; f++)
      c += a === 1 ? u - (ur(e, f - 2, u, r, 1) - u) * e : u - ur(e, f - 1, u, r, 0) * e;
    return R.create(c);
  }
}
class m0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 5);
    A(this, "needsLocale", !0);
  }
  calculate(e, t, r, n, s) {
    let a = s != null ? s : R.create(12);
    a.isNull() && (a = R.create(12));
    const u = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1,
      a.isArray() ? a.getRowCount() : 1
    ), c = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1,
      a.isArray() ? a.getColumnCount() : 1
    ), l = O(u, c, e, g.create(m.NA)), f = O(u, c, t, g.create(m.NA)), h = O(u, c, r, g.create(m.NA)), d = O(u, c, n, g.create(m.NA)), _ = O(u, c, a, g.create(m.NA)), C = l.map((E, b, y) => {
      const p = f.get(b, y), N = h.get(b, y), w = d.get(b, y), S = _.get(b, y), { isError: M, errorObject: D, variants: L } = I(E, p, N, w, S);
      if (M)
        return D;
      const [v, j, k, G, ne] = L, z = +v.getValue(), Q = +j.getValue(), ge = +k.getValue();
      let de = +G.getValue();
      const Ee = Math.floor(+ne.getValue());
      return z < 0 || Q < 0 || ge <= 0 || de <= 0 || Math.floor(de) > Math.floor(ge) || Ee < 1 || Ee > 12 ? g.create(m.NUM) : (de < 1 && (de = 1), de = Math.floor(de), this._getResult(z, Q, ge, de, Ee, b, y));
    });
    return u === 1 && c === 1 ? C.get(0, 0) : C;
  }
  _getResult(e, t, r, n, s, a, u) {
    const c = +(1 - (t / e) ** (1 / r)).toFixed(3), l = e * c * s / 12;
    let f = l, h = 0;
    const d = n === r ? r - 1 : n;
    for (let C = 2; C <= d; C++)
      h = (e - f) * c, f += h;
    let _;
    return n === 1 ? _ = l : n === r ? _ = (e - f) * c : _ = h, Number.isNaN(_) || !Number.isFinite(_) ? g.create(m.NUM) : a === 0 && u === 0 ? R.create(_, Je(this.getLocale())) : R.create(_);
  }
}
class g0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 5);
    A(this, "needsLocale", !0);
  }
  calculate(e, t, r, n, s) {
    let a = s != null ? s : R.create(2);
    a.isNull() && (a = R.create(2));
    const u = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1,
      a.isArray() ? a.getRowCount() : 1
    ), c = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1,
      a.isArray() ? a.getColumnCount() : 1
    ), l = O(u, c, e, g.create(m.NA)), f = O(u, c, t, g.create(m.NA)), h = O(u, c, r, g.create(m.NA)), d = O(u, c, n, g.create(m.NA)), _ = O(u, c, a, g.create(m.NA)), C = l.map((E, b, y) => {
      const p = f.get(b, y), N = h.get(b, y), w = d.get(b, y), S = _.get(b, y), { isError: M, errorObject: D, variants: L } = I(E, p, N, w, S);
      if (M)
        return D;
      const [v, j, k, G, ne] = L, z = +v.getValue(), Q = +j.getValue(), ge = +k.getValue(), de = +G.getValue(), Ee = +ne.getValue();
      if (z < 0 || Q < 0 || ge <= 0 || de <= 0 || de > ge || Ee <= 0)
        return g.create(m.NUM);
      const pe = ma(z, Q, ge, de, Ee);
      return Number.isNaN(pe) || !Number.isFinite(pe) ? g.create(m.NUM) : b === 0 && y === 0 ? R.create(pe, Je(this.getLocale())) : R.create(pe);
    });
    return u === 1 && c === 1 ? C.get(0, 0) : C;
  }
}
class d0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 5);
  }
  calculate(e, t, r, n, s) {
    const a = s != null ? s : R.create(0), { isError: u, errorObject: c, variants: l } = H(e, t, r, n, a);
    if (u)
      return c;
    const [f, h, d, _, C] = l, E = $(f);
    if (typeof E != "number")
      return E;
    const b = $(h);
    if (typeof b != "number")
      return b;
    const y = +d.getValue(), p = +_.getValue(), N = Math.floor(+C.getValue());
    if (Number.isNaN(y) || Number.isNaN(p) || Number.isNaN(N))
      return g.create(m.VALUE);
    if (y <= 0 || p <= 0 || N < 0 || N > 4 || Math.floor(E) >= Math.floor(b))
      return g.create(m.NUM);
    const { days: w, yearDays: S } = _e(E, b, N), M = (p - y) / p * (S / w);
    return R.create(M);
  }
}
class C0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const { isError: r, errorObject: n, variants: s } = H(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue();
    let l = Math.floor(+u.getValue());
    if (Number.isNaN(c) || Number.isNaN(l))
      return g.create(m.VALUE);
    if (l < 0)
      return g.create(m.NUM);
    if (l >= 0 && l < 1)
      return g.create(m.DIV_BY_ZERO);
    l = Number.parseInt(`${l}`, 10);
    let f = Number.parseInt(`${c}`, 10);
    f += c % 1 * 10 ** Math.ceil(Math.log(l) / Math.LN10) / l;
    const h = 10 ** (Math.ceil(Math.log(l) / Math.LN2) + 1);
    return f = Math.round(f * h) / h, R.create(f);
  }
}
class _0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const { isError: r, errorObject: n, variants: s } = H(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue();
    let l = Math.floor(+u.getValue());
    if (Number.isNaN(c) || Number.isNaN(l))
      return g.create(m.VALUE);
    if (l < 0)
      return g.create(m.NUM);
    if (l >= 0 && l < 1)
      return g.create(m.DIV_BY_ZERO);
    l = Number.parseInt(`${l}`, 10);
    let f = Number.parseInt(`${c}`, 10);
    return f += c % 1 * 10 ** -Math.ceil(Math.log(l) / Math.LN10) * l, R.create(f);
  }
}
class A0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 5);
    A(this, "maxParams", 6);
  }
  calculate(e, t, r, n, s, a) {
    let u = a != null ? a : R.create(0);
    u.isNull() && (u = R.create(0));
    const { isError: c, errorObject: l, variants: f } = ke(e, t, r, n, s, u);
    if (c)
      return l;
    const [h, d, _, C, E, b] = f, y = $(h);
    if (typeof y != "number")
      return y;
    const p = $(d);
    if (typeof p != "number")
      return p;
    const N = +_.getValue(), w = +C.getValue(), S = Math.floor(+E.getValue()), M = Math.floor(+b.getValue());
    if (Number.isNaN(N) || Number.isNaN(w) || Number.isNaN(S) || Number.isNaN(M))
      return g.create(m.VALUE);
    if (N < 0 || w < 0 || ![1, 2, 4].includes(S) || M < 0 || M > 4 || Math.floor(y) >= Math.floor(p) || y <= 0 || p <= 366)
      return g.create(m.NUM);
    const D = Hu(y, p, N, w, S, M);
    return R.create(D);
  }
}
class E0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const { isError: r, errorObject: n, variants: s } = H(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue();
    let l = Math.floor(+u.getValue());
    if (Number.isNaN(c) || Number.isNaN(l))
      return g.create(m.VALUE);
    if (c <= 0 || l < 1)
      return g.create(m.NUM);
    l = Number.parseInt(`${l}`, 10);
    const f = (1 + c / l) ** l - 1;
    return R.create(f);
  }
}
class b0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 5);
    A(this, "needsLocale", !0);
  }
  calculate(e, t, r, n, s) {
    const a = n != null ? n : R.create(0), u = s != null ? s : R.create(0), c = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      a.isArray() ? a.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1
    ), l = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      a.isArray() ? a.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1
    ), f = O(c, l, e, g.create(m.NA)), h = O(c, l, t, g.create(m.NA)), d = O(c, l, r, g.create(m.NA)), _ = O(c, l, a, g.create(m.NA)), C = O(c, l, u, g.create(m.NA)), E = f.map((b, y, p) => {
      const N = h.get(y, p), w = d.get(y, p), S = _.get(y, p), M = C.get(y, p), { isError: D, errorObject: L, variants: v } = I(b, N, w, S, M);
      if (D)
        return L;
      const [j, k, G, ne, z] = v, Q = +j.getValue(), ge = +k.getValue(), de = +G.getValue(), Ee = +ne.getValue(), pe = +z.getValue(), me = ur(Q, ge, de, Ee, pe ? 1 : 0);
      return Number.isNaN(me) || !Number.isFinite(me) ? g.create(m.NUM) : y === 0 && p === 0 ? R.create(me, Je(this.getLocale())) : R.create(me);
    });
    return c === 1 && l === 1 ? E.get(0, 0) : E;
  }
}
class y0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const { isError: r, errorObject: n, variants: s } = H(e);
    if (r)
      return n;
    const [a] = s, u = +a.getValue();
    if (Number.isNaN(u))
      return g.create(m.VALUE);
    let c = u;
    if (t.isArray()) {
      const l = t.getArrayValue().flat();
      for (let f = 0; f < l.length; f++) {
        const h = l[f];
        if (h.isBoolean())
          return g.create(m.VALUE);
        const d = +h.getValue();
        if (Number.isNaN(d))
          return g.create(m.VALUE);
        c *= 1 + d;
      }
    } else {
      if (t.isBoolean())
        return g.create(m.VALUE);
      const l = +t.getValue();
      if (Number.isNaN(l))
        return g.create(m.VALUE);
      c *= 1 + l;
    }
    return R.create(c);
  }
}
class R0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 5);
  }
  calculate(e, t, r, n, s) {
    const a = s != null ? s : R.create(0), { isError: u, errorObject: c, variants: l } = H(e, t, r, n, a);
    if (u)
      return c;
    const [f, h, d, _, C] = l, E = $(f);
    if (typeof E != "number")
      return E;
    const b = $(h);
    if (typeof b != "number")
      return b;
    const y = +d.getValue(), p = +_.getValue(), N = Math.floor(+C.getValue());
    if (Number.isNaN(y) || Number.isNaN(p) || Number.isNaN(N))
      return g.create(m.VALUE);
    if (y <= 0 || p <= 0 || N < 0 || N > 4 || Math.floor(E) >= Math.floor(b))
      return g.create(m.NUM);
    const { days: w, yearDays: S } = _e(E, b, N), M = (p - y) / y * (S / w);
    return R.create(M);
  }
}
class p0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 6);
    A(this, "needsLocale", !0);
  }
  calculate(e, t, r, n, s, a) {
    const u = s != null ? s : R.create(0), c = a != null ? a : R.create(0), l = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1
    ), f = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1
    ), h = O(l, f, e, g.create(m.NA)), d = O(l, f, t, g.create(m.NA)), _ = O(l, f, r, g.create(m.NA)), C = O(l, f, n, g.create(m.NA)), E = O(l, f, u, g.create(m.NA)), b = O(l, f, c, g.create(m.NA)), y = h.map((p, N, w) => {
      const S = d.get(N, w), M = _.get(N, w), D = C.get(N, w), L = E.get(N, w), v = b.get(N, w), { isError: j, errorObject: k, variants: G } = I(p, S, M, D, L, v);
      if (j)
        return k;
      const [ne, z, Q, ge, de, Ee] = G, pe = +ne.getValue(), me = +z.getValue(), ve = +Q.getValue(), mt = +ge.getValue(), xt = +de.getValue(), Pt = +Ee.getValue();
      if (me < 1 || Math.floor(me) > Math.ceil(ve))
        return g.create(m.NUM);
      const ot = Qu(pe, me, ve, mt, xt, Pt ? 1 : 0);
      return Number.isNaN(ot) || !Number.isFinite(ot) ? g.create(m.NUM) : N === 0 && w === 0 ? R.create(ot, Je(this.getLocale())) : R.create(ot);
    });
    return l === 1 && f === 1 ? y.get(0, 0) : y;
  }
}
class N0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = t != null ? t : R.create(0.1);
    return r.isNull() && (r = R.create(0.1)), r.isArray() ? r.map((n, s, a) => this._handleSingleObject(e, n, s, a)) : this._handleSingleObject(e, r);
  }
  _handleSingleObject(e, t, r = 0, n = 0) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (e.isNull())
      return g.create(m.VALUE);
    if (!e.isArray())
      return g.create(m.NUM);
    const { _values: s, valuesHasError: a } = this._getValues(e);
    if (a)
      return g.create(m.VALUE);
    let u = t;
    if (u.isString() && (u = u.convertToNumberObjectValue(), u.isError()))
      return u;
    const c = +u.getValue(), { positive: l, negative: f } = this._checkValues(s);
    if (!l || !f)
      return g.create(m.NUM);
    const h = ti(c, (d) => Gu(d, s));
    return typeof h != "number" ? h : r === 0 && n === 0 ? R.create(h, "0%") : R.create(h);
  }
  _getValues(e) {
    const t = [];
    let r = !1;
    return e.iterator((n) => {
      const s = n;
      if (s.isError())
        return r = !0, !1;
      if (s.isNull() || s.isBoolean())
        return !0;
      const a = +s.getValue();
      if (Number.isNaN(a))
        return !0;
      t.push(a);
    }), {
      _values: t,
      valuesHasError: r
    };
  }
  _checkValues(e) {
    let t = !1, r = !1;
    for (let n = 0; n < e.length; n++)
      e[n] > 0 && (t = !0), e[n] < 0 && (r = !0);
    return {
      positive: t,
      negative: r
    };
  }
}
class V0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = O(s, a, n, g.create(m.NA)), h = u.map((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C), y = f.get(_, C), { isError: p, errorObject: N, variants: w } = I(d, E, b, y);
      if (p)
        return N;
      const [S, M, D, L] = w, v = +S.getValue(), j = +M.getValue(), k = +D.getValue(), G = +L.getValue();
      if (k === 0)
        return g.create(m.DIV_BY_ZERO);
      const ne = G * v * (j / k - 1);
      return R.create(ne);
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
}
class O0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 5);
    A(this, "maxParams", 6);
  }
  calculate(e, t, r, n, s, a) {
    let u = a != null ? a : R.create(0);
    u.isNull() && (u = R.create(0));
    const { isError: c, errorObject: l, variants: f } = ke(e, t, r, n, s, u);
    if (c)
      return l;
    const [h, d, _, C, E, b] = f, y = $(h);
    if (typeof y != "number")
      return y;
    const p = $(d);
    if (typeof p != "number")
      return p;
    const N = +_.getValue(), w = +C.getValue(), S = Math.floor(+E.getValue()), M = Math.floor(+b.getValue());
    if (Number.isNaN(N) || Number.isNaN(w) || Number.isNaN(S) || Number.isNaN(M))
      return g.create(m.VALUE);
    if (N < 0 || w < 0 || ![1, 2, 4].includes(S) || M < 0 || M > 4 || Math.floor(y) >= Math.floor(p))
      return g.create(m.NUM);
    let D = Hu(y, p, N, w, S, M);
    return D /= 1 + w / S, R.create(D);
  }
}
class w0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { _values: n, numberValues: s, positive: a, negative: u } = this._getValues(e), c = Math.max(
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), l = Math.max(
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), f = O(c, l, t, g.create(m.NA)), h = O(c, l, r, g.create(m.NA)), d = f.map((_, C, E) => {
      const b = h.get(C, E);
      if (e.isError())
        return e;
      if (b.isError())
        return b;
      if (n.isError())
        return n;
      if (!a || !u)
        return g.create(m.DIV_BY_ZERO);
      const y = +_.getValue(), p = +b.getValue();
      if (Number.isNaN(y) || Number.isNaN(p))
        return g.create(m.VALUE);
      if (p === -1)
        return g.create(m.DIV_BY_ZERO);
      const N = this._getResult(s, y, p);
      return C === 0 && E === 0 ? R.create(N, "0%") : R.create(N);
    });
    return c === 1 && l === 1 ? d.get(0, 0) : d;
  }
  _getValues(e) {
    let t = e, r = [], n = !1, s = !1;
    if (!e.isError())
      if (e.isNull())
        t = g.create(m.VALUE);
      else if (!e.isArray())
        t = g.create(m.DIV_BY_ZERO);
      else {
        const { numberValues: a, valuesHasError: u, errorObject: c, positive: l, negative: f } = this._checkValues(e);
        u && (t = c), r = a, n = l, s = f;
      }
    return {
      _values: t,
      numberValues: r,
      positive: n,
      negative: s
    };
  }
  _checkValues(e) {
    const t = [];
    let r = !1, n = g.create(m.VALUE), s = !1, a = !1;
    return e.iterator((u) => {
      const c = u;
      if (c.isError())
        return r = !0, n = c, !1;
      if (c.isNull() || c.isBoolean())
        return !0;
      const l = +c.getValue();
      if (Number.isNaN(l))
        return !0;
      l > 0 && (s = !0), l < 0 && (a = !0), t.push(l);
    }), {
      numberValues: t,
      valuesHasError: r,
      errorObject: n,
      positive: s,
      negative: a
    };
  }
  _getResult(e, t, r) {
    const n = e.length, s = [], a = [];
    for (let d = 0; d < n; d++)
      e[d] > 0 ? a.push(e[d]) : e[d] < 0 && s.push(e[d]);
    const u = this._npv(r, e, "positive"), c = this._npv(t, e, "negative"), l = -u * (1 + r) ** n, f = c * (1 + t);
    return (l / f) ** (1 / (n - 1)) - 1;
  }
  _npv(e, t, r) {
    let n = 0;
    for (let s = 1; s <= t.length; s++) {
      const a = t[s - 1];
      (r === "positive" && a > 0 || r === "negative" && a < 0) && (n += a / (1 + e) ** s);
    }
    return n;
  }
}
class S0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const { isError: r, errorObject: n, variants: s } = H(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue();
    let l = Math.floor(+u.getValue());
    if (Number.isNaN(c) || Number.isNaN(l))
      return g.create(m.VALUE);
    if (c <= 0 || l < 1)
      return g.create(m.NUM);
    l = Number.parseInt(`${l}`, 10);
    const f = ((c + 1) ** (1 / l) - 1) * l;
    return R.create(f);
  }
}
class M0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 5);
  }
  calculate(e, t, r, n, s) {
    const a = n != null ? n : R.create(0), u = s != null ? s : R.create(0), c = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      a.isArray() ? a.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1
    ), l = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      a.isArray() ? a.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1
    ), f = O(c, l, e, g.create(m.NA)), h = O(c, l, t, g.create(m.NA)), d = O(c, l, r, g.create(m.NA)), _ = O(c, l, a, g.create(m.NA)), C = O(c, l, u, g.create(m.NA)), E = f.map((b, y, p) => {
      const N = h.get(y, p), w = d.get(y, p), S = _.get(y, p), M = C.get(y, p), { isError: D, errorObject: L, variants: v } = I(b, N, w, S, M);
      if (D)
        return L;
      const [j, k, G, ne, z] = v, Q = +j.getValue(), ge = +k.getValue(), de = +G.getValue(), Ee = +ne.getValue();
      let pe = +z.getValue();
      if (pe = pe ? 1 : 0, Q === 0 && ge === 0)
        return g.create(m.DIV_BY_ZERO);
      let me;
      if (Q === 0)
        me = -(de + Ee) / ge;
      else {
        const ve = ge * (1 + Q * pe) - Ee * Q, mt = de * Q + ge * (1 + Q * pe);
        me = Math.log(ve / mt) / Math.log(1 + Q);
      }
      return Number.isNaN(me) || !Number.isFinite(me) ? g.create(m.NUM) : R.create(me);
    });
    return c === 1 && l === 1 ? E.get(0, 0) : E;
  }
}
class D0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 255);
    A(this, "needsLocale", !0);
  }
  calculate(e, ...t) {
    if (e.isError())
      return e;
    const { isError: r, errorObject: n, values: s } = this._getValues(t);
    return e.isArray() ? e.map((a, u, c) => this._handleSingleObject(a, r, n, s, u, c)) : this._handleSingleObject(e, r, n, s);
  }
  _handleSingleObject(e, t, r, n, s = 0, a = 0) {
    let u = e;
    if (u.isString() && (u = u.convertToNumberObjectValue()), u.isError())
      return u;
    if (t)
      return r;
    const c = +e.getValue(), l = Gu(c, n);
    return Number.isNaN(l) || Math.abs(l) === 1 / 0 ? g.create(m.DIV_BY_ZERO) : s === 0 && a === 0 ? R.create(l, Je(this.getLocale())) : R.create(l);
  }
  _getValues(e) {
    const t = [];
    for (let r = 0; r < e.length; r++) {
      const n = e[r];
      if (n.isError())
        return {
          isError: !0,
          errorObject: n
        };
      if (n.isArray()) {
        let s = !1, a = g.create(m.VALUE);
        if (n.iterator((u) => {
          const c = u;
          if (c.isError())
            return s = !0, a = c, !1;
          if (c.isNull() || c.isBoolean())
            return !0;
          const l = +c.getValue();
          if (Number.isNaN(l))
            return !0;
          t.push(l);
        }), s)
          return {
            isError: s,
            errorObject: a
          };
      } else {
        const s = +n.getValue();
        if (Number.isNaN(s))
          return {
            isError: !0,
            errorObject: g.create(m.VALUE)
          };
        t.push(s);
      }
    }
    return {
      isError: !1,
      values: t
    };
  }
}
class L0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 8);
    A(this, "maxParams", 9);
  }
  calculate(e, t, r, n, s, a, u, c, l) {
    let f = l != null ? l : R.create(0);
    f.isNull() && (f = R.create(0));
    const { isError: h, errorObject: d, variants: _ } = ke(e, t, r, n, s, a, u, c, f);
    if (h)
      return d;
    const [C, E, b, y, p, N, w, S, M] = _, D = $(C);
    if (typeof D != "number")
      return D;
    const L = $(E);
    if (typeof L != "number")
      return L;
    const v = $(b);
    if (typeof v != "number")
      return v;
    const j = $(y);
    if (typeof j != "number")
      return j;
    const k = +p.getValue(), G = +N.getValue(), ne = +w.getValue(), z = Math.floor(+S.getValue()), Q = Math.floor(+M.getValue());
    if (Number.isNaN(k) || Number.isNaN(G) || Number.isNaN(ne) || Number.isNaN(z) || Number.isNaN(Q))
      return g.create(m.VALUE);
    if (k < 0 || G < 0 || ne <= 0 || ![1, 2, 4].includes(z) || Q < 0 || Q > 4 || !this._validDate(L, j, D, v, z))
      return g.create(m.NUM);
    const ge = Wu(D, L, v, j, k, G, ne, z, Q);
    return R.create(ge);
  }
  _validDate(e, t, r, n, s) {
    return this._getDateCorrectOrder(e, t, r, n) && qu(e, t, s) && xr(n, e, s);
  }
  _getDateCorrectOrder(e, t, r, n) {
    return Math.floor(e) > Math.floor(t) && Math.floor(t) > Math.floor(r) && Math.floor(r) > Math.floor(n);
  }
}
class x0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 8);
    A(this, "maxParams", 9);
  }
  calculate(e, t, r, n, s, a, u, c, l) {
    let f = l != null ? l : R.create(0);
    f.isNull() && (f = R.create(0));
    const { isError: h, errorObject: d, variants: _ } = ke(e, t, r, n, s, a, u, c, f);
    if (h)
      return d;
    const [C, E, b, y, p, N, w, S, M] = _, D = $(C);
    if (typeof D != "number")
      return D;
    const L = $(E);
    if (typeof L != "number")
      return L;
    const v = $(b);
    if (typeof v != "number")
      return v;
    const j = $(y);
    if (typeof j != "number")
      return j;
    const k = +p.getValue(), G = +N.getValue(), ne = +w.getValue(), z = Math.floor(+S.getValue()), Q = Math.floor(+M.getValue());
    return Number.isNaN(k) || Number.isNaN(G) || Number.isNaN(ne) || Number.isNaN(z) || Number.isNaN(Q) ? g.create(m.VALUE) : k < 0 || G <= 0 || ne <= 0 || ![1, 2, 4].includes(z) || Q < 0 || Q > 4 || !this._validDate(L, j, D, v, z) ? g.create(m.NUM) : this._getResult(D, L, v, j, k, G, ne, z, Q);
  }
  _validDate(e, t, r, n, s) {
    return this._getDateCorrectOrder(e, t, r, n) && qu(e, t, s) && xr(n, e, s);
  }
  _getDateCorrectOrder(e, t, r, n) {
    return Math.floor(e) > Math.floor(t) && Math.floor(t) > Math.floor(r) && Math.floor(r) > Math.floor(n);
  }
  _getResult(e, t, r, n, s, a, u, c, l) {
    const { days: f } = _e(e, t, l), h = (s * f * 100 - (a - 100)) / ((a - 100) * 0.25 * (1 + 2 * f) + f * 100);
    function d(C) {
      return a - Wu(e, t, r, n, s, C, u, c, l);
    }
    const _ = ti(h, (C) => d(C));
    return typeof _ != "number" ? _ : R.create(_);
  }
}
class P0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 7);
    A(this, "maxParams", 8);
  }
  calculate(e, t, r, n, s, a, u, c) {
    let l = c != null ? c : R.create(0);
    l.isNull() && (l = R.create(0));
    const { isError: f, errorObject: h, variants: d } = ke(e, t, r, n, s, a, u, l);
    if (f)
      return h;
    const [_, C, E, b, y, p, N, w] = d, S = $(_);
    if (typeof S != "number")
      return S;
    const M = $(C);
    if (typeof M != "number")
      return M;
    const D = $(E);
    if (typeof D != "number")
      return D;
    const L = +b.getValue(), v = +y.getValue(), j = +p.getValue(), k = Math.floor(+N.getValue()), G = Math.floor(+w.getValue());
    if (Number.isNaN(L) || Number.isNaN(v) || Number.isNaN(j) || Number.isNaN(k) || Number.isNaN(G))
      return g.create(m.VALUE);
    if (L < 0 || v < 0 || j <= 0 || ![1, 2, 4].includes(k) || G < 0 || G > 4 || !this._validDate(M, S, D, k))
      return g.create(m.NUM);
    const ne = this._getResult(S, M, D, L, v, j, k, G);
    return R.create(ne);
  }
  _validDate(e, t, r, n) {
    return Math.floor(e) > Math.floor(t) && Math.floor(t) > Math.floor(r) && xr(r, e, n);
  }
  _getResult(e, t, r, n, s, a, u, c) {
    const l = this._getCoupDate(t, r, u), f = this._getFrac(r, e, l, u, c), h = this._getFrac(r, t, l, u, c), d = this._getFrac(e, t, l, u, c);
    return (a * u + 100 * n * (h - f * (1 + s * d / u))) / (s * d + u);
  }
  _getCoupDate(e, t, r) {
    const n = re(e), s = re(t);
    for (s.setUTCFullYear(n.getUTCFullYear()), s > n && s.setUTCFullYear(s.getUTCFullYear() - 1); s < n; )
      s.setUTCMonth(s.getUTCMonth() + 12 / r);
    return Re(s);
  }
  _getFrac(e, t, r, n, s) {
    const a = re(e), u = re(t), c = re(r);
    for (c.setUTCFullYear(a.getUTCFullYear()), c < a && c.setUTCFullYear(c.getUTCFullYear() + 1); c > a; )
      c.setUTCMonth(c.getUTCMonth() - 12 / n);
    let l = Re(c);
    c.setUTCMonth(c.getUTCMonth() + 12 / n);
    let f = Re(c);
    if (f >= t) {
      const { days: p } = _e(e, t, s), N = Ze(l, f, n, s);
      return p / N;
    }
    const { days: h } = _e(e, f, s), d = Ze(l, f, n, s);
    let _ = h / d;
    const C = re(f), E = re(f);
    for (E.setUTCMonth(E.getUTCMonth() + 12 / n); E < u; )
      C.setUTCMonth(C.getUTCMonth() + 12 / n), E.setUTCMonth(E.getUTCMonth() + 12 / n), _ += 1;
    l = Re(C), f = Re(E);
    const { days: b } = _e(l, t, s), y = Ze(l, f, n, s);
    return _ += b / y, _;
  }
}
class j0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 8);
    A(this, "maxParams", 9);
  }
  calculate(e, t, r, n, s, a, u, c) {
    let l = c != null ? c : R.create(0);
    l.isNull() && (l = R.create(0));
    const { isError: f, errorObject: h, variants: d } = ke(e, t, r, n, s, a, u, l);
    if (f)
      return h;
    const [_, C, E, b, y, p, N, w] = d, S = $(_);
    if (typeof S != "number")
      return S;
    const M = $(C);
    if (typeof M != "number")
      return M;
    const D = $(E);
    if (typeof D != "number")
      return D;
    const L = +b.getValue(), v = +y.getValue(), j = +p.getValue(), k = Math.floor(+N.getValue()), G = Math.floor(+w.getValue());
    if (Number.isNaN(L) || Number.isNaN(v) || Number.isNaN(j) || Number.isNaN(k) || Number.isNaN(G))
      return g.create(m.VALUE);
    if (L < 0 || v <= 0 || j <= 0 || ![1, 2, 4].includes(k) || G < 0 || G > 4 || !this._validDate(M, S, D, k))
      return g.create(m.NUM);
    const ne = this._getResult(S, M, D, L, v, j, k, G);
    return R.create(ne);
  }
  _validDate(e, t, r, n) {
    return Math.floor(e) > Math.floor(t) && Math.floor(t) > Math.floor(r) && xr(r, e, n);
  }
  _getResult(e, t, r, n, s, a, u, c) {
    const l = this._getCoupDate(t, r, u), f = this._getFrac(r, e, l, u, c), h = this._getFrac(r, t, l, u, c), d = this._getFrac(e, t, l, u, c);
    return (u * (a - s) + 100 * n * (h - f)) / (d * s + 100 * n * f * d / u);
  }
  _getCoupDate(e, t, r) {
    const n = re(e), s = re(t);
    for (s.setUTCFullYear(n.getUTCFullYear()), s > n && s.setUTCFullYear(s.getUTCFullYear() - 1); s < n; )
      s.setUTCMonth(s.getUTCMonth() + 12 / r);
    return Re(s);
  }
  _getFrac(e, t, r, n, s) {
    const a = re(e), u = re(t), c = re(r);
    for (c.setUTCFullYear(a.getUTCFullYear()), c < a && c.setUTCFullYear(c.getUTCFullYear() + 1); c > a; )
      c.setUTCMonth(c.getUTCMonth() - 12 / n);
    let l = Re(c);
    c.setUTCMonth(c.getUTCMonth() + 12 / n);
    let f = Re(c);
    if (f >= t) {
      const { days: p } = _e(e, t, s), N = Ze(l, f, n, s);
      return p / N;
    }
    const { days: h } = _e(e, f, s), d = Ze(l, f, n, s);
    let _ = h / d;
    const C = re(f), E = re(f);
    for (E.setUTCMonth(E.getUTCMonth() + 12 / n); E < u; )
      C.setUTCMonth(C.getUTCMonth() + 12 / n), E.setUTCMonth(E.getUTCMonth() + 12 / n), _ += 1;
    l = Re(C), f = Re(E);
    const { days: b } = _e(l, t, s), y = Ze(l, f, n, s);
    return _ += b / y, _;
  }
}
class U0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.map((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d), { isError: E, errorObject: b, variants: y } = I(f, _, C);
      if (E)
        return b;
      const [p, N, w] = y, S = +p.getValue(), M = +N.getValue(), D = +w.getValue();
      if (S <= -1)
        return g.create(m.NUM);
      const L = (Math.log(D) - Math.log(M)) / Math.log(1 + S);
      return Number.isNaN(L) || !Number.isFinite(L) ? g.create(m.NUM) : R.create(L);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
}
class v0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 5);
    A(this, "needsLocale", !0);
  }
  calculate(e, t, r, n, s) {
    const a = n != null ? n : R.create(0), u = s != null ? s : R.create(0), c = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      a.isArray() ? a.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1
    ), l = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      a.isArray() ? a.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1
    ), f = O(c, l, e, g.create(m.NA)), h = O(c, l, t, g.create(m.NA)), d = O(c, l, r, g.create(m.NA)), _ = O(c, l, a, g.create(m.NA)), C = O(c, l, u, g.create(m.NA)), E = f.map((b, y, p) => {
      const N = h.get(y, p), w = d.get(y, p), S = _.get(y, p), M = C.get(y, p), { isError: D, errorObject: L, variants: v } = I(b, N, w, S, M);
      if (D)
        return L;
      const [j, k, G, ne, z] = v, Q = +j.getValue(), ge = +k.getValue(), de = +G.getValue(), Ee = +ne.getValue(), pe = +z.getValue();
      if (Q <= -1)
        return g.create(m.NUM);
      const me = zr(Q, ge, de, Ee, pe ? 1 : 0);
      return Number.isNaN(me) || !Number.isFinite(me) ? g.create(m.NUM) : y === 0 && p === 0 ? R.create(me, Je(this.getLocale())) : R.create(me);
    });
    return c === 1 && l === 1 ? E.get(0, 0) : E;
  }
}
class T0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 6);
    A(this, "needsLocale", !0);
  }
  calculate(e, t, r, n, s, a) {
    const u = s != null ? s : R.create(0), c = a != null ? a : R.create(0), l = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1
    ), f = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1
    ), h = O(l, f, e, g.create(m.NA)), d = O(l, f, t, g.create(m.NA)), _ = O(l, f, r, g.create(m.NA)), C = O(l, f, n, g.create(m.NA)), E = O(l, f, u, g.create(m.NA)), b = O(l, f, c, g.create(m.NA)), y = h.map((p, N, w) => {
      const S = d.get(N, w), M = _.get(N, w), D = C.get(N, w), L = E.get(N, w), v = b.get(N, w), { isError: j, errorObject: k, variants: G } = I(p, S, M, D, L, v);
      if (j)
        return k;
      const [ne, z, Q, ge, de, Ee] = G, pe = +ne.getValue(), me = +z.getValue(), ve = +Q.getValue(), mt = +ge.getValue(), xt = +de.getValue(), Pt = +Ee.getValue();
      if (me < 1 || Math.floor(me) > Math.ceil(ve) || me - ve >= 1)
        return g.create(m.NUM);
      const ot = zr(pe, ve, mt, xt, Pt ? 1 : 0) - Qu(pe, me, ve, mt, xt, Pt ? 1 : 0);
      return Number.isNaN(ot) || !Number.isFinite(ot) ? g.create(m.NUM) : N === 0 && w === 0 ? R.create(ot, Je(this.getLocale())) : R.create(ot);
    });
    return l === 1 && f === 1 ? y.get(0, 0) : y;
  }
}
class B0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 6);
    A(this, "maxParams", 7);
  }
  calculate(e, t, r, n, s, a, u) {
    let c = u != null ? u : R.create(0);
    c.isNull() && (c = R.create(0));
    const { isError: l, errorObject: f, variants: h } = ke(e, t, r, n, s, a, c);
    if (l)
      return f;
    const [d, _, C, E, b, y, p] = h, N = $(d);
    if (typeof N != "number")
      return N;
    const w = $(_);
    if (typeof w != "number")
      return w;
    const S = +C.getValue(), M = +E.getValue(), D = +b.getValue(), L = Math.floor(+y.getValue()), v = Math.floor(+p.getValue());
    if (Number.isNaN(S) || Number.isNaN(M) || Number.isNaN(D) || Number.isNaN(L) || Number.isNaN(v))
      return g.create(m.VALUE);
    if (S < 0 || M < 0 || D <= 0 || ![1, 2, 4].includes(L) || v < 0 || v > 4 || N >= w || !xr(N, w, L))
      return g.create(m.NUM);
    const j = An(N, w, S, M, D, L, v);
    return R.create(j);
  }
}
class I0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 5);
  }
  calculate(e, t, r, n, s) {
    let a = s != null ? s : R.create(0);
    a.isNull() && (a = R.create(0));
    const { isError: u, errorObject: c, variants: l } = ke(e, t, r, n, a);
    if (u)
      return c;
    const [f, h, d, _, C] = l, E = $(f);
    if (typeof E != "number")
      return E;
    const b = $(h);
    if (typeof b != "number")
      return b;
    const y = +d.getValue(), p = +_.getValue(), N = Math.floor(+C.getValue());
    if (Number.isNaN(y) || Number.isNaN(p) || Number.isNaN(N))
      return g.create(m.VALUE);
    if (y <= 0 || p <= 0 || N < 0 || N > 4 || E >= b)
      return g.create(m.NUM);
    const { days: w, yearDays: S } = _e(E, b, N), M = p - y * p * w / S;
    return R.create(M);
  }
}
class F0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 5);
    A(this, "maxParams", 6);
  }
  calculate(e, t, r, n, s, a) {
    let u = a != null ? a : R.create(0);
    u.isNull() && (u = R.create(0));
    const { isError: c, errorObject: l, variants: f } = ke(e, t, r, n, s, u);
    if (c)
      return l;
    const [h, d, _, C, E, b] = f, y = $(h);
    if (typeof y != "number")
      return y;
    const p = $(d);
    if (typeof p != "number")
      return p;
    const N = $(_);
    if (typeof N != "number")
      return N;
    const w = +C.getValue(), S = +E.getValue(), M = Math.floor(+b.getValue());
    if (Number.isNaN(w) || Number.isNaN(S) || Number.isNaN(M))
      return g.create(m.VALUE);
    const D = this._getDateCorrectOrder(p, y, N);
    if (w < 0 || S < 0 || M < 0 || M > 4 || !D)
      return g.create(m.NUM);
    const L = $u(y, M), { days: v } = _e(y, p, M), { days: j } = _e(N, p, M), { days: k } = _e(N, y, M), G = (100 + j / L * w * 100) / (1 + v / L * S) - k / L * w * 100;
    return R.create(G);
  }
  _getDateCorrectOrder(e, t, r) {
    return Math.floor(e) > Math.floor(t) && Math.floor(t) > Math.floor(r);
  }
}
class k0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 5);
    A(this, "needsLocale", !0);
  }
  calculate(e, t, r, n, s) {
    const a = n != null ? n : R.create(0), u = s != null ? s : R.create(0), c = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      a.isArray() ? a.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1
    ), l = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      a.isArray() ? a.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1
    ), f = O(c, l, e, g.create(m.NA)), h = O(c, l, t, g.create(m.NA)), d = O(c, l, r, g.create(m.NA)), _ = O(c, l, a, g.create(m.NA)), C = O(c, l, u, g.create(m.NA)), E = f.map((b, y, p) => {
      const N = h.get(y, p), w = d.get(y, p), S = _.get(y, p), M = C.get(y, p), { isError: D, errorObject: L, variants: v } = I(b, N, w, S, M);
      if (D)
        return L;
      const [j, k, G, ne, z] = v, Q = +j.getValue(), ge = +k.getValue(), de = +G.getValue(), Ee = +ne.getValue();
      let pe = +z.getValue();
      pe = pe ? 1 : 0;
      const me = Q === 0 ? -de * ge - Ee : ((1 - (1 + Q) ** ge) / Q * de * (1 + Q * pe) - Ee) / (1 + Q) ** ge;
      return Number.isNaN(me) || !Number.isFinite(me) ? g.create(m.NUM) : y === 0 && p === 0 ? R.create(me, Je(this.getLocale())) : R.create(me);
    });
    return c === 1 && l === 1 ? E.get(0, 0) : E;
  }
}
class $0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 6);
  }
  calculate(e, t, r, n, s, a) {
    const u = n != null ? n : R.create(0), c = s != null ? s : R.create(0), l = a != null ? a : R.create(0.1), f = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1,
      l.isArray() ? l.getRowCount() : 1
    ), h = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1,
      l.isArray() ? l.getColumnCount() : 1
    ), d = O(f, h, e, g.create(m.NA)), _ = O(f, h, t, g.create(m.NA)), C = O(f, h, r, g.create(m.NA)), E = O(f, h, u, g.create(m.NA)), b = O(f, h, c, g.create(m.NA)), y = O(f, h, l, g.create(m.NA)), p = d.map((N, w, S) => {
      const M = _.get(w, S), D = C.get(w, S), L = E.get(w, S), v = b.get(w, S), j = y.get(w, S), { isError: k, errorObject: G, variants: ne } = I(N, M, D, L, v, j);
      if (k)
        return G;
      const [z, Q, ge, de, Ee, pe] = ne, me = +z.getValue(), ve = +Q.getValue(), mt = +ge.getValue(), xt = +de.getValue();
      let Pt = +Ee.getValue();
      const ot = +pe.getValue();
      return Pt = Pt ? 1 : 0, me <= 0 || ve >= 0 && mt >= 0 && xt >= 0 || ve <= 0 && mt <= 0 && xt <= 0 ? g.create(m.NUM) : this._getResult(me, ve, mt, xt, Pt, ot, w, S);
    });
    return f === 1 && h === 1 ? p.get(0, 0) : p;
  }
  _getResult(e, t, r, n, s, a, u, c) {
    let h = a;
    for (let d = 0; d < 20; d++) {
      if (h <= -1)
        return g.create(m.NUM);
      let _, C;
      if (Math.abs(h) < 1e-10 ? _ = r * (1 + e * h) + t * (1 + h * s) * e + n : (C = (1 + h) ** e, _ = r * C + t * (1 / h + s) * (C - 1) + n), Math.abs(_) < 1e-10)
        break;
      let E;
      if (Math.abs(h) < 1e-10)
        E = r * e + t * s * e;
      else {
        C = (1 + h) ** e;
        const b = e * (1 + h) ** (e - 1);
        E = r * b + t * (1 / h + s) * b + t * (-1 / (h * h)) * (C - 1);
      }
      h -= _ / E;
    }
    return u === 0 && c === 0 ? R.create(h, "0%") : R.create(h);
  }
}
class Y0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 5);
  }
  calculate(e, t, r, n, s) {
    let a = s != null ? s : R.create(0);
    a.isNull() && (a = R.create(0));
    const { isError: u, errorObject: c, variants: l } = ke(e, t, r, n, a);
    if (u)
      return c;
    const [f, h, d, _, C] = l, E = $(f);
    if (typeof E != "number")
      return E;
    const b = $(h);
    if (typeof b != "number")
      return b;
    const y = +d.getValue(), p = +_.getValue(), N = Math.floor(+C.getValue());
    if (Number.isNaN(y) || Number.isNaN(p) || Number.isNaN(N))
      return g.create(m.VALUE);
    if (y <= 0 || p <= 0 || N < 0 || N > 4 || E >= b)
      return g.create(m.NUM);
    const { days: w, yearDays: S } = _e(E, b, N), M = y / (1 - p * w / S);
    return M < 0 ? g.create(m.NUM) : R.create(M);
  }
}
class H0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 6);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.map((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d), { isError: E, errorObject: b, variants: y } = I(f, _, C);
      if (E)
        return b;
      const [p, N, w] = y, S = +p.getValue(), M = +N.getValue(), D = +w.getValue();
      if (S <= 0)
        return g.create(m.NUM);
      if (M === 0 && D === 0)
        return R.create(0);
      const L = (D / M) ** (1 / S) - 1;
      return Number.isNaN(L) || !Number.isFinite(L) || D / M < 0 ? g.create(m.NUM) : R.create(L);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
}
class Q0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
    A(this, "needsLocale", !0);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.map((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      if (_.isError())
        return _;
      if (C.isError())
        return C;
      const E = +f.getValue(), b = +_.getValue(), y = +C.getValue();
      if (Number.isNaN(E) || Number.isNaN(b) || Number.isNaN(y))
        return g.create(m.VALUE);
      if (y === 0)
        return g.create(m.DIV_BY_ZERO);
      const p = (E - b) / y;
      return h === 0 && d === 0 ? R.create(p, Je(this.getLocale())) : R.create(p);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
}
class G0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
    A(this, "needsLocale", !0);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = O(s, a, n, g.create(m.NA)), h = u.map((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C), y = f.get(_, C);
      if (E.isError())
        return E;
      if (b.isError())
        return b;
      if (y.isError())
        return y;
      const p = +d.getValue(), N = +E.getValue(), w = +b.getValue(), S = +y.getValue();
      if (Number.isNaN(p) || Number.isNaN(N) || Number.isNaN(w) || Number.isNaN(S))
        return g.create(m.VALUE);
      if (N < 0 || w <= 0 || S > w)
        return g.create(m.NUM);
      const M = (p - N) * (w - S + 1) * 2 / (w * (w + 1));
      return _ === 0 && C === 0 ? R.create(M, Je(this.getLocale())) : R.create(M);
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
}
class W0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = ke(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = $(u);
    if (typeof f != "number")
      return f;
    const h = $(c);
    if (typeof h != "number")
      return h;
    const d = +l.getValue();
    if (Number.isNaN(d))
      return g.create(m.VALUE);
    if (d <= 0 || f >= h)
      return g.create(m.NUM);
    const _ = Math.floor(h) - Math.floor(f), E = re(f).getUTCFullYear(), b = ls(E);
    if (_ > b)
      return g.create(m.NUM);
    let y = 365 * d / (360 - d * _);
    if (_ > 182) {
      const p = 100 * (1 - d * _ / 360), N = _ / 365;
      if (y = (-N + Math.sqrt(N * N - (N * 2 - 1) * (1 - 100 / p))) / (N - 0.5), Number.isNaN(y))
        return g.create(m.NUM);
    }
    return y < 0 ? g.create(m.NUM) : R.create(y);
  }
}
class q0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
    A(this, "needsLocale", !0);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = ke(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = $(u);
    if (typeof f != "number")
      return f;
    const h = $(c);
    if (typeof h != "number")
      return h;
    const d = +l.getValue();
    if (Number.isNaN(d))
      return g.create(m.VALUE);
    if (d <= 0 || f >= h)
      return g.create(m.NUM);
    const _ = Math.floor(h) - Math.floor(f), E = re(f).getUTCFullYear(), b = ls(E);
    if (_ > b)
      return g.create(m.NUM);
    const y = 100 * (1 - d * _ / 360);
    return y < 0 ? g.create(m.NUM) : R.create(y, Je(this.getLocale()));
  }
}
class K0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = ke(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = $(u);
    if (typeof f != "number")
      return f;
    const h = $(c);
    if (typeof h != "number")
      return h;
    const d = +l.getValue();
    if (Number.isNaN(d))
      return g.create(m.VALUE);
    if (d <= 0 || f >= h)
      return g.create(m.NUM);
    const _ = Math.floor(h) - Math.floor(f), E = re(f).getUTCFullYear(), b = ls(E);
    if (_ > b)
      return g.create(m.NUM);
    const y = (100 - d) / d * 360 / _;
    return R.create(y);
  }
}
class X0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 5);
    A(this, "maxParams", 7);
    A(this, "needsLocale", !0);
  }
  calculate(e, t, r, n, s, a, u) {
    let c = a != null ? a : R.create(2);
    c.isNull() && (c = R.create(2));
    let l = u != null ? u : T.create(!1);
    l.isNull() && (l = T.create(!1));
    const f = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1,
      l.isArray() ? l.getRowCount() : 1
    ), h = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1,
      l.isArray() ? l.getColumnCount() : 1
    ), d = O(f, h, e, g.create(m.NA)), _ = O(f, h, t, g.create(m.NA)), C = O(f, h, r, g.create(m.NA)), E = O(f, h, n, g.create(m.NA)), b = O(f, h, s, g.create(m.NA)), y = O(f, h, c, g.create(m.NA)), p = O(f, h, l, g.create(m.NA));
    return this._getResultArray(
      d,
      _,
      C,
      E,
      b,
      y,
      p,
      f,
      h
    );
  }
  _getResultArray(e, t, r, n, s, a, u, c, l) {
    const f = e.map((h, d, _) => {
      const C = t.get(d, _), E = r.get(d, _), b = n.get(d, _), y = s.get(d, _), p = a.get(d, _), N = u.get(d, _), { isError: w, errorObject: S, variants: M } = I(h, C, E, b, y, p, N);
      if (w)
        return S;
      const [D, L, v, j, k, G, ne] = M, z = +D.getValue(), Q = +L.getValue(), ge = +v.getValue(), de = +j.getValue(), Ee = +k.getValue(), pe = +G.getValue(), me = +ne.getValue();
      if (z < 0 || Q < 0 || ge < 0 || de < 0 || Ee < 0 || Ee > ge || de > Ee || pe < 0)
        return g.create(m.NUM);
      if (ge === 0 && de === 0 && Ee === 0)
        return g.create(m.DIV_BY_ZERO);
      const ve = this._getResult(z, Q, ge, de, Ee, pe, me);
      return d === 0 && _ === 0 ? R.create(ve, Je(this.getLocale())) : R.create(ve);
    });
    return c === 1 && l === 1 ? f.get(0, 0) : f;
  }
  _getResult(e, t, r, n, s, a, u) {
    const c = Math.floor(n), l = Math.ceil(s);
    let f = 0;
    if (e < t) {
      if (n >= 1 || u)
        return f;
      const h = Math.abs(e - t);
      return f = h * (s - n) > h ? h : h * (s - n), -f;
    }
    if (u)
      for (let h = c + 1; h <= l; h++) {
        let d = ma(e, t, r, h, a);
        h === c + 1 ? d *= Math.min(s, c + 1) - n : h === l && (d *= s + 1 - l), f += d;
      }
    else {
      const h = e - this._getVdb(e, t, r, r, n, a);
      f = this._getVdb(h, t, r, r - n, s - n, a);
    }
    return f;
  }
  _getVdb(e, t, r, n, s, a) {
    const u = Math.ceil(s);
    let c = 0, l = e - t, f = 0, h = 0, d = !1;
    for (let _ = 1; _ <= u; _++) {
      if (d)
        h = f;
      else {
        const C = ma(e, t, r, _, a);
        f = l / (n - (_ - 1)), f > C ? (h = f, d = !0) : (h = C, l -= C);
      }
      _ === u && (h *= s + 1 - u), c += h;
    }
    return c;
  }
}
class Z0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    if (e.isNull() || t.isNull())
      return g.create(m.NA);
    const { isError: n, errorObejct: s, _values: a, _dates: u } = this._checkErrors(e, t);
    if (n)
      return s;
    let c = r != null ? r : R.create(0.1);
    c.isNull() && (c = R.create(0.1));
    const { isError: l, errorObject: f, variants: h } = H(c);
    if (l)
      return f;
    const [d] = h, _ = +d.getValue();
    if (Number.isNaN(_))
      return g.create(m.VALUE);
    const { positive: C, negative: E } = this._checkValues(a);
    if (!C || !E || (a == null ? void 0 : a.length) !== (u == null ? void 0 : u.length) || _ < 0)
      return g.create(m.NUM);
    const b = ti(_, (y) => this._iterF(a, u, y));
    return typeof b != "number" ? b : R.create(b);
  }
  _checkErrors(e, t) {
    if (e.isError())
      return {
        isError: !0,
        errorObejct: e
      };
    if (t.isError())
      return {
        isError: !0,
        errorObejct: t
      };
    const { isError: r, errorObejct: n, _values: s } = this._checkErrorValues(e);
    if (r)
      return {
        isError: r,
        errorObejct: n
      };
    const { isError: a, errorObejct: u, _dates: c } = this._checkErrorDates(t);
    return a ? {
      isError: a,
      errorObejct: u
    } : {
      isError: !1,
      _values: s,
      _dates: c
    };
  }
  _checkErrorValues(e) {
    const t = [];
    if (e.isArray()) {
      let r = !1, n = g.create(m.VALUE);
      return e.iterator((s) => {
        const a = s;
        if (a.isError())
          return r = !0, n = a, !1;
        if (a.isBoolean())
          return r = !0, n = g.create(m.VALUE), !1;
        const u = +a.getValue();
        if (Number.isNaN(u))
          return r = !0, n = g.create(m.VALUE), !1;
        t.push(u);
      }), r ? {
        isError: r,
        errorObejct: n
      } : t.length <= 1 ? {
        isError: !0,
        errorObejct: g.create(m.NA)
      } : {
        isError: r,
        _values: t
      };
    } else {
      const r = e.getValue();
      return e.isBoolean() || e.isString() && !q(r) ? {
        isError: !0,
        errorObejct: g.create(m.VALUE)
      } : {
        isError: !0,
        errorObejct: g.create(m.NA)
      };
    }
  }
  _checkErrorDates(e) {
    const t = [];
    if (e.isArray()) {
      let r = !1, n = g.create(m.VALUE);
      return e.iterator((s) => {
        if (s != null && s.isError())
          return r = !0, n = s, !1;
        if (s != null && s.isBoolean())
          return r = !0, n = g.create(m.VALUE), !1;
        const a = +s.getValue();
        if (Number.isNaN(a))
          return r = !0, n = g.create(m.VALUE), !1;
        if (a < 0)
          return r = !0, n = g.create(m.NUM), !1;
        t.push(Math.floor(a));
      }), r ? {
        isError: r,
        errorObejct: n
      } : t.length <= 1 ? {
        isError: !0,
        errorObejct: g.create(m.NA)
      } : {
        isError: r,
        _dates: t
      };
    } else {
      const r = e.getValue();
      return e.isBoolean() || e.isString() && !q(r) ? {
        isError: !0,
        errorObejct: g.create(m.VALUE)
      } : +r < 0 ? {
        isError: !0,
        errorObejct: g.create(m.NUM)
      } : {
        isError: !0,
        errorObejct: g.create(m.NA)
      };
    }
  }
  _checkValues(e) {
    let t = !1, r = !1;
    for (let n = 0; n < e.length; n++)
      e[n] > 0 && (t = !0), e[n] < 0 && (r = !0);
    return {
      positive: t,
      negative: r
    };
  }
  _iterF(e, t, r) {
    return e.reduce((n, s, a) => n + s / (1 + r) ** ((t[a] - t[0]) / 365), 0);
  }
}
class z0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    if (e.isNull() || t.isNull() || r.isNull())
      return g.create(m.NA);
    const { isError: n, errorObject: s, variants: a } = H(e);
    if (n)
      return s;
    const [u] = a, c = +u.getValue();
    if (Number.isNaN(c))
      return g.create(m.VALUE);
    let { isError: l, errorObejct: f, _values: h, _dates: d } = this._checkErrors(t, r);
    if (l)
      return f;
    if (h = h, d = d, c < 0 || h.length !== d.length)
      return g.create(m.NUM);
    let _ = 0;
    const C = d[0];
    for (let E = 0; E < d.length; E++) {
      const b = d[E], y = h[E];
      _ += y / (1 + c) ** ((b - C) / 365);
    }
    return R.create(_);
  }
  _checkErrors(e, t) {
    if (e.isError())
      return {
        isError: !0,
        errorObejct: e
      };
    if (t.isError())
      return {
        isError: !0,
        errorObejct: t
      };
    const { isError: r, errorObejct: n, _values: s } = this._checkErrorValues(e);
    if (r)
      return {
        isError: r,
        errorObejct: n
      };
    const { isError: a, errorObejct: u, _dates: c } = this._checkErrorDates(t);
    return a ? {
      isError: a,
      errorObejct: u
    } : {
      isError: !1,
      _values: s,
      _dates: c
    };
  }
  _checkErrorValues(e) {
    const t = [];
    if (e.isArray()) {
      let r = !1, n = g.create(m.VALUE);
      return e.iterator((s) => {
        const a = s;
        if (a.isError())
          return r = !0, n = a, !1;
        if (a.isNull() || a.isBoolean())
          return r = !0, n = g.create(m.VALUE), !1;
        const u = +a.getValue();
        if (Number.isNaN(u))
          return r = !0, n = g.create(m.VALUE), !1;
        t.push(u);
      }), r ? {
        isError: r,
        errorObejct: n
      } : {
        isError: r,
        _values: t
      };
    } else {
      if (e.isError())
        return {
          isError: !0,
          errorObejct: e
        };
      if (e.isNull() || e.isBoolean())
        return {
          isError: !0,
          errorObejct: g.create(m.VALUE)
        };
      const r = +e.getValue();
      return Number.isNaN(r) ? {
        isError: !0,
        errorObejct: g.create(m.VALUE)
      } : {
        isError: !1,
        _values: [r]
      };
    }
  }
  _checkErrorDates(e) {
    const t = [];
    if (e.isArray()) {
      let r = !1, n = g.create(m.VALUE);
      return e.iterator((s) => {
        const a = s;
        if (a.isError())
          return r = !0, n = a, !1;
        if (a.isNull() || a.isBoolean())
          return r = !0, n = g.create(m.VALUE), !1;
        const u = +a.getValue();
        if (Number.isNaN(u))
          return r = !0, n = g.create(m.VALUE), !1;
        if (u < 0)
          return r = !0, n = g.create(m.NUM), !1;
        t.push(Math.floor(u));
      }), r ? {
        isError: r,
        errorObejct: n
      } : {
        isError: r,
        _dates: t
      };
    } else {
      if (e.isError())
        return {
          isError: !0,
          errorObejct: e
        };
      if (e.isNull() || e.isBoolean())
        return {
          isError: !0,
          errorObejct: g.create(m.VALUE)
        };
      const r = +e.getValue();
      return Number.isNaN(r) ? {
        isError: !0,
        errorObejct: g.create(m.VALUE)
      } : +r < 0 ? {
        isError: !0,
        errorObejct: g.create(m.NUM)
      } : {
        isError: !1,
        _dates: [Math.floor(r)]
      };
    }
  }
}
class J0 extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 6);
    A(this, "maxParams", 7);
  }
  calculate(e, t, r, n, s, a, u) {
    let c = u != null ? u : R.create(0);
    c.isNull() && (c = R.create(0));
    const { isError: l, errorObject: f, variants: h } = ke(e, t, r, n, s, a, c);
    if (l)
      return f;
    const [d, _, C, E, b, y, p] = h, N = $(d);
    if (typeof N != "number")
      return N;
    const w = $(_);
    if (typeof w != "number")
      return w;
    const S = +C.getValue(), M = +E.getValue(), D = +b.getValue(), L = Math.floor(+y.getValue()), v = Math.floor(+p.getValue());
    if (Number.isNaN(S) || Number.isNaN(M) || Number.isNaN(D) || Number.isNaN(L) || Number.isNaN(v))
      return g.create(m.VALUE);
    if (S < 0 || M <= 0 || D <= 0 || ![1, 2, 4].includes(L) || v < 0 || v > 4 || N >= w || !xr(N, w, L))
      return g.create(m.NUM);
    const j = this._getResult(N, w, S, M, D, L, v);
    return R.create(j);
  }
  _getResult(e, t, r, n, s, a, u) {
    if (or(e, t, a) > 1) {
      let E = r || 0.01, b = An(e, t, r, E, s, a, u), y = b - n;
      for (let p = 0; p < 100 && Math.abs(y) > 1e-7; p++)
        b = An(e, t, r, 1.01 * E, s, a, u), E += -y / (b - n - y) * E * 0.01, y = An(e, t, r, E, s, a, u) - n;
      return E;
    }
    const l = fs(e, t, a, u), f = Ze(e, t, a, u), { days: h } = _e(e, t, u), d = n / 100 + l / f * r / a;
    return (s / 100 + r / a - d) / d * a * f / h;
  }
}
class ed extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 5);
  }
  calculate(e, t, r, n, s) {
    let a = s != null ? s : R.create(0);
    a.isNull() && (a = R.create(0));
    const { isError: u, errorObject: c, variants: l } = ke(e, t, r, n, a);
    if (u)
      return c;
    const [f, h, d, _, C] = l, E = $(f);
    if (typeof E != "number")
      return E;
    const b = $(h);
    if (typeof b != "number")
      return b;
    const y = +d.getValue(), p = +_.getValue(), N = Math.floor(+C.getValue());
    if (Number.isNaN(y) || Number.isNaN(p) || Number.isNaN(N))
      return g.create(m.VALUE);
    if (y <= 0 || p <= 0 || N < 0 || N > 4 || E >= b)
      return g.create(m.NUM);
    const { days: w, yearDays: S } = _e(E, b, N), M = (p / y - 1) / (w / S);
    return R.create(M);
  }
}
class td extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 5);
    A(this, "maxParams", 6);
  }
  calculate(e, t, r, n, s, a) {
    let u = a != null ? a : R.create(0);
    u.isNull() && (u = R.create(0));
    const { isError: c, errorObject: l, variants: f } = ke(e, t, r, n, s, u);
    if (c)
      return l;
    const [h, d, _, C, E, b] = f, y = $(h);
    if (typeof y != "number")
      return y;
    const p = $(d);
    if (typeof p != "number")
      return p;
    const N = $(_);
    if (typeof N != "number")
      return N;
    const w = +C.getValue(), S = +E.getValue(), M = Math.floor(+b.getValue());
    if (Number.isNaN(w) || Number.isNaN(S) || Number.isNaN(M))
      return g.create(m.VALUE);
    const D = this._getDateCorrectOrder(p, y, N);
    if (w < 0 || S <= 0 || M < 0 || M > 4 || !D)
      return g.create(m.NUM);
    const L = $u(y, M), { days: v } = _e(y, p, M), { days: j } = _e(N, p, M), { days: k } = _e(N, y, M), G = ((1 + j / L * w) / (S / 100 + k / L * w) - 1) / (v / L);
    return R.create(G);
  }
  _getDateCorrectOrder(e, t, r) {
    return Math.floor(e) > Math.floor(t) && Math.floor(t) > Math.floor(r);
  }
}
const rd = [
  [r0, ee.ACCRINT],
  [n0, ee.ACCRINTM],
  [s0, ee.AMORLINC],
  [a0, ee.COUPDAYBS],
  [i0, ee.COUPDAYS],
  [o0, ee.COUPDAYSNC],
  [u0, ee.COUPNCD],
  [c0, ee.COUPNUM],
  [l0, ee.COUPPCD],
  [f0, ee.CUMIPMT],
  [h0, ee.CUMPRINC],
  [m0, ee.DB],
  [g0, ee.DDB],
  [d0, ee.DISC],
  [C0, ee.DOLLARDE],
  [_0, ee.DOLLARFR],
  [A0, ee.DURATION],
  [E0, ee.EFFECT],
  [b0, ee.FV],
  [y0, ee.FVSCHEDULE],
  [R0, ee.INTRATE],
  [p0, ee.IPMT],
  [N0, ee.IRR],
  [V0, ee.ISPMT],
  [O0, ee.MDURATION],
  [w0, ee.MIRR],
  [S0, ee.NOMINAL],
  [M0, ee.NPER],
  [D0, ee.NPV],
  [L0, ee.ODDFPRICE],
  [x0, ee.ODDFYIELD],
  [P0, ee.ODDLPRICE],
  [j0, ee.ODDLYIELD],
  [U0, ee.PDURATION],
  [v0, ee.PMT],
  [T0, ee.PPMT],
  [B0, ee.PRICE],
  [I0, ee.PRICEDISC],
  [F0, ee.PRICEMAT],
  [k0, ee.PV],
  [$0, ee.RATE],
  [Y0, ee.RECEIVED],
  [H0, ee.RRI],
  [Q0, ee.SLN],
  [G0, ee.SYD],
  [W0, ee.TBILLEQ],
  [q0, ee.TBILLPRICE],
  [K0, ee.TBILLYIELD],
  [X0, ee.VDB],
  [Z0, ee.XIRR],
  [z0, ee.XNPV],
  [J0, ee.YIELD],
  [ed, ee.YIELDDISC],
  [td, ee.YIELDMAT]
];
class nd extends V {
  constructor() {
    super(...arguments);
    A(this, "needsReferenceObject", !0);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = e;
    if (r.isError())
      return r;
    if (r.isReferenceObject() && (r = r.toArrayValueObject()), r.isArray()) {
      const n = r.getRowCount(), s = r.getColumnCount();
      if (n === 1 && s === 1) {
        const a = r.get(0, 0);
        return this._handleSingleObject(a, t);
      }
      return r.map((a) => a.isError() ? a : this._handleSingleObject(a, t, !0));
    }
    return this._handleSingleObject(r, t);
  }
  _handleSingleObject(e, t, r = !1) {
    let n = t;
    if (n.isError())
      return n;
    if (!n.isReferenceObject())
      return g.create(m.NA);
    const s = n.getCurrentActiveSheetData(), { columnData: a, defaultColumnWidth: u } = s;
    n = n.toArrayValueObject();
    const c = n.getCurrentRow(), l = n.getCurrentColumn();
    n = n.getFirstCell();
    const f = `${e.getValue()}`;
    let h;
    switch (f.toLocaleLowerCase()) {
      case "address":
        return U.create(`$${Ke.chatAtABC(l)}$${c + 1}`);
      case "col":
        return R.create(l + 1);
      case "color":
        return R.create(0);
      case "contents":
        return n;
      case "filename":
        return g.create(m.VALUE);
      case "format":
        return U.create("G");
      case "parentheses":
        return R.create(0);
      case "prefix":
        return U.create("");
      case "protect":
        return R.create(1);
      case "row":
        return R.create(c + 1);
      case "type":
        return h = "v", n.isNull() && (h = "b"), n.isString() && (h = "l"), U.create(h);
      case "width":
        return this._getWidthResult(a, u, l, r);
      default:
        return g.create(m.VALUE);
    }
  }
  _getWidthResult(e, t, r, n) {
    var u;
    let s = (u = e[r]) == null ? void 0 : u.w;
    if (!s && s !== 0 && (s = t), n)
      return R.create(s);
    const a = [[s, s === t]];
    return W.createByArray(a);
  }
}
class sd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
    A(this, "_errorTypeValueMap", /* @__PURE__ */ new Map([
      [m.NULL, 1],
      [m.DIV_BY_ZERO, 2],
      [m.VALUE, 3],
      [m.REF, 4],
      [m.NAME, 5],
      [m.NUM, 6],
      [m.NA, 7],
      [m.CONNECT, 8],
      [m.CALC, 14]
    ]));
  }
  calculate(e) {
    return e.isArray() ? e.mapValue((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const t = e.getValue(), r = this._errorTypeValueMap.get(t);
    return r ? R.create(r) : g.create(m.NA);
  }
}
var Oe = /* @__PURE__ */ ((o) => (o.CELL = "CELL", o.ERROR_TYPE = "ERROR.TYPE", o.INFO = "INFO", o.ISBETWEEN = "ISBETWEEN", o.ISBLANK = "ISBLANK", o.ISDATE = "ISDATE", o.ISEMAIL = "ISEMAIL", o.ISERR = "ISERR", o.ISERROR = "ISERROR", o.ISEVEN = "ISEVEN", o.ISFORMULA = "ISFORMULA", o.ISLOGICAL = "ISLOGICAL", o.ISNA = "ISNA", o.ISNONTEXT = "ISNONTEXT", o.ISNUMBER = "ISNUMBER", o.ISODD = "ISODD", o.ISOMITTED = "ISOMITTED", o.ISREF = "ISREF", o.ISTEXT = "ISTEXT", o.ISURL = "ISURL", o.N = "N", o.NA = "NA", o.SHEET = "SHEET", o.SHEETS = "SHEETS", o.TYPE = "TYPE", o))(Oe || {});
class ad extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 5);
  }
  calculate(e, t, r, n, s) {
    const a = n != null ? n : T.create(!0), u = s != null ? s : T.create(!0), { isError: c, errorObject: l, variants: f } = Wr(e, t, r, a, u);
    if (c)
      return l;
    const [h, d, _, C, E] = f;
    if (C.isString() || E.isString())
      return g.create(m.VALUE);
    if (d.compare(_, ">").getValue() === !0)
      return g.create(m.NUM);
    const y = +C.getValue(), p = +E.getValue(), N = y ? ">=" : ">", w = p ? "<=" : "<";
    return h.compare(d, N).getValue() === !1 || h.compare(_, w).getValue() === !1 ? T.create(!1) : T.create(!0);
  }
}
class id extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isNull() ? T.create(!0) : e.isArray() ? e.mapValue((t) => t.isNull() ? T.create(!0) : T.create(!1)) : T.create(!1);
  }
}
class od extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    if (t.isArray()) {
      const r = t.getRowCount(), n = t.getColumnCount();
      if (r > 1 || n > 1)
        return g.create(m.VALUE);
      t = t.get(0, 0);
    }
    return t.isError() ? t : t.isNumber() && t.getPattern() !== "" ? T.create(ei(t.getPattern())) : t.isString() && (vn(`${t.getValue()}`) || Tn(`${t.getValue()}`)) ? T.create(!0) : T.create(!1);
  }
}
class ud extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    if (t.isArray()) {
      const a = t.getRowCount(), u = t.getColumnCount();
      if (a > 1 || u > 1)
        return g.create(m.VALUE);
      t = t.get(0, 0);
    }
    if (t.isError())
      return t;
    if (t.isNull() || t.isBoolean() || t.isNumber())
      return T.create(!1);
    const r = `${t.getValue()}`;
    if (r.length > 254)
      return T.create(!1);
    const n = Ke.topLevelDomainCombiningString(), s = new RegExp(
      `^(?:[\\w+-]+\\.)*[\\w+-]+@[a-z0-9_-]+(?:\\.[a-z0-9_-]+)*\\.(?:${n})$`,
      "i"
    );
    return T.create(s.test(r));
  }
}
class cd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.getValue() === m.NA ? T.create(!1) : e.isError() ? T.create(!0) : e.isArray() ? e.mapValue((t) => t.getValue() === m.NA ? T.create(!1) : t.isError() ? T.create(!0) : T.create(!1)) : T.create(!1);
  }
}
class ld extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isError() ? T.create(!0) : e.isArray() ? e.mapValue((t) => t.isError() ? T.create(!0) : T.create(!1)) : T.create(!1);
  }
}
class fd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    if (t.isArray()) {
      const s = t.getRowCount(), a = t.getColumnCount();
      if (s > 1 || a > 1)
        return g.create(m.VALUE);
      t = t.get(0, 0);
    }
    if (t.isError())
      return t;
    if (t.isBoolean())
      return g.create(m.VALUE);
    const r = Math.trunc(+t.getValue());
    if (Number.isNaN(r))
      return g.create(m.VALUE);
    const n = r % 2 === 0;
    return T.create(n);
  }
}
class hd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e) {
    var u;
    if (e.isError())
      return e;
    if (!e.isReferenceObject())
      return g.create(m.NA);
    const t = (u = e.getCurrentActiveSheetData()) == null ? void 0 : u.cellData.getMatrix(), { startRow: r, startColumn: n } = e.getRangePosition(), s = e.toArrayValueObject(), a = s.mapValue((c, l, f) => {
      var d;
      const h = (d = t == null ? void 0 : t[r + l]) == null ? void 0 : d[n + f];
      return h != null && h.f || h != null && h.si ? T.create(!0) : T.create(!1);
    });
    return s.getRowCount() === 1 && s.getColumnCount() === 1 ? a.get(0, 0) : a;
  }
}
class md extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isBoolean() ? T.create(!0) : e.isArray() ? e.mapValue((t) => t.isBoolean() ? T.create(!0) : T.create(!1)) : T.create(!1);
  }
}
class gd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.getValue() === m.NA ? T.create(!0) : e.isArray() ? e.mapValue((t) => t.getValue() === m.NA ? T.create(!0) : T.create(!1)) : T.create(!1);
  }
}
class dd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return !e.isArray() && !e.isString() ? T.create(!0) : e.isArray() ? e.mapValue((t) => t.isString() ? T.create(!1) : T.create(!0)) : T.create(!1);
  }
}
class Cd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isNumber() ? T.create(!0) : e.isArray() ? e.mapValue((t) => t.isNumber() ? T.create(!0) : T.create(!1)) : T.create(!1);
  }
}
class _d extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    if (t.isArray()) {
      const s = t.getRowCount(), a = t.getColumnCount();
      if (s > 1 || a > 1)
        return g.create(m.VALUE);
      t = t.get(0, 0);
    }
    if (t.isError())
      return t;
    if (t.isBoolean())
      return g.create(m.VALUE);
    const r = Math.trunc(+t.getValue());
    if (Number.isNaN(r))
      return g.create(m.VALUE);
    const n = r % 2 !== 0;
    return T.create(n);
  }
}
class Ad extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e) {
    return e.isReferenceObject() ? T.create(!0) : T.create(!1);
  }
}
class Ed extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isString() ? T.create(!0) : e.isArray() ? e.mapValue((t) => t.isString() ? T.create(!0) : T.create(!1)) : T.create(!1);
  }
}
class bd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    if (t.isArray()) {
      const a = t.getRowCount(), u = t.getColumnCount();
      if (a > 1 || u > 1)
        return g.create(m.VALUE);
      t = t.get(0, 0);
    }
    if (t.isError())
      return t;
    if (t.isNull() || t.isBoolean() || t.isNumber())
      return T.create(!1);
    const r = `${t.getValue()}`.replace(/^\s+|\s+$/g, "");
    if (r.length > 1e3)
      return T.create(!1);
    const n = Ke.topLevelDomainCombiningString(), s = new RegExp(
      `^(?:(?:https?|s?ftp|ftps|nfs|ssh)://+[a-z0-9_-]+(?:\\.[a-z0-9_-]+)*(?::[0-9]+)?(?:/(?:[A-Za-z0-9\\-._~!$&'()*+,;=:@]|%[A-Fa-f0-9]{2})*)*/?(?:[?#]\\S*)?|[a-z0-9_-]+(?:\\.[a-z0-9_-]+)*\\.(?:${n})(?::[0-9]+)?(?:/(?:[A-Za-z0-9\\-._~!$&'()*+,;=:@]|%[A-Fa-f0-9]{2})*)*/?(?:[?#]\\S*)?|mailto:(?:[\\w+-]+\\.)*[\\w+-]+@[a-z0-9_-]+(?:\\.[a-z0-9_-]+)*\\.(?:${n})|(?:news|aim):[%a-z0-9$_\\.+!*(),;/?#:@&~=-]+)$`,
      "i"
    );
    return T.create(s.test(r));
  }
}
class yd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    if (e.isArray() && (t = e.get(0, 0)), t.isError())
      return t;
    if (t.isString())
      return R.create(0);
    const r = +t.getValue();
    return R.create(r);
  }
}
class Rd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 0);
    A(this, "maxParams", 0);
  }
  calculate() {
    return g.create(m.NA);
  }
}
class pd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 0);
    A(this, "maxParams", 1);
    A(this, "needsReferenceObject", !0);
    A(this, "needsSheetsInfo", !0);
  }
  calculate(e) {
    var u;
    if (e != null && e.isError())
      return e;
    const { sheetOrder: t, sheetNameMap: r } = this.getSheetsInfo();
    if (!e) {
      const c = t.findIndex((l) => l === this.subUnitId);
      return R.create(c + 1);
    }
    if (e.isReferenceObject()) {
      const c = e.getForcedSheetId(), l = e.getDefaultSheetId(), f = t.findIndex((h) => c ? h === c : h === l);
      return R.create(f + 1);
    }
    if (e.isArray())
      return g.create(m.NA);
    const n = `${e.getValue()}`.toLocaleLowerCase(), s = (u = Object.entries(r).find(([c, l]) => l.toLocaleLowerCase() === n)) == null ? void 0 : u[0];
    if (!s)
      return g.create(m.NA);
    const a = t.findIndex((c) => c === s);
    return R.create(a + 1);
  }
}
class Nd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 0);
    A(this, "maxParams", 0);
    A(this, "needsSheetsInfo", !0);
  }
  calculate() {
    const { sheetOrder: e } = this.getSheetsInfo();
    return R.create(e.length);
  }
}
class Vd extends V {
  constructor() {
    super(...arguments);
    A(this, "needsReferenceObject", !0);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isReferenceObject()) {
      const t = e.getRowCount(), r = e.getColumnCount();
      if (t === 1 && r === 1) {
        const n = e.getFirstCell();
        if (n.isError())
          return R.create(16);
        if (n.isBoolean())
          return R.create(4);
        if (n.isString())
          return R.create(2);
        if (n.isNumber() || n.isNull())
          return R.create(1);
      } else
        return R.create(64);
    } else {
      if (e.isArray())
        return R.create(64);
      if (e.isError())
        return R.create(16);
      if (e.isBoolean())
        return R.create(4);
      if (e.isString())
        return R.create(2);
      if (e.isNumber() || e.isNull())
        return R.create(1);
    }
    return R.create(128);
  }
}
const Od = [
  [nd, Oe.CELL],
  [sd, Oe.ERROR_TYPE],
  [ad, Oe.ISBETWEEN],
  [id, Oe.ISBLANK],
  [od, Oe.ISDATE],
  [ud, Oe.ISEMAIL],
  [cd, Oe.ISERR],
  [ld, Oe.ISERROR],
  [fd, Oe.ISEVEN],
  [hd, Oe.ISFORMULA],
  [md, Oe.ISLOGICAL],
  [gd, Oe.ISNA],
  [dd, Oe.ISNONTEXT],
  [Cd, Oe.ISNUMBER],
  [_d, Oe.ISODD],
  [Ad, Oe.ISREF],
  [Ed, Oe.ISTEXT],
  [bd, Oe.ISURL],
  [yd, Oe.N],
  [Rd, Oe.NA],
  [pd, Oe.SHEET],
  [Nd, Oe.SHEETS],
  [Vd, Oe.TYPE]
];
class wd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = !0, r = !0, n = null;
    for (const s of e) {
      if (s.isError())
        return s;
      if (s.isArray()) {
        if (s.iterator((a) => {
          if (a != null && a.isError())
            return n = a, !1;
          (a != null && a.isBoolean() || a != null && a.isNumber()) && (t = t && !!a.getValue(), r = !1);
        }), n)
          return n;
      } else (s.isBoolean() || s.isNumber()) && (t = t && !!s.getValue(), r = !1);
    }
    return r ? g.create(m.VALUE) : T.create(t);
  }
}
class Sd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
    A(this, "needsReferenceObject", !0);
  }
  // eslint-disable-next-line
  calculate(e, t) {
    let r = e, n = null;
    if (e.isReferenceObject() && (r = e.toArrayValueObject(), n = e), r.isError())
      return r;
    if (t.isError())
      return t;
    if (!(t.isValueObject() && t.isLambda() && t.getLambdaPrivacyVarKeys().length === 1))
      return g.create(m.VALUE);
    const s = t, a = r.isArray() ? r.getRowCount() : 1, u = r.isArray() ? r.getColumnCount() : 1;
    r = O(a, u, r);
    const c = [[]];
    for (let l = 0; l < u; l++) {
      const f = [];
      for (let _ = 0; _ < a; _++) {
        const C = r.get(_, l);
        f.push([C]);
      }
      let h = W.create({
        calculateValueList: f,
        rowCount: a,
        columnCount: 1,
        unitId: "",
        sheetId: "",
        row: 0,
        column: 0
      });
      if (n) {
        const { startRow: _, startColumn: C } = n.getRangePosition(), E = {
          startRow: _,
          startColumn: C + l,
          endRow: _ + a - 1,
          endColumn: C + l
        };
        h = this.createReferenceObject(n, E);
      }
      let d = s.execute(h);
      if (d.isArray()) {
        const _ = d.getRowCount(), C = d.getColumnCount();
        if (_ > 1 || C > 1)
          return g.create(m.CALC);
        d = d.get(0, 0);
      }
      d.isNull() && (d = R.create(0)), c[0].push(d);
    }
    return u === 1 ? c[0][0] : W.create({
      calculateValueList: c,
      rowCount: 1,
      columnCount: u,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
}
class Md extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
    A(this, "needsReferenceObject", !0);
  }
  // eslint-disable-next-line
  calculate(e, t) {
    let r = e, n = null;
    if (e.isReferenceObject() && (r = e.toArrayValueObject(), n = e), r.isError())
      return r;
    if (t.isError())
      return t;
    if (!(t.isValueObject() && t.isLambda() && t.getLambdaPrivacyVarKeys().length === 1))
      return g.create(m.VALUE);
    const s = t, a = r.isArray() ? r.getRowCount() : 1, u = r.isArray() ? r.getColumnCount() : 1;
    r = O(a, u, r);
    const c = [];
    for (let l = 0; l < a; l++) {
      const f = [[]];
      for (let _ = 0; _ < u; _++) {
        const C = r.get(l, _);
        f[0].push(C);
      }
      let h = W.create({
        calculateValueList: f,
        rowCount: 1,
        columnCount: u,
        unitId: "",
        sheetId: "",
        row: 0,
        column: 0
      });
      if (n) {
        const { startRow: _, startColumn: C } = n.getRangePosition(), E = {
          startRow: _ + l,
          startColumn: C,
          endRow: _ + l,
          endColumn: C + u - 1
        };
        h = this.createReferenceObject(n, E);
      }
      let d = s.execute(h);
      if (d.isArray()) {
        const _ = d.getRowCount(), C = d.getColumnCount();
        if (_ > 1 || C > 1)
          return g.create(m.CALC);
        d = d.get(0, 0);
      }
      d.isNull() && (d = R.create(0)), c.push([d]);
    }
    return a === 1 ? c[0][0] : W.create({
      calculateValueList: c,
      rowCount: a,
      columnCount: 1,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
}
class Dd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 0);
    A(this, "maxParams", 0);
  }
  calculate() {
    return T.create(!1);
  }
}
var Le = /* @__PURE__ */ ((o) => (o.AND = "AND", o.BYCOL = "BYCOL", o.BYROW = "BYROW", o.FALSE = "FALSE", o.IF = "IF", o.IFERROR = "IFERROR", o.IFNA = "IFNA", o.IFS = "IFS", o.LAMBDA = "LAMBDA", o.LET = "LET", o.MAKEARRAY = "MAKEARRAY", o.MAP = "MAP", o.NOT = "NOT", o.OR = "OR", o.REDUCE = "REDUCE", o.SCAN = "SCAN", o.SWITCH = "SWITCH", o.TRUE = "TRUE", o.XOR = "XOR", o))(Le || {});
class Ld extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r = T.create(!1)) {
    let n = e;
    if (e.isArray()) {
      const f = e.getRowCount(), h = e.getColumnCount();
      f === 1 && h === 1 && (n = e.get(0, 0));
    }
    if (!n.isArray())
      return this._handleSingleObject(n, t, r);
    const s = Math.max(
      n.isArray() ? n.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), a = Math.max(
      n.isArray() ? n.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), u = O(s, a, n, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA));
    return u.mapValue((f, h, d) => {
      const _ = c.get(h, d), C = l.get(h, d);
      return this._handleSingleObject(f, _, C);
    });
  }
  _handleSingleObject(e, t, r) {
    return e.isError() ? e : e.getValue() ? t.isNull() ? R.create(0) : t : r.isNull() ? R.create(0) : r;
  }
}
class xd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (!e.isArray())
      return e.isError() ? t : e;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e), a = O(r, n, t);
    return s.iterator((u, c, l) => {
      u != null && u.isError() && s.set(c, l, a.get(c, l));
    }), s;
  }
}
class Pd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError() && e.getErrorType() !== m.NA)
      return e;
    if (t.isError())
      return t;
    if (!e.isArray())
      return e.isError() && e.getErrorType() === m.NA ? t : e;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e), a = O(r, n, t);
    return s.iterator((u, c, l) => {
      u != null && u.isError() && u.getErrorType() === m.NA && s.set(c, l, a.get(c, l));
    }), s;
  }
}
class jd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    if (e.length % 2 !== 0)
      return g.create(m.NA);
    for (let a = 0; a < e.length; a++)
      if (e[a].isError())
        return e[a];
    const t = Math.max(
      ...e.map((a) => a.isArray() ? a.getRowCount() : 1)
    ), r = Math.max(
      ...e.map((a) => a.isArray() ? a.getColumnCount() : 1)
    ), n = e.map(
      (a) => O(t, r, a, g.create(m.NA))
    ), s = n[0].map((a, u, c) => {
      for (let l = 0; l < n.length; l += 2) {
        const f = n[l].get(u, c) || ue.create(), h = n[l + 1].get(u, c) || ue.create();
        if (f.isNull())
          continue;
        if (f.isError())
          return f;
        const d = f.getValue();
        if (f.isString()) {
          if (`${d}`.toLocaleUpperCase() === "TRUE")
            return h;
          if (`${d}`.toLocaleUpperCase() === "FALSE")
            continue;
          return g.create(m.VALUE);
        }
        if (+d)
          return h.isNull() ? g.create(m.NA) : h;
      }
      return g.create(m.NA);
    });
    return t === 1 && r === 1 ? s.get(0, 0) : s;
  }
}
class Ud extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    return g.create(m.VALUE);
  }
}
class vd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    return g.create(m.VALUE);
  }
}
class Td extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(...e) {
    const t = this.getIndexNumValue(e[0]);
    if (typeof t != "number")
      return t;
    const r = this.getIndexNumValue(e[1]);
    if (typeof r != "number")
      return r;
    if (!(e[2].isValueObject() && e[2].isLambda()))
      return g.create(m.VALUE);
    const n = e[2], s = [];
    for (let a = 0; a < t; a++) {
      s[a] == null && (s[a] = []);
      for (let u = 0; u < r; u++) {
        let c = n.execute(R.create(a + 1), R.create(u + 1));
        c.isArray() && (c = c.get(0, 0)), s[a][u] = c;
      }
    }
    return new Jf(s);
  }
  isAsync() {
    return !0;
  }
}
let Bd = class extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 255);
    A(this, "needsReferenceObject", !0);
  }
  calculate(...e) {
    const t = [];
    let r = null, n = 0, s = 0;
    for (let u = 0; u < e.length; u++) {
      const c = e[u];
      let l = c;
      if (l.isReferenceObject() && (l = c.toArrayValueObject()), l.isError())
        return l;
      if (u === e.length - 1) {
        if (!(c.isValueObject() && c.isLambda()))
          return g.create(m.VALUE);
        r = c;
      } else
        t.push(c), n = Math.max(n, l.isArray() ? l.getRowCount() : 1), s = Math.max(s, l.isArray() ? l.getColumnCount() : 1);
    }
    const a = this._getResultArray(t, r, n, s);
    return a instanceof g ? a : n === 1 && s === 1 ? a[0][0] : W.create({
      calculateValueList: a,
      rowCount: n,
      columnCount: s,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
  // eslint-disable-next-line
  _getResultArray(e, t, r, n) {
    const s = [];
    for (let a = 0; a < r; a++) {
      const u = [];
      for (let c = 0; c < n; c++) {
        const l = [];
        let f = !1;
        for (let d = 0; d < e.length; d++) {
          let _ = e[d], C = null;
          if (_.isReferenceObject() && (C = _, _ = _.toArrayValueObject()), !_.isArray())
            if (a === 0 && c === 0) {
              l.push(C || _);
              continue;
            } else {
              u.push(g.create(m.NA)), f = !0;
              break;
            }
          let E = _.get(a, c);
          if (!E) {
            u.push(g.create(m.NA)), f = !0;
            break;
          }
          if (C) {
            const { startRow: b, startColumn: y } = C.getRangePosition(), p = {
              startRow: b + a,
              startColumn: y + c,
              endRow: b + a,
              endColumn: y + c
            };
            E = this.createReferenceObject(C, p);
          }
          l.push(E);
        }
        if (f || l.length === 0)
          continue;
        let h = t.execute(...l);
        if (h.isArray()) {
          const d = h.getRowCount(), _ = h.getColumnCount();
          if (d > 1 || _ > 1)
            return g.create(m.CALC);
          h = h.get(0, 0);
        }
        h.isNull() && (h = R.create(0)), u.push(h);
      }
      s.push(u);
    }
    return s;
  }
};
class Id extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    const t = +e.getValue();
    return Number.isNaN(t) ? g.create(m.VALUE) : T.create(!t);
  }
}
class Fd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = !1, r = !0, n = null;
    for (const s of e) {
      if (s.isError())
        return s;
      if (s.isArray()) {
        if (s.iterator((a) => {
          if (a != null && a.isError())
            return n = a, !1;
          (a != null && a.isBoolean() || a != null && a.isNumber()) && (t = t || !!a.getValue(), r = !1);
        }), n)
          return n;
      } else (s.isBoolean() || s.isNumber()) && (t = t || !!s.getValue(), r = !1);
    }
    return r ? new g(m.VALUE) : new T(t);
  }
}
class kd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, t, r) {
    let n, s;
    e.isReferenceObject() ? (n = e.toArrayValueObject(), s = e) : (n = e, s = null);
    let a, u;
    return t.isReferenceObject() ? (a = t.toArrayValueObject(), u = t) : (a = t, u = null), n.isArray() ? n.mapValue((c) => this._handleSingleValueObject(c, a, r, s, u)) : this._handleSingleValueObject(n, a, r, s, u);
  }
  _handleSingleValueObject(e, t, r, n, s) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (r.isError())
      return r;
    if (!(r.isValueObject() && r.isLambda() && r.getLambdaPrivacyVarKeys().length === 2))
      return g.create(m.VALUE);
    const a = r, u = t.isArray() ? t.getRowCount() : 1, c = t.isArray() ? t.getColumnCount() : 1;
    let l = e;
    n && (l = n);
    for (let f = 0; f < u; f++)
      for (let h = 0; h < c; h++) {
        let d = t.isArray() ? t.get(f, h) : t;
        if (d.isError())
          return d;
        if (s) {
          const { startRow: C, startColumn: E } = s.getRangePosition(), b = {
            startRow: C + f,
            startColumn: E + h,
            endRow: C + f,
            endColumn: E + h
          };
          d = this.createReferenceObject(s, b);
        }
        let _ = a.execute(l, d);
        if (_.isError())
          return _;
        _.isNull() && (_ = R.create(0)), l = _;
      }
    return l.isReferenceObject() ? l.toArrayValueObject() : l;
  }
}
class $d extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, t, r) {
    let n = e, s = null;
    e.isReferenceObject() && (n = e.toArrayValueObject(), s = e), n = n;
    let a = t, u = null;
    if (t.isReferenceObject() && (a = t.toArrayValueObject(), u = t), a = a, n.isError())
      return n;
    if (a.isError())
      return a;
    if (r.isError())
      return r;
    if (!(r.isValueObject() && r.isLambda() && r.getLambdaPrivacyVarKeys().length === 2))
      return g.create(m.VALUE);
    if (n.isArray()) {
      const c = n.getRowCount(), l = n.getColumnCount();
      if (c > 1 || l > 1)
        return g.create(m.CALC);
      n = n.get(0, 0);
    }
    return this._getResult(n, a, r, s, u);
  }
  // eslint-disable-next-line
  _getResult(e, t, r, n, s) {
    const a = [], u = t.isArray() ? t.getRowCount() : 1, c = t.isArray() ? t.getColumnCount() : 1;
    let l = e;
    n && (l = n);
    for (let f = 0; f < u; f++) {
      const h = [];
      for (let d = 0; d < c; d++) {
        if (l.isError()) {
          h.push(l);
          continue;
        }
        let _ = t.isArray() ? t.get(f, d) : t;
        if (_.isError()) {
          l = _, h.push(_);
          continue;
        }
        if (s) {
          const { startRow: E, startColumn: b } = s.getRangePosition(), y = {
            startRow: E + f,
            startColumn: b + d,
            endRow: E + f,
            endColumn: b + d
          };
          _ = this.createReferenceObject(s, y);
        }
        let C = r.execute(l, _);
        if (C.isArray()) {
          const E = C.getRowCount(), b = C.getColumnCount();
          if (E > 1 || b > 1)
            return g.create(m.CALC);
          C = C.get(0, 0);
        }
        C.isNull() && (C = R.create(0)), l = C, h.push(C);
      }
      a.push(h);
    }
    return u === 1 && c === 1 ? a[0][0] : W.create({
      calculateValueList: a,
      rowCount: u,
      columnCount: c,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
}
class Yd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
  }
  calculate(e, ...t) {
    if (e.isError())
      return e;
    const r = t.length % 2 !== 0, n = r ? t[t.length - 1] : ue.create();
    return !e.isArray() && !t.some((s) => s.isArray()) ? this._handleNonArrayInputs(e, t, n, r) : this._handleArrayInputs(e, t, n, r);
  }
  _handleNonArrayInputs(e, t, r, n) {
    for (let s = 0; s < t.length - (n ? 1 : 0); s += 2) {
      const a = t[s], u = t[s + 1];
      if (!a.isNull()) {
        if (a.isError())
          return a;
        if (`${e.getValue()}`.toLocaleLowerCase() === `${a.getValue()}`.toLocaleLowerCase())
          return u.isNull() ? g.create(m.NA) : u;
      }
    }
    return r.isNull() ? g.create(m.NA) : r;
  }
  _handleArrayInputs(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      ...t.map((f) => f.isArray() ? f.getRowCount() : 1),
      r.isArray() ? r.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      ...t.map((f) => f.isArray() ? f.getColumnCount() : 1),
      r.isArray() ? r.getColumnCount() : 1
    ), u = O(s, a, e), c = t.map(
      (f) => O(s, a, f, g.create(m.NA))
    ), l = O(s, a, r, g.create(m.NA));
    return u.map((f, h, d) => {
      for (let C = 0; C < c.length - (n ? 1 : 0); C += 2) {
        const E = c[C].get(h, d) || ue.create(), b = c[C + 1].get(h, d) || ue.create();
        if (!E.isNull()) {
          if (E.isError() || f.isError())
            return E.isError() ? E : f;
          if (`${f.getValue()}`.toLocaleLowerCase() === `${E.getValue()}`.toLocaleLowerCase())
            return b.isNull() ? g.create(m.NA) : b;
        }
      }
      const _ = l.get(h, d) || ue.create();
      return _.isNull() ? g.create(m.NA) : _;
    });
  }
}
class Hd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 0);
    A(this, "maxParams", 0);
  }
  calculate() {
    return T.create(!0);
  }
}
class Qd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = 0, r = !0, n = null;
    for (const s of e) {
      if (s.isError())
        return s;
      if (s.isArray()) {
        if (s.iterator((a) => {
          if (a != null && a.isError())
            return n = a, !1;
          (a != null && a.isBoolean() || a != null && a.isNumber()) && (a.getValue() && t++, r = !1);
        }), n)
          return n;
      } else (s.isBoolean() || s.isNumber()) && (s.getValue() && t++, r = !1);
    }
    return r ? g.create(m.VALUE) : T.create(t % 2 === 1);
  }
}
const Gd = [
  [wd, Le.AND],
  [Sd, Le.BYCOL],
  [Md, Le.BYROW],
  [Dd, Le.FALSE],
  [Ld, Le.IF],
  [xd, Le.IFERROR],
  [Pd, Le.IFNA],
  [jd, Le.IFS],
  [Ud, Le.LAMBDA],
  [vd, Le.LET],
  [Td, Le.MAKEARRAY],
  [Bd, Le.MAP],
  [Id, Le.NOT],
  [Fd, Le.OR],
  [kd, Le.REDUCE],
  [$d, Le.SCAN],
  [Yd, Le.SWITCH],
  [Hd, Le.TRUE],
  [Qd, Le.XOR]
];
class Wd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 5);
  }
  // eslint-disable-next-line max-lines-per-function
  calculate(e, t, r, n, s) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (r != null && r.isError())
      return r;
    if (n != null && n.isError())
      return n;
    if (s != null && s.isError())
      return s;
    const a = r != null ? r : R.create(1), u = n != null ? n : T.create(!0), c = s != null ? s : U.create(""), l = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      a.isArray() ? a.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1
    ), f = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      a.isArray() ? a.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1
    ), h = O(l, f, e, g.create(m.NA)), d = O(l, f, t, g.create(m.NA)), _ = O(l, f, a, g.create(m.NA)), C = O(l, f, u, g.create(m.NA)), E = O(l, f, c, g.create(m.NA));
    return h.map((b, y, p) => {
      const N = d.get(y, p) || g.create(m.NA), w = _.get(y, p) || g.create(m.NA), S = C.get(y, p) || g.create(m.NA), M = E.get(y, p) || g.create(m.NA);
      return b.isError() ? b : N.isError() ? N : w.isError() ? w : S.isError() ? S : M.isError() ? M : this._calculateSingleCell(b, N, w, S, M);
    });
  }
  _calculateSingleCell(e, t, r, n, s) {
    const a = Number.parseInt(`${Number(e.getValue()) - 1}`), u = Number.parseInt(`${Number(t.getValue()) - 1}`), c = Number.parseInt(`${Number(r.getValue())}`);
    if (Number.isNaN(a) || Number.isNaN(u) || Number.isNaN(c) || c < 1 || c > 4)
      return g.create(m.VALUE);
    const l = qd(c), f = this.getZeroOrOneByOneDefault(n), h = `${s.getValue()}`, d = Eo(h), _ = {
      startRow: a,
      startColumn: u,
      endRow: a,
      endColumn: u,
      startAbsoluteRefType: l,
      endAbsoluteRefType: l
    }, C = n && !f ? lm(_) : $t(_);
    return U.create(d !== "" ? `${d}!${C}` : C);
  }
}
function qd(o) {
  switch (o) {
    case 1:
      return Me.ALL;
    case 2:
      return Me.ROW;
    case 3:
      return Me.COLUMN;
    case 4:
      return Me.NONE;
    default:
      return Me.ALL;
  }
}
class Kd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e) {
    return e.isReferenceObject() ? R.create(1) : g.create(m.VALUE);
  }
}
class Xd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 255);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, ...t) {
    let r = e;
    if (r.isError())
      return r;
    if (r.isReferenceObject() && (r = r.toArrayValueObject()), !r.isArray()) {
      const c = r.convertToNumberObjectValue();
      return c.isError() ? c : t[Math.trunc(+c.getValue()) - 1] || g.create(m.VALUE);
    }
    let n = r.isArray() ? r.getRowCount() : 1, s = r.isArray() ? r.getColumnCount() : 1;
    t.forEach((c, l) => {
      if (c.isArray()) {
        const f = c;
        n = Math.max(n, f.getRowCount()), s = Math.max(s, f.getColumnCount());
      } else
        n = Math.max(n, 1), s = Math.max(s, 1);
    });
    const a = O(n, s, r, g.create(m.NA)), u = t.map((c) => {
      let l = c;
      return l.isReferenceObject() && (l = l.toArrayValueObject()), O(n, s, l, g.create(m.NA));
    });
    return a.map((c, l, f) => {
      if (c.isError())
        return c;
      const h = c.convertToNumberObjectValue();
      if (h.isError())
        return h;
      const d = u[Math.trunc(+h.getValue()) - 1];
      let _ = (d == null ? void 0 : d.get(l, f)) || g.create(m.VALUE);
      return _ != null && _.isNull() && (_ = R.create(0)), _;
    });
  }
}
class Zd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 255);
  }
  calculate(e, ...t) {
    if (e.isError())
      return e;
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = [];
    for (let a = 0; a < t.length; a++) {
      let u = t[a];
      if (u.isArray()) {
        const f = u.getRowCount(), h = u.getColumnCount();
        if (f > 1 || h > 1)
          return g.create(m.VALUE);
        u = u.get(0, 0);
      }
      if (u.isString() && (u = u.convertToNumberObjectValue()), u.isError())
        return u;
      const c = Math.trunc(+u.getValue());
      if (c === 0 || Math.abs(c) > n)
        return g.create(m.VALUE);
      let l = e;
      n > 1 && (c < 0 ? l = e.slice(void 0, [c + n, c + 1 + n]) : l = e.slice(void 0, [c - 1, c]));
      for (let f = 0; f < r; f++)
        s[f] || (s[f] = []), e.isArray() ? s[f].push(l.get(f, 0)) : s[f].push(e);
    }
    return W.create({
      calculateValueList: s,
      rowCount: s.length,
      columnCount: s[0].length || 0,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
}
class zd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 255);
  }
  calculate(e, ...t) {
    if (e.isError())
      return e;
    const r = e.isArray() ? e.getRowCount() : 1, n = [];
    for (let s = 0; s < t.length; s++) {
      let a = t[s];
      if (a.isArray()) {
        const l = a.getRowCount(), f = a.getColumnCount();
        if (l > 1 || f > 1)
          return g.create(m.VALUE);
        a = a.get(0, 0);
      }
      if (a.isString() && (a = a.convertToNumberObjectValue()), a.isError())
        return a;
      const u = Math.trunc(+a.getValue());
      if (u === 0 || Math.abs(u) > r)
        return g.create(m.VALUE);
      let c = e;
      r > 1 && (u < 0 ? c = e.slice([u + r, u + 1 + r]) : c = e.slice([u - 1, u])), e.isArray() ? n.push(c.getArrayValue()[0]) : n.push([e]);
    }
    return W.create({
      calculateValueList: n,
      rowCount: n.length,
      columnCount: n[0].length || 0,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
}
class Jd extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 0);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e == null)
      return R.create(this.column + 1);
    if (e.isError())
      return e;
    if (!e.isArray())
      return g.create(m.NA);
    const t = e.getCurrentColumn(), r = e.getColumnCount(), n = [];
    for (let a = 0; a < r; a++)
      n.push(R.create(t + a + 1));
    const s = {
      calculateValueList: [n],
      rowCount: 1,
      columnCount: r,
      unitId: this.unitId || "",
      sheetId: this.subUnitId || "",
      row: this.row,
      column: this.column
    };
    return W.create(s);
  }
}
class eC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isError())
      return e;
    if (e.isString() || e.isNumber() || e.isBoolean())
      return R.create(1);
    if (!e.isArray())
      return g.create(m.NA);
    const t = e.getColumnCount();
    return R.create(t);
  }
}
class tC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = r != null ? r : R.create(0), s = e.isArray() ? e.getRowCount() : 1, a = e.isArray() ? e.getColumnCount() : 1, u = Math.max(
      t.isArray() ? t.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), c = Math.max(
      t.isArray() ? t.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), l = O(u, c, t, g.create(m.NA)), f = O(u, c, n, g.create(m.NA));
    if (u > 1 || c > 1)
      return l.mapValue((y, p, N) => {
        const w = f.get(p, N);
        if (e.isError())
          return e;
        if (e.isNull())
          return g.create(m.VALUE);
        const { isError: S, errorObject: M } = this._checkRowsColumns(y, w, s, a);
        return S ? M : e.isArray() ? g.create(m.VALUE) : e;
      });
    if (e.isError())
      return e;
    if (e.isNull())
      return g.create(m.VALUE);
    const h = t.isArray() ? t.get(0, 0) : t, d = n.isArray() ? n.get(0, 0) : n, { isError: _, errorObject: C, rowsValue: E, columnsValue: b } = this._checkRowsColumns(h, d, s, a);
    return _ ? C : this._getResultArray(e, E, b, s, a);
  }
  _checkRowsColumns(e, t, r, n) {
    if (e.isError())
      return {
        isError: !0,
        errorObject: e
      };
    if (t.isError())
      return {
        isError: !0,
        errorObject: t
      };
    const s = Math.trunc(+e.getValue()), a = Math.trunc(+t.getValue());
    return Number.isNaN(s) || Number.isNaN(a) ? {
      isError: !0,
      errorObject: g.create(m.VALUE)
    } : Math.abs(s) >= r || Math.abs(a) >= n ? {
      isError: !0,
      errorObject: g.create(m.CALC)
    } : {
      isError: !1,
      rowsValue: s,
      columnsValue: a
    };
  }
  _getResultArray(e, t, r, n, s) {
    const a = t >= 0 ? [t, n] : [0, n + t], u = r >= 0 ? [r, s] : [0, s + r];
    let c;
    return t === 0 && r === 0 ? c = e : t === 0 ? c = e.slice(void 0, u) : r === 0 ? c = e.slice(a, void 0) : c = e.slice(a, u), c = c.map((l) => l.isNull() ? R.create(0) : l), n - t === 1 && s - r === 1 ? c.get(0, 0) : c;
  }
}
class rC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = e.isArray() ? e.getRowCount() : 1, a = e.isArray() ? e.getColumnCount() : 1;
    let u = t, c = r != null ? r : R.create(a);
    const l = n != null ? n : g.create(m.NA);
    t.isNull() && (u = R.create(s)), c.isNull() && (c = R.create(a));
    const f = Math.max(
      u.isArray() ? u.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1
    ), h = Math.max(
      u.isArray() ? u.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1
    ), d = O(f, h, u, g.create(m.NA)), _ = O(f, h, c, g.create(m.NA));
    if (f > 1 || h > 1)
      return d.mapValue((S, M, D) => {
        const L = _.get(M, D);
        if (e.isError())
          return e;
        if (e.isNull())
          return g.create(m.VALUE);
        const { isError: v, errorObject: j } = this._checkRowsColumnsPadWith(S, L, l, s, a);
        return v ? j : e.isArray() ? e.get(0, 0) : e;
      });
    if (e.isError())
      return e;
    if (e.isNull())
      return g.create(m.VALUE);
    const C = u.isArray() ? u.get(0, 0) : u, E = c.isArray() ? c.get(0, 0) : c, { isError: b, errorObject: y, rowsValue: p, columnsValue: N, padWithObject: w } = this._checkRowsColumnsPadWith(C, E, l, s, a);
    return b ? y : this._getResultArray(e, p, N, w, s, a);
  }
  _checkRowsColumnsPadWith(e, t, r, n, s) {
    if (e.isError())
      return {
        isError: !0,
        errorObject: e
      };
    if (t.isError())
      return {
        isError: !0,
        errorObject: t
      };
    const a = Math.trunc(+e.getValue()), u = Math.trunc(+t.getValue());
    if (Number.isNaN(a) || Number.isNaN(u))
      return {
        isError: !0,
        errorObject: g.create(m.VALUE)
      };
    if (Math.abs(a) < n || Math.abs(u) < s)
      return {
        isError: !0,
        errorObject: g.create(m.VALUE)
      };
    let c = r;
    if (r.isArray()) {
      const l = r.getRowCount(), f = r.getColumnCount();
      if (l > 1 || f > 1)
        return {
          isError: !0,
          errorObject: g.create(m.VALUE)
        };
      c = r.get(0, 0);
    }
    return {
      isError: !1,
      rowsValue: a,
      columnsValue: u,
      padWithObject: c
    };
  }
  _getResultArray(e, t, r, n, s, a) {
    let u = [];
    e.isArray() ? u = e.map((f) => f.isNull() ? R.create(0) : f).getArrayValue() : u = [[e]];
    const c = Math.max(0, t - s), l = Math.max(0, r - a);
    for (let f = 0; f < c; f++)
      u.push(new Array(a).fill(n));
    for (let f = 0; f < l; f++)
      u.forEach((h) => {
        h.push(n);
      });
    return t === 1 && r === 1 ? u[0][0] : W.create({
      calculateValueList: u,
      rowCount: u.length,
      columnCount: u[0].length,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
}
class nC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = r != null ? r : g.create(m.CALC);
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const s = e.isArray() ? e.getRowCount() : 1, a = e.isArray() ? e.getColumnCount() : 1, u = t.isArray() ? t.getRowCount() : 1, c = t.isArray() ? t.getColumnCount() : 1;
    return u > 1 && c > 1 || u === 1 && c !== a || c === 1 && u !== s ? g.create(m.VALUE) : s === 1 && a === 1 ? this._getResultArrayByR1C1(e, t, n) : u === 1 ? c !== a ? g.create(m.VALUE) : this._getResultArrayByR1(s, a, e, t, n) : c === 1 ? u !== s ? g.create(m.VALUE) : this._getResultArrayByC1(s, a, e, t, n) : n;
  }
  _getResultArrayByR1C1(e, t, r) {
    let n = e, s = t;
    return n.isArray() && (n = n.get(0, 0)), s.isArray() && (s = s.get(0, 0)), s.isString() && (s = s.convertToNumberObjectValue()), s.isError() ? s : +s.getValue() ? n : r;
  }
  _getResultArrayByR1(e, t, r, n, s) {
    const a = [];
    for (let u = 0; u < t; u++) {
      let c = n.get(0, u);
      if (c.isString() && (c = c.convertToNumberObjectValue()), c.isError())
        return c;
      if (+c.getValue())
        for (let f = 0; f < e; f++) {
          a[f] || (a[f] = []);
          const h = r.get(f, u);
          a[f].push(h);
        }
    }
    return a.length === 0 ? s : W.create({
      calculateValueList: a,
      rowCount: a.length,
      columnCount: a[0].length || 0,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
  _getResultArrayByC1(e, t, r, n, s) {
    const a = [];
    for (let u = 0; u < e; u++) {
      let c = n.get(u, 0);
      if (c.isString() && (c = c.convertToNumberObjectValue()), c.isError())
        return c;
      if (!+c.getValue())
        continue;
      const f = [];
      for (let h = 0; h < t; h++) {
        const d = r.get(u, h);
        f.push(d);
      }
      a.push(f);
    }
    return a.length === 0 ? s : W.create({
      calculateValueList: a,
      rowCount: a.length,
      columnCount: a[0].length || 0,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
}
class sC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
    A(this, "needsReferenceObject", !0);
    A(this, "needsFormulaDataModel", !0);
  }
  calculate(e) {
    var f, h;
    if (!e.isReferenceObject())
      return g.create(m.NA);
    const t = e.getUnitId(), r = e.getSheetId(), s = (h = (f = e.getUnitData()[t]) == null ? void 0 : f[r]) == null ? void 0 : h.cellData, { startRow: a, startColumn: u } = e.getRangePosition(), l = e.toArrayValueObject().mapValue((d, _, C) => {
      const E = s.getValue(a + _, u + C);
      if (E != null && E.f || E != null && E.si) {
        const b = this._formulaDataModel.getFormulaStringByCell(a + _, u + C, r, t);
        return U.create(b);
      }
      return g.create(m.NA);
    });
    return l.getRowCount() === 1 && l.getColumnCount() === 1 ? l.get(0, 0) : l;
  }
}
var fe = /* @__PURE__ */ ((o) => (o.ADDRESS = "ADDRESS", o.AREAS = "AREAS", o.CHOOSE = "CHOOSE", o.CHOOSECOLS = "CHOOSECOLS", o.CHOOSEROWS = "CHOOSEROWS", o.COLUMN = "COLUMN", o.COLUMNS = "COLUMNS", o.DROP = "DROP", o.EXPAND = "EXPAND", o.FILTER = "FILTER", o.FORMULATEXT = "FORMULATEXT", o.GETPIVOTDATA = "GETPIVOTDATA", o.HLOOKUP = "HLOOKUP", o.HSTACK = "HSTACK", o.HYPERLINK = "HYPERLINK", o.IMAGE = "IMAGE", o.INDEX = "INDEX", o.INDIRECT = "INDIRECT", o.LOOKUP = "LOOKUP", o.MATCH = "MATCH", o.OFFSET = "OFFSET", o.ROW = "ROW", o.ROWS = "ROWS", o.RTD = "RTD", o.SORT = "SORT", o.SORTBY = "SORTBY", o.TAKE = "TAKE", o.TOCOL = "TOCOL", o.TOROW = "TOROW", o.TRANSPOSE = "TRANSPOSE", o.UNIQUE = "UNIQUE", o.VLOOKUP = "VLOOKUP", o.VSTACK = "VSTACK", o.WRAPCOLS = "WRAPCOLS", o.WRAPROWS = "WRAPROWS", o.XLOOKUP = "XLOOKUP", o.XMATCH = "XMATCH", o))(fe || {});
class aC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    if (e.isError())
      return e;
    if (t.isError())
      return g.create(m.REF);
    if (!t.isArray() || r.isError() || n != null && n.isError())
      return g.create(m.NA);
    const s = this.getZeroOrOneByOneDefault(n);
    if (s == null)
      return g.create(m.VALUE);
    const a = this.getIndexNumValue(r);
    if (a instanceof g)
      return a;
    const u = t.slice([0, 1]), c = t.slice([a - 1, a]);
    return u == null || c == null ? g.create(m.REF) : e.isArray() ? e.map((l) => this._handleSingleObject(l, u, c, s)) : this._handleSingleObject(e, u, c, s);
  }
  _handleSingleObject(e, t, r, n) {
    return n === 0 ? this.equalSearch(e, t, r) : this.binarySearch(e, t, r);
  }
}
class iC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = Math.max(
      ...e.map((n) => n.isArray() ? n.getRowCount() : 1)
    ), r = [];
    for (let n = 0; n < e.length; n++) {
      const s = e[n];
      if (s.isError())
        return s;
      const a = s.isArray() ? s.getRowCount() : 1, u = s.isArray() ? s.getColumnCount() : 1;
      for (let c = 0; c < t; c++) {
        r[c] || (r[c] = []);
        for (let l = 0; l < u; l++) {
          let f = s;
          s.isArray() && (f = s.get(c, l)), c > a - 1 || !f ? r[c].push(g.create(m.NA)) : r[c].push(f);
        }
      }
    }
    return W.create({
      calculateValueList: r,
      rowCount: r.length,
      columnCount: r[0].length || 0,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
}
class oC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = e;
    if (e.isArray() && (r = e.get(0, 0)), r.isError())
      return r;
    let n = t;
    if (t != null && t.isArray() && (n = t.get(0, 0)), n != null && n.isError())
      return n;
    let s = `${r.getValue()}`;
    r.isNull() && (s = "");
    let a = s;
    return n && (a = `${n.getValue()}`, n.isNull() ? a = "0" : n.isBoolean() && (a = a.toLocaleUpperCase())), U.create(a, {
      isHyperlink: !0,
      hyperlinkUrl: s
    });
  }
}
class uC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 4);
    A(this, "needsReferenceObject", !0);
  }
  // eslint-disable-next-line max-lines-per-function, complexity
  calculate(e, t, r, n) {
    if (e.isError())
      return e;
    const s = this._getReferenceCounts(e);
    let a = t;
    t.isReferenceObject() && (a = t.toArrayValueObject());
    let u = r;
    u != null && u.isReferenceObject() && (u = r.toArrayValueObject());
    let c = n != null ? n : R.create(1);
    c.isReferenceObject() && (c = n.toArrayValueObject());
    const l = Math.max(
      a.isArray() ? a.getRowCount() : 1,
      u != null && u.isArray() ? u.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1
    ), f = Math.max(
      a.isArray() ? a.getColumnCount() : 1,
      u != null && u.isArray() ? u.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1
    );
    if (l === 1 && f === 1)
      return this._handleSingleObject(
        e,
        a.isArray() ? a.get(0, 0) : a,
        u ? u.isArray() ? u.get(0, 0) : u : void 0,
        c.isArray() ? c.get(0, 0) : c,
        s
      );
    {
      const h = O(l, f, a, g.create(m.NA)), d = u ? O(l, f, u, g.create(m.NA)) : [], _ = O(l, f, c, g.create(m.NA));
      return h.mapValue((C, E, b) => {
        const y = u ? d.get(E, b) : void 0, p = _.get(E, b), N = this._handleSingleObject(e, C, y, p, s);
        return N.isReferenceObject() ? N.getCellByPosition() : N;
      });
    }
  }
  _handleSingleObject(e, t, r, n, s) {
    let a, u, c;
    if (r) {
      const { isError: C, errorObject: E, variants: b } = I(t, r, n);
      if (C)
        return E;
      a = b[0], u = b[1], c = b[2];
    } else {
      const { isError: C, errorObject: E, variants: b } = I(t, n);
      if (C)
        return E;
      a = b[0], c = b[1];
    }
    const l = Math.floor(+c.getValue());
    if (l < 0)
      return g.create(m.VALUE);
    if (l > s.length)
      return g.create(m.REF);
    const f = s[l - 1];
    let h = 0, d = 0;
    if (f.rowCount === 1 && !r ? d = Math.floor(+a.getValue()) : (h = Math.floor(+a.getValue()), r && (d = Math.floor(+u.getValue()))), h < 0 || d < 0)
      return g.create(m.VALUE);
    if (h > f.rowCount || d > f.columnCount)
      return g.create(m.REF);
    let _ = e;
    return l > 1 && (_ = e.getCubeValues()[l - 1]), _.isReferenceObject() ? this._calculateReferenceObject(_, h, d) : this._calculateArrayObject(_, h, d);
  }
  _getReferenceCounts(e) {
    let t = [
      {
        rowCount: 1,
        columnCount: 1
      }
    ];
    return e.isReferenceObject() ? t = [
      {
        rowCount: e.getRowCount(),
        columnCount: e.getColumnCount()
      }
    ] : e.isCube() ? t = e.getCubeValues().map((r) => ({
      rowCount: r.getRowCount(),
      columnCount: r.getColumnCount()
    })) : e.isArray() && (t = [
      {
        rowCount: e.getRowCount(),
        columnCount: e.getColumnCount()
      }
    ]), t;
  }
  _calculateReferenceObject(e, t, r) {
    const { startRow: n, endRow: s, startColumn: a, endColumn: u } = e.getRangePosition();
    let c = 0, l = 0, f = 0, h = 0;
    if (t === 0 ? (c = n, l = s) : c = l = n + t - 1, r === 0 ? (f = a, h = u) : f = h = a + r - 1, c > s || f > u)
      return g.create(m.REF);
    const d = {
      startRow: c,
      startColumn: f,
      endRow: l,
      endColumn: h
    };
    return this.createReferenceObject(e, d);
  }
  _calculateArrayObject(e, t, r) {
    return !e.isArray() || t === 0 && r === 0 ? e : t === 0 ? e.slice(void 0, [r - 1, r]) : r === 0 ? e.slice([t - 1, t], void 0) : e.get(t - 1, r - 1);
  }
}
class cC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  isAddress() {
    return !0;
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t != null && t.isError())
      return t;
    let r = this.getZeroOrOneByOneDefault(t);
    r == null && (r = 1);
    let n = e;
    if (e.isArray()) {
      const s = e.getRowCount(), a = e.getColumnCount();
      if (s > 1 || a > 1)
        return e.map(() => g.create(m.VALUE));
      n = e.getFirstCell();
    }
    return this._handleSingleObject(n, r);
  }
  _handleSingleObject(e, t) {
    const r = `${e.getValue()}`;
    if (r.trim() === "")
      return g.create(m.REF);
    const n = this._convertToDefinedName(r);
    if (t === 0) {
      const f = cm(n), { range: h, sheetName: d, unitId: _ } = f, C = new On(h);
      return C.setForcedUnitIdDirect(_), C.setForcedSheetName(d), this._setDefault(C);
    }
    if (Yr(n))
      return this._setDefault(new La(n));
    if (ho(n))
      return this._setDefault(new Pa(n));
    if (mo(n))
      return this._setDefault(new xa(n));
    const s = Er(n), { range: a, sheetName: u, unitId: c } = s;
    if (Number.isNaN(a.startRow) || a.endRow + 1 > 1048576 || Number.isNaN(a.startColumn) || a.endColumn + 1 > 16384)
      return g.create(m.REF);
    const l = new On(a);
    return l.setForcedUnitIdDirect(c), l.setForcedSheetName(u), this._setDefault(l);
  }
  _setDefault(e) {
    return this.unitId == null || this.subUnitId == null ? g.create(m.REF) : (e.setDefaultUnitId(this.unitId), e.setDefaultSheetId(this.subUnitId), e);
  }
  /**
   * In Excel, to inject a defined name into a function that has positioning capabilities,
   * such as using the INDIRECT function to reference a named range,
   * you can write it as follows:
   * =INDIRECT("DefinedName1")
   */
  _convertToDefinedName(e) {
    const t = this.getDefinedName(e);
    if (t == null)
      return e;
    const r = t.formulaOrRefString;
    return r == null ? e : r.startsWith(X.EQUALS) ? r.slice(1) : r;
  }
}
class lC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
    A(this, "needsExpandParams", !0);
  }
  calculate(e, t, r) {
    return e.isError() ? e : t.isError() ? g.create(m.REF) : t.isArray() ? r != null && r.isError() ? r : t.getColumnCount() === 1 || t.getRowCount() === 1 ? r != null && !r.isArray() ? g.create(m.REF) : this._handleVector(e, t, r) : this._handleArray(e, t) : g.create(m.VALUE);
  }
  _handleVector(e, t, r) {
    let n = r;
    if (n == null)
      n = t;
    else if (n.getRowCount() !== t.getRowCount() || n.getColumnCount() !== t.getColumnCount())
      return g.create(m.REF);
    return e.isArray() ? e.map((s) => this.binarySearch(s, t, n)) : this.binarySearch(e, t, n);
  }
  _handleArray(e, t) {
    const r = t.getRowCount(), n = t.getColumnCount();
    let s, a;
    return n > r ? (s = t.slice([0, 1]), a = t.slice([r - 1, r])) : (s = t.slice(void 0, [0, 1]), a = t.slice(void 0, [n - 1, n])), s == null || a == null ? g.create(m.VALUE) : e.isArray() ? e.map((u) => this.binarySearch(u, s, a)) : this.binarySearch(e, s, a);
  }
}
class fC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    if (e.isError())
      return e;
    if (t.isError())
      return g.create(m.REF);
    if (!t.isArray())
      return g.create(m.VALUE);
    const n = t.getRowCount(), s = t.getColumnCount();
    if (n !== 1 && s !== 1 || r != null && r.isError())
      return g.create(m.NA);
    const a = this.getMatchTypeValue(r);
    return a == null ? g.create(m.VALUE) : e.isArray() ? e.map((u) => this._handleSingleObject(
      u,
      t,
      a
    )) : this._handleSingleObject(
      e,
      t,
      a
    );
  }
  _handleSingleObject(e, t, r) {
    const n = this._getSearchModeValue(r), s = t.orderSearch(e, n);
    if (s == null)
      return g.create(m.NA);
    if (s instanceof g)
      return s;
    const a = t.getRowCount() === 1 ? s.column + 1 : s.row + 1;
    return R.create(a);
  }
  _getSearchModeValue(e) {
    switch (e) {
      case 1:
        return Se.MIN;
      case 0:
        return Se.NORMAL;
      case -1:
        return Se.MAX;
    }
  }
}
class hC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 5);
    A(this, "needsReferenceObject", !0);
  }
  isAddress() {
    return !0;
  }
  // eslint-disable-next-line max-lines-per-function, complexity
  calculate(e, t, r, n, s) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (r.isError())
      return r;
    if (n != null && n.isError())
      return n;
    if (s != null && s.isError())
      return s;
    if (!e.isReferenceObject())
      return g.create(m.VALUE);
    const a = e.getRowCount(), u = e.getColumnCount();
    let c = t;
    c.isReferenceObject() && (c = c.toArrayValueObject());
    let l = r;
    l.isReferenceObject() && (l = l.toArrayValueObject());
    let f = n != null ? n : R.create(a);
    f.isReferenceObject() && (f = f.toArrayValueObject()), f.isNull() && (f = R.create(a));
    let h = s != null ? s : R.create(u);
    h.isReferenceObject() && (h = h.toArrayValueObject()), h.isNull() && (h = R.create(u));
    const d = Math.max(
      c.isArray() ? c.getRowCount() : 1,
      l.isArray() ? l.getRowCount() : 1,
      f.isArray() ? f.getRowCount() : 1,
      h.isArray() ? h.getRowCount() : 1
    ), _ = Math.max(
      c.isArray() ? c.getColumnCount() : 1,
      l.isArray() ? l.getColumnCount() : 1,
      f.isArray() ? f.getColumnCount() : 1,
      h.isArray() ? h.getColumnCount() : 1
    );
    if (c = c, l = l, f = f, h = h, d === 1 && _ === 1)
      return c = c.isArray() ? c.get(0, 0) : c, l = l.isArray() ? l.get(0, 0) : l, f = f.isArray() ? f.get(0, 0) : f, h = h.isArray() ? h.get(0, 0) : h, this._handleSingleObject(e, c, l, f, h);
    const C = O(d, _, c, g.create(m.NA)), E = O(d, _, l, g.create(m.NA)), b = O(d, _, f, g.create(m.NA)), y = O(d, _, h, g.create(m.NA));
    return C.mapValue((p, N, w) => {
      const S = E.get(N, w), M = b.get(N, w), D = y.get(N, w);
      return p.isError() ? p : S.isError() ? S : M.isError() ? M : D.isError() ? D : this._handleSingleObject(e, p, S, M, D, !0);
    });
  }
  // eslint-disable-next-line
  _handleSingleObject(e, t, r, n, s, a = !1) {
    const { startRow: u, startColumn: c } = e.getRangePosition();
    let l = t;
    if (l.isString() && (l = l.convertToNumberObjectValue()), l.isError())
      return l;
    let f = r;
    if (f.isString() && (f = f.convertToNumberObjectValue()), f.isError())
      return f;
    const h = +l.getValue(), d = +f.getValue();
    if (typeof h != "number" || typeof d != "number")
      return g.create(m.VALUE);
    const _ = u + h, C = c + d;
    if (_ < 0 || C < 0)
      return g.create(m.REF);
    const E = this.getIndexNumValue(n), b = this.getIndexNumValue(s);
    if (typeof E != "number" || typeof b != "number")
      return g.create(m.VALUE);
    if (E === 0 || b === 0)
      return g.create(m.REF);
    const y = E > 0 ? _ + E - 1 : _ + E + 1, p = b > 0 ? C + b - 1 : C + b + 1;
    if (y < 0 || p < 0)
      return g.create(m.REF);
    if (a)
      return g.create(m.VALUE);
    const N = _ < y ? _ : y, w = C < p ? C : p, S = _ > y ? _ : y, M = C > p ? C : p, D = {
      startRow: N,
      startColumn: w,
      endRow: S,
      endColumn: M
    };
    return this.createReferenceObject(e, D);
  }
}
class mC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 0);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e == null)
      return R.create(this.row + 1);
    if (e.isError())
      return e;
    if (!e.isArray())
      return g.create(m.NA);
    const t = e.getCurrentRow(), r = e.getRowCount(), n = [];
    for (let a = 0; a < r; a++)
      n.push([R.create(t + a + 1)]);
    const s = {
      calculateValueList: n,
      rowCount: r,
      columnCount: 1,
      unitId: this.unitId || "",
      sheetId: this.subUnitId || "",
      row: this.row,
      column: this.column
    };
    return W.create(s);
  }
}
class gC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isError())
      return e;
    if (e.isString() || e.isNumber() || e.isBoolean())
      return R.create(1);
    if (!e.isArray())
      return g.create(m.NA);
    const t = e.getRowCount();
    return R.create(t);
  }
}
class dC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = t != null ? t : R.create(1), a = r != null ? r : R.create(1), u = n != null ? n : T.create(!1);
    if (u.isArray()) {
      const c = u.getRowCount(), l = u.getColumnCount();
      if (c === 1 && l === 1) {
        const f = u.get(0, 0);
        return this._handleSingleObject(e, s, a, f);
      }
      return u.map((f) => {
        const h = this._handleSingleObject(e, s, a, f);
        return h.isArray() ? h.get(0, 0) : h;
      });
    }
    return this._handleSingleObject(e, s, a, u);
  }
  _handleSingleObject(e, t, r, n) {
    if (e.isError())
      return e;
    const s = e.isArray() ? e.getRowCount() : 1, a = e.isArray() ? e.getColumnCount() : 1, u = this._checkArrayError(t);
    if (u.isError())
      return u;
    const c = Math.floor(+u.getValue());
    if (c < 1)
      return g.create(m.VALUE);
    const l = this._checkArrayError(r);
    if (l.isError())
      return l;
    const f = Math.floor(+l.getValue());
    if (f !== -1 && f !== 1)
      return g.create(m.VALUE);
    let h = n;
    if (h.isString() && (h = h.convertToNumberObjectValue()), h.isError())
      return h;
    if (!e.isArray() || s === 1 && a === 1)
      return e;
    const d = +h.getValue();
    return this._getResult(e, c, f, d, s, a);
  }
  _checkArrayError(e) {
    let t = e;
    if (t.isArray()) {
      const r = t.getRowCount(), n = t.getColumnCount();
      if (r > 1 || n > 1)
        return g.create(m.VALUE);
      t = t.get(0, 0);
    }
    return t.isString() && (t = t.convertToNumberObjectValue()), t;
  }
  _getResult(e, t, r, n, s, a) {
    if (n) {
      if (t > s)
        return g.create(m.VALUE);
      const u = e.transpose().getArrayValue();
      return u.sort(this._sort(t - 1, r)), W.create({
        calculateValueList: u,
        rowCount: u.length,
        columnCount: u[0].length || 0,
        unitId: this.unitId,
        sheetId: this.subUnitId,
        row: this.row,
        column: this.column
      }).transpose();
    } else {
      if (t > a)
        return g.create(m.VALUE);
      const u = e.getArrayValue();
      return u.sort(this._sort(t - 1, r)), W.create({
        calculateValueList: u,
        rowCount: u.length,
        columnCount: u[0].length || 0,
        unitId: this.unitId,
        sheetId: this.subUnitId,
        row: this.row,
        column: this.column
      });
    }
  }
  _sort(e, t = 1) {
    const r = Rn();
    return t === 1 ? this._sortAsc(e, r) : this._sortDesc(e, r);
  }
  _sortAsc(e, t) {
    return (r, n) => {
      const s = r[e], a = n[e];
      if (s == null || s.isNull())
        return 1;
      if (a == null || a.isNull())
        return -1;
      if (s.isError() && a.isError())
        return 0;
      if (s.isError())
        return 1;
      if (a.isError())
        return -1;
      const u = s.getValue(), c = a.getValue();
      return s.isBoolean() && u === !0 ? 1 : a.isBoolean() && c === !0 ? -1 : s.isBoolean() && u === !1 ? 1 : a.isBoolean() && c === !1 ? -1 : s.isNumber() && a.isNumber() ? +u - +c : t(
        u,
        c
      );
    };
  }
  _sortDesc(e, t) {
    return (r, n) => {
      const s = r[e], a = n[e];
      if (s == null || s.isNull())
        return 1;
      if (a == null || a.isNull())
        return -1;
      if (s.isError() && a.isError())
        return 0;
      if (s.isError())
        return -1;
      if (a.isError())
        return 1;
      const u = s.getValue(), c = a.getValue();
      return s.isBoolean() && u === !0 ? -1 : a.isBoolean() && c === !0 ? 1 : s.isBoolean() && u === !1 ? -1 : a.isBoolean() && c === !1 ? 1 : s.isNumber() && a.isNumber() ? +c - +u : t(
        c,
        u
      );
    };
  }
}
class CC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 255);
  }
  calculate(e, ...t) {
    t.length === 1 && t.push(R.create(1));
    const r = this._getVariantsError(e, ...t), { maxRowLength: n, maxColumnLength: s } = nh(t);
    if (r.isError()) {
      const c = O(n, s, r);
      return n === 1 && s === 1 ? c.get(0, 0) : c;
    }
    const a = t.map((c, l) => l % 2 === 0 ? c : O(n, s, c, g.create(m.NA))), u = this._getResultArray(e, a, n, s);
    return n === 1 && s === 1 ? u[0][0] : W.create({
      calculateValueList: u,
      rowCount: u.length,
      columnCount: u[0].length || 0,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
  // eslint-disable-next-line complexity
  _getVariantsError(e, ...t) {
    if (e.isError())
      return e;
    for (let u = 0; u < t.length; u++) {
      const c = t[u];
      if (c.isError())
        return c;
    }
    if (t.length < 2 || t.length % 2 !== 0)
      return g.create(m.VALUE);
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t[0].isArray() ? t[0].getRowCount() : 1, a = t[0].isArray() ? t[0].getColumnCount() : 1;
    if ((s > 1 || a > 1) && (s > 1 && a > 1 || s === 1 && a !== n || a === 1 && s !== r))
      return g.create(m.VALUE);
    for (let u = 2; u < t.length; u++) {
      if (u % 2 === 1)
        continue;
      const c = t[u].isArray() ? t[u].getRowCount() : 1, l = t[u].isArray() ? t[u].getColumnCount() : 1;
      if (c !== s || l !== a)
        return g.create(m.VALUE);
    }
    return T.create(!0);
  }
  _getResultArray(e, t, r, n) {
    const s = e.isArray() ? e.getRowCount() : 1, a = e.isArray() ? e.getColumnCount() : 1, u = t[0].isArray() ? t[0].getRowCount() : 1, c = t[0].isArray() ? t[0].getColumnCount() : 1, l = [];
    for (let f = 0; f < r; f++) {
      l[f] = [];
      for (let h = 0; h < n; h++) {
        const { isError: d, errorObject: _, byArrays: C, sortOrders: E } = this._getByArraysAndSortOrders(t, f, h, c);
        if (d) {
          l[f].push(_);
          continue;
        }
        if (!e.isArray() || s === 1 && a === 1) {
          l[f].push(e);
          continue;
        }
        let b = e.getArrayValue();
        u === 1 && c === 1 || (u === 1 ? (b = b.concat(C), b = this._transposeArray(b), b.sort(this._sort(s, E)), b = this._transposeArray(b).slice(0, s)) : c === 1 && (b = this._transposeArray(b), b = b.concat(C), b = this._transposeArray(b), b.sort(this._sort(a, E)), b = b.map((p) => p.slice(0, a))));
        const y = W.create({
          calculateValueList: b,
          rowCount: b.length,
          columnCount: b[0].length || 0,
          unitId: this.unitId,
          sheetId: this.subUnitId,
          row: this.row,
          column: this.column
        });
        if (r > 1 || n > 1) {
          l[f].push(y.get(0, 0));
          continue;
        }
        l[f].push(y);
      }
    }
    return l;
  }
  _getByArraysAndSortOrders(e, t, r, n) {
    const s = [], a = [];
    let u = !1, c = null;
    for (let l = 0; l < e.length; l++) {
      if (l % 2 === 1)
        continue;
      const f = e[l];
      let h = e[l + 1].get(t, r);
      if (h.isString() && (h = h.convertToNumberObjectValue()), h.isError()) {
        u = !0, c = h;
        break;
      }
      const d = Math.floor(+h.getValue());
      if (d !== -1 && d !== 1) {
        u = !0, c = g.create(m.VALUE);
        break;
      }
      if (a.push(d), f.isArray()) {
        let _ = f.getArrayValue();
        n === 1 && (_ = this._transposeArray(_)), s.push(_[0]);
      } else
        s.push([f]);
    }
    return {
      isError: u,
      errorObject: c,
      byArrays: s,
      sortOrders: a
    };
  }
  _transposeArray(e) {
    const t = e.length, r = e[0].length, n = [];
    for (let s = 0; s < r; s++) {
      n[s] = [];
      for (let a = 0; a < t; a++)
        n[s][a] = e[a][s];
    }
    return n;
  }
  _sort(e, t) {
    const r = Rn();
    return (n, s) => {
      let a = n[e], u = s[e], c = this._compare(a, u, t[0], r);
      if (c === 0 && t.length > 1) {
        for (let l = 1; l < t.length; l++)
          if (a = n[e + l], u = s[e + l], c = this._compare(a, u, t[l], r), c !== 0)
            return c;
      }
      return c;
    };
  }
  _compare(e, t, r, n) {
    return r === 1 ? this._asc(e, t, n) : this._desc(e, t, n);
  }
  _asc(e, t, r) {
    if (e == null || e.isNull())
      return 1;
    if (t == null || t.isNull())
      return -1;
    if (e.isError() && t.isError())
      return 0;
    if (e.isError())
      return 1;
    if (t.isError())
      return -1;
    const n = e.getValue(), s = t.getValue();
    return e.isBoolean() && n === !0 ? 1 : t.isBoolean() && s === !0 ? -1 : e.isBoolean() && n === !1 ? 1 : t.isBoolean() && s === !1 ? -1 : e.isNumber() && t.isNumber() ? +n - +s : r(
      n,
      s
    );
  }
  _desc(e, t, r) {
    if (e == null || e.isNull())
      return 1;
    if (t == null || t.isNull())
      return -1;
    if (e.isError() && t.isError())
      return 0;
    if (e.isError())
      return -1;
    if (t.isError())
      return 1;
    const n = e.getValue(), s = t.getValue();
    return e.isBoolean() && n === !0 ? -1 : t.isBoolean() && s === !0 ? 1 : e.isBoolean() && n === !1 ? -1 : t.isBoolean() && s === !1 ? 1 : e.isNumber() && t.isNumber() ? +s - +n : r(
      s,
      n
    );
  }
}
class _C extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = e.isArray() ? e.getRowCount() : 1, s = e.isArray() ? e.getColumnCount() : 1;
    let a = t, u = r != null ? r : R.create(s);
    t.isNull() && (a = R.create(n)), u.isNull() && (u = R.create(s));
    const c = Math.max(
      a.isArray() ? a.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1
    ), l = Math.max(
      a.isArray() ? a.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1
    ), f = O(c, l, a, g.create(m.NA)), h = O(c, l, u, g.create(m.NA));
    if (c > 1 || l > 1)
      return f.mapValue((p, N, w) => {
        const S = h.get(N, w);
        if (e.isError())
          return e;
        if (e.isNull())
          return g.create(m.VALUE);
        const { isError: M, errorObject: D } = this._checkRowsColumns(p, S, n, s);
        return M ? D : e.isArray() ? g.create(m.VALUE) : e;
      });
    if (e.isError())
      return e;
    if (e.isNull())
      return g.create(m.VALUE);
    const d = a.isArray() ? a.get(0, 0) : a, _ = u.isArray() ? u.get(0, 0) : u, { isError: C, errorObject: E, rowsValue: b, columnsValue: y } = this._checkRowsColumns(d, _, n, s);
    return C ? E : this._getResultArray(e, b, y, n, s);
  }
  _checkRowsColumns(e, t, r, n) {
    if (e.isError())
      return {
        isError: !0,
        errorObject: e
      };
    if (t.isError())
      return {
        isError: !0,
        errorObject: t
      };
    let s = Math.trunc(+e.getValue()), a = Math.trunc(+t.getValue());
    return Number.isNaN(s) || Number.isNaN(a) ? {
      isError: !0,
      errorObject: g.create(m.VALUE)
    } : Math.abs(s) === 0 || Math.abs(a) === 0 ? {
      isError: !0,
      errorObject: g.create(m.CALC)
    } : (s > r && (s = r), a > n && (a = n), {
      isError: !1,
      rowsValue: s,
      columnsValue: a
    });
  }
  _getResultArray(e, t, r, n, s) {
    if (!e.isArray())
      return e;
    const a = t >= 0 ? [0, t] : [n + t, n], u = r >= 0 ? [0, r] : [s + r, s];
    let c;
    return t === n && r === s ? c = e : t === n ? c = e.slice(void 0, u) : r === s ? c = e.slice(a, void 0) : c = e.slice(a, u), c = c.map((l) => l.isNull() ? R.create(0) : l), t === 1 && r === 1 ? c.get(0, 0) : c;
  }
}
class AC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = t != null ? t : R.create(0), s = r != null ? r : T.create(!1), a = Math.max(
      n.isArray() ? n.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1
    ), u = Math.max(
      n.isArray() ? n.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1
    ), c = O(a, u, n, g.create(m.NA)), l = O(a, u, s, g.create(m.NA)), f = c.mapValue((h, d, _) => {
      const C = l.get(d, _);
      if (e.isError())
        return e;
      if (e.isNull())
        return g.create(m.VALUE);
      if (h.isError())
        return h;
      if (C.isError())
        return C;
      const E = Math.trunc(+h.getValue()), b = +C.getValue();
      if (Number.isNaN(E) || E < 0 || E > 3 || Number.isNaN(b))
        return g.create(m.VALUE);
      if (!e.isArray())
        return e;
      let y = [];
      return b ? y = this._getArrayValueByColumn(e, E) : y = this._getArrayValueByRow(e, E), y.length === 0 ? g.create(m.CALC) : a > 1 || u > 1 || y.length === 1 ? y[0] : W.create({
        calculateValueList: y.map((p) => [p]),
        rowCount: y.length,
        columnCount: 1,
        unitId: this.unitId,
        sheetId: this.subUnitId,
        row: this.row,
        column: this.column
      });
    });
    return a === 1 && u === 1 ? f.get(0, 0) : f;
  }
  _getArrayValueByColumn(e, t) {
    const r = e, n = r.getRowCount(), s = r.getColumnCount(), a = [];
    for (let u = 0; u < s; u++)
      for (let c = 0; c < n; c++) {
        const l = r.get(c, u);
        this._isIgnore(l, t) || a.push(l.isNull() ? R.create(0) : l);
      }
    return a;
  }
  _getArrayValueByRow(e, t) {
    const r = e, n = r.getRowCount(), s = r.getColumnCount(), a = [];
    for (let u = 0; u < n; u++)
      for (let c = 0; c < s; c++) {
        const l = r.get(u, c);
        this._isIgnore(l, t) || a.push(l.isNull() ? R.create(0) : l);
      }
    return a;
  }
  _isIgnore(e, t) {
    switch (t) {
      case 0:
        return !1;
      case 1:
        return e.isNull();
      case 2:
        return e.isError();
      case 3:
        return e.isNull() || e.isError();
      default:
        return !1;
    }
  }
}
class EC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = t != null ? t : R.create(0), s = r != null ? r : T.create(!1), a = Math.max(
      n.isArray() ? n.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1
    ), u = Math.max(
      n.isArray() ? n.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1
    ), c = O(a, u, n, g.create(m.NA)), l = O(a, u, s, g.create(m.NA)), f = c.mapValue((h, d, _) => {
      const C = l.get(d, _);
      if (e.isError())
        return e;
      if (h.isError())
        return h;
      if (C.isError())
        return C;
      const E = Math.trunc(+h.getValue()), b = +C.getValue();
      if (Number.isNaN(E) || E < 0 || E > 3 || Number.isNaN(b) || e.isNull())
        return g.create(m.VALUE);
      if (!e.isArray())
        return e;
      let y = [];
      return b ? y = this._getArrayValueByColumn(e, E) : y = this._getArrayValueByRow(e, E), y.length === 0 ? g.create(m.CALC) : a > 1 || u > 1 || y.length === 1 ? y[0] : W.create({
        calculateValueList: [y],
        rowCount: 1,
        columnCount: y.length,
        unitId: this.unitId,
        sheetId: this.subUnitId,
        row: this.row,
        column: this.column
      });
    });
    return a === 1 && u === 1 ? f.get(0, 0) : f;
  }
  _getArrayValueByColumn(e, t) {
    const r = e, n = r.getRowCount(), s = r.getColumnCount(), a = [];
    for (let u = 0; u < s; u++)
      for (let c = 0; c < n; c++) {
        const l = r.get(c, u);
        this._isIgnore(l, t) || a.push(l.isNull() ? R.create(0) : l);
      }
    return a;
  }
  _getArrayValueByRow(e, t) {
    const r = e, n = r.getRowCount(), s = r.getColumnCount(), a = [];
    for (let u = 0; u < n; u++)
      for (let c = 0; c < s; c++) {
        const l = r.get(u, c);
        this._isIgnore(l, t) || a.push(l.isNull() ? R.create(0) : l);
      }
    return a;
  }
  _isIgnore(e, t) {
    switch (t) {
      case 0:
        return !1;
      case 1:
        return e.isNull();
      case 2:
        return e.isError();
      case 3:
        return e.isNull() || e.isError();
      default:
        return !1;
    }
  }
}
class bC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isError())
      return e;
    if (e.isArray()) {
      const t = e.getRowCount(), r = e.getColumnCount();
      return t === 1 && r === 1 ? e.get(0, 0) : e.transpose();
    }
    return e;
  }
}
class yC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = t != null ? t : T.create(!1), s = r != null ? r : T.create(!1), a = e.isArray() ? e.getRowCount() : 1, u = e.isArray() ? e.getColumnCount() : 1, c = Math.max(
      n.isArray() ? n.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1
    ), l = Math.max(
      n.isArray() ? n.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1
    ), f = O(c, l, n, g.create(m.NA)), h = O(c, l, s, g.create(m.NA)), d = f.map((_, C, E) => {
      let b = _, y = h.get(C, E);
      if (e.isError())
        return e;
      if (b.isString() && (b = b.convertToNumberObjectValue()), b.isError())
        return b;
      if (y.isString() && (y = y.convertToNumberObjectValue()), y.isError())
        return y;
      const p = +b.getValue(), N = +y.getValue();
      let w;
      return !p && a === 1 || p && u === 1 ? w = e : w = this._getResult(e, p, N), (c > 1 || l > 1) && (w != null && w.isArray()) ? w.get(0, 0) : w;
    });
    return c === 1 && l === 1 ? d.get(0, 0) : d;
  }
  _getResult(e, t, r) {
    const n = e.isArray() ? e.getRowCount() : 1, s = e.isArray() ? e.getColumnCount() : 1;
    let a = e.getArrayValue(), u = n, c = s;
    t && (a = this._transposeArray(a), u = s, c = n);
    const l = this._getRepeatRows(a, u, c);
    if (l.length > 0) {
      const f = [];
      l.forEach((h) => {
        h.forEach((d, _) => {
          (_ !== 0 || r) && f.push(d);
        });
      }), a = a.filter((h, d) => !f.includes(d));
    }
    return a.length === 0 ? g.create(m.CALC) : (t && (a = this._transposeArray(a)), W.create({
      calculateValueList: a,
      rowCount: a.length,
      columnCount: a[0].length || 0,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    }));
  }
  _getRepeatRows(e, t, r) {
    let n = [];
    for (let s = 0; s < r; s++)
      if (s === 0) {
        const a = new Array(t).fill(null).map((u, c) => ({
          r: c,
          valueObject: e[c][s]
        }));
        n = this._getRepeatRowsByObjects(a);
      } else {
        if (n.length === 0)
          break;
        let a = [];
        n.forEach((u) => {
          const c = u.map((f) => ({
            r: f,
            valueObject: e[f][s]
          })), l = this._getRepeatRowsByObjects(c);
          a = a.concat(l);
        }), n = a;
      }
    return n;
  }
  _getRepeatRowsByObjects(e) {
    const t = /* @__PURE__ */ new Map();
    return e.forEach((r) => {
      const n = r.r, s = r.valueObject;
      let a = s.getValue();
      if (s.isNull() ? a = null : s.isString() && q(a) && (a = +a), !t.has(a))
        t.set(a, [n]);
      else {
        const u = t.get(a);
        u.push(n), t.set(a, u);
      }
    }), Array.from(t.values()).filter((r) => r.length > 1);
  }
  _transposeArray(e) {
    const t = e.length, r = e[0].length, n = [];
    for (let s = 0; s < r; s++) {
      n[s] = [];
      for (let a = 0; a < t; a++)
        n[s][a] = e[a][s];
    }
    return n;
  }
}
class RC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (!t.isArray())
      return g.create(m.VALUE);
    if (r.isError())
      return r;
    if (n != null && n.isError())
      return n;
    const s = n != null ? n : T.create(!0);
    return xi(e) && xi(s) && r.isArray() ? this._handleArrayColIndexNum(e, t, r, s) : this._handleNonArrayColIndexNum(e, t, r, s);
  }
  _handleArrayColIndexNum(e, t, r, n) {
    const s = e.isArray() ? e.getFirstCell() : e, a = this.getZeroOrOneByOneDefault(n);
    if (a == null)
      return g.create(m.VALUE);
    let u;
    const c = [];
    return r.iterator((l, f, h) => {
      if (l == null)
        return u = g.create(m.VALUE), !1;
      const d = this._handleTableArray(s, t, l, a);
      if (d.isError())
        return u = d, !1;
      c[f] === void 0 && (c[f] = []), c[f][h] = d;
    }), u || Qr(c, c.length, c[0].length, this.unitId || "", this.subUnitId || "");
  }
  _handleNonArrayColIndexNum(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e), c = O(s, a, n);
    return u.map((l, f, h) => {
      if (l.isError())
        return l;
      const d = c.get(f, h);
      if (d == null)
        return g.create(m.VALUE);
      if (d.isError())
        return d;
      const _ = this.getZeroOrOneByOneDefault(d);
      return _ == null ? g.create(m.VALUE) : this._handleTableArray(l, t, r, _);
    });
  }
  _handleTableArray(e, t, r, n) {
    let s = this.getIndexNumValue(r);
    if (s instanceof g)
      return s;
    if (s = Math.floor(s), s < 1)
      return g.create(m.VALUE);
    const a = t.slice(void 0, [0, 1]);
    if (a == null)
      return g.create(m.VALUE);
    const u = t.slice(void 0, [s - 1, s]);
    return u == null ? g.create(m.REF) : this._handleSingleObject(e, a, u, n);
  }
  _handleSingleObject(e, t, r, n) {
    return n === 0 ? this.equalSearch(e, t, r) : this.binarySearch(e, t, r);
  }
}
class pC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = Math.max(
      ...e.map((n) => n.isArray() ? n.getColumnCount() : 1)
    ), r = [];
    for (let n = 0; n < e.length; n++) {
      const s = e[n];
      if (s.isError())
        return s;
      const a = s.isArray() ? s.getRowCount() : 1, u = s.isArray() ? s.getColumnCount() : 1;
      for (let c = 0; c < a; c++) {
        const l = [];
        for (let f = 0; f < t; f++) {
          let h = s;
          s.isArray() && (h = s.get(c, f)), f > u - 1 || !h ? l.push(g.create(m.NA)) : l.push(h);
        }
        r.push(l);
      }
    }
    return W.create({
      calculateValueList: r,
      rowCount: r.length,
      columnCount: r[0].length || 0,
      unitId: this.unitId,
      sheetId: this.subUnitId,
      row: this.row,
      column: this.column
    });
  }
}
class NC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    let n = r != null ? r : g.create(m.NA);
    n.isNull() && (n = g.create(m.NA));
    const s = e.isArray() ? e.getRowCount() : 1, a = e.isArray() ? e.getColumnCount() : 1, u = Math.max(
      t.isArray() ? t.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), c = Math.max(
      t.isArray() ? t.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), l = O(u, c, t, g.create(m.NA)), f = O(u, c, n, g.create(m.NA)), h = l.mapValue((d, _, C) => {
      const E = f.get(_, C);
      if (e.isError())
        return e;
      if (e.isNull())
        return g.create(m.VALUE);
      if (d.isError())
        return d;
      const b = Math.trunc(+d.getValue());
      if (s > 1 && a > 1 || Number.isNaN(b))
        return g.create(m.VALUE);
      if (b < 1)
        return g.create(m.NUM);
      const y = e.isArray() ? e.getArrayValue().flat() : [e], p = this._getWrapArray(y, b, E);
      return u > 1 || c > 1 || p.length === 1 && p[0].length === 1 ? p[0][0] : W.create({
        calculateValueList: p,
        rowCount: p.length,
        columnCount: p[0].length,
        unitId: this.unitId,
        sheetId: this.subUnitId,
        row: this.row,
        column: this.column
      });
    });
    return u === 1 && c === 1 ? h.get(0, 0) : h;
  }
  _getWrapArray(e, t, r) {
    const n = Math.ceil(e.length / t), s = n > 1 ? t : e.length, a = [];
    for (let u = 0; u < n; u++)
      for (let c = 0; c < s; c++) {
        a[c] || (a[c] = []);
        const l = u * s + c;
        l < e.length ? a[c].push(e[l].isNull() ? R.create(0) : e[l]) : a[c].push(r);
      }
    return a;
  }
}
class VC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    let n = r != null ? r : g.create(m.NA);
    n.isNull() && (n = g.create(m.NA));
    const s = e.isArray() ? e.getRowCount() : 1, a = e.isArray() ? e.getColumnCount() : 1, u = Math.max(
      t.isArray() ? t.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), c = Math.max(
      t.isArray() ? t.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), l = O(u, c, t, g.create(m.NA)), f = O(u, c, n, g.create(m.NA)), h = l.mapValue((d, _, C) => {
      const E = f.get(_, C);
      if (e.isError())
        return e;
      if (e.isNull())
        return g.create(m.VALUE);
      if (d.isError())
        return d;
      const b = Math.trunc(+d.getValue());
      if (s > 1 && a > 1 || Number.isNaN(b))
        return g.create(m.VALUE);
      if (b < 1)
        return g.create(m.NUM);
      const y = e.isArray() ? e.getArrayValue().flat() : [e], p = this._getWrapArray(y, b, E);
      return u > 1 || c > 1 || p.length === 1 && p[0].length === 1 ? p[0][0] : W.create({
        calculateValueList: p,
        rowCount: p.length,
        columnCount: p[0].length,
        unitId: this.unitId,
        sheetId: this.subUnitId,
        row: this.row,
        column: this.column
      });
    });
    return u === 1 && c === 1 ? h.get(0, 0) : h;
  }
  _getWrapArray(e, t, r) {
    const n = Math.ceil(e.length / t), s = n > 1 ? t : e.length, a = [];
    for (let u = 0; u < n; u++) {
      const c = [];
      for (let l = 0; l < s; l++) {
        const f = u * s + l;
        f < e.length ? c.push(e[f].isNull() ? R.create(0) : e[f]) : c.push(r);
      }
      a.push(c);
    }
    return a;
  }
}
class OC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 6);
  }
  // eslint-disable-next-line
  calculate(e, t, r, n, s, a) {
    let u = n != null ? n : g.create(m.NA);
    n != null && n.isNull() && (u = g.create(m.NA));
    let c = s != null ? s : R.create(0);
    s != null && s.isNull() && (c = R.create(0));
    let l = a != null ? a : R.create(1);
    if (a != null && a.isNull() && (l = R.create(1)), e.isError())
      return e;
    const f = t.isArray() ? t.getRowCount() : 1, h = t.isArray() ? t.getColumnCount() : 1, d = r.isArray() ? r.getRowCount() : 1, _ = r.isArray() ? r.getColumnCount() : 1;
    if (f !== 1 && h !== 1 || f === 1 && h > 1 && h !== _ || h === 1 && f > 1 && f !== d)
      return g.create(m.VALUE);
    if (c.isError())
      return c;
    if (l.isError())
      return l;
    const C = this.getIndexNumValue(c);
    if (C instanceof g)
      return C;
    const E = this.getIndexNumValue(l);
    return E instanceof g ? E : this._getResult(
      e,
      zs(t),
      zs(r),
      u,
      C,
      E,
      f,
      h,
      d,
      _
    );
  }
  _getResult(e, t, r, n, s, a, u, c, l, f) {
    const h = e.isArray() ? e.getRowCount() : 1, d = e.isArray() ? e.getColumnCount() : 1;
    if (h > 1 || d > 1) {
      let b;
      return u === 1 ? b = r.slice([0, 1]) : b = r.slice(void 0, [0, 1]), b == null ? g.create(m.NA) : e.map((y) => {
        const p = this._checkErrorCombination(s, a);
        if (p)
          return p;
        const N = this._handleSingleObject(y, t, b, s, a);
        return N.isError() ? n : N;
      });
    }
    const _ = e.isArray() ? e.get(0, 0) : e;
    if (c === f && u === l) {
      const b = this._checkErrorCombination(s, a);
      if (b)
        return b;
      const y = this._handleSingleObject(_, t, r, s, a);
      return y.isError() ? n : y;
    }
    let C = 0;
    c === f && (C = 1);
    const E = this._handleExpandObject(_, t, r, s, a, C);
    return E == null ? g.create(m.NA) : E;
  }
  _handleExpandObject(e, t, r, n, s, a = 0) {
    if ((s === 2 || s === -2) && n !== 2) {
      const u = Gs(s), c = Qs(n);
      return this.binarySearchExpand(
        e,
        t,
        r,
        a,
        u,
        c
      );
    }
    return n === 2 ? this.fuzzySearchExpand(e, t, r, s !== -1, a) : n === -1 || n === 1 ? this.orderSearchExpand(
      e,
      t,
      r,
      n === 1 ? Se.MAX : Se.MIN,
      s === -1,
      a
    ) : this.equalSearchExpand(e, t, r, s !== -1, a);
  }
  _handleSingleObject(e, t, r, n, s) {
    if ((s === 2 || s === -2) && n !== 2) {
      const a = Gs(s), u = Qs(n);
      return this.binarySearch(e, t, r, a, u);
    }
    return n === 2 ? this.fuzzySearch(e, t, r, s !== -1) : n === -1 || n === 1 ? this.orderSearch(
      e,
      t,
      r,
      n === 1 ? Se.MAX : Se.MIN,
      s === -1
    ) : this.equalSearch(e, t, r, s !== -1);
  }
  /**
   * Wildcard matching and binary search cannot appear at the same time
   * @param matchModeValue
   * @param searchModeValue
   * @returns
   */
  _checkErrorCombination(e, t) {
    return e === 2 && (t === -2 || t === 2) ? g.create(m.VALUE) : null;
  }
}
class wC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    let s = R.create(0);
    r && !r.isNull() && (s = r);
    let a = R.create(1);
    n && !n.isNull() && (a = n);
    const u = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1,
      a.isArray() ? a.getRowCount() : 1
    ), c = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1,
      a.isArray() ? a.getColumnCount() : 1
    ), l = O(u, c, e, g.create(m.NA)), f = O(u, c, s, g.create(m.NA)), h = O(u, c, a, g.create(m.NA)), d = l.mapValue((_, C, E) => {
      if (_.isError())
        return _;
      const b = f.get(C, E), y = h.get(C, E);
      return b.isError() ? b : y.isError() ? y : this._handleSingleObject(_, t, b, y);
    });
    return u === 1 && c === 1 ? d.get(0, 0) : d;
  }
  _handleSingleObject(e, t, r, n) {
    const s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    if (s !== 1 && a !== 1)
      return g.create(m.VALUE);
    let u = r;
    if ((r.isString() || r.isBoolean() || r.isNull()) && (u = r.convertToNumberObjectValue()), u.isError())
      return u;
    let c = n;
    if ((n.isString() || n.isBoolean() || n.isNull()) && (c = n.convertToNumberObjectValue()), c.isError())
      return c;
    const l = u.getValue(), f = c.getValue();
    return ![-1, 0, 1, 2].includes(l) || ![-1, 1, 2].includes(f) ? g.create(m.VALUE) : this._getResult(e, t, l, f);
  }
  _getResult(e, t, r, n) {
    const s = zs(t);
    let a;
    if ((n === 2 || n === -2) && r !== 2) {
      const u = Gs(n), c = Qs(r);
      a = s.binarySearch(e, u, c);
    } else if (r === 2) {
      const u = s.compare(e, x.EQUALS);
      let c;
      if (n !== -1 ? c = u.getFirstTruePosition() : c = u.getLastTruePosition(), c == null)
        return g.create(m.NA);
      a = s.getRowCount() === 1 ? c.column : c.row;
    } else if (r === -1 || r === 1) {
      const u = s.orderSearch(e, r === 1 ? Se.MAX : Se.MIN, n === -1);
      if (u == null)
        return g.create(m.NA);
      if (u instanceof g)
        return u;
      a = s.getRowCount() === 1 ? u.column : u.row;
    } else {
      const u = s.isEqual(e);
      let c;
      if (n !== -1 ? c = u.getFirstTruePosition() : c = u.getLastTruePosition(), c == null)
        return g.create(m.NA);
      a = s.getRowCount() === 1 ? c.column : c.row;
    }
    return a == null ? g.create(m.NA) : R.create(a + 1);
  }
}
const SC = [
  [Wd, fe.ADDRESS],
  [Kd, fe.AREAS],
  [Xd, fe.CHOOSE],
  [Zd, fe.CHOOSECOLS],
  [zd, fe.CHOOSEROWS],
  [Jd, fe.COLUMN],
  [eC, fe.COLUMNS],
  [tC, fe.DROP],
  [rC, fe.EXPAND],
  [nC, fe.FILTER],
  [sC, fe.FORMULATEXT],
  [aC, fe.HLOOKUP],
  [iC, fe.HSTACK],
  [oC, fe.HYPERLINK],
  [uC, fe.INDEX],
  [cC, fe.INDIRECT],
  [lC, fe.LOOKUP],
  [fC, fe.MATCH],
  [hC, fe.OFFSET],
  [mC, fe.ROW],
  [gC, fe.ROWS],
  [dC, fe.SORT],
  [CC, fe.SORTBY],
  [_C, fe.TAKE],
  [AC, fe.TOCOL],
  [EC, fe.TOROW],
  [bC, fe.TRANSPOSE],
  [yC, fe.UNIQUE],
  [RC, fe.VLOOKUP],
  [pC, fe.VSTACK],
  [NC, fe.WRAPCOLS],
  [VC, fe.WRAPROWS],
  [OC, fe.XLOOKUP],
  [wC, fe.XMATCH]
];
class MC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.abs();
  }
}
class DC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.acos();
  }
}
class LC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.acosh();
  }
}
class xC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.isArray() ? t.map((r) => r.isError() ? r : Wi(r)) : Wi(t);
  }
}
function Wi(o) {
  let i = o.getValue();
  if (o.isBoolean() && (i = i ? 1 : 0), !Number.isFinite(i))
    return g.create(m.VALUE);
  i = Number(i);
  let e = Math.atan(1 / i);
  return i < 0 && (e += Math.PI), Number.isNaN(e) ? g.create(m.VALUE) : R.create(e);
}
class PC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.isArray() ? t.map((r) => r.isError() ? r : qi(r)) : qi(t);
  }
}
function qi(o) {
  let i = o.getValue();
  if (o.isBoolean() && (i = i ? 1 : 0), !Number.isFinite(i))
    return g.create(m.VALUE);
  if (i = Number(i), Math.abs(i) <= 1)
    return g.create(m.NUM);
  const e = 1 / 2 * Math.log((i + 1) / (i - 1));
  return Number.isNaN(e) ? g.create(m.VALUE) : R.create(e);
}
class jC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  // eslint-disable-next-line
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    if (e.isNull())
      return R.create(0);
    if (e.isBoolean() || e.isNumber())
      return g.create(m.VALUE);
    let t = e.getValue().toLocaleString().toLocaleUpperCase();
    if (t.length > 255)
      return g.create(m.VALUE);
    const r = t.startsWith("-");
    r && (t = t.slice(1));
    let n = 0;
    for (let s = 0; s < t.length; s++) {
      const a = sn.get(t[s]) || 0, u = sn.get(t[s + 1]) || 0, c = sn.get(t[s + 2]) || 0, l = sn.get(t[s + 3]) || 0;
      if (!a || c >= u && c > a || a === u && a === c && a === l || a === u / 2)
        return g.create(m.VALUE);
      a < u ? n -= a : n += a;
    }
    return R.create(r ? -n : n);
  }
}
class UC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.asin();
  }
}
class vC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.asinh();
  }
}
class TC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.atan();
  }
}
class BC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = e, n = t;
    return r.isString() && (r = r.convertToNumberObjectValue()), r.isError() ? r : (n.isString() && (n = n.convertToNumberObjectValue()), n.isError() ? n : n.atan2(r));
  }
}
class IC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.atanh();
  }
}
class FC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = r != null ? r : R.create(0);
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (n.isError())
      return n;
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, n, g.create(m.NA)), f = u.map((h, d, _) => {
      const C = c.get(d, _), E = l.get(d, _);
      return this._handleSingleObject(h, C, E);
    });
    return f.getRowCount() === 1 && f.getColumnCount() === 1 ? f.getArrayValue()[0][0] : f;
  }
  _handleSingleObject(e, t, r) {
    let n = e;
    if (n.isString() && (n = n.convertToNumberObjectValue()), n.isError())
      return n;
    let s = t;
    if (s.isString() && (s = s.convertToNumberObjectValue()), s.isError())
      return s;
    let a = r;
    if (a.isString() && (a = a.convertToNumberObjectValue()), a.isError())
      return a;
    const u = Math.floor(+n.getValue()), c = Math.floor(+s.getValue()), l = Math.floor(+a.getValue());
    if (u < 0 || u >= 2 ** 53 || c < 2 || c > 36 || l < 0)
      return g.create(m.NUM);
    let f = u.toString(c);
    return f.length < l && (f = new Array(l - f.length + 1).join("0") + f), U.create(f.toLocaleUpperCase());
  }
}
class kC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.map((c, l, f) => {
      let h = c, d = a.get(l, f);
      if (h.isString() && (h = h.convertToNumberObjectValue()), h.isError())
        return h;
      if (d.isString() && (d = d.convertToNumberObjectValue()), d.isError())
        return d;
      const _ = +h.getValue(), C = +d.getValue();
      if (_ > 0 && C < 0)
        return g.create(m.NUM);
      if (_ === 0 || C === 0)
        return R.create(0);
      const E = Qe(_ / C, 0) * C;
      return R.create(E);
    });
    return u.getRowCount() === 1 && u.getColumnCount() === 1 ? u.getArrayValue()[0][0] : u;
  }
}
class $C extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = t != null ? t : R.create(1), s = r != null ? r : R.create(0);
    if (e.isError())
      return e;
    if (n.isError())
      return n;
    if (s.isError())
      return s;
    const a = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1
    ), u = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1
    ), c = O(a, u, e, g.create(m.NA)), l = O(a, u, n, g.create(m.NA)), f = O(a, u, s, g.create(m.NA)), h = c.map((d, _, C) => {
      let E = d, b = l.get(_, C), y = f.get(_, C);
      if (E.isString() && (E = E.convertToNumberObjectValue()), E.isError())
        return E;
      if (b.isString() && (b = b.convertToNumberObjectValue()), b.isError())
        return b;
      if (y.isString() && (y = y.convertToNumberObjectValue()), y.isError())
        return y;
      const p = +E.getValue(), N = +b.getValue(), w = +y.getValue();
      return p === 0 || N === 0 ? R.create(0) : this._getResult(p, N, w);
    });
    return a === 1 && u === 1 ? h.get(0, 0) : h;
  }
  _getResult(e, t, r) {
    let n;
    return e < 0 && r !== 0 ? n = (t < 0 ? Qe(Math.abs(e) / Math.abs(t), 0) : -Qe(Math.abs(e) / t, 0)) * t : n = (t < 0 ? -Qe(e / Math.abs(t), 0) : Qe(e / t, 0)) * t, R.create(n);
  }
}
class YC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(1);
    if (e.isError())
      return e;
    if (r.isError())
      return r;
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, r, g.create(m.NA)), c = a.map((l, f, h) => {
      let d = u.get(f, h), _ = l;
      if (_.isString() && (_ = _.convertToNumberObjectValue()), _.isError())
        return _;
      if (d.isString() && (d = d.convertToNumberObjectValue()), d.isError())
        return d;
      const C = +_.getValue(), E = +d.getValue();
      if (C === 0 || E === 0)
        return R.create(0);
      const b = (E < 0 ? -Qe(C / Math.abs(E), 0) : Qe(C / E, 0)) * E;
      return R.create(b);
    });
    return n === 1 && s === 1 ? c.get(0, 0) : c;
  }
}
class HC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      let h = c, d = a.get(l, f);
      if (h.isString() && (h = h.convertToNumberObjectValue()), d.isString() && (d = d.convertToNumberObjectValue()), h.isError())
        return h;
      if (d.isError())
        return d;
      const _ = Math.floor(+h.getValue()), C = Math.floor(+d.getValue());
      if (_ < 0 || C < 0 || _ < C)
        return g.create(m.NUM);
      const E = nr(_, C);
      return Number.isNaN(E) || !Number.isFinite(E) ? g.create(m.NUM) : R.create(E);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
}
class QC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      let h = c, d = a.get(l, f);
      if (h.isString() && (h = h.convertToNumberObjectValue()), d.isString() && (d = d.convertToNumberObjectValue()), h.isError())
        return h;
      if (d.isError())
        return d;
      const _ = Math.floor(+h.getValue()), C = Math.floor(+d.getValue());
      if (_ < 0 || C < 0 || _ === 0 && _ < C)
        return g.create(m.NUM);
      const E = nr(_ + C - 1, _ - 1);
      return Number.isNaN(E) || !Number.isFinite(E) ? g.create(m.NUM) : R.create(E);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
}
class GC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.cos();
  }
}
class WC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.cosh();
  }
}
class qC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = +t.getValue();
    return Math.abs(r) >= 2 ** 27 ? g.create(m.NUM) : r === 0 ? g.create(m.DIV_BY_ZERO) : t.tan().getReciprocal();
  }
}
class KC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : +t.getValue() === 0 ? g.create(m.DIV_BY_ZERO) : t.tanh().getReciprocal();
  }
}
class XC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = +t.getValue();
    return Math.abs(r) >= 2 ** 27 ? g.create(m.NUM) : r === 0 ? g.create(m.DIV_BY_ZERO) : t.sin().getReciprocal();
  }
}
class ZC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = +t.getValue();
    return r === 0 ? g.create(m.DIV_BY_ZERO) : !Number.isNaN(r) && !Number.isFinite(Math.sinh(r)) ? R.create(0) : t.sinh().getReciprocal();
  }
}
class zC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.map((c, l, f) => {
      if (c.isError())
        return c;
      let h = a.get(l, f);
      if (h.isString() && (h = h.convertToNumberObjectValue()), h.isError())
        return h;
      const d = `${c.getValue()}`, _ = Math.floor(+h.getValue());
      if (q(d) && (+d < 0 || +d >= 2 ** 53 || !Number.isInteger(+d)) || d.toLocaleLowerCase() === "true" || d.toLocaleLowerCase() === "false" || _ < 2 || _ > 36)
        return g.create(m.NUM);
      if (d.replace(/\s/g, "") === "")
        return R.create(0);
      if (!this._isValidCharForRadix(d, _))
        return g.create(m.NUM);
      const C = Number.parseInt(d, _);
      return Number.isNaN(C) ? g.create(m.NUM) : R.create(C);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
  _isValidCharForRadix(e, t) {
    for (const r of e) {
      const n = r.toUpperCase().charCodeAt(0);
      if (t <= 10 && !(n >= 48 && n < 48 + t) || t > 10 && !(n >= 48 && n < 58 || n >= 65 && n < 65 + t - 10))
        return !1;
    }
    return !0;
  }
}
class JC extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = +t.getValue();
    if (!Number.isFinite(r))
      return g.create(m.VALUE);
    const n = r * (180 / Math.PI);
    return Number.isNaN(n) ? g.create(m.VALUE) : R.create(n);
  }
}
class e_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = +t.getValue();
    if (!Number.isFinite(r))
      return g.create(m.VALUE);
    const n = (r < 0 ? -Qe(Math.abs(r) / 2, 0) : Qe(r / 2, 0)) * 2;
    return Number.isNaN(n) ? g.create(m.VALUE) : n === 0 ? R.create(0) : R.create(n);
  }
}
class t_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.exp();
  }
}
class r_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.mapValue((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = Math.floor(+t.getValue()), n = Cr(r);
    return Number.isNaN(n) || !Number.isFinite(n) ? g.create(m.NUM) : R.create(n);
  }
}
class n_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    if (e.isArray()) {
      const r = e.getRowCount(), n = e.getColumnCount();
      if (r > 1 || n > 1)
        return g.create(m.VALUE);
      t = e.get(0, 0);
    }
    return this._handleSingleObject(t);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = Math.floor(+t.getValue()), n = Cr(r, 2);
    return Number.isNaN(n) || !Number.isFinite(n) ? g.create(m.NUM) : R.create(n);
  }
}
class s_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.map((c, l, f) => {
      let h = a.get(l, f), d = c;
      if (d.isString() && (d = d.convertToNumberObjectValue()), d.isError())
        return d;
      if (h.isString() && (h = h.convertToNumberObjectValue()), h.isError())
        return h;
      const _ = +d.getValue(), C = +h.getValue();
      if (_ > 0 && C < 0)
        return g.create(m.NUM);
      if (_ === 0)
        return R.create(0);
      if (C === 0)
        return g.create(m.DIV_BY_ZERO);
      const E = Xe(_ / C, 0) * C;
      return R.create(E);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
}
class a_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = t != null ? t : R.create(1), s = r != null ? r : R.create(0), a = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1
    ), u = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1
    ), c = O(a, u, e, g.create(m.NA)), l = O(a, u, n, g.create(m.NA)), f = O(a, u, s, g.create(m.NA)), h = c.map((d, _, C) => {
      let E = d, b = l.get(_, C), y = f.get(_, C);
      if (E.isString() && (E = E.convertToNumberObjectValue()), E.isError())
        return E;
      if (b.isString() && (b = b.convertToNumberObjectValue()), b.isError())
        return b;
      if (y.isString() && (y = y.convertToNumberObjectValue()), y.isError())
        return y;
      const p = +E.getValue(), N = +b.getValue(), w = +y.getValue();
      if (p === 0 || N === 0)
        return R.create(0);
      let S;
      return p < 0 && w !== 0 ? S = (N < 0 ? Xe(Math.abs(p) / Math.abs(N), 0) : -Xe(Math.abs(p) / N, 0)) * N : S = (N < 0 ? -Xe(p / Math.abs(N), 0) : Xe(p / N, 0)) * N, R.create(S);
    });
    return a === 1 && u === 1 ? h.get(0, 0) : h;
  }
}
class i_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(1);
    if (e.isError())
      return e;
    if (r.isError())
      return r;
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, r, g.create(m.NA)), c = a.map((l, f, h) => {
      let d = u.get(f, h), _ = l;
      if (_.isString() && (_ = _.convertToNumberObjectValue()), _.isError())
        return _;
      if (d.isString() && (d = d.convertToNumberObjectValue()), d.isError())
        return d;
      const C = +_.getValue(), E = +d.getValue();
      if (C === 0 || E === 0)
        return R.create(0);
      const b = (E < 0 ? -Xe(C / Math.abs(E), 0) : Xe(C / E, 0)) * E;
      return R.create(b);
    });
    return n === 1 && s === 1 ? c.get(0, 0) : c;
  }
}
class o_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = 0;
    for (let r = 0; r < e.length; r++) {
      const n = e[r];
      if (!n.isNull())
        if (n.isArray()) {
          let s = !1, a = g.create(m.VALUE);
          if (n.iterator((u) => {
            if (u != null && u.isNull())
              return !0;
            const { isError: c, errorObject: l, number: f } = this._handleSingleObject(u);
            if (c)
              return s = !0, a = l, !1;
            t = fa(t, f);
          }), s)
            return a;
        } else {
          const { isError: s, errorObject: a, number: u } = this._handleSingleObject(n);
          if (s)
            return a;
          t = fa(t, u);
        }
    }
    return R.create(t);
  }
  _handleSingleObject(e) {
    if (e.isBoolean())
      return {
        isError: !0,
        errorObject: g.create(m.VALUE),
        number: null
      };
    let t = e;
    if (e.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return {
        isError: !0,
        errorObject: t,
        number: null
      };
    const r = Math.floor(+t.getValue());
    return r < 0 || r >= 2 ** 53 ? {
      isError: !0,
      errorObject: g.create(m.NUM),
      number: null
    } : {
      isError: !1,
      errorObject: null,
      number: r
    };
  }
}
class u_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.mapValue((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = Math.floor(+t.getValue());
    return R.create(r);
  }
}
class c_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = 1, r = !0;
    for (let n = 0; n < e.length; n++) {
      const s = e[n];
      if (!s.isNull())
        if (s.isArray()) {
          let a = !1, u = g.create(m.VALUE);
          if (s.iterator((c) => {
            if (c != null && c.isNull())
              return !0;
            const { isError: l, errorObject: f, number: h } = this._handleSingleObject(c);
            if (l)
              return a = !0, u = f, !1;
            t = Hi(t, h), r = !1;
          }), a)
            return u;
        } else {
          const { isError: a, errorObject: u, number: c } = this._handleSingleObject(s);
          if (a)
            return u;
          t = Hi(t, c), r = !1;
        }
    }
    return r ? R.create(0) : Number.isNaN(t) || !Number.isFinite(t) || t >= 2 ** 53 ? g.create(m.VALUE) : R.create(t);
  }
  _handleSingleObject(e) {
    if (e.isBoolean())
      return {
        isError: !0,
        errorObject: g.create(m.VALUE),
        number: null
      };
    let t = e;
    if (e.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return {
        isError: !0,
        errorObject: t,
        number: null
      };
    const r = Math.floor(+t.getValue());
    return r < 0 ? {
      isError: !0,
      errorObject: g.create(m.NUM),
      number: null
    } : {
      isError: !1,
      errorObject: null,
      number: r
    };
  }
}
class l_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.log();
  }
}
class f_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(10);
    if (e.isError())
      return e;
    if (r.isError())
      return r;
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, r, g.create(m.NA)), c = a.map((l, f, h) => {
      let d = u.get(f, h), _ = l;
      if (_.isString() && (_ = _.convertToNumberObjectValue()), _.isError())
        return _;
      if (d.isString() && (d = d.convertToNumberObjectValue()), d.isError())
        return d;
      const C = +_.getValue(), E = +d.getValue();
      if (C <= 0 || E <= 0)
        return g.create(m.NUM);
      const b = Math.log(E);
      if (b === 0)
        return g.create(m.DIV_BY_ZERO);
      const y = Math.log(C) / b;
      return R.create(y);
    });
    return n === 1 && s === 1 ? c.get(0, 0) : c;
  }
}
class h_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.log10();
  }
}
class m_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const t = e.isArray() ? e.getRowCount() : 1, r = e.isArray() ? e.getColumnCount() : 1, n = [];
    for (let a = 0; a < t; a++) {
      const u = [];
      for (let c = 0; c < r; c++) {
        let l = e.isArray() ? e.get(a, c) : e;
        if (l.isError())
          return l;
        if (l.isNull() || l.isBoolean())
          return g.create(m.VALUE);
        if (l.isString() && (l = l.convertToNumberObjectValue()), l.isError())
          return l;
        const f = +l.getValue();
        u.push(f);
      }
      n.push(u);
    }
    if (t !== r)
      return g.create(m.VALUE);
    const s = ka(n);
    return R.create(s);
  }
}
class g_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    const t = e.isArray() ? e.getRowCount() : 1, r = e.isArray() ? e.getColumnCount() : 1, n = [];
    for (let a = 0; a < t; a++) {
      const u = [];
      for (let c = 0; c < r; c++) {
        let l = e.isArray() ? e.get(a, c) : e;
        if (l.isError())
          return l;
        if (l.isNull() || l.isBoolean())
          return g.create(m.VALUE);
        if (l.isString() && (l = l.convertToNumberObjectValue()), l.isError())
          return l;
        const f = +l.getValue();
        u.push(f);
      }
      n.push(u);
    }
    if (t !== r)
      return g.create(m.VALUE);
    const s = dm(n);
    return s === null ? g.create(m.NUM) : W.createByArray(s);
  }
}
class d_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    if (n !== s)
      return g.create(m.VALUE);
    const u = this._getMatrix(e, r, n), c = this._getMatrix(t, s, a);
    if (u instanceof g)
      return u;
    if (c instanceof g)
      return c;
    const l = _r(u, c);
    return W.createByArray(l);
  }
  _getMatrix(e, t, r) {
    const n = [];
    for (let s = 0; s < t; s++) {
      const a = [];
      for (let u = 0; u < r; u++) {
        let c = e.isArray() ? e.get(s, u) : e;
        if (c.isError())
          return c;
        if (c.isNull() || c.isBoolean())
          return g.create(m.VALUE);
        if (c.isString() && (c = c.convertToNumberObjectValue()), c.isError())
          return c;
        const l = +c.getValue();
        a.push(l);
      }
      n.push(a);
    }
    return n;
  }
}
class C_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = e;
    if (r.isString() && (r = r.convertToNumberObjectValue()), r.isError())
      return r;
    let n = t;
    return n.isString() && (n = n.convertToNumberObjectValue()), n.isError() ? n : r.mod(n);
  }
}
class __ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = e;
    if (r.isArray()) {
      const c = r.getRowCount(), l = r.getColumnCount();
      if (c > 1 || l > 1)
        return g.create(m.VALUE);
      r = r.get(0, 0);
    }
    if (r.isError())
      return r;
    let n = t;
    if (n.isArray()) {
      const c = n.getRowCount(), l = n.getColumnCount();
      if (c > 1 || l > 1)
        return g.create(m.VALUE);
      n = n.get(0, 0);
    }
    if (n.isError())
      return n;
    if (r.isBoolean() || n.isBoolean())
      return g.create(m.VALUE);
    const s = +r.getValue(), a = +n.getValue();
    if (Number.isNaN(s) || Number.isNaN(a))
      return g.create(m.VALUE);
    if (a === 0)
      return R.create(0);
    if (s > 0 && a < 0 || s < 0 && a > 0)
      return g.create(m.NUM);
    const u = Ft(s / a, 0) * a;
    return R.create(u);
  }
}
class A_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = 0, r = 1;
    for (let s = 0; s < e.length; s++) {
      const a = e[s];
      if (!a.isNull())
        if (a.isArray()) {
          let u = !1, c = g.create(m.VALUE);
          if (a.iterator((l) => {
            if (l != null && l.isNull())
              return !0;
            const { isError: f, errorObject: h, number: d } = this._handleSingleObject(l);
            if (f)
              return u = !0, c = h, !1;
            if (t += d, t > 170)
              return u = !0, c = g.create(m.NUM), !1;
            r *= Cr(d);
          }), u)
            return c;
        } else {
          const { isError: u, errorObject: c, number: l } = this._handleSingleObject(a);
          if (u)
            return c;
          if (t += l, t > 170)
            return g.create(m.NUM);
          r *= Cr(l);
        }
    }
    const n = Cr(t) / r;
    return R.create(n);
  }
  _handleSingleObject(e) {
    if (e.isBoolean())
      return {
        isError: !0,
        errorObject: g.create(m.VALUE),
        number: null
      };
    let t = e;
    if (e.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return {
        isError: !0,
        errorObject: t,
        number: null
      };
    const r = Math.floor(+t.getValue());
    return r < 0 ? {
      isError: !0,
      errorObject: g.create(m.NUM),
      number: null
    } : {
      isError: !1,
      errorObject: null,
      number: r
    };
  }
}
class E_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.getRowCount(), r = e.getColumnCount(), n = e.mapValue((s) => {
        const a = this._handleSingleObject(s);
        return a.isError() ? a : t > 1 || r > 1 ? a.get(0, 0) : a;
      });
      return t === 1 && r === 1 ? n.get(0, 0) : n;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = Math.floor(+t.getValue());
    if (r <= 0)
      return g.create(m.VALUE);
    const n = [];
    for (let s = 0; s < r; s++) {
      n[s] = [];
      for (let a = 0; a < r; a++)
        n[s][a] = s === a ? 1 : 0;
    }
    return W.createByArray(n);
  }
}
class b_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = +t.getValue();
    if (!Number.isFinite(r))
      return g.create(m.VALUE);
    let n = r < 0 ? -Qe(Math.abs(r), 0) : Qe(r, 0);
    return Number.isNaN(n) ? g.create(m.VALUE) : (Math.abs(n) % 2 === 0 && (r < 0 ? n-- : n++), R.create(n));
  }
}
class y_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 0);
    A(this, "maxParams", 0);
  }
  calculate() {
    return R.create(Math.PI);
  }
}
class R_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = e;
    if (r.isString() && (r = r.convertToNumberObjectValue()), r.isError())
      return r;
    let n = t;
    return n.isString() && (n = n.convertToNumberObjectValue()), n.isError() ? n : r.pow(n);
  }
}
class p_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = R.create(1), r = !0;
    for (let n = 0; n < e.length; n++) {
      let s = e[n];
      if (s.isError())
        return s;
      if (s.isArray()) {
        let a = !1, u = null;
        if (s.iterator((c) => {
          if (c != null && c.isError())
            return a = !0, u = c, !1;
          if (!c || c.isNull() || c.isString() || c.isBoolean())
            return !0;
          t = t.multiply(c), r = !1;
        }), a)
          return u;
      } else {
        if (s.isNull())
          continue;
        if (s.isString() && (s = s.convertToNumberObjectValue()), s.isError())
          return s;
        t = t.multiply(s), r = !1;
      }
      if (t.isError())
        return t;
    }
    return r ? R.create(0) : t;
  }
}
class N_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const { isError: r, errorObject: n, variants: s } = H(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = +u.getValue();
    if (Number.isNaN(c) || Number.isNaN(l))
      return g.create(m.VALUE);
    if (l === 0)
      return g.create(m.DIV_BY_ZERO);
    const f = Math.trunc(c / l);
    return R.create(f);
  }
}
class V_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = +t.getValue();
    if (!Number.isFinite(r))
      return g.create(m.VALUE);
    const n = r * (Math.PI / 180);
    return Number.isNaN(n) ? g.create(m.VALUE) : R.create(n);
  }
}
class O_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 0);
    A(this, "maxParams", 0);
  }
  calculate() {
    return R.create(Math.random());
  }
}
class w_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 0);
    A(this, "maxParams", 5);
    A(this, "needsSheetRowColumnCount", !0);
  }
  calculate(e, t, r, n, s) {
    if (e != null && e.isError())
      return e;
    if (t != null && t.isError())
      return t;
    if (r != null && r.isError())
      return r;
    if (n != null && n.isError())
      return n;
    if (s != null && s.isError())
      return s;
    const a = e != null ? e : R.create(1), u = t != null ? t : R.create(1), c = r != null ? r : R.create(0), l = n != null ? n : R.create(1), f = s != null ? s : R.create(0);
    return this._calculateResult(a, u, c, l, f);
  }
  _calculateResult(e, t, r, n, s) {
    const a = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1
    ), u = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1
    );
    if (a === 1 && u === 1)
      return this._calculateSingleCell(e, t, r, n, s);
    const c = O(a, u, e, g.create(m.NA)), l = O(a, u, t, g.create(m.NA)), f = O(a, u, r, g.create(m.NA)), h = O(a, u, n, g.create(m.NA)), d = O(a, u, s, g.create(m.NA));
    return c.map((_, C, E) => {
      const b = l.get(C, E), y = f.get(C, E), p = h.get(C, E), N = d.get(C, E), w = this._handleError(_, b, y, p, N);
      if (w.errorObject)
        return w.errorObject;
      let { minValue: S, maxValue: M, wholeNumberValue: D } = w, L;
      return D ? (S = Math.ceil(S), M = Math.floor(M), L = Math.floor(Math.random() * (M - S + 1)) + S) : L = Math.random() * (M - S) + S, L < S || L > M ? g.create(m.VALUE) : R.create(L);
    });
  }
  _calculateSingleCell(e, t, r, n, s) {
    let a = e;
    a.isArray() && (a = a.get(0, 0));
    let u = t;
    u.isArray() && (u = u.get(0, 0));
    let c = r;
    c.isArray() && (c = c.get(0, 0));
    let l = n;
    l.isArray() && (l = l.get(0, 0));
    let f = s;
    f.isArray() && (f = f.get(0, 0));
    const h = this._handleError(a, u, c, l, f);
    if (h.errorObject)
      return h.errorObject;
    let { rowsValue: d, columnsValue: _, minValue: C, maxValue: E, wholeNumberValue: b } = h;
    if (b && (C = Math.ceil(C), E = Math.floor(E), C > E))
      return g.create(m.VALUE);
    const y = [];
    for (let p = 0; p < d; p++) {
      const N = [];
      for (let w = 0; w < _; w++)
        b ? N.push(Math.floor(Math.random() * (E - C + 1)) + C) : N.push(Math.random() * (E - C) + C);
      y.push(N);
    }
    return d === 1 && _ === 1 ? R.create(y[0][0]) : W.createByArray(y);
  }
  _handleError(e, t, r, n, s) {
    let a = e;
    if (a.isString() && (a = a.convertToNumberObjectValue()), a.isError())
      return {
        errorObject: a
      };
    let u = t;
    if (u.isString() && (u = u.convertToNumberObjectValue()), u.isError())
      return {
        errorObject: u
      };
    let c = r;
    if (c.isString() && (c = c.convertToNumberObjectValue()), c.isError())
      return {
        errorObject: c
      };
    let l = n;
    if (l.isString() && (l = l.convertToNumberObjectValue()), l.isError())
      return {
        errorObject: l
      };
    let f = s;
    return f.isString() && (f = f.convertToNumberObjectValue()), f.isError() ? {
      errorObject: f
    } : this._getValue(a, u, c, l, f);
  }
  _getValue(e, t, r, n, s) {
    const a = Math.floor(+e.getValue()), u = Math.floor(+t.getValue());
    if (a === 0 || u === 0)
      return {
        errorObject: g.create(m.CALC)
      };
    const c = this._rowCount - this.row, l = this._columnCount - this.column;
    if (a < 0 || u < 0 || a * u > 10 ** 7)
      return {
        errorObject: g.create(m.VALUE)
      };
    if (a > c || u > l)
      return {
        errorObject: g.create(m.REF)
      };
    const f = +r.getValue(), h = +n.getValue(), d = +s.getValue();
    return f > h ? {
      errorObject: g.create(m.VALUE)
    } : d && (!Number.isInteger(f) || !Number.isInteger(h)) ? {
      errorObject: g.create(m.VALUE)
    } : {
      rowsValue: a,
      columnsValue: u,
      minValue: f,
      maxValue: h,
      wholeNumberValue: d
    };
  }
}
class S_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = e;
    if (r.isArray()) {
      const c = r.getRowCount(), l = r.getColumnCount();
      if (c > 1 || l > 1)
        return g.create(m.VALUE);
      r = r.get(0, 0);
    }
    if (r.isError())
      return r;
    let n = t;
    if (n.isArray()) {
      const c = n.getRowCount(), l = n.getColumnCount();
      if (c > 1 || l > 1)
        return g.create(m.VALUE);
      n = n.get(0, 0);
    }
    if (n.isError())
      return n;
    if (r.isBoolean() || n.isBoolean())
      return g.create(m.VALUE);
    let s = +r.getValue(), a = +n.getValue();
    if (Number.isNaN(s) || Number.isNaN(a))
      return g.create(m.VALUE);
    if (s > a)
      return g.create(m.NUM);
    s = Math.ceil(s), a = Math.floor(a);
    const u = Math.floor(Math.random() * (a - s + 1)) + s;
    return R.create(u);
  }
}
class M_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(0), n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, r, g.create(m.NA)), c = a.mapValue((l, f, h) => {
      const d = u.get(f, h);
      return l.isError() ? l : d.isError() ? d : this._handleSingleObject(l, d);
    });
    return n === 1 && s === 1 ? c.get(0, 0) : c;
  }
  _handleSingleObject(e, t) {
    const { isError: r, errorObject: n, variants: s } = I(e);
    if (r)
      return n;
    const [a] = s;
    let u = Math.floor(+a.getValue()), c = t;
    if (c.isString() && (c = c.convertToNumberObjectValue(), c.isError()))
      return c;
    let l = Math.floor(+c.getValue());
    if (c.isBoolean() && (l = c.getValue() ? 0 : 4), u < 0 || u > 3999 || l < 0 || l > 4)
      return g.create(m.VALUE);
    const f = pm[l];
    let h = f.length - 1, d = "";
    for (; u > 0; ) {
      h = this._binarySearch(u, 0, h, f);
      const _ = f[h];
      u -= _, d += Rm.get(_);
    }
    return U.create(d);
  }
  _binarySearch(e, t, r, n) {
    let s = t, a = r;
    for (; a - s > 1; ) {
      const u = Math.floor((s + a) / 2), c = n[u];
      if (c === e)
        return u;
      c > e ? a = u : s = u;
    }
    return s !== a && n[a] <= e ? a : s;
  }
}
class D_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = e;
    if (r.isString() && (r = r.convertToNumberObjectValue()), r.isError())
      return r;
    let n = t;
    return n.isString() && (n = n.convertToNumberObjectValue()), n.isError() ? n : r.round(n);
  }
}
class L_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      if (c.isError())
        return c;
      if (h.isError())
        return h;
      const { isError: d, errorObject: _, variants: C } = I(c, h);
      if (d)
        return _;
      const [E, b] = C, y = +E.getValue(), p = Math.trunc(+b.getValue()), N = this._roundBank(y, p);
      return R.create(N);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
  _roundBank(e, t) {
    if (t > 16)
      return e;
    if (t < -16)
      return 0;
    const r = 1e-8, n = 10 ** t, s = +(e * n).toFixed(8), a = Math.floor(s), u = s - a;
    let c = Math.round(s);
    return u > 0.5 - r && u < 0.5 + r && (c = a % 2 === 0 ? a : a + 1), t ? c / n : c;
  }
}
class x_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = e;
    if (r.isString() && (r = r.convertToNumberObjectValue()), r.isError())
      return r;
    let n = t;
    return n.isString() && (n = n.convertToNumberObjectValue()), n.isError() ? n : r.floor(n);
  }
}
class P_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = e;
    if (r.isString() && (r = r.convertToNumberObjectValue()), r.isError())
      return r;
    let n = t;
    return n.isString() && (n = n.convertToNumberObjectValue()), n.isError() ? n : r.ceil(n);
  }
}
class j_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = +t.getValue();
    return Math.abs(r) >= 2 ** 27 ? g.create(m.NUM) : t.cos().getReciprocal();
  }
}
class U_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.map((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    let t = e;
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = +t.getValue();
    return Number.isFinite(Math.cosh(r)) ? Math.abs(r) >= 2 ** 27 ? g.create(m.NUM) : t.cosh().getReciprocal() : R.create(0);
  }
}
class v_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 4);
    A(this, "needsSheetRowColumnCount", !0);
  }
  calculate(e, t, r, n) {
    let s = e, a = t != null ? t : R.create(1), u = r != null ? r : R.create(1), c = n != null ? n : R.create(1);
    s.isNull() && (s = R.create(1)), a.isNull() && (a = R.create(1)), u.isNull() && (u = R.create(1)), c.isNull() && (c = R.create(1));
    const l = Math.max(
      s.isArray() ? s.getRowCount() : 1,
      a.isArray() ? a.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1
    ), f = Math.max(
      s.isArray() ? s.getColumnCount() : 1,
      a.isArray() ? a.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1
    ), h = O(l, f, s, g.create(m.NA)), d = O(l, f, a, g.create(m.NA)), _ = O(l, f, u, g.create(m.NA)), C = O(l, f, c, g.create(m.NA)), E = h.mapValue((b, y, p) => {
      const N = d.get(y, p), w = _.get(y, p), S = C.get(y, p);
      return b.isError() ? b : N.isError() ? N : w.isError() ? w : S.isError() ? S : this._getResult(b, N, w, S, l, f);
    });
    return l === 1 && f === 1 ? E.get(0, 0) : E;
  }
  _getResult(e, t, r, n, s, a) {
    const { isError: u, errorObject: c, variants: l } = I(e, t, r, n);
    if (u)
      return c;
    const [f, h, d, _] = l, C = Math.floor(+f.getValue()), E = Math.floor(+h.getValue()), b = +d.getValue(), y = +_.getValue();
    if (C < 0 || E < 0 || C * E > 10 ** 7)
      return g.create(m.VALUE);
    if (C === 0 || E === 0)
      return g.create(m.CALC);
    const p = this._rowCount - this.row, N = this._columnCount - this.column;
    if (C > p || E > N)
      return g.create(m.REF);
    const w = [];
    for (let S = 0; S < C; S++) {
      w[S] = [];
      for (let M = 0; M < E; M++)
        w[S][M] = b + (S * E + M) * y;
    }
    return s > 1 || a > 1 ? R.create(w[0][0]) : W.createByArray(w);
  }
}
class T_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    if (e.isNull() || t.isNull() || r.isNull() || n.isNull())
      return g.create(m.NA);
    const { isError: s, errorObject: a, variants: u } = H(e, t, r);
    if (s)
      return a;
    const { isError: c, errorObject: l, variants: f } = I(...u);
    if (c)
      return l;
    const [h, d, _] = f, C = +h.getValue(), E = +d.getValue(), b = +_.getValue(), y = [];
    if (n.isArray()) {
      let N = !1, w = g.create(m.VALUE);
      if (n.iterator((S) => {
        const { isError: M, errorObject: D, coefficientsObject: L } = this._handleSingleObject(S);
        if (M)
          return N = !0, w = D, !1;
        const v = +L.getValue();
        y.push(v);
      }), N)
        return w;
    } else {
      const { isError: N, errorObject: w, coefficientsObject: S } = this._handleSingleObject(n);
      if (N)
        return w;
      const M = +S.getValue();
      y.push(M);
    }
    let p = 0;
    for (let N = 0; N < y.length; N++)
      p += y[N] * C ** (E + N * b);
    return Number.isNaN(p) || !Number.isFinite(p) ? g.create(m.NUM) : R.create(p);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return {
        isError: !0,
        errorObject: e,
        coefficientsObject: null
      };
    if (e != null && e.isBoolean())
      return {
        isError: !0,
        errorObject: g.create(m.VALUE),
        coefficientsObject: null
      };
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? {
      isError: !0,
      errorObject: t,
      coefficientsObject: null
    } : {
      isError: !1,
      errorObject: null,
      coefficientsObject: t
    };
  }
}
class B_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    const { isError: t, errorObject: r, variants: n } = I(e);
    if (t)
      return r;
    const [s] = n, a = +s.getValue();
    return a > 0 ? R.create(1) : a < 0 ? R.create(-1) : R.create(0);
  }
}
class I_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.sin();
  }
}
class F_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.sinh();
  }
}
class k_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.sqrt();
  }
}
class $_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    if (t.isArray()) {
      const s = t.getRowCount(), a = t.getColumnCount();
      if (s > 1 || a > 1)
        return g.create(m.VALUE);
      t = t.get(0, 0);
    }
    if (t.isString() && (t = t.convertToNumberObjectValue()), t.isError())
      return t;
    const r = +t.getValue();
    if (r < 0)
      return g.create(m.NUM);
    const n = Math.sqrt(r * Math.PI);
    return R.create(n);
  }
}
class Y_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 255);
    A(this, "needsReferenceObject", !0);
    A(this, "needsFilteredOutRows", !0);
    A(this, "needsFormulaDataModel", !0);
  }
  calculate(e, ...t) {
    return e.isError() ? e : e.isReferenceObject() ? e.toArrayValueObject().mapValue((r) => this._handleSingleObject(r, ...t)) : this._handleSingleObject(e, ...t);
  }
  // eslint-disable-next-line max-lines-per-function, complexity
  _handleSingleObject(e, ...t) {
    const r = this._getIndexNumValue(e);
    let n;
    if (r instanceof g)
      return r;
    switch (r) {
      case 1:
        n = this._average(!1, ...t);
        break;
      case 2:
        n = this._count(!1, ...t);
        break;
      case 3:
        n = this._counta(!1, ...t);
        break;
      case 4:
        n = this._max(!1, ...t);
        break;
      case 5:
        n = this._min(!1, ...t);
        break;
      case 6:
        n = this._product(!1, ...t);
        break;
      case 7:
        n = this._stdev(!1, ...t);
        break;
      case 8:
        n = this._stdevp(!1, ...t);
        break;
      case 9:
        n = this._sum(!1, ...t);
        break;
      case 10:
        n = this._var(!1, ...t);
        break;
      case 11:
        n = this._varp(!1, ...t);
        break;
      case 101:
        n = this._average(!0, ...t);
        break;
      case 102:
        n = this._count(!0, ...t);
        break;
      case 103:
        n = this._counta(!0, ...t);
        break;
      case 104:
        n = this._max(!0, ...t);
        break;
      case 105:
        n = this._min(!0, ...t);
        break;
      case 106:
        n = this._product(!0, ...t);
        break;
      case 107:
        n = this._stdev(!0, ...t);
        break;
      case 108:
        n = this._stdevp(!0, ...t);
        break;
      case 109:
        n = this._sum(!0, ...t);
        break;
      case 110:
        n = this._var(!0, ...t);
        break;
      case 111:
        n = this._varp(!0, ...t);
        break;
      default:
        n = g.create(m.VALUE);
    }
    return n;
  }
  _getIndexNumValue(e) {
    const t = e ? Number(e.getValue()) : 0;
    if (Number.isNaN(t))
      return g.create(m.VALUE);
    const r = Math.floor(t);
    return r >= 1 && r <= 11 || r >= 101 && r <= 111 ? r : g.create(m.VALUE);
  }
  _average(e, ...t) {
    const r = this._flattenRefArray(e, ...t);
    return r.isError() ? r : r.mean();
  }
  _count(e, ...t) {
    let r = R.create(0);
    for (let n = 0; n < t.length; n++) {
      const s = t[n];
      if (!s.isReferenceObject())
        return g.create(m.VALUE);
      const a = s.getFilteredOutRows(), u = s.getRowData();
      s.iterator((c, l) => {
        if (a.includes(l) || e && this._isRowHidden(u, l))
          return !0;
        c != null && c.isNumber() && (r = r.plusBy(1));
      });
    }
    return r;
  }
  _counta(e, ...t) {
    let r = R.create(0);
    for (let n = 0; n < t.length; n++) {
      const s = t[n];
      if (!s.isReferenceObject())
        return g.create(m.VALUE);
      const a = s.getFilteredOutRows(), u = s.getRowData();
      s.iterator((c, l) => {
        if (a.includes(l) || e && this._isRowHidden(u, l) || c == null || c.isNull())
          return !0;
        r = r.plusBy(1);
      });
    }
    return r;
  }
  _max(e, ...t) {
    const r = this._flattenRefArray(e, ...t);
    return r.isError() ? r : this._isBlankArrayObject(r) ? R.create(0) : r.max();
  }
  _min(e, ...t) {
    const r = this._flattenRefArray(e, ...t);
    return r.isError() ? r : this._isBlankArrayObject(r) ? R.create(0) : r.min();
  }
  _product(e, ...t) {
    const r = this._flattenRefArray(e, ...t);
    if (r.isError())
      return r;
    if (this._isBlankArrayObject(r))
      return R.create(0);
    let n = R.create(1);
    return r.iterator((s) => {
      n = n.multiply(
        s
      );
    }), n;
  }
  _stdev(e, ...t) {
    const r = this._flattenRefArray(e, ...t);
    return r.isError() ? r : this._isBlankArrayObject(r) ? g.create(m.DIV_BY_ZERO) : r.std(1);
  }
  _stdevp(e, ...t) {
    const r = this._flattenRefArray(e, ...t);
    return r.isError() ? r : this._isBlankArrayObject(r) ? g.create(m.DIV_BY_ZERO) : r.std();
  }
  _sum(e, ...t) {
    const r = this._flattenRefArray(e, ...t);
    return r.isError() ? r : r.sum();
  }
  _var(e, ...t) {
    const r = this._flattenRefArray(e, ...t);
    return r.isError() ? r : this._isBlankArrayObject(r) ? g.create(m.DIV_BY_ZERO) : r.var(1);
  }
  _varp(e, ...t) {
    const r = this._flattenRefArray(e, ...t);
    return r.isError() ? r : this._isBlankArrayObject(r) ? g.create(m.DIV_BY_ZERO) : r.var();
  }
  _flattenRefArray(e, ...t) {
    var n, s;
    const r = [];
    r[0] = [];
    for (let a = 0; a < t.length; a++) {
      const u = t[a];
      if (u.isError())
        return u;
      if (!u.isReferenceObject())
        return g.create(m.VALUE);
      const c = u.getFilteredOutRows(), l = u.getRowData(), f = u.getUnitId(), h = u.getSheetId(), _ = (s = (n = u.getUnitData()[f]) == null ? void 0 : n[h]) == null ? void 0 : s.cellData;
      let C;
      if (u.iterator((E, b, y) => {
        if (c.includes(b) || e && this._isRowHidden(l, b))
          return !0;
        const p = _.getValue(b, y);
        if (p != null && p.f || p != null && p.si) {
          const N = this._formulaDataModel.getFormulaStringByCell(b, y, h, f);
          if (N && N.indexOf(`${this.name}(`) > -1)
            return !0;
        }
        if (E == null || E.isNull() || E.isString() || E.isBoolean())
          return !0;
        if (E.isError())
          return C = E, !1;
        r[0].push(E);
      }), C != null && C.isError())
        return C;
    }
    return Qr(r, 1, r[0].length);
  }
  _isRowHidden(e, t) {
    const r = e[t];
    return r ? r.hd === io.TRUE : !1;
  }
  _isBlankArrayObject(e) {
    return e.getArrayValue()[0].length === 0;
  }
}
class H_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = R.create(0);
    for (let r = 0; r < e.length; r++) {
      let n = e[r];
      if (n.isString() && (n = n.convertToNumberObjectValue()), n.isError() || (n.isArray() && (n = n.sum()), n.isError()))
        return n;
      if (t = t.plus(n), t.isError())
        return t;
    }
    return t;
  }
}
class Q_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 255);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, ...t) {
    const {
      isError: r,
      errorObject: n,
      rangeIsDifferentSize: s,
      criteriaMaxRowLength: a,
      criteriaMaxColumnLength: u,
      targetRange: c,
      variants: l
    } = Gr(t, e);
    if (r)
      return n;
    if (s)
      return a === 1 && u === 1 ? g.create(m.VALUE) : O(a, u, g.create(m.VALUE));
    if (a === 1 && u === 1)
      return this._scalarSumifs(c, l);
    const f = Rt(l, a, u, !0);
    return this._aggregateResults(c, f);
  }
  /**
   * Fast path: all criteria are scalar. Single pass over range data,
   * accumulating the sum-range value when all criteria match.
   */
  _scalarSumifs(e, t) {
    if (!e.isArray())
      return this._fallbackSumifs(e, t);
    const r = e, n = t.length / 2, s = new Array(n), a = new Array(n), u = new Array(n);
    for (let _ = 0; _ < n; _++) {
      const C = t[_ * 2], E = t[_ * 2 + 1];
      if (!C.isArray())
        return this._fallbackSumifs(e, t);
      s[_] = C;
      const b = E.isArray() && E.get(0, 0) || E;
      if (b.isError())
        return b;
      if (b.isString()) {
        const [y, p] = pt(`${b.getValue()}`);
        a[_] = y, u[_] = p;
      } else
        a[_] = x.EQUALS, u[_] = b;
    }
    const c = s[0].getRowCount(), l = s[0].getColumnCount();
    let f = 0;
    const h = new Array(n);
    let d = !0;
    for (let _ = 0; _ < n; _++) {
      const C = u[_].getValue();
      h[_] = typeof C == "string" ? C.toLocaleLowerCase() : C, typeof C == "string" && Mr(C) && (d = !1);
    }
    if (d) {
      for (let _ = 0; _ < c; _++)
        for (let C = 0; C < l; C++) {
          let E = !0;
          for (let b = 0; b < n; b++)
            if (!s[b].rawCompare(_, C, h[b], a[b])) {
              E = !1;
              break;
            }
          E && (f += r.getNumberDirect(_, C));
        }
      return R.create(f);
    }
    for (let _ = 0; _ < c; _++)
      for (let C = 0; C < l; C++) {
        let E = !0;
        for (let b = 0; b < n; b++) {
          const y = s[b].get(_, C);
          if (!y || y.isError()) {
            E = !1;
            break;
          }
          if (!Lr(y, u[b])) {
            const N = a[b];
            if (N === x.EQUALS || N === x.NOT_EQUAL) {
              if (y.isNumber() && u[b].isString()) {
                const w = u[b].convertToNumberObjectValue();
                if (w.isNumber()) {
                  const S = y.compare(w, N);
                  (S.isError() || S.getValue() !== !0) && (E = !1);
                  continue;
                }
              }
              if (u[b].isNumber() && y.isString()) {
                const w = y.convertToNumberObjectValue();
                if (w.isNumber()) {
                  const S = w.compare(u[b], N);
                  (S.isError() || S.getValue() !== !0) && (E = !1);
                  continue;
                }
              }
              E = N === x.NOT_EQUAL;
            } else
              E = !1;
            if (!E) break;
            continue;
          }
          const p = y.compare(u[b], a[b]);
          if (p.isError() || p.getValue() !== !0) {
            E = !1;
            break;
          }
        }
        if (E) {
          const b = r.get(_, C);
          b && b.isNumber() && (f += b.getValue());
        }
      }
    return R.create(f);
  }
  _fallbackSumifs(e, t) {
    const r = Rt(t, 1, 1, !0);
    return this._aggregateResults(e, r);
  }
  _aggregateResults(e, t) {
    const r = t.map((n) => n.map((s) => e.pick(s).sum()));
    return r.length === 1 && r[0].length === 1 ? r[0][0] : W.create({
      calculateValueList: r,
      rowCount: r.length,
      columnCount: r[0].length,
      unitId: this.unitId || "",
      sheetId: this.subUnitId || "",
      row: this.row,
      column: this.column
    });
  }
}
class G_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  // READNOW: MEMORY HOTSPOT #3 — SUMPRODUCT creates intermediate number[][]
  // arrays per variant. For SUMPRODUCT((A=B)*(C*D)), the comparison (A=B)
  // already materialized a full ArrayValueObject via mapValue before reaching
  // here. Then this method allocates another number[][] to accumulate products.
  // With 10K rows and 3 variants, that's 30K+ row arrays. The real cost is
  // upstream in mapValue — by the time we're here, the arrays are already in
  // memory. But the concat+reduce on line 90 creates yet another copy.
  calculate(e, ...t) {
    if (e.isError())
      return e;
    const r = this._initArray1(e);
    if (t.length > 0) {
      const n = r.getRowCount(), s = r.getColumnCount();
      for (let u = 0; u < t.length; u++) {
        if (t[u].isError())
          return t[u];
        let c = 1, l = 1;
        if (t[u].isArray() && (c = t[u].getRowCount(), l = t[u].getColumnCount()), c !== n || l !== s)
          return g.create(m.VALUE);
      }
      let a = 0;
      for (let u = 0; u < n; u++)
        for (let c = 0; c < s; c++) {
          const l = r.get(u, c);
          if (l.isError())
            return l;
          let f = l.isNumber() ? l.getValue() : 0;
          for (let h = 0; h < t.length; h++) {
            let d = t[h];
            if (t[h].isArray() && (d = t[h].get(u, c)), d.isError())
              return d;
            d.isNumber() ? f *= d.getValue() : f = 0;
          }
          a += f;
        }
      return R.create(a);
    } else
      return r.sum();
  }
  _initArray1(e) {
    let t = e;
    return t.isArray() || (t = W.create({
      calculateValueList: [[t]],
      rowCount: 1,
      columnCount: 1,
      unitId: "",
      sheetId: "",
      row: 0,
      column: 0
    })), t;
  }
}
class W_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = R.create(0);
    const r = R.create(2);
    for (let n = 0; n < e.length; n++) {
      let s = e[n];
      if (s.isString() && (s = s.convertToNumberObjectValue()), s.isError())
        return s;
      if (s.isArray() ? s.iterator((a) => {
        if (a == null || a.isString() || a.isBoolean() || a.isNull())
          return !0;
        if (a.isError())
          return t = a, !1;
        t = t.plus(
          a.pow(r)
        );
      }) : t = t.plus(s.pow(r)), t.isError())
        return t;
    }
    return t;
  }
}
class q_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = r * n, a = t.isArray() ? t.getRowCount() : 1, u = t.isArray() ? t.getColumnCount() : 1, c = a * u;
    if (s !== c)
      return g.create(m.NA);
    if (s === 1)
      return this._calculateSingleCell(e, t);
    {
      const l = e.flatten(), f = t.flatten();
      let h = ue.create();
      const d = [], _ = [];
      let C = 0;
      return l.iterator((E, b, y) => {
        const p = f.get(b, y);
        if (E == null || E.isString() || E.isBoolean() || E.isNull())
          return !0;
        if (E != null && E.isError())
          return h = E, !1;
        if (p == null || p.isString() || p.isBoolean() || p.isNull())
          return !0;
        if (p != null && p.isError())
          return h = p, !1;
        const N = +E.getValue(), w = +p.getValue();
        d.push(N), _.push(w), C += N ** 2 - w ** 2;
      }), h.isError() ? h : d.length === 0 || _.length === 0 ? g.create(m.DIV_BY_ZERO) : R.create(C);
    }
  }
  _calculateSingleCell(e, t) {
    let r = e;
    if (r.isArray() && (r = r.get(0, 0)), r.isError())
      return r;
    let n = t;
    if (n.isArray() && (n = n.get(0, 0)), n.isError())
      return n;
    if (r.isNull() || n.isNull())
      return g.create(m.VALUE);
    const s = +r.getValue(), a = +n.getValue();
    if (r.isString() && !q(s) || r.isBoolean() || n.isString() && !q(a) || n.isBoolean())
      return g.create(m.DIV_BY_ZERO);
    const u = s ** 2 - a ** 2;
    return R.create(u);
  }
}
class K_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = r * n, a = t.isArray() ? t.getRowCount() : 1, u = t.isArray() ? t.getColumnCount() : 1, c = a * u;
    if (s !== c)
      return g.create(m.NA);
    if (s === 1)
      return this._calculateSingleCell(e, t);
    {
      const l = e.flatten(), f = t.flatten();
      let h = ue.create();
      const d = [], _ = [];
      let C = 0;
      return l.iterator((E, b, y) => {
        const p = f.get(b, y);
        if (E == null || E.isString() || E.isBoolean() || E.isNull())
          return !0;
        if (E != null && E.isError())
          return h = E, !1;
        if (p == null || p.isString() || p.isBoolean() || p.isNull())
          return !0;
        if (p != null && p.isError())
          return h = p, !1;
        const N = +E.getValue(), w = +p.getValue();
        d.push(N), _.push(w), C += N ** 2 + w ** 2;
      }), h.isError() ? h : d.length === 0 || _.length === 0 ? g.create(m.DIV_BY_ZERO) : R.create(C);
    }
  }
  _calculateSingleCell(e, t) {
    let r = e;
    if (r.isArray() && (r = r.get(0, 0)), r.isError())
      return r;
    let n = t;
    if (n.isArray() && (n = n.get(0, 0)), n.isError())
      return n;
    if (r.isNull() || n.isNull())
      return g.create(m.VALUE);
    const s = +r.getValue(), a = +n.getValue();
    if (r.isString() && !q(s) || r.isBoolean() || n.isString() && !q(a) || n.isBoolean())
      return g.create(m.DIV_BY_ZERO);
    const u = s ** 2 + a ** 2;
    return R.create(u);
  }
}
class X_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = r * n, a = t.isArray() ? t.getRowCount() : 1, u = t.isArray() ? t.getColumnCount() : 1, c = a * u;
    if (s !== c)
      return g.create(m.NA);
    if (s === 1)
      return this._calculateSingleCell(e, t);
    {
      const l = e.flatten(), f = t.flatten();
      let h = ue.create();
      const d = [], _ = [];
      let C = 0;
      return l.iterator((E, b, y) => {
        const p = f.get(b, y);
        if (E == null || E.isString() || E.isBoolean() || E.isNull())
          return !0;
        if (E != null && E.isError())
          return h = E, !1;
        if (p == null || p.isString() || p.isBoolean() || p.isNull())
          return !0;
        if (p != null && p.isError())
          return h = p, !1;
        const N = +E.getValue(), w = +p.getValue();
        d.push(N), _.push(w), C += (N - w) ** 2;
      }), h.isError() ? h : d.length === 0 || _.length === 0 ? g.create(m.DIV_BY_ZERO) : R.create(C);
    }
  }
  _calculateSingleCell(e, t) {
    let r = e;
    if (r.isArray() && (r = r.get(0, 0)), r.isError())
      return r;
    let n = t;
    if (n.isArray() && (n = n.get(0, 0)), n.isError())
      return n;
    if (r.isNull() || n.isNull())
      return g.create(m.VALUE);
    const s = +r.getValue(), a = +n.getValue();
    if (r.isString() && !q(s) || r.isBoolean() || n.isString() && !q(a) || n.isBoolean())
      return g.create(m.DIV_BY_ZERO);
    const u = (s - a) ** 2;
    return R.create(u);
  }
}
class Z_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.tan();
  }
}
class z_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    let t = e;
    return t.isString() && (t = t.convertToNumberObjectValue()), t.isError() ? t : t.tanh();
  }
}
class J_ extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(0), n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, r, g.create(m.NA)), c = a.map((l, f, h) => {
      let d = u.get(f, h), _ = l;
      if (_.isString() && (_ = _.convertToNumberObjectValue()), _.isError())
        return _;
      if (d.isString() && (d = d.convertToNumberObjectValue()), d.isError())
        return d;
      const C = +_.getValue(), E = +d.getValue(), b = 10 ** Math.trunc(E), y = Zn(C, b), p = Math.trunc(Dr(C, b) + y) / b;
      return R.create(p);
    });
    return n === 1 && s === 1 ? c.get(0, 0) : c;
  }
}
const eA = [
  [MC, Y.ABS],
  [DC, Y.ACOS],
  [LC, Y.ACOSH],
  [xC, Y.ACOT],
  [PC, Y.ACOTH],
  [jC, Y.ARABIC],
  [UC, Y.ASIN],
  [vC, Y.ASINH],
  [TC, Y.ATAN],
  [BC, Y.ATAN2],
  [IC, Y.ATANH],
  [FC, Y.BASE],
  [kC, Y.CEILING],
  [$C, Y.CEILING_MATH],
  [YC, Y.CEILING_PRECISE],
  [HC, Y.COMBIN],
  [QC, Y.COMBINA],
  [GC, Y.COS],
  [WC, Y.COSH],
  [qC, Y.COT],
  [KC, Y.COTH],
  [XC, Y.CSC],
  [ZC, Y.CSCH],
  [zC, Y.DECIMAL],
  [JC, Y.DEGREES],
  [e_, Y.EVEN],
  [t_, Y.EXP],
  [r_, Y.FACT],
  [n_, Y.FACTDOUBLE],
  [s_, Y.FLOOR],
  [a_, Y.FLOOR_MATH],
  [i_, Y.FLOOR_PRECISE],
  [o_, Y.GCD],
  [u_, Y.INT],
  [c_, Y.LCM],
  [l_, Y.LN],
  [f_, Y.LOG],
  [h_, Y.LOG10],
  [m_, Y.MDETERM],
  [g_, Y.MINVERSE],
  [d_, Y.MMULT],
  [C_, Y.MOD],
  [__, Y.MROUND],
  [A_, Y.MULTINOMIAL],
  [E_, Y.MUNIT],
  [b_, Y.ODD],
  [y_, Y.PI],
  [R_, Y.POWER],
  [p_, Y.PRODUCT],
  [N_, Y.QUOTIENT],
  [V_, Y.RADIANS],
  [O_, Y.RAND],
  [w_, Y.RANDARRAY],
  [S_, Y.RANDBETWEEN],
  [M_, Y.ROMAN],
  [D_, Y.ROUND],
  [L_, Y.ROUNDBANK],
  [x_, Y.ROUNDDOWN],
  [P_, Y.ROUNDUP],
  [j_, Y.SEC],
  [U_, Y.SECH],
  [T_, Y.SERIESSUM],
  [v_, Y.SEQUENCE],
  [B_, Y.SIGN],
  [I_, Y.SIN],
  [F_, Y.SINH],
  [k_, Y.SQRT],
  [$_, Y.SQRTPI],
  [Y_, Y.SUBTOTAL],
  [H_, Y.SUM],
  [Jh, Y.SUMIF],
  [Q_, Y.SUMIFS],
  [G_, Y.SUMPRODUCT],
  [W_, Y.SUMSQ],
  [q_, Y.SUMX2MY2],
  [K_, Y.SUMX2PY2],
  [X_, Y.SUMXMY2],
  [Z_, Y.TAN],
  [z_, Y.TANH],
  [J_, Y.TRUNC]
];
class tA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
    A(this, "_compareType", x.EQUALS);
  }
  setCompareType(e) {
    this._compareType = e;
  }
  calculate(e, t) {
    return e.isError() ? e : t.isError() ? t : e.compare(t, this._compareType);
  }
}
class ri extends Gt {
  constructor(e) {
    super("");
    A(this, "_values", []);
    this._values = e;
  }
  static create(e) {
    return new ri(e);
  }
  isCube() {
    return !0;
  }
  dispose() {
    this._values.forEach((e) => {
      e.dispose();
    }), this._values = [];
  }
  getCubeValues() {
    return this._values;
  }
  getCubeCount() {
    return this._values.length;
  }
  sum() {
    const e = R.create(0);
    return this._values.forEach((t) => {
      e.plus(t.sum());
    }), e;
  }
  max() {
    let e = R.create(Number.NEGATIVE_INFINITY);
    return this._values.forEach((t) => {
      const r = t.max();
      e.isLessThan(r) && (e = r);
    }), e;
  }
  min() {
    let e = R.create(Number.POSITIVE_INFINITY);
    return this._values.forEach((t) => {
      const r = t.max();
      e.isGreaterThan(r) && (e = r);
    }), e;
  }
  count() {
    const e = R.create(0);
    return this._values.forEach((t) => {
      e.plus(t.count());
    }), e;
  }
  countA() {
    const e = R.create(0);
    return this._values.forEach((t) => {
      e.plus(t.countA());
    }), e;
  }
  countBlank() {
    const e = R.create(0);
    return this._values.forEach((t) => {
      e.plus(t.countBlank());
    }), e;
  }
}
class rA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = [];
    for (let r = 0; r < e.length; r++) {
      const n = e[r];
      if (n.isError())
        return n;
      if (!n.isArray())
        return g.create(m.VALUE);
      t.push(n);
    }
    return ri.create(t);
  }
}
class nA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    return e.isError() ? e : t.isError() ? t : !t.isArray() && t.getValue() === 0 ? g.create(m.DIV_BY_ZERO) : e.divided(t);
  }
}
class sA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    return e.isError() ? e : t.isError() ? t : e.minus(t);
  }
}
class aA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    return e.isError() ? e : t.isError() ? t : e.multiply(t);
  }
}
class iA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    return e.isError() ? e : t.isError() ? t : e.plus(t);
  }
}
const oA = [
  [tA, Be.COMPARE],
  [nA, Be.DIVIDED],
  [sA, Be.MINUS],
  [aA, Be.MULTIPLY],
  [iA, Be.PLUS],
  [rA, Be.CUBE]
];
class uA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = R.create(0), r = R.create(0);
    for (let a = 0; a < e.length; a++) {
      let u = e[a];
      if (u.isString() && (u = u.convertToNumberObjectValue()), u.isError())
        return u;
      if (u.isArray()) {
        if (u = cA(u), u.isError())
          return u;
        if (e[a] = u, t = t.plus(u.sum()), t.isError())
          return t;
        r = r.plus(u.count());
      } else u.isNull() || (t = t.plus(u), r = r.plus(R.create(1)));
    }
    if (r.getValue() === 0)
      return g.create(m.NUM);
    const n = t.divided(r);
    if (n.isError())
      return n;
    let s = R.create(0);
    for (let a = 0; a < e.length; a++) {
      let u = e[a];
      if (u.isString() && (u = u.convertToNumberObjectValue()), u.isError())
        return u;
      if (u.isArray()) {
        if (s = s.plus(u.minus(n).abs().sum()), s.isError())
          return s;
      } else u.isNull() || (s = s.plus(u.minus(n).abs()));
    }
    return s.divided(r);
  }
}
function cA(o) {
  const i = [];
  i[0] = [];
  let e = null;
  return o.iterator((t, r, n) => {
    if (t != null && t.isError())
      return e = t, !1;
    t != null && t.isNumber() && i[0].push(t);
  }), e || Qr(i, 1, i[0].length);
}
class lA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = R.create(0), r = R.create(0);
    for (let n = 0; n < e.length; n++) {
      let s = e[n];
      if ((s.isString() || s.isBoolean()) && (s = s.convertToNumberObjectValue()), s.isError())
        return s;
      if (s.isArray()) {
        if (t = t.plus(s.sum()), t.isError())
          return t;
        r = r.plus(s.count());
      } else s.isNull() || (t = t.plus(s), r = r.plus(R.create(1)));
    }
    return t.divided(r);
  }
}
class fA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 254);
  }
  // eslint-disable-next-line
  calculate(...e) {
    let t = !1, r;
    e.length % 2 !== 0 && (t = !0, r = g.create(m.NA));
    const n = [], s = [];
    for (let a = 0; a < e.length; a += 2) {
      const u = e[a], c = u.isArray() ? u.getRowCount() : 1, l = u.isArray() ? u.getColumnCount() : 1;
      for (let _ = 0; _ < c; _++)
        for (let C = 0; C < l; C++) {
          const E = u.isArray() ? u.get(_, C) : u;
          if (E.isError())
            return E;
          if (t)
            continue;
          const b = E.isNull() ? "" : E.getValue();
          n.push(b);
        }
      if (a + 1 >= e.length)
        continue;
      const f = e[a + 1], h = f.isArray() ? f.getRowCount() : 1, d = f.isArray() ? f.getColumnCount() : 1;
      (h !== c || d !== l) && (t = !0, r = g.create(m.VALUE));
      for (let _ = 0; _ < h; _++)
        for (let C = 0; C < d; C++) {
          const E = f.isArray() ? f.get(_, C) : f;
          if (E.isError())
            return E;
          if (t)
            continue;
          const b = E.isNull() ? "" : E.getValue();
          s.push(b);
        }
    }
    return t ? r : this._getResult(n, s);
  }
  _getResult(e, t) {
    const r = e.length;
    let n = 0, s = 0;
    for (let u = 0; u < r; u++) {
      const c = e[u], l = t[u];
      if (!(typeof c != "number" && typeof l != "number")) {
        if (typeof c != "number" || typeof l != "number" || l < 0)
          return g.create(m.VALUE);
        n += c * l, s += l;
      }
    }
    if (s === 0)
      return g.create(m.DIV_BY_ZERO);
    const a = n / s;
    return R.create(a);
  }
}
class hA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = R.create(0), r = R.create(0);
    for (let n = 0; n < e.length; n++) {
      let s = e[n];
      if ((s.isString() || s.isBoolean()) && (s = s.convertToNumberObjectValue()), s.isError())
        return s;
      if (s.isArray()) {
        if (s.iterator((a) => {
          if (a == null || a.isNull())
            return !0;
          let u = a;
          if (u.isString() && (u = u.convertToNumberObjectValue(), u.isError() && (u = R.create(0))), u.isBoolean() && (u = u.convertToNumberObjectValue()), u.isError())
            return t = u, !1;
          t = t.plus(u), r = r.plus(R.create(1));
        }), t.isError())
          return t;
      } else s.isNull() || (t = t.plus(s), r = r.plus(R.create(1)));
    }
    return t.divided(r);
  }
}
class mA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, t, r) {
    if (!e.isReferenceObject() || r && !r.isReferenceObject())
      return g.create(m.VALUE);
    let n = t;
    if (t.isReferenceObject() && (n = t.toArrayValueObject()), n.isArray()) {
      const s = n.mapValue((a) => this._handleSingleObject(e, a, r));
      return s.getRowCount() === 1 && s.getColumnCount() === 1 ? s.get(0, 0) : s;
    }
    return this._handleSingleObject(e, n, r);
  }
  _handleSingleObject(e, t, r) {
    const n = e.toArrayValueObject();
    let s = es(n, t);
    s = ts(s, n, t);
    const a = n.getRowCount(), u = n.getColumnCount();
    let c = n;
    if (r) {
      c = r.toArrayValueObject();
      const d = c.getRowCount(), _ = c.getColumnCount();
      if (a !== d || u !== _) {
        const C = r.getRangeData();
        C.endRow = C.startRow + a - 1, C.endColumn = C.startColumn + u - 1, r.setRangeData(C), c = r.toArrayValueObject();
      }
    }
    const l = c.pick(s), f = l.sum(), h = l.count();
    return f.divided(h);
  }
}
class gA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 255);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, ...t) {
    const {
      isError: r,
      errorObject: n,
      rangeIsDifferentSize: s,
      criteriaMaxRowLength: a,
      criteriaMaxColumnLength: u,
      targetRange: c,
      variants: l
    } = Gr(t, e);
    if (r)
      return n;
    if (s)
      return a === 1 && u === 1 ? g.create(m.VALUE) : O(a, u, g.create(m.VALUE));
    if (a === 1 && u === 1)
      return this._scalarAverageifs(c, l);
    const f = Rt(l, a, u, !0);
    return this._aggregateResults(c, f);
  }
  _scalarAverageifs(e, t) {
    if (!e.isArray())
      return this._fallbackAverageifs(e, t);
    const r = e, n = t.length / 2, s = new Array(n), a = new Array(n), u = new Array(n);
    for (let C = 0; C < n; C++) {
      const E = t[C * 2], b = t[C * 2 + 1];
      if (!E.isArray())
        return this._fallbackAverageifs(e, t);
      s[C] = E;
      const y = b.isArray() && b.get(0, 0) || b;
      if (y.isError())
        return y;
      if (y.isString()) {
        const [p, N] = pt(`${y.getValue()}`);
        a[C] = p, u[C] = N;
      } else
        a[C] = x.EQUALS, u[C] = y;
    }
    const c = s[0].getRowCount(), l = s[0].getColumnCount();
    let f = 0, h = 0;
    const d = new Array(n);
    let _ = !0;
    for (let C = 0; C < n; C++) {
      const E = u[C].getValue();
      d[C] = typeof E == "string" ? E.toLocaleLowerCase() : E, typeof E == "string" && Mr(E) && (_ = !1);
    }
    if (_)
      for (let C = 0; C < c; C++)
        for (let E = 0; E < l; E++) {
          let b = !0;
          for (let y = 0; y < n; y++)
            if (!s[y].rawCompare(C, E, d[y], a[y])) {
              b = !1;
              break;
            }
          b && (f += r.getNumberDirect(C, E), h++);
        }
    else
      for (let C = 0; C < c; C++)
        for (let E = 0; E < l; E++) {
          let b = !0;
          for (let y = 0; y < n; y++) {
            const p = s[y].get(C, E);
            if (!p || p.isError()) {
              b = !1;
              break;
            }
            if (!Lr(p, u[y])) {
              const w = a[y];
              if (w === x.EQUALS || w === x.NOT_EQUAL) {
                if (p.isNumber() && u[y].isString()) {
                  const S = u[y].convertToNumberObjectValue();
                  if (S.isNumber()) {
                    const M = p.compare(S, w);
                    (M.isError() || M.getValue() !== !0) && (b = !1);
                    continue;
                  }
                }
                if (u[y].isNumber() && p.isString()) {
                  const S = p.convertToNumberObjectValue();
                  if (S.isNumber()) {
                    const M = S.compare(u[y], w);
                    (M.isError() || M.getValue() !== !0) && (b = !1);
                    continue;
                  }
                }
                b = w === x.NOT_EQUAL;
              } else
                b = !1;
              if (!b) break;
              continue;
            }
            const N = p.compare(u[y], a[y]);
            if (N.isError() || N.getValue() !== !0) {
              b = !1;
              break;
            }
          }
          if (b) {
            const y = r.get(C, E);
            y && y.isNumber() && (f += y.getValue(), h++);
          }
        }
    return h === 0 ? g.create(m.DIV_BY_ZERO) : R.create(f / h);
  }
  _fallbackAverageifs(e, t) {
    const r = Rt(t, 1, 1, !0);
    return this._aggregateResults(e, r);
  }
  _aggregateResults(e, t) {
    const r = t.map((n) => n.map((s) => {
      const a = e.pick(s), u = a.sum(), c = a.count();
      return u.divided(c);
    }));
    return r.length === 1 && r[0].length === 1 ? r[0][0] : W.create({
      calculateValueList: r,
      rowCount: r.length,
      columnCount: r[0].length,
      unitId: this.unitId || "",
      sheetId: this.subUnitId || "",
      row: this.row,
      column: this.column
    });
  }
}
class dA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 6);
  }
  calculate(e, t, r, n, s, a) {
    let u = s != null ? s : R.create(0), c = a != null ? a : R.create(1);
    u.isNull() && (u = R.create(0)), c.isNull() && (c = R.create(1));
    const l = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1,
      u.isArray() ? u.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1
    ), f = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1,
      u.isArray() ? u.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1
    ), h = O(l, f, e, g.create(m.NA)), d = O(l, f, t, g.create(m.NA)), _ = O(l, f, r, g.create(m.NA)), C = O(l, f, n, g.create(m.NA)), E = O(l, f, u, g.create(m.NA)), b = O(l, f, c, g.create(m.NA)), y = h.mapValue((p, N, w) => {
      const S = d.get(N, w), M = _.get(N, w), D = C.get(N, w), L = E.get(N, w), v = b.get(N, w);
      return this._handleSingleObject(p, S, M, D, L, v);
    });
    return l === 1 && f === 1 ? y.get(0, 0) : y;
  }
  _handleSingleObject(e, t, r, n, s, a) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (r.isError())
      return r;
    if (n.isError())
      return n;
    if (s.isError())
      return s;
    if (a.isError())
      return a;
    const { isError: u, errorObject: c, variants: l } = I(e, t, r, n, s, a);
    if (u)
      return c;
    const [f, h, d, _, C, E] = l, b = +f.getValue(), y = +h.getValue(), p = +d.getValue(), N = +_.getValue(), w = +C.getValue(), S = +E.getValue();
    if (y <= 0 || p <= 0 || b < w || b > S || w === S)
      return g.create(m.NUM);
    let M;
    return N ? M = eu((b - w) / (S - w), y, p) : M = Nm((b - w) / (S - w), y, p) / (S - w), R.create(M);
  }
}
class CA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    let s = n != null ? n : r;
    s.isNull() && (s = r);
    const a = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1
    ), u = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1
    ), c = O(a, u, e, g.create(m.NA)), l = O(a, u, t, g.create(m.NA)), f = O(a, u, r, g.create(m.NA)), h = O(a, u, s, g.create(m.NA)), d = c.mapValue((_, C, E) => {
      const b = l.get(C, E), y = f.get(C, E), p = h.get(C, E);
      return _.isError() ? _ : b.isError() ? b : y.isError() ? y : p.isError() ? p : this._handleSingleObject(_, b, y, p);
    });
    return a === 1 && u === 1 ? d.get(0, 0) : d;
  }
  _handleSingleObject(e, t, r, n) {
    const { isError: s, errorObject: a, variants: u } = I(e, t, r, n);
    if (s)
      return a;
    const [c, l, f, h] = u, d = Math.floor(+c.getValue()), _ = +l.getValue(), C = Math.floor(+f.getValue()), E = Math.floor(+h.getValue());
    if (d < 0 || _ < 0 || _ > 1 || C < 0 || C > d || E < 0 || E < C || E > d)
      return g.create(m.NUM);
    let b = 0;
    for (let y = C; y <= E; y++)
      b += Qa(y, d, _);
    return R.create(b);
  }
}
class _A extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = Math.floor(+c.getValue()), d = +l.getValue();
    if (f < 0 || h < 1 || h > 10 ** 10)
      return g.create(m.NUM);
    let _;
    return d ? _ = Ga(f, h) : _ = Vm(f, h), R.create(_);
  }
}
class AA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      return c.isError() ? c : h.isError() ? h : this._handleSingleObject(c, h);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
  _handleSingleObject(e, t) {
    const { isError: r, errorObject: n, variants: s } = I(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = Math.floor(+u.getValue());
    if (c < 0 || c > 1 || l < 1 || l > 10 ** 10)
      return g.create(m.NUM);
    const f = ru(c, l);
    return Number.isNaN(f) || !Number.isFinite(f) ? g.create(m.NUM) : R.create(f);
  }
}
class EA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      if (f.isError())
        return f;
      if (_.isError())
        return _;
      if (C.isError())
        return C;
      const { isError: E, errorObject: b, variants: y } = I(f, _, C);
      if (E)
        return b;
      const [p, N, w] = y, S = +p.getValue(), M = +N.getValue(), D = Math.floor(+w.getValue());
      if (S <= 0 || S >= 1 || M <= 0 || D < 1)
        return g.create(m.NUM);
      if (D === 1)
        return g.create(m.DIV_BY_ZERO);
      const L = Math.abs(as(S / 2, D - 1) * M / Math.sqrt(D));
      return R.create(L);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
}
class bA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    let u = e;
    if (e.isArray() && r === 1 && n === 1 && (u = e.get(0, 0)), u.isError())
      return u;
    let c = t;
    if (t.isArray() && s === 1 && a === 1 && (c = t.get(0, 0)), c.isError())
      return c;
    if (r * n === 1 || s * a === 1)
      return u.isNull() || c.isNull() ? g.create(m.VALUE) : g.create(m.DIV_BY_ZERO);
    if (r * n !== s * a)
      return g.create(m.NA);
    const {
      isError: l,
      errorObject: f,
      array1Values: h,
      array2Values: d,
      noCalculate: _
    } = rt(
      e,
      t,
      r * n,
      n,
      a
    );
    return l ? f : _ ? g.create(m.DIV_BY_ZERO) : this._getResult(h, d);
  }
  _getResult(e, t) {
    const r = e.length;
    let n = 0, s = 0;
    for (let d = 0; d < r; d++)
      n += e[d], s += t[d];
    const a = n / r, u = s / r;
    let c = 0, l = 0, f = 0;
    for (let d = 0; d < r; d++) {
      const _ = e[d] - a, C = t[d] - u;
      c += _ * C, l += _ ** 2, f += C ** 2;
    }
    const h = Math.sqrt(l * f);
    return h === 0 ? g.create(m.DIV_BY_ZERO) : R.create(c / h);
  }
}
class yA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = R.create(0);
    for (let r = 0; r < e.length; r++) {
      const n = e[r];
      n.isError() || (n.isArray() ? t = t.plus(n.count()) : n.isString() ? n.convertToNumberObjectValue().isError() || (t = t.plus(R.create(1))) : n.isNull() || (t = t.plus(R.create(1))));
    }
    return t;
  }
}
class RA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = R.create(0);
    for (let r = 0; r < e.length; r++) {
      let n = e[r];
      if (n.isError()) {
        t = t.plus(R.create(1));
        continue;
      }
      n.isArray() ? (n = n.countA(), t = t.plus(n)) : n.isNull() || (t = t.plus(R.create(1)));
    }
    return t;
  }
}
class pA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isError() ? e : e.getValue() === "" || e.isNull() ? R.create(1) : e.isArray() ? e.countBlank() : R.create(0);
  }
}
class NA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    let u = e;
    if (e.isArray() && r === 1 && n === 1 && (u = e.get(0, 0)), u.isError())
      return u;
    let c = t;
    if (t.isArray() && s === 1 && a === 1 && (c = t.get(0, 0)), c.isError())
      return c;
    if ((r * n === 1 || s * a === 1) && (u.isNull() || c.isNull()))
      return g.create(m.VALUE);
    if (r * n !== s * a)
      return g.create(m.NA);
    const {
      isError: l,
      errorObject: f,
      array1Values: h,
      array2Values: d,
      noCalculate: _
    } = rt(
      e,
      t,
      r * n,
      n,
      a
    );
    return l ? f : _ ? g.create(m.DIV_BY_ZERO) : this._getResult(h, d);
  }
  _getResult(e, t) {
    if (e.length <= 1)
      return g.create(m.DIV_BY_ZERO);
    const r = e.length;
    let n = 0, s = 0;
    for (let l = 0; l < r; l++)
      n += e[l], s += t[l];
    const a = n / r, u = s / r;
    let c = 0;
    for (let l = 0; l < r; l++) {
      const f = e[l] - a, h = t[l] - u;
      c += f * h;
    }
    return R.create(c / (r - 1));
  }
}
class VA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = [];
    let r = 0, n = !0;
    for (let u = 0; u < e.length; u++) {
      const c = e[u];
      if (c.isArray()) {
        let l = !1, f = g.create(m.VALUE);
        if (c.iterator((h) => {
          const d = this._handleSingleObject(h);
          if (d.isError())
            return l = !0, f = d, !1;
          if (d.isNull())
            return !0;
          const _ = d.getValue();
          t.push(_), r += _, n = !1;
        }), l)
          return f;
      } else {
        const l = this._handleSingleObject(c);
        if (l.isError())
          return l;
        if (l.isNull())
          continue;
        const f = l.getValue();
        t.push(f), r += f, n = !1;
      }
    }
    if (n)
      return g.create(m.NUM);
    const s = r / t.length;
    let a = 0;
    for (let u = 0; u < t.length; u++)
      a += (t[u] - s) ** 2;
    return R.create(a);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    if (e.isNull() || e.isBoolean())
      return ue.create();
    const t = e.getValue();
    return q(t) ? R.create(+t) : ue.create();
  }
}
class OA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = O(s, a, n, g.create(m.NA)), h = u.mapValue((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C), y = f.get(_, C);
      return d.isError() ? d : E.isError() ? E : b.isError() ? b : y.isError() ? y : this._handleSingleObject(d, E, b, y);
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
  _handleSingleObject(e, t, r, n) {
    const { isError: s, errorObject: a, variants: u } = I(e, t, r, n);
    if (s)
      return a;
    const [c, l, f, h] = u, d = +c.getValue(), _ = Math.floor(+l.getValue()), C = Math.floor(+f.getValue()), E = +h.getValue();
    if (d < 0 || _ < 1 || _ > 10 ** 10 || C < 1 || C > 10 ** 10)
      return g.create(m.NUM);
    let b;
    return E ? b = Wa(d, _, C) : b = Om(d, _, C), Number.isNaN(b) || !Number.isFinite(b) ? g.create(m.NUM) : R.create(b);
  }
}
class wA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = Math.floor(+c.getValue()), d = Math.floor(+l.getValue());
    if (f < 0 || f > 1 || h < 1 || h > 10 ** 10 || d < 1 || d > 10 ** 10)
      return g.create(m.NUM);
    const _ = nu(f, h, d);
    return Number.isNaN(_) || !Number.isFinite(_) ? g.create(m.NUM) : R.create(_);
  }
}
class SA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.mapValue((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const { isError: t, errorObject: r, variants: n } = I(e);
    if (t)
      return r;
    const [s] = n, a = s.getValue();
    if (a <= -1 || a >= 1)
      return g.create(m.NUM);
    const u = Math.log((1 + a) / (1 - a)) / 2;
    return R.create(u);
  }
}
class MA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    return e.isArray() ? e.mapValue((t) => this._handleSingleObject(t)) : this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const { isError: t, errorObject: r, variants: n } = I(e);
    if (t)
      return r;
    const [s] = n, a = +s.getValue(), u = Math.exp(2 * a) - 1, c = Math.exp(2 * a) + 1;
    return !Number.isFinite(u) && u > 0 && !Number.isFinite(c) && c > 0 ? R.create(1) : R.create(u / c);
  }
}
class Ki extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = t.isArray() ? t.getRowCount() : 1, s = t.isArray() ? t.getColumnCount() : 1, a = r.isArray() ? r.getRowCount() : 1, u = r.isArray() ? r.getColumnCount() : 1;
    let c = t;
    t.isArray() && n === 1 && s === 1 && (c = t.get(0, 0));
    let l = r;
    return r.isArray() && a === 1 && u === 1 && (l = r.get(0, 0)), e.isArray() ? e.mapValue(
      (f) => this._handleSingleObject(f, c, l, n, s, a, u)
    ) : this._handleSingleObject(e, c, l, n, s, a, u);
  }
  _handleSingleObject(e, t, r, n, s, a, u) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    if (r.isError())
      return r;
    let c = e;
    if (e.isString() && (c = e.convertToNumberObjectValue()), c.isError())
      return c;
    const l = +c.getValue();
    if ((n * s === 1 || a * u === 1) && (t.isNull() || r.isNull()))
      return g.create(m.VALUE);
    if (n * s !== a * u)
      return g.create(m.NA);
    const {
      isError: f,
      errorObject: h,
      array1Values: d,
      array2Values: _,
      noCalculate: C
    } = rt(
      t,
      r,
      n * s,
      s,
      u
    );
    if (f)
      return h;
    if (C)
      return g.create(m.DIV_BY_ZERO);
    const E = su(l, d, _);
    return Number.isFinite(E) ? R.create(E) : g.create(m.DIV_BY_ZERO);
  }
}
class DA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const { isError: r, errorObject: n, values: s } = this._getValues(e);
    if (r)
      return n;
    if (e.isNull() || t.isNull())
      return g.create(m.VALUE);
    let { values: a } = this._getValues(t, !0);
    a.length === 0 && (a = [0]);
    const u = this._getNewBinsArrayValues(a), c = new Array(u.length).fill(0);
    for (let l = 0; l < s.length; l++) {
      const f = s[l], h = u.findIndex((d) => f > d.start && f <= d.end);
      c[h]++;
    }
    return W.createByArray(c.map((l) => [l]));
  }
  _getValues(e, t = !1) {
    const r = [], n = e.isArray() ? e.getRowCount() : 1, s = e.isArray() ? e.getColumnCount() : 1;
    for (let a = 0; a < n; a++)
      for (let u = 0; u < s; u++) {
        const c = e.isArray() ? e.get(a, u) : e;
        if (c.isError()) {
          if (!t)
            return {
              isError: !0,
              errorObject: c,
              values: r
            };
          continue;
        }
        if (c.isNull() || c.isBoolean())
          continue;
        const l = c.getValue();
        q(l) && r.push(+l);
      }
    return {
      isError: !1,
      errorObject: null,
      values: r
    };
  }
  _getNewBinsArrayValues(e) {
    const t = e.map((n, s) => ({
      value: n,
      index: s
    })).sort((n, s) => n.value - s.value), r = [];
    for (let n = 0; n < t.length; n++) {
      const s = t[n].index;
      if (n === 0) {
        r[s] = {
          start: -1 / 0,
          end: t[n].value
        };
        continue;
      }
      r[s] = {
        start: t[n - 1].value,
        end: t[n].value
      };
    }
    return r.push({
      start: t[t.length - 1].value,
      end: 1 / 0
    }), r;
  }
}
var B = /* @__PURE__ */ ((o) => (o.AVEDEV = "AVEDEV", o.AVERAGE = "AVERAGE", o.AVERAGE_WEIGHTED = "AVERAGE.WEIGHTED", o.AVERAGEA = "AVERAGEA", o.AVERAGEIF = "AVERAGEIF", o.AVERAGEIFS = "AVERAGEIFS", o.BETA_DIST = "BETA.DIST", o.BETA_INV = "BETA.INV", o.BINOM_DIST = "BINOM.DIST", o.BINOM_DIST_RANGE = "BINOM.DIST.RANGE", o.BINOM_INV = "BINOM.INV", o.CHISQ_DIST = "CHISQ.DIST", o.CHISQ_DIST_RT = "CHISQ.DIST.RT", o.CHISQ_INV = "CHISQ.INV", o.CHISQ_INV_RT = "CHISQ.INV.RT", o.CHISQ_TEST = "CHISQ.TEST", o.CONFIDENCE_NORM = "CONFIDENCE.NORM", o.CONFIDENCE_T = "CONFIDENCE.T", o.CORREL = "CORREL", o.COUNT = "COUNT", o.COUNTA = "COUNTA", o.COUNTBLANK = "COUNTBLANK", o.COUNTIF = "COUNTIF", o.COUNTIFS = "COUNTIFS", o.COVARIANCE_P = "COVARIANCE.P", o.COVARIANCE_S = "COVARIANCE.S", o.DEVSQ = "DEVSQ", o.EXPON_DIST = "EXPON.DIST", o.F_DIST = "F.DIST", o.F_DIST_RT = "F.DIST.RT", o.F_INV = "F.INV", o.F_INV_RT = "F.INV.RT", o.F_TEST = "F.TEST", o.FISHER = "FISHER", o.FISHERINV = "FISHERINV", o.FORECAST = "FORECAST", o.FORECAST_ETS = "FORECAST.ETS", o.FORECAST_ETS_CONFINT = "FORECAST.ETS.CONFINT", o.FORECAST_ETS_SEASONALITY = "FORECAST.ETS.SEASONALITY", o.FORECAST_ETS_STAT = "FORECAST.ETS.STAT", o.FORECAST_LINEAR = "FORECAST.LINEAR", o.FREQUENCY = "FREQUENCY", o.GAMMA = "GAMMA", o.GAMMA_DIST = "GAMMA.DIST", o.GAMMA_INV = "GAMMA.INV", o.GAMMALN = "GAMMALN", o.GAMMALN_PRECISE = "GAMMALN.PRECISE", o.GAUSS = "GAUSS", o.GEOMEAN = "GEOMEAN", o.GROWTH = "GROWTH", o.HARMEAN = "HARMEAN", o.HYPGEOM_DIST = "HYPGEOM.DIST", o.INTERCEPT = "INTERCEPT", o.KURT = "KURT", o.LARGE = "LARGE", o.LINEST = "LINEST", o.LOGEST = "LOGEST", o.LOGNORM_DIST = "LOGNORM.DIST", o.LOGNORM_INV = "LOGNORM.INV", o.MARGINOFERROR = "MARGINOFERROR", o.MAX = "MAX", o.MAXA = "MAXA", o.MAXIFS = "MAXIFS", o.MEDIAN = "MEDIAN", o.MIN = "MIN", o.MINA = "MINA", o.MINIFS = "MINIFS", o.MODE_MULT = "MODE.MULT", o.MODE_SNGL = "MODE.SNGL", o.NEGBINOM_DIST = "NEGBINOM.DIST", o.NORM_DIST = "NORM.DIST", o.NORM_INV = "NORM.INV", o.NORM_S_DIST = "NORM.S.DIST", o.NORM_S_INV = "NORM.S.INV", o.PEARSON = "PEARSON", o.PERCENTILE_EXC = "PERCENTILE.EXC", o.PERCENTILE_INC = "PERCENTILE.INC", o.PERCENTRANK_EXC = "PERCENTRANK.EXC", o.PERCENTRANK_INC = "PERCENTRANK.INC", o.PERMUT = "PERMUT", o.PERMUTATIONA = "PERMUTATIONA", o.PHI = "PHI", o.POISSON_DIST = "POISSON.DIST", o.PROB = "PROB", o.QUARTILE_EXC = "QUARTILE.EXC", o.QUARTILE_INC = "QUARTILE.INC", o.RANK_AVG = "RANK.AVG", o.RANK_EQ = "RANK.EQ", o.RSQ = "RSQ", o.SKEW = "SKEW", o.SKEW_P = "SKEW.P", o.SLOPE = "SLOPE", o.SMALL = "SMALL", o.STANDARDIZE = "STANDARDIZE", o.STDEV_P = "STDEV.P", o.STDEV_S = "STDEV.S", o.STDEVA = "STDEVA", o.STDEVPA = "STDEVPA", o.STEYX = "STEYX", o.T_DIST = "T.DIST", o.T_DIST_2T = "T.DIST.2T", o.T_DIST_RT = "T.DIST.RT", o.T_INV = "T.INV", o.T_INV_2T = "T.INV.2T", o.T_TEST = "T.TEST", o.TREND = "TREND", o.TRIMMEAN = "TRIMMEAN", o.VAR_P = "VAR.P", o.VAR_S = "VAR.S", o.VARA = "VARA", o.VARPA = "VARPA", o.WEIBULL_DIST = "WEIBULL.DIST", o.Z_TEST = "Z.TEST", o))(B || {});
class LA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const { isError: t, errorObject: r, variants: n } = I(e);
    if (t)
      return r;
    const [s] = n, a = +s.getValue();
    if (a === 0 || a < 0 && a % 1 === 0)
      return g.create(m.NUM);
    const u = _n(a);
    return Number.isNaN(u) || !Number.isFinite(u) ? g.create(m.NUM) : R.create(u);
  }
}
class Xi extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const { isError: t, errorObject: r, variants: n } = I(e);
    if (t)
      return r;
    const [s] = n, a = +s.getValue();
    if (a <= 0)
      return g.create(m.NUM);
    const u = ht(a);
    return Math.abs(u) < 1e-15 ? R.create(0) : R.create(u);
  }
}
class xA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const { isError: t, errorObject: r, variants: n } = I(e);
    if (t)
      return r;
    const [s] = n, a = +s.getValue(), u = qr(a, 0, 1) - 0.5;
    return R.create(u);
  }
}
class PA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = 1, r = 0, n = !1;
    for (let a = 0; a < e.length; a++) {
      const u = e[a];
      if (u.isArray()) {
        let c = !1, l = g.create(m.VALUE);
        if (u.iterator((f) => {
          if (f != null && f.isError())
            return c = !0, l = f, !1;
          if (f != null && f.isNull() || f != null && f.isBoolean())
            return !0;
          const h = f.getValue();
          if (!q(h))
            return !0;
          +h <= 0 && (n = !0), t *= +h, r++;
        }), c)
          return l;
      } else {
        if (u.isError())
          return u;
        if (u.isString()) {
          const l = u.convertToNumberObjectValue();
          if (l.isError())
            return l;
        }
        if (u.isNull() || u.isBoolean())
          continue;
        const c = u.getValue();
        if (!q(c))
          continue;
        +c <= 0 && (n = !0), t *= +c, r++;
      }
    }
    if (r === 0 || n)
      return g.create(m.NUM);
    const s = t ** (1 / r);
    return R.create(s);
  }
}
class jA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const { isError: s, errorObject: a } = is(e, t, r);
    if (s)
      return a;
    const u = bt(e);
    if (u instanceof g)
      return u;
    const c = this._getKnownXsValues(u, t);
    if (c instanceof g)
      return c;
    const l = this._getNewXsValues(c, r);
    if (l instanceof g)
      return l;
    let f = n != null ? n : T.create(!0);
    if (f.isArray() && (f = f.get(0, 0)), f.isString() && (f = f.convertToNumberObjectValue()), f.isError())
      return f;
    const h = +f.getValue();
    return this._getResult(u, c, l, h);
  }
  _getResult(e, t, r, n) {
    return e.length === 1 && t.length > 1 || e[0].length === 1 && t[0].length > 1 ? e.length === 1 && t.length > 1 && (n ? t.length + 1 : t.length) > e[0].length || e[0].length === 1 && t[0].length > 1 && (n ? t[0].length + 1 : t[0].length) > e.length ? g.create(m.NA) : this._getResultByMultipleVariables(e, t, r, n) : this._getResultBySimpleVariables(e, t, r, n);
  }
  _getResultByMultipleVariables(e, t, r, n) {
    const s = e.length === 1 && e[0].length > 1, a = cs(e, t, r, n, !0);
    if (a instanceof g)
      return a;
    const { coefficients: u, newX: c } = a, l = u[0].length, f = u[0][l - 1];
    let h = [];
    for (let d = 0; d < c.length; d++) {
      h[d] = [];
      let _ = f;
      for (let C = l - 2; C >= 0; C--)
        _ *= u[0][l - 2 - C] ** c[d][C];
      h[d].push(_);
    }
    return s && (h = _t(h)), W.createByArray(h);
  }
  _getResultBySimpleVariables(e, t, r, n) {
    const s = e.flat(), a = t.flat(), { slope: u, intercept: c } = us(a, s, n, !0), l = r.map((f) => f.map((h) => c * u ** h));
    return W.createByArray(l);
  }
  _getKnownXsValues(e, t) {
    return !t || t.isNull() ? os(e.length, e[0].length) : bt(t);
  }
  _getNewXsValues(e, t) {
    return !t || t.isNull() ? e : bt(t);
  }
}
class UA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = 0, r = 0, n = !1;
    for (let a = 0; a < e.length; a++) {
      const u = e[a];
      if (u.isArray()) {
        let c = !1, l = g.create(m.VALUE);
        if (u.iterator((f) => {
          if (f != null && f.isError())
            return c = !0, l = f, !1;
          if (f != null && f.isNull() || f != null && f.isBoolean())
            return !0;
          const h = f.getValue();
          if (!q(h))
            return !0;
          +h <= 0 && (n = !0), t += 1 / +h, r++;
        }), c)
          return l;
      } else {
        if (u.isError())
          return u;
        if (u.isString()) {
          const l = u.convertToNumberObjectValue();
          if (l.isError())
            return l;
        }
        if (u.isNull() || u.isBoolean())
          continue;
        const c = u.getValue();
        if (!q(c))
          continue;
        +c <= 0 && (n = !0), t += 1 / +c, r++;
      }
    }
    if (r === 0)
      return g.create(m.NA);
    if (n)
      return g.create(m.NUM);
    const s = r / t;
    return R.create(s);
  }
}
class vA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 5);
    A(this, "maxParams", 5);
  }
  calculate(e, t, r, n, s) {
    const a = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1
    ), u = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1
    ), c = O(a, u, e, g.create(m.NA)), l = O(a, u, t, g.create(m.NA)), f = O(a, u, r, g.create(m.NA)), h = O(a, u, n, g.create(m.NA)), d = O(a, u, s, g.create(m.NA)), _ = c.mapValue((C, E, b) => {
      const y = l.get(E, b), p = f.get(E, b), N = h.get(E, b), w = d.get(E, b);
      return C.isError() ? C : y.isError() ? y : p.isError() ? p : N.isError() ? N : w.isError() ? w : this._handleSingleObject(C, y, p, N, w);
    });
    return a === 1 && u === 1 ? _.get(0, 0) : _;
  }
  _handleSingleObject(e, t, r, n, s) {
    const { isError: a, errorObject: u, variants: c } = I(e, t, r, n, s);
    if (a)
      return u;
    const [l, f, h, d, _] = c, C = Math.floor(+l.getValue()), E = Math.floor(+f.getValue()), b = Math.floor(+h.getValue()), y = Math.floor(+d.getValue()), p = +_.getValue();
    if (C < 0 || C > E || C > b || C < E - y + b || E <= 0 || E > y || b <= 0 || b > y || y <= 0)
      return g.create(m.NUM);
    let N;
    return p ? N = xm(C, E, b, y) : N = Ka(C, E, b, y), Number.isNaN(N) && (N = 0), R.create(N);
  }
}
class TA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    let u = e;
    if (e.isArray() && r === 1 && n === 1 && (u = e.get(0, 0)), u.isError())
      return u;
    let c = t;
    if (t.isArray() && s === 1 && a === 1 && (c = t.get(0, 0)), c.isError())
      return c;
    if (r * n === 1 || s * a === 1)
      return u.isNull() || c.isNull() ? g.create(m.VALUE) : g.create(m.DIV_BY_ZERO);
    if (r * n !== s * a)
      return g.create(m.NA);
    const {
      isError: l,
      errorObject: f,
      array1Values: h,
      array2Values: d,
      noCalculate: _
    } = rt(
      e,
      t,
      r * n,
      n,
      a
    );
    return l ? f : _ ? g.create(m.DIV_BY_ZERO) : this._getResult(h, d);
  }
  _getResult(e, t) {
    const r = su(0, e, t);
    return Number.isFinite(r) ? R.create(r) : g.create(m.DIV_BY_ZERO);
  }
}
class BA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = [];
    let r = 0;
    for (let n = 0; n < e.length; n++) {
      const s = e[n];
      if (s.isError())
        return s;
      if (s.isString()) {
        const c = s.convertToNumberObjectValue();
        if (c.isError())
          return c;
      }
      const a = s.isArray() ? s.getRowCount() : 1, u = s.isArray() ? s.getColumnCount() : 1;
      for (let c = 0; c < a; c++)
        for (let l = 0; l < u; l++) {
          const f = s.isArray() ? s.get(c, l) : s;
          if (f.isError())
            return f;
          if (f.isNull() || f.isBoolean())
            continue;
          const h = f.getValue();
          q(h) && (t.push(+h), r += +h);
        }
    }
    return t.length <= 3 ? g.create(m.DIV_BY_ZERO) : this._getResult(t, r);
  }
  _getResult(e, t) {
    const r = e.length, n = t / r;
    let s = 0;
    for (let l = 0; l < r; l++)
      s += (e[l] - n) ** 2;
    const a = Math.sqrt(s / (r - 1));
    if (a === 0)
      return g.create(m.DIV_BY_ZERO);
    let u = 0;
    for (let l = 0; l < r; l++)
      u += ((e[l] - n) / a) ** 4;
    const c = r * (r + 1) / ((r - 1) * (r - 2) * (r - 3)) * u - 3 * (r - 1) ** 2 / ((r - 2) * (r - 3));
    return R.create(c);
  }
}
class IA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = this._getValues(e);
    if (t.isArray()) {
      const n = t.mapValue((s) => this._handleSingleObject(r, s));
      return n.getRowCount() === 1 && n.getColumnCount() === 1 ? n.get(0, 0) : n;
    }
    return this._handleSingleObject(r, t);
  }
  _handleSingleObject(e, t) {
    if (e instanceof g)
      return e;
    const { isError: r, errorObject: n, variants: s } = I(t);
    if (r)
      return n;
    const [a] = s;
    let u = +a.getValue();
    return u < 1 || u > e.length ? g.create(m.NUM) : (u = Math.ceil(u), R.create(e[u - 1]));
  }
  _getValues(e) {
    if (e.isArray()) {
      const s = e.getSortedNumericValues(!0);
      if (s) return s;
    }
    const t = e.isArray() ? e.getRowCount() : 1, r = e.isArray() ? e.getColumnCount() : 1, n = [];
    for (let s = 0; s < t; s++)
      for (let a = 0; a < r; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        if (u.isNull() || u.isBoolean() || u.isString())
          continue;
        const c = u.getValue();
        q(c) && n.push(+c);
      }
    return n.length === 0 ? g.create(m.NUM) : n.sort((s, a) => a - s);
  }
}
class FA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const { isError: s, errorObject: a } = is(e, t);
    if (s)
      return a;
    const u = bt(e);
    if (u instanceof g)
      return u;
    const c = this._getKnownXsValues(u, t);
    if (c instanceof g)
      return c;
    let l = r != null ? r : T.create(!0);
    l.isArray() && (l = l.get(0, 0));
    let f = n != null ? n : T.create(!1);
    f.isArray() && (f = f.get(0, 0));
    const { isError: h, errorObject: d, variants: _ } = I(l, f);
    if (h)
      return d;
    const [C, E] = _;
    return this._getResult(u, c, +C.getValue(), +E.getValue());
  }
  _getResult(e, t, r, n) {
    return e.length === 1 && t.length > 1 || e[0].length === 1 && t[0].length > 1 ? e.length === 1 && t.length > 1 && (r ? t.length + 1 : t.length) > e[0].length || e[0].length === 1 && t[0].length > 1 && (r ? t[0].length + 1 : t[0].length) > e.length ? g.create(m.NA) : this._getResultByMultipleVariables(e, t, r, n) : this._getResultBySimpleVariables(e, t, r, n);
  }
  _getResultByMultipleVariables(e, t, r, n) {
    const s = cs(e, t, t, r, !1);
    if (s instanceof g)
      return s;
    const { coefficients: a, X: u, XTXInverse: c } = s;
    let l = [];
    if (n) {
      const f = e.flat(), h = f.length, d = r ? f.reduce((j, k) => j + k, 0) / h : 0, _ = c.length, C = h - _, E = a[0].length, b = new Array(E - 2).fill(m.NA), y = a[0][E - 1], p = [];
      for (let j = 0; j < u.length; j++) {
        let k = y;
        for (let G = E - 2; G >= 0; G--)
          k += a[0][E - 2 - G] * u[j][G];
        p.push(k);
      }
      let N = 0, w = 0;
      for (let j = 0; j < h; j++)
        N += (f[j] - d) ** 2, w += (f[j] - p[j]) ** 2;
      const S = N - w, M = N === 0 ? 0 : S / N, D = [];
      for (let j = _ - 1; j >= 0; j--) {
        const k = C > 0 ? Math.sqrt(w / C * c[j][j]) : 0;
        D.push(k);
      }
      if (r) {
        const j = D.shift();
        D.push(j);
      } else
        D.push(m.NA);
      const L = C > 0 ? Math.sqrt(w / C) : 0, v = C > 0 ? S / (E - 1) / (w / C) : m.NUM;
      l = [
        a[0],
        // [mn, mn-1, ..., m1, b]
        [...D],
        // [sen, sen-1, ..., se1, seb]
        [M, L, ...b],
        // [r2, sey]
        [v, C, ...b],
        // [F, df]
        [S, w, ...b]
        // [ssreg, ssresid]
      ];
    } else
      l = [
        a[0]
        // [mn, mn-1, ..., m1, b]
      ];
    return W.createByArray(l);
  }
  _getResultBySimpleVariables(e, t, r, n) {
    const s = e.flat(), a = t.flat(), { slope: u, intercept: c } = us(a, s, r, !1);
    if (Number.isNaN(u))
      return g.create(m.NA);
    let l = [];
    if (n) {
      const f = s.length;
      let h = 0, d = 0, _ = f - 1;
      if (r) {
        let D = 0, L = 0;
        for (let v = 0; v < f; v++)
          D += s[v], L += a[v];
        h = D / f, d = L / f, _ = f - 2;
      }
      let C = 0, E = 0, b = 0;
      for (let D = 0; D < f; D++)
        C += (s[D] - h) ** 2, E += (s[D] - (u * a[D] + c)) ** 2, b += (a[D] - d) ** 2;
      const y = C - E, p = y === C ? 1 : y / C;
      let N = 0, w = 0, S = 0;
      _ > 0 && (b > 0 && (N = Math.sqrt(E / _ / b), w = Math.sqrt(E / _ * (1 / f + d ** 2 / b))), S = Math.sqrt(E / _));
      const M = _ > 0 ? y / 1 / (E / _) : m.NUM;
      r || (w = m.NA), l = [
        [u, c],
        // [m, b]
        [N, w],
        // [se, seb]
        [p, S],
        // [r2, sey]
        [M, _],
        // [F, df]
        [y, E]
        // [ssreg, ssresid]
      ];
    } else
      l = [
        [u, c]
        // [m, b]
      ];
    return W.createByArray(l);
  }
  _getKnownXsValues(e, t) {
    return !t || t.isNull() ? os(e.length, e[0].length) : bt(t);
  }
}
class kA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const { isError: s, errorObject: a } = is(e, t);
    if (s)
      return a;
    const u = bt(e);
    if (u instanceof g)
      return u;
    const c = this._getKnownXsValues(u, t);
    if (c instanceof g)
      return c;
    let l = r != null ? r : T.create(!0);
    l.isArray() && (l = l.get(0, 0));
    let f = n != null ? n : T.create(!1);
    f.isArray() && (f = f.get(0, 0));
    const { isError: h, errorObject: d, variants: _ } = I(l, f);
    if (h)
      return d;
    const [C, E] = _;
    return this._getResult(u, c, +C.getValue(), +E.getValue());
  }
  _getResult(e, t, r, n) {
    return e.length === 1 && t.length > 1 || e[0].length === 1 && t[0].length > 1 ? e.length === 1 && t.length > 1 && (r ? t.length + 1 : t.length) > e[0].length || e[0].length === 1 && t[0].length > 1 && (r ? t[0].length + 1 : t[0].length) > e.length ? g.create(m.NA) : this._getResultByMultipleVariables(e, t, r, n) : this._getResultBySimpleVariables(e, t, r, n);
  }
  // eslint-disable-next-line max-lines-per-function
  _getResultByMultipleVariables(e, t, r, n) {
    const s = cs(e, t, t, r, !0);
    if (s instanceof g)
      return s;
    const { coefficients: a, Y: u, X: c, XTXInverse: l } = s;
    let f = [];
    if (n) {
      const h = u.flat(), d = h.length, _ = r ? h.reduce((k, G) => k + G, 0) / d : 0, C = l.length, E = d - C, b = a[0].length, y = new Array(b - 2).fill(m.NA), p = a[0][b - 1], N = [];
      for (let k = 0; k < c.length; k++) {
        let G = p;
        for (let ne = b - 2; ne >= 0; ne--)
          G *= a[0][b - 2 - ne] ** c[k][ne];
        N.push(Math.log(G));
      }
      let w = 0, S = 0;
      for (let k = 0; k < d; k++)
        w += (h[k] - _) ** 2, !(!r && !Number.isFinite(N[k])) && (S += (h[k] - N[k]) ** 2);
      Number.isFinite(S) || (S = 0);
      const M = w - S, D = w === 0 ? 0 : M / w, L = [];
      for (let k = C - 1; k >= 0; k--) {
        const G = E > 0 ? Math.sqrt(S / E * l[k][k]) : 0;
        L.push(G);
      }
      if (r) {
        const k = L.shift();
        L.push(k);
      } else
        L.push(m.NA);
      const v = E > 0 ? Math.sqrt(S / E) : 0, j = E > 0 ? M / (b - 1) / (S / E) : m.NUM;
      f = [
        a[0],
        // [mn, mn-1, ..., m1, b]
        [...L],
        // [sen, sen-1, ..., se1, seb]
        [D, v, ...y],
        // [r2, sey]
        [j, E, ...y],
        // [F, df]
        [M, S, ...y]
        // [ssreg, ssresid]
      ];
    } else
      f = [
        a[0]
        // [mn, mn-1, ..., m1, b]
      ];
    return W.createByArray(f);
  }
  _getResultBySimpleVariables(e, t, r, n) {
    const s = e.flat(), a = t.flat(), { slope: u, intercept: c, Y: l } = us(a, s, r, !0);
    if (Number.isNaN(u))
      return g.create(m.NA);
    let f = [];
    if (n) {
      const h = l.length;
      let d = 0, _ = 0, C = h - 1;
      if (r) {
        let L = 0, v = 0;
        for (let j = 0; j < h; j++)
          L += l[j], v += a[j];
        d = L / h, _ = v / h, C = h - 2;
      }
      let E = 0, b = 0, y = 0;
      for (let L = 0; L < h; L++)
        E += (l[L] - d) ** 2, b += (l[L] - Math.log(c * u ** a[L])) ** 2, y += (a[L] - _) ** 2;
      const p = E - b, N = E === 0 ? 0 : p / E;
      let w = 0, S = 0, M = 0, D = 0;
      C > 0 && (y > 0 && (w = Math.sqrt(b / C / y), S = Math.sqrt(b / C * (1 / h + _ ** 2 / y))), M = Math.sqrt(b / C), D = p / 1 / (b / C)), r || (S = m.NA), f = [
        [u, c],
        // [m, b]
        [w, S],
        // [se, seb]
        [N, M],
        // [r2, sey]
        [D, C],
        // [F, df]
        [p, b]
        // [ssreg, ssresid]
      ];
    } else
      f = [
        [u, c]
        // [m, b]
      ];
    return W.createByArray(f);
  }
  _getKnownXsValues(e, t) {
    return !t || t.isNull() ? os(e.length, e[0].length) : bt(t);
  }
}
class $A extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = O(s, a, n, g.create(m.NA)), h = u.mapValue((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C), y = f.get(_, C);
      return d.isError() ? d : E.isError() ? E : b.isError() ? b : y.isError() ? y : this._handleSingleObject(d, E, b, y);
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
  _handleSingleObject(e, t, r, n) {
    const { isError: s, errorObject: a, variants: u } = I(e, t, r, n);
    if (s)
      return a;
    const [c, l, f, h] = u, d = +c.getValue(), _ = +l.getValue(), C = +f.getValue(), E = +h.getValue();
    if (d <= 0 || C <= 0)
      return g.create(m.NUM);
    let b;
    return E ? b = iu(d, _, C) : b = Pm(d, _, C), R.create(b);
  }
}
class YA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = this._getRangeValues(e);
    if (r instanceof g)
      return r;
    const n = tr(t);
    if (n.isError())
      return n;
    const { isError: s, errorObject: a, variants: u } = I(n);
    if (s)
      return a;
    const [c] = u, l = +c.getValue();
    if (l <= 0 || l >= 1)
      return g.create(m.NUM);
    if (r.length < 2)
      return g.create(m.DIV_BY_ZERO);
    const f = 1 - l, h = r.length, d = r.reduce((b, y) => b + y, 0) / h, _ = r.reduce((b, y) => b + (y - d) ** 2, 0) / (h - 1), C = Math.sqrt(_);
    if (C <= 0)
      return g.create(m.NUM);
    const E = Math.abs(as(f / 2, h - 1) * C / Math.sqrt(h));
    return R.create(E);
  }
  _getRangeValues(e) {
    const t = [], r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1;
    for (let s = 0; s < r; s++)
      for (let a = 0; a < n; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        u.isNull() || u.isBoolean() || u.isString() || t.push(+u.getValue());
      }
    return t;
  }
}
class HA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = R.create(Number.NEGATIVE_INFINITY);
    for (let r = 0; r < e.length; r++) {
      let n = e[r];
      if (!n.isNull()) {
        if ((n.isString() || n.isBoolean()) && (n = n.convertToNumberObjectValue()), n.isArray() && (n = n.max()), n.isError())
          return n;
        t = this._validator(t, n);
      }
    }
    return t.getValue() === Number.NEGATIVE_INFINITY ? R.create(0) : t;
  }
  _validator(e, t) {
    const r = e.isLessThan(t);
    let n = e;
    return r.getValue() && (n = t), n;
  }
}
class QA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = R.create(Number.NEGATIVE_INFINITY);
    for (let r = 0; r < e.length; r++) {
      let n = e[r];
      if (!n.isNull()) {
        if ((n.isString() || n.isBoolean()) && (n = n.convertToNumberObjectValue()), n.isError())
          return n;
        if (n.isArray() && n.iterator((s) => {
          let a = s;
          if ((a == null || a.isNull() || a.isString()) && (a = R.create(0)), a.isBoolean() && (a = a.convertToNumberObjectValue()), a.isError())
            return t = a, !1;
          t = this._validator(t, a);
        }), t.isError())
          return t;
        t = this._validator(t, n);
      }
    }
    return t.getValue() === Number.NEGATIVE_INFINITY ? R.create(0) : t;
  }
  _validator(e, t) {
    const r = e.isLessThan(t);
    let n = e;
    return r.getValue() && (n = t), n;
  }
}
class GA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 255);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, ...t) {
    const {
      isError: r,
      errorObject: n,
      rangeIsDifferentSize: s,
      criteriaMaxRowLength: a,
      criteriaMaxColumnLength: u,
      targetRange: c,
      variants: l
    } = Gr(t, e);
    if (r)
      return n;
    if (s)
      return a === 1 && u === 1 ? g.create(m.VALUE) : O(a, u, g.create(m.VALUE));
    if (a === 1 && u === 1)
      return this._scalarMaxifs(c, l);
    const f = Rt(l, a, u, !0);
    return this._aggregateResults(c, f);
  }
  _scalarMaxifs(e, t) {
    if (!e.isArray())
      return this._fallbackMaxifs(e, t);
    const r = e, n = t.length / 2, s = new Array(n), a = new Array(n), u = new Array(n);
    for (let C = 0; C < n; C++) {
      const E = t[C * 2], b = t[C * 2 + 1];
      if (!E.isArray())
        return this._fallbackMaxifs(e, t);
      s[C] = E;
      const y = b.isArray() && b.get(0, 0) || b;
      if (y.isError())
        return y;
      if (y.isString()) {
        const [p, N] = pt(`${y.getValue()}`);
        a[C] = p, u[C] = N;
      } else
        a[C] = x.EQUALS, u[C] = y;
    }
    const c = s[0].getRowCount(), l = s[0].getColumnCount();
    let f = -1 / 0, h = !1;
    const d = new Array(n);
    let _ = !0;
    for (let C = 0; C < n; C++) {
      const E = u[C].getValue();
      d[C] = typeof E == "string" ? E.toLocaleLowerCase() : E, typeof E == "string" && Mr(E) && (_ = !1);
    }
    if (_)
      for (let C = 0; C < c; C++)
        for (let E = 0; E < l; E++) {
          let b = !0;
          for (let y = 0; y < n; y++)
            if (!s[y].rawCompare(C, E, d[y], a[y])) {
              b = !1;
              break;
            }
          if (b) {
            const y = r.getNumberDirect(C, E);
            y > f && (f = y), h = !0;
          }
        }
    else
      for (let C = 0; C < c; C++)
        for (let E = 0; E < l; E++) {
          let b = !0;
          for (let y = 0; y < n; y++) {
            const p = s[y].get(C, E);
            if (!p || p.isError()) {
              b = !1;
              break;
            }
            if (!Lr(p, u[y])) {
              const w = a[y];
              if (w === x.EQUALS || w === x.NOT_EQUAL) {
                if (p.isNumber() && u[y].isString()) {
                  const S = u[y].convertToNumberObjectValue();
                  if (S.isNumber()) {
                    const M = p.compare(S, w);
                    (M.isError() || M.getValue() !== !0) && (b = !1);
                    continue;
                  }
                }
                if (u[y].isNumber() && p.isString()) {
                  const S = p.convertToNumberObjectValue();
                  if (S.isNumber()) {
                    const M = S.compare(u[y], w);
                    (M.isError() || M.getValue() !== !0) && (b = !1);
                    continue;
                  }
                }
                b = w === x.NOT_EQUAL;
              } else
                b = !1;
              if (!b) break;
              continue;
            }
            const N = p.compare(u[y], a[y]);
            if (N.isError() || N.getValue() !== !0) {
              b = !1;
              break;
            }
          }
          if (b) {
            const y = r.get(C, E);
            if (y && y.isNumber()) {
              const p = y.getValue();
              p > f && (f = p), h = !0;
            }
          }
        }
    return R.create(h ? f : 0);
  }
  _fallbackMaxifs(e, t) {
    const r = Rt(t, 1, 1, !0);
    return this._aggregateResults(e, r);
  }
  _aggregateResults(e, t) {
    const r = t.map((n) => n.map((s) => {
      const a = e.pick(s);
      return a.getColumnCount() === 0 ? W.create("0") : a.max();
    }));
    return r.length === 1 && r[0].length === 1 ? r[0][0] : W.create({
      calculateValueList: r,
      rowCount: r.length,
      columnCount: r[0].length,
      unitId: this.unitId || "",
      sheetId: this.subUnitId || "",
      row: this.row,
      column: this.column
    });
  }
}
class WA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = [];
    for (let r = 0; r < e.length; r++) {
      const n = e[r];
      if (n.isArray()) {
        let s = !1, a = g.create(m.VALUE);
        if (n.iterator((u) => {
          if (u != null && u.isError())
            return s = !0, a = u, !1;
          if (u != null && u.isNull() || u != null && u.isBoolean())
            return !0;
          const c = u.getValue();
          if (!q(c))
            return !0;
          t.push(+c);
        }), s)
          return a;
      } else {
        if (n.isError())
          return n;
        if (n.isNull() || n.isBoolean())
          continue;
        if (n.isString()) {
          const a = n.convertToNumberObjectValue();
          if (a.isError())
            return a;
        }
        const s = n.getValue();
        if (!q(s))
          continue;
        t.push(+s);
      }
    }
    return t.length === 0 ? g.create(m.NUM) : this._getResult(t.sort((r, n) => r - n));
  }
  _getResult(e) {
    const t = e.length;
    let r;
    if (t % 2 === 0) {
      const n = t / 2;
      r = (e[n - 1] + e[n]) / 2;
    } else
      r = e[Math.floor(t / 2)];
    return R.create(r);
  }
}
class qA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = R.create(Number.POSITIVE_INFINITY);
    for (let r = 0; r < e.length; r++) {
      let n = e[r];
      if (!n.isNull()) {
        if ((n.isString() || n.isBoolean()) && (n = n.convertToNumberObjectValue()), n.isArray() && (n = n.min()), n.isError())
          return n;
        t = this._validator(t, n);
      }
    }
    return t.getValue() === Number.POSITIVE_INFINITY ? R.create(0) : t;
  }
  _validator(e, t) {
    const r = e.isGreaterThan(t);
    let n = e;
    return r.getValue() && (n = t), n;
  }
}
class KA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = R.create(Number.POSITIVE_INFINITY);
    for (let r = 0; r < e.length; r++) {
      let n = e[r];
      if (!n.isNull()) {
        if ((n.isString() || n.isBoolean()) && (n = n.convertToNumberObjectValue()), n.isError())
          return n;
        if (n.isArray() && n.iterator((s) => {
          let a = s;
          if ((a == null || a.isNull() || a.isString()) && (a = R.create(0)), a.isBoolean() && (a = a.convertToNumberObjectValue()), a.isError())
            return t = a, !1;
          t = this._validator(t, a);
        }), t.isError())
          return t;
        t = this._validator(t, n);
      }
    }
    return t.getValue() === Number.POSITIVE_INFINITY ? R.create(0) : t;
  }
  _validator(e, t) {
    const r = e.isGreaterThan(t);
    let n = e;
    return r.getValue() && (n = t), n;
  }
}
class XA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 255);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, ...t) {
    const {
      isError: r,
      errorObject: n,
      rangeIsDifferentSize: s,
      criteriaMaxRowLength: a,
      criteriaMaxColumnLength: u,
      targetRange: c,
      variants: l
    } = Gr(t, e);
    if (r)
      return n;
    if (s)
      return a === 1 && u === 1 ? g.create(m.VALUE) : O(a, u, g.create(m.VALUE));
    if (a === 1 && u === 1)
      return this._scalarMinifs(c, l);
    const f = Rt(l, a, u, !0);
    return this._aggregateResults(c, f);
  }
  _scalarMinifs(e, t) {
    if (!e.isArray())
      return this._fallbackMinifs(e, t);
    const r = e, n = t.length / 2, s = new Array(n), a = new Array(n), u = new Array(n);
    for (let C = 0; C < n; C++) {
      const E = t[C * 2], b = t[C * 2 + 1];
      if (!E.isArray())
        return this._fallbackMinifs(e, t);
      s[C] = E;
      const y = b.isArray() && b.get(0, 0) || b;
      if (y.isError())
        return y;
      if (y.isString()) {
        const [p, N] = pt(`${y.getValue()}`);
        a[C] = p, u[C] = N;
      } else
        a[C] = x.EQUALS, u[C] = y;
    }
    const c = s[0].getRowCount(), l = s[0].getColumnCount();
    let f = 1 / 0, h = !1;
    const d = new Array(n);
    let _ = !0;
    for (let C = 0; C < n; C++) {
      const E = u[C].getValue();
      d[C] = typeof E == "string" ? E.toLocaleLowerCase() : E, typeof E == "string" && Mr(E) && (_ = !1);
    }
    if (_)
      for (let C = 0; C < c; C++)
        for (let E = 0; E < l; E++) {
          let b = !0;
          for (let y = 0; y < n; y++)
            if (!s[y].rawCompare(C, E, d[y], a[y])) {
              b = !1;
              break;
            }
          if (b) {
            const y = r.getNumberDirect(C, E);
            y < f && (f = y), h = !0;
          }
        }
    else
      for (let C = 0; C < c; C++)
        for (let E = 0; E < l; E++) {
          let b = !0;
          for (let y = 0; y < n; y++) {
            const p = s[y].get(C, E);
            if (!p || p.isError()) {
              b = !1;
              break;
            }
            if (!Lr(p, u[y])) {
              const w = a[y];
              if (w === x.EQUALS || w === x.NOT_EQUAL) {
                if (p.isNumber() && u[y].isString()) {
                  const S = u[y].convertToNumberObjectValue();
                  if (S.isNumber()) {
                    const M = p.compare(S, w);
                    (M.isError() || M.getValue() !== !0) && (b = !1);
                    continue;
                  }
                }
                if (u[y].isNumber() && p.isString()) {
                  const S = p.convertToNumberObjectValue();
                  if (S.isNumber()) {
                    const M = S.compare(u[y], w);
                    (M.isError() || M.getValue() !== !0) && (b = !1);
                    continue;
                  }
                }
                b = w === x.NOT_EQUAL;
              } else
                b = !1;
              if (!b) break;
              continue;
            }
            const N = p.compare(u[y], a[y]);
            if (N.isError() || N.getValue() !== !0) {
              b = !1;
              break;
            }
          }
          if (b) {
            const y = r.get(C, E);
            if (y && y.isNumber()) {
              const p = y.getValue();
              p < f && (f = p), h = !0;
            }
          }
        }
    return R.create(h ? f : 0);
  }
  _fallbackMinifs(e, t) {
    const r = Rt(t, 1, 1, !0);
    return this._aggregateResults(e, r);
  }
  _aggregateResults(e, t) {
    const r = t.map((n) => n.map((s) => {
      const a = e.pick(s);
      return a.getColumnCount() === 0 ? W.create("0") : a.min();
    }));
    return r.length === 1 && r[0].length === 1 ? r[0][0] : W.create({
      calculateValueList: r,
      rowCount: r.length,
      columnCount: r[0].length,
      unitId: this.unitId || "",
      sheetId: this.subUnitId || "",
      row: this.row,
      column: this.column
    });
  }
}
class ZA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = {};
    let r = 0, n = 1;
    for (let s = 0; s < e.length; s++) {
      const a = e[s];
      if (a.isError())
        return a;
      if (a.isString()) {
        const l = a.convertToNumberObjectValue();
        if (l.isError())
          return l;
      }
      const u = a.isArray() ? a.getRowCount() : 1, c = a.isArray() ? a.getColumnCount() : 1;
      for (let l = 0; l < u; l++)
        for (let f = 0; f < c; f++) {
          const h = a.isArray() ? a.get(l, f) : a;
          if (h.isError())
            return h;
          if (h.isNull() || h.isBoolean() || h.isString())
            continue;
          const d = h.getValue();
          q(d) && (t[+d] ? (t[+d].count++, t[+d].count > n && (n = t[+d].count)) : t[+d] = { count: 1, order: r++ });
        }
    }
    return r === 0 || n === 1 ? new g(m.NA) : this._getResult(t, n);
  }
  _getResult(e, t) {
    const r = Object.entries(e).filter(([n, { count: s }]) => s === t).sort((n, s) => n[1].order - s[1].order).map(([n]) => +n);
    return r.length === 1 ? R.create(r[0]) : W.createByArray(r.map((n) => [n]));
  }
}
class zA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = O(s, a, n, g.create(m.NA)), h = u.mapValue((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C), y = f.get(_, C);
      return d.isError() ? d : E.isError() ? E : b.isError() ? b : y.isError() ? y : this._handleSingleObject(d, E, b, y);
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
  _handleSingleObject(e, t, r, n) {
    const { isError: s, errorObject: a, variants: u } = I(e, t, r, n);
    if (s)
      return a;
    const [c, l, f, h] = u, d = Math.floor(+c.getValue()), _ = Math.floor(+l.getValue()), C = +f.getValue(), E = +h.getValue();
    if (d < 0 || _ < 1 || C <= 0 || C >= 1)
      return g.create(m.NUM);
    let b;
    return E ? b = Um(d, _, C) : b = Xa(d, _, C), R.create(b);
  }
}
class JA extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      return c.isError() ? c : h.isError() ? h : this._handleSingleObject(c, h);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
  _handleSingleObject(e, t) {
    const { isError: r, errorObject: n, variants: s } = I(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = +u.getValue();
    let f;
    return l ? f = qr(c, 0, 1) : f = ou(c, 0, 1), R.create(f);
  }
}
class eE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    let u = e;
    if (e.isArray() && r === 1 && n === 1 && (u = e.get(0, 0)), u.isError())
      return u;
    let c = t;
    if (t.isArray() && s === 1 && a === 1 && (c = t.get(0, 0)), c.isError())
      return c;
    if (r * n === 1 || s * a === 1)
      return u.isNull() || c.isNull() ? g.create(m.VALUE) : g.create(m.DIV_BY_ZERO);
    if (r * n !== s * a)
      return g.create(m.NA);
    const {
      isError: l,
      errorObject: f,
      array1Values: h,
      array2Values: d,
      noCalculate: _
    } = rt(
      e,
      t,
      r * n,
      n,
      a
    );
    return l ? f : _ ? g.create(m.DIV_BY_ZERO) : this._getResult(h, d);
  }
  _getResult(e, t) {
    const r = e.length;
    let n = 0, s = 0;
    for (let d = 0; d < r; d++)
      n += e[d], s += t[d];
    const a = n / r, u = s / r;
    let c = 0, l = 0, f = 0;
    for (let d = 0; d < r; d++)
      c += (e[d] - a) * (t[d] - u), l += (e[d] - a) ** 2, f += (t[d] - u) ** 2;
    if (l === 0 || f === 0)
      return g.create(m.DIV_BY_ZERO);
    const h = c / Math.sqrt(l * f);
    return R.create(h);
  }
}
class tE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = this._getValues(e);
    if (t.isArray()) {
      const n = t.mapValue((s) => this._handleSingleObject(r, s));
      return n.getRowCount() === 1 && n.getColumnCount() === 1 ? n.get(0, 0) : n;
    }
    return this._handleSingleObject(r, t);
  }
  _handleSingleObject(e, t) {
    if (e instanceof g)
      return e;
    const { isError: r, errorObject: n, variants: s } = I(t);
    if (r)
      return n;
    const [a] = s, u = +a.getValue(), c = e.length;
    if (u < 1 / (c + 1) || u > 1 - 1 / (c + 1))
      return g.create(m.NUM);
    const l = u * (c + 1) - 1, f = Math.floor(l), h = l - f;
    if (h === 0)
      return R.create(e[f]);
    const d = e[f] + h * (e[f + 1] - e[f]);
    return R.create(d);
  }
  _getValues(e) {
    const t = e.isArray() ? e.getRowCount() : 1, r = e.isArray() ? e.getColumnCount() : 1, n = [];
    for (let s = 0; s < t; s++)
      for (let a = 0; a < r; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        if (u.isNull() || u.isBoolean() || u.isString())
          continue;
        const c = u.getValue();
        q(c) && n.push(+c);
      }
    return n.length === 0 ? g.create(m.NUM) : n.sort((s, a) => s - a);
  }
}
class rE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = this._getValues(e);
    let s = r != null ? r : R.create(3);
    s.isNull() && (s = R.create(3));
    const a = Math.max(
      t.isArray() ? t.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1
    ), u = Math.max(
      t.isArray() ? t.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1
    ), c = O(a, u, t, g.create(m.NA)), l = O(a, u, s, g.create(m.NA)), f = c.mapValue((h, d, _) => {
      const C = l.get(d, _);
      return h.isError() ? h : C.isError() ? C : this._handleSingleObject(n, h, C);
    });
    return a === 1 && u === 1 ? f.get(0, 0) : f;
  }
  _handleSingleObject(e, t, r) {
    if (e instanceof g)
      return e;
    const { isError: n, errorObject: s, variants: a } = I(t, r);
    if (n)
      return s;
    const [u, c] = a, l = +u.getValue(), f = Math.floor(+c.getValue()), h = e.length;
    if (h === 0 || l < e[0] || l > e[h - 1])
      return g.create(m.NA);
    if (h === 1)
      return l === e[0] ? R.create(1) : g.create(m.NA);
    let d = 0, _ = !1, C = 0;
    for (; !_ && C < h; )
      l === e[C] ? (d = (C + 1) / (h + 1), _ = !0) : l > e[C] && C + 1 < h && l < e[C + 1] && (d = (C + 1 + (l - e[C]) / (e[C + 1] - e[C])) / (h + 1), _ = !0), C++;
    return _ ? f < 1 ? g.create(m.NUM) : (d = Xe(d, f), R.create(d)) : g.create(m.NA);
  }
  _getValues(e) {
    const t = e.isArray() ? e.getRowCount() : 1, r = e.isArray() ? e.getColumnCount() : 1, n = [];
    for (let s = 0; s < t; s++)
      for (let a = 0; a < r; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        if (u.isNull() || u.isBoolean())
          continue;
        const c = u.getValue();
        q(c) && n.push(+c);
      }
    return n.sort((s, a) => s - a);
  }
}
class nE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      if (c.isError())
        return c;
      if (h.isError())
        return h;
      const { isError: d, errorObject: _, variants: C } = I(c, h);
      if (d)
        return _;
      const [E, b] = C, y = Math.floor(+E.getValue()), p = Math.floor(+b.getValue());
      if (y < 0 || y >= 2147483647 || p < 0 || y < p)
        return g.create(m.NUM);
      let N = 1;
      for (let w = y - p + 1; w <= y; w++)
        N *= w;
      return Number.isFinite(N) ? R.create(N) : g.create(m.NUM);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
}
class sE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      if (c.isError())
        return c;
      if (h.isError())
        return h;
      const { isError: d, errorObject: _, variants: C } = I(c, h);
      if (d)
        return _;
      const [E, b] = C, y = Math.floor(+E.getValue()), p = Math.floor(+b.getValue());
      if (y < 0 || y >= 2147483647 || p < 0)
        return g.create(m.NUM);
      if (y === 0)
        return p === 0 ? R.create(1) : R.create(0);
      const N = y ** p;
      return Number.isFinite(N) ? R.create(N) : g.create(m.NUM);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
}
class aE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const { isError: t, errorObject: r, variants: n } = I(e);
    if (t)
      return r;
    const [s] = n, a = +s.getValue(), u = Math.exp(-0.5 * a * a) / Math.sqrt(2 * Math.PI);
    return R.create(u);
  }
}
class iE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const { isError: s, errorObject: a, xRangeValues: u, probRangeValues: c } = this._handleXRangeAndProbRange(e, t);
    let l = n != null ? n : r;
    n != null && n.isNull() && (l = r);
    const f = Math.max(
      r.isArray() ? r.getRowCount() : 1,
      l.isArray() ? l.getRowCount() : 1
    ), h = Math.max(
      r.isArray() ? r.getColumnCount() : 1,
      l.isArray() ? l.getColumnCount() : 1
    ), d = O(f, h, r, g.create(m.NA)), _ = O(f, h, l, g.create(m.NA)), C = d.mapValue((E, b, y) => {
      const p = _.get(b, y);
      return e.isError() ? e : t.isError() ? t : E.isError() ? E : p.isError() ? p : s ? a : this._handleSingleObject(u, c, E, p);
    });
    return f === 1 && h === 1 ? C.get(0, 0) : C;
  }
  _handleSingleObject(e, t, r, n) {
    const { isError: s, errorObject: a, variants: u } = I(r, n);
    if (s)
      return a;
    const [c, l] = u, f = +c.getValue(), h = +l.getValue();
    if (t.reduce((_, C) => _ + C, 0) !== 1)
      return g.create(m.NUM);
    let d = 0;
    for (let _ = 0; _ < e.length; _++)
      e[_] >= f && e[_] <= h && (d += t[_]);
    return R.create(d);
  }
  // eslint-disable-next-line
  _handleXRangeAndProbRange(e, t) {
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    let u = e;
    if (e.isArray() && r === 1 && n === 1 && (u = e.get(0, 0)), u.isError())
      return {
        isError: !0,
        errorObject: u,
        xRangeValues: [],
        probRangeValues: []
      };
    let c = t;
    if (t.isArray() && s === 1 && a === 1 && (c = t.get(0, 0)), c.isError())
      return {
        isError: !0,
        errorObject: c,
        xRangeValues: [],
        probRangeValues: []
      };
    if ((r * n === 1 || s * a === 1) && (u.isNull() || c.isNull()))
      return {
        isError: !0,
        errorObject: g.create(m.VALUE),
        xRangeValues: [],
        probRangeValues: []
      };
    if (r * n !== s * a)
      return {
        isError: !0,
        errorObject: g.create(m.NA),
        xRangeValues: [],
        probRangeValues: []
      };
    const {
      isError: l,
      errorObject: f,
      array1Values: h,
      array2Values: d,
      noCalculate: _
    } = rt(
      e,
      t,
      r * n,
      n,
      a
    );
    return l ? {
      isError: !0,
      errorObject: f,
      xRangeValues: [],
      probRangeValues: []
    } : _ ? {
      isError: !0,
      errorObject: g.create(m.DIV_BY_ZERO),
      xRangeValues: [],
      probRangeValues: []
    } : {
      isError: !1,
      errorObject: null,
      xRangeValues: h,
      probRangeValues: d
    };
  }
}
class oE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = this._getValues(e);
    if (t.isArray()) {
      const n = t.mapValue((s) => this._handleSingleObject(r, s));
      return n.getRowCount() === 1 && n.getColumnCount() === 1 ? n.get(0, 0) : n;
    }
    return this._handleSingleObject(r, t);
  }
  _handleSingleObject(e, t) {
    if (e instanceof g)
      return e;
    const { isError: r, errorObject: n, variants: s } = I(t);
    if (r)
      return n;
    const [a] = s, u = Math.floor(+a.getValue());
    if (u <= 0 || u >= 4)
      return g.create(m.NUM);
    const c = u / 4, l = e.length;
    if (c < 1 / (l + 1) || c > 1 - 1 / (l + 1))
      return g.create(m.NUM);
    const f = c * (l + 1) - 1, h = Math.floor(f), d = f - h;
    if (d === 0)
      return R.create(e[h]);
    const _ = e[h] + d * (e[h + 1] - e[h]);
    return R.create(_);
  }
  _getValues(e) {
    const t = e.isArray() ? e.getRowCount() : 1, r = e.isArray() ? e.getColumnCount() : 1, n = [];
    for (let s = 0; s < t; s++)
      for (let a = 0; a < r; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        if (u.isNull() || u.isBoolean())
          continue;
        const c = u.getValue();
        q(c) && n.push(+c);
      }
    return n.length === 0 ? g.create(m.NUM) : n.sort((s, a) => s - a);
  }
}
class uE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, t, r) {
    let n = e;
    n.isReferenceObject() && (n = n.toArrayValueObject());
    const { refHasError: s, refErrorObject: a, refNumbers: u } = this._checkRefReferenceObject(t);
    let c = r != null ? r : R.create(0);
    c.isReferenceObject() && (c = c.toArrayValueObject());
    const l = Math.max(
      n.isArray() ? n.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1
    ), f = Math.max(
      n.isArray() ? n.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1
    ), h = O(l, f, n, g.create(m.NA)), d = O(l, f, c, g.create(m.NA)), _ = h.map((C, E, b) => {
      const y = d.get(E, b);
      if (!e.isReferenceObject() && e.isNull())
        return g.create(m.NA);
      if (s)
        return a;
      if (y.isError())
        return y;
      const p = +C.getValue(), N = +y.getValue();
      return Number.isNaN(p) || Number.isNaN(N) ? g.create(m.VALUE) : this._getResult(p, N, u);
    });
    return l === 1 && f === 1 ? _.get(0, 0) : _;
  }
  _getResult(e, t, r) {
    const n = r.sort((c, l) => t ? c - l : l - c);
    let s = n.indexOf(e);
    const a = [];
    for (; s >= 0; ) {
      const c = s + 1;
      a.push(c), s = n.indexOf(e, c);
    }
    if (a.length === 0)
      return g.create(m.NA);
    const u = a.reduce((c, l) => c + l, 0) / a.length;
    return R.create(u);
  }
  _checkRefReferenceObject(e) {
    let t = !1, r = g.create(m.NA);
    const n = [];
    return e.isReferenceObject() ? (e.toArrayValueObject().iterator((a) => {
      const u = a;
      if (u.isError())
        return t = !0, r = u, !1;
      if (u.isNull() || u.isBoolean())
        return !0;
      const c = +u.getValue();
      if (Number.isNaN(c))
        return !0;
      n.push(c);
    }), {
      refHasError: t,
      refErrorObject: r,
      refNumbers: n
    }) : {
      refHasError: !0,
      refErrorObject: r,
      refNumbers: n
    };
  }
}
class cE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e, t, r) {
    let n = e;
    n.isReferenceObject() && (n = n.toArrayValueObject());
    const { refHasError: s, refErrorObject: a, refNumbers: u } = this._checkRefReferenceObject(t);
    let c = r != null ? r : R.create(0);
    c.isReferenceObject() && (c = c.toArrayValueObject());
    const l = Math.max(
      n.isArray() ? n.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1
    ), f = Math.max(
      n.isArray() ? n.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1
    ), h = O(l, f, n, g.create(m.NA)), d = O(l, f, c, g.create(m.NA)), _ = [...u].sort((p, N) => N - p), C = [...u].sort((p, N) => p - N), E = /* @__PURE__ */ new Map();
    for (let p = 0; p < _.length; p++)
      E.has(_[p]) || E.set(_[p], p);
    const b = /* @__PURE__ */ new Map();
    for (let p = 0; p < C.length; p++)
      b.has(C[p]) || b.set(C[p], p);
    const y = h.map((p, N, w) => {
      const S = d.get(N, w);
      if (!e.isReferenceObject() && e.isNull())
        return g.create(m.NA);
      if (s)
        return a;
      if (S.isError())
        return S;
      const M = +p.getValue(), D = +S.getValue();
      if (Number.isNaN(M) || Number.isNaN(D))
        return g.create(m.VALUE);
      const v = (D ? b : E).get(M);
      return v === void 0 ? g.create(m.NA) : R.create(v + 1);
    });
    return l === 1 && f === 1 ? y.get(0, 0) : y;
  }
  _checkRefReferenceObject(e) {
    let t = !1, r = g.create(m.NA);
    const n = [];
    if (!e.isReferenceObject())
      return {
        refHasError: !0,
        refErrorObject: r,
        refNumbers: n
      };
    const s = e.toArrayValueObject(), a = s.getNumericData();
    if (a) {
      for (let u = 0; u < a.length; u++) {
        const c = a[u];
        Number.isFinite(c) && n.push(c);
      }
      return { refHasError: t, refErrorObject: r, refNumbers: n };
    }
    return s.iterator((u) => {
      const c = u;
      if (c.isError())
        return t = !0, r = c, !1;
      if (c.isNull() || c.isBoolean())
        return !0;
      const l = +c.getValue();
      if (Number.isNaN(l))
        return !0;
      n.push(l);
    }), {
      refHasError: t,
      refErrorObject: r,
      refNumbers: n
    };
  }
}
class lE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    let u = e;
    if (e.isArray() && r === 1 && n === 1 && (u = e.get(0, 0)), u.isError())
      return u;
    let c = t;
    if (t.isArray() && s === 1 && a === 1 && (c = t.get(0, 0)), c.isError())
      return c;
    if ((r * n === 1 || s * a === 1) && (u.isNull() || c.isNull()))
      return g.create(m.VALUE);
    if (r * n !== s * a)
      return g.create(m.NA);
    const {
      isError: l,
      errorObject: f,
      array1Values: h,
      array2Values: d,
      noCalculate: _
    } = rt(
      e,
      t,
      r * n,
      n,
      a
    );
    return l ? f : _ ? g.create(m.DIV_BY_ZERO) : this._getResult(h, d);
  }
  _getResult(e, t) {
    if (e.length === 0)
      return g.create(m.DIV_BY_ZERO);
    const r = e.length;
    let n = 0, s = 0;
    for (let d = 0; d < r; d++)
      n += e[d], s += t[d];
    const a = n / r, u = s / r;
    let c = 0, l = 0, f = 0;
    for (let d = 0; d < r; d++)
      c += (e[d] - a) * (t[d] - u), l += (e[d] - a) ** 2, f += (t[d] - u) ** 2;
    if (l === 0 || f === 0)
      return g.create(m.DIV_BY_ZERO);
    const h = (c / Math.sqrt(l * f)) ** 2;
    return R.create(h);
  }
}
class fE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = [];
    let r = 0;
    for (let n = 0; n < e.length; n++) {
      const s = e[n];
      if (s.isError())
        return s;
      if (s.isString()) {
        const c = s.convertToNumberObjectValue();
        if (c.isError())
          return c;
      }
      const a = s.isArray() ? s.getRowCount() : 1, u = s.isArray() ? s.getColumnCount() : 1;
      for (let c = 0; c < a; c++)
        for (let l = 0; l < u; l++) {
          const f = s.isArray() ? s.get(c, l) : s;
          if (f.isError())
            return f;
          if (f.isNull() || f.isBoolean())
            continue;
          const h = f.getValue();
          q(h) && (t.push(+h), r += +h);
        }
    }
    return t.length <= 2 ? g.create(m.DIV_BY_ZERO) : this._getResult(t, r);
  }
  _getResult(e, t) {
    const r = e.length, n = t / r;
    let s = 0;
    for (let l = 0; l < r; l++)
      s += (e[l] - n) ** 2;
    const a = Math.sqrt(s / (r - 1));
    if (a === 0)
      return g.create(m.DIV_BY_ZERO);
    let u = 0;
    for (let l = 0; l < r; l++)
      u += ((e[l] - n) / a) ** 3;
    const c = r / ((r - 1) * (r - 2)) * u;
    return R.create(c);
  }
}
class hE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = [];
    let r = 0;
    for (let n = 0; n < e.length; n++) {
      const s = e[n];
      if (s.isError())
        return s;
      if (s.isString()) {
        const c = s.convertToNumberObjectValue();
        if (c.isError())
          return c;
      }
      const a = s.isArray() ? s.getRowCount() : 1, u = s.isArray() ? s.getColumnCount() : 1;
      for (let c = 0; c < a; c++)
        for (let l = 0; l < u; l++) {
          const f = s.isArray() ? s.get(c, l) : s;
          if (f.isError())
            return f;
          if (f.isNull() || f.isBoolean())
            continue;
          const h = f.getValue();
          q(h) && (t.push(+h), r += +h);
        }
    }
    return t.length <= 2 ? g.create(m.DIV_BY_ZERO) : this._getResult(t, r);
  }
  _getResult(e, t) {
    const r = e.length, n = t / r;
    let s = 0;
    for (let l = 0; l < r; l++)
      s += (e[l] - n) ** 2;
    const a = Math.sqrt(s / r);
    if (a === 0)
      return g.create(m.DIV_BY_ZERO);
    let u = 0;
    for (let l = 0; l < r; l++)
      u += ((e[l] - n) / a) ** 3;
    const c = u / r;
    return R.create(c);
  }
}
class mE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    let u = e;
    if (e.isArray() && r === 1 && n === 1 && (u = e.get(0, 0)), u.isError())
      return u;
    let c = t;
    if (t.isArray() && s === 1 && a === 1 && (c = t.get(0, 0)), c.isError())
      return c;
    if (r * n === 1 || s * a === 1)
      return u.isNull() || c.isNull() ? g.create(m.VALUE) : g.create(m.DIV_BY_ZERO);
    if (r * n !== s * a)
      return g.create(m.NA);
    const {
      isError: l,
      errorObject: f,
      array1Values: h,
      array2Values: d,
      noCalculate: _
    } = rt(
      e,
      t,
      r * n,
      n,
      a
    );
    return l ? f : _ ? g.create(m.DIV_BY_ZERO) : this._getResult(h, d);
  }
  _getResult(e, t) {
    const r = e.length;
    let n = 0, s = 0;
    for (let h = 0; h < r; h++)
      n += e[h], s += t[h];
    const a = n / r, u = s / r;
    let c = 0, l = 0;
    for (let h = 0; h < r; h++)
      c += (e[h] - a) * (t[h] - u), l += (t[h] - u) ** 2;
    if (l === 0)
      return g.create(m.DIV_BY_ZERO);
    const f = c / l;
    return R.create(f);
  }
}
class gE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = this._getValues(e);
    if (t.isArray()) {
      const n = t.mapValue((s) => this._handleSingleObject(r, s));
      return n.getRowCount() === 1 && n.getColumnCount() === 1 ? n.get(0, 0) : n;
    }
    return this._handleSingleObject(r, t);
  }
  _handleSingleObject(e, t) {
    if (e instanceof g)
      return e;
    const { isError: r, errorObject: n, variants: s } = I(t);
    if (r)
      return n;
    const [a] = s;
    let u = +a.getValue();
    return u < 1 || u > e.length ? g.create(m.NUM) : (u = Math.floor(u), R.create(e[u - 1]));
  }
  _getValues(e) {
    const t = e.isArray() ? e.getRowCount() : 1, r = e.isArray() ? e.getColumnCount() : 1, n = [];
    for (let s = 0; s < t; s++)
      for (let a = 0; a < r; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        if (u.isNull() || u.isBoolean() || u.isString())
          continue;
        const c = u.getValue();
        q(c) && n.push(+c);
      }
    return n.length === 0 ? g.create(m.NUM) : n.sort((s, a) => s - a);
  }
}
class dE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = +c.getValue(), d = +l.getValue();
    if (d <= 0)
      return g.create(m.NUM);
    const _ = (f - h) / d;
    return R.create(_);
  }
}
class CE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = this.flattenArray(e, !1);
    return t.isError() ? t : t.std(1);
  }
}
class _E extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = this.flattenArray(e, !1);
    return t.isError() ? t : t.std();
  }
}
class AE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1, s = t.isArray() ? t.getRowCount() : 1, a = t.isArray() ? t.getColumnCount() : 1;
    let u = e;
    if (e.isArray() && r === 1 && n === 1 && (u = e.get(0, 0)), u.isError())
      return u;
    let c = t;
    if (t.isArray() && s === 1 && a === 1 && (c = t.get(0, 0)), c.isError())
      return c;
    if ((r * n === 1 || s * a === 1) && (u.isNull() || c.isNull()))
      return g.create(m.VALUE);
    if (r * n !== s * a)
      return g.create(m.NA);
    const {
      isError: l,
      errorObject: f,
      array1Values: h,
      array2Values: d,
      noCalculate: _
    } = rt(
      e,
      t,
      r * n,
      n,
      a
    );
    return l ? f : _ ? g.create(m.DIV_BY_ZERO) : this._getResult(h, d);
  }
  _getResult(e, t) {
    const r = e.length;
    if (r <= 2)
      return g.create(m.DIV_BY_ZERO);
    let n = 0, s = 0;
    for (let d = 0; d < r; d++)
      n += e[d], s += t[d];
    const a = n / r, u = s / r;
    let c = 0, l = 0, f = 0;
    for (let d = 0; d < r; d++)
      c += (e[d] - a) * (t[d] - u), l += (e[d] - a) ** 2, f += (t[d] - u) ** 2;
    if (f === 0)
      return g.create(m.DIV_BY_ZERO);
    const h = Math.sqrt((l - c ** 2 / f) / (r - 2));
    return Number.isNaN(h) || !Number.isFinite(h) ? g.create(m.NUM) : R.create(h);
  }
}
class EE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a, f = +u.getValue(), h = Math.floor(+c.getValue()), d = +l.getValue();
    if (h < 1)
      return g.create(m.NUM);
    let _;
    return d ? _ = Kr(f, h) : _ = Tm(f, h), Number.isNaN(_) || !Number.isFinite(_) ? g.create(m.NUM) : R.create(_);
  }
}
class bE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      return c.isError() ? c : h.isError() ? h : this._handleSingleObject(c, h);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
  _handleSingleObject(e, t) {
    const { isError: r, errorObject: n, variants: s } = I(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = Math.floor(+u.getValue());
    if (c < 0 || l < 1 || l > 10 ** 10)
      return g.create(m.NUM);
    const f = Kr(-c, l) * 2;
    return Number.isNaN(f) || !Number.isFinite(f) ? g.create(m.NUM) : R.create(f);
  }
}
class yE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      return c.isError() ? c : h.isError() ? h : this._handleSingleObject(c, h);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
  _handleSingleObject(e, t) {
    const { isError: r, errorObject: n, variants: s } = I(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = Math.floor(+u.getValue());
    if (l < 1 || l > 10 ** 10)
      return g.create(m.NUM);
    const f = Kr(-c, l);
    return Number.isNaN(f) || !Number.isFinite(f) ? g.create(m.NUM) : R.create(f);
  }
}
class RE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      return c.isError() ? c : h.isError() ? h : this._handleSingleObject(c, h);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
  _handleSingleObject(e, t) {
    const { isError: r, errorObject: n, variants: s } = I(e, t);
    if (r)
      return n;
    const [a, u] = s, c = +a.getValue(), l = Math.floor(+u.getValue());
    if (c <= 0 || c > 1 || l < 1 || l > 10 ** 10)
      return g.create(m.NUM);
    const f = as(c, l);
    return Number.isNaN(f) || !Number.isFinite(f) ? g.create(m.NUM) : R.create(f);
  }
}
class pE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const { isError: s, errorObject: a } = is(e, t, r);
    if (s)
      return a;
    const u = bt(e);
    if (u instanceof g)
      return u;
    const c = this._getKnownXsValues(u, t);
    if (c instanceof g)
      return c;
    const l = this._getNewXsValues(c, r);
    if (l instanceof g)
      return l;
    let f = n != null ? n : T.create(!0);
    if (f.isArray() && (f = f.get(0, 0)), f.isString() && (f = f.convertToNumberObjectValue()), f.isError())
      return f;
    const h = +f.getValue();
    return this._getResult(u, c, l, h);
  }
  _getResult(e, t, r, n) {
    return e.length === 1 && t.length > 1 || e[0].length === 1 && t[0].length > 1 ? e.length === 1 && t.length > 1 && (n ? t.length + 1 : t.length) > e[0].length || e[0].length === 1 && t[0].length > 1 && (n ? t[0].length + 1 : t[0].length) > e.length ? g.create(m.NA) : this._getResultByMultipleVariables(e, t, r, n) : this._getResultBySimpleVariables(e, t, r, n);
  }
  _getResultByMultipleVariables(e, t, r, n) {
    const s = e.length === 1 && e[0].length > 1, a = cs(e, t, r, n, !1);
    if (a instanceof g)
      return a;
    const { coefficients: u, newX: c } = a, l = u[0].length, f = u[0][l - 1];
    let h = [];
    for (let d = 0; d < c.length; d++) {
      h[d] = [];
      let _ = f;
      for (let C = l - 2; C >= 0; C--)
        _ += u[0][l - 2 - C] * c[d][C];
      h[d].push(_);
    }
    return s && (h = _t(h)), W.createByArray(h);
  }
  _getResultBySimpleVariables(e, t, r, n) {
    const s = e.flat(), a = t.flat(), { slope: u, intercept: c } = us(a, s, n, !1);
    if (Number.isNaN(u))
      return g.create(m.NA);
    const l = r.map((f) => f.map((h) => u * h + c));
    return W.createByArray(l);
  }
  _getKnownXsValues(e, t) {
    return !t || t.isNull() ? os(e.length, e[0].length) : bt(t);
  }
  _getNewXsValues(e, t) {
    return !t || t.isNull() ? e : bt(t);
  }
}
class NE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = this._getValues(e);
    if (t.isArray()) {
      const n = t.mapValue((s) => this._handleSingleObject(r, s));
      return n.getRowCount() === 1 && n.getColumnCount() === 1 ? n.get(0, 0) : n;
    }
    return this._handleSingleObject(r, t);
  }
  _handleSingleObject(e, t) {
    if (e instanceof g)
      return e;
    const { isError: r, errorObject: n, variants: s } = I(t);
    if (r)
      return n;
    const [a] = s, u = +a.getValue();
    if (u < 0 || u >= 1)
      return g.create(m.NUM);
    const c = Xe(e.length * u / 2, 0) * 2, l = e.slice(c / 2, e.length - c / 2), f = l.reduce((h, d) => h + d, 0) / l.length;
    return R.create(f);
  }
  _getValues(e) {
    const t = e.isArray() ? e.getRowCount() : 1, r = e.isArray() ? e.getColumnCount() : 1, n = [];
    for (let s = 0; s < t; s++)
      for (let a = 0; a < r; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        if (u.isNull() || u.isBoolean())
          continue;
        const c = u.getValue();
        q(c) && n.push(+c);
      }
    return n.length === 0 ? g.create(m.NUM) : n.sort((s, a) => s - a);
  }
}
class VE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = this.flattenArray(e, !1);
    return t.isError() ? t : t.var(1);
  }
}
class OE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    const t = this.flattenArray(e, !1);
    return t.isError() ? t : t.var();
  }
}
const wE = [
  [uA, B.AVEDEV],
  [lA, B.AVERAGE],
  [fA, B.AVERAGE_WEIGHTED],
  [hA, B.AVERAGEA],
  [mA, B.AVERAGEIF],
  [gA, B.AVERAGEIFS],
  [dA, B.BETA_DIST],
  [cu, B.BETA_INV],
  [lu, B.BINOM_DIST],
  [CA, B.BINOM_DIST_RANGE],
  [fu, B.BINOM_INV],
  [_A, B.CHISQ_DIST],
  [hu, B.CHISQ_DIST_RT],
  [AA, B.CHISQ_INV],
  [mu, B.CHISQ_INV_RT],
  [gu, B.CHISQ_TEST],
  [du, B.CONFIDENCE_NORM],
  [EA, B.CONFIDENCE_T],
  [bA, B.CORREL],
  [yA, B.COUNT],
  [RA, B.COUNTA],
  [pA, B.COUNTBLANK],
  [em, B.COUNTIF],
  [tm, B.COUNTIFS],
  [Cu, B.COVARIANCE_P],
  [NA, B.COVARIANCE_S],
  [VA, B.DEVSQ],
  [_u, B.EXPON_DIST],
  [OA, B.F_DIST],
  [Au, B.F_DIST_RT],
  [wA, B.F_INV],
  [Eu, B.F_INV_RT],
  [bu, B.F_TEST],
  [SA, B.FISHER],
  [MA, B.FISHERINV],
  [Ki, B.FORECAST],
  [Ki, B.FORECAST_LINEAR],
  [DA, B.FREQUENCY],
  [LA, B.GAMMA],
  [yu, B.GAMMA_DIST],
  [Ru, B.GAMMA_INV],
  [Xi, B.GAMMALN],
  [Xi, B.GAMMALN_PRECISE],
  [xA, B.GAUSS],
  [PA, B.GEOMEAN],
  [jA, B.GROWTH],
  [UA, B.HARMEAN],
  [vA, B.HYPGEOM_DIST],
  [TA, B.INTERCEPT],
  [BA, B.KURT],
  [IA, B.LARGE],
  [FA, B.LINEST],
  [kA, B.LOGEST],
  [$A, B.LOGNORM_DIST],
  [pu, B.LOGNORM_INV],
  [YA, B.MARGINOFERROR],
  [HA, B.MAX],
  [QA, B.MAXA],
  [GA, B.MAXIFS],
  [WA, B.MEDIAN],
  [qA, B.MIN],
  [KA, B.MINA],
  [XA, B.MINIFS],
  [ZA, B.MODE_MULT],
  [Nu, B.MODE_SNGL],
  [zA, B.NEGBINOM_DIST],
  [Vu, B.NORM_DIST],
  [Ou, B.NORM_INV],
  [JA, B.NORM_S_DIST],
  [wu, B.NORM_S_INV],
  [eE, B.PEARSON],
  [tE, B.PERCENTILE_EXC],
  [Su, B.PERCENTILE_INC],
  [rE, B.PERCENTRANK_EXC],
  [Mu, B.PERCENTRANK_INC],
  [nE, B.PERMUT],
  [sE, B.PERMUTATIONA],
  [aE, B.PHI],
  [Du, B.POISSON_DIST],
  [iE, B.PROB],
  [oE, B.QUARTILE_EXC],
  [Lu, B.QUARTILE_INC],
  [uE, B.RANK_AVG],
  [cE, B.RANK_EQ],
  [lE, B.RSQ],
  [fE, B.SKEW],
  [hE, B.SKEW_P],
  [mE, B.SLOPE],
  [gE, B.SMALL],
  [dE, B.STANDARDIZE],
  [xu, B.STDEV_P],
  [Pu, B.STDEV_S],
  [CE, B.STDEVA],
  [_E, B.STDEVPA],
  [AE, B.STEYX],
  [EE, B.T_DIST],
  [bE, B.T_DIST_2T],
  [yE, B.T_DIST_RT],
  [RE, B.T_INV],
  [ju, B.T_INV_2T],
  [Uu, B.T_TEST],
  [pE, B.TREND],
  [NE, B.TRIMMEAN],
  [vu, B.VAR_P],
  [Tu, B.VAR_S],
  [VE, B.VARA],
  [OE, B.VARPA],
  [Bu, B.WEIBULL_DIST],
  [Iu, B.Z_TEST]
];
class SE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    let r = t != null ? t : R.create(0);
    if (t != null && t.isNull() && (r = R.create(0)), r.isArray()) {
      const n = r.mapValue((s) => this._handleSingleObject(e, s));
      return n.getRowCount() === 1 && n.getColumnCount() === 1 ? n.get(0, 0) : n;
    }
    return this._handleSingleObject(e, r);
  }
  _handleSingleObject(e, t) {
    const r = this._checkArray(e);
    if (r.isError())
      return r;
    const { isError: n, errorObject: s, variants: a } = I(t);
    if (n)
      return s;
    const [u] = a, c = +u.getValue(), l = e.isArray() ? e.getRowCount() : 1, f = e.isArray() ? e.getColumnCount() : 1;
    let h = "";
    for (let d = 0; d < l; d++)
      for (let _ = 0; _ < f; _++) {
        const C = e.isArray() ? e.get(d, _) : e;
        let E = `${C.getValue()}`;
        C.isNull() && (E = ""), C.isBoolean() && (E = E.toLocaleUpperCase()), C.isString() && c ? h += `"${E}"` : h += E, d === l - 1 && _ === f - 1 || (c ? _ === f - 1 ? h += ";" : h += "," : h += ", ");
      }
    return c && (h = `{${h}}`), h.length > 32767 ? g.create(m.CALC) : U.create(h);
  }
  _checkArray(e) {
    if (e.isArray()) {
      const t = e.getRowCount(), r = e.getColumnCount();
      return t > 1 || r > 1 ? e : e.get(0, 0);
    }
    return e;
  }
}
class ME extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError() || e.isNull() || e.isBoolean() || e.isNumber())
      return e;
    const t = e.getValue().toLocaleString();
    let r = "";
    for (let n = 0; n < t.length; n++) {
      let s = t.charCodeAt(n);
      s === 12288 ? s = 32 : s >= 65281 && s <= 65374 && (s -= 65248), r += String.fromCharCode(s);
    }
    return U.create(r);
  }
}
class DE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const { isError: t, errorObject: r, variants: n } = I(e);
    if (t)
      return r;
    const [s] = n, a = +s.getValue(), u = Math.abs(Number.parseInt(a.toString(), 10)), c = Number.parseFloat((Math.abs(a) - u).toFixed(2));
    let l = "";
    return u === 0 ? c !== 0 ? l = `${a < 0 ? "ลบ" : ""}${this._convertNumberToThaiText(c * 100)}สตางค์` : l = "ศูนย์บาทถ้วน" : c === 0 ? l = `${a < 0 ? "ลบ" : ""}${this._convertNumberToThaiText(u)}บาทถ้วน` : l = `${a < 0 ? "ลบ" : ""}${this._convertNumberToThaiText(u)}บาท${this._convertNumberToThaiText(c * 100)}สตางค์`, U.create(l);
  }
  _convertNumberToThaiText(e) {
    const t = ["ล้าน", "สิบ", "ร้อย", "พัน", "หมื่น", "แสน", ""], r = ["ศูนย์", "หนึ่ง", "สอง", "สาม", "สี่", "ห้า", "หก", "เจ็ด", "แปด", "เก้า"], n = ["ลบ", "บาท", "ถ้วน", "สตางค์", "ยี่", "เอ็ด", ",", " ", "฿"], s = e.toString(), a = s.length;
    let u = "";
    for (let c = a; c > 0; c--) {
      const l = Number.parseInt(s.charAt(a - c), 10);
      let f = r[l];
      const h = c > 1 ? (c - 1) % 6 : 6;
      if (h === 1 && l === 2 && (f = n[4]), l === 1)
        switch (h) {
          case 0:
          case 6:
            u += c < a ? n[5] : f;
            break;
          case 1:
            break;
          default:
            u += f;
            break;
        }
      else if (l === 0) {
        h === 0 && (u += t[h]);
        continue;
      } else
        u += f;
      u += t[h];
    }
    return u;
  }
}
const LE = Object.values(Te).filter((o) => [
  Te.TABLE_START,
  Te.TABLE_ROW_START,
  Te.TABLE_CELL_START,
  Te.TABLE_CELL_END,
  Te.TABLE_ROW_END,
  Te.TABLE_END,
  Te.CUSTOM_BLOCK
].includes(o));
class xE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const { isError: t, errorObject: r, variants: n } = I(e);
    if (t)
      return r;
    const [s] = n, a = Math.floor(+s.getValue());
    if (a <= 0)
      return g.create(m.VALUE);
    let u = String.fromCharCode(a);
    return LE.some((c) => c === u) && (u = ""), U.create(u);
  }
}
class PE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError() || e.isBoolean() || e.isNumber())
      return e;
    if (e.isNull())
      return U.create("");
    const r = `${e.getValue()}`.replace(/[\0-\x1F]/g, "");
    return U.create(r);
  }
}
class jE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    if (e.isNull())
      return g.create(m.VALUE);
    let t = e.getValue().toLocaleString();
    if (e.isBoolean() && (t = t.toLocaleUpperCase()), t === "")
      return g.create(m.VALUE);
    const r = t.charCodeAt(0);
    return R.create(r);
  }
}
class UE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = "", r = null;
    for (const n of e)
      if (n.isArray()) {
        if (n.iterator((s) => {
          if (s == null || s.isNull())
            return !0;
          if (s.isError())
            return r = s, !1;
          s.isBoolean() ? t += `${s.getValue()}`.toLocaleUpperCase() : (s.isString() || s.isNumber()) && (t += s.getValue());
        }), r)
          return r;
      } else !n.isError() && !n.isNull() && (t += n.getValue());
    return U.create(t);
  }
}
class vE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 255);
  }
  calculate(...e) {
    let t = 0, r = 0;
    e.forEach((s) => {
      if (s.isArray()) {
        const a = s;
        t = Math.max(t, a.getRowCount()), r = Math.max(r, a.getColumnCount());
      } else
        t = Math.max(t, 1), r = Math.max(r, 1);
    });
    let n = null;
    for (const s of e)
      n = O(t, r, s, g.create(m.NA)).mapValue((u, c, l) => {
        const f = n && n.get(c, l);
        if (f != null && f.isError())
          return f;
        if (u.isError())
          return u;
        let h = f == null ? void 0 : f.getValue(), d = u == null ? void 0 : u.getValue();
        f != null && f.isBoolean() && (h = `${h}`.toLocaleUpperCase()), u != null && u.isBoolean() && (d = `${d}`.toLocaleUpperCase());
        const _ = f != null && f.isNull() ? "" : h != null ? h : "", C = u != null && u.isNull() ? "" : d != null ? d : "";
        return U.create(`${_}${C}`);
      });
    return n || g.create(m.VALUE);
  }
}
class TE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    if (e.isNull())
      return U.create("");
    let t = `${e.getValue()}`;
    e.isBoolean() && (t = t.toLocaleUpperCase());
    let r = "";
    for (let n = 0; n < t.length; n++) {
      const s = t.charCodeAt(n);
      s >= 33 && s <= 126 ? r += String.fromCharCode(s + 65248) : s === 32 ? r += "　" : r += t.charAt(n);
    }
    return U.create(r);
  }
}
class BE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
    A(this, "needsLocale", !0);
  }
  calculate(e, t) {
    let r = t != null ? t : R.create(2);
    r.isNull() && (r = R.create(2));
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, r, g.create(m.NA)), c = a.mapValue((l, f, h) => {
      const d = u.get(f, h);
      return l.isError() ? l : d.isError() ? d : this._handleSingleObject(l, d);
    });
    return n === 1 && s === 1 ? c.get(0, 0) : c;
  }
  _handleSingleObject(e, t) {
    const { isError: r, errorObject: n, variants: s } = I(e, t);
    if (r)
      return n;
    const [a, u] = s;
    let c = +a.getValue(), l = Math.trunc(+u.getValue());
    if (l > 127)
      return g.create(m.VALUE);
    l < 0 && (`${c}`.length < Math.abs(l) ? c = 0 : c = c < 0 ? -Ft(Math.abs(c), l) : Ft(c, l), l = 0);
    const f = jl(this.getLocale(), c, l);
    return U.create(f);
  }
}
class IE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, ue.create()), a = O(r, n, t, ue.create()), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      return c.isError() ? c : h.isError() ? h : this._handleSingleObject(c, h);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
  _handleSingleObject(e, t) {
    if (e.isNull() || t.isNull()) {
      const a = e.isNull() && t.isNull();
      return T.create(a);
    }
    let r = `${e.getValue()}`;
    e.isBoolean() && (r = r.toLocaleUpperCase());
    let n = `${t.getValue()}`;
    t.isBoolean() && (n = n.toLocaleUpperCase());
    const s = r === n;
    return T.create(s);
  }
}
const ni = (o, i) => Et.format(o, i, { throws: !1 }), De = (o) => {
  let i = `${o.getValue()}`;
  return o.isNull() && (i = ""), o.isBoolean() && (i = i.toLocaleUpperCase()), o.isNumber() && (o.getPattern() !== "" ? i = ni(o.getPattern(), +o.getValue()) : i = `${Da(+o.getValue())}`), i;
};
class FE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = r != null ? r : R.create(1), s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, n, g.create(m.NA)), f = u.mapValue((h, d, _) => {
      const C = c.get(d, _), E = l.get(d, _);
      return h.isError() ? h : C.isError() ? C : E.isError() ? E : this._handleSingleObject(h, C, E);
    });
    return s === 1 && a === 1 ? f.get(0, 0) : f;
  }
  _handleSingleObject(e, t, r) {
    const n = De(e), s = De(t), { isError: a, errorObject: u, variants: c } = I(r);
    if (a)
      return u;
    const [l] = c, f = Math.floor(+l.getValue());
    if (t.isNull() || f <= 0 || f > s.length)
      return g.create(m.VALUE);
    if (e.isNull() || n.length === 0)
      return R.create(f);
    const h = s.indexOf(n, f - 1);
    return h === -1 ? g.create(m.VALUE) : R.create(h + 1);
  }
}
function si(o) {
  let i = 0;
  for (let e = 0; e < o.length; e++)
    i += Jr(o, e);
  return i;
}
function Jr(o, i, e = "ltr") {
  return kE(o, i, e) > 255 ? 2 : 1;
}
function Zi(o) {
  return o >= 55296 && o <= 56319;
}
function zi(o) {
  return o >= 56320 && o <= 57343;
}
function Ji(o, i) {
  const e = (o & 1023) << 10, t = i & 1023;
  return e + t + 65536;
}
function kE(o, i, e = "ltr") {
  const t = o.charCodeAt(i);
  if (e === "ltr" && Zi(t) && i + 1 < o.length) {
    const r = o.charCodeAt(i + 1);
    if (zi(r))
      return Ji(t, r);
  }
  if (e === "rtl" && zi(t) && i - 1 >= 0) {
    const r = o.charCodeAt(i - 1);
    if (Zi(r))
      return Ji(r, t);
  }
  return t;
}
class $E extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = r != null ? r : R.create(1), s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, n, g.create(m.NA)), f = u.mapValue((h, d, _) => {
      const C = c.get(d, _), E = l.get(d, _);
      return h.isError() ? h : C.isError() ? C : E.isError() ? E : this._handleSingleObject(h, C, E);
    });
    return s === 1 && a === 1 ? f.get(0, 0) : f;
  }
  _handleSingleObject(e, t, r) {
    const n = De(e), s = De(t), { isError: a, errorObject: u, variants: c } = I(r);
    if (a)
      return u;
    const [l] = c, f = Math.floor(+l.getValue());
    if (t.isNull() || f <= 0 || f > s.length)
      return g.create(m.VALUE);
    if (e.isNull() || n.length === 0)
      return R.create(f);
    const h = s.indexOf(n, f - 1);
    if (h === -1)
      return g.create(m.VALUE);
    const d = si(s.substring(0, h)) + 1;
    return R.create(d);
  }
}
class YE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    let n = t != null ? t : R.create(2);
    n.isNull() && (n = R.create(2));
    let s = r != null ? r : T.create(!1);
    s.isNull() && (s = T.create(!1));
    const a = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1,
      s.isArray() ? s.getRowCount() : 1
    ), u = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1,
      s.isArray() ? s.getColumnCount() : 1
    ), c = O(a, u, e, g.create(m.NA)), l = O(a, u, n, g.create(m.NA)), f = O(a, u, s, g.create(m.NA)), h = c.mapValue((d, _, C) => {
      const E = l.get(_, C), b = f.get(_, C);
      return d.isError() ? d : E.isError() ? E : b.isError() ? b : this._handleSingleObject(d, E, b);
    });
    return a === 1 && u === 1 ? h.get(0, 0) : h;
  }
  _handleSingleObject(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = I(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a;
    let f = +u.getValue(), h = Math.trunc(+c.getValue());
    const d = +l.getValue();
    if (h > 127)
      return g.create(m.VALUE);
    h < 0 && (`${f}`.length < Math.abs(h) ? f = 0 : f = f < 0 ? -Ft(Math.abs(f), h) : Ft(f, h), h = 0);
    let _ = d ? "###0" : "#,##0";
    h > 0 && (_ += `.${"0".repeat(h)}`);
    const C = ni(_, f);
    return U.create(C);
  }
}
class HE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(1), n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, r, g.create(m.NA)), c = a.mapValue((l, f, h) => {
      const d = u.get(f, h);
      return l.isError() ? l : d.isError() ? d : this._handleSingleObject(l, d);
    });
    return n === 1 && s === 1 ? c.get(0, 0) : c;
  }
  _handleSingleObject(e, t) {
    const r = De(e), { isError: n, errorObject: s, variants: a } = I(t);
    if (n)
      return s;
    const [u] = a, c = Math.floor(+u.getValue());
    if (c < 0)
      return g.create(m.VALUE);
    if (e.isNull() || c === 0)
      return U.create("");
    if (c >= r.length)
      return U.create(r);
    const l = r.substring(0, c);
    return U.create(l);
  }
}
class QE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(1), n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, ue.create()), u = O(n, s, r, ue.create()), c = a.mapValue((l, f, h) => {
      const d = u.get(f, h);
      return l.isError() ? l : d.isError() ? d : this._handleSingleObject(l, d);
    });
    return n === 1 && s === 1 ? c.get(0, 0) : c;
  }
  _handleSingleObject(e, t) {
    const r = De(e), { isError: n, errorObject: s, variants: a } = I(t);
    if (n)
      return s;
    const [u] = a, c = Math.floor(+u.getValue());
    if (c < 0)
      return g.create(m.VALUE);
    if (e.isNull() || c === 0)
      return U.create("");
    let l = 0, f = 0, h = "";
    for (; f < c && l < r.length; )
      f += Jr(r, l), h += r.charAt(l), l++;
    return U.create(h);
  }
}
class GE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleText(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleText(e);
  }
  _handleSingleText(e) {
    if (e.isError())
      return e;
    const t = De(e);
    return R.create(t.length);
  }
}
class WE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleText(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleText(e);
  }
  _handleSingleText(e) {
    if (e.isError())
      return e;
    const t = De(e), r = si(t);
    return R.create(r);
  }
}
class qE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    if (e.isNull())
      return U.create("");
    const t = `${e.getValue()}`.toLocaleLowerCase();
    return U.create(t);
  }
}
class KE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    const n = De(e), { isError: s, errorObject: a, variants: u } = I(t, r);
    if (s)
      return a;
    const [c, l] = u, f = Math.floor(+c.getValue()), h = Math.floor(+l.getValue());
    if (f <= 0 || h < 0)
      return g.create(m.VALUE);
    if (e.isNull() || f > n.length || h === 0)
      return U.create("");
    const d = n.substring(f - 1, f - 1 + h);
    return U.create(d);
  }
}
class XE extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, t, g.create(m.NA)), c = O(n, s, r, g.create(m.NA)), l = a.mapValue((f, h, d) => {
      const _ = u.get(h, d), C = c.get(h, d);
      return f.isError() ? f : _.isError() ? _ : C.isError() ? C : this._handleSingleObject(f, _, C);
    });
    return n === 1 && s === 1 ? l.get(0, 0) : l;
  }
  _handleSingleObject(e, t, r) {
    let n = De(e);
    const { isError: s, errorObject: a, variants: u } = I(t, r);
    if (s)
      return a;
    const [c, l] = u, f = Math.floor(+c.getValue()), h = Math.floor(+l.getValue());
    if (f <= 0 || h < 0)
      return g.create(m.VALUE);
    if (e.isNull() || f > n.length || h === 0)
      return U.create("");
    n = n.substring(f - 1);
    let d = 0, _ = 0, C = "";
    for (; _ < h && d < n.length; )
      _ += Jr(n, d), C += n.charAt(d), d++;
    return U.create(C);
  }
}
const Pr = ["〇", "一", "二", "三", "四", "五", "六", "七", "八", "九"], an = ["零", "壹", "贰", "叁", "肆", "伍", "陆", "柒", "捌", "玖"], ZE = ["", "十", "百", "千"], zE = ["", "拾", "佰", "仟"], JE = ["", "万", "亿", "兆"];
class eb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.mapValue((c, l, f) => {
      const h = a.get(l, f);
      return c.isError() ? c : h.isError() ? h : this._handleSingleObject(c, h);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
  // eslint-disable-next-line
  _handleSingleObject(e, t) {
    const r = e.convertToNumberObjectValue();
    if (r.isError())
      return r;
    const n = t.convertToNumberObjectValue();
    if (n.isError())
      return n;
    let s = r.getValue();
    const a = Math.trunc(n.getValue());
    if (s < 0 || ![1, 2, 3].includes(a))
      return g.create(m.NUM);
    s = Math.round(s);
    const u = s.toString(), c = u.length;
    let l = "", f = !1;
    for (let h = 0; h < c; h++) {
      const d = Number(u[h]);
      if (c === 1 && d === 0) {
        l += a === 2 ? an[0] : Pr[0];
        break;
      }
      if (a === 3) {
        l += Pr[d];
        continue;
      }
      const _ = c - h - 1, C = _ % 4, E = Math.trunc(_ / 4);
      if (c >= 17 && E > 2) {
        if (l += a === 1 ? Pr[d] : an[d], E > 3)
          continue;
      } else
        d === 0 ? f = C !== 0 : (f && (l += a === 1 ? Pr[0] : an[0], f = !1), l += a === 1 ? Pr[d] + ZE[C] : an[d] + zE[C]);
      C === 0 && E > 0 && u.slice(Math.max(0, h - 3), h + 1) !== "0000" && (l += JE[E]);
    }
    return U.create(l);
  }
}
class tb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 3);
  }
  isArgumentsIgnoreNumberPattern() {
    return !0;
  }
  calculate(e, t, r) {
    const n = t != null ? t : U.create("."), s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1,
      r != null && r.isArray() ? r.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1,
      r != null && r.isArray() ? r.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, n, g.create(m.NA)), l = r ? O(s, a, r, g.create(m.NA)) : void 0, f = u.mapValue((h, d, _) => {
      const C = c.get(d, _), E = r ? l.get(d, _) : void 0;
      return h.isError() ? h : C.isError() ? C : E != null && E.isError() ? E : C.isNull() || E != null && E.isNull() ? g.create(m.VALUE) : h.isNull() ? R.create(0) : this._handleSingleObject(h, C, E);
    });
    return s === 1 && a === 1 ? f.get(0, 0) : f;
  }
  // eslint-disable-next-line
  _handleSingleObject(e, t, r) {
    let n = `${e.getValue()}`;
    n = n.replace(/\s+/g, "");
    let s = `${t.getValue()}`;
    t.isBoolean() && (s = s.toLocaleUpperCase()), s = s.charAt(0);
    let a;
    if (r && (a = `${r.getValue()}`, r.isBoolean() && (a = a.toLocaleUpperCase()), a = a.charAt(0), s === a))
      return g.create(m.VALUE);
    if (n.trim() === "")
      return R.create(0);
    if (!n.match(/^\s*[+-]?\s*(?:(?:\d+(?:\.\d*)?)|(?:\.\d+))(?:[eE][+-]?\d+)?[ \t]*/))
      return g.create(m.VALUE);
    const u = n.split(s);
    if (u.length > 2)
      return g.create(m.VALUE);
    let c = u[0].replace(/,/g, "");
    r && (c = c.split(a).join(""));
    let l = 0;
    if (u.length === 1) {
      s === "," && (c = c.replace(/\./g, ""));
      let f = 0;
      for (; c.endsWith("%"); )
        c = c.slice(0, -1), f++;
      f > 0 ? l = +c / 100 ** f : l = +c;
    } else {
      if (!q(c))
        return g.create(m.VALUE);
      let f = u[1], h = 0;
      for (; f.endsWith("%"); )
        f = f.slice(0, -1), h++;
      const d = `${c}.${f}`;
      if (!q(d))
        return g.create(m.VALUE);
      h > 0 ? l = +d / 100 ** h : l = +d;
    }
    return Number.isNaN(l) ? g.create(m.VALUE) : R.create(l);
  }
}
class rb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError() || e.isNull() || e.isBoolean() || e.isNumber())
      return e;
    const r = e.getValue().toLocaleString().toLocaleLowerCase().replace(/(^|\b|\W|\d|_)[a-z]/g, (n) => n.toLocaleUpperCase());
    return U.create(r);
  }
}
const Ku = () => [
  {
    type: 4,
    from: 48,
    to: 57
  }
], Xu = () => [
  {
    type: 7,
    value: 95
  },
  {
    type: 4,
    from: 97,
    to: 122
  },
  {
    type: 4,
    from: 65,
    to: 90
  },
  {
    type: 4,
    from: 48,
    to: 57
  }
], Zu = () => [
  {
    type: 7,
    value: 9
  },
  {
    type: 7,
    value: 10
  },
  {
    type: 7,
    value: 11
  },
  {
    type: 7,
    value: 12
  },
  {
    type: 7,
    value: 13
  },
  {
    type: 7,
    value: 32
  },
  {
    type: 7,
    value: 160
  },
  {
    type: 7,
    value: 5760
  },
  {
    type: 4,
    from: 8192,
    to: 8202
  },
  {
    type: 7,
    value: 8232
  },
  {
    type: 7,
    value: 8233
  },
  {
    type: 7,
    value: 8239
  },
  {
    type: 7,
    value: 8287
  },
  {
    type: 7,
    value: 12288
  },
  {
    type: 7,
    value: 65279
  }
], zu = () => ({
  type: 3,
  set: Xu(),
  not: !1
}), Ju = () => ({
  type: 3,
  set: Xu(),
  not: !0
}), ec = () => ({
  type: 3,
  set: Ku(),
  not: !1
}), tc = () => ({
  type: 3,
  set: Ku(),
  not: !0
}), rc = () => ({
  type: 3,
  set: Zu(),
  not: !1
}), nc = () => ({
  type: 3,
  set: Zu(),
  not: !0
}), nb = () => ({
  type: 3,
  set: [
    {
      type: 7,
      value: 10
    },
    {
      type: 7,
      value: 13
    },
    {
      type: 7,
      value: 8232
    },
    {
      type: 7,
      value: 8233
    }
  ],
  not: !0
});
function sb(o) {
  const i = {
    type: 0,
    stack: []
  };
  let e = i, t = i.stack;
  const r = [], n = [];
  let s = 0;
  const a = (f) => {
    throw new SyntaxError(`Invalid regular expression: /${o}/: Nothing to repeat at column ${f - 1}`);
  }, u = ib(o);
  let c = 0, l;
  for (; c < u.length; )
    switch (l = u[c++], l) {
      case "\\":
        if (c === u.length)
          throw new SyntaxError(`Invalid regular expression: /${o}/: \\ at end of pattern`);
        switch (l = u[c++], l) {
          case "b":
            t.push({
              type: 2,
              value: "b"
            });
            break;
          case "B":
            t.push({
              type: 2,
              value: "B"
            });
            break;
          case "w":
            t.push(zu());
            break;
          case "W":
            t.push(Ju());
            break;
          case "d":
            t.push(ec());
            break;
          case "D":
            t.push(tc());
            break;
          case "s":
            t.push(rc());
            break;
          case "S":
            t.push(nc());
            break;
          default:
            if (/\d/.test(l)) {
              for (; /\d/.test(u[c]) && c < u.length; )
                l += u[c++];
              const f = Number.parseInt(l, 10);
              t.push({
                type: 6,
                value: f
              }), n.push({
                reference: {
                  type: 6,
                  value: f
                },
                stack: t,
                index: t.length - 1
              });
            } else
              t.push({
                type: 7,
                value: l.charCodeAt(0)
              });
        }
        break;
      case "^":
        t.push({
          type: 2,
          value: "^"
        });
        break;
      case "$":
        t.push({
          type: 2,
          value: "$"
        });
        break;
      case "[": {
        const f = u[c] === "^";
        f && c++;
        const h = ab(u.slice(c), o);
        c += h[1], t.push({
          type: 3,
          set: h[0],
          not: f
        });
        break;
      }
      case ".":
        t.push(nb());
        break;
      case "(": {
        const f = {
          type: 1,
          stack: [],
          remember: !0
          // 默认情况下，捕获组是需要记住（捕获）的
        };
        if (u[c] === "?") {
          const h = u[c + 1];
          if (c += 2, h === "=")
            f.followedBy = !0;
          else if (h === "!")
            f.notFollowedBy = !0;
          else if (h !== ":")
            throw new SyntaxError(`Invalid regular expression: /${o}/: Invalid group, character '${h}' after '?' at column ${c - 1}`);
          f.remember = !1;
        } else
          s += 1;
        t.push(f), r.push(e), e = f, t = f.stack;
        break;
      }
      case ")":
        if (r.length === 0)
          throw new SyntaxError(`Invalid regular expression: /${o}/: Unmatched ) at column ${c - 1}`);
        e = r.pop(), t = e.options ? e.options[e.options.length - 1] : e.stack;
        break;
      case "|": {
        e.options || (e.options = [e.stack], delete e.stack);
        const f = [];
        e.options.push(f), t = f;
        break;
      }
      case "{": {
        const f = /^(\d+)(,(\d+)?)?\}/.exec(u.slice(c));
        if (f) {
          t.length === 0 && a(c);
          const h = Number.parseInt(f[1], 10), d = f[2] ? f[3] ? Number.parseInt(f[3], 10) : 1 / 0 : h;
          c += f[0].length, t.push({
            type: 5,
            min: h,
            max: d,
            value: t.pop()
          });
        } else
          t.push({
            // Assuming 123 is the character code for '{'
            type: 7,
            value: 123
          });
        break;
      }
      case "?":
        t.length === 0 && a(c), t.push({
          type: 5,
          min: 0,
          max: 1,
          value: t.pop()
        });
        break;
      case "+":
        t.length === 0 && a(c), t.push({
          type: 5,
          min: 1,
          max: 1 / 0,
          value: t.pop()
        });
        break;
      case "*":
        t.length === 0 && a(c), t.push({
          type: 5,
          min: 0,
          max: 1 / 0,
          value: t.pop()
        });
        break;
      default:
        t.push({
          type: 7,
          value: l.charCodeAt(0)
        });
    }
  if (r.length > 0)
    throw new SyntaxError(`Invalid regular expression: /${o}/: Unterminated group`);
  return ob(n, s), i;
}
function ab(o, i) {
  let e;
  const t = [], r = /\\(?:(w)|(d)|(s)|(W)|(D)|(S))|((?:(?:\\)(.)|([^\]\\]))-(((?:\\)])|(((?:\\)?([^\]])))))|(\])|(?:\\)?([^])/g;
  for (; (e = r.exec(o)) !== null; ) {
    let n = null;
    const [, s, a, u, c, l, f, h, d, _, C] = e;
    if (s || a || u || c || l || f ? n = (() => {
      let E = {
        type: 3,
        set: [],
        not: !1
      };
      return s ? E = zu() : a ? E = ec() : u ? E = rc() : c ? E = Ju() : l ? E = tc() : f && (E = nc()), E;
    })() : h && C ? n = {
      type: 4,
      from: (d || _).charCodeAt(0),
      to: C.charCodeAt(C.length - 1)
    } : e[16] && (n = {
      type: 7,
      value: e[16].charCodeAt(0)
    }), !n)
      return [t, r.lastIndex];
    t.push(n);
  }
  throw new SyntaxError(`Invalid regular expression: /${i}/: Unterminated character class`);
}
function ib(o) {
  return o.replace(
    /(\[\\b\])|(\\)?\\(?:u([A-F0-9]{4})|x([A-F0-9]{2})|c([@A-Z[\\\]^?])|([0tnvfr]))/g,
    (i, e, t, r, n, s, a) => {
      if (t)
        return i;
      let u;
      e ? u = 8 : r ? u = Number.parseInt(r, 16) : n ? u = Number.parseInt(n, 16) : s ? u = "@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^ ?".indexOf(s) : u = {
        0: 0,
        t: 9,
        n: 10,
        v: 11,
        f: 12,
        r: 13
      }[a];
      const c = String.fromCharCode(u);
      return /[[\]{}^$.|?*+()]/.test(c) ? `\\${c}` : c;
    }
  );
}
function ob(o, i) {
  for (const e of o.reverse()) {
    const t = e.reference.value;
    if (i < t) {
      e.reference.type = 7;
      const r = t.toString();
      if (e.reference.value = Number.parseInt(r, 8), !/^[0-7]+$/.test(r)) {
        let n = 0;
        for (; r[n] !== "8" && r[n] !== "9" && n < r.length; )
          n += 1;
        if (n === 0 ? (e.reference.value = r.charCodeAt(0), n += 1) : e.reference.value = Number.parseInt(r.slice(0, n), 8), r.length > n) {
          const s = e.stack.splice(e.index + 1);
          for (const a of r.slice(n))
            e.stack.push({
              type: 7,
              value: a.charCodeAt(0)
            });
          e.stack.push(...s);
        }
      }
    }
  }
}
function ai(o, i) {
  if (!ub(o))
    return {
      isError: !0,
      regExp: null
    };
  try {
    const e = new RegExp(o, i ? "ug" : "u");
    return cb(e) ? {
      isError: !1,
      regExp: e
    } : {
      isError: !0,
      regExp: null
    };
  } catch {
    return {
      isError: !0,
      regExp: null
    };
  }
}
function ub(o) {
  return !(/\(\?<=.*?\)/g.test(o) && !/\[.*?(\?<=.*?)\]/g.test(o) || /\(\?<!.*?\)/g.test(o) && !/\[.*?(\?<!.*?)\]/g.test(o));
}
function cb(o, i) {
  let t;
  Object.prototype.toString.call(o) === "[object RegExp]" ? t = o.source : o && typeof o != "string" && (t = `${o}`);
  let r;
  try {
    r = sb(t);
  } catch {
    return !1;
  }
  let n = 0;
  const s = (a, u) => {
    let c = u;
    if (a.type === 5 && (c++, n++, c > 1 || n > 25))
      return !1;
    if (a.options) {
      for (const f of a.options)
        if (!s({ stack: f }, c))
          return !1;
    }
    const l = a.stack || a.value && a.value.stack;
    if (!l)
      return !0;
    for (const f of l)
      if (!s(f, c))
        return !1;
    return !0;
  };
  return s(r, 0);
}
class lb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const { isError: r, errorObject: n, variants: s } = Wr(e, t);
    if (r)
      return n;
    const [a, u] = s;
    let c = a.getValue();
    a.isNull() && (c = ""), a.isBoolean() && (c = c ? "TRUE" : "FALSE"), c = `${c}`;
    let l = u.getValue();
    u.isNull() && (l = ""), u.isBoolean() && (l = l ? "TRUE" : "FALSE"), l = `${l}`;
    const { isError: f, regExp: h } = ai(l, !1);
    if (f)
      return g.create(m.REF);
    const d = c.match(h);
    if (d === null)
      return g.create(m.NA);
    if (d.length > 1) {
      const _ = d.slice(1).map((C) => U.create(C));
      return _.length > 1 ? W.create({
        calculateValueList: [_],
        rowCount: 1,
        columnCount: _.length,
        unitId: this.unitId,
        sheetId: this.subUnitId,
        row: this.row,
        column: this.column
      }) : _[0];
    }
    return U.create(d[0]);
  }
}
class fb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const { isError: r, errorObject: n, variants: s } = Wr(e, t);
    if (r)
      return n;
    const [a, u] = s;
    let c = a.getValue();
    a.isNull() && (c = ""), a.isBoolean() && (c = c ? "TRUE" : "FALSE"), c = `${c}`;
    let l = u.getValue();
    u.isNull() && (l = ""), u.isBoolean() && (l = l ? "TRUE" : "FALSE"), l = `${l}`;
    const { isError: f, regExp: h } = ai(l, !1);
    return f ? g.create(m.REF) : c.match(h) === null ? T.create(!1) : T.create(!0);
  }
}
class hb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const { isError: n, errorObject: s, variants: a } = Wr(e, t, r);
    if (n)
      return s;
    const [u, c, l] = a;
    let f = u.getValue();
    u.isNull() && (f = ""), u.isBoolean() && (f = f ? "TRUE" : "FALSE"), f = `${f}`;
    let h = c.getValue();
    c.isNull() && (h = ""), c.isBoolean() && (h = h ? "TRUE" : "FALSE"), h = `${h}`;
    let d = l.getValue();
    l.isNull() && (d = ""), l.isBoolean() && (d = d ? "TRUE" : "FALSE"), d = `${d}`;
    const { isError: _, regExp: C } = ai(h, !0);
    if (_)
      return g.create(m.REF);
    const E = f.replace(C, d);
    return U.create(E);
  }
}
class mb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = O(s, a, n, g.create(m.NA)), h = u.mapValue((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C), y = f.get(_, C);
      return d.isError() ? d : E.isError() ? E : b.isError() ? b : y.isError() ? y : this._handleSingleObject(d, E, b, y);
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
  _handleSingleObject(e, t, r, n) {
    const { isError: s, errorObject: a, variants: u } = I(t, r);
    if (s)
      return a;
    const [c, l] = u, f = Math.floor(+c.getValue()), h = Math.floor(+l.getValue());
    if (f <= 0 || h < 0)
      return g.create(m.VALUE);
    const d = De(e), _ = De(n), C = d.substring(0, f - 1) + _ + d.substring(f - 1 + h);
    return U.create(C);
  }
}
class gb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 4);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = O(s, a, n, g.create(m.NA)), h = u.mapValue((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C), y = f.get(_, C);
      return d.isError() ? d : E.isError() ? E : b.isError() ? b : y.isError() ? y : this._handleSingleObject(d, E, b, y);
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
  _handleSingleObject(e, t, r, n) {
    const { isError: s, errorObject: a, variants: u } = I(t, r);
    if (s)
      return a;
    const [c, l] = u, f = Math.floor(+c.getValue()), h = Math.floor(+l.getValue());
    if (f <= 0 || h < 0)
      return g.create(m.VALUE);
    let d = De(e);
    const _ = De(n);
    let C = d.substring(0, f - 1);
    d = d.substring(f - 1);
    let E = 0, b = 0;
    for (; b < h && E < d.length; )
      b += Jr(d, E), E++;
    return C += _ + d.substring(E), U.create(C);
  }
}
class db extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e, g.create(m.NA)), a = O(r, n, t, g.create(m.NA)), u = s.map((c, l, f) => {
      let h = a.get(l, f);
      if (c.isError())
        return c;
      let d = c.getValue();
      if (c.isNull() && (d = ""), c.isBoolean() && (d = d ? "TRUE" : "FALSE"), d += "", h.isString() && (h = h.convertToNumberObjectValue()), h.isError())
        return h;
      const _ = 32767, C = Math.floor(+h.getValue());
      if (C < 0 || C > _ / d.length)
        return g.create(m.VALUE);
      const E = d.repeat(C);
      return U.create(E);
    });
    return r === 1 && n === 1 ? u.get(0, 0) : u;
  }
}
class Cb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(1), n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, r, g.create(m.NA)), c = a.mapValue((l, f, h) => {
      const d = u.get(f, h);
      return l.isError() ? l : d.isError() ? d : this._handleSingleObject(l, d);
    });
    return n === 1 && s === 1 ? c.get(0, 0) : c;
  }
  _handleSingleObject(e, t) {
    const r = De(e), { isError: n, errorObject: s, variants: a } = I(t);
    if (n)
      return s;
    const [u] = a, c = Math.floor(+u.getValue());
    if (c < 0)
      return g.create(m.VALUE);
    if (e.isNull() || c === 0)
      return U.create("");
    if (c >= r.length)
      return U.create(r);
    const l = r.substring(r.length - c);
    return U.create(l);
  }
}
class _b extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(1), n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, r, g.create(m.NA)), c = a.mapValue((l, f, h) => {
      const d = u.get(f, h);
      return l.isError() ? l : d.isError() ? d : this._handleSingleObject(l, d);
    });
    return n === 1 && s === 1 ? c.get(0, 0) : c;
  }
  _handleSingleObject(e, t) {
    const r = De(e), { isError: n, errorObject: s, variants: a } = I(t);
    if (n)
      return s;
    const [u] = a, c = Math.floor(+u.getValue());
    if (c < 0)
      return g.create(m.VALUE);
    if (e.isNull() || c === 0)
      return U.create("");
    let l = r.length - 1, f = 0, h = "";
    for (; f < c && l >= 0; )
      f += Jr(r, l, "rtl"), h = r.charAt(l) + h, l--;
    return U.create(h);
  }
}
class Ab extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = r != null ? r : R.create(1), s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, n, g.create(m.NA)), f = u.mapValue((h, d, _) => {
      const C = c.get(d, _), E = l.get(d, _);
      return h.isError() ? h : C.isError() ? C : E.isError() ? E : this._handleSingleObject(h, C, E);
    });
    return s === 1 && a === 1 ? f.get(0, 0) : f;
  }
  _handleSingleObject(e, t, r) {
    const n = De(e).toLocaleUpperCase(), s = De(t).toLocaleUpperCase(), { isError: a, errorObject: u, variants: c } = I(r);
    if (a)
      return u;
    const [l] = c, f = Math.floor(+l.getValue());
    if (t.isNull() || f <= 0 || f > s.length)
      return g.create(m.VALUE);
    if (e.isNull() || n.length === 0)
      return R.create(f);
    const h = s.indexOf(n, f - 1);
    return h === -1 ? g.create(m.VALUE) : R.create(h + 1);
  }
}
class Eb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 3);
  }
  calculate(e, t, r) {
    const n = r != null ? r : R.create(1), s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, n, g.create(m.NA)), f = u.mapValue((h, d, _) => {
      const C = c.get(d, _), E = l.get(d, _);
      return h.isError() ? h : C.isError() ? C : E.isError() ? E : this._handleSingleObject(h, C, E);
    });
    return s === 1 && a === 1 ? f.get(0, 0) : f;
  }
  _handleSingleObject(e, t, r) {
    const n = De(e).toLocaleUpperCase(), s = De(t).toLocaleUpperCase(), { isError: a, errorObject: u, variants: c } = I(r);
    if (a)
      return u;
    const [l] = c, f = Math.floor(+l.getValue());
    if (t.isNull() || f <= 0 || f > s.length)
      return g.create(m.VALUE);
    if (e.isNull() || n.length === 0)
      return R.create(f);
    const h = s.indexOf(n, f - 1);
    if (h === -1)
      return g.create(m.VALUE);
    const d = si(s.substring(0, h)) + 1;
    return R.create(d);
  }
}
class bb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 4);
  }
  calculate(e, t, r, n) {
    const s = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1,
      n != null && n.isArray() ? n.getRowCount() : 1
    ), a = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1,
      n != null && n.isArray() ? n.getColumnCount() : 1
    ), u = O(s, a, e, g.create(m.NA)), c = O(s, a, t, g.create(m.NA)), l = O(s, a, r, g.create(m.NA)), f = n ? O(s, a, n, g.create(m.NA)) : void 0, h = u.mapValue((d, _, C) => {
      const E = c.get(_, C), b = l.get(_, C);
      let y = n ? f.get(_, C) : void 0;
      return d.isError() ? d : E.isError() ? E : b.isError() ? b : y != null && y.isError() ? y : y != null && y.isNull() || y != null && y.isBoolean() ? g.create(m.VALUE) : (y != null && y.isString() && (y = y.convertToNumberObjectValue()), y != null && y.isError() ? y : this._handleSingleObject(d, E, b, y));
    });
    return s === 1 && a === 1 ? h.get(0, 0) : h;
  }
  _handleSingleObject(e, t, r, n) {
    const s = this._getObjectString(e), a = this._getObjectString(t), u = this._getObjectString(r), c = n ? Math.floor(+n.getValue()) : void 0;
    if (n && c <= 0)
      return g.create(m.VALUE);
    if (a === "")
      return U.create(s);
    let l = "", f = 0;
    for (let h = 0; h < s.length; h++) {
      const d = s.substr(h, a.length);
      if (d === a || d.length === a.length && d.trim() === a.trim()) {
        if (f++, f === c) {
          l = s.substr(0, h) + u + s.substr(h + a.length);
          break;
        }
        c === void 0 && (l += u), h += a.length - 1;
      } else
        c === void 0 && (l += s[h]);
    }
    return c && f < c && (l = s), U.create(l);
  }
  _getObjectString(e) {
    let t = `${e.getValue()}`;
    return e.isNull() && (t = ""), e.isBoolean() && (t = t.toLocaleUpperCase()), t;
  }
}
class yb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
    A(this, "needsReferenceObject", !0);
  }
  calculate(e) {
    let t = e;
    return e.isReferenceObject() && (t = e.toArrayValueObject().get(0, 0)), t.isArray() ? t.mapValue((r) => this._handleSingleObject(r)) : this._handleSingleObject(t);
  }
  _handleSingleObject(e) {
    return e.isError() ? e : e.isNull() || e.isBoolean() || e.isNumber() ? U.create("") : e;
  }
}
class Rb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 2);
  }
  isArgumentsIgnoreNumberPattern() {
    return !0;
  }
  calculate(e, t) {
    if (e.isError())
      return e;
    if (t.isError())
      return t;
    const r = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      t.isArray() ? t.getRowCount() : 1
    ), n = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      t.isArray() ? t.getColumnCount() : 1
    ), s = O(r, n, e), a = O(r, n, t), u = s.map((c, l, f) => {
      if (c.isError())
        return c;
      let h = a.get(l, f) || U.create(" ");
      if (h.isError())
        return h;
      if (h.isBoolean())
        return g.create(m.VALUE);
      if (c.isBoolean())
        return c;
      let d = c.getValue();
      if (c.isNull() && (d = 0), c.isString()) {
        if (!q(d))
          return c;
        d = Number(d);
      }
      h.isNull() && (h = U.create(" "));
      const _ = `${h.getValue()}`, C = ni(_, d);
      return U.create(_ === " " ? C.trimEnd() : C);
    });
    return u.getRowCount() === 1 && u.getColumnCount() === 1 ? u.get(0, 0) : u;
  }
}
class pb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 6);
  }
  calculate(e, t, r, n, s, a) {
    let u = !1, c = r != null ? r : R.create(1);
    c.isNull() && (u = !0, c = R.create(1));
    const l = !n, f = n != null ? n : R.create(0), h = s != null ? s : R.create(0), d = a != null ? a : g.create(m.NA), _ = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1,
      f.isArray() ? f.getRowCount() : 1,
      h.isArray() ? h.getRowCount() : 1,
      d.isArray() ? d.getRowCount() : 1
    ), C = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1,
      f.isArray() ? f.getColumnCount() : 1,
      h.isArray() ? h.getColumnCount() : 1,
      d.isArray() ? d.getColumnCount() : 1
    ), E = O(_, C, e, g.create(m.NA)), b = O(_, C, c, g.create(m.NA)), y = O(_, C, f, g.create(m.NA)), p = O(_, C, h, g.create(m.NA)), N = O(_, C, d, g.create(m.NA)), w = this._getResultArray(E, t, b, y, p, N, u, l);
    return _ === 1 && C === 1 ? w.get(0, 0) : w;
  }
  _getResultArray(e, t, r, n, s, a, u, c) {
    return e.map((f, h, d) => {
      const _ = r.get(h, d), C = n.get(h, d), E = s.get(h, d), b = a.get(h, d), y = this._checkVariantsError(f, _, C, E);
      if (y.isError())
        return y;
      const p = this._getStringValue(f), N = this._getDelimiterValue(t);
      if (N instanceof g)
        return N;
      const w = this._getVariantsNumberFloorValue(_, C, E);
      if (w instanceof g)
        return w;
      const [S, M, D] = w;
      return S === 0 || M < 0 || M > 1 || D < 0 || D > 1 ? g.create(m.VALUE) : N.includes("") ? S > 0 ? U.create(p) : U.create("") : !u && Math.abs(S) > p.length ? g.create(m.VALUE) : N.every((L) => L.length > p.length) ? g.create(m.NA) : this._getResult(p, N, S, M, D, b, c);
    });
  }
  _checkVariantsError(...e) {
    for (let t = 0; t < e.length; t++) {
      const r = e[t];
      if (r.isError())
        return r;
    }
    return T.create(!0);
  }
  _getStringValue(e) {
    let t = `${e.getValue()}`;
    return e.isNull() && (t = ""), e.isBoolean() && (t = t.toLocaleUpperCase()), t;
  }
  _getDelimiterValue(e) {
    const t = [];
    if (e.isArray()) {
      let r = !1, n = g.create(m.VALUE);
      if (e.iterator((s) => {
        const a = s;
        if (a.isError())
          return r = !0, n = a, !1;
        t.push(this._getStringValue(a));
      }), r)
        return n;
    } else {
      if (e.isError())
        return e;
      t.push(this._getStringValue(e));
    }
    return t;
  }
  _getVariantsNumberFloorValue(...e) {
    const t = [];
    for (let r = 0; r < e.length; r++) {
      let n = e[r];
      if (n.isString() && (n = n.convertToNumberObjectValue()), n.isError())
        return n;
      const s = Math.floor(+n.getValue());
      t.push(s);
    }
    return t;
  }
  _getResult(e, t, r, n, s, a, u) {
    let c = n ? e.toLocaleLowerCase() : e;
    const l = n ? t.map((C) => C.toLocaleLowerCase()) : t;
    let f = 0, h = 0, d = 0;
    for (let C = 0; C < Math.abs(r); C++)
      if (r < 0) {
        const E = l.map((b) => ({
          index: c.lastIndexOf(b),
          length: b.length
        })).filter((b) => b.index !== -1).sort((b, y) => y.index - b.index)[0];
        if (!E)
          break;
        f = E.index, c = c.substr(0, E.index), d = E.length, h++;
      } else {
        const E = l.map((b) => ({
          index: c.indexOf(b),
          length: b.length
        })).filter((b) => b.index !== -1).sort((b, y) => b.index - y.index)[0];
        if (!E)
          break;
        f += E.index + d, c = c.substr(E.index + E.length), d = E.length, h++;
      }
    if (h && h < Math.abs(r) && u)
      return g.create(m.NA);
    if (!h || h < Math.abs(r))
      return s ? r > 0 ? U.create("") : U.create(e) : a;
    const _ = e.substr(f + d);
    return U.create(_);
  }
}
class Nb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 6);
  }
  calculate(e, t, r, n, s, a) {
    let u = !1, c = r != null ? r : R.create(1);
    c.isNull() && (u = !0, c = R.create(1));
    const l = !n, f = n != null ? n : R.create(0), h = s != null ? s : R.create(0), d = a != null ? a : g.create(m.NA), _ = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1,
      f.isArray() ? f.getRowCount() : 1,
      h.isArray() ? h.getRowCount() : 1,
      d.isArray() ? d.getRowCount() : 1
    ), C = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1,
      f.isArray() ? f.getColumnCount() : 1,
      h.isArray() ? h.getColumnCount() : 1,
      d.isArray() ? d.getColumnCount() : 1
    ), E = O(_, C, e, g.create(m.NA)), b = O(_, C, c, g.create(m.NA)), y = O(_, C, f, g.create(m.NA)), p = O(_, C, h, g.create(m.NA)), N = O(_, C, d, g.create(m.NA)), w = this._getResultArray(E, t, b, y, p, N, u, l);
    return _ === 1 && C === 1 ? w.get(0, 0) : w;
  }
  _getResultArray(e, t, r, n, s, a, u, c) {
    return e.map((f, h, d) => {
      const _ = r.get(h, d), C = n.get(h, d), E = s.get(h, d), b = a.get(h, d), y = this._checkVariantsError(f, _, C, E);
      if (y.isError())
        return y;
      const p = this._getStringValue(f), N = this._getDelimiterValue(t);
      if (N instanceof g)
        return N;
      const w = this._getVariantsNumberFloorValue(_, C, E);
      if (w instanceof g)
        return w;
      const [S, M, D] = w;
      return S === 0 || M < 0 || M > 1 || D < 0 || D > 1 ? g.create(m.VALUE) : N.includes("") ? S > 0 ? U.create("") : U.create(p) : !u && Math.abs(S) > p.length ? g.create(m.VALUE) : N.every((L) => L.length > p.length) ? g.create(m.NA) : this._getResult(p, N, S, M, D, b, c);
    });
  }
  _checkVariantsError(...e) {
    for (let t = 0; t < e.length; t++) {
      const r = e[t];
      if (r.isError())
        return r;
    }
    return T.create(!0);
  }
  _getStringValue(e) {
    let t = `${e.getValue()}`;
    return e.isNull() && (t = ""), e.isBoolean() && (t = t.toLocaleUpperCase()), t;
  }
  _getDelimiterValue(e) {
    const t = [];
    if (e.isArray()) {
      let r = !1, n = g.create(m.VALUE);
      if (e.iterator((s) => {
        const a = s;
        if (a.isError())
          return r = !0, n = a, !1;
        t.push(this._getStringValue(a));
      }), r)
        return n;
    } else {
      if (e.isError())
        return e;
      t.push(this._getStringValue(e));
    }
    return t;
  }
  _getVariantsNumberFloorValue(...e) {
    const t = [];
    for (let r = 0; r < e.length; r++) {
      let n = e[r];
      if (n.isString() && (n = n.convertToNumberObjectValue()), n.isError())
        return n;
      const s = Math.floor(+n.getValue());
      t.push(s);
    }
    return t;
  }
  _getResult(e, t, r, n, s, a, u) {
    let c = n ? e.toLocaleLowerCase() : e;
    const l = n ? t.map((C) => C.toLocaleLowerCase()) : t;
    let f = 0, h = 0, d = 0;
    for (let C = 0; C < Math.abs(r); C++)
      if (r < 0) {
        const E = l.map((b) => ({
          index: c.lastIndexOf(b),
          length: b.length
        })).filter((b) => b.index !== -1).sort((b, y) => y.index - b.index)[0];
        if (!E)
          break;
        f = E.index, c = c.substr(0, E.index), h++;
      } else {
        const E = l.map((b) => ({
          index: c.indexOf(b),
          length: b.length
        })).filter((b) => b.index !== -1).sort((b, y) => b.index - y.index)[0];
        if (!E)
          break;
        f += E.index + d, c = c.substr(E.index + E.length), d = E.length, h++;
      }
    if (h && h < Math.abs(r) && u)
      return g.create(m.NA);
    if (!h || h < Math.abs(r))
      return s ? r > 0 ? U.create(e) : U.create("") : a;
    const _ = e.substr(0, f);
    return U.create(_);
  }
}
class Vb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 3);
    A(this, "maxParams", 255);
  }
  calculate(e, t, ...r) {
    const n = this._getDelimiterValues(e), s = this._getTextValues(r);
    if (t.isArray()) {
      const u = t.mapValue((c) => this._handleSingleObject(n, c, s));
      return u.getRowCount() === 1 && u.getColumnCount() === 1 ? u.get(0, 0) : u;
    }
    const a = t;
    if (a.isString()) {
      const u = `${a.getValue()}`.toLocaleUpperCase();
      if (u === "TRUE")
        return this._handleSingleObject(n, T.create(!0), s);
      if (u === "FALSE")
        return this._handleSingleObject(n, T.create(!1), s);
    }
    return this._handleSingleObject(n, t, s);
  }
  _handleSingleObject(e, t, r) {
    if (e instanceof g)
      return e;
    const { isError: n, errorObject: s, variants: a } = I(t);
    if (n)
      return s;
    if (r instanceof g)
      return r;
    const [u] = a, c = +u.getValue();
    let l = r;
    c && (l = r.filter((h) => h !== null));
    let f = "";
    for (let h = 0; h < l.length; h++)
      l[h] !== null && (f += l[h]), h < l.length - 1 && (f += e[h % e.length]);
    return U.create(f);
  }
  _getDelimiterValues(e) {
    const t = [], r = e.isArray() ? e.getRowCount() : 1, n = e.isArray() ? e.getColumnCount() : 1;
    for (let s = 0; s < r; s++)
      for (let a = 0; a < n; a++) {
        const u = e.isArray() ? e.get(s, a) : e;
        if (u.isError())
          return u;
        let c = `${u.getValue()}`;
        u.isNull() && (c = ""), u.isBoolean() && (c = c.toLocaleUpperCase()), t.push(c);
      }
    return t;
  }
  _getTextValues(e) {
    const t = [];
    for (const r of e) {
      const n = r.isArray() ? r.getRowCount() : 1, s = r.isArray() ? r.getColumnCount() : 1;
      for (let a = 0; a < n; a++)
        for (let u = 0; u < s; u++) {
          const c = r.isArray() ? r.get(a, u) : r;
          if (c.isError())
            return c;
          if (c.isNull()) {
            t.push(null);
            continue;
          }
          let l = `${c.getValue()}`;
          c.isBoolean() && (l = l.toLocaleUpperCase()), t.push(l);
        }
    }
    return t;
  }
}
class Ob extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 2);
    A(this, "maxParams", 6);
  }
  calculate(e, t, r, n, s, a) {
    let u = r != null ? r : U.create("\\s");
    const c = n != null ? n : R.create(0), l = s != null ? s : R.create(0), f = a != null ? a : U.create(m.NA), { _variant: h, values: d } = this._getStringValues(t), { _variant: _, values: C } = this._getStringValues(u, !1);
    u = _;
    const E = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      c.isArray() ? c.getRowCount() : 1,
      l.isArray() ? l.getRowCount() : 1
    ), b = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      c.isArray() ? c.getColumnCount() : 1,
      l.isArray() ? l.getColumnCount() : 1
    ), y = O(E, b, e, g.create(m.NA)), p = O(E, b, c, g.create(m.NA)), N = O(E, b, l, g.create(m.NA)), w = this._getResultArray(
      y,
      h,
      u,
      p,
      N,
      f,
      d,
      C
    );
    return E === 1 && b === 1 ? w.get(0, 0) : w.map((S) => S.get(0, 0));
  }
  _getStringValues(e, t = !0) {
    let r = e;
    const n = [];
    if (r.isArray())
      r.iterator((s) => {
        if (s != null && s.isError())
          return r = s, !1;
        if (s != null && s.isNull() && t)
          return r = g.create(m.VALUE), !1;
        const a = this._getRegExpStringValue(s);
        if (a === "")
          return r = g.create(m.VALUE), !1;
        n.push(a);
      });
    else {
      r.isNull() && t && (r = g.create(m.VALUE));
      const s = this._getRegExpStringValue(r);
      s === "" && (r = g.create(m.VALUE)), n.push(s);
    }
    return {
      _variant: r,
      values: n
    };
  }
  _getResultArray(e, t, r, n, s, a, u, c) {
    return e.map((f, h, d) => {
      let _ = n.get(h, d), C = s.get(h, d);
      const E = this._checkVariantsError(f, t, r, _, C);
      if (E.isError())
        return E;
      if (f.isNull())
        return g.create(m.VALUE);
      let b = a;
      if (b.isArray()) {
        const S = b.getRowCount(), M = b.getColumnCount();
        if (S > 1 || M > 1)
          return g.create(m.VALUE);
        b = b.get(0, 0);
      }
      let y = `${f.getValue()}`;
      if (f.isBoolean() && (y = y.toLocaleUpperCase()), _.isString() && (_ = _.convertToNumberObjectValue(), _.isError()))
        return _;
      const p = Math.floor(+_.getValue());
      if (C.isString() && (C = C.convertToNumberObjectValue(), C.isError()))
        return C;
      const N = Math.floor(+C.getValue());
      if (N < 0 || N > 1)
        return g.create(m.VALUE);
      let w = `${b.getValue()}`;
      return b.isBoolean() && (w = w.toLocaleUpperCase()), this._getResult(y, u, c, p, N, w);
    });
  }
  _getResult(e, t, r, n, s, a) {
    const u = new RegExp(r.join("|"), `g${s ? "i" : ""}`), c = new RegExp(t.join("|"), `g${s ? "i" : ""}`), l = e.split(u);
    let f = 1, h = l.map((d) => {
      let _ = d.split(c);
      return n && (_ = _.filter((C) => C !== "")), f = Math.max(f, _.length), _;
    });
    return h = h.map((d) => {
      let _ = d;
      return _.length < f && (_ = _.concat(new Array(f - _.length).fill(a))), _;
    }), W.createByArray(h);
  }
  _checkVariantsError(...e) {
    for (let t = 0; t < e.length; t++) {
      const r = e[t];
      if (r.isError())
        return r;
    }
    return T.create(!0);
  }
  _getRegExpStringValue(e) {
    let t = e.getValue();
    return e.isNull() && (t = "\\s"), e.isBoolean() && (t = t ? "TRUE" : "FALSE"), t += "", this._escapeRegExp(t);
  }
  _escapeRegExp(e) {
    return e.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  }
}
class wb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    if (e.isNull())
      return U.create("");
    let t = `${e.getValue()}`;
    return e.isBoolean() && (t = t.toLocaleUpperCase()), t = t.trim().replace(/\s+/g, " "), U.create(t);
  }
}
const Sb = Object.values(Te).filter((o) => [
  Te.TABLE_START,
  Te.TABLE_ROW_START,
  Te.TABLE_CELL_START,
  Te.TABLE_CELL_END,
  Te.TABLE_ROW_END,
  Te.TABLE_END,
  Te.CUSTOM_BLOCK
].includes(o));
class Mb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    const { isError: t, errorObject: r, variants: n } = I(e);
    if (t)
      return r;
    const [s] = n, a = Math.floor(+s.getValue());
    if (a < 1 || a > 1114111)
      return g.create(m.VALUE);
    if (a === 1114111 || a === 1114110)
      return g.create(m.NA);
    let u = String.fromCharCode(a);
    return Sb.some((c) => c === u) && (u = ""), U.create(u);
  }
}
class Db extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    if (e.isNull())
      return g.create(m.VALUE);
    let t = e.getValue().toLocaleString();
    e.isBoolean() && (t = t.toLocaleUpperCase());
    const r = t.charCodeAt(0);
    return R.create(r);
  }
}
class Lb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    if (e.isNull())
      return U.create("");
    const t = `${e.getValue()}`.toLocaleUpperCase();
    return U.create(t);
  }
}
class xb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    if (e.isBoolean())
      return g.create(m.VALUE);
    let t = e;
    if (e.isString() && (t = t.convertToNumberObjectValue(), t.isError()))
      return t;
    const r = +t.getValue();
    return R.create(r);
  }
}
class Pb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 2);
  }
  calculate(e, t) {
    const r = t != null ? t : R.create(0), n = Math.max(
      e.isArray() ? e.getRowCount() : 1,
      r.isArray() ? r.getRowCount() : 1
    ), s = Math.max(
      e.isArray() ? e.getColumnCount() : 1,
      r.isArray() ? r.getColumnCount() : 1
    ), a = O(n, s, e, g.create(m.NA)), u = O(n, s, r, g.create(m.NA)), c = a.mapValue((l, f, h) => {
      const d = u.get(f, h);
      return l.isError() ? l : d.isError() ? d : this._handleSingleObject(l, d);
    });
    return n === 1 && s === 1 ? c.get(0, 0) : c;
  }
  _handleSingleObject(e, t) {
    const { isError: r, errorObject: n, variants: s } = I(t);
    if (r)
      return n;
    const [a] = s, u = Math.floor(+a.getValue());
    if (u < 0 || u > 1)
      return g.create(m.VALUE);
    if (e.isNull())
      return U.create("");
    if (e.isBoolean())
      return e;
    if (e.isNumber())
      return R.create(e.getValue());
    const c = u ? `"${e.getValue()}"` : `${e.getValue()}`;
    return U.create(c);
  }
}
const jb = [
  [ME, se.ASC],
  [SE, se.ARRAYTOTEXT],
  [DE, se.BAHTTEXT],
  [xE, se.CHAR],
  [PE, se.CLEAN],
  [jE, se.CODE],
  [UE, se.CONCAT],
  [vE, se.CONCATENATE],
  [TE, se.DBCS],
  [BE, se.DOLLAR],
  [IE, se.EXACT],
  [FE, se.FIND],
  [$E, se.FINDB],
  [YE, se.FIXED],
  [HE, se.LEFT],
  [QE, se.LEFTB],
  [GE, se.LEN],
  [WE, se.LENB],
  [qE, se.LOWER],
  [KE, se.MID],
  [XE, se.MIDB],
  [eb, se.NUMBERSTRING],
  [tb, se.NUMBERVALUE],
  [lb, se.REGEXEXTRACT],
  [fb, se.REGEXMATCH],
  [hb, se.REGEXREPLACE],
  [rb, se.PROPER],
  [mb, se.REPLACE],
  [gb, se.REPLACEB],
  [db, se.REPT],
  [Cb, se.RIGHT],
  [_b, se.RIGHTB],
  [Ab, se.SEARCH],
  [Eb, se.SEARCHB],
  [bb, se.SUBSTITUTE],
  [yb, se.T],
  [Rb, se.TEXT],
  [pb, se.TEXTAFTER],
  [Nb, se.TEXTBEFORE],
  [Vb, se.TEXTJOIN],
  [Ob, se.TEXTSPLIT],
  [wb, se.TRIM],
  [Mb, se.UNICHAR],
  [Db, se.UNICODE],
  [Lb, se.UPPER],
  [xb, se.VALUE],
  [Pb, se.VALUETOTEXT]
], Ub = [];
var vb = /* @__PURE__ */ ((o) => o)(vb || {});
class Tb extends Ve {
  constructor() {
    super(...arguments);
    // 18.5.1.2 table (Table)
    A(this, "_tableMap", /* @__PURE__ */ new Map());
    // 18.5.1.2 table (Table) for I18N
    A(this, "_tableOptionMap", /* @__PURE__ */ new Map());
  }
  dispose() {
    super.dispose(), this._tableMap.clear(), this._tableOptionMap.clear();
  }
  remove(e, t) {
    var r;
    (r = this._tableMap.get(e)) == null || r.delete(t);
  }
  getTableMap(e) {
    return this._tableMap.get(e);
  }
  getTableOptionMap() {
    return this._tableOptionMap;
  }
  registerTable(e, t, r) {
    var n;
    this._tableMap.get(e) == null && this._tableMap.set(e, /* @__PURE__ */ new Map()), (n = this._tableMap.get(e)) == null || n.set(t, r);
  }
  registerTableOptionMap(e, t) {
    this._tableOptionMap.set(e, t);
  }
}
const sc = ze("univer.formula.super-table.service");
function eo(o) {
  return o.map((i) => i.map((e) => typeof e == "number" ? Da(e) : e));
}
function _y(o, i = !1) {
  if (o.isReferenceObject()) {
    const e = o.toArrayValueObject().toValue();
    return i ? eo(e) : e;
  } else if (o.isArray()) {
    const e = o.toValue();
    return i ? eo(e) : e;
  } else if (o.isNumber()) {
    const e = o.getValue();
    return i ? Da(e) : e;
  }
  return o.getValue();
}
class Bb extends V {
  constructor() {
    super(...arguments);
    A(this, "minParams", 1);
    A(this, "maxParams", 1);
  }
  calculate(e) {
    if (e.isArray()) {
      const t = e.mapValue((r) => this._handleSingleObject(r));
      return t.getRowCount() === 1 && t.getColumnCount() === 1 ? t.get(0, 0) : t;
    }
    return this._handleSingleObject(e);
  }
  _handleSingleObject(e) {
    if (e.isError())
      return e;
    const t = De(e), r = encodeURIComponent(t).replace(/[!~'()*]/g, (n) => `%${n.charCodeAt(0).toString(16).toLocaleUpperCase()}`);
    return U.create(r);
  }
}
var ac = /* @__PURE__ */ ((o) => (o.ENCODEURL = "ENCODEURL", o.FILTERXML = "FILTERXML", o.WEBSERVICE = "WEBSERVICE", o))(ac || {});
const Ib = [
  [Bb, ac.ENCODEURL]
];
class ic extends Ve {
  constructor() {
    super(...arguments);
    A(this, "_allSubjects", []);
    A(this, "_computingStatus$", new uo(!0));
    A(this, "computingStatus$", this._computingStatus$.pipe(co()));
    A(this, "_computingSubscription");
  }
  get computingStatus() {
    return this._computingStatus$.getValue();
  }
  dispose() {
    var e;
    super.dispose(), (e = this._computingSubscription) == null || e.unsubscribe(), this._computingStatus$.next(!0), this._computingStatus$.complete(), this._allSubjects.forEach((t) => {
      t.complete();
    });
  }
  pushComputingStatusSubject(e) {
    return this._allSubjects.push(e), this._updateComputingObservable(), {
      dispose: () => {
        const t = this._allSubjects.indexOf(e);
        t !== -1 && this._allSubjects.splice(t, 1), this._updateComputingObservable();
      }
    };
  }
  _updateComputingObservable() {
    var e;
    if ((e = this._computingSubscription) == null || e.unsubscribe(), this._allSubjects.length === 0) {
      this._computingStatus$.next(!0);
      return;
    }
    this._computingSubscription = Oc(this._allSubjects).pipe(wc((t) => t.every((r) => r))).subscribe((t) => this._computingStatus$.next(t));
  }
}
var Fb = Object.getOwnPropertyDescriptor, kb = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? Fb(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, to = (o, i) => (e, t) => i(e, t, o);
let ga = class extends Ve {
  constructor(i, e) {
    super();
    A(this, "_computingCompleted$", new Sc((i) => {
      this._commandService.onCommandExecuted((e) => {
        if (e.id !== yn.id) return;
        const t = e.params;
        if (t.functionsExecutedState !== void 0)
          return i.next(t.functionsExecutedState === mr.SUCCESS);
        if (t.stageInfo)
          return i.next(t.stageInfo.stage === $e.IDLE);
      });
    }).pipe(
      co(),
      Mc()
    ));
    this._commandService = i, this._globalComputingSrv = e;
    const t = new Rc(), r = new uo(!0);
    t.add(this._globalComputingSrv.pushComputingStatusSubject(r)), t.add(this._computingCompleted$.subscribe((n) => r.next(n))), t.add(() => r.complete()), this.disposeWithMe(t);
  }
};
ga = kb([
  to(0, cr),
  to(1, ye(ic))
], ga);
var $b = Object.getOwnPropertyDescriptor, Yb = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? $b(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, on = (o, i) => (e, t) => i(e, t, o);
let Fn = class extends Ve {
  constructor(o, i, e, t) {
    super(), this._commandService = o, this._functionService = i, this._configService = e, this._dataSyncPrimaryController = t, this._initialize();
  }
  _initialize() {
    this._registerCommands(), this._registerFunctions();
  }
  _registerCommands() {
    [
      No,
      Us,
      yo,
      Ro,
      yn,
      po,
      Ea,
      bo,
      ba,
      ya,
      Ra,
      pa,
      fl,
      hl,
      Vo,
      zc
    ].forEach((o) => {
      var i;
      this._commandService.registerCommand(o), (i = this._dataSyncPrimaryController) == null || i.registerSyncingMutations(o);
    });
  }
  _registerFunctions() {
    var e;
    const o = this._configService.getConfig(Oa), i = [
      ...mm,
      ...Wm,
      ...qm,
      ...cg,
      ...Wg,
      ...Z1,
      ...rd,
      ...Od,
      ...Gd,
      ...SC,
      ...eA,
      ...oA,
      ...wE,
      ...jb,
      ...Ub,
      ...Ib
    ].concat((e = o == null ? void 0 : o.function) != null ? e : []).map((t) => {
      const r = t[0], n = t[1];
      return new r(n);
    });
    this._functionService.registerExecutors(...i);
  }
};
Fn = Yb([
  on(0, cr),
  on(1, qt),
  on(2, Ca),
  on(3, pc(Dc))
], Fn);
var Hb = Object.getOwnPropertyDescriptor, Qb = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? Hb(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, un = (o, i) => (e, t) => i(e, t, o);
let kn = class extends Ve {
  constructor(o, i, e) {
    super(), this._commandService = o, this._dependencyManagerService = i, this._featureCalculationManagerService = e, this._initialize();
  }
  _initialize() {
    this._commandExecutedListener(), this._featureCalculationManagerServiceListener();
  }
  _featureCalculationManagerServiceListener() {
    this.disposeWithMe(
      this._featureCalculationManagerService.onChanged$.subscribe((o) => {
        const { unitId: i, subUnitId: e, featureIds: t } = o;
        this._dependencyManagerService.removeFeatureFormulaDependency(i, e, t);
      })
    );
  }
  _commandExecutedListener() {
    this.disposeWithMe(
      this._commandService.onCommandExecuted((o) => {
        if (o.id === ya.id) {
          const i = o.params;
          if (i == null)
            return;
          const { featureIds: e, unitId: t, subUnitId: r } = i;
          this._dependencyManagerService.removeFeatureFormulaDependency(t, r, e);
        } else if (o.id === ba.id) {
          const i = o.params;
          if (i == null)
            return;
          const { featureId: e, calculationParam: t } = i, { unitId: r, subUnitId: n } = t;
          this._dependencyManagerService.removeFeatureFormulaDependency(r, n, [e]);
        } else if (o.id === pa.id) {
          const i = o.params;
          if (i == null)
            return;
          this._dependencyManagerService.removeOtherFormulaDependency(i.unitId, i.subUnitId, i.formulaIdList);
        } else if (o.id === Ra.id) {
          const i = o.params;
          if (i == null)
            return;
          const e = i.formulaMap, t = [];
          Object.keys(e).forEach((r) => {
            t.push(r);
          }), this._dependencyManagerService.removeOtherFormulaDependency(i.unitId, i.subUnitId, t);
        } else if (o.id === No.id) {
          const i = o.params.formulaData;
          Object.keys(i).forEach((e) => {
            const t = i[e];
            if (t !== void 0) {
              if (t === null) {
                this._dependencyManagerService.clearFormulaDependency(e);
                return;
              }
              Object.keys(t).forEach((r) => {
                const n = t[r];
                if (n !== void 0) {
                  if (n === null)
                    return this._dependencyManagerService.clearFormulaDependency(e, r), !0;
                  new Ce(n).forValue((s, a) => {
                    this._dependencyManagerService.removeFormulaDependency(e, r, s, a);
                  });
                }
              });
            }
          });
        } else o.id === Ea.id && this._handleSetDefinedName(o);
      })
    );
  }
  _handleSetDefinedName(o) {
    const i = o.params;
    if (i == null)
      return;
    const { unitId: e, name: t } = i;
    this._dependencyManagerService.removeFormulaDependencyByDefinedName(e, t);
  }
};
kn = Qb([
  un(0, cr),
  un(1, kr),
  un(1, rs),
  un(2, kr)
], kn);
var Gb = Object.getOwnPropertyDescriptor, Wb = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? Gb(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, ro = (o, i) => (e, t) => i(e, t, o);
let $n = class extends Ve {
  constructor(o, i) {
    super(), this._commandService = o, this._featureCalculationManagerService = i, this._initialize();
  }
  _initialize() {
    this._commandExecutedListener();
  }
  _commandExecutedListener() {
    this.disposeWithMe(
      this._commandService.onCommandExecuted((o) => {
        if (o.id === ba.id) {
          const i = o.params;
          if (i == null)
            return;
          const { featureId: e, calculationParam: t } = i, { unitId: r, subUnitId: n } = t;
          this._featureCalculationManagerService.register(r, n, e, t);
        } else if (o.id === ya.id) {
          const i = o.params;
          if (i == null)
            return;
          const { featureIds: e, unitId: t, subUnitId: r } = i;
          this._featureCalculationManagerService.remove(t, r, e);
        }
      })
    );
  }
};
$n = Wb([
  ro(0, cr),
  ro(1, kr)
], $n);
var qb = Object.getOwnPropertyDescriptor, Kb = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? qb(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, Ls = (o, i) => (e, t) => i(e, t, o);
let Yn = class extends Ve {
  constructor(o, i, e) {
    super(), this._commandService = o, this._otherFormulaManagerService = i, this._dependencyManagerService = e, this._initialize();
  }
  _initialize() {
    this._commandExecutedListener();
  }
  _commandExecutedListener() {
    this.disposeWithMe(
      this._commandService.onCommandExecuted((o) => {
        if (o.id === Ra.id) {
          const i = o.params;
          if (i == null)
            return;
          const e = { [i.unitId]: { [i.subUnitId]: i.formulaMap } };
          this._otherFormulaManagerService.batchRegister(e);
        } else if (o.id === pa.id) {
          const i = o.params;
          if (i == null)
            return;
          const e = {};
          i.formulaIdList.forEach((r) => e[r] = !0);
          const t = { [i.unitId]: { [i.subUnitId]: e } };
          this._otherFormulaManagerService.batchRemove(t);
        }
      })
    );
  }
};
Yn = Kb([
  Ls(0, cr),
  Ls(1, va),
  Ls(2, rs)
], Yn);
var Xb = Object.getOwnPropertyDescriptor, Zb = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? Xb(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, no = (o, i) => (e, t) => i(e, t, o);
let Hn = class extends Ve {
  constructor(o, i) {
    super(), this._commandService = o, this._superTableService = i, this._initialize();
  }
  _initialize() {
    this._commandExecutedListener();
  }
  _commandExecutedListener() {
    this.disposeWithMe(
      this._commandService.onCommandExecuted((o) => {
        if (o.id === Ea.id) {
          const i = o.params;
          if (i == null)
            return;
          const { unitId: e, tableName: t, reference: r } = i;
          this._superTableService.registerTable(e, t, r);
        } else if (o.id === bo.id) {
          const i = o.params;
          if (i == null)
            return;
          const { unitId: e, tableName: t } = i;
          this._superTableService.remove(e, t);
        } else if (o.id === Vo.id) {
          const i = o.params;
          if (i == null)
            return;
          const { tableOption: e, tableOptionType: t } = i;
          this._superTableService.registerTableOptionMap(e, t);
        }
      })
    );
  }
};
Hn = Zb([
  no(0, cr),
  no(1, sc)
], Hn);
class zb extends Ve {
  constructor() {
    super(...arguments);
    A(this, "_dirtyConversionMap", /* @__PURE__ */ new Map());
  }
  dispose() {
    this._dirtyConversionMap.clear();
  }
  remove(e) {
    this._dirtyConversionMap.delete(e);
  }
  get(e) {
    return this._dirtyConversionMap.get(e);
  }
  has(e) {
    return this._dirtyConversionMap.has(e);
  }
  register(e, t) {
    this._dirtyConversionMap.set(e, t);
  }
  getDirtyConversionMap() {
    return this._dirtyConversionMap;
  }
}
const Jb = ze(
  "univer.formula.active-dirty-manager.service"
);
var ey = Object.getOwnPropertyDescriptor, ty = (o, i, e, t) => {
  for (var r = t > 1 ? void 0 : t ? ey(i, e) : i, n = o.length - 1, s; n >= 0; n--)
    (s = o[n]) && (r = s(r) || r);
  return r;
}, so = (o, i) => (e, t) => i(e, t, o);
const ry = "UNIVER_ENGINE_FORMULA_PLUGIN";
var xs;
let ao = (xs = class extends Nc {
  constructor(o = Ri, i, e) {
    super(), this._config = o, this._injector = i, this._configService = e;
    const { ...t } = Vc(
      {},
      Ri,
      this._config
    );
    this._configService.setConfig(Oa, t);
  }
  onStarting() {
    this._initialize(), this._initializeWithOverride();
  }
  onReady() {
    var o;
    hs(this._injector, [
      [Fn],
      [Hn]
    ]), (o = this._config) != null && o.notExecuteFormula || hs(this._injector, [
      [Yn],
      [$n],
      [kn],
      [Un]
    ]);
  }
  onRendered() {
    var o;
    (o = this._config) != null && o.notExecuteFormula || hs(this._injector, [
      [ua],
      [na]
    ]);
  }
  _initialize() {
    const o = !this._config.notExecuteFormula, i = [
      // Services
      [qt, { useClass: mh }],
      [Sr, { useClass: Is }],
      [Jb, { useClass: zb }],
      [Yo, { useClass: Js }],
      [Mo, { useClass: pl }],
      [sc, { useClass: Tb }],
      [ic],
      // Models
      [br],
      // Engine
      [Na],
      //Controllers
      [Fn],
      [Hn],
      [ga]
    ];
    o && i.push(
      // Services
      [va, { useClass: qh }],
      [Lt, { useClass: ea }],
      [Qt, { useClass: $s }],
      [kr, { useClass: Wh }],
      //Controller
      [Un],
      [Yn],
      [kn],
      [$n],
      // Calculation engine
      [pr],
      [Fr],
      [yr],
      // AstNode factory
      [Ho],
      [Mn],
      [Dn],
      [Xo],
      [Ln],
      [Sn],
      [xn],
      [Pn],
      [jn],
      [Zo]
    ), i.forEach((e) => this._injector.add(e));
  }
  _initializeWithOverride() {
    var o;
    (o = this._config) != null && o.notExecuteFormula || [
      [ua, { useClass: ca }],
      [rs, { useClass: Gh }],
      [na, { useClass: sa }]
    ].forEach((e) => this._injector.add(e));
  }
}, A(xs, "pluginName", ry), xs);
ao = ty([
  so(1, ye(oo)),
  so(2, Ca)
], ao);
export {
  zb as ActiveDirtyManagerService,
  W as ArrayValueObject,
  Ho as AstRootNodeFactory,
  Fr as AstTreeBuilder,
  Jf as AsyncArrayObject,
  dy as AsyncCustomFunction,
  zf as AsyncObject,
  V as BaseFunction,
  Jn as BaseReferenceObject,
  Gt as BaseValueObject,
  ar as BooleanValue,
  T as BooleanValueObject,
  Un as CalculateController,
  ca as CalculateFormulaService,
  Xm as CustomFunction,
  am as DEFAULT_INTERVAL_COUNT,
  Is as DefinedNamesService,
  Qh as DependencyManagerBaseService,
  Gh as DependencyManagerService,
  hy as ENGINE_FORMULA_CYCLE_REFERENCE_COUNT,
  Oa as ENGINE_FORMULA_PLUGIN_CONFIG_KEY,
  lr as ERROR_TYPE_SET,
  m as ErrorType,
  g as ErrorValueObject,
  la as FUNCTION_NAMES_ARRAY,
  le as FUNCTION_NAMES_COMPATIBILITY,
  Km as FUNCTION_NAMES_CUBE,
  qe as FUNCTION_NAMES_DATABASE,
  be as FUNCTION_NAMES_DATE,
  Z as FUNCTION_NAMES_ENGINEERING,
  ee as FUNCTION_NAMES_FINANCIAL,
  Oe as FUNCTION_NAMES_INFORMATION,
  Le as FUNCTION_NAMES_LOGICAL,
  fe as FUNCTION_NAMES_LOOKUP,
  Y as FUNCTION_NAMES_MATH,
  B as FUNCTION_NAMES_STATISTICAL,
  se as FUNCTION_NAMES_TEXT,
  vb as FUNCTION_NAMES_UNIVER,
  ac as FUNCTION_NAMES_WEB,
  Wh as FeatureCalculationManagerService,
  $s as FormulaCurrentConfigService,
  br as FormulaDataModel,
  sa as FormulaDependencyGenerator,
  Ns as FormulaDependencyTree,
  ra as FormulaDependencyTreeType,
  ps as FormulaDependencyTreeVirtual,
  $e as FormulaExecuteStageType,
  mr as FormulaExecutedStateType,
  ea as FormulaRuntimeService,
  Mn as FunctionNodeFactory,
  mh as FunctionService,
  xc as FunctionType,
  ic as GlobalComputingStatusService,
  Js as HyperlinkEngineFormulaService,
  Jb as IActiveDirtyManagerService,
  ua as ICalculateFormulaService,
  Sr as IDefinedNamesService,
  rs as IDependencyManagerService,
  kr as IFeatureCalculationManagerService,
  Qt as IFormulaCurrentConfigService,
  na as IFormulaDependencyGenerator,
  Lt as IFormulaRuntimeService,
  qt as IFunctionService,
  Yo as IHyperlinkEngineFormulaService,
  va as IOtherFormulaManagerService,
  Mo as ISheetRowFilteredService,
  sc as ISuperTableService,
  pr as Interpreter,
  Dn as LambdaNodeFactory,
  Xo as LambdaParameterNodeFactory,
  Ua as LambdaValueObjectObject,
  yr as Lexer,
  ae as LexerNode,
  Na as LexerTreeBuilder,
  ue as NullValueObject,
  R as NumberValueObject,
  Ln as OperatorNodeFactory,
  qh as OtherFormulaManagerService,
  Sn as PrefixNodeFactory,
  On as RangeReferenceObject,
  xn as ReferenceNodeFactory,
  zc as RegisterFunctionMutation,
  bo as RemoveDefinedNameMutation,
  ya as RemoveFeatureCalculationMutation,
  pa as RemoveOtherFormulaMutation,
  hl as RemoveSuperTableMutation,
  Us as SetArrayFormulaDataMutation,
  Ea as SetDefinedNameMutation,
  fy as SetDefinedNameMutationFactory,
  ba as SetFeatureCalculationMutation,
  yn as SetFormulaCalculationNotificationMutation,
  po as SetFormulaCalculationResultMutation,
  yo as SetFormulaCalculationStartMutation,
  Ro as SetFormulaCalculationStopMutation,
  No as SetFormulaDataMutation,
  Ra as SetOtherFormulaMutation,
  fl as SetSuperTableMutation,
  Vo as SetSuperTableOptionMutation,
  pl as SheetRowFilteredService,
  U as StringValueObject,
  Pn as SuffixNodeFactory,
  Tb as SuperTableService,
  jn as UnionNodeFactory,
  ao as UniverFormulaEnginePlugin,
  Zo as ValueNodeFactory,
  Mt as ValueObjectFactory,
  x as compareToken,
  Xc as convertUnitDataToRuntime,
  cm as deserializeRangeForR1C1,
  Ur as deserializeRangeWithSheet,
  Er as deserializeRangeWithSheetWithCache,
  gy as extractFormulaError,
  mm as functionArray,
  Wm as functionCompatibility,
  qm as functionCube,
  cg as functionDatabase,
  Wg as functionDate,
  Z1 as functionEngineering,
  rd as functionFinancial,
  Od as functionInformation,
  Gd as functionLogical,
  SC as functionLookup,
  eA as functionMath,
  oA as functionMeta,
  wE as functionStatistical,
  jb as functionText,
  Ub as functionUniver,
  Ib as functionWeb,
  ji as generateAstNode,
  vi as generateExecuteAstNodeData,
  Xt as generateRandomDependencyTreeId,
  Ei as generateStringWithSequence,
  cy as getAbsoluteRefTypeWitString,
  fn as getAbsoluteRefTypeWithSingleString,
  _y as getObjectValue,
  ly as getRangeWithRefsString,
  my as handleNumfmtInCell,
  Aa as handleRefStringInfo,
  Uc as includeFormulaLexerToken,
  yi as initSheetFormulaData,
  ln as isFormulaLexerToken,
  Lc as isInDirtyRange,
  _o as isReferenceString,
  hn as isReferenceStringWithEffectiveColumn,
  sl as isReferenceStrings,
  uy as matchRefDrawToken,
  K as matchToken,
  oy as normalizeSheetName,
  X as operatorToken,
  Ye as sequenceNodeType,
  $t as serializeRange,
  vs as serializeRangeToRefString,
  tl as serializeRangeWithSheet,
  rl as serializeRangeWithSpreadsheet,
  gs as singleReferenceToGrid,
  Li as strip,
  Da as stripErrorMargin
};
